# 보류 — 빈 헬스장 배경 2장 (운영자 260725 업로드분)

인물이 없는 **공간 사진**이라 캐릭터 감정 버킷(main/<감정>/)에 넣을 물건이 아니다.
쓸 자리는 **장소 배경**(`viewer/assets/yeta_bg/var_*.webp` · 체육관 `gym` = 본의 '강철') 인데,
그 폴더는 `yeta_bg_var.py` 생성기의 이름공간이라 운영자 업로드를 그대로 섞으면
다음 생성 때 드리프트가 난다(⚙ 기계산출물 규약).

배선하려면 둘 중 하나 — **운영자 결정 대기**:
1. `Y_PLACE_POOL.gym`에 업로드 씬을 별도 접두(`up_`)로 추가 + 뷰어 URL 규약에 예외 1줄
2. 생성기 씬 목록에 `gym_a`/`gym_b`를 등재하고 이 파일을 그 산출물로 취급(다음 생성 때 덮임 감수)

현재 gym 장소 배경 = `persona_von`(인물 초상 유용) 이므로, 실제 공간 사진이 더 낫다는 판단이면 ①이 맞다.
