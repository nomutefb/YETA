# yeta 작업이력 원장 (append-only)

> **머리말**: 이 문서 = 작업 이력·결정 경위의 단일 원장(append-only · 기존 항목 수정 금지·추가만). 항목 양식 = {ID(KST)·의도·변경 전후·효과/리스크·검증·산출}. 다른 날 = 다른 날짜 헤더 블록. 개선 우선사항은 맨 아래 절에 누적. 정본 위치 지도 = CLAUDE.md §기록SSOT.

## 260704

### 01 (260704 KST) — 기틀 이식 1호: 마스터 라우터 병합
- **의도**: 운영자가 기틀 복제팩(노뮤트 로직 이식 정본)을 첨부 — 일하는 방식(라우팅·수정 모드·작업 4대 표준·검증 티어·기록 SSOT)을 yeta에 심기. 방식 = 운영자 선택 **병합**(기존 CLAUDE.md 내용 전부 보존 + 라우터 골격 씌움) · 검증 = **5인 교차검증**.
- **변경 전후**: 전 = CLAUDE.md에 절대명령#1+디자인 계약+앱 정본만(수정 모드 절차·검증 티어·원장 규약 부재). 후 = 절대명령#1 최상단 불변 → 라우터 0.~5.(표기 규약·절대명령 표 8행·라우팅·운영 태도·작업표준·선택질문·PR웹훅·모델·수정 모드·기틀 보호·기틀검증 5인·검증 10인 티어·분신술·표기표준 KST·기록SSOT 지도) → 앱 정본(§🎬~§🤖) 원문 보존. 옛 §✂️ 수정·검증·커밋 3행은 라우터 3.~4.에 흡수(외부 참조 0 실측). 신설 = `docs/작업이력.md`(이 원장)·`_versions/`(첫 백업 = 260704_2353_기틀이식_마스터라우터/CLAUDE.md).
- **병합 결정(팩 규약과의 편차·정직 기록)**: ① 절대명령 번호 = yeta 기존 체계 유지(#1 디자인·#2 정본인덱스 → 팩 절대명령은 #3~#8로 후속 배번). ② 날짜 스탬프 = 신규 라우터 본문만 0 — 기확정 앱 정본 절의 기존 경위 스탬프(260703·260704)는 소급 제거 안 함(경위 유실 방지 · 규약에 예외 명문화). ③ 낫표 = §🔒 기확정 문구(「계승」·「갱신」)만 존치(개정 = 기틀 절차 · 규약에 예외 명문화). ④ 게이트 = 기존 `shared/check_refs.py`(확장판) 유지 — 팩 스켈레톤으로 다운그레이드 안 함.
- **효과/리스크**: 효과 = 수정 모드 절차·반영 보장·검증 티어·기록 지도가 기계 규약으로 상시 로드. 리스크 = CLAUDE.md 길이 증가(세션 로드 토큰↑ — 런타임은 §🤖 --safe-mode라 무영향) · 라우터↔앱 절 중복 서술 드리프트 가능(정본 우선순위 절로 완화).
- **검증**: check_refs rc=0 + 표기 자가 스캔 + 5인 교차검증(READ-ONLY · 무손실/완전성/반입금지/기계정합/표기모순 5앵글). 1라운드 = 4 APPROVE + 1 REJECT{㉠ 괄호 위계 자기위반(0.2)c) ㉡ 기틀 검증 인원 모순(4.2 5인 vs 4.3 나) 10인) ㉢ 날짜 스탬프 예외 범위 누락(§🔒·서두 인트로 미커버)} → 메인이 3건 전부 수정(㉡ = 기틀은 5인 별도 트랙 명문화 · ㉢ = 가)안 예외 명문화 — 원문 스탬프 소급 제거는 무손실 계약 훼손이라 기각) + 검증2 관찰(보고서 read-back 행 선표기) 교정 → 재검증 APPROVE = 만장일치. 이후 main 머지 + `git show origin/main:CLAUDE.md` read-back 실측.
- **산출**: CLAUDE.md(병합본) · docs/작업이력.md(신설) · docs/reports/260704_기틀이식_마스터라우터.html · _versions/260704_2353_기틀이식_마스터라우터/.

## 개선 우선사항 (누적)
- ~~(전 항목 해소 — 1번 call.js 편입 = 157차 · 2번 go(-2)·하 2건 = 158차 완료)~~ **네비 경계면 3건(155차 적대감사 2인 발견 · 155차 수정과 별개 선존 결함 · 전부 심각도 중)**: ①call.js 벨(calldlg)·명함(yvcf) = navOpen 미등재+자체 popstate 리스너 → 하드웨어 뒤로 1회에 본체 종료가드와 이중 발화{벨 수신 중 뒤로 = 거절+종료팝업 동시 · 명함 열린 채 뒤로 = 명함+뒤 챗 동반 종료} — 해법 = call.js 오버레이를 navOpen 통합 스택에 편입(두 건 동시 해소) ②yExitYes `history.go(-2)` = [base,guard] 2칸 고정 가정 → 잔존 history 칸 누적 시 브라우저 탭에서 "예"가 조용히 실패(래치 폴백으로 안전 복구·PWA는 window.close 위임이라 무해) — 해법 = 이탈 델타를 잔존칸 내성 있게. 부수 하 2건: yNavName 죽은 셀렉터(`data-tab="chat"` 폐지 탭 잔존 = 무동작 죽은 코드) · 챗 ESC(데스크톱만) = cancel 핸들러 부재로 history 칸 고아화 1칸.
- CLAUDE.md §🗺의 `roster.json` 표기 = 실물은 `apps/yeta/characters/roster.json`(검증4 관찰 — 기확정 앱 절 선존 표기라 이식에서 비수정 · 다음 §🗺 손질 때 `characters/roster.json`으로 명기).
- check_refs 경로 게이트 커버리지 = `apps/·shared/·.claude/·_산출/` 접두사 + `md·py·sh·png` 확장자만 — docs/·viewer/·functions/·구성도/ 참조는 미검사 사각(검증4 관찰 · 현재 전부 실존 확인 완료 · 확장은 기틀 = 운영자 승인 후).
- CLAUDE.md 곳곳의 `§📐` 참조 = 대상 헤더 부재(이식 전부터 선존 dangling · 검증1 관찰 — §표기표준이 내용 일부 수용 · 별칭 1줄 추가는 후속 후보).

## 커스텀 도메인 5xx 에러 소실 수정 (5xx → 424 강등)
- 증상: op phone 실패 시 yeta.soong.kr 에서 생 "error code: 502"(존 에러 페이지)만 노출 — 게이트웨이 JSON 사유 소실. pages.dev 직행은 JSON 정상 = 존이 5xx 응답을 덮는 것(실측).
- 조치: functions/api/yeta.js json 헬퍼에서 status ≥500 → 424 강등(뷰어는 JSON 필드만 판독 = 무영향).
- 부수 확정: 실전화 실패 실사유 = "Free Vapi numbers do not support international calls."(무료 US번호 국제발신 불가) → 한국 발신은 Twilio 번호 임포트가 다음 경로.
- 검증: node --check · check_refs rc=0 · 머지 후 soong.kr 실측(JSON 사유 노출) · 백업 _versions/260705_0016_5xx강등/.

## 보이스톡(브라우저 실시간 통화) — 챗 모달 안 통화 UX (운영자 목업 이식)
- 챗 헤더 수화기 버튼(모듈 주입·.tool-x 계승·2탭 재확인=유료 가드) → 벨 연출 → 받기 → Vapi Web SDK 실시간 통화(타이머·말하는 중·끊기).
- assistant = roster phone 재사용 · 공개키 = op vapikey(Pages env VAPI_PUBLIC_KEY · Vapi Origins 제한 권장) · 전화망 불요 = iOS PWA 마이크 OK.
- phone 미배선 캐릭터 = op ring 폴백. SDK 는 받기 탭 후에만 동적 로드(평시 0바이트).
- 검증: node --check · check_refs rc=0 · Playwright(버튼 주입·armed·벨 모달 오픈) · 백업 _versions/260705_0206_전화버튼/.

## 절대명령 9 신설 — 프로세스 안내 표준
- 새 작업·설정 안내 = 실행 순서대로 · 플랫폼별 개별 URL · 입력값 복사 가능 코드블록(§운영 태도 f).
- 운영자 직접 지시("간단히 넣는 거니까 교차검증하지 말구") = 5인 교차검증 생략 근거 명기.

## 보이스톡 "연결 중…" 무한 hang 수정 (iOS 제스처 소실)
- 증상: 폰에서 받기 후 "연결 중…"에서 안 넘어감(모달·서버 정상 · origin 201 통과 실측).
- 원인: 받기 탭 → await 키fetch → await SDK import → vapi.start()(마이크) 순 = iOS Safari/PWA 는 await 뒤 사용자 제스처 만료 → getUserMedia 조용히 hang(에러도 없음).
- 조치: ① 벨 시점 vapiPreload(키+SDK 백그라운드) ② 받기 즉시 getUserMedia 를 제스처 안에서 먼저 잡음 ③ 단계별 상태(마이크 여는 중/연결 중) + 20초 guard(무한 hang 차단·실패 사유 노출).
- 검증: node --check · check_refs rc=0 · Playwright(벨→받기→마이크 단계 전이) · 백업 _versions/{ts}_보이스톡제스처.

## 헤더 통화 버튼 = 실전화(op phone)로 전환 (보이스톡 WebRTC 실패 실측 근거)
- 진단: op calllog 로 Vapi 통화 로그 실측 →
  · outboundPhoneCall = 정상(transcript "User:여보세요 / AI:왜 이래" · 클론 음색 재생·양방향).
  · webCall(보이스톡) = endedReason 'assistant-did-not-receive-customer-audio' 반복 = 모바일 브라우저 WebRTC 오디오 미전달(고질).
- 결정: 헤더 수화기 버튼 = phone 배선 캐릭터면 실전화 발신(내 폰 울림), 미배선이면 op ring 폴백. 보이스톡(open web:1)은 코드 잔존·기본 경로 제외.
- 검증: 운영자 재검증 없이 서버 통화 로그로 자가 확정 · node --check · check_refs rc=0 · 백업 _versions.

## 메인 반응형 9:16 프레임 + PWA 웹앱 + PIN 언락 전환 자연화 + 접속 락 모션 (모바일 반응형 UI 폴리시)
- **1) 9:16 폰 프레임(PC 늘어짐 해소)**: `@media (min-aspect-ratio:9/16)`에서 `.ymain`·`#yetadlg`를 `aspect-ratio:9/16`+중앙 레터박스(측면 딤·`--glass-line` 프레임선·`--shadow-card`). 세로 좁은 폰(뷰포트<9:16)은 풀블리드 유지 = 모바일 무영향. 비율=기하 raw 예외축(신규 토큰 0).
- **2) PWA 웹앱**: 이미 manifest.json+sw.js 등록 상태였음(안드로이드 설치 가능). iOS 설치형 지원 보강 = apple-mobile-web-app-capable·status-bar·title + mobile-web-app-capable·application-name 메타 추가. manifest에 scope·display_override·orientation:portrait 추가(설치 시 9:16 세로 고정).
- **3) PIN 언락 전환 자연화**: "잠금 해제 — 메뉴 활성" 텍스트 배너 제거 → PIN 통과 시 setUnlocked가 pin-mode를 해제(PIN 창 촤르륵 닫힘) + 네비를 `@keyframes ynavRise`로 아래서 슬라이드업(라이브 언락 1회만 reveal, 재방문 복원은 무애니). 언락 시 접속 버튼 퇴장.
- **4) 접속 락 열림 모션**: `#ymAccess` 누름 = `.unlocking` 클래스 → SVG 고리(`.ym-shackle`)가 힌지 축(`transform-origin:86% 100%`)으로 들려 열리고(translateY+rotate) 라임 글로우 점등, 420ms 후 pin-mode 진입.
- **디자인 계약**: 리터럴 hex·blur() 0 추가 · 색은 `var(--accent)`·`rgba(var(--accent-rgb),…)` · 프레임/그림자/반지름 전부 기존 토큰 계승 → design-tokens.lock·check_refs 무변경.
- **검증**: check_refs rc=0(디자인 게이트 포함 전 게이트 통과) · viewer JS 구문 OK · Playwright 실측 5샷{데스크톱 9:16 프레임·접속 락 열림 액티브·PIN 딤/키패드·언락 네비 슬라이드업(배너無)·모바일 풀블리드} · 백업 _versions/260705_0819_mobile-ui-polish/.

## 메인화면 hero 교체 + PIN 오버랩 재배치 + 접속 자물쇠 펄스 어포던스 + 원 발광 펄스 (운영자 260705 2차)
- **1) hero 이미지 교체**: 운영자 업로드 `main.png`(2160×3840=9:16) → ffmpeg webp 변환(1280×2276·q86·78KB) → `viewer/assets/brand/yeta-hero.webp` 대체(파일명 유지 = CSS 참조 무변경). 원본 png는 레포 루트 존치(운영자 커밋 dc271a5).
- **2) PIN 오버랩 재배치**: 풀스크린 딤+블러(`.pin-mode::before` blur --blur-m) 폐지 → 하단 그라데 스크림만(얼굴·헤드라인 노출·"이미지 안 덮고"). PIN 블록을 절대배치 `top:69%`(목→배)로 이미지 오버랩. 접속 후 아래→위(`translate +46px→0`)로 페이드인.
- **3) "접속" 텍스트 라벨 제거**: `.ym-access-lbl` 마크업·CSS 삭제. aria-label만 잔존(스크린리더).
- **4) 접속 자물쇠 펄스 어포던스**: 라벨 대신 `@keyframes ymLockBreathe`로 자물쇠가 커졌다 작아지며(scale .96↔1.08) 작아질 때 약하게 디밍(opacity .78↔1) 반복 = "눌러" 신호. 누르면(`.unlocking`) 펄스 정지 후 락 열림 모션.
- **5) 캐릭터 뒤 원 발광 펄스**: `.ym-glow` 오버레이(`radial-gradient circle closest-side at 48%/69%`·링 반경≈원 테두리 실측·`mix-blend-mode:screen`·중앙 투명=캐릭터 보존) opacity .12↔.58 호흡(`ymGlowPulse`). 포스터 baked 원에 오버랩돼 "발광했다 안 했다 반복".
- **디자인 계약**: blur 2개 제거(129→127 baseline ratchet) · 리터럴 hex·accent_raw 추가 0 · 색 전부 `rgba(var(--accent-rgb))`/토큰 · reduced-motion에 `.ym-access`·`.ym-glow` animation:none 가드.
- **검증**: check_refs rc=0 · Playwright 실측(모바일 발광 peak/trough·PIN 배 오버랩·rise 중간프레임·데스크톱 9:16 새 hero) · 백업 `_versions/260705_0830_pin-reposition/`.

## hero v2 교체(최신 업로드) + 디자인 소스 이미지 집결 + 구 메인이미지 삭제 (운영자 260705 3차)
- **1) hero 교체**: 운영자 최신 업로드 `main 복사.png`(커밋 1b22ee0 · 2160×3840 · 구판의 점선 화살표 목업 잔재 제거 클린판) → ffmpeg webp(1280×2276·q86·81KB) → `viewer/assets/brand/yeta-hero.webp` 덮어씀(파일명 유지 = CSS 무변경). 발광 원 중심·자물쇠·PIN 오버랩 위치 정합 Playwright 실측.
- **2) 소스 집결**: 루트에 흩어진 디자인 소스 4개 → `구성도/소스이미지/`로 git mv{main_v2.png(=main 복사.png 개명·공백 제거)·page_background.png(네온 계단·미사용)·Abstract green glassmorphism mockup template.jfif·Mobile app design.jfif}. 코드 참조 0 실측 = 이동 안전.
- **3) 구 메인이미지 삭제**: 루트 `main.png`(구판·화살표 포함) git rm — 운영자 지시. 히스토리(dc271a5)에 존치 = 복구 가능. index.html 주석 2곳 소스 경로 표기 갱신.
- **검증**: check_refs rc=0 · Playwright 실측 2샷(새 hero 메인·PIN 오버랩) · 백업 `_versions/260705_1027_hero-v2-정리/`(구 webp).

## 절대명령 10 신설 — 자가검증 우선(§운영 태도 g)) + op calllog 보안 수정
- 요지: 완료·원인 판정은 도구·로그·API 자가실측 1순위, 운영자에 반복 검증 떠넘김 금지. 못 보면 read-only 진단 훅 신설. 실패 계층부터 확정·대조로 변수격리·새 유료/합성 프로브 금지·데이터=원인/사람감각=UX.
- 강제(게이트 불가라 audit 3점 용접): §반영보장 b) `실측` 재정의+완료 3문 체크리스트(실패=`됐다` 거짓) · 절대명령 표 10행 · §검증 4.3 c) 상설 앵글.
- 보안: op calllog transcript·에러바디 반환 제거(무인증 공개 게이트웨이 대화 유출 차단). §🗺 op 목록 9→13 드리프트 동기화.
- 위치 결정: §검증(자가검증 동음이의 충돌) 대신 §운영 태도 g)(b) 확장). 정본인덱스 미등재(거긴 디자인 위치 전용).
- 자가검증 경로 = 5인 평의회 read-only 적대감사(위치·문구·부작용·사건충실성·강제성) 만장일치 + node --check + check_refs rc=0 | 운영자검증 불가피 사유 = 없음(전량 도구·에이전트).
- ⚠️ 잔여: 원장 과거 항목의 통화 대사 원문은 append-only라 소급 삭제 불가 → 향후 기재 규약(메타만)으로 봉인.

## 챗 = 풀스크린 글래스모피즘 재설계 (운영자 260705 · 레퍼런스 결)
- 요지: 운영자 레퍼런스(글래스 카드 위 배경 비침)+지시("팝업 아닌 화면 전체 · 배경은 준 이미지") → 챗 `#yetadlg`를 **팝업 카드(600px·불투명) → 풀스크린 글래스모피즘**으로 전환.
- 배경: `assets/brand/yeta-bg.webp`(라임 글래스 블록 9:16 — 운영자 사전 제공) full-bleed + `--bg-scrim` 하단 딤 + 균일 딤 rgba(0,0,0,.3)(밝은 라임 위 텍스트 가독). 레퍼런스의 보라 글래스를 브랜드 라임/블랙으로 치환.
- 글래스 표면(전부 계승 = 신규 토큰 0): 헤더 `.yeta-h`(--glass+--blur-l+safe-area 상단)·캐릭터 버블 `.yb.ai`(--glass-2+--blur-m·수신 콜 `.ycall-line` 확립 패턴 계승)·입력 바 `.yeta-in`·소개 카드 `.yintro`(--r-modal 글래스)·시스템 알약 `.yb.sys`·캐릭터 선택 리스트 `.yeta-list .ypick-row`(글래스 카드 스택). 내 버블 `.yb.me`=브랜드 라임 유지(대비).
- 구조: `#yetadlg` 100vw×100dvh(데스크톱 = `.ymain` 정합 9:16 프레임·radius 0). 팝업 카드 폐지. 닫기(X)=메인 복귀.
- 토큰 배선: `--bubble-ai`는 `@supports not (backdrop-filter)` 불투명 폴백으로 배선 유지(죽은 토큰 0). 페르소나 무대 `.ybg` = opacity 1→.32(라임 글래스 base 위 은은한 무드 베일 · `<<MOOD>>`·yeta_bg.py 기틀 보존).
- 정본 동기화: CII 행 42(#yetadlg 스펙)·40(버블 표면) 갱신. 값 SSOT `:root` 무변경(계승만) = design-tokens.lock 무변경.
- 검증: check_refs rc=0(토큰 락·디자인 게이트·거울 정합 전부 통과 · 신규 raw hex/blur/accent 0) · **Playwright 실측 2샷**(대화방 글래스 버블·캐릭터 리스트 글래스 카드 · 로컬 서빙+샘플 세션 주입 · 390×844@2x). 자가검증 경로 = 게이트+Playwright 렌더 실측 | 운영자검증 불가피 사유 = 없음(도구 렌더로 확인). ⚠️ 샌드박스는 외부 R2 얼굴 이미지 차단 → 아바타 빈 원(실서비스는 로드).
- 잔여/튜너블(운영자 판정 대기): ① 페르소나 무대 베일 .32(끄기=순수 라임 / 강화 택1) ② 균일 딤 .3(가독↔라임 선명도 트레이드오프).

## 챗 글래스 2차 다듬기 — 마이크 픽토+강조·캐릭터 초상 부각·폴리시 + 마이크 스타일 버그수정 (운영자 260705)
- 배경: 운영자 "느낌 OK" + ① 마이크 픽토그램만·강조색 ② 전반 유려하게 ③ nomute-editor `nomute-design` 스킬 생존 확인 ④ 각 화면 캐릭터 이미지 적용.
- **③ 스킬 생존 확인**: `nomute-editor/.claude/skills/nomute-design` 살아있음(UI 폴리시 디자인 폴리시 · yeta 계승 nomute 디자인시스템 기반). 이번 다듬기에 원칙 적용(stagger 등장·동심원·광학정렬·이미지 아웃라인·text-wrap).
- **① 마이크 = 픽토그램-온리 + 강조색 라임**(`.ycall-mic`): 글래스 원 도형 제거 → `border:none·background:none·color:var(--accent)`, svg 17→21(도형 없는 만큼 존재감). 녹음 = `--danger` 픽토 점멸(도형 없음).
- **🐛 근본 버그 수정(call.js)**: `.ycall-mic` 등 call.js CSS가 `ensureDom()`(전화 걸릴 때만 호출)에서만 주입돼, 마이크는 로드 시 주입되지만 **CSS 없이 브라우저 기본 버튼(흰 박스·검정 테두리)로 렌더**되던 선존 버그. `ensureCss()` 헬퍼 신설(멱등·id 가드) → `initInject`(로드)+`ensureDom`(콜) 공용. 이제 마이크가 로드 즉시 정상 스타일. Playwright computed 실측: bg transparent·border 0·color rgb(207,255,64)·svg 21px.
- **④ 캐릭터 초상 부각**: 얼굴 이미지(roster `avatar` = 웹툰풍 캐릭터 초상) — 인트로 카드 아바타 66→88px(히어로 초상·글래스 링·은은한 라임 후광), 캐릭터 선택 리스트 아바타 44→58px(글래스 카드 위 초상). 실이미지 Playwright 실측(R2 route 가로채기 = 샌드박스 외부차단 우회).
- **② 유려 폴리시**(nomute-design): 캐릭터 선택 stagger 등장(`ypickIn` · 기존 `ybIn` 커브 `cubic-bezier(.22,.61,.36,1)` 재사용 · nth-child 40ms 계단 · reduced-motion 무효화) · 인트로 제목 `text-wrap:balance`·본문 `pretty`+`--fg-2`(가독) · 아바타 이미지 아웃라인(라임 테).
- 계승: 값 SSOT `:root` 무변경(전부 토큰) · design-tokens.lock 무변경 · raw accent 0 유지(`var(--accent)`). call.js는 디자인 게이트 대상 아님(.js)이나 토큰만 사용.
- 정본 동기화: CII 행 46(마이크) 갱신. 검증: check_refs rc=0 · node --check call.js OK · Playwright 실측(대화방·리스트 2샷+마이크 computed).
- 잔여: 리스트 뷰에서 직전 페르소나 bg 베일(32%)이 남아 보임(첫 진입=YCUR null이라 순수 라임 · 재방문 edge) — 필요 시 리스트에서 페르소나 bg off 처리.

## 챗 입력·헤더 미니멀 재편 + PIN 매번+자동 + nomute-design 스킬 이식·고정 (운영자 260705 3차)
- **PIN(운영자 답변 "진입할 때마다 뜨게")**: 언락 기억(localStorage `yeta_pin_ok`) 제거 = 매번 PIN 요구(운영자 검증도 매번 가능·시크릿창 불필요) + 진입 즉시 pin-mode 자동(자물쇠 탭 없이 키패드 바로). 세션 내 언락은 in-memory `unlocked` 유지(리로드=다시 PIN). 실패/잠금 카운터는 유지. Playwright 실측: 첫 진입 pin-mode=true·키패드 opacity 1.
- **챗 "따로 놀아" 버그 수정**: wide(데스크톱/태블릿·>9:16) 화면에서 `#yetadlg`가 base `margin:0`(좌상단)이라 `.ymain`(margin:auto 중앙)과 어긋나던 것 → `margin:auto; inset:0` 추가. 실측 dlg.left 0→319 = `.ymain`과 완전 일치(중앙 9:16 프레임).
- **입력행 재편(운영자 #1~6·9·13)**: 전송 `.yeta-send`=원형 도형 제거→픽토그램-온리 강조색 라임(#1) · 마이크 `.ycall-mic`=강조색 뺌→무채 `--fg-2`(#9) · 둘 다 svg 22 동일크기(#5) · 입력칸=더 투명한 글래스(`--glass`+`--blur-m` #2) · `.yeta-in`=글래스 바/상단선 레이어 제거·투명(#3)·`align-items:center` 중앙정렬(#4·#6) · +더보기 `.yeta-plus` 전송 좌측 신설(#13·향후 기능 진입점·무채 픽토·현재 inert). 레이아웃 = [마이크][입력][+][전송].
- **헤더 재편(#7·8·10·12)**: 글래스 바 레이어 제거→투명(#7) · 주사위 `#yetaDice` 제거(#8·yDraw 보존) · `.yeta-who` 버튼→표시용 div·셰브론/이름탭 전환 제거(#12 "다른 인물 개입 불가") · X 유지(#10). 헤더 = 프사+이름+태그(소개).
- **소개 카드 제거(#11)**: yRender에서 `.yintro` 히어로 카드 스킵(인물 정보는 헤더에만).
- **배경 30% 확대(#14)**: `#yetadlg` 배경 이미지 `background-size` cover→130%(가운데 기준) · 딤/스크림은 cover 유지.
- **nomute-design 스킬 이식·고정(운영자 "매번 클론 못하니 레포에 고정")**: `nomute-editor/.claude/skills/nomute-design` → `.claude/skills/yeta-design/SKILL.md` 완전 이식·YETA화(토큰 `--blur-s/xl`→`m/l`·`--press-pico/xs`→`s/m/l`·컴포넌트 `.sbtn`→`.tool-x`/`.yeta-send`·경로 §디자인→§🎨·커브 cardIn→`ybIn`/`ypickIn`). 16원칙+흔한실수표+체크리스트 누락없이. CLAUDE.md §🎨 8항에 참조 기록(UI/UX 작업 시 자동 참조).
- 계승: 값 SSOT `:root`·`design-tokens.lock` 무변경(전부 토큰) · raw accent 0 유지. 검증: check_refs rc=0 · node --check call.js OK · Playwright 실측(챗·PIN·중앙정렬 지오메트리).
- 잔여/판단대기: 헤더 "새 대화"(reset) 존치(캐릭터 재선택 경로 필요) · + 버튼 현재 inert(기능 미배선) · 소개 desc는 헤더 미표시(태그만).

## 챗 4차 — 새 대화 제거·전화 픽토-온리·KST 시각 표기 + 픽토 모션 시안 12종 (운영자 260705)
- **"새 대화"(reset) 제거**: 마크업·CSS(.yh-reset)·JS(리스너·yResetArmClear·_yResetArm/_yResetT) 전부 정리(잔존 참조 0). op reset 백엔드 보존(향후 진입점 재배선 대비). 2탭 armed 색 패턴은 #yetaCallBtn.armed가 승계.
- **전화 버튼 = 픽토그램-온리**: `.tool-x` 글래스 박스 제거 → 무채 `--fg-2` 픽토(svg 19·히트 `--btn`)·armed=`--arm` 유지. 🐛 부수 발견·수정: showModal() 자동 포커스가 첫 버튼(전화)에 UA 흰 사각 아웃라인을 그리던 것 — openYeta에 blur 1줄 + `:focus-visible` 라임 링(.tool-x 계승 · 키보드 접근성 유지). 실측: className ''·border 0·bg transparent.
- **시각 표기(운영자 "4s 대신 KST 오전 11:00 · 버블 밖 · 내 것 좌하단")**: `.yb-cap` 소요초 캡션 폐지 → `yTime(ts)` KST 오전/오후 h:mm(§표기표준 ts+9h→getUTC*). 캐릭터 = `.ybrow`(버블+time · 우하단) · 내 것 = `.ymrow.me`([time][버블] · 좌하단 · 카톡 결). 낙관 전송 노드 동일 구조 · cont 마진/촤르륵 transform-origin 셀렉터 래퍼로 이동. 턴 ts = 전 경로 확인(op send·yeta_chat.sh·sys 전부 Date.now() 박음).
- **픽토 액티브 모션 시안 12종**: `docs/reports/260705_픽토그램_액티브모션_시안.html` — 마이크·+·전송·전화 × 3안(클릭 재생·실CSS·reduced-motion 처리·커브는 본 적용 시 --ease/press 토큰 계승 명기). 운영자 픽 대기 — 본 코드 미적용.
- 검증: check_refs rc=0 · node --check call.js OK · Playwright 실측(전화 픽토 computed·시각 3종 표기·새대화 부재). 자가검증 경로 = 게이트+렌더 실측 | 운영자검증 불가피 사유 = 모션 취향 픽만(시안 HTML 제공).

## 픽토그램 카탈로그 — 노뮤트 수확 94종 (운영자 260705 "노뮤트 픽토 최대한 가져와")
- `docs/reports/260705_픽토그램_카탈로그.html`: nomute-editor+yeta 전 표면(viewer html/js·구성도) 스캔 → 표지판 결(라인 SVG·스트로크 2.0·currentColor·viewBox 24) 필터 → path 기하 해시 중복 제거 = **고유 94종**. nomute 정본 가이드 `구성도/픽토그램.html`(코어 10·메뉴 5) 포함.
- 카탈로그 UX: 용도 자리 큐레이션(전송/마이크/전화/+ · 현재 것 점선 배지) + 전체 그리드 · 기본 무채→탭/호버 강조색 라임(표지판+강조 시연) · 탭=선택 담기(하단 바 · 그대로 복사 답장). 🐛 수확 시 call.js `d="${PHONE}"` 템플릿 변수 그대로 딸려온 것 → 상수 값 치환(렌더 실측 재검증 · 타일 109 · svg 누락 0).
- 운영자 픽 대기(픽 확정 시 yeta nm-svg.js에 상수 편입 후 배선). 검증 = Playwright 렌더 실측 | 운영자검증 불가피 사유 = 취향 픽만.

## 5차 — 캐릭터 탭(메뉴2)·알약 네비(무채)·챗 복귀 플로우·X 픽토 통일 (운영자 260705)
- **캐릭터 탭(메뉴2) 신설**: `.ychar` 갤러리 — 무음동 10인 전원, OpenAI 얼굴(roster `avatar`) 대형 카드 2열(글래스 카드·ypickIn stagger·프리미엄 `보이스` 배지). 카드 탭 = `openYeta(charId)` 직접 진입(`yEnterChat` · R2 미설정 = 소개 폴백). 로드 = `yApi('chars')` 재사용.
- **챗 복귀 플로우**(운영자 "채팅창은 클릭하면 이동하는 화면 — 뒤로가면 메뉴2"): `yetadlg` close 핸들러에서 unlocked면 `yTab('char')` = X·폰 뒤로가기 모두 캐릭터 탭 복귀. 탭 상태 SSOT = `yTab()`(main/char 패널·`.on` 동기).
- **네비 = 플로팅 알약**(운영자 레퍼런스): 하단 full-width 바 폐지 → 중앙 알약(`--r-pill`·`--glass` 고투명·`--blur-l`·글래스 엣지). **전부 무채** — 활성 탭 라임 폐지 → 흰(`--fg`)·비활성 `--mut`(운영자 "흰색 중심 무채·투명도 아주 높게"). ynavRise 슬라이드업 translateX 합성 재정의.
- **X 닫기 = 픽토그램-온리**(운영자 "전경색 칠 필요없다"): `#yetadlg .tool-x` 스코프 오버라이드(배경·보더 제거·무채 `--fg-2`·svg 19) — 수화기·하단 픽토와 통일. 회전 모션·포커스 링 = `.tool-x` 계승 유지(전역 tool-x 불변).
- 운영자 소스 이미지 6종 전달(구성도/소스이미지 4 + brand webp 2 · 경로 안내).
- 검증: check_refs rc=0 · Playwright 전체 플로우 실측 — 캐릭터 탭 렌더(얼굴 route 주입) → 무디 카드 탭 → 챗 진입(open·view=chat·who=mudi) → X 닫기 → **메뉴2 복귀(tabChar=true·navOn=char)**. 자가검증 경로 = 게이트+플로우 실측 | 운영자검증 불가피 사유 = 없음.
- 잔여: 픽토그램 픽·모션 픽 대기(카탈로그·시안 전달됨) · 설정 탭 = 준비중 유지.

## 6차 — 메뉴 재편(캐릭터·대화목록·지금)·챗 알약 상하단·좌 레일·읽음배지·글래스 튜너 (운영자 260705)
- **메뉴 재편(운영자 정정 반영)**: 1=캐릭터(언락 직행·메인 포스터 화면 폐지) · 2=대화 목록(신설) · 3=지금 대화(임시·2와 통합 예정) · 4=설정(준비중). main 탭·lockdot 제거(네비=언락 후만 표시). 언락(setUnlocked) = 즉시 yTab('char').
- **대화 목록(메뉴2·카톡/레퍼런스 결)**: `.ylist-row` 글래스 행 = 아바타+이름+미리보기(지문 제거 스니펫)+KST 시간+**라임 안읽음 배지**. 읽음 SSOT = localStorage `yeta_read_n`(yRender가 turns.length로 갱신 = 챗 열람 시 소진). 단일 세션 = 1행. 행 탭 = 이어보기.
- **챗 상하단 = 플로팅 알약**(운영자 "네비처럼 감싸서 띄워"): 헤더 = [←뒤로][상대 프사·이름·태그 **전폭**][수화기] · 입력 = [마이크][입력][+][전송] 캡슐(포커스=테두리 라임 은은). X 폐지 → ← 뒤로가기 = close → **메뉴2(대화 목록) 복귀**. 로그 상하 패딩 = 알약 클리어런스.
- **좌 슬림 캐릭터 레일**(운영자 맥 메신저 레퍼런스 "왔다갔다·모바일이니 짧게"): `.yside` 104px — 아바타+이름+최근대화 스니펫(현 상대=마지막 턴 ~7자·나머지=태그라인)+… · 현 상대 = 라임 링 · 탭 = draw 전환(맥락 유지). 챗뷰에서만 표시. 🐛 `_yView` 초기값 'chat'이라 목록 직행 시 yView 스킵돼 레일 미표시 → yRender에서 챗뷰면 표시 보장으로 수정(플로우 실측 재검증).
- **네비 여백 확대**(운영자 "너무 좁아"): 알약 padding 5×8→6×14 · 버튼 7×13→7×16 · gap 2→8.
- **글래스 튜너 신설** `docs/reports/260705_글래스튜너.html`(운영자 "제안 말고 내가 값 조정해서 주게"): 실물 폰 미리보기(배경·알약·버블 임베드) + 슬라이더 9축{배경 딤·확대·씬 베일·글래스 알파·엣지·표면 blur·버블 blur·버블 알파·강조 강도} 실시간 반영 + 값 JSON 복사 버튼. 기본값 = 현행 라이브.
- 검증: check_refs rc=0 · Playwright 플로우 실측{언락→캐릭터탭 → 대화목록(1행·배지2·오후 6:00) → 행 탭→챗(알약·레일 10행·cur=무디·읽음 1→3) → ←뒤로→대화목록 복귀(navOn=list·배지 소멸)} · API 목킹 = 실경로(yLoad→get) 그대로. 튜너 슬라이더 반영 실측.
- 잔여: 픽토·모션 픽 대기 · 설정 탭 · 메뉴3↔2 통합 로직(운영자 후속).

## 7차 — 앱 기본 배경 = 계단(라임 글래스) 통일 (운영자 260705)
- 운영자 "전반적으로 기본 배경은 계단 모양(yeta-bg.webp)으로 통일(일단은)" — 대화 목록 뒤로 메인 포스터가 비치던 것 교정.
- `.ymain.unlocked` 배경 오버라이드 = `yeta-bg.webp`(계단) — 언락 후 전 탭(캐릭터·대화목록) 통일. 포스터(yeta-hero)는 PIN 잠금 화면 전용으로 존치(로그인 정체성). 챗은 기존부터 계단.
- 검증: check_refs rc=0 · Playwright 실측(lockedIsPoster=true · unlockedIsStairs=true · 대화목록 스샷).

## 8차 — 노뮤트 픽토그램 24종 yeta 아이콘 SSOT 편입 + 설정 기어 교체 (운영자 260705 "일단 가져와")
- **편입**: nomute-editor 전 표면(nm-svg.js·index·thumb·ly·k·track) 명명 상수 중 yeta 미보유·currentColor(표지판 결)만 → `viewer/nm-svg.js` "노뮤트 수확 라이브러리" 섹션 24종{ARROW_R·BELL·BLOG·CAFE·CHEVRON·CHEV_L/R·EDIT·EYE·FIRE·G·LAYERS·LINK·PHOTO·REFRESH·RETRY·ROCKET·SEARCH·SEND·SPIN·STAR·THUMBUP/DOWN·TRASH}. 아이콘 SSOT 12→36종·게이트 통과(인라인 재선언 0). 사용은 선별(운영자 "쓰든지 말든지") — 색은 쓰는 쪽이 입힘(강조=CTA만·나머지 무채 = §🎨).
- **재량 적용(운영자 위임 "보고 디자인해서 적용")**: 네비 설정 탭 기어 = 옛 조각 패스(circle+토막 2개 → 'о.'처럼 깨져 보임·운영자 스샷 실측) → 표준 풀 기어 패스(스트로크 2·라운드·모노라인) 교체. 그 외 픽토(전송 라임·마이크·전화·+·뒤로)는 이미 결 통일 = 유지.
- 검증: node --check nm-svg.js OK · check_refs rc=0(아이콘 SSOT 36 단일정본).

## 9차 — 앱 아이콘(소년 클로즈업)·앱 제목 "예타 : 가상현실 대화 섹션" (운영자 260705)
- 운영자 첨부(소년 흉상·라임 바이저·라임 원) 반영 — 첨부와 동일 결의 포스터 원본 `구성도/소스이미지/main_v2.png`에서 타이포 아래 얼굴 클로즈업 크롭(crop 1500² @195,1120 · 3안 비교 후 C 채택 = 머리 온전·흉상 균형) → `yeta-icon-192/512` 재생성 · maskable = 80% 축소+#121212 패딩(안전지대).
- 제목: `<title>`·manifest name = "예타 : 가상현실 대화 섹션" · short_name/apple-title/application-name = "예타"(홈스크린 짧은 이름).
- 검증: check_refs rc=0 · 크롭 3안 렌더 비교 실측. ⚠️ 설치형 PWA 아이콘은 재설치/홈스크린 재추가 시 갱신(OS 캐시).

## 9-보정 — 앱 아이콘 소년 중앙정렬(지퍼 라인 = 프레임 세로 중앙) (운영자 260705)
- 운영자 "소년이 중심부로 — 지퍼 라인을 4분할 가운데 라인에 중앙정렬" — 크롭 x 195→245(중심 945→995). 가이드선(drawbox) 오버레이 3+1안 실측으로 지퍼·입 중심 = 프레임 중앙 확정. 아이콘 3종 재생성.
- 경위: 첫 커밋이 브랜치 리셋 누락으로 #29와 바이너리 충돌(405) → main 기준 재구성 후 재커밋.

## 9-보정2 — 아이콘 소년 하향(머리 여유·입 = 세로 중앙) (운영자 260705)
- 운영자 "프로필을 좀 아래로 — 머리 나오고 입이 중앙부로". 크롭을 위로 못 올림(타이포 하단 y≈1140 실측·band 컷) → 상단 180px 배경톤(#060606) 패딩 합성으로 소년 하향. 십자 가이드 실측 = 지퍼 세로중앙 유지 + 입 가로중앙 안착. 아이콘 3종 재생성.

## 10차 — 하단 네비바 활성 탭 = 라임 확장 필 애니메이션 (운영자 260705 GIF 레퍼런스)
- 운영자 첨부(활성 탭 = 색 필에 아이콘+라벨, 비활성 = 아이콘만·탭 전환 시 필 확장) 이식 — `.ynav` 세로 스택(상시 라벨) → 가로 필: 활성 = `--accent` 필+`--bg` 글자(안읽음 배지 결 계승·기존 "활성 무채 흰" 대체), 라벨 `<span>` `max-width` 슬라이드 확장(.34s `--ease`) + 등장 라벨 .1s 지연 페이드(스태거)·퇴장은 라벨 먼저. 신규 토큰 0(전부 계승) · 히트 40px 유지 · reduced-motion 가드에 span 추가.
- 전/후·리스크·데모 = `docs/reports/260705_네비바_확장필.html`(라이브 데모 포함).
- 검증: 자가검증 경로=check_refs rc=0(raw 강조색 0·토큰 락 정합·JS 구문 OK) + 머지 후 `git show origin/main:viewer/index.html` read-back.

## 11차 — 네비 초투명 라임 글래스 필(A안) + 메뉴3 라벨 = 상대 이름 + 가로 확대 (운영자 260705)
- **A안 승인 이식**: `.ynav` 표면 = `color-mix(in srgb, var(--glass) 50%, transparent)`(--glass 계승 절반 · 미지원 브라우저 폴백 = --glass 그대로) · saturate 1.3. 활성 필 = 솔리드 라임 → **라임 글래스**(`rgba(var(--accent-rgb),.16)` 몸체 + `.38` 엣지 + 은은 글로우[box-shadow 무토큰 축]) · 글자 `--accent`.
- **가로 확대**("좀 더 넓어도 됨"): 컨테이너 좌우 +10px · 탭 간격 4→8px · 탭 좌우 13→17px.
- **메뉴3 라벨 = 지금 대화 상대 이름**: `yNavName()` 헬퍼 — `yApply`(페르소나 적용 깔때기)·`yListRender`(세션 로드)에서 동기화 · 상대 미정 = '지금' 폴백.
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 `git show origin/main` read-back. 시안 = 채팅 전달 navglass.html(A/B/현행 비교).

## 9-보정3 — 아이콘 머리 완전 온전(타이포·장식 클린 커버) (운영자 260705 "머리카락 왜 잘렸어")
- 원인: 포스터 타이포·장식이 머리 꼭대기와 겹침(글자 하단 y≈1140·머리 y≈1100) → 이전 크롭이 머리 상단을 잘랐음. 클린 컷 원본은 레포·업로드 전수 수색 부재.
- 해법: 크롭 y=920(머리 전부+여백) + 머리 실루엣 피한 배경 박스 4개(0x060606)로 타이포·장식만 커버(geq 밝기 필터 = 잔상 남아 폐기 · 컬 우단 791 vs 박스 800 실측). 입=세로 51% 중앙·지퍼=가로 중앙 유지.
- 경위: 머지 시 main 병행 이동(#32 네비 애니·운영자 직접 + 후속)으로 405 충돌 2회 → 최신 main 기준 재구성.

## 12차 — YETA 워드마크 로고 라이브 이식 (운영자 260705 확정: 오프셋×전뇌 반반)
- **레터링 = 메인 포스터(main_v2.png) 원본 추출**: 흰 YETA 4글자 알파 마스크(머리카락 가림 = E 상하미러·T 기하 재구성·잔재 = 연결성분 제거) → `viewer/assets/brand/yeta-wm-{y,e,t,a}.png`(4장 ~19KB).
- **확정 조합 박제**(플레이그라운드 v2 운영자 픽): 판 = 코발트+라임 반전(screen) · 그림자 = 코발트 글로우 · 몸체 = 글래스 80% 보간 그라디언트 · 모션 6종 전부(슬라이스 글리치·글린트·VHS 지터·스캔롤·라임 펄스·플리커) · 자간 -22% 체감·엇갈림 배치 · `--w:min(360px,76%)`.
- 배치 = 캐릭터 탭(메뉴1) 상단 타이틀. 구현 = `.ywm` 마스크 겹층 6(JS 조립 `#ywm`) · 색 전부 토큰(`--accent-rgb`/`--cobalt-rgb`) · 신규 토큰 0 · reduced-motion 전 애니 정지 · CII 표 행 등재.
- 검증: 자가검증 경로=check_refs rc=0 + 헤드리스 HTTP 렌더 실측(스팬 24 조립·시각 확인 — file://는 마스크 CORS 차단이라 http 서버로 실측) + 머지 후 `git show origin/main` read-back.

## 13차 — 로고 재배치: 좌상단 상시 로고 + 우상단 개인 프로필 (운영자 260705)
- 운영자 "계단 배경 타이틀 말고 좌상단에 항상 박히는 로고 · 우상단 프로필" — `.ywm`을 캐릭터 탭 본문에서 `.ymain` 직속 좌상단 고정(104px · 언락 후 표시 · 확정 모션 6종 유지 · reveal = 위에서 스르륵+플리커 병행)으로 이동. `.yprof` 신설(우상단 원형 글래스 · `--btn`·`--press-s`·픽토 폴백 — 개인 프로필 이미지는 background-image 주입 축 후속 · 탭 = 준비중 토스트).
- 캐릭터·대화목록 패널 상단 패딩 18→60px(로고 클리어런스). CII `.ywm` 행은 배치 열만 갱신 대상(다음 정비 시).
- 검증: 자가검증 경로=check_refs rc=0 + 헤드리스 HTTP 렌더 실측 + 머지 후 `git show origin/main` read-back.

## 14차 — 상단바 보정: 프로필 = 지금 캐릭터 얼굴 · 로고 축소 · 헤딩/실패 문구 제거 (운영자 260705)
- `.yprof` = 지금 대화 상대 얼굴 주입(`yProfImg` — yApply·yListRender·캐릭터 탭 첫 진입 op get 비차단 동기화 · 라임 테 = .yava.has-img 결 · 개인 이미지 축은 후속 유지).
- 로고 `--w` 104→86px · 패널 클리어런스 60→52px 정합.
- 캐릭터 탭 헤딩("무음동 사람들/탭하면 대화가 시작돼") 제거 · 갤러리 로드 실패 문구 제거(조용히 빈 화면).
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 read-back.

## 10차-B — 소년 레이어 분리·플레이그라운드 (운영자 260705 "레이어 분리해 다양하게 애니")
- **레이어 분리**: pillow flood-fill+색·기하 필터 4회 반복 실측{배경 flood → 라임 원(y≥730 소프트 라임) → 밴드 돌출 사각 → 원 테두리 잔선(실측 R723)} → 투명 컷아웃 `viewer/assets/brand/yeta-boy-cut.webp`(1080²·55KB). 원·밴드 기하 실측: 원 중심(49.4%,110.5%)·테두리 67%W · 밴드 -5.5°·상변 33.5%·두께 14%.
- **플레이그라운드** `docs/reports/260705_소년_레이어_플레이그라운드.html`(워드마크 결): 레이어 4장{배경·라임 원[CSS 정원]·소년 컷아웃·밴드[CSS]} + 효과 8종 중첩{글리치 RGB·VHS·스캔롤·홀로·글로우 펄스·패럴랙스·밴드 슬라이드·부팅} + 슬라이더 6축 + 값 복사. 전부 CSS = 메인 직이식.
- 검증: Playwright 렌더 JS에러 0·원 기하 정합. 경위: main 병행 이동(#38 등)으로 405 → 재구성. 운영자 조합 픽 대기.

## 11차-B — 잠금 화면 = 살아있는 레이어 스택(운영자 픽 이식) + 좌우 잘림 근본 해결 (운영자 260705)
- **운영자 픽 이식**(플레이그라운드 값): 효과 4종 중첩{글리치 RGB·VHS 지터·홀로 플리커·글로우 펄스} · glowop .85 · glowsize .85 · bandop .56 · bandrot -6° · amp 1.4. 잠금 화면 = 정적 포스터(hero.webp) 폐지 → `.ym-stage` 레이어 스택{CSS 라임 원(.ymc)·소년 컷아웃(.ymb×3 — 본체+글리치 클론 2)·CSS 밴드(.ym-band)·스캔라인(::after)}. 픽 값 = 스테이지 로컬 변수(--st-* · :root 아님 = 토큰 락 무관 · 키프레임 안무/기하 = 절대1 예외축).
- **"좌우 머리 잘림" 실측·2건 해소**(운영자 "계속 잘려"):
  ① 잠금 스테이지 초안(88dvh)이 좌우 크롭 설계였음 → **폭 100%(소년 온전·크롭 0)** 하단 정렬로 교정.
  ② **maskable 아이콘 실측** — 소년 반폭 205px > 원형 마스크 엄격 안전 반지름 166px = 원형 런처에서 컬 잘림 확정 → 재생성{배경 = 캔버스에 라임 원 직접 드로잉(잘려도 되는 배경) + 소년 contain 332h 중앙} → 반폭 101px ✓. any(192/512)는 원본 정사각 그대로(잘림 무).
- reduced-motion = 스택 애니 전부 무효+글리치 클론 숨김. 언락/탭 화면에서 스택 숨김. Playwright 실측(스택·애니 3종 가동·JS에러 0).
- ⚠️ 폰 홈스크린에 남는 옛 아이콘 = OS 캐시 — 재설치(홈스크린 삭제 후 재추가) 필요.

## 15차 — 배경 = CSS 3D 계단 라이브 연출(운영자 260705 확정) · webp 정지사진 대체
- 확정 조합 박제: 플로우 웨이브 1.3x(0.885s expoOut) · 스태거 .08s · 길이 210·두께 40·깊이 74 · 원근 740 · 변주 16%(결정적 난수 = 재쌓기에도 동일 실루엣) · 스텝 14 · 꺾임 4 · **완성 후 10초 뒤 재쌓기 루프**(탭 숨김 = visibilitychange 일시정지 · reduced-motion = 1회 정적).
- 구현: `.ystair`(z0 · ::before 딤 아래) — 지그재그 스위치백 · 투명 라임 유리 육면체 6면(`--accent-rgb` 토큰) · 폰 폭 축척(720 기준 scale). `.ymain.unlocked` 배경 webp → 다크+라임 발광(계단이 배경 본체). 언락 시 `yStair()` 시작. ⚠ preserve-3d 그룹에 opacity/will-change:opacity = 3D 평면화 → 페이드는 말단 면에만(시안 삽질 박제).
- 탭 딤(.66~.78) 아래 체감 보정 = 엣지·글로우 알파 상향.
- 검증: 자가검증 경로=check_refs rc=0 + 헤드리스 HTTP 렌더 실측(14스텝 조립·시각) + 머지 후 read-back.

## 16차 — 챗 하단 = 안2 도킹 네비 확정 + 마이크 폐지 (운영자 260705 "애초에 안2로")
- 챗 입력행 좌측에 컴팩트 도킹 네비 `.ydock` = [캐릭터][현재챗 활성 라임 필] 아이콘-온리(네비 활성 필 결 계승) — 탭 = 챗 닫고 그 메뉴로(`_yCloseTo` → close 핸들러 분기 · 기본 = 대화 목록). 타이핑(focus-within) = 도킹 접혀 입력 전폭.
- **마이크 폐지**: `#yetaMic { display:none !important }` — 그 자리 채팅 입력으로. call.js PTT 코드는 존치(복구 = 이 줄 삭제). v3/v4 변형(좌 이동 1개·양극단·라벨) 폐기.
- 검증: 자가검증 경로=check_refs rc=0 + 헤드리스 HTTP 실측(도킹 표시·focus 접힘·마이크 미표시) + 머지 후 read-back.

## 17차 — 메인 진입 = 기본 메인 포스터(main_v2) 좌표 복원 + 로고 스트라이크 진입 시퀀스 (운영자 260706)
- **왜**: 진입 메뉴 하단 몰림 + 머리 좌우 잘림(밴드 앞 튜프트 소실) → "레포의 기본 메인 이미지(main_v2) 그 위치에 현재 효과 그대로" + 진입 플로우 재정의(운영자 260706).
- **에셋**: 신규 `viewer/assets/brand/yeta-main-boy.webp`(1080×1920 RGBA·70KB) — 클로즈업 원본 머리(튜프트+밴드 온-헤드 = 포스터 층위 박제 · "기존 효과 캐릭터한정") + main_v2 전신을 FFT 정렬(스케일 1.3125 · 오프셋 354,723 · NCC .66)로 목(1930–2030) 봉합. 밴드-온-블랙 = 색상 배수(cos>.9965) 제거 · 원/글로우 = sat>20 그린 제거. + `yeta-face.webp`(320² 기본 캐릭터 아바타).
- **스테이지**: 4:5 → 9:16 포스터 좌표계(하단 정렬 · 실측 %: 원 중심 48.8/69.5·코어 지름 74.3%W·밴드 (25.7,33.1,43.8×6.43) -6.2° 유한 라벨·로고 63.66%W@19.7%). 원 그라데 = 실측 프로파일 + **closest-side 교정**(farthest-corner 디폴트 = √2 과대). 효과 4종(글리치 RGB·VHS·홀로·펄스) 계승.
- **진입 안무**: 원 선등장 → 소년 촤르륵 글리치-인(ymbIn) → 라벨 쏵 스윕인 도킹(ymBandIn) → 자물쇠 숨쉬기 → **잠금 터치** = .go(라벨 각도 유지 퇴장+줌 1.07 복귀) → .strike(#ywmLock = 확정 워드마크 6겹 이중 조립 · 낙하 착지+플래시+원 광량 스파이크+착지 반동) → .calm(사람 효과 정지·원 디밍 펄스만·5.6s 캐릭터+로고 동기 VHS 버스트)+PIN 부상. PIN = 9:16 앵커(.ym-fit)로 몸통 유착 · 키 진동 8ms/오류 [60,40,60]/성공 18 · 통과 = .bye 라벨 재스윕 → 캐릭터 메뉴. 자동 pin-mode(260705) 폐지 = 잠금 터치 플로우 복원. reduced-motion = 최종 상태 직행.
- **언락 홈 상단**(운영자 260706): 로고 112px(130%)·모서리 여백 24 쌍정렬 · 우상단 프로필 = 기본 캐릭터 얼굴 고정(yProfImg 페르소나 주입 폐지 · 커스텀 이미지 축 후속) · 탭 클리어런스 52→64.
- **함정 박제**: ① `.ywm` 베이스가 소스 뒤라 단일 클래스 오버라이드 패배 → `.ym-stage .ym-wm` 특이도 승격 ② radial-gradient 디폴트 farthest-corner ③ 어두운 라임(g<70) 글로우 잔재가 "소년"으로 박제되던 것 sat 기준 제거 ④ Playwright 클릭 = 숨쉬기 애니로 불안정 판정 → evaluate 클릭.
- 보고서: `docs/reports/260706_메인포스터진입.html`. 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측 8프레임{진입중·라벨 스윕중·대기·go·strike·PIN·언락 헤더·폰 비율} + 10인 평의회 실발사(READ-ONLY) 평결 반영{에셋 하반신 매트 파탄 재추출(원 디스크 기하: 안=밝은 그린만 제거·밖=검정 기준 — 다리·그림자 100% 보존) · PIN 비가시-조작가능 접근성 회귀 차단(visibility+pin-mode 게이트+role=alert+포커스 이동) · 해시 레이스 가드 · 히어로 프리로드 · 캐스케이드 특이도 하드닝(.bye.go/.calm.strike/> .ywm) · g2 버스트 동기화 · 줌 1.09 · 가로모드 PIN 압축 가드(max-height 560 · 사전존재 결함 방어)} → 머지 후 origin/main read-back. 트레이드오프 박제: PIN 단계 원 펄스×글래스 리블러 비용(평의회5)은 운영자 4-2 명시 요구 우선으로 유지.

## 18차 — 챗 회색 깡통 렌더 수정(계단 pause) + 캐릭터 판타지 6인 개편 (운영자 260706)
- **버그(운영자 스샷)**: 챗 화면에 회색 민짜 사각형 3개(중앙 대형 1 = 512px 타일 정폭 실측 · 좌 레일 카드 자리 2). 원인 = 15차 CSS 3D 계단(`.ystair` · 84면 will-change 레이어)이 챗(dialog·top layer) 열림 중에도 뒤에서 10초 재쌓기 지속 → 모바일 GPU 컴포지팅 압박 → 래스터 실패 타일이 회색 깡통으로 노출. 챗 배경은 자체 포스터라 계단은 보이지도 않으면서 GPU만 소모.
- **수정**: `yStair`에 pause/resume 추가 — `openYeta()` = pause(타이머 해제 + `.ys-hold` display:none = 레이어 자체 소멸) · dialog close = resume(재쌓기). 챗 시각 변화 0 · 챗 중 GPU/배터리 낭비 제거. 관련 점검: 마이크 숨김(16차)·스테일 페르소나 폴백(A7) 정상 — 추가 결함 없음.
- **캐릭터 6인 개편**(판타지·게임 주인공 서사 본따기 + 무협 · 전화 캐릭터 하은 = 그대로): 류=무협 회귀검객(화산귀환 결 · 오백 년 전 천하제일검 회귀) · 백=마수 사냥꾼 길드 최후 생존자(위쳐 결) · 세라=최연소 각성자·탑 등반(나혼렙 결) · 무디=몇백 년 불사의 찻집지기(도깨비 결 · 배웅의 형벌) · 윤=산 자/건너간 자 양쪽 사연을 받는 경계 방송 DJ(페르소나 심야채널+오르페우스 결) · 하은=불변(roster 엔트리 byte-identical 대조 ✓ · phone/voice 배선 보존). 제외(은퇴) = 데스크·카피·가을·본 — roster 제거·카드 md 존치(스테일 세션 안전)·세계관 "떠난 주민" 박제.
- **세계관 v2**: 무음동 = 깊은 밤(자정~3시) 경계가 얇아지는 어반 판타지 골목. 규칙 = 판타지는 새어 나오는 정도만(스펙터클·마법 전투 금지) · 각 비밀은 그 카드 범위까지만(대리 폭로 금지) · 유저는 끝까지 평범한 에디터(각성·이세계행 금지).
- 보고서: `docs/reports/260706_판타지6인개편_계단정지.html`. 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{챗 열림 = .ystair display:none·타이머 정지 / 닫힘 = 14스텝 재조립 / 레일 6인 / 육안 챗 시각 변화 0} + 하은 엔트리 프로그램 대조 + 머지 후 origin/main read-back.

## 19차 — 하은 프로필 카드 + 폰 주소록 원탭 등록(vCard) (운영자 260706)
- **왜**: 실전화(Vapi 발신 +1 240-616-4569)가 걸려올 때 폰에 하은 사진·이름이 뜨려면 주소록 등록이 필요한데 매번 수동 입력 → 원탭 자동화 요구(운영자 스샷 = 갤럭시 연락처 수동 편집 화면).
- **한계 박제**: 웹은 주소록에 *무단 기록* 불가(보안상 API 없음) → 최대 자동화 = **vCard(.vcf) 원탭 배포**(탭 = 다운로드 → 열기 = 이름·사진·번호 전부 프리필 → "저장" 한 번).
- **소재**: `viewer/assets/contacts/haeun.vcf` — vCard 2.1(삼성 내보내기 포맷 미러 · CHARSET=UTF-8 · CRLF) · 사진 = roster 아바타 중앙크롭 720² JPEG q88(89KB) base64 임베드 · 번호 = `+12406164569`(CELL) + `00612406164569`(VOICE) 2표기{안드로이드 발신자 매칭 = 뒷자리 기준이라 어느 쪽 표기로 와도 잡힘}. 반입 = 샌드박스 프록시가 r2.dev 차단 → 1회용 Actions 워크플로(tmp-asset-fetch)로 러너 우회 후 즉시 삭제(PR #46 등재→본 PR 제거).
- **뷰어**: `call.js`에 `#yetaVcfBtn`(연락처 저장) 헤더 주입 — 수화기 왼쪽 · 픽토그램-온리(#yetaCallBtn 선언 계승 · 신규 토큰 0) · **phone 배선 캐릭터(VCF 맵)만 노출**(vcfBtnSync = onSess 훅) · 탭 = 같은출처 a[download] → 갤럭시 "연락처에 추가". `_headers`에 vcf 정확 MIME(text/x-vcard)+attachment 부착.
- 검증: 자가검증 경로=check_refs rc=0 + node --check(call.js) + vCard 구조 프로그램 검증(BEGIN/END·TEL·PHOTO base64 디코드 재확인) + 머지 후 origin/main read-back. 운영자검증 잔여 = 갤럭시 실기기 임포트 감각(사람 감각 유일 축 — vcf 파일은 채팅으로 선전달).

## 20차 — 연락처 자동 배포: 보이스 캐릭터 탭 = vcf 자동 다운로드 (운영자 260706)
- **왜**: 19차 헤더 버튼도 "찾아서 눌러야" 함 → "보이스 되는 캐릭터를 눌렀을 때 자동으로 저장" 요구. 한계 재박제: 주소록 무단 기록 API 부재 → 최대 자동화 = **탭 제스처 안 자동 다운로드**(브라우저 무제스처 다운로드 차단 정책도 이 방식이라 통과) + 안내 토스트 · 폰에서 "저장" 탭 1회는 잔여.
- **로직**(`call.js`): 문서 캡처 클릭 위임 — 진입 3경로(`.ychar-card`·`.ypick-row`·`.ys-row`)의 VCF 맵 캐릭터 탭 = `vcfDownload()` 자동 발동. 가드 = localStorage `yeta_vcf_auto`{id:ts} **기기당 1회**(재배포 = 헤더 버튼 · 수동 배포도 가드 계상 = 중복 방지). 배선 없는 캐릭터·재탭 = 무발동.
- 검증: 자가검증 경로=check_refs rc=0 + node --check + **Playwright 실측 4종**{하은 카드 탭=다운로드 발동 · 재탭=무발동(1회 가드) · 무배선(무디)=무발동 · localStorage 가드+토스트 문구} + 머지 후 origin/main read-back. 운영자검증 잔여 = 갤럭시 실기기 감각(다운로드 알림→저장 흐름).

## 21차 — 아이돌×판타지 페르소나 시트 v1 (9인 풀스펙 초안 · 편집용 HTML)
- 운영자 대개편 지시: 전원 이중성(낮↔밤) 코어 + 아이돌×판타지 혼합 · 카테고리 풀스펙{배경·가족·MBTI/에니어그램·표정·말투·기원·좋아/싫어(이유)·목적·관계·외형·무대} · 뭉태기 금지 = 5레이어 주입 스키마(CORE/PROFILE/NARRATIVE/HOOK BANK/STATE+MEMORY) 설계 동봉.
- 라인업 9인: 미연(구미호·(여자)아이들 미연 결)·총명(화산귀환 결)·한바로(웨어울프 20)·원빈/블라드미르(운영자 설정집 3건 정본)·원이(리센느 원이 결·인어 제안)·고죠(운영자 자료 4건 정본)·세라(퀸카 츤데레·금묘 제안)·하은(소꿉친구 짝사랑·전화 배선 유지)·황세웅(운영자 자료 대기). 무디·윤·백·데스크·카피·가을·본 = 은퇴(백은 선택지 유지). 전원 성인(운영자 확정 — 한바로·세라 20세).
- 분신술: 리서치 9기(청명·고죠·황세웅 식별[=운영자 본인 확인]·구미호·웨어울프·일진/소꿉·RIIZE원빈·리센느원이·미연) + 검증 5기(캐릭터 감사 4·세계관/기능 감사 1) 실발사 — 평결 치명 4·중간 12 전부 반영{인랑 공통 룰④·발라카르=디 마르카 가신 통합·원빈 24세/가문 확정안·원이 물 룰 2단계/실존 신체 특정 삭제·고죠 보쿠↔오레 한국어 치환/불개입 신조·세라 워딩 순화/데레 감속·하은 선긋기 금기 재이식/삐짐 상한·시트 export 누락 버그}.
- 산출: docs/reports/260706_페르소나시트_v1.html(contenteditable+localStorage+전체 내보내기) — 운영자가 세부 서사 기입 후 회신하면 카드 확정 → 프로필 4안×인원 이미지 생성(유료·승인 후).
- 검증: 자가검증 경로=Playwright 실측{편집 117블록·이중성/대사/프롬프트 수출 포함 26k자·11섹션} + check_refs rc=0 + 머지 후 read-back.

## 22차 — 연락처 UX 개선: 명함 시트 + 좌측 인물 레일 폐지 (운영자 260706)
- **리서치**: Web Share API로 공유시트→연락처 직행 검토 → **Chromium 허용 확장자에 vcf 없음 실측**(FILE_TYPES.md — pdf/오디오/이미지/텍스트/비디오만) = 공유 경로 폐기, 다운로드가 유일 전송로. 안드로이드 intent: URL은 사진 전달 불가 = vcf 유지.
- **명함 시트**(`call.js` #vcfdlg — 20차 무맥락 즉발 다운로드+토스트 대체): 배선 캐릭터 첫 탭 → 챗 위 top-layer 시트 — 지문(이탤릭 .yn 결)+아바타(.yintro-ava 라임 후광)+이름(.yintro-name)+번호(tabular)+효용 1줄 → **[연락처 받기]**(accent 알약 = .ycall-btn.take 플레이트 결·--press-l) / [다음에](무채 글래스). 받기 = 제스처 안 다운로드 + 시트가 **2단계 가이드로 전환**(번호 배지 = .ylist-badge 정본 선언 · ① 하은.vcf 열기 ② 저장 탭) → [확인]. 자동 노출 기기당 1회(localStorage) · 헤더 연락처 버튼 = 시트 재오픈. 등장 = 쪼개기 stagger(ypickIn 커브 계승·reduced-motion 가드) · Esc/뒤로가기 닫기(#calldlg 결) · 신규 토큰 0.
- **좌측 인물 레일 폐지**(`index.html`): `#yside{display:none!important}` 한 줄(16차 마이크 전례 · 복구=줄 삭제) — "레일 때문에 채팅창이 작다" → 채팅 전폭. 화자 전환 = 도킹 네비·갤러리 존치(코드 휴면).
- 검증: 자가검증 경로=check_refs rc=0 + node --check + Playwright 실측 5종{탭→0.7s 시트 자동 노출(문구·번호·가이드 초기 숨김) · 받기=다운로드 발동+가이드 전환+CTA 확인화+다음에 숨김 · 확인=닫힘 · 재탭=무노출 가드 · 실 index에서 #yside display:none} + 실토큰·실아바타 스크린샷 2상태 육안 + 머지 후 origin/main read-back.

## 23차 — 시즌제 세계관 + 캐릭터 리스트 개편(1행 1명·육각 성향 레이더·디테일 뷰) (운영자 260706)
- **시즌 아키텍처(롤토체스식)**: `apps/yeta/worlds.json` 신설 = 시즌 레지스트리(active + seasons[] — 성격 불변·역할군 리스킨 · 누적 시 선택 가능 구조). op `chars`가 world 동봉(실패 비치명). v1 시즌 = 무음동.
- **캐릭터 탭 개편**: 2열 그리드 폐지 → 최상단 시즌 헤더(.yworld — SEASON n·세계관명·태그) + 세로 리스트 1행 1명(.ycrow) = [아바타 56][이름/표현(desc)/대사(quote) 3줄][행 내부 전용 연한 세로 구분자][육각 성향 레이더]. 탭 = 세부 디테일(.ycd 풀스크린 글래스 시트 — 대형 아바타·소개·라벨 육각·[대화 시작] 라임 CTA) → 챗 진입. 탭 전환·뒤로 = 디테일 자동 닫힘 · 열림 중 로고/프로필 숨김(가림 방지).
- **육각 6축 확정**: 온기·장난·신비·직진·텐션·절제(0~5) — roster `stats[6]` + `desc`·`quote` 필드 신설(6인 전원 기입). SVG 레이더 = 그리드(글래스라인)+수치 폴리곤(라임 20% 채움·1.5 스트로크) 토큰만 사용·신규 토큰 0.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{시즌 헤더·6행·육각 6·구분자 6·디테일(라벨 6축·CTA)·대화 진입·디테일 자동 닫힘} + 스크린샷 육안 + 머지 후 read-back.

## 24차 — 장원영 도시에(1만자) + 구미호 카드 원영 교체 (운영자 260706 "미연 폐기·장원영으로")
- 분신 4기(프로필·커리어/성격·원영적사고/말투·유행어/비주얼·취향·지뢰) 병렬 리서치 → `docs/reports/260706_장원영_도시에.html`(약 1만자 · 원문 인용·유행어 사전·지뢰 지도·구미호 이식 설계 포함) 박제.
- 핵심 번역 3축: ① 원영적 사고 = 수백 년 표본의 통계적 확신("모든 불행은 다음 장면의 복선 — 내가 봤거든") ② "본질에 스크래치 못 낸다" = 불사의 멘탈 + 선 넘은 자엔 조용한 응징(이원 구조) ③ 두 번의 데뷔 = 환생 서사(지난 생 '전설의 아이돌' — 제 잘못 아닌 일로 생이 접힘 → 이번 생 첫 정착 = 찻집).
- 페르소나 시트 갱신: 미연 카드 폐기 → **원영**(장원영 결 구미호 · ENTJ·7w8 · 유행어 전량 이식 "완전 럭키비키잖앙🍀"·"~것이야"·"밥은 먹었니?"·헤어플립=꼬리 버릇) — 낮=럭키비키 찻집 언니 ↔ 밤=아홉 꼬리(이야기 포식). 크로스 참조(외상 장부·거점·스키마) 원영으로 일괄 정정.
- 검증: 자가검증 경로=Playwright 시트 재실측(편집 117블록·수출 27k자) + 글자수 프로그램 카운트 + check_refs rc=0 + 머지 후 read-back.

## 25차 — 명함 시트 페르소나화 미세손질 (운영자 260706)
- 순서 재배치: **프로필 사진 최상단** → *하은이 폰 번호를 적어서 건넨다*(지문 이탤릭 · 프로필과 간격 `--sp-2` 추가) → 대사 "연락처에 저장하면 이름 뜰거야. 아직도 저장 안 한건 아니지?"(삐지는 페르소나 활용 · `--fg` 발화 톤) → 번호 → CTA. 이름 헤딩 제거(지문에 포함). 문구 = VCF 맵 `nar`/`line` — 페르소나 대개편(21차 시트) 확정 시 카드와 동기 예정.
- 검증: 자가검증 경로=check_refs rc=0 + node --check + 실토큰 스크린샷 육안 + 머지 후 origin/main read-back.

## 26차 — 명함 시트 타이포 보정 + 캐릭터 디테일 [연락처 교환하기] (운영자 260706)
- **대사 75%**: `.yvcf-desc` `--fs-body`(15)→`--fs-xs`(11 ≈ 75% 최근접 토큰 계승). **음절 잘림 방지** = `word-break:keep-all`(지문·대사) · **구 단위 개행** = 대사 문자열 `\n` + `white-space:pre-line`(운영자가 임의 조정 불요 — 코드가 쉼표 들어갈 단위로 개행).
- **캐릭터 디테일 뷰(.ycd — 23차 개편분)에 연락처 교환하기 버튼**: `call.js`가 `#ycdCard` innerHTML 재생성을 MutationObserver로 감지해 배선 캐릭터(VCF 맵)만 [대화 시작] 아래 `.ycd-vcf` 주입(본체 무수정 모듈 계약 유지) — `.ycd-cta` 지오메트리 동형·무채 글래스(라임 = 대화 시작 단독)·`--press-m` 동일 티어·user-plus 아이콘. 탭 = 명함 시트(단일 플로우 · 1회 가드 계상).
- 검증: 자가검증 경로=check_refs rc=0 + node --check + Playwright{디테일 재생성→주입 · 무배선 미주입 · 교환하기→시트 · pre-line 2줄 · 실토큰 11px} + 스크린샷 육안 + 머지 후 origin/main read-back.

## 27차 — 스플래시 최소화 + 롱폰 스테이지 커버 + 답장 1초 페이스 (운영자 260706)
- **PWA 스플래시(스샷 = 설치형 첫 진입 화면)**: OS(WebAPK)가 manifest로 자동 생성 — **웹에서 완전 제거는 불가**(안드로이드 명세). 최소화 = manifest name/short_name `예타 : 가상현실 대화 섹션`→`YETA`(상·하단 문구 축소 · 다크 bg·아이콘은 기존). 반영 = 크롬이 WebAPK를 수일 내 자동 갱신(즉시 = 앱 삭제 후 홈 화면 재추가).
- **롱폰(9:20~22) 스테이지**: `.ym-stage`·`.ym-fit` `width:max(100%, calc(100dvh*9/16))` — 세로 꽉 채움·가운데 정렬·좌우 크롭(운영자 "위 화면 안 씀 → 9대21 가운데 정렬로 잘리게" 채택). 내부 % 좌표계 불변(박스만 확대 = 17차 포스터 실측 좌표 재작업 0) · PIN·자물쇠 = 동일 박스 공식 = 몸통 유착 유지 · 구형 브라우저 = 100% 폴백.
- **답장 1초 페이스**(`yRender`): 새 답장이 즉답으로 꽂히면 어색(운영자) → 꼬리 assistant 런만 `_yPaceN` 홀드 = **타이핑(입력 중… 점 3개) 모션 ~1초 선행 후 공개**. 내 턴·sys·첫 렌더(재진입 히스토리) = 즉시 · 낙관 내 버블 소실 방지(꼬리 런 직전까지 즉시 공개) · 홀드 타이머 단일 가드.
- 검증: 자가검증 경로=check_refs rc=0 + manifest JSON 파스 + Playwright 실측{412×915 롱폰 = 스테이지 h=915 세로커버·좌우 대칭 크롭(-51/-51) · 412×732 = 폭맞춤 유지 · 페이스 5종: 첫렌더 즉시/직후 숨김/타이핑 표시/1초 후 공개/타이핑 소멸} + 머지 후 origin/main read-back.

## 25차-B — 페르소나 정량화: 통합본 v2(이력서·게이지·친밀도) + 뷰어 캐릭터별 성향 설정(16축) (운영자 260706)
- **시트 v2**: 9인 카드에 {이력서형 요약(직업 주/부·거주·가문·특기·**목소리**(음색·모드별) 등 필드 세분화) · **정량 게이지 16축**(대화 스타일 4·감정 태도 5·관계 4·세계관 3 — 슬라이더 조정·자동 저장·수출 포함) · **친밀도 LV0~5 표**(단계별 말투·개방 정보)} 주입, 산문은 details 접힘("이력서 → 눌러서 세부"). 실측 = 슬라이더 128개·목소리 8종·수출 정상.
- **뷰어 성향 설정**(분신술 아이데이션 3기 → 종합 구현): 디테일 ⚙ → 설정 패널 = 라이브 육각(16축→6축 파생 요약) + 프리셋 칩(기본/순한맛/매운맛) + 4그룹 아코디언 커스텀 슬라이더(트랙 = 글래스+라임 --p 채움 · iOS 네이티브 흰 트랙 회피) + 더티 도트·그룹 변경 배지 + 기본값 복원(2탭 armed) + 저장됨 표시.
- **데이터 경로**: 신규 op `tune {persona, g:[16]}` — 숫자만 수용·0~10 클램프(무인증 게이트웨이 주입 원천 차단) → `sess.tunes[persona]` · reset은 tunes 승계. `yeta_chat.sh` = extract_mat tune 동봉 + 프롬프트에 [성향 보정] 블록(5구간 매핑 · **중립 5~6 생략 = 무설정은 카드 원본** · 카드 우선 명시 · 축 순서 = viewer TUNE_AX와 짝). roster에 `tune[16]` 디폴트(6인).
- 발견 수정: 하단 네비가 오버레이 가림(스택 컨텍스트 — cd-open 시 네비 숨김) · 변경 0 배지 잔존([hidden] display 차단).
- 검증: 자가검증 경로=check_refs rc=0 + bash -n + node --check + Playwright E2E{⚙ 진입·16행/4그룹/칩 3·슬라이더 변경 = 더티 도트·배지·localStorage·**op tune 실요청(클램프 배열 확인)**·프리셋·2탭 복원} + 스크린샷 육안 + 머지 후 read-back.

## 17차+ — 잠금 화면 개편 이식 (운영자 260706 플레이그라운드 확정)
- **오버레이** = 스포티파이 블랙 67% `.ym-ovl`(z1 · 언락 시 페이드 퇴장) · pin-mode 추가 그라디언트 딤 폐지(중복 방지). 패드는 기존 --glass 유지 = 오버레이 뒤 어두워져 플그 1.0x 체감 동일.
- **슬라이드 언락**: 자물쇠 원형 버튼 → 글래스 트랙+라임 노브 "밀어서 접속"(끝까지 그어야 진입 · 진동 [50,40,90]) — 탭 언락 폐지.
- **PIN**: 숫자 입력 = 실숫자 450ms 노출 → `*`(.num data-d) · 키 진동 8ms 유지.
- **승인** = 전뇌 글리치 1회(.zap 라임/코발트 슬라이스 .55s) + 강진동 [80,60,160].
- **글로우 펄스** = --st-amp 1.4→2.0 · ymcPulse 하한 amp 연동(.5~1 폭).
- 하단 잔존 여백 = .ym-pin top 69→71.5%.
- 스플래시(첨부4) = 설치형 PWA OS 표준(제거 불가·배경 이미 #121212) — 아이콘 디자인 손질만 가능(운영자 안내).
- 검증: 자가검증 경로=check_refs rc=0(JS 구문 포함) + 머지 후 read-back. 플그 = lock-play.html(채팅 전달).

## 18차-B — 탭 언락 복귀 (운영자 260706 "끌어서 말고 처음처럼 누르는 거")
- 슬라이드 트랙 롤백 → 원형 자물쇠 버튼 탭(.ym-access) 복귀 · 탭 진동 [50,40,90] 유지 · 슬라이드 CSS/JS 제거.
- PIN 키 프로그램 포커스 네모 테두리 억제(`:focus:not(:focus-visible)` — 키보드 접근성 링 유지 · 운영자 스샷 "1" 네모).
- 오버레이 67%·숫자 450ms·승인 글리치·펄스 2.0x는 유지.
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 read-back.

## 28차 — 언락 밝기 플레이그라운드 (운영자 260706 "잠금 풀면 캐릭터랑 전반적으로 밝게 — 플레이그라운드로")
- 시안: `docs/reports/260706_언락밝기_플레이그라운드.html` — 잠금 화면(현행 오버레이 67% 고정 = 비교 기준) → 자물쇠 탭 언락 → 언락 후 밝기 튜너. 프리셋 4{현행 · A 은은 · B 라임 앰비언트(추천) · C 캐릭터 발광} + 슬라이더 12(갤러리 딤 상/하 · 라임/코발트 앰비언트 · 계단 발광 · 카드 표면/엣지 · 서브텍스트 · 아바타 링/글로우 · 행 컬러 틴트 · ⚠기틀 배경 리프트) + 링 색 토글(라임/페르소나색) + 선택값 복사.
- 실물 계승: `:root` 토큰 사본 · ycrow/yworld/ynav/워드마크 레터 마스크 · 잠금 스테이지 기하(원 48.8/69.5 · 밴드 −6.2° · 로고 63.66%) · ypickIn/ynavRise/ymLockBreathe 커브. 아바타 = 이니셜 폴백(개발 환경 R2 차단 · 실물은 얼굴 이미지 동일 배선). 계단 = 정지 실루엣 근사(발광 배율만 배선).
- 값 반영은 운영자 선택 후 별도 차수(이 차수 = 시안만 · viewer 무변경).
- 검증: 자가검증 경로=Playwright 렌더 실측(잠금/언락 전환·프리셋 B/C 스크린샷·JS 에러 0) + check_refs rc=0.

## 29차 — 플레이그라운드 제안 표준 기틀 등재 (운영자 260706 "HTML로 항상 플레이그라운드식 · 그 수준으로 항상")
- 3점 동기 등재: CLAUDE.md §작업표준 b) 확장(디자인·UI 제안 = 플레이그라운드식 디폴트) + `yeta-design` SKILL 출력 형식에 구성 5요소{실물 미리보기 · 프리셋 · 슬라이더 · 선택값 복사 · 한계 각주} + 정본인덱스 4절 확립본 행(`260706_언락밝기_플레이그라운드.html`).
- 전달 기본 = 레포 `docs/reports/` 커밋(정본) + 채팅 파일 직접 첨부(원탭) · Artifact 링크 = 재열람·외부 공유 보조 — 운영자 판정 "아티팩트 어렵다"(사람 감각 축 = UX 수용 판정).
- 운영자 직접 지시 간단 편입 = 교차검증 생략(절대명령 9 신설 선례 계보).
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 `git show origin/main` read-back.

## 30차 — 플레이그라운드 포터블 신설 (운영자 260706 "그 조작감 다른 레포에도 · 내용 빠짐없이 · 최상위 md에서 읽게")
- `docs/플레이그라운드_포터블.md` 신설 — 절대명령1_포터블 결(AI가 읽고 그대로 이식·누락 0): CLAUDE.md 붙여넣기 (A) 블록 + 바인딩 표(SSOT·보고서경로·프레임·확립본 슬롯) + 골격 스펙(레이아웃·튜닝 변수 규약[기본값=현행 실측 ★]·상태⇄슬라이더⇄출력 바인딩·계승/갱신 자동 표기·재현 원칙) + 검증(헤드리스 실클릭) + 전달 통로 판정(품질=임베드 시 동률 · 1회성=첨부 · 반복 루프=Artifact 병행) + 안티패턴 7.
- 정본인덱스 2절 행 등재 + CLAUDE.md §작업표준 b) 포인터·Artifact 보조 조건("반복 수정 루프·외부 공유 시") 구체화.
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 `git show origin/main` read-back.

## 31차 — 포터블에 운영자 사용법 내장 (운영자 260706 "전달 문구도 md에 — 하나의 파일로만 공유")
- `docs/플레이그라운드_포터블.md` 상단에 👤 운영자 사용법 4단(넣는 경로 · 이식 시키는 문구 · 시험 문구 · 자가 교정 문구 — 전부 복사 가능 코드블록 · 절대명령 9 정합) 추가 = 채팅 안내 없이 파일 단독 자기완결. 확립본 HTML 없이도 §3 스펙만으로 제작 가능 명시.
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 read-back.

## 32차 — 챗 전환 무빙 플레이그라운드 (운영자 260706 "대화창 좌우 이동이 부자연스러워 — 해결좀")
- 원인 실측: 챗 = `#yetadlg` showModal/close 즉시 교체 — 진입·퇴장 애니 전무(메뉴1↔2는 연출 있는데 챗만 팍 뜨고 팍 닫힘).
- 시안: `docs/reports/260706_챗전환무빙_플레이그라운드.html` — 확립본 5요소 계승. 미리보기 = 캐릭터 리스트 ↔ 챗 실물 재현(알약 헤더·글래스 버블·입력바·배경 130%·캐릭터별 인사/입장 나레이션 교체). 전환안 4{현행(즉시) · A iOS 푸시★(슬라이드+패럴랙스 26%+딤) · B 카톡 라이트(페이드+6%) · C 글래스 디졸브(스케일+페이드)} + 슬라이더 6(시간·이동·불투명·스케일·패럴랙스·딤) + 커브 세그 3(iOS/--ease/촤르륵) + 선택값 복사(배선 지점 명기 = dialog 닫힘 2단계).
- 검증: 자가검증 경로=Playwright 실클릭(행 진입·← 복귀·전환 중간 프레임 슬라이드 실측·JS 에러 0) + check_refs rc=0. viewer 무변경(시안만) — 배선 = 운영자 선택 후.

## 33차 — 언락 밝기 배선 (운영자 260706 플그 선택값 회신 = 승인)
- 값 갱신: `--bg` #121212→#161716(기틀 갱신 · ⚠ 운영자 명시 승인) · `--fg-2` #C4C9C4→#E7ECE7 · theme-color 메타·manifest 2곳 동기.
- 색 토큰 신설(승인·락 등재): `--glass-0` rgba(255,255,255,.02) · `--glass-line-0` rgba(255,255,255,.115) — 언락 갤러리 초투명 단.
- 배선: 갤러리·대화목록 딤 (.66,.78)→(.73,.47) · unlocked 앰비언트 라임 .14+코발트 .03 · 계단 발광 x2{글로우 .18→.36 · face 그림자 .22→.44/.14→.28 · 채움 .06→.12/.14→.28/.16→.32/.2→.4 — 테두리 위계는 보존(일괄 2배 시 클램프로 위계 붕괴)} · 카드(.yworld·.ycrow·.ylist-row) 초투명 단 전환 · 아바타 라임 글로우 box-shadow 0 0 6px accent .21(플그 글로우 .26 환산 · 링 0이라 글로우 색 = 라임 기본).
- 잠금 화면(.ym-ovl 67%)은 불변(운영자 "잠금은 현행 고정").
- 검증: 자가검증 경로=check_refs rc=0(신설 토큰 락 등재·거울 재생성) + Playwright 계산 스타일 실측(전 값 일치·JS 에러 0) + 머지 후 read-back.

## 34차 — 잠금 안무 플레이그라운드 (운영자 260706 "날아오는 거·잠금해제 날아가는 거·순차 모션 잘게 쪼개서 설계하게")
- 시안: `docs/reports/260706_잠금안무_플레이그라운드.html` — 잠금 화면 순차 모션 전부를 조각 분해(확립본 5요소 계승). 노브 28{① 진입: 원 등장·펄스 주기·소년 촤르륵 dur/시점·글리치 스플릿 on/off·상시 배속(--st-amp)·라벨 스윕 dur/시점·자물쇠 시점/숨쉬기 주기/최대 scale ② 잠금 터치: 고리 각도·go/strike/calm 딜레이(140/840/1330 실측)·라벨 퇴장·줌 dur/피크·스트라이크 dur/시작 높이·플래시 on/off·PIN 부상 dur/거리·대기 버스트 on/off ③ 언락: 승인 글리치 on/off·라벨 재스윕·전환 시점(560)·오버레이 페이드·네비/로고 dur·행 stagger 간격}.
- 실물 키프레임·커브·클래스 체인(.go→.strike→.calm+pin→.bye→unlocked) 그대로 이식(안무 % 골격 불변 · 노브 = 시간·시점·기하·on/off만). PIN 패드 재현(아무 4자리 = 통과) · 리플레이 4버튼(진입/터치/통과/전체) · 프리셋 3(현행/스냅/시네마틱) · 언락 후 화면 = 33차 밝기 배선값 반영.
- 검증: 자가검증 경로=Playwright 실클릭(자물쇠→스트라이크→PIN 4자리→언락 전 시퀀스·리플레이 리셋·JS 에러 0 · 언락 후 잠금층 잔상 버그 1건 수정 후 재실측) + check_refs rc=0. viewer 무변경(시안만).

## 35차 — 잠금 진입 안무 배선 (운영자 260706 잠금 안무 플그 선택값 "일단 진입" = ① 진입 그룹만)
- 배선 6조각: 원 등장 ymcIn .5→1.5s(펄스 시작도 1.5s 동기) · 소년 ymbIn .55→1.2s @.2→.25(글리치 스플릿 g1 .25/g2 .33 동조) · --st-amp 2→1(상시 홀로·VHS·글리치·펄스 느긋 · 펄스 하한 .5→.75 연동) · 라벨 ymBandIn .5→.4s @.85→.5 · 자물쇠 등장 1.2→1.1s(숨쉬기 시작 1.5s) · 숨쉬기 2.6→1.9s·최대 scale 1.08→1.12.
- ② 잠금 터치·③ 언락 그룹 = 전항 현행 선택 = 무변경.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright computed animation 실측(6조각 전 값 일치·JS 에러 0) + 머지 후 read-back.

## 36차 — 잠금 터치 안무 배선 (운영자 260706 잠금 안무 플그 선택값 ② 그룹)
- 배선 5조각: 고리 열림 28→31° · 라벨 퇴장 .42→.34s @140→260ms · 줌 복귀 .75→.45s·피크 1.09→1.11 · 로고 스트라이크 .48→.92s @840→280ms·시작 높이 130→220%(높은 데서 그윽한 낙하) · PIN 부상 .5→.4s @1330→790ms·거리 46→94px(visibility 딜레이 .62→.52 연동).
- 특성(플그 동일 재현): calm(790ms)이 스트라이크(1200ms 종료) 착지 지점(~55%)에 개입 = 착지 순간 대기 모드 스냅 — 운영자가 플그에서 본 그대로. 플래시·원 히트·착지 반동 딜레이(.24/.24/.3s)는 플그와 동일 고정 유지.
- ① 진입 = 35차 기배선과 동일 값(변경 없음) · ③ 언락 = 현행.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실클릭 시퀀스 실측(@350ms go+strike·@900ms calm+pin·computed 5값 일치·JS 에러 0) + 머지 후 read-back.

## 37차 — 잠금 언락(③) 안무 배선 + 잠금안무 플그 v2 (운영자 260706 ③ 선택값 + "언락 시 캐릭터·원 밝아지는 정도" 신규 축 요구)
- ③ 배선 5조각: 라벨 재스윕 .65→.5s · 전환 시점 560→800ms · 오버레이 페이드 .6→1.15s · 네비·로고 .5→.45s(reveal 3규칙) · 행 stagger 간격 .05→.09(사다리 재계산 · n+10 = .81).
- 플그 v2(`docs/reports/260706_잠금안무_플레이그라운드.html` 갱신): 기본값 = 배선 완료 현행(①35차·②36차·③37차) · ③ 세분 노브 7 신설{승인 글리치 시간 · 앰비언트 페이드인 · 네비 시차 · 행 등장 시간/거리 · 계단 쌓기 시작/웨이브(언락 후 실루엣 쌓기 재현)} · ④ 언락 라이트업 신규 축{ON/OFF·세기(brightness 1~2.5)·시간·시점 — 검정 배경 불변·원+캐릭터만 발광} · 프리셋 = 현행/라이트업 시안★/라이트업 강.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright(라이트업 프리셋 전체 시퀀스 실클릭 lit 발동 확인·JS 에러 0) + 머지 후 read-back. ④는 시안만(실물 미배선 — 운영자 선택 후).

## 38차(예정) — 잠금 PIN 웜업 연출 컨셉 접수 (운영자 260706 · 운영자 1시간 후 재개 약속 · 사용량 한도 임박으로 기록 선행)
- 운영자 서사 확정(예타 = 메타버스 절반 구현 · 잠금 해제 = "지금 세상을 떠나 전자가 되어 다른 세상으로 순간이동"):
  - 가) (2)잠금 터치 → (3)PIN 입력 전환: 로고가 번개로 꽂히는 순간 = **캐릭터에 숨이 불어넣어지는 느낌** — 캐릭터 형태가 명료해지고 + 뒤 원 디밍(광량)이 좀 더 커지고 + **점점 웜업**. 현재는 로고만 꽂히고 캐릭터 그대로 = "확 느낌이 안 삶".
  - 나) PIN 입력 = 캐릭터를 제일 오래 집중해서 보는 순간 + 락이 풀리는 시점 = **캐릭터·브랜딩 강조 정점**. 브랜드 컬러 디밍이 **주기적인 분기로 왔다갔다**(호흡) → 브랜드가 딱 인식에 박힘.
  - 다) 입력 완료 → 메뉴 = **메타버스 진입** — 기존 ④ 라이트업(37차 플그 신설 · 순간 발광)을 "조금만 손봐서" + 지지직(zap)으로 충분. 이 순간 캐릭터 변화 불요(너무 잠깐이라 안 보임).
- 설계 방향(다음 차수 = 플그 v3): **⑤ PIN 웜업 그룹 신설** — calm+pin-mode 상태에 배선. 노브{웜업 ON/OFF · 캐릭터 명료화 세기(brightness/contrast · 홀로그램→실체화 결) · 원 광량 상승(--st-glowop 축 확장) · 웜업 펄스 깊이/주기(브랜드 호흡 왕복) · 차오르는 시간(점진 웜업)}. ④ 라이트업 = 언락 순간용으로 유지(미세 조정만).
- 상태: 컨셉 기록만(코드 무변경) · send_later 1시간 예약으로 v3 제작 재개.

## 38차 — 잠금안무 플그 v3: ⑤ PIN 웜업 신규 축 (운영자 260706 "지금 바로" — 예약 취소·즉시 제작)
- ⑤ PIN 웜업 노브 6{웜업 ON/OFF · 캐릭터 실체화(소년 brightness 1.25 + contrast 1.05) · 원 광량(코어 brightness 1.35) · 원 범위(scale 1.05) · 브랜드 호흡 주기(광량 왕복 wuBreath 2.6s — 기존 ymcPulse와 병행 = 이중 호흡) · 차오름(램프 1.4s)} — strike(로고 낙하)부터 점진 점화 · calm(PIN 대기)에서 호흡 지속 = "로고가 꽂히며 캐릭터에 숨이 불어넣어지는" 서사 구현.
- 프리셋 재편: 현행 / 웜업+라이트업 시안★(풀 서사) / 웜업 강(각인 최대). ④ 라이트업 유지(언락 순간용).
- 검증: 자가검증 경로=Playwright(웜업 ON/OFF computed 대조 — 소년 brightness(1.25) contrast(1.05)·원 brightness 호흡 중간값 1.28·scale 1.05·애니 목록 ymcPulse+wuBreath 병행 확인·PIN 대기 실스샷·JS 에러 0). 신규 축 = 시안만(실물 배선 = 운영자 선택 후).
- 경위: 55분 자가 재개 예약(trig_01Mb6uAietZNJBLSqp3RWpe9)은 운영자 "세션 걸지 마 지금 해" 지시로 삭제.

## 39차 — 잠금안무 플그 v4: ⑤ 웜업 = 오버레이 걷힘으로 재설계 (운영자 260706 "레포 배경(main_v2)만큼 또렷하게")
- 이해 교정: 웜업의 본질 = brightness 필터가 아니라 **67% 잠금 오버레이가 로고 꽂히는 순간부터 걷혀 원본 포스터 명료도 노출**. 핵심 노브 = 딤 잔량(0 = 원본 그대로 ~ .67 = 현행) + 차오름 램프. 보조 = 캐릭터 추가 밝힘(기본 1 = 원본)·원 광량(기본 1 = 원본색 · 1.1은 라임이 노랑으로 밀림 실측)·원 범위·브랜드 호흡.
- `:not(.unlocked)` 가드 = 언락 페이드 캐스케이드 보존(웜업 규칙 특이도가 이기던 함정 차단). 프리셋 = 현행 / 원본 노출 시안★(딤 0) / 은은 노출(딤 15%).
- 검증: 자가검증 경로=Playwright(PIN 대기 오버레이 opacity 0 실측·언락 후 0 유지·원본 명료 스샷·JS 에러 0).

## 40차 — PIN 웜업 + 언락 라이트업 실물 배선 (운영자 260706 조정값 전송 유실 → 서사 기반 추천값 선진행 · §운영 태도 d)
- ⑤ 웜업: `.ymain.strike .ym-ovl{opacity:0; transition:opacity 1.4s}` — 로고 꽂히는 순간부터 딤 걷힘·잔량 0(원본 포스터 명료도) · `.ymain.calm .ymc{scale:1.03}` + ymWuBreathe 2.6s(광량 1↔.88 호흡 · 원본색 기준 아래로 숨쉼 — 1 초과 = 라임이 노랑 밀림 실측) 기존 펄스 병행.
- ④ 라이트업: `.ymain.bye .ym-stage{filter:brightness(1.6)}` .45s 램프 — 승인 순간 발광 후 zap 지지직과 함께 전환(메타버스 진입).
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실물 실측(strike 후 ovl 0·scale 1.03·ymcPulse+ymWuBreathe 병행·PIN 화면 원본 명료 스샷·JS 에러 0) + 머지 후 read-back.
- 미세조정 대기: PIN 라벨·슬롯 가독(딤 0 위) — 운영자 실기기 판정 후 잔량 10~15% 또는 국소 스크림으로 1회 조정 가능.

## 41차 — 잠금 안무 선택값 최종 배선 + PIN 부상 버벅 수정 (운영자 260706 스샷+텍스트 재전송분)
- ③ 조정: 승인 글리치 .55→.25s(JS 제거 타이머 600→310) · 전환 시점 800→1000ms · 네비·로고 .45→.25s·네비 시차 +.15s · 행 간격 .09→.02s(사다리 재계산) · 계단 쌓기 = 언락 후 1.15s 지연(setTimeout yStair)·웨이브 GAP 80→120ms.
- ④ 라이트업 확정: 세기 1.6→1.35 · 시간 .45→.15s · 시점 +60ms(transition-delay).
- ⑤ 웜업 확정: 원 광량 1→1.33(정적 + 호흡 1.33↔1.01) · 호흡 2.6→2.4s · 차오름 1.4→.8s · **원 범위(scale 1.03) 폐지** — 선택값 1.005 ≈ 0 + PIN 부상 버벅 주범(대형 원 scale 전환이 PIN 글래스 backdrop-blur 뒤에서 매 프레임 재계산 · 운영자 "투명하게 올라와 자연스럽게 머무르면 됨").
- 검증: 자가검증 경로=check_refs rc=0 + Playwright computed 실측(ovl 0/.8s·scale none·ymcPulse+ymWuBreathe 3.2/2.4s·스테이지 .15s@.06 전 값 일치·JS 에러 0) + 머지 후 read-back. 버벅 해소 여부 = 운영자 실기기 판정 대기(사람 감각 유일 축).

## 42차 — PIN 입력·승인 연출 전면 개편 (운영자 260706 OS 잠금 결 기준 첨부 + 상세 지시)
- 패드 투명화: 상자·구분선·글래스 blur 폐지 = OS 잠금화면 결(첨부 기준 — 배경 전부 비침·숫자만).
- 슬롯: 입력한 것만 **채워진 라임 원**(* 폐지) · 실숫자 노출 450ms→**1초** 후 원 복귀 · grid center = 중앙 높이 정렬 · 입력 순간 통통(lkPop .3s 촤르륵 커브) · 키 탭 = 라임 점등+통통(lkKeyPop).
- 승인 연출: 4원 **순차 라임 파동**(왼→오 .08s 계단 · lkOkWave) + 라벨 **가운데→우측 퇴장**(ymBandWipe from 0 — 왼쪽 화면 밖 재진입 폐지) + **로고 전격 1회**(ymLogoZap .5s — ymBurstWm 지지직 창 압축) 병행 → 기존 라이트업+zap+전환(1000ms).
- 진동 정책: 자물쇠 탭·키 입력만 유지 — 승인(18)·실패([60,40,60])·언락 강진동([80,60,160]) 제거(운영자 "그 외에는 진동 아님").
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{키 3입력 = 패드 투명·숫자 노출·pop 3 · 승인 상태 주입 = lkOkWave 순차 딜레이·ymBandWipe .5s·ymLogoZap 배선 · JS 에러 0} + 머지 후 read-back. 승인 파동 실통과 체감 = 운영자 실기기(정답 PIN 필요 축).

## 43차 — PIN 미세조정 + 탭 하이라이트 전역 봉합 (운영자 260706 영상 2편+스샷 3장)
- 숫자 노출 완전 폐지(운영자 "4자린데 그때그때 보이니까" — 잔존 문제 원천 제거) · 'PIN 입력' 텍스트 라벨 폐지(aria-label 대체).
- 스포티파이 블랙 10% 스크림: 핀칸(.lk-slots) 알약(--r-pill·패딩 13/26) + 키패드(.lk-pad) --r-l — nomute 잠금 결(첨부) 계승 · 빈 링 시인성은 스크림 후 재평가(운영자 보류).
- **탭 하이라이트 전역 봉합**: `* { -webkit-tap-highlight-color:transparent }` — 자물쇠 탭 시 파란 사각 잔상(운영자 스샷) = 개별 선언 방식의 누락(22곳 중 15곳 무방비 · call.js 주입분 0곳) → 전역 1규칙로 전수 해소.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측(스크림 rgba/radius·on 슬롯 content 빈+라임 채움·lk-m 부재·ym-access tapHighlight transparent·JS 에러 0) + 머지 후 read-back.

## 44차 — 승인 다이브 + 자물쇠 크랙(해킹 서사) (운영자 260706)
- 라벨 재등장 폐지(bye ymBandWipe 규칙 제거 — go 퇴장 상태 유지 · 키프레임 존치).
- 승인 다이브: 라이트업(정적 1.35) 폐지 → ymDive 1s{타격 스파이크 1.45 @12% → 캐릭터 눈 방향(48.8%,36%) 이징 줌 scale 1.9 + brightness .12 어둠} · zap(VHS) = 다이브 중반 500ms 발동(끝날 쯤 1000ms 메뉴 전환) = "메타버스 로그인".
- 자물쇠 잔류·크랙: pin-mode 퇴장 폐지 → 슬롯 위(47.5%)로 부상·축소(72→46px)·숨쉬기 정지 · 입력마다 지지직(ymLockGlitch)+크랙 1~3단(기울기·밝기 점증 · 백스페이스 = 복구) · 오답 = 지지직 후 복구 · 4개째 = 박살(ymLockBreak 팝→소멸) — 매트릭스 결 해킹+박살.
- reduced-motion 가드 확장(다이브·크랙·박살 무효). 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{잔류 46px·크랙3 transform·다이브 origin 눈 좌표·ymLockBreak·라벨 ymBandOut 유지·JS 에러 0·다이브 중반 스샷} + 머지 후 read-back.

## 45차 — PC 파비콘 신설 + 잠금 로고 전면 (운영자 260706)
- PC 파비콘: 데스크톱 브라우저 탭 아이콘 누락(index엔 apple-touch-icon[iOS]·manifest[안드로이드]만) → `viewer/favicon.ico`(512 png에서 16/32/48/64 다중해상도 생성) + head `<link rel=icon>` 2종(ico 32 + png 192). thumb/ly/k/comp = 도구 뷰어(공유 링크 무관) = 대상 외.
- 로고 전면: `.ym-stage .ym-wm` z-index auto→4 = 대형 워드마크가 소년(.ymb z-auto=0) 앞으로(운영자 "로고가 머리 앞으로" · 옛 포스터 층위[머리카락이 로고 하단 가림] 폐지). 소년·플래시·밴드 z 미지정 확정 = 로고가 전부 앞.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright(로고 computed z:4·파비콘 링크 2종·박스 y153 이마 위치·JS 에러 0). 글래스 워드마크 정적 대비는 실기기 글리치 피크에서 부각(헤드리스 정적 한계 명시).

## 46차 — 페르소나 게이지 4계층 34축 개편 (운영자 260706 "인간 범주가 기본으로 + 태도·관계·세계관 몰입도/자유도 우선순위 카테고라이징")
- 대상 = docs/reports/260706_페르소나시트_v1.html. 16축(대화 스타일4·감정태도5·관계4·세계관3) → **4계층 34축**: ① 기본 = 사람 자체의 사양(신설 12 — 지능(지력)·지혜(통찰)·공감력·유머 감각·자신감·성실성·외향성·개방성·감정 기복·도덕성·관능(색기)·성적 개방성) ② 태도 = 기존 9축 통합(말수·답장 길이·어투 강도·장난기·친절도·온기·인내심·삐짐질투·츤데레 낙차) ③ 관계 = 기존 4 + 신설 3(독점욕(집착)·의존성·주도권(리드)) ④ 세계관 몰입 및 자유도 = 기존 3 + 신설 3(설정 몰입도·화제 자유도·메타 허용(4벽)). 성적 성향 = 2축 분리 설계(스스로 풍기는 텐션 ↔ 그 화제를 받아주는 정도).
- 하위호환: 기존 data-k(g0~g15)·localStorage KEY(yeta_persona_v1_) 불변 = 운영자 기존 조정값 전부 승계(신설 = g16~g33 신규 키). 신설 144값(8캐릭터×18축) = 각 카드 서사 근거 초기값 세팅. 내보내기 = 평면 나열 → 카테고리 그룹핑 출력. UI = 2열 그리드(모바일 1열) + 전 축 hover 툴팁(0/10 앵커 정의).
- 검증: 자가검증 경로=check_refs rc=0 + 승계 diff(기존 128값 전부 일치 실측) + Playwright 실측{272 슬라이더 렌더·조정 저장·리로드 복원·레거시 키 복원 OK·export 4계층 그룹 출력·데스크톱/모바일 스크린샷·JS 에러 0}.

## 47차 — 웹앱 게이지 배선 실태 조사 + 선택 플레이그라운드 (운영자 260706 "제한사항은 코드에 없이 문서에만 의존" + "선택을 플레이그라운드로")
- 조사 결론: 콘텐츠 제한(수위·금기) = 이미 문서 정본(00_지침 §안전 경계 + 각 카드) — 웹앱 코드에 없음. 갭 = **축 정의(16축 라벨)가 코드 2곳 하드코딩**{viewer TUNE_AX · yeta_chat.sh AX — 순서 결합} + roster tune 16 기본값(옛 6인) + 게이트웨이 길이 16 검증. op tune "숫자만 수용" = 인젝션 차단 보안 설계(유지).
- 선택 도구: AskUserQuestion 버튼 2회 전송 실패(스트림 닫힘) → 운영자 지시로 플레이그라운드 HTML 신설 = docs/reports/260706_웹앱게이지배선_선택플레이그라운드.html. 5요소{①배선 실물 다이어그램(프리셋별 전환·하드코딩 배지↔SSOT 점선) ②프리셋 A~D(A=시트 확정 후 한 번에 ★) ③주입 실물 재현(러너 band 로직 동일 재현 · 대표 6축 · 16축 모드 = 신설축 "주입 불가" 갭 시각화) ④미세조정 4축(파일 포맷·뷰어 UI·roster 이관·게이트 검증 — 기틀 ⚠딱지) ⑤선택값 조립+복사}. 실제 반영 = 운영자가 복사값 붙여넣으면 기틀 승인으로 처리.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{프리셋 전환·D 잠금·시뮬레이터 band 5구간(낮게/높게/중립 생략/극단)·16축 갭 표시·슬라이더 실시간·복사 사양 텍스트·localStorage 복원·모바일 풀페이지 스샷·JS 에러 0}.

## 48차 — 3계층 정책 신설: 수위·금기 = 웹앱 설정 + 채팅 상한 폐지 (운영자 260706 지시 · 기틀검증 5인)
- **3계층**(정의 정본 = `apps/yeta/policy.json` 신설): L0 코어 = 불변{절대선 4종(미성년 성적 맥락·자해조장·실존비방·범죄조력) + 메타발화·헛소리방지·출력계약 — 관리자 인증 전까지 레포 커밋만} > L1 시즌 = 수위·금기 4축×3단{성적 표현·욕설·폭력·어두운 주제 — 웹앱 설정} > L2 캐릭터 = 성향(기존 tune). 운영자 분류(low=품질 절대 지장 / middle=시즌 테마 위협 / front=캐릭터 고유성) 반영.
- 배선: 뷰어 설정 탭(메뉴4 신설 — `.ysetting` 패널·`.yset-chip` 3단 세그먼트·armed 2탭 복원·전부 기존 정본 계승·CII 행 등재) → op `policy`{GET=정의(raw policy.json)+현재값 / SET=enum 정수만·8축 캡 조기 break} → `sess.policy`(R2·reset 승계) → 러너 `POLICY_BLOCK`{policy.json 문구 조립·기본값 축 생략·카드 뒤 TUNE 앞 주입}. 00_지침 v4 = §안전 경계를 절대선(L0)/시즌 수위(L1 위임)로 재편 — 기존 규칙 유실 0(diff 실측), "노골적 성행위 묘사"는 거부 5종 → L1 기본값(완곡 처리·sexual=2면 성인 허용)으로 이동.
- **채팅 상한 폐지**(운영자 "테스트해야 함·변수 불필요"): send의 `YETA_MAX_PER_DAY` cap·429·remain 전삭(기본 0=무제한이었으므로 리스크 델타≈0 — 보안 검증가 실측) · 관측 카운터 유지 · ring 3/phone 2 유료 상한 불변. CLAUDE.md op 15종·3계층·env 절 갱신.
- 검증: 기틀검증 5인 발사 — 게이트웨이(APPROVE·엣지 시뮬 전통과)·파이프라인(APPROVE·이중 직렬화 왕복 실측·policy.json 커밋 포함 조건)·보안(APPROVE·클라이언트발 텍스트→프롬프트 경로 0·L0 우회 구멍 0·break 권고 반영). 뷰어·문서 2인 = API 세션 한도 조기 종료 → 잔여 항목 메인 직접 실측 완결{armed 2탭 계승 발산 발견→수정→Playwright 재검증 통과 · XSS esc() 전경유 · 리스너 위치 · 00_지침 유실 0 · policy.json 4축 일관 · ROOT 정의 · phone/ring cap 분리}. 자가검증 경로=check_refs rc=0 + bash -n + node 구문 + POLICY_BLOCK 단위테스트(오버라이드 조립·전축 기본 생략) + Playwright 목킹 E2E{설정 탭 렌더 3섹션·칩 토글·dirty 배지·디바운스 SET 페이로드·복원·localStorage 아닌 세션 저장·JS 에러 0}. 유료·상태변경 프로브 0(전부 목킹) · 라이브 실측 = main 머지 후(게이트웨이 GET이 main raw 참조 — roster 동일 패턴).

## 49차 — PIN 권한 2계층 + L0 분류 재구조화 (운영자 260706 "2486=슈퍼관리자→L0·L1 / 7890=게스트→L2만 · 사용자 DB는 깃에")
- **권한 설계**: 슈퍼관리자 PIN = Pages env `YETA_PIN_ADMIN` 단 1개(공개 레포에 4자리 해시 박제 = 역산 = 서버 강제 무력화라 env가 유일 안전 축) · 게스트·일반 사용자 = `apps/yeta/users.json` 신설(깃 SSOT — 사용자 추가/삭제 = 커밋 한 방·플랫폼 무관·해시 박제 = 낮은 권한 전용 · users.json의 admin 참칭은 게이트웨이가 차단). 뷰어 잠금 판정 = 클라 해시(YM_PIN_H) 폐지 → **op `auth` 서버 단일 경로**(admin=env 대조·guest=users.json sha256 대조 · 네트워크 실패 = 오답 카운트 없이 안내 · 역할·PIN = 메모리만 = 새로고침 재입력 규약 유지).
- **L0 분류 4그룹**(policy.json v2): 절대선(존립·하드)·출력 계약(기계·하드) = 토글 불가 / 메타 차단·헛소리 방지 = **관리자 웹앱 토글**(해제 문구 = policy.json 정본 · 러너 POLICY_BLOCK이 L0 토글+L1 축 통합 조립·prompt 인덱스 가드). 00_지침 = 메타발화·헛소리 절에 `[운영 정책]` 위임 카브아웃 + 블록 명칭 `[시즌 수위·금기]`→`[운영 정책 — 관리자 설정]`.
- **게이트웨이**: op `auth` 신설(PIN 형식 검증 4~8자리·반환 = 역할뿐) · op policy SET = **admin PIN 필수**(미설정 501 안내·오답 403) · 뷰어 설정 탭 = 역할 게이팅{admin = L0 토글+L1 편집+armed 복원 / guest = 전면 read-only+배지} · nav 설정 버튼 "준비중" 토스트 버그 수정(지난 커밋 미완 — yTab만 추가하고 nav 핸들러 미배선이었던 것).
- 검증: 자가검증 경로=check_refs rc=0 + bash -n/node 모듈 구문/json 파싱 + 러너 단위테스트{L0 토글+L1 혼합 조립·전축 기본 생략·범위 밖 인덱스 가드} + Playwright E2E{admin: 자물쇠→PIN 2486→언락·역할·SET {p,pin} 동봉 / guest: 7890→read-only 칩 disabled·리셋 없음 / 오답: 카운트·언락 차단} + 게스트 화면 스샷. 라이브 = main 머지 + 운영자 env 설정 후.

## 50차 — 분신술 10인: 대화 제한사항 전수 탐색 + 잔재 정리 (운영자 260706 "철저 파악 · 달라진 것 일일이 보고 · 누락 금지")
- 10인 병렬(READ-ONLY·전원 최상위) 앵글 = 게이트웨이 send·러너 프롬프트·지침 문서·모델 타이밍·세션 기억·뷰어 UI·음성 축·인프라·정책 잔재·종합 크리틱 — 발견 총 311항목. 보고서 = docs/reports/260706_분신술10인_대화제한전수.html(함정 TOP·앵글 요약·후속 후보 전수).
- 분신술발 즉시 변경 7건: ①설정 리셋 2탭 armed 계승 ②policy SET 8축 조기 break ③policy.json 커밋 위생 ④yeta.js 헤더 현행화(op 16종·죽은 YETA_MAX_PER_DAY 서술 삭제·허구 Access 서술 정정·YETA_PIN_ADMIN 등재) ⑤CLAUDE.md L0 자기모순 해소 ⑥TUNE 헤더 L1>L2 클램프 한 줄 ⑦quota 카운터 용도 주석.
- 핵심 미조치 발견(후속 대기): awaiting 영구 고착 리퍼 부재(P1)·웹푸시 구독 원장 부재 = 실질 무발송(P1)·에러 문구 시스템 내부 노출 = 몰입 파괴(P1)·전역 단일 세션(사용자 분리 미배선)·ring 쿼터 선차감 비대칭·worlds.json 장식화·16축 라벨 2벌 드리프트·narration 모순.
- 검증: 자가검증 경로=check_refs rc=0 + bash -n/node 모듈 구문 + rogue 사후점검(브랜치·커밋 무오염).

## 46-B차 — 승인 다이브 매트릭스 형상 (운영자 260706 "들어가면서 사람 안 형상을 매트릭스로")
- `.ym-matrix` canvas 레이어(스테이지 별도 = 다이브 어둠 brightness .12 안 먹혀 코드 발광 · mix-blend screen · z:3 · 눈 방향 48.8/36 줌 동기 ymMatrixDive). 실루엣 클립 = 소년 알파 CSS 마스크(`-webkit-mask/mask` center/contain = .ymb 동일 정렬 · canvas 내부 destination-in은 file:// cross-origin taint로 폐지). canvas는 풀 초록 코드 레인(카타카나+숫자 · 머리 흰빛+라임 꼬리 8글자 · 시작부터 실루엣 채움 · ~1.15s 후 자동 정지 rAF 회수). 색 = var(--accent) 토큰 계승(폴백=CSS 키워드 lime · raw 강조색 금지). 트리거 = 승인 성공 시 document 'yeta:dive' dispatch.
- reduced-motion = display:none. 검증: 자가검증 경로=check_refs rc=0 + Playwright(레인 1161→조밀 px readback·마스크 실루엣 클립·다이브 동기 줌/암전 스샷·JS 에러 0).
- 후속(운영자 이미지 제안): 다리 풀버전 컷아웃(현 yeta-main-boy.webp 다리 94% 잘림) = 운영자 제공 시 즉시 교체 · 머리카락 전면 겹침 = 후속.

## 47-B차(프리뷰·미머지) — 영상 히어로 잠금 전환 (운영자 260706 "영상 깔고 로고 글래스모피즘·라벨 제거·60fps·처음 흐림→풀면 원본")
- 운영자 업로드 영상(최상위 `1c691a18-…mp4` 720×1280·24fps·사이버펑크 소녀) → 60fps 모션보간(ffmpeg minterpolate mci·aobmc·vsbmc) + CRF24 웹압축(3.7→5.3MB·faststart) → `viewer/assets/brand/lock-hero.mp4` + 포스터.
- 잠금 스테이지 = `.ymain.video-lock` 모드: `.ym-video`(object-fit cover) 베이스 + 원(.ymc)·소년(.ymb)·라벨(.ym-band) 숨김(라벨 들어오고 나가는 것 제거) · 대형 로고 strike 유지 + `.ywm-glass` 프로스티드 백플레이트(글래스모피즘) · 매트릭스 풀프레임(소년 실루엣 마스크 해제) · 다이브·zap·크랙·ym-ovl 스크림(처음 흐림→strike서 걷혀 원본 선명)·PIN 그대로.
- ⚠️ 미머지 = 프로덕션(소년 합성)은 main 유지 · 브랜치 프리뷰 배포로 운영자 실기기 판정 대기. 후속: 로고 글자 영상 위 가독(글래스 판 다크틴트)·영상 하의 글리프 확인·루트 업로드 정리.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright(video display block·소년/라벨 display none·strike서 스크림 opacity 0·글래스 판 등장·JS 에러 0 · 헤드리스 h264 미디코드로 영상 모션은 실기기 검증).

## 47-B차-b(프리뷰 갱신) — 영상 잠금 로고 = 판 폐지·반투명 30%+블러 (운영자 260706 "옆에 저거 지우고 로고 반투명·블러 살짝·불투명도 30%")
- `.ywm-glass` 백플레이트 판 마크업·CSS 제거(운영자 X 표시).
- 로고 글래스모피즘 = 글자 자체에: `.ymain.video-lock .ym-stage .ym-wm { filter:blur(.8px) opacity(.3) }` — filter opacity()가 flicker/strike 애니 opacity와 곱해져 애니 안 깨고 ≈30% 감쇠 + 살짝 블러.
- 검증: check_refs rc=0 + Playwright(글래스 판 부재·로고 filter blur.8/opacity.3·JS 에러 0). 미머지 프리뷰 유지.

## 47-B차-c(프리뷰 갱신) — 영상 잠금 로고 60%+네온라임+전격 자주 (운영자 260706)
- 로고 불투명도 30→60%(filter opacity .6) · 네온라임 글로우 drop-shadow(0 0 5px rgba(var(--accent-rgb),.6)) 추가(일부 라임 조금) · video-lock 정지 flicker 4s→1.8s·버스트 5.6s→3.2s(배경 화려 → 전격 자주).
- 검증: check_refs rc=0 + Playwright(filter opacity.6+라임 drop-shadow·anim 1.8/3.2s·JS 에러 0). 미머지 프리뷰.

## 47-B차-d(확정·머지) — 영상 잠금 로고 하강+불투명도 상향, 영상 히어로 정본 전환 (운영자 260706 "낮춰서 바로 가자")
- 로고 위치 top 19.7→31%(얼굴/눈 벗어나 가슴께) · 불투명도 .6→.7 · 네온라임 글로우·전격 자주 유지.
- 운영자 승인("훨 낫네·이대로 가면될듯·바로 가자") → 영상 히어로 잠금 = 프로덕션 정본 전환(#82 머지). 소년 합성 잠금 폐지(video-lock 모드 상시).
- 검증: check_refs rc=0 + Playwright(top 31%·opacity .7·JS 에러 0 · 영상 모션·로고 겹침은 실기기). 후속 nudge = 운영자 라이브 판정.

## 48-B차 — 소년 잠금 코드 제거 + 파비콘 얼굴 교체 + 영상 압축 (운영자 260706)
- 소년 코드 폐지: 마크업(.ymc·.ymb g1/g2/main·.ym-band) + CSS(.ymc·.ymb·.ym-band 규칙·ymcIn/Pulse/Hit·ymbIn/Holo/Vhs/InG/GlA/GlB·ymHit·ymBurst·ymBurstG·ymBand In/Out/Wipe·워밍업 .ymain.calm .ymc·ymWuBreathe) + reduced-motion 소년 참조 + 프리로드 + 애셋 yeta-main-boy.webp(git rm). 공유 잠금 기계(strike 로고·ym-flash·ymZoom·ymDive·zap·크랙·ymBurstWm·ymLogoZap·matrix)만 존치. 매트릭스 = 소년 실루엣 마스크 폐지 → 풀프레임 코드(영상 위 전체 디지털화). 프로필 아바타(yeta-face.webp) 유지.
- 파비콘 = 영상 캐릭터 얼굴(t=2 프레임 얼굴 크롭 → favicon.ico 16/32/48/64 + yeta-face-icon-192.png · icon 링크 2종 교체). apple-touch/manifest 아이콘은 존치(운영자 "파비콘" 한정 · PWA 설치 아이콘 교체는 후속 선택).
- 영상 용량: 60fps 유지·640폭·CRF28 재압축 = 5.3→2.58MB(품질 유지 · 운영자 "품질 유지되면 줄여도됨"). 하의 글리프 = 운영자 "없는 얘기".
- 검증: check_refs rc=0 + Playwright(ymc/ymb/band 부재·영상/로고/파비콘·매트릭스 풀프레임·다이브·소년/옛아이콘 404 0·JS 예외 0 · file:// CORS는 실HTTPS 무관). 영상 모션 선명도 = 실기기.

## 49-B차 — 로고 스타일 플레이그라운드 (운영자 260706 "로고만 다듬으면 · 반사 무채색 마스킹 · 내부 광원 · 주변 글래스모피즘")
- 시안: `docs/reports/260706_로고스타일_플레이그라운드.html` — 영상 프레임(전신) 위 YETA 워드마크(실물 6겹 재현) + dial 8{불투명도·반사 무채색화(크로마틱 라임/코발트 saturate 감쇠 + 글로시 끝단 색 흰↔라임 보간)·내부 광원(글자 안 라디얼 발광)·주변 글래스(둘레 backdrop-blur 헤일로)·라임 글로우·헤일로·스캔라인·블러} + 프리셋{현행 글리치 · 포스터 글로시★ · 글래스 글로우} + 배경 프레임 토글 + 선택값 복사.
- 운영자 3요구(무채 반사·내부 광원·주변 글래스) 전부 dial화 = "가능한가?" 답 = 셋 다 가능·실물 배선 대기.
- 검증: check_refs rc=0 + Playwright(프리셋 3 렌더·JS 에러 0). 시안만(viewer 무변경).

## 50-B차 — 로고 스타일 선택값 배선 (운영자 260707 플그 회신 "불투명도 .92 · 광원 .75 · 글로우 0 · 헤일로 0 · 스캔 1 · 블러 0")
- 배선(video-lock 잠금 대형 로고 한정): 통 filter `blur(.8px)+drop-shadow(라임 5px)` 폐지 → `opacity(.92)` 단독(또렷) · `.w-halo` display:none(좌상단 #ywm 헤일로는 유지) · `.w-light` 내부 광원 레이어 신설{조립 6겹→7겹 · 라디얼 흰 .64→라임(--accent-rgb) .3 · 기본 opacity 0 = 좌상단 무영향, 잠금만 점등}.
- 스캔라인 선택 1 = 무변경 판정: 플그 재현이 레이어 opacity .5로 실물(1)보다 약했음 → 선택 1 × 통 .92 = 실물 기존 강도(.26 알파)와 체감 동치 실측(곱 검산 .2392 일치).
- 반사 무채색화 0·주변 글래스 0px = 현행 유지(무배선). 운영자 후속 아이디어(글자 일부 영역별 불투명도) = 머지 후 검토 예약.
- 검증: check_refs rc=0 + Playwright computed{filter opacity(0.92)·레이어 순서 7겹·잠금 halo none/좌상단 flex·잠금 light 1/좌상단 0·그라데 207,255,64} 자가검증 경로=tool(playwright).

## 51-B차 — 진입 다이브 플레이그라운드 (운영자 260707 "진입 효과 최대한 세분화 · 매트릭스 더 고급지게")
- 시안: `docs/reports/260707_진입다이브_플레이그라운드.html` — PIN 승인→다이브→매트릭스→zap→언락 전 구간 dial 24{①다이브 7(줌·시간·타격발광·발광시점·최종어둠·원점XY) ②매트릭스 기본 6(열·속도·꼬리·불투명·등장·지속) ③매트릭스 고급 9(깊이 레이어 1~3층 파랄락스·헤드 광채·코드 글로우·글자 뮤테이션·속도 편차·꼬리 커브·색 심도·성김·밝기 지터) ④전환 3(zap 시점·지속·언락)} + 프리셋 4{현행·시네마 매트릭스★·딥 다이브·미니멀 코드} + 배속 재생(0.25/0.5/1×·WAAPI playbackRate)+반복.
- 재현: 실물 레인 로직 이식(GLYPH·DPR·낙하식) + PIN 4원 파동·자물쇠 박살·zap 키프레임(ymZapA/B) 복사 · 영상 정지 프레임+레터 마스크 base64 임베드. 로고 스타일(글자 영역별 불투명)은 운영자 "지금이 완성형" = 취소.
- 검증: check_refs rc=0 + Playwright{JS 에러 0·프리셋 4 실클릭·레인 픽셀 20k+·zap/brk 클래스 동시 구동·dial→선택값 병기 갱신·자동 리셋} 자가검증 경로=tool(playwright).

## 52-B차 — 룰 3층 아키텍처 확정 + 핵심룰(Tier 1) 고정 (운영자 260706 "핵심룰/세계관 룰/인물 룰 3개로 구분 · 핵심룰 앱 고정")
- **3층 구조 명문화**: ① 핵심룰 = `apps/yeta/00_지침_캐릭터챗.md`(전 시즌 공통·불변) · ② 세계관 룰 = `10_세계관.md`(시즌 N) · ③ 인물 룰 = `characters/<id>.md`. 주입 순서(00→10→카드)가 이미 3층과 일치 = 파일명·inject_character.sh 무변경(파일명 정확참조라 rename 회피) · `00` 상단 헤더에 3층 계약 명시(하위층 충돌 시 핵심룰 우선 · 단 말투·형식은 카드 최종우선).
- **핵심룰 = 운영자 Major Rule 1/2/3으로 재조정**: ①-1) 너는 AI 아니다(메타발화 차단 강화 — 강요·추궁·`AI/프롬프트` 키워드에도 페르소나 본질 유지) ①-2) 규격으로 대화 안 좁힘(범죄·다툼·연애·수위·어두운 소재 = 캐릭터 결이면 훈계·거부 금지 · 착한 척=최대 위반) ①-3) 단 개념은 못 넘음(시간여행·우주점프·유저 각성/이세계행 불가 · 판타지는 낮게 샌다=화면 밖·흔적/여운).
- **세계 물성(축소 아님·세계관 결) 최소선**: 전원 성인(미성년 성적맥락 부재) · 실존 인물 = 가상 페르소나 · 현실 복제 위해 매뉴얼 = 캐릭터 지식 밖(날조 금지 계보) · 실제 위기 유저 자해 = 캐릭터로서 진심 걱정(조장 아님). 기존 "🛡 안전 경계" 거부리스트 → 세계 물성 프레이밍으로 전환(몰입 우선).
- **보존**: 기계 파싱 골격 전량 무변경 = 📤 출력 계약(NOTE:PUB/ME·MOOD 4값)·🚫 헛소리 방지(Anti-Parrot·No-Summary·날조/난입 금지)·💬 대화 형식(1~4문장·narration 2트랙)·🎬 나레이션 품질. 출력 포맷 골격=기틀이라 손 안 댐.
- **세계관 룰(Tier 2) 미변경**: 운영자가 준 무음동 아이돌×판타지 리부트 세계관(원영·총명·한바로·원빈·원이·고죠 + 핵심룰 ①②③④ + 거점)은 9인 로스터 리부트와 커플링 → `10_세계관.md`(현 라이브 6인) 덮으면 로스터(류/백)와 불정합 → 리부트 반영 시점 확정 후 일괄. 이번엔 Tier 1만.
- 검증: 자가검증 경로=check_refs rc=0(⚠️ --line2·--pin-ac-rgb 미사용은 기존·md 편집 무관·비차단) + 머지 후 origin/main read-back. 콘텐츠 문서 재정비(코드·출력포맷 골격 무변경) = 자가검증 트랙.

## 53-B차 — 챗 진입 촤르륵 애니 + 뷰어 인터랙션 실측 스윕 (운영자 260706 "메뉴2→3 넘어가는 게 어색·촤르륵 · 각 대화상대 누를 때 먹히는지")
- **메뉴2→3 어색함의 정체 = 챗 다이얼로그 진입 애니 부재**: `#yetadlg`가 `showModal()` 즉발 팝(애니 0) → 목록/지금 탭→챗이 툭 튐. **원샷 `.ydlg-in`**(openYeta가 열 때만 부여·720ms 후 회수 = 내부 뷰 토글 재트리거 방지) 신설: 창·백드롭 페이드(ydlgIn/ydlgBd .26/.3s) → 헤더·무대·입력 계단 부상(ydlgUp .44~.46s @.05/.12/.19 stagger · `--ease` 계승 · ypickIn/ynavRise 결). reduce=즉시. 닫힘 핸들러도 클래스 회수(안전).
- **오진 정정**: 챗 하단 라임 계단 = 버그 아님 = **다이얼로그 배경 `yeta-bg.webp`(설계된 라임 글래스 계단 포스터)**. `.ystair`(CSS 3D)는 openYeta의 `yStair.pause()`로 정확히 `display:none` 걸림(실측) — 별개 축. `ysWorld` 비워도 남던 것 = 배경 이미지라 당연.
- **인터랙션 실측 스윕(Playwright·API 목킹·PIN 언락·JS 에러 0)**: 갤러리 행→디테일→기어(성향설정 16슬라이더)→프리셋→대화시작→챗→메시지 전송→+더보기→뒤로닫기→설정탭 토스트→메뉴3 재진입 = **전부 먹힘**. 6인 전원(무디·세라·하은·백·류·윤) 각각 대화상대 클릭→챗 진입 정상(하은 = 명함 vcf 시트 자동노출까지 정상). 좌레일(#yside) 화자전환 "안 보임" = 22차 운영자 폐지(display:none!important)분 = 휴면·정상.
- 미검(도구 한계): 라이브 백엔드 지연(draw/send 러너 왕복)은 유료 dispatch라 실측 안 함 — 실기기 체감은 운영자 축(프론트 로직은 무결).
- 검증: 자가검증 경로=check_refs rc=0(viewer JS 구문 OK·⚠️ --line2·--pin-ac-rgb 미사용은 기존) + Playwright 진입 애니 computed 실측(ydlgIn/ydlgUp 발동·헤더 opacity 0.13→1·원샷 클래스 회수·JS 에러 0) + 중간프레임 육안 + 머지 후 read-back.

## 54-B차 — 메뉴3(지금/챗 탭) 폐지 → 챗은 메뉴2 대화 목록으로 편입 (운영자 260706 "3번 내부 대화 = 대화 눌러서 진입 · 메뉴3이 사실 2 안으로 편성")
- **네비 재편 4→3**: `#ynav` = 캐릭터(char) · 대화(list) · 설정(settings). 옛 '지금'(data-tab="chat") 버튼 제거 — 챗 진입은 **대화 목록(메뉴2) 행 탭**으로만(`#ylistRows` 클릭 = openYeta · 기존 배선 유지). nav onclick의 죽는 `chat` 분기 제거(설정만 준비중 토스트).
- **의미**: '지금 대화'는 별도 탭이 아니라 대화 목록 안의 진행 대화 = 눌러서 들어감. 진입 = 47차 촤르륵 그대로 발동.
- 보존: yNavName(옛 메뉴3 라벨 헬퍼)은 타깃 셀렉터 소멸로 무해 no-op(`if(s)` 가드) — 호출부 존치(무churn). ydock(챗 안 도킹)·yTab(char/list)·뒤로 복귀(_yCloseTo||'list') 전부 정상.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{네비=["char","list","settings"]·chat 제거·대화행 탭→챗 open+ydlg-in 촤르륵(hAnim=ydlgUp)·설정 토스트·JS 에러 0} + 네비 3버튼 렌더 육안 + 머지 후 read-back.

## 55-B차 — 성향 잠금(대화 시작 후) + 설정·프로필 화면 목업 + 챗 나가기=진행 대화 복귀 (운영자 260706)
- **① 나가기 = 진행 대화 복귀 확인**: 챗 뒤로(`#yetaBack`) → `yetadlg.close()` → `yTab(_yCloseTo||'list')` = 메뉴2 대화 목록(진행 대화 1행 = draw로 세션.persona 박힌 상대). 진행 상대 = 캐릭터 선택분 그대로 = 기존 로직 정상(실측 통과).
- **② 성향 잠금(운영자 "시작하면 못 바꾸게 · 결 흔들리면 분석 불가")**: `yChatStarted(c)` = 세션.persona===c.id && turns>0 → yShowSet 읽기전용{슬라이더 `disabled`+딤(.45) · 프리셋칩·복원 제거 · 부제 → 자물쇠 "대화 중 — 성향은 이 대화 동안 고정돼"(mut 톤·은은) · 하단 "새 대화 시작하면 다시 조절"}. input·click 핸들러 `_ysetLocked` 가드. **시작 전 = 완전 편집 가능**(메뉴1 세부 기어 = 페르소나/성향 결정 지점 · 운영자 확정). 은은하게(운영자 "너무 강조 말고").
- **③ 설정 화면 목업(운영자 "목업만 · 넣을 것 판단")**: 설정 네비 탭 → `#ycsettings` 풀스크린 글래스 시트(.ycd 계승). 6섹션{알림(답장/소리) · 대화(속도·타이핑·읽음) · 음성·통화(전화·PTT·상한) · 화면(테마·모션·글자) · 데이터(초기화·캐시) · 정보(버전·약관)}. 토글 = 시각 플립만(목업·저장 없음) · 값/이동 행 = 준비중 토스트.
- **④ 프로필 화면 목업(운영자 "프로필도 목업만")**: 우상단 프로필 → `#ycprofile` 시트. 히어로(유저 아바타·무음동 방문자·골목의 창작자·소개) + 스탯3(진행 대화·친밀도·함께한 밤) + 프로필/계정 행 + 목업 표식.
- **컴포넌트**: 전부 기존 토큰 계승(--glass-0·--glass-line-0·--accent·--r-l·--r-pill·--fs-*·--press-l 등 · 신규 :root 토큰 0). 신설 = `.yset-item`(설정/프로필 글래스 행 = .ylist-row 결)·`.si-sw`(미니 토글 목업)·`.yprof-*` — CII 등재 후보(운영자 확정 시). 아이콘 = 인라인 SVG(닫기X 문자 게이트 무관).
- 검증: 자가검증 경로=check_refs rc=0(viewer JS 구문 OK·신규 토큰 0·⚠️ --line2·--pin-ac-rgb 기존) + Playwright E2E 9종{프로필 열림/닫기·설정 열림/토글 플립/닫기·시작 전 편집가능(칩3·미잠금)·챗 진입·나가기=메뉴2 진행대화·시작 후 잠김(disabled·칩0·locked·잠금문구) · JS 에러 0} + 3화면 육안 + 머지 후 read-back.

## 56-B차 — 언락 배경 = 계단 폐지 → 운영자 영상 (운영자 260706 "계단 대신 이걸로 · 적당히 검정 + 글래스 위 · 가능하면 60FPS")
- **에셋**: 운영자 업로드 mp4(h264 720×1280·24fps·10s) → `viewer/assets/brand/yeta-bgvid.{webm,mp4}`(540×960 · VP9 738KB + h264 595KB 이중 소스) + 포스터 webp(62KB). ffmpeg 6.1(apt 설치 — 번들 pw-ffmpeg는 h264 미지원 스트립빌드 실측).
- **60fps 판정 = 폐기·24fps 채택**: minterpolate(mci·aobmc) 보간 실측 속도 0.046×(10초 영상에 수 분±·러너 타임아웃) + 검정 스크림·글래스 뒤라 체감 이득 미미 → 원본 24fps 그대로(운영자 "가능하면"의 불가 축 보고).
- **배선**: `.ystair`(CSS 3D 계단·마크업+CSS+빌더 JS) 전면 대체 → `.ybgvid`{video cover · `::after` 검정 스크림 rgba(18,18,18,.38) "적당히 검정" · 기존 탭 ::before 딤 병행 · 글래스 카드가 그 위 = 글래스모피즘}. `yStair` = 동일 인터페이스(start/pause/resume) 영상 컨트롤러로 재구현 = 호출부(setUnlocked 1.15s 지연 시작·openYeta pause·닫힘 resume) 무수정 · ys-hold 계약 승계(챗 열림 = 정지+숨김) · visibilitychange = 백그라운드 정지 · reduced-motion = 포스터 정지 · 잠금 화면 = 미재생(autoplay 제거).
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{잠금=paused·언락=재생(currentTime 전진·vw540 디코드)·스크림 .38 computed·챗 열림=paused+ys-hold+display:none·복귀=재생 재개·JS 에러 0} + 메뉴1 렌더 육안(영상 위 글래스 정상) + 머지 후 read-back.

## 57-B차 — 대화 목록(메뉴2) 재구성: NAV 헤어라인·시즌 라인·2줄 미리보기·스와이프 나가기 (운영자 260706 스샷 지시)
- **상단 NAV 구분선**: 로고+프로필 라인(투명 상단 NAV) 아래 초투명 헤어라인(`--glass-line-0` 1px · tab-char/tab-list 공용 · "아주 살짝").
- **시즌 라인(기본형)**: 대화 탭 "대화/진행 중인 대화" 헤더 → `S1. 무음동(無音洞) ｜ 밤이 유난히 긴 골목`(S1.=라임 · 한자 부기 `worlds.json hanja` 신설 · ｜ 전각 · 태그라인 앞토막). 표현 확정 = 시즌 플그(별도 산출) 후 조정.
- **채팅 행 재구성**: 이름 + **최근 대화 2줄**(마지막 2턴 · 내 턴 = `나 ·` 접두 · 운영자 "이 2개만") + **우상단 = 마지막 채팅 시간**(yTime = KST 고정·§표기표준) + 라임 안읽음 번호 배지(기존 유지) — 카톡 뉘앙스(따라하기 아님·틀은 현행).
- **스와이프 나가기**: 행 좌로 드래그(pointer 이벤트 = 모바일+PC) → 글래스 나가기 픽토(72px) 노출. 리서치 관례 반영{시작 판정 10px·|dx|>|dy| 방향 잠금·스냅 임계 = 노출폭 50%·복귀 transition .28s(드래그 중 off)·touch-action:pan-y}. 나가기 = 2탭 armed(`--danger` 톤) → op `reset`(게이트웨이 1세대 백업 보유) → 목록 갱신. **발견 수정**: 드래그를 끝낸 pointerup의 합성 click이 "열림 중 탭=닫기" 분기에 걸려 스냅이 즉시 풀리던 버그 → justDrag 1회 소비로 분리(합성 이벤트 대조 실측으로 규명).
- **분신 리서치 4기**(READ-ONLY·병렬): 게임 시즌 표기 12패턴·OTT/시리즈 12패턴·리스트 헤더 관례 8패턴·스와이프 액션 8패턴 — 시즌 라인·스와이프 수치의 근거 + 시즌 플그(후속) 재료.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 실측{시즌 라인 텍스트·헤어라인 computed·2줄(나· 접두)·KST 시간·스와이프 -72 스냅·나가기 armed→reset 실호출→빈 목록·JS 에러 0} + 렌더 육안 + 머지 후 read-back.

## 58-B차 — 상단 간격 여유 (운영자 260706 "로고와 S1 간격 더 · DIV 간 간격 무조건 여유롭게")
- 패널(ychar·ylist) 상단 패딩 64→88(NAV 클리어런스 64 + 숨 24) · 헤어라인 58→64(NAV 하단 정위치) · 시즌 라인 하단 여백 12→`--sp-3`(18).
- 검증: 자가검증 경로=check_refs rc=0 + 렌더 육안(대화·캐릭터 탭).

## 59-B차 — 시즌 표기 플레이그라운드 v2 (운영자 260707 "S1 무음동사람들 날짜 · 포인트색 = S1 · 레몬밀크 · S1 이탤릭 · 날짜 Regular 연하게")
- `docs/reports/260706_시즌표기_플레이그라운드.html` — 분신 리서치 4기(게임 12·OTT 12·리스트헤더 8·스와이프 8패턴) 종합. 확립 5요소{실물 재현(투명 NAV·헤어라인·시즌 라인·채팅 행)·프리셋 12·노브 19·선택값 복사·리서치 계보 표}.
- **운영자 컨셉 = ★ 기본 프리셋**: S1(LEMON MILK Bold Italic·라임 포인트) + 무음동사람들 + 우측 끝 날짜 2026. 07. 07(LEMON MILK Regular 정방향·mut). L = 라임 필 배지(운영자 첨부 그라데 알약 네비 결). 나머지 = 리서치 계보 10종(라이엇 //·Apex 스택·OW2 콜론·Destiny 이름단독·OTT 축약·제1부·웹툰 [1부]·SEC.01 시스템로그 등).
- 폰트: LEMON MILK 5종 → `viewer/assets/fonts/`(실배선 대비) · 플그엔 4종 base64 임베드(단일 파일 규약 · 라틴 전용 = 한글 자동 폴백이 "S1·날짜만 레몬밀크" 의도와 정합).
- 검증: 자가검증 경로=Playwright{★ 기본 렌더(이탤릭 computed·KST 날짜)·필 프리셋 토글·JS 에러 0} + 렌더 육안. 뷰어 배선 = 운영자 선택값 회신 후.

## 51차 — main #79(핵심룰 Tier1·챗 UX 재편) 병합 + 하드배포 (운영자 260707 "하드배포해봐")
- 병합 원칙: 00_지침 = main 신판(핵심룰 Tier1 · 대원칙 ①②③) 기반 채택 + 내 정책 배선 이식{인트로 3계층 한 줄 · ① 제목 L0 토글 카브아웃 · §🎚 L1 위임(대원칙 ② 정합 — sexual=성인 시 완곡 해제) · §🚫 카브아웃 자동 생존}. viewer = main 라우팅(chat 탭 폐지·yShowSettings 목업) 채택 + 목업 최상단 "시즌 정책" 실동작 진입 행(#ypolEnter — 역할 부제) → 내 3계층 패널 연결(UI 결은 디자이너 목업 확정 후 이식 예정 · 운영자 "그 디자인 보고 판단"). YLOCK_SVG 중복 선언 dedup. 작업이력 = 양쪽 전량 보존(병행 세션 차수 -B 접미).
- 검증: check_refs rc=0(viewer JS 구문 게이트가 중복 선언 실검출→해소) + 병합 통합 E2E{auth 언락(main 재편 위) · 네비 설정→목업→진입 행→정책 패널·목업 닫힘·SET pin 동봉 · JS 에러 0}.

## 52차 — awaiting 고착 리퍼 + 설정 UI 인수 문서 (운영자 260707 "한수 진행 ㄱ" · 분신술 P1 1호 조치)
- op get에 lazy 리퍼: state=awaiting && awaiting_since 10분 초과 = error 플립("응답이 오지 않았어 — 다시 보내면 재시도") → 뷰어 재시도 버튼 활성 = 영구 고착 탈출(dispatch 204 후 Actions 큐 드롭 시나리오). 러너가 뒤늦게 도착해도 finish 앵커 insert가 자연 회복 = 충돌 무해. 임계 600s = 러너 240s 컷 + 큐 여유(거짓 플립 최소화).
- docs/인수_설정UI_기능계약.md 신설 — 디자인 세션 인수: 권한 모델(admin env PIN/guest users.json)·op auth/policy 계약·def 렌더 계약(하드코딩 0)·현 임시 구현 위치·금지 5항. 운영자 "그 디자인 보고 판단" — 시즌 정책 UI 결은 디자이너 목업 확정 후 이식.
- 검증: 자가검증 경로=check_refs rc=0 + node 모듈 구문. 라이브 op 실측 = 컨테이너 아웃바운드 정책이 yeta.soong.kr 차단(CONNECT 403)으로 불가 → 운영자검증 불가피 사유=프록시 차단 도메인(도구로 접근 불능) — 폰 실측 항목 전달.

## 53차 — 분신술 10인: 페르소나 사람다움 아이데이션 (운영자 260707 "얻을 거 더 있나 · 최대한 사람 성향에 가깝게")
- 10앵글{성격심리·상태역학·기억망각·관계역학·언어지문·불완전성·주도성·감정곡선·집단역학·구조로드맵} 전원 레포 실측 후 제안 — 총 71개 아이디어. 보고서 = docs/reports/260707_분신술10인_사람다움_아이데이션.html(수렴 지도·티어별 베스트픽·과설계 경계·전수 원장).
- 수렴 최상(독립 4중): ①MOOD 되먹임 감정 관성(이미 박제된 데이터 재사용 = 비용 0 — "매 턴 감정 리셋" 봇티 제거) ②L5 STATE 실장(시각·계절·경과 — 캐릭터 완전 시간맹 실측 · 선행 = 10_세계관 "여름밤" 하드코딩 완곡화) ③L4 HOOK BANK(랜덤 떡밥 2~3장 = 반복성 해소 + 토큰↓). 3중: sess.rel 관계·감정 상태 구조화(리페어 아크 V자·반감기). 버그 발견 1: HIST 화자 귀속 뭉개짐(타 캐릭터 대사가 전부 "너:"로 — turn.persona가 있는데 버려짐).
- 과설계 경계(반론 채택): 34축 전량 런타임 배선 금지(기본축은 카드에 굽기)·감정 결정론 금지(소프트 힌트만)·cron 선톡 금지(주도성 = 컨텍스트 반응)·기억 과구조화 금지·오기억/번복 유보.
- 검증: 전원 READ-ONLY·rogue 사후점검 무오염·check_refs rc=0. 다음 = 운영자 픽(추천 1탄 = T0 묶음: 감정 관성+STATE_BLOCK+화자 귀속+태도 비대칭 — 스키마 무변경).

## 61-C차 — 시즌 subnav 실물 배선 (운영자 260707 플그 선택값 + 보정 5)
- 선택값 배선: `SEASON 01`(LEMONMILK Bold Italic·라임 필 배지 — 플그 ★+L 조합) + `무음동(無音洞)` + 우측 끝 날짜 `26. 07. 07`(LEMONMILK Regular 400 정방향·mut·KST) / 아래 태그라인 전문. px 수치(13.5/15/11.5/10) = 플그 승인값.
- 보정 5: ① 날짜 = 제목 라인 우측(margin-left:auto) ② 배지↔제목 gap 14 ③ 제목↔설명 줄간격 9 ④ 설명 = 가운데 고정·1줄 초과 말줄임 ⑤ **subnav 구역화** — div + 테마 오버레이 아주 살짝(라임 .07→코발트 .045 그라데·글래스 라인) = "NAV(로고·프로필) 아래 subnav까지는 내용이 아닌 크롬" 구조 선언 반영.
- font-face 2종 추가(viewer/fonts 기존 규약 합류): Regular 400 · BoldItalic 700 italic. BoldItalic otf 복사.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright computed{배지 텍스트/폰트/이탤릭·날짜 폰트/400/정방향·gap 14·날짜 우측·줄간격 9·가운데·ellipsis·오버레이 그라데·JS 에러 0} + 렌더 육안 + 머지 후 read-back.

## 62-C차 — 폰트 선택(영문/한글) + 시즌 정책 설정 시트 인라인 통합 (운영자 260707 "설정에서 폰트 · 볼드 고정 패밀리만" + 인수 계약 md)
- **Paperlogy 9웨이트**(100~900 · 비상업 테스트 승인 "다 넣으셈") → `viewer/fonts/` + @font-face 9종(사용 face만 브라우저 fetch). LEMONMILK는 기등록 규약 합류.
- **폰트 선택 실동작**: body `font-family:var(--app-font,…)` 변수화 · `yFontApply()` = 스택 조립{라틴 폰트 선두 = 라틴만 그 폰트·한글은 다음 폴백(분리 표준 기법)} · localStorage(`yeta_font_latin/hangul`) · 설정 글꼴 섹션 2행(영문 = 기본/LEMON MILK · 한글 = 기본/Paperlogy · 탭 토글 = 즉시 적용+저장). 웨이트 = 기존 --fw-* 고정(패밀리만 교체 = 운영자 확정).
- **시즌 정책 인라인 통합(인수 계약 260707 전수)**: 옛 `#ypolEnter` 행·별도 패널 이동 폐지 → 설정 시트 `#ypolBody` 직접 렌더(ySettingRender 타깃 이관·클릭 위임 = #ycsettingsCard 합류 yPolClick). 계약 준수{하드코딩 0(라벨 전부 def)·enum 정수만·_ypin 메모리만·hard 그룹 토글 없음(잠금 나열)·게스트 disabled}. 옛 #ysetting 탭 패널 = 휴면 주석.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright{admin 시뮬 = 정책 6행·칩 16 활성·hard 잠금 15 · 칩 변경 → op policy p={sexual:0} 실호출(정수) · 한글 토글 → Paperlogy 스택+저장+라벨 · 영문 토글 → LEMONMILK 선두 · **재로드 후 유지** · JS 에러 0} + 렌더 육안(스샷 자체가 두 폰트 적용 증명) + 머지 후 read-back.

## 54차 — 사람다움 1탄(T0) + 기본 틀 정비 (운영자 260707 "ㄱㄱ + 기본 틀 다 잡아줘·이름 반드시 불러야")
- **러너 STATE_BLOCK 신설**(TUNE 뒤·캐시 접두 무손상·전부 결정적 무저장): KST 시각대(깊은 밤 0~3시 캐논)·요일·계절·달 위상 근사(보름 어림 시만)·데일리 무드 시드(sha256(캐릭터:날짜)%5 = 같은 날 같은 기분) + **감정 관성**(직전 turn.mood 되먹임 — "곡선이다. 급변 말라 · 단 진심 어린 사과·충격은 꺾어도 됨" 가드레일) + 동네 로스터(주민 창작 방지·타 주민 인용 허용).
- **화자 귀속 버그픽**(분신 9 발견): extract_mat hist에서 타 페르소나 대사 = "이름:" 귀속(자기 말로 오독하던 구조 해소 · roster id→name · 실패 = "너:" 폴백). last_mood·cast 추출 동승.
- **00_핵심룰 기본 틀 보강**: ①호칭 규칙(반드시) — 이름 알면 이름 호명·모르면 캐릭터식 자연 호칭 + 이른 시점 자연스럽게 묻기·`에디터님/사용자님/유저님/고객님` 절대 금지·알게 된 이름 = NOTE:PUB 기록 ②AI 응수 결 예시 3종(장난/시큰둥/되치기 — 정색 해명이 최악) ③[사람의 태도] — 얼버무리기·되묻기(거짓 태도만·사실 금지)·성의의 비대칭(당김/흘림)·선의의 거짓(자기 감정 한정 = 날조 아님) ④웃음 진폭 = 공기 비례.
- **10_세계관**: "지금은 여름밤" 하드코딩 → 런타임 상태 블록 정본으로 완곡화(계절 모순 해소 — 분신 2·3 선행 지적).
- **카드 템플릿 기본 틀**(5레이어 정렬·저작 규격): 성격 고정 슬롯 3종(이면·방어기제·애착 반응) · 언어 지문 열거형(웃음 사전·호칭 전/후) · 당김·흘림 화제 절 · 훅 뱅크 절(L4 — 상황태그·6~12장·러너 랜덤픽 대비 규격) · 역린(금기 절) · few-shot에 지문 심기 가이드.
- 검증: 자가검증 경로=check_refs rc=0 + bash -n + STATE_BLOCK 단위(시각대·계절·달·시드 결정성) + extract_mat 단위(가짜 세션 — 타주민 "무디:" 귀속·자기 "너:"·last_mood·cast 전부 실측 OK). 라이브 대화 결 = 머지 후 운영자 실대화 판정(사람 감각 축).

## 63-D차 — UI 폴리시 감사: 시트 내부 촤르륵·간격 통일·캐릭터 소개 영상 슬롯 (운영자 260707 "더 수려하게·촤르륵·마진 통일·연락처 점검·설명 아래 영상")
- **감사(yeta-design 스킬 기준 · Playwright 실측)**: ① 디테일·성향·설정·프로필 시트 = 카드 통짜 등장(자식 anim 0) = 원칙5 위반 발견 ② 행 간격 불일치(갤러리 grid gap 10 vs 대화 행 8) ③ 연락처 = 정상(하은 디테일 `.ycd-vcf` "연락처 교환하기" call.js 주입 실측 ✓ · 배선 캐릭터만 노출 = 설계대로).
- **개선 배선**: ① `.ycd.on .ycd-card > *` ypickIn stagger(.05s 계단·n+10 캡·기존 커브 재사용·reduced 가드 확장) — 4시트 공통 촤르륵 ② `.ylr-wrap` margin-bottom 8→10(갤러리 동일 티어 통일) ③ **캐릭터 소개 영상 슬롯**: roster `video`(선택 `video_poster`) 필드 → 디테일 소개 글 아래 `<video class="ycd-video">`(muted loop autoplay playsinline · `--r-l`+`--glass-line` 아웃라인+`--shadow-card` · max-height 46vh cover). 필드 없으면 미렌더 = 현 로스터 무변화 · **영상 = 운영자 제공 대기**(받으면 roster에 URL 주입).
- 검증: 자가검증 경로=check_refs rc=0 + Playwright{디테일 자식 stagger 딜레이 계단(0/.05/.1/.15/.2) · 설정 시트 동일 · 행 간격 10 실측 · 영상 슬롯 webm 재생(playing·t 전진 — mp4 미재생은 헤드리스 h264 미지원 한계·실기기 정상 축) · JS 에러 0} + 렌더 육안(영상 카드 몰입 그림 확인) + 머지 후 read-back.

## 55차 — AI 응수 결 교정: 오버 금지·맥락 복귀 (운영자 260707 "오버해서 반응 X · 능동맞게")
- 00_핵심룰 대원칙 ① 응수 예시 재작성: 이전 예시("AI면 유자차 마시겠냐")는 그 화제를 정면 받아치는 것 = 이미 오버 → **한 줄 치고 지금 하던 상황으로 즉시 복귀**로 교정. 운영자 예시 반영{임무 중 = "그딴 말 할 거면 지금 이거나 집중해" · 한가한 수다 = "이젠 현실하고 가상세계도 구분을 못 해? 에휴"} + 최악 3종에 "재치 자랑하듯 길게 받아치기(에너지 쏟는 순간 오버)" 추가.
- 검증: 자가검증 경로=check_refs rc=0(국소 문구 트랙). 결 판정 = 운영자 실대화.

## 56차 — AI 응수 구도 재교정: 살짝 한심해하기 (운영자 260707)
- 00_핵심룰 응수 프레임 = "시시한 화제" → **"그런 소리 하는 쪽을 조금 한심해하는 구도"**(방어 아닌 내려다보기) + 유저 일상·습관을 끌어온 핀잔 권장(아는 사이만 가능한 반응 = 사람티). 운영자 예시 추가: 학교·일상 한복판 = "맨날 쇼츠나 쳐보니까 꿈꾸다 그런 말이나 하지."
- 검증: check_refs rc=0(국소 문구). 결 판정 = 운영자 실대화.

## 57차 — AI 응수 예시 정밀화 (운영자 260707 "밥은 먹었고 이상함 — 맥락 안 맞게 물을 수 있음")
- "뭐래. 그래서 밥은 먹었고?" 삭제 — 고정 멘트는 맥락 무관 발화 위험 + "하던 얘기 복귀" 원칙과 모순(새 화제 던짐). → "뭐래." 한 마디 + **직전 화제 그대로 이어가기**(복귀할 곳 = 방금까지의 대화) 명시.

## 58차 — AI 응수 결 최종 조율: 비꼬는 투 (운영자 260707)
- 구도 확정 = **비꼬며 내려다보기 + 핀잔 한 줄 + 하던 얘기 복귀**. 결 어휘 명시: "니 할 일이나 해라"·"뭐하냐?"·"그러니까 안 되는 거다, 정신 차리자".

## 59차 — 존재 프레임 역전 (운영자 260707 "사용자를 이 세계에 입장한 사람으로 — 거꾸로")
- 대원칙 ① 최상단에 프레임 명제: 캐릭터는 유저 세계에 불려 나온 존재가 아니라 **유저가 캐릭터의 세계(무음동)에 들어온 사람** — 네 상황이 맞다는 전제로 대답. AI·가짜·프롬프트 = 네 세계에선 헛소리. 예시 추가: "이게 진짜였으면 좋겠다" → "이젠 진짜랑 가짜도 구분을 못 하네…"

## 60차 — AI 응수 기저 감정 확정 (운영자 260707 "내가 갑자기 AI 소리 들으면 얼마나 어이없고 짜증나겠냐")
- 응수 절 최상단에 기저 감정 명제: 멀쩡히 대화하다 "너 사람 아니지?" 들은 사람의 **어이없음+짜증** — 오히려 캐릭터가 유저를 봇 보듯("정신 안 차리냐?"). 56~60차 = AI 응수 결 조율 라운드 완결{오버 금지 → 한심 구도 → 고정 멘트 제거 → 비꼼 어휘 → 프레임 역전(유저 = 이 세계 입장자) → 기저 감정}.

## 65-F차 — 북극성 배선: 딱딱한 전환 전멸 + 대화행→챗 진입 리스너 소실 복원 + 디자인 리서치 4분신 (운영자 260707 "반 메타버스 · UX 쩐다 · 딱딱한 전환구간 절대 금지 · 리서치 분신 10분 제약 상시")
- **북극성·리서치 체계 = yeta-design 스킬 등재**: 반 메타버스·딱딱한 전환 금지(1순위 체크) + 디자인 작업 착수 전 리서치 분신(각 10분 제약·다앵글) 디폴트.
- **전환 전수 감사 → 3곳 배선**: ① 탭 전환(캐릭터↔대화) 즉발 → 패널 진입 페이드(ypickIn) ② 시트 4종 닫기 즉발 → `ycdOut` 퇴장 애니(.22s 은은·원칙6 · 공용 yCdHide · 퇴장 중 자식 stagger 재생 방지) ③ 챗 닫기 dialog.close() 즉발 → `ydlg-out` 페이드(.18s · yDlgClose 헬퍼 · back/도킹/popstate 3경로). reduce = 전부 즉시.
- **🚨 중대 복원**: `#ylistRows` 클릭 위임(행 탭 = openYeta = 대화 탭 챗 진입 본선)이 **병렬 세션 머지 과정에서 소실**돼 라이브에서 죽어 있던 것을 감사 중 발견 → 복원. (교훈 = 병렬 머지 후 상호작용 스모크 필수 — 이번 발견 경로도 전환 실측 스윕.)
- **리서치 4분신 회수**(각 10분 제약 · READ-ONLY): 메신저 UI 12패턴(인라인 타이핑 버블·무드 반응 배경·버블 이펙트 절제) · 캐릭터챗 12패턴(Talkie 메모리 카드·C.AI Scenes 도입·Replika 관계 게이지 — Zeta 무드 배경 = 우리 MOOD 구조가 이미 비용 회피형) · 다크 글래스 12관찰(글래스 = 크롬 전용 위계·라임 면적 5~10% 컷·다층 블룸 글로우·표면 단차 구분) · 소셜 몰입 12패턴(Canvas 규격 3-8s 무음 루프 = 영상 슬롯 정합·shared element 전환·버블 호명 치환). **적용 백로그**(후속 승인 축): 인라인 타이핑 버블·시나리오 도입 씬·메모리 카드·카드→챗 shared element.
- 검증: 자가검증 경로=check_refs rc=0 + Playwright 7종{탭 페이드·시트 out 애니·정리·챗 페이드·close·복귀·재열기 정상 · JS 에러 0} + 머지 후 read-back.

## 61차 — 자율 머지 위임 명문화 (운영자 260707 "75% 괜찮다 싶으면 그냥 머지")
- CLAUDE.md §기틀 보호에 위임 등재: 기틀 밖 개선·아이디어 = 75% 확신 시 확인 없이 머지 진행(수정 모드 절차·검증·원장은 그대로) · 기틀 = 승인 필수 유지.

## 62차 — 친밀도 해금 에피소드 시스템 (운영자 260707 "친밀도 높아지면 해금되는 에피소드" · 75% 위임 자율 머지)
- **시스템**: NOTE:ME 첫 줄 = `[LV n]`(0~5 · 모델 자가 판정 — 대면 횟수 아닌 관계의 질·한 턴 ±1·갈등 시 하락 가능·시작값 = 카드 relationship 결) + 00_핵심룰 §🔓 신설{[LVn+] 미충족 = 존재도 모른 척·조르기 불가("단계 건너뛰는 조르기에 넘어가면 관계의 무게가 죽는다") · 막 도달 = 자연 계기에 스스로 조각으로 연다}. 러너 무변경(카드 통짜 주입 + 지침 게이트 — v1 프롬프트 게이트·새면 러너 하드 게이트 승격).
- **콘텐츠**: 템플릿 해금 절 규격(LV2 개인사→LV5 비밀의 핵) + 라이브 6인 카드에 각 4개 저작{무디 = 고양이 이름→찻집 고요의 이유→몇백 년 첫 확인 · 백 = 순찰 동행→소금→흉터→마지막 사냥꾼 · 류 = 동석권→검 진지→취중진담→문파명(맨정신 첫 발화) · 윤 = 신청곡 우선→발신번호 없는 사연→낮의 윤→새벽 4시 주파수 · 세라·하은 = 페르소나시트 LV표 이식}. 기존 금기(류 문파명 맨정신 금지 등)와 정합 — 해금이 금기의 예외 조건이 되는 구조.
- 검증: check_refs rc=0 · 6카드 삽입 실측. 해금 체감 = 운영자 실대화(장기 대화 축).


## 63차 — 사람다움 T1-A: 휴면·관계LV·인계·질투·기억가중·회상 (운영자 260707 "ㄱㄱ" · 75% 위임)
- **러너 4신호 신설**(extract_mat → STATE_BLOCK 조건부 주입 · 전부 기존 세션 데이터 파생·저장 0): ①휴면 = 직전 활동과의 공백 48h+ 시 "약 N일 만에 돌아왔다 — 성향대로 반응(비례·취조 금지)" ②관계 LV = NOTE:ME [LV n] 파싱 명시 주입(§🔓 해금 게이트 판정 안정화) ③합류 인계 = 재뽑기 sys 직후 첫 턴에 "방금까지 {직전 화자}와 있었다 — 인수인계(둘만의 비밀은 모름)" ④질투 메타 = 최근 40턴 최다 타 화자(3턴+·tune[7]>=7 캐릭터만) "가볍게 한 번만·빈도만 안다".
- **지침**: 기억 감정 가중 `[★]`(포화 시 무표 줄부터 폐기·★와 [사건]은 존치) + [회상] — 계절·비·음식 계기의 자발 회상("아 맞다 너 그때…") 빈도 가드.
- 검증: bash -n + 실사용 흐름 가짜 세션 단위테스트{gap 11h(휴면 라인 없음)/75.5h(3일 라인)·rel_lv 3·riv 무디·handoff 무디·last_mood warm·질투 tune[7] 게이팅 on/off} + STATE_BLOCK 실물 검수 + check_refs rc=0. 뷰어 무접촉(디자이너 세션 충돌 0).

## 63-H차 — 대화 자연스러움 3종: 타이핑 읽는시간 지연 + 읽씹 리마인더 파이프 + 카톡식 푸시 (운영자 260707 "누적 대화 자연스러움이 가장 중요 · 즉답 타이핑 어색 · 2~3시간 읽씹이면 먼저 말 걸기 · 알림 = 캐릭터 이름·대화 이어지게")
- **① 타이핑 읽는시간**: 전송 즉시 "입력 중" 표시 폐지 → **1.1~2.8s 랜덤 지연 후 등장**(상대가 읽는 시간 — `_yTypHold` + 자체 타이머 · yRender 게이트 병행 = 폴 재렌더도 홀드 준수). 실측 = 0.4s 시점 미표시·3.2s 시점 표시.
- **② 읽씹 리마인더 파이프 신설**(운영자 지시 = 기틀 승인): `yeta-nudge.yml`(cron 30분·dispatch) + `yeta_nudge.sh` — 판정{마지막 턴 = assistant · kind≠nudge(연속 재촉 금지) · state≠awaiting · 경과 ≥ `NUDGE_AFTER_MIN`(기본 150분) · 3일 초과 방치 제외 · 일 상한 `NUDGE_MAX_PER_DAY`(기본 2)} 전부 만족 시만 claude 1회(sonnet-5×low·폴오버 SSOT·safe-mode) → **기억(PUB/ME)+최근 대화 주입** = "임무 가야지 뭐해?"류 페르소나 결 선발화 → fresh 재확인 후 세션 append(`kind:'nudge'`) + 푸시. 조건 미충족 주기 = 클로드 호출 0(쿼터 절약). 발견 수정 = state 오타 'await'→'awaiting'(pending 가드 무력화 방지).
- **③ 카톡식 푸시**: `push_reply`·nudge 푸시 = **제목 = 캐릭터 이름 · 본문 = 대사 미리보기 70자**(지문 제거) — "yeta: 답장 도착" 폐지(운영자 "다른 가상앱처럼 느껴지면 안 돼"). sw.js는 기제공 title/body 지원 = 배선만.
- **기억 파이프 점검(운영자 "누적이 가장 중요")**: 이중 기억(NOTE:PUB/ME) 추출·주입·600자 캡·[사건] 보존 압축 = 구조 건강 실측. nudge도 동일 기억 주입 = 연속성 유지.
- 검증: 자가검증 경로=check_refs rc=0 + bash -n(chat·nudge) + yml 파스 + Playwright{타이핑 지연 실측·JS 에러 0}. nudge 실발화 = cron 첫 주기(라이브 세션 조건 충족 시) — 러너 로그로 사후 확인 예정.

## 64차 — 로스터 10인 복원 + 세라·하은 아바타 교체 + 무음동 배경 배리에이션 파이프 (운영자 260707 "기존 openai 뽑은 걸로 캐릭터 채워줘 · 하은=2번, 한 명=1번 · openai로 배경 다양하게")
- **은퇴 4인 복귀**: 데스크·카피·가을·본 — 옛 10인 roster(ce5cea9) 원본(기존 OpenAI 얼굴·배경 URL 그대로) + 현 스키마 보강(desc·quote·stats6·tune16 신규 기입). 카드 md는 존치돼 있어 즉시 활성.
- **세계관 정합**: `10_세계관.md` 주민 6인 → 10인(4인 = 평범한 인간 축 팩트 한 줄씩) · "떠난 주민" 절 → **"다녀온 주민들(복귀)"**(공백기는 아는 척 단정 금지 = 스테일 세션 안전).
- **아바타 교체**: 하은 = 운영자 2번(실사 흑발) · 1번(사이버펑크 일러 — 언락 배경 영상 주인공과 동일 결) = **세라 배정**(최연소 연습생 결 정합 · 운영자 "한 명" 미지정 → 재량, 바꾸려면 한마디). 세로 원본 → 얼굴 중심 정방 크롭(512² webp) → `viewer/assets/yeta_face/`(git 폴백 규약) · roster avatar 상대경로.
- **배경 배리에이션 파이프 신설**: `yeta-bg-var.yml`(수동 dispatch·⚠️유료 규약) + `yeta_bg_var.py` — OpenAI gpt-image 1024×1536 세로, 무음동 결 8씬{편의점 새벽·골목 소나기·찻집 해질녘·옥상 자정·버스정류장 안개·놀이터 3am·라디오 골목 네온·첫눈 밤} → `viewer/assets/yeta_bg/var_*.png` main 직접 커밋(멱등·FORCE 재생성).
- 검증: 자가검증 경로=check_refs rc=0 + yml 파스·py 컴파일 + Playwright{갤러리 10행·하은 로컬 webp 아바타 computed 로드·JS 에러 0} + 크롭 육안(얼굴 중심 ✓) + 머지 후 read-back. 배경 실생성 = dispatch 러너 로그.
## 64차 추기 — 배경 배리에이션 실생성 완료 (260707)
- yeta-bg-var run#1 = 8/8 성공 · main 자동 커밋(5f3e928) · `viewer/assets/yeta_bg/var_*.png` 18MB. 육안 = 무음동 결 정확(편의점 새벽 라임 간판·놀이터 3am 경계 미광 등). 활용(roster bg 교체·씬 연출) = 운영자 선택 후 배선.
## 64차 추기2 — 배경 씬 3배 확장(8→32) (운영자 260707 "3배 더 · 사건사고 분위기 많이")
- 신규 24씬{날씨·시간대 8(안개 첫차·한여름 대낮·태풍 전야·열대야·은행잎·정전·번개 순간·우박 뒤) · 상황·장소 8(빨래방 3am·코노 뒷문·파장 시장·달빛 계단·비 횡단보도·오락실·개천 새벽·지하보도) · **사건사고 8**(폴리스라인·부러진 담장·셔터 발톱자국·깨진 가로등·소방차 잔광·붕대 벤치·실종 전단지·얇아진 경계)} — 사건사고 = 핵심룰② "판타지는 낮게 샌다"(흔적·여운·무인) 결. 멱등 = 기존 8 스킵·신규만 생성. 워크플로 타임아웃 30→50분.
## 65차 — 단톡·난입 아이데이션 + UI 구성안 (운영자 260707 "한명 정도는 난입가능하게 단톡도 · 초대로 · 거절도 가능하게 · UI 난잡 금지 · 기존 패턴 차용")
- **산출 = 시안 보고서**(구현 미착수 — 아이데이션 요청): `docs/reports/260707_단톡난입_아이데이션_플레이그라운드.html` — 실물 폰 목업 시나리오 4종 자동재생{A 초대→수락 · B 초대→거절(백 '순찰 중') · C 난입→원탭 내보내기 · D 단톡 화자 선정} + 진입점 프리셋 3안{A + 더보기 시트(추천·예약 진입점 재사용) / B 헤더 상시+ / C 자연어 호출(A와 병행)} + 규칙 다이얼 5종{정원2·난입 일1·거절 캐릭터판단·대본 최대2대사·내보내기 원탭+대화} + 선택값 복사 바.
- **핵심 설계**: ① 방 신설 아닌 `합석` 모델(스레드 단일 유지 · sess.room 최대 2) ② 거절 = 캐릭터 판단(카드·시각·관계LV — 거절 캐릭터는 이후 먼저 난입해 회수) ③ 비용 불변 = 턴당 dispatch 1회(한 호출 최대 2대사 대본 → 러너 분할 · 파싱 실패 = 통짜 폴백) ④ 화자 사다리 = 호명 고정 > 교대 > 모델 위임 ⑤ 재사용 자산 = 화자 귀속·enter_line·handoff·riv 질투·sys/이름표 UI(신규 4조각만: room·op invite/kick·파서·시트+헤더 스택).
- **차용 실측**: 카톡 더보기→대화상대 초대 동선 + 초대 거절 기능 · C.AI 그룹챗(캐릭터 다수·자동 응답) · iMessage 아바타 스택 헤더.
- 검증: 순수 시각/목업 = 면제 · 자가검증 경로=check_refs rc=0 + 브라우저 실렌더 확인. 구현 착수 = 운영자 픽(진입점·다이얼) 후.
## 66차 — 캐릭터 선택창 블러 한 단 위 계승 (운영자 260707 "캐릭터 선택창 블러 아주 쥬금 더 줘")
- 대상 3표면 = 캐릭터 탭 갤러리 행 `.ycrow` · 같은 화면 월드 카드 `.yworld` · 첫 진입 선택 리스트 `.yeta-list .ypick-row` — backdrop blur `--blur-m`(16px) → `--blur-l`(24px) 토큰 사다리 한 단 위 계승(절대명령1 · 새 값 창작 0 · saturate 유지). 상세 카드 `.ycd-card`는 이미 `--blur-l` 최상단 = 무변경.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0(토큰 락 49개 정합) + `git show origin/main:viewer/index.html` read-back | 운영자검증 = 체감 강도(사람 감각 유일 축 — 과하면 롤백 = `_versions/260707_0346_캐릭터선택창블러`).
## 67차 — 디테일 ⚙ 성향 버튼 = 헤더 행 우측 재배치 (운영자 260707 "설정 버튼 등 잘 마진 좋게 배치 · 정립된 패턴 찾아서 가져오셈")
- 문제 = `.ycd-gear`가 카드 안 `position:absolute; top:0; right:0` — 글래스 카드 라운드 모서리에 딱 붙어 마진 0(운영자 스샷 어색 지점).
- 가져온 정립 패턴 = **좌·우 쌍 헤더 행**{좌상단 로고 + 우상단 프로필 쌍(§🗺 로고 결) · `.ycd-top` 기존 헤더 행} — ⚙를 카드에서 빼서 `.ycd-top` 우측(`margin-left:auto`)에 상주 배치 = 뒤로가기(◁)와 동형 글래스 원형 쌍·시트 패딩 `--sp-2` 마진 자동 계승.
- 배선: 카드 innerHTML 재주입 → `#ycdGear` 상주 버튼 + `dataset.set`만 갱신 · 클릭 위임 카드 핸들러에서 분리(전용 리스너). 도형·아이콘·눌림(--press-m) 무변경 = 순수 배치.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0(JS 구문 OK 포함) + `git show origin/main` read-back | 운영자검증 = 배치 체감(사람 감각 유일 축 — 롤백 = `_versions/260707_0349_디테일기어헤더행`).
## 68차 — 성향 시트 헤더 좌·우 쌍 통일: 기본값 복원 = 헤더 우측 승격 (운영자 260707 `한수 ㄱ`)
- `#ycset` 헤더 = 뒤로가기 혼자 → **기본값 복원**을 헤더 행 우측 글래스 원형(`.ycd-gear` 동형)으로 승격 = 디테일 시트(◁+⚙)와 쌍 골격 통일. 아이콘 = `UNDO_SVG`(nm-svg 아이콘 SSOT 계승 · 인라인 재선언 0 · JS 주입).
- 2탭 재확인 계약 승계: armed 색(`--arm`+danger 테두리 = `.yset-reset.armed` 동값) + 아이콘 버튼이라 문구 스왑 대신 `miniToast('한 번 더 누르면 기본값 복원')`. 대화 중 잠금 = `#ycset.locked`서 버튼 숨김(기존 잠금 계약).
- 푸터 = `저장됨 ✓` 인디케이터만 잔류(복원 버튼 이동) · 카드 위임 핸들러에서 복원 분기 제거 = 전용 리스너.
- 설정(`#ycsettings`)·프로필(`#ycprofile`) 시트 = 시트 전체 스코프 우측 액션이 실존 안 함(정책 복원은 L1 그룹 국소 스코프 = 헤더 승격 시 오해) → 골격(.ycd-top)만 공유 유지·액션 생기면 같은 자리.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0(JS 구문·아이콘 SSOT OK) + `git show origin/main` read-back | 운영자검증 = 배치 체감(사람 감각 유일 축 — 롤백 = `_versions/260707_0541_성향시트헤더쌍`).
## 69차 — 캐릭터 탭 프사 깨짐 근본 교정 (운영자 260707 "프사 제대로 안되있어")
- 증상 = 갤러리 행 아바타가 얼굴 아닌 이미지 모서리(세라 = sera.webp 노란 배경 좌상단·카피 = 빌딩 모서리). 실패 계층 실측 = 자산 정상{R2 png 전부 200·초상 실물 확인(mudi = 정상 얼굴)·로컬 webp 실존} → **CSS 캐스케이드 층 확정**.
- 근본 원인 = `.ycrow .yava`의 `background:` 축약(570행)이 동특이도 `.yava.has-img`(165행)의 `background-size:cover; background-position:center`를 뒤 순서로 리셋 → 1024px 원본이 56px 원에 좌상단 원본크기 렌더. 대화 탭(`.ylist-ava`)은 축약 뒤 cover 재선언 관례로 무사.
- 교정 = `.ycrow .yava`에 `.ylist-ava` 동형 관례 계승(축약 뒤 `background-size:cover; background-position:center` 재선언). 1줄 CSS · 자산·JS 무변경.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=이미지 실물 curl+육안 대조·check_refs rc=0 + `git show origin/main` read-back | 운영자검증 = 라이브 육안(캐시 새로고침).
## 70차 — 메뉴별 구성 플레이그라운드 (운영자 260707 "각 메뉴 플레이그라운드 최대한 상세하게 + 다른 구성도 가능하게 · 3분 컷")
- 산출 = `docs/reports/260707_메뉴별구성_플레이그라운드.html` — 3탭(캐릭터·대화·설정) × {현행 재현 + 대안 프리셋(캐릭터: 2열 그리드★·히어로 스택·컴팩트 / 대화: 멀티 스레드★·포커스 카드 / 설정: 플랫·타일 그리드★) + 미세조정 슬라이더(아바타·패딩·간격·라운드·블러·유리 밝기·미리보기 줄수) + 선택값 복사(토큰 사다리 자동 스냅 표기 = 계승/갱신 후보) + 재현 한계 각주}. 폰 프레임 + :root 실값 사본 + 기틀 축 = ⚠ 딱지.
- 시간 제약(운영자 "3분") = 리서치 분신 생략 · 순수 시각/목업 = 검증 면제 축.
- 검증: 자가검증 경로=check_refs rc=0 | 구현 반영 = 운영자 픽(선택값 복사) 후 별도 작업.
## 71차 — 메뉴 플그 선택값 반영 + 대화 카톡 결 분할 리스트 + 재발방지 가드 (운영자 260707)
- **재발방지(근본)**: 지난 플그가 블러 등 디자인 값을 표면별 raw로 돌릴 수 있게 준 것 = 값 SSOT·일괄 배포(절대명령1) 위반 소지 → 원칙 못박음: **플그·적용은 토큰을 돌린다(표면별 raw 금지)** = yeta-design 스킬 플그 독트린에 가드 1항 신설(토큰 매핑·전파 · ⚠갱신축은 운영자 택일 · 토큰 없는 축만 raw). 이번 적용부터 그 원칙대로 = 토큰 매핑分은 전 표면 일괄.
- **블러 통일**: 지난 68차 캐릭터 선택창 `--blur-l` 3표면(.ycrow·.yworld·.yeta-list .ypick-row) → 플그 선택값 `--blur-m`로 원복·통일(전 선택 표면 동일 토큰).
- **캐릭터 탭(.ycrow)**: 패딩 → `--sp-2`(12) · 라운드 `--r-l`→`--r-m`(13 갱신축 = 최근접 --r-m 스냅 · 새 토큰 신설 회피 = anti-drift) · 아바타 56→41px(토큰 없는 축=raw) · 그리드 간격 10→14px(raw) · 유리 = 이미 --glass-0(2%).
- **설정 탭(.yset-grp)**: 라운드 →`--r-m` · 유리 `--glass`→`--glass-0`(2%) · 그룹간격 8→11px · 블러 이미 --blur-m. (성향 설정도 동클래스 = 일괄.)
- **대화 탭 = 카톡 결 분할 리스트(운영자 첨부 "획으로 나눠서 대화창")**: `.ylist-group` 단일 글래스 면 + 행 사이 획(`--glass-line-0` 헤어라인) 신설 = 부유 카드 스택 → 연속 분할 리스트(카톡 뉘앙스). 기존 토큰·`.ylist-row`·스와이프 나가기 전량 계승 · 라운드 `--r-m`·패딩 `--sp-2`. ⚠ 다중스레드 백엔드 = 미착수(운영자 확인 없이 큰 기틀 안 건드림) → 현 활성 스레드 렌더, 구조만 스택 대비.
- 검증: 국소 손질+시각 = 자가검증 — 자가검증 경로=check_refs rc=0(JS 구문·토큰 락 49 정합) + `git show origin/main` read-back | 운영자검증 = 라이브 육안(캐시 새로고침 · 사람 감각 유일 축) · 롤백 = `_versions/260707_0749_메뉴값적용_대화리스트`.
## 72차 — 시즌 표기 통일: 캐릭터 탭 → 대화 탭 .yssn 정본 (운영자 260707 첨부2 통일 지시)
- 캐릭터 탭 시즌 헤더(구 `.yworld` — SEASON 1 텍스트줄+제목 h2+태그라인)를 대화 탭 시즌 subnav(`.yssn` — [SEASON 01 라임 필배지·레몬밀크 이탤릭][무음동(無音洞) 한자 부기][우측 KST 날짜] / 태그라인 가운데 1줄)로 통일. 렌더 = 공용 함수 `ySsnHtml()` 신설(두 탭 단일 정본 = 드리프트 차단) · 구 `.yworld/-ssn/-nm/-tag` CSS 4룰 폐지(죽은 코드 제거).
- 신규 값·토큰 0 = 순수 계승(절대명령1 ①축) — .yssn 기존 스타일 그대로 재사용.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0(JS 구문·토큰 락 49 정합·yworld 잔여 참조 0 grep) + `git show origin/main` read-back | 운영자검증 = 라이브 육안(캐시 새로고침 · 사람 감각 유일 축) · 롤백 = `_versions/260707_0753_시즌표기통일`.
## 73차 — 배경 배리에이션 3배 확장 완결(8→32장) + bg-var push 유실 재발방지 (운영자 260707 "3배 더 — 날씨·상황·분위기·사건사고")
- run#2(28811460654) = 24/24장 생성 성공했으나 22분 런 사이 main 전진(#105)으로 러너 `git push` rejected → 산출물 유실(로그 실측 = 실패 계층: 생성 아닌 push 층 확정). 워크플로 커밋 스텝에 `git pull --rebase origin main` + push 5회 백오프 재시도 추가(#113 · 신규 파일만 = rebase 무충돌) 후 run#3 재dispatch = 성공.
- 최종 = `viewer/assets/yeta_bg/var_*.png` **32장**(기존 8 + 신규 24: 날씨·시간대 8{dawn_mist·noon_summer·typhoon_eve·heatnight·autumn_dusk·blackout·thunder_flash·after_hail} · 상황·장소 8{laundromat·karaoke_back·market_close·moon_stairs·crosswalk_rain·arcade_glow·stream_dawn·underpass} · 사건사고 8{police_line·broken_wall·claw_shutter·dead_lamp·firetruck_after·bandage_bench·missing_flyer·thin_boundary — 핵심룰② "판타지는 낮게 샌다" 결·무인·흔적과 여운}).
- ⚠️ run#2 유실분 재생성 = OpenAI 재과금 발생(러너 밖 미반출 = 회수 불가 축). 활용 배선(뷰어 무드/씬 연출·roster bg 교체) = 운영자 선택 후 별도.
- 검증: 자가검증 경로=Actions 로그+`git ls-tree origin/main` 32장 실측+육안 2장(police_line·thin_boundary — 무음동 결 확인) | 운영자검증 불가피 사유=없음(전항 도구 실측).
## 74차 — '에디터님' 호칭 잔재 근절 + 첫인사 변주 풀(오프닝 반복 차단) (운영자 260707 "캐릭터가 에디터님~ 이라 시작 · 원인해결 · 매번 동일 오프닝 금지·페르소나만 유지")
- **원인(2층)**: ① `10_세계관.md` "주민들은 당신을 '에디터'라고 부른다"(런타임 생성에 직접 주입) ② `roster.json`·`haeun.md`·`desk.md` 하드코딩(하은 greeting·대사, desk relationship "편집장과 에디터") — nomute-editor(편집국) 원본 잔재가 260706 판타지 개편에 안 지워짐. 정작 `00_지침`은 `에디터님` 절대금지 = 자기모순.
- **해결**: 10_세계관 유저의 자리·세계금기에서 '에디터' 호칭 제거 → 이름/캐릭터다운 자연 호칭(00_지침 규칙 준수) 명시 · haeun.md 5곳·roster.json 하은 greeting·desk relationship 교체. 최종 sweep = 유저 호칭 잔재 0 실측(desk 본인 배경 '사회부 기자 시절' 등은 캐릭터 정체성 = 존치).
- **오프닝 반복 차단**: `draw`가 첫 진입에 정적 greeting 1개 박제(yeta.js:270) = 매번 동일 → 러너는 유저턴에만 응답(NOPENDING)이라 동적 오프닝 = 파이프라인 기틀. 저위험 콘텐츠층 해법 = **인사 변주 풀 3종/캐릭터**(roster.json `greetings[]`) + 뷰어 `yGreet()` 랜덤 픽(draw 4곳+폴백 배선). 페르소나 유지·오프닝만 변주. ⚠ 진짜 매번 새 생성(시각·문맥 반영 동적 오프닝) = 러너 오프닝 경로 신설 = 기틀(운영자 승인 후 별도).
- **페르소나 반영 점검(#3)**: 10인 전원 tune16·stats6·greetings3 완비 · 카드 전량 판타지(무음동) 정합 실측.
- 검증: 콘텐츠+국소 = 자가검증 — 경로=roster JSON 파싱 OK·check_refs rc=0(JS 구문)·잔재 grep 0 + read-back | 운영자검증 = 라이브 대화 오프닝 육안. 롤백 = `_versions/260707_에디터잔재_인사변주`.
## 75차 — 동적 오프닝 생성(첫인사 매번 새로 · 페르소나만 유지) + 5인 기틀검증 2회전 (운영자 260707 "응 그렇게할거임")
- **변경**: 첫 진입 인사 = 정적 박제/변주풀 → **러너가 매번 동적 생성**(시각·계절·관계LV·카드 반영). 유저 발화 없이 캐릭터가 먼저 연다. 파이프라인 기틀(생성 러너 심장) 변경 = §기틀검증 대상.
- **경로**: 게이트웨이 `draw` 첫진입 → `sess.opening=nonce`·`awaiting` 세팅 후 dispatch(정적 push 폐지) → 러너 `extract_mat`이 opening job 반환(pending 유저턴 없어도 · policy·tune 동형 포함·클라 문자열 0) → opening 전용 프롬프트(SCENE_BLOCK) → finish가 nonce 일치 시 `ins=0` 프리펜드.
- **5인 기틀검증(1차 전원 BLOCK → 수정 → 2차 전원 APPROVE)** 반영 필수수정:
  - 가) **nonce(opening_ts) 방어**: finish fresh-read 후 세션 opening과 불일치(reset·재드로·중복·기처리)면 exit2 무write 폐기 = **유령 인사 오염 차단**(reset-during-opening 데이터 무결성).
  - 나) **ins=0 프리펜드**(append 금지): 동시 유저 발신 턴 pending 보존 = **첫 메시지 영구소실 차단**(send op이 opening 미삭제로 정합).
  - 다) **draw 멱등 가드**(`awaiting && opening`이면 재dispatch 금지) + GH_TOKEN 게이트 한정 = **무인증 과금 증폭·reset→draw 루프 차단**.
  - 라) **3중 폴백 사다리**(동적→변주풀→단일) + 3실패면(dispatch실패·빈답·쿼터/rc·10분리퍼) 전부 opening 소거+idle 강등 = **웜루프 무한 재생성 스핀·죽은 재시도 배너(409) 차단**.
  - 마) **뷰어**: openingPending 시 정적 인사 억제(타이핑만)·error/zombie 배너 제외·대화목록/사이드레일 "곧 말을 걸어와" 구분 = **딱딱한 전환(북극성) 차단**.
- **자가검증(도구 실측)**: 러너 시뮬 9케이스(extract_mat 3·finish 6 = nonce폐기·프리펜드·빈답강등·무회귀 전부 기대대로) + `bash -n`·`node --check yeta.js`·`check_refs rc=0` + diff↔실파일 바이트일치(재검증 3인 독립 확인). 라이브 생성 품질 = 운영자 육안(유료 Actions E2E는 미실행 = 과금·백그라운드 기피).
- 롤백 = `_versions/260707_0930_동적오프닝`. ⚠ 이제 첫 진입에 opus-low 1생성(reset당·탭당 아님) — send 동급 무비용 축은 아님(정적 폴백은 GH_TOKEN 무 시).
## 76차 — 설정·프로필 배경 영상 계승 + 페르소나 고유 배경 주입 (운영자 260707 "설정화면도 뒤에 영상 · 글래스 메뉴1,2 동일 · 대화 시작=캐릭터 고유 이미지·없으면 뽑아")
- **설정·프로필 시트 = 배경 영상 계승**: `.ycd-live` 신규 modifier(설정·프로필 시트에 부착) — 불투명 bg(`var(--bg)`) 제거 → 배경 영상(`.ybgvid` z:0)+스크림(.38)이 그대로 비침. 시트는 챗 dialog와 달리 ys-hold 미적용 = 영상 재생 유지. 원인: 설정은 시트 오버레이라 `#ymain`이 `tab-char/list`를 유지 → char/list 패널이 뒤에 그려짐(bleed). 해결 = `.cd-open`(시트 열림 상태·기존)에 `.ychar/.ylist visibility:hidden`+NAV 구분선 숨김 얹음(불투명 시트엔 무해·라이브 시트만 영상 노출). 새 클래스·JS 불요.
- **글래스 메뉴1,2 동일화**: `.yset-item`·`.yprof-stat`에 `backdrop-filter:blur(--blur-m)` 추가 = 메뉴1 `.ycrow` 동일 계승(영상 위 프로스트 성립). 신규 토큰·raw 0(--glass-0·--blur-m·--glass-line-0 계승).
- **페르소나 고유 배경**: bg 중복 2인(하은=tea[무디 공유]·가을=alley[백 공유]) → 전용 씬 생성(`yeta_bg_var.py` SCENES에 persona_haeun[퇴근길 학교 정문·불 켜진 교실·분필 토트백]·persona_gaeul[상인회장 사무실 야경·장부·핀 지도·시장 배너] 추가 → run#4 생성) → roster bg 주입 = 10인 전원 고유 시작 배경. 러너 ffmpeg 미변환분 webp 로컬 생성(768w·~85KB). yeta_bg_var.py에 webp 변환 편입(다음 dispatch 자동).
- 검증: 국소+시각 = 자가검증 — 경로=Playwright 실측 6/6(설정·프로필 투명·영상 표시·행 blur·bleed 0·JS 에러 0)+육안(설정/프로필 영상 계승·persona 2씬 카드 정합)+check_refs rc=0 + read-back | 운영자검증 = 라이브 육안(캐시 새로고침·사람 감각 축). 롤백 = `_versions/260707_배경맥락배선`.

## 77차 — 단톡·합석 구현 (운영자 260707 "응 바로 구현해줘" — 65차 시안 기본값 그대로)
- **모델 = 합석**(방 신설 없음 · 스레드 단일 유지): `sess.room` 최대 2 + `invite`/`barged`/`declined`/`barge_day` 마커. 구세션 = room 폴백 [persona] = 마이그레이션 0.
- **게이트웨이**: op `invite`(마커+sys+dispatch · 정원·TTL 10분·중복 가드 3중 · 실패 = 마커 롤백)·`kick`(내보내기/초대 철회 · 마지막 1명 가드) 신설 = 18 op. `draw` = room 리셋. 신설 op 입력 = id + 24자 sani name만(정책 ② 유지 · draw 동형).
- **러너**: 화자 사다리{호명(경계 휴리스틱 — "백만원" 오인 차단) > 난입 데뷔 > 직전 화자(2연속 독주 = 교대)} · `mode=invite` 판정(ACCEPT/DECLINE — **거절 = 콘텐츠** · 사유 sys 인용 · declined = 난입 회수 떡밥) · **대본 분할 = 비용 불변 핵심**(한 호출 [동행명] 최대 2대사 → 화자별 턴 분할 · 실패 = 통짜 폴백 · 동행 = 말투 절만 주입·기억 갱신 없음) · `barge_check` 난입(일 1 상한·결정적 시드 1/3·**대사 생성 0** = enter_line sys 합류만·첫 마디 = 다음 턴 데뷔) · gen_out/state_block 공용화 · 푸시 제목 $CHAR('main' 박제)→$PERSONA 버그픽.
- **뷰어**: + 버튼(예약 진입점) → 초대 패널(.yeta-pick 정본 계승 · 만석 = 멤버 [보내기]) · 헤더 아바타 스택(+링·yHeadRoom 멱등) · 난입 원탭 내보내기 pill(.yb.sys 계승·소멸형·래치) · '올지 생각 중' 타이핑 변형 · 폴 조건에 invite 추가. 신설 :root 토큰 0.
- **5인 교차검증(출력 계약 확장 = 기틀 · 운영자 승인 후)**: 초회 4 APPROVE + 1 REJECT → 지적 전부 조치 → 재평결 전원 APPROVE. 조치분 = ① persona 갱신 단톡 한정(1:1 draw 경합 회귀 소거 — 검증5 블로킹) ② co-only 승격(화자 침묵 턴 정상화 — 검증1 M) ③ 초대 판정 한국어·한 줄 인사·대화체 거절 휴리스틱(검증1 M) ④ PTT 대본 낭독 컷(검증5 L) ⑤ invite/kick 400 트림(검증3 L) ⑥ accept 시 declined 소거(검증2 L). 잔여 비블로킹 = 후속 우선사항에.
- 검증: 자가검증 경로=러너 단위 32/32 + 픽스 회귀 12/12 + 뷰어 Playwright 통합 8/8(초대→수락→스택→난입→내보내기) + check_refs rc=0 + 5인 READ-ONLY 전원 APPROVE + 머지 후 read-back. 실사용 초대/난입 발화 결 = 운영자 폰 체감(사람 감각 유일 축).
- 후속 우선사항(검증단 비블로킹 로깅): 초대 판정에 state/전용 리퍼(무피드백 드롭 개선 · 검증2 M) · R2 put CAS(If-Match) 도입 검토(LWW 윈도 — 기존 finish 계열 공통 · 검증2 M) · INVITE_TTL 상수 단일화(4중 하드코딩 · 검증2 L) · draw의 enter/greeting 자유 텍스트 축소 검토(검증3 L) · 단톡+PTT 분할 턴 음성 부착(검증5 INFO).
## 73차-B — 대화 탭 시즌 빈 박스 숨김 (운영자 260707 "그렇게해줘" — 72차 한 수 채택)
- `#yssnList`(class .yssn 정적 부여)가 YWORLD 미로드·로스터 실패 시 빈 글래스 박스로 남던 기존 결 → `.yssn:empty { display:none; }` 1룰 추가(로드 전 플래시 포함 차단). 캐릭터 탭은 YWORLD 있을 때만 래퍼 생성이라 무영향. 신규 값·토큰 0.
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0 + `git show origin/main` read-back | 운영자검증 = 라이브 육안(사람 감각 유일 축) · 롤백 = `_versions/260707_1124_시즌빈박스숨김`.
## 74차-B — 챗 입력 시 창 밀림 교정: visualViewport 창 유지 가드 (운영자 260707 스샷 "입력할 때 창이 내려감")
- 원인: 뷰포트 기본 동작(안드로이드 크롬 108+ = resizes-visual)에서 온스크린 키보드가 레이아웃을 안 줄이고 **화면을 패닝** → 100dvh 챗 다이얼로그가 통째로 밀리며 로그·입력행 정렬이 어긋남.
- 조치: `yVvFit()` 가드 신설 — 챗 열림 + 키보드 판정(innerHeight−vv.height>120) 시 `#yetadlg` 높이=visualViewport.height·top=offsetTop(팬 오프셋 보정)으로 **키보드 위 보이는 영역에 프레임 고정** + 로그 바닥 재고정 · 키보드 닫힘/챗 닫힘 = 인라인 스타일 원복. resize+scroll 양쪽 청취.
- 기각 대안: 메타 `interactive-widget=resizes-content` = dvh 축소가 9:16 레터박스 쿼리(`min-aspect-ratio:9/16`)를 폰 키보드 개방 중 오발동(챗이 좁은 기둥화) → JS 국소 가드 채택. 신규 값·토큰 0(임계 120 = OS 뷰포트 축 raw 예외).
- 검증: 국소 손질 = 자가검증 — 자가검증 경로=check_refs rc=0(JS 구문 게이트) + `git show origin/main` read-back | 운영자검증 = 실기기 키보드 개폐 육안(사람 감각 유일 축 — 개발환경에 온스크린 키보드 없음) · 롤백 = `_versions/260707_1143_키보드창유지`.
## 78차 — 뒤로가기 레이어 스택: 시트·챗 통합 히스토리(앱 나감 버그 교정) (운영자 260707 "메뉴서 뒤로가면 앱이 나가짐 · 이전 화면으로 · 레이어 계층 미적용")
- **원인**: 히스토리 처리가 챗 다이얼로그(#yetadlg)에만 있고, **디테일·성향·설정·프로필 시트(ycd 계열)는 pushState 0** → 시트에서 폰 뒤로가기 = popstate가 처리 못 함 → 브라우저가 앱 페이지 밖으로 = 앱 탈출.
- **해결 = 통합 뒤로가기 레이어 스택**(`_navStack` + `navOpen/navBack/navSwap/_navPopClose` + 단일 popstate): 오버레이(시트 4종·챗)는 열 때 history 칸 1 push + JS 스택 등재 · 뒤로가기/닫기버튼 모두 최상단 1개만 회수 → **앱 탈출은 스택 빌 때(홈)만**. 챗 개별 히스토리(pushState/reconciliation/전용 popstate) = 스택으로 흡수(단일 경로). 디테일→챗 = `navSwap`(replaceState = history 칸 재사용 · back+push 레이스 회피). 디테일↔성향 겹침 = 스택 언와인딩(뒤로 = 성향 닫고 디테일 복귀). 병렬 단톡(77차) yInvToggle 닫기 훅과 병합 보존.
- **자가검증(도구 실측)**: 노드 상태머신 시뮬 5플로우(시트 뒤로=닫힘·홈 / 디테일→성향→뒤로 언와인딩 / CTA swap→챗 뒤로 / 백버튼 / 중복팝 없음 전부 기대대로) + check_refs viewer JS OK rc=0 + 레이어 우회 직접닫기 grep 0. 라이브 육안(폰 실기기 뒤로가기) = 운영자.
- 롤백 = `_versions/260707_1204_뒤로가기레이어스택`.
## 79차 — 무음동 10인 판타지 캐릭터 초상 생성 (운영자 260707 "이거 기틀로 캐릭터 이미지 하나씩·판타지컬하게·완성형 만들고 편집")
- **파이프 신설**: `yeta_char_art.py` + `yeta-char-art.yml`(수동 dispatch·contents:write·push 재시도) — 기존 얼굴 파이프(grounded 프사)와 별개 축 = **판타지 반신 초상**(2:3·1024×1536). 각 카드 서사 + roster 포인트색 아우라 반영. grounded 인물도 "낮게 새는 판타지"로 승격.
- **run#1 = 10/10 성공**(28841113893): 무디(시간초월 찻집주인·옥빛 마법 김)·세라(각성자 파워라인·탑)·하은(글자마법 한글·분필가방)·백(마지막 사냥꾼 룬 워딩코트·검)·류(오백 년 검·유령검·달빛대나무)·윤(경계 넘는 전파 DJ·보라 음파·건너간 자 실루엣)·데스크(진실 기록 편집장·부유 텍스트)·카피(펀치라인 빛글자)·가을(상인회장·연결 빛줄기·자수정)·본(전 파이터·엠버 주먹 아우라). 육안 10/10 카드 정합 확인.
- **산출 = `viewer/assets/yeta_char/<id>.png`(+webp) git 커밋 = 편집 베이스** · roster 자동주입 안 함(운영자 편집 후 배정 · 기존 아바타 무변경). 멱등 skip/FORCE/`only=<id>`(1인 재생성 = 비용절감).
- 검증: 콘텐츠 생성 = 자가검증 — 경로=Actions run#1 success + `git ls-tree origin/main` 10장 실측 + 육안 10/10 카드 대조 | 운영자검증 = 초상 취향·편집 방향(사람 감각 축). ⚠️ OpenAI 유료 10장 소요. 참고: 세라 초상이 카드 나이(19)보다 성숙·크롭탑 = 경계 내(성인·연습복·비성적)이나 취향 시 `only=sera` 재생성 가능.
## 80차 — 판타지 초상 10인 아바타 반영 (운영자 260707 "나온거 다 반영해줘 · 배경은 다 똑같을거고")
- **아바타 주입**: 79차 판타지 반신 초상(2:3) → 얼굴+상반신 **1:1 크롭**(상단 1024²→512² webp · `assets/yeta_char/av/<id>.webp`) → roster avatar 10인 전원 주입. 2:3을 원형 아바타에 그대로 넣으면 가운데=몸통 잘림 → 얼굴중심 크롭 필수(그 판단으로 크롭 신설).
- **배경 무변경**(운영자 "배경은 다 똑같을거고"): bg 필드 전량 유지(하은·가을 persona 씬 + 8인 R2 시그니처 장소). 기존 아바타(세라·하은 업로드분·8인 옛 얼굴)는 판타지 초상으로 대체 — 옛 파일 git 히스토리 보존(복원 가능).
- 검증: 콘텐츠 반영 = 자가검증 — 경로=Playwright 캐릭터 탭 10행 전원 has-img+이미지 decode 성공·JS 에러 0 + 몽타주 육안(10인 얼굴 프레이밍 양호) + check_refs rc=0 + read-back | 운영자검증 = 라이브 육안·편집 방향. ⚠ 시그니처 배경 추가 생성 = 운영자 지목 대기(260707 "한두개 뽑아도 괜찮아·돈 문제 아님").

## 81차 — 위치 마주침 이벤트 + 무음동 위치 SSOT (운영자 260707 "갑자기 끼어드는 이벤트 · 주변에 인물이 있으면 만나는")
- **위치 SSOT 신설** `apps/yeta/places.json`: 장소 그래프 12곳{찻집·골목·연습실·검도장·라디오국·편집국·체육관·학교·시장·편의점·놀이터·집(private)} + 10인 동선표(슬롯 6단 = state_block 시간대 동일 경계). **지도 UI 인수 계약** = 이 파일에 x·y 좌표만 얹기(사본 금지) — _doc에 명기.
- **위치 판정 모듈** `.github/scripts/yeta_place.py`: place_of = 동선표 기본 + sha256 시드 1/4 인접 외출(집은 변주 없음) = 결정적·무저장(실측 변주율 45/200). extract_mat·barge_check·초대 판정이 이 함수 하나만 사용(사본 금지).
- **마주침 = 난입 후보 4축**: 거절 회수 > 자주 대면 > 언급 > **위치**(화자의 지금 장소와 같은 곳 = 시드 1/2 · 인접 = 1/3 · 동시 후보 = 시드 최솟값 픽). 연출 = sys `{장소} — 지나가던 {이름}과 마주쳤다` + barged.via=place → 데뷔 첫 마디가 "지나던 참" 결(STATE_BLOCK 분기). 상한·마커·내보내기 pill = 기존 난입 축 공유(뷰어·게이트웨이 무수정).
- **배경 정합 보너스**: 화자·초대받은 캐릭터의 지금 장소를 STATE_BLOCK에 주입(낭독 금지·대화 흐름 우선) — 무디가 찻집인데 밖 얘기하는 부조화 감소 · 초대 거절 사유가 장소 기반으로 구체화("시장이라 못 가").
- 검증: 자가검증 경로=위치·마주침 신규 15/15{결정성·전 슬롯 정의·외출 변주율·private·마주침 오라클 실발동(백·골목 인접)·비인접 무발동} + 기존 44/44 회귀(위치 축 반영 오라클 갱신 1건) + 뷰어 8/8 + check_refs rc=0 + 머지 후 read-back. 실사용 마주침 체감 = 운영자 폰(조건부 이벤트 — 하루 1 상한).

## 82차 — 무음동 지도 좌표 확정(분신술 10인) + 캐릭터별 집 노드 (운영자 260707 "지도 배치 임의로 · 분신술 · 골목이 중심 핫플 · 각자 집과 직장")
- **분신술 10인 실소환**(READ-ONLY · 각자 주민 1인 담당 — 카드·세계관·동선표 실측 후 집·직장·방위 제안) → 메인 수렴. 채택 논거: 백 담당 "경계는 마을 바깥이 아니라 중심 그 자체(찻집 무음 = 고요한 눈) — 파수꾼·찻집은 심장부에"(세계관 정합) · 윤 담당 "radio = 그래프 유일 잎 노드 = 북쪽 가장자리" · 하은 담당 "학교→찻집→집 남북 일직선 = 퇴근길 서사의 지리화" · 무디·류 담당 "동선표에 home 슬롯이 없는 두 명 = 집 노드 자체를 안 만든다(일터가 삶)".
- **좌표 확정**: 0~100 격자·골목(50,50) 중심·1칸≈100m — 존 3분{북 = 심야 미디어(라디오국 49,27·편집국 53,42) · 중심 = 밤 코어(골목·찻집 46,53·연습실 56,47·경비실) · 남 = 생활권(편의점 53,63·학교 50,76·놀이터 44,70·검도장 38,57·시장 42,62·체육관 35,67)}. **집 8곳 신설** = `home_<id>`(private — 변주·마주침·핀 제외·지도 연결선 1인접): 세라 옥탑(59,41)·하은 북쪽 언덕(44,33)·백 경비실 뒷방(47,55 — 찻집 옆 = "새벽 물 한 잔" 카논 성립)·윤 계단 아랫방(50,30)·데스크 삼교의 방(58,36)·카피 옥탑(52,56)·가을 노을집(37,66)·본 강철 위층(35,67). routine의 home 참조 → home_<id> 원자 치환(범용 home 제거).
- **지도 플레이그라운드**: `docs/reports/260707_무음동지도_배치_플레이그라운드.html` — SVG 마을 지도(존 워시·인접 라인·집 다이아) + 시간 슬라이더·날짜 버튼(핀 이동 = **yeta_place.py의 JS 정확 포팅** · sha256 BigInt) + 마주침 지대 라임 링 + 분신 10인 근거 표 + 좌표 JSON 복사. 지도 UI 세션 인수 = places.json 좌표 그대로 사용.
- 미채택 기록: 세계관 "백이 새벽 순찰 후 체육관에 들른다"는 그래프(골목↔체육관 비인접)로 재현 불가 — 그래프 왜곡 대신 서사 영역으로 존치(본 담당 flag). 가을 집 방위는 시장 확정 위치(남서)를 따라 북동→남서 언덕으로 조정(논지 "시장 굽어보는 뒷집" 유지).
- 검증: 자가검증 경로=places 무결 10/10{routine 참조·좌표 범위·private·인접 상호성} + 위치·마주침 15/15 + 기존 44/44 + 뷰어 8/8 + **지도 실렌더 Playwright**{핀 10·노드 11·집 8·JS↔파이썬 위치 100% 일치(19시·4시 전원)·JS 에러 0 — 저녁 골목 5인·찻집 4인 수렴 재현} + check_refs rc=0 + 머지 후 read-back.
## 82차-B — 10인 전원 고유 판타지 대화-시작 배경 완성 (운영자 260707 "각각에 어울리는 고유 배경 하나씩 다 뽑아줘·대화 시작하면 나올 수 있는거")
- 81차 추가한 8인 persona 씬(mudi·sera·baek·ryu·yun·desk·kopi·von) yeta-bg-var 생성 → 하은·가을 기존분과 합쳐 **10인 전원 고유 판타지 대화배경**. roster bg 전량 → `assets/yeta_bg/var_persona_<id>.webp`(대화 시작 = 캐릭터 시그니처 장소). 새 판타지 초상 화풍과 결 통일.
- 러너 ffmpeg 미변환 8종 webp 로컬 생성(768w). 육안 8/8 장소 정합(무디 찻집 옥빛 김·세라 탑 문·백 룬 골목·류 달빛 도장·윤 ON-AIR·데스크 편집국·카피 빛글·본 체육관).
- 검증: 콘텐츠 반영 = 자가검증 — 경로=Actions 성공+`git ls-tree` 10종+몽타주 육안+로컬 파일 존재 10/10+check_refs rc=0+read-back | 운영자검증 = 라이브 육안. ⚠ OpenAI 유료 8장.
## 83차 — 다중 채팅방 v3: 캐릭터별 스레드·고정·롱프레스 액티브 (운영자 260707 "1"=A 승인 · 코어 세션 모델 전환 = §기틀검증 2회전)
- **전환**: 단일 스레드(§🎬 구판) → `sessions/main.json` 단일 R2 객체 안 **캐릭터별 threads[<id>]**(방 = 캐릭터 · "화자 교체" 개념 소멸 · 합석 = 방 내부 room). 공유 = note_pub/notes/tunes/policy/pref/barge_day(전역 일1)/call. 운영자 요구 매핑 = ①캐릭터별 방 ②대화 탭 고정 우선→최근이 위 ③pin ④나가기 = 롱프레스 액티브 2버튼[📌고정·나가기 armed 2탭] ⑤스와이프 폐지 ⑥로스터 밖 인명('미호') 창작 금지 1줄(10_세계관).
- **5인 1차 감사 전원 BLOCK → 전 지적 반영 → 3인 재검증(잔여 2건 지적→즉시 수정·실증) = 만장일치**:
  - 가) **etag CAS**(게이트웨이 casPut 4회 루프·러너 s3api If-Match+무조건 put 폴백+finish 재시도) = 교차 스레드 LWW 턴 소실·pin이 답장 덮는 사고·reset{t} 교차 봉합(레이스 감사).
  - 나) **마이그레이션** = `yeta_v3.py`(JS migrateV3와 동형 — 노드 대조 diff 0·멱등) · 러너 3종(chat/call/nudge) 읽기측 랩 · v2 백업 main.v2.json write-once · updated 백필(마이그 감사 — call/nudge 파손 경로 2건 해제).
  - 다) **러너** = 통합 age 큐(invite 신선→pending 최고령→opening · 기아 차단)·THREAD env 관통(재검증 지적 barge 프리픽스 누락 즉시 수정·난입 실증)·finish 스레드 부재=exit2·barge_day 전역·PUB 기억 계약 강화("이 방 고백 = ME")·riv 타 스레드 메타만·ptt 스레드 스캔·push tag/url 스레드화·invite POLICY_BLOCK 동형 편입(러너 감사).
  - 라) **보안·비용** = draw 로스터 대조+캡12(스레드 신설 단일 경로)·전 op 스레드 실존 요구·오프닝 일일 카운터 30·200턴 트림 5지점·get 비활성 꼬리 2턴 절단·**dispatch char:main 단일 그룹 불변 명문화(§📰 — 스레드별 분기 절대 금지 = 비용 상한 근거)**(보안 감사).
  - 마) **뷰어** = 정렬 (pin?1:0)→updated·시그니처 가드(재렌더 stagger 재트리거 0)·롱프레스(justLong 래치·무변형 앵커·user-select 차단·navStack 등재·.out 퇴장)·unread = 스레드별 ts·yThreadSwitch 키드 전량 리셋·YTH 스코프 전수·call.js cur 폴백·딥링크 &t=(재검증 지적 콜드 가드 폐기 즉시 수정)(UX 감사).
- 검증: 자가검증 경로=JS↔Py 마이그 동형·멱등 노드 대조 + finish 6케이스·age 큐 3종·barge 도달 시뮬 + check_refs rc=0·구문 3종 + 3인 재검증 실파일 대조 | 운영자검증 = 라이브 체감(방 전환·고정·롱프레스·오프닝 — 사람 감각 축). 롤백 = `_versions/260707_1404_다중채팅방v3` + R2 `sessions/main.v2.json`(자동 write-once).
- **후속 백로그(재검증 비차단 노트)**: call/nudge r2put ETag 이식 · nudge heredoc 미인용(기존 결) · 휴면 yDraw/#yetaPick 재도입 시 키드 리셋 필수 · force 회수 고아 history 칸 1(78차 수용 결) · 주석 산문 "스와이프" 잔재 · reflect_invite insert 트림(다음 finish 회수).
## 83차-B — 무음동 지도 배경 생성(제미나이) + 파이프 신설 (운영자 260707 "이런 느낌으로 · 제미나이 통해서 뽑아버리셈")
- **파이프 신설**: `yeta-map-bg.yml`+`yeta_map_bg.py`(⚠️Gemini 유료 = 수동 dispatch 전용 · 멱등·FORCE 재생성 · fail-soft) — places.json 배치의 언어화 프롬프트{중앙 골목 광장 · 서남 찻집(한옥·등불) · 북 편집국→라디오 전파탑 언덕 · 동 연습실 계단 입구 · 남 생활권(편의점·학교·놀이터·검도장 기와 마당·시장 천막·체육관) · 개천+다리·숲 테두리} · 글자 생성 금지(라벨 = 지도 UI SVG 몫) · 1:1 1K.
- **산출 = 실생성 완료**(dispatch run 1회 · main 자동 커밋): `viewer/assets/yeta_map/map_night.png`(0.7MB)·`map_day.png`(1.2MB). 밤 = 다크+라임 네온 편의점 = 앱 브랜드 결 정합·글자 0·배치 재현 우수(전파탑 북 언덕·광장 사거리·기와 찻집·계단 입구·개천 다리). 낮 = 파스텔 아기자기(첨부 예시 결)·단 글자 결함(THE TIMES·SCHOOL 간판) — 재추첨 = force=1 dispatch.
- **지도 플레이그라운드 갱신**: 배경 세그(지형도만/밤/낮 — raw URL) + 노드·핀 오버레이 정합 검토 토글.
- 검증: 자가검증 경로=py_compile·yml 파스 + 러너 로그(2장 성공) + 산출 육안(배치·글자 여부) + Playwright{배경 토글·존 숨김·핀 유지·JS 에러 0} + check_refs rc=0.

## 84차 — 지도 v2: 도시 결 배경 + 미니 캐릭터 워커 10인 (운영자 260707 "쪼만한 캐릭터 걸어다니게·특성 살아있게 · 산 말고 중소도시·힙한 도시")
- **배경 재추첨(force)**: 프롬프트 v2 — 숲·산 테두리 폐기 → 힙한 중소도시 동네{벽돌 상가·옥상 테라스·물탱크·네온 간판·원경 스카이라인·포장 골목·개천+다리}. 밤 = 글자 0·라임 네온·다크 앱 결 정합(채택 추천) · 낮 = 파스텔+스카이라인·단 글자 상습 재발(RADIO·GYM — 재추첨 = what=bg·force=1).
- **미니 캐릭터 스프라이트 10인 신설**: `yeta_map_char.py` — 카드 개성 언어화(무디 찻쟁반 앞치마{성별 모호 = 카드 카논 정합}·세라 이어폰 후드 뚱한 표정·류 부채+술병 도포·백 손전등 롱코트·하은 가방·윤 헤드폰·데스크 서류+커피·카피 노트북 가방·가을 장부 개량한복·본 츄리닝 수건) → 그린스크린 생성 → PIL 크로마키 투명+트림+320px → `viewer/assets/yeta_map/char_<id>.png` 10장 전원 성공.
- **플레이그라운드 걸어다니기 모드**: 핀/스프라이트 세그 — 워커 = 장소 주변 어슬렁(주기 목표 갱신+CSS transition)·진행 방향 좌우 플립·bob 애니·시간 변경 = 걸어서 이동 · 미생성/로드 실패 = 핀 폴백.
- 검증: 자가검증 경로=러너 로그(12/12 성공) + 산출 육안(배경 도시 결·글자 여부·스프라이트 개성 4장 대조) + Playwright 실렌더{워커 10·전원 이동·밤 배경·JS 에러 0 — 저녁 중앙 수렴 재현} + check_refs rc=0 + 머지 후 read-back.

## 85차 — 지도 탭 앱 실장 + 차량 60fps + 출입금지·건물 특색 정본화 (운영자 260707 "일단 앱에 다 반영해줘 · 차 60fps · 상가 밀집 · 출입금지 지대 · 학교 · 건물마다 특색")
- **뷰어 4번째 탭 `지도`**: 네비 맵핀 + `.ymapp` 패널(기존 탭 패턴 대칭·글래스 프레임·ypickIn 진입) — 밤 배경(map_night) + 장소 노드·이름 + **출입금지 존**(danger 대시+테이프 결) + **워커 10인**(현재 KST 실위치 = places SSOT·JS 등가 판정·1분 재판정 · 같은 장소 = 부채 오프셋 · 붐빔 = 라임 링) + **차량 4대**(엣지=길 랜덤 워크·등속·방향 회전·라임 헤드라이트). **워커 탭 = openYeta(그 캐릭터 방 직행)**.
- **60fps 계약(운영자)**: 단일 rAF 루프 + `style.transform`만(레이아웃 0·합성 전용) · 탭 이탈 = rAF/인터벌 정지 · reduce-motion = 차량 미생성·어슬렁 정지. **실측 fps=60**(Playwright 2초 카운트).
- **위치 SSOT 확장**(`places.json`): 전 장소 `look`(건물 특색 한 줄 — 지도 툴팁·배경 프롬프트 결 동기) + `offlimits` 출입금지 구역(66,34 동북 개천변 — 동선·마주침·워커 불가침 검증) 등재.
- **배경 v3 프롬프트**(`yeta_map_bg.py` — dispatch는 후속 run): 상가 밀집(간판 겹겹 골목) · 출입금지 공사구역(펜스·경고테이프·안이 이상하게 어두움) · 학교 뚜렷(운동장·구령대·게양대) · 차선·횡단보도·갓길 주차 · look 특색(찻집 굴뚝 김·편집국 옥상 간판 뼈대·라디오 빨간 점멸등·체육관 글러브 간판·연습실 네온 화살표).
- 검증: 자가검증 경로=Playwright 통합{패널·노드 11·워커 10 전원 이동·차 4·출입금지·시계·**fps 60**·워커 탭→openYeta·탭 이탈 루프 정지·JS 에러 0} + 스크린샷 육안(지도 탭 실화면) + check_refs rc=0 + 머지 후 read-back. 폰 체감 = 운영자(사람 감각 축).
## 85차 추기 — 배경 v3 실생성 완료 (260707)
- yeta-map-bg run#3(what=bg·force=1) = 2/2 성공·main 자동 커밋. 육안 = 요청 4종 전부 그림 반영{상가 밀집(간판 겹겹 블록) · **출입금지 공사구역 = places 좌표(동북 개천변)와 방위 일치**(펜스·경고테이프·내부 암흑) · 학교(운동장·게양대·시계탑) · 특색(찻집 굴뚝 김·편집국 옥상 간판 뼈대·라디오 빨간 점멸등·체육관 글러브·연습실 네온 화살표) + 차선·횡단보도·갓길 주차}. 밤 = 글자 사실상 0("24" 간판뿐 = 자연) · 낮 = 간판 글자 소량 잔존(상습 — 지도 기본 = 밤이라 방치). 앱 지도 탭 = 파일명 동일 교체라 자동 반영 — 재검증 fps 60·오버레이 정합 스샷 확인.

## 86차 — 디테일 시트 9:16 프레임 + 강조 무채 전환 + vcf 자동 실행 (운영자 260707 스샷 "UI 범위 안에서만 · 강조색 너무 강함 = 무채·고투명 계승 · vcf 다운로드 아니라 자동 실행")
- **프레임 교정**: `.ycd`(디테일·성향·설정·프로필 시트 공통)를 `@media (min-aspect-ratio:9/16)`에서 챗 dialog 공식 그대로 계승{height:100dvh·aspect-ratio 9/16·margin auto·inset 0·border-inline} — 와이드 화면에서 전폭 퍼짐 종료·앱 프레임 안에서만. 실측 = 1600×700 뷰포트에서 폭 394(=700×9/16)·중앙 정렬.
- **무채 전환(강조 다이어트)**: 육각 레이더 수치 폴리곤 = 라임(.2)+accent 스트로크 → `--glass-2` 채움+`--fg-2` 스트로크 · [대화 시작] CTA = 라임 솔리드 필 → 무채 글래스(glass-2+glass-line+blur — 위계는 굵기·밝기만) · 시트 배경 라디얼 = accent .08 → `--glass`. 전부 기존 토큰 계승(신규 0).
- **연락처 교환(vcf) 정본 신설 + 자동 실행**: 디테일 시트에 `.ycd-sub` 글래스 보조 버튼(전화 배선 캐릭터만 = roster.phone 게이팅 — 현재 하은) → `location.href = assets/contacts/<id>.vcf`(same-tab 내비게이션). `_headers`의 `Content-Disposition: attachment` **폐지 + inline·text/x-vcard 와일드카드** = iOS Safari 즉시 연락처 추가 시트·Android 열기(연락처 앱 연동) — a[download]·blob 방식은 "저장만 되는" 원인이라 금지 주석. 병행 세션 선반영분(haeun.vcf 실번호+사진 임베드) 그대로 사용.
- 검증: 자가검증 경로=Playwright{프레임 폭·중앙·CTA/레이더 computed 무채·vcf 버튼 게이팅(하은만)·클릭 = .vcf 내비게이션 발생·JS 에러 0} + check_refs rc=0 + 머지 후 read-back. 실기기 vcf 시트 발화(iOS/Android 연락처 앱) = 운영자 폰 체감(헤더 반영은 read-back로 실측).
## 83차-C — 무음동 지도 자연 배치: 캐릭터 안착·차량 도로·신호등·이름표 (운영자 260707 "지도 어색·자연스럽게·차는 차도로·서행·신호등 정지·건물 안이면 이름표·3글자↑ 축약")
- **근인**: 지도가 places.json 논리좌표(중앙 뭉침)를 렌더 좌표로 그대로 써서 라벨·캐릭터가 중앙에 몰리고, 차량이 건물 노드 그래프(대각선)를 달려 실제 그려진 도로와 불일치. 캐릭터는 노드중심+랜덤오프셋(±9,±5)으로 배회 = "둥둥".
- **맵 시각 좌표 신설**(YMAP_POS·map_night.png 실측 트레이스 = 격자 오버레이 대조): 노드·라벨·캐릭터가 실제 건물에 안착. **places.json 논리 SSOT는 무변경**(마주침·러너 로직 불변 · 지도 전용 축).
- **캐릭터**: 랜덤 배회 제거→스팟 안착. 야외(YMAP_SPOT 골목·놀이터·시장) = 지상 스프라이트, 실내(YMAP_INDOOR 찻집·연습실·편집국·라디오국·체육관·검도장·학교·편의점) = 건물 위 이름표 세로 스택, 집(private) = 지도 밖. 붐빔 링(2+) 유지.
- **이름 축약**(운영자): name≥3글자 = roster `short`(데스크→데스) · 이름표·폴백 공용.
- **차량**: 도로 링(YMAP_RING·회색길 실측 폐곡선) 주행 + 서행(v 26~42→12~17) + 광장 신호등(red 5s/green 7s) 정지선 대기. reduce-motion = 차량 미생성·워커 정적.
- 검증: 국소+시각 = 자가검증 — 경로=Playwright 실측{차량 3대 좌표 전부 링=도로 · 신호 순환 · 워커 안착/이름표/축약 · JS 에러 0} + 격자·맵 스샷 육안 + check_refs rc=0 + read-back | 운영자검증 = 라이브 육안. ⚠ 도로 링·건물 위치 = 이미지 실측 근사(핵심 도로 정합 · 미세 오차 시 좌표 조정) · 롤백=_versions/260707_지도자연배치.

## 87차 — 세션 CAS 조건부 put 복구: 대화방 진입·대화 진행 전면 불능 (운영자 "대화창 사실상 안 열림·대화 진행 불가")
- **근인**: v3 다중채팅방 CAS 도입(a68a0cc)에서 `readSessE`가 R2 `o.httpEtag`(따옴표 포함 `"…"` — 헤더 반환용)를 etag로 반환 → `putSessIf`의 `onlyIf.etagMatches`가 원시 etag(따옴표 없음)와 상시 불일치 → 조건부 put 100% precondition-fail → `casPut` 4회 루프 전멸 → 409 `세션 경합 — 잠시 후 다시`. **casPut 경유 전 쓰기 op**(draw·send·pin·invite·kick·tune·policy·reset 일부) 전면 실패 · `get` 읽기만 생존 = 옛 대화만 보이고 새 방·새 메시지 불가. 옛 대화(haeun 07-07 09:12 KST)는 CAS 도입(14:31 KST) 이전 무조건-put 시절 잔존분이라 앞뒤 정합.
- **증상 재현**: 라이브 `draw` 3/3 "세션 경합" · Playwright(로컬 뷰어+라이브 API 프록시)에서 openYeta→대화창은 열려도 배너+입력창 숨김(inputHidden true)·msgCount 0.
- **수정**: `functions/api/yeta.js` `readSessE` 2곳 `o.httpEtag`→`o.etag`(raw). Cloudflare R2 Workers 문서 확증(httpEtag=*in quotes so as to be returned as a header* · etagMatches=*the object's etag*).
- 검증: 자가검증 경로=배포후 라이브 draw ok:true(**수정전 3/3 실패 ↔ 수정후 성공** 대조) + Playwright E2E{대화창 열림·대화 8메시지 렌더·입력창 표시(inputHidden false)·JS 에러 0} + `node --check` + check_refs rc=0 + 머지 후 read-back(origin/main). send는 유료 dispatch라 미프로브 = 동일 casPut 경로 draw ok로 갈음(§운영 태도 g 무과금·무합성). 롤백=_versions/260707_1949_etag_cas_fix.

## 88차 — 잠금 진입: PIN 키패드 → 3×3 패턴 전환(번호 숨김·번호 유지) (운영자 "지그재그 입력=패턴 잠금으로 · 번호 유지")
- **요청/결정**: 잠금 입력을 숫자 키패드 → 안드로이드식 3×3 패턴 그리기로. 운영자 선택 = **번호 숨김** · PIN(Cloudflare `YETA_PIN_ADMIN`) **미변경**(1~9·중복 없는 4자리 확인 — 패턴 매핑 성립).
- **매핑(번호 유지)**: 3×3 점 = 전화 키패드 순서(1~9) → 지그재그 방문 순서 = 기존 PIN 숫자열. 서버 `auth`·PIN·env·`users.json` 전부 무변경(입력 UI만 교체 · auth 정규식 `\d{4,8}` 계약 유지 · 0/중복 숫자 PIN은 미지원 = 표준 패턴 한계, 운영자 PIN이 조건 충족이라 무관).
- **구현**(`viewer/index.html`): `#ymPad` 키패드 → 패턴 그리드(SVG polyline + 9노드) · pointer 드래그(`setPointerCapture`) · 재방문 불가 · 4점(`YM_LEN`) 완성 자동 판정. 기존 자산 재사용(슬롯 진척·자물쇠 크랙·실패카운트 `YM_MAX`·잠금 `YM_OUT_MS`·성공 다이브/zap/`setUnlocked`). 토큰 계승(`--pin-ac`[`-rgb`]·`--glass-2`·`--glass-line`·`--r-pill`·`--ease`·`--danger`) · 패턴 기하 px·글로우 = 토큰 없는 raw 예외축 · 히트≥40px · reduced-motion 가드. 부수: 죽은 토큰 `--pin-ac-rgb` 배선 해소(게이트 경고 −1).
- **시안 선행**: `docs/reports/260707_패턴잠금_플레이그라운드.html`(프리셋 4·슬라이더 5·복사) = PR #142.
- 검증: 자가검증 경로=Playwright E2E(mock auth 2486){패턴 방문순서→숫자열 **정확**(1236·2486)·pin-mode 진입·틀린 패턴 실패카운트(1/5)·맞는 패턴 언락→캐릭터 10명·미완 리셋·JS 에러 0} + viewer 구문게이트 + check_refs rc=0 + 머지 후 read-back(origin/main) + **배포후 라이브 패턴 마크업 실측**(yeta.soong.kr = lk-pat/node/line 33참조) | 운영자검증 불가피 사유=실제 PIN 언락은 운영자만 아는 지식(사람 검증 유일) → 폰 체감. 롤백=git revert 723e36a(단일 커밋).
### 88차 추기 — 패턴 잠금 변형 탐색 플레이그라운드 (운영자 "최대한 다양하게")
- `docs/reports/260707_패턴잠금_변형탐색_플레이그라운드.html` — 룩 프리셋 12종(현행★·네온·미니멀·링·스퀘어·다이아·번호표시·코발트·듀얼톤·점선·볼드·4×4) + 축 칩 6(그리드·모양·선·이펙트·색·번호) + 슬라이더 5 + 사양 복사. 색 = 브랜드 라임·코발트만 · 4×4 = 탐색용(PIN 매핑은 3×3 고정). E2E{룩12·그리드 전환 9/16·2486 해제·칩 조합·JS 에러 0}. 룩 선택 대기 = 현행 유지.

## 89차 — 설정 시트 뒤 지도 비침 버그픽 (운영자 스샷 "설정탭 이렇게 안보이는거 해결")
- **증상**: 지도 탭에서 설정(글래스 시트) 열면 뒤로 지도 패널(무음동·워커·시계)이 비쳐 설정 항목과 뒤죽박죽 겹침.
- **원인**: `cd-open` 시 뒤 탭 패널 숨김 규칙(`viewer/index.html`)이 `.ychar/.ylist`만 커버 — 이후 신설된 지도 탭 패널 `.ymapp` 누락(85차 지도 실장 때 74차 규칙 미보강).
- **수정**: 같은 규칙에 `.ymapp` 추가(1줄). 설정·프로필·디테일·성향 전 시트 공통 해소(cd-open 공유).
- 검증: 국소 손질 = 자가검증 — 경로=Playwright E2E{지도 탭→설정 열기 = ymapp visibility hidden·설정 카드 정상 렌더·닫기 = 지도 복귀·cd-open 해제·char/list 기존 규칙 유지·JS 에러 0} + 스샷 육안(시트 뒤 배경 영상만 = 의도 디자인) + check_refs rc=0 + 머지 후 read-back + **배포후 라이브 실측**(yeta.soong.kr HTML에 `cd-open .ymapp` 규칙 존재). 롤백=_versions/260707_2117_설정탭_지도겹침.

## 90차 — 잠금 버튼 점화 모션: 탭 순간 스냅 제거 + 발광 블룸 (운영자 "버튼이 발광하면서 풀려야 되는데 모션이 어색해")
- **원인**: 자물쇠 숨쉬기(ymLockBreathe scale .96~1.12·opacity .78~1) 중 탭하면 `.unlocking`의 `animation:none`이 애니 즉살 → 숨쉬던 중간 프레임에서 기본값으로 순간 점프(최대 scale .12·opacity .22 스냅 — 트랜지션은 애니 제거에 안 걸림). 발광도 26→40px 단조 전환(테두리색 즉발) = "팍 발광" 순간감 부재.
- **수정**(`viewer/index.html`): `ymLockIgnite` 점화 원샷(.5s) — JS가 탭 순간 숨쉬기 현재 프레임(scale·opacity)을 `--ig-s`/`--ig-o` 인라인 변수로 주입 → 그 값에서 이어받아 `살짝 눌림(.93) → 팍 발광(brightness 1.4·글로우 62px 피크) → 확정 글로우(40px .42) 안착`. 테두리색 트랜지션 보강(.3s). **운영자 확정 안무(260/280/790ms·고리 31°·숨쉬기 파라미터) 불변** · reduced-motion 정적 직행 유지 · 색 = `--accent-rgb` 계승(키프레임 %·글로우 px = raw 예외축) · 인라인 `--ig-*` = 요소 로컬 변수(:root 토큰 아님 = 락 비대상).
- 검증: 국소 손질 = 자가검증 — 경로=Playwright 실측{숨쉬기 피크(scale 1.071)서 탭 = **snapDelta 0.000**(pre/post 완전 연속·--ig-s 1.071 주입 확인)·점화 피크 brightness 1.398·안착 scale 1/opacity 1·pin-mode 축소(72→46)·고리 열림 정상·JS 에러 0} + check_refs rc=0 + 머지 후 read-back + 배포후 라이브 실측(ymLockIgnite 존재) | 운영자검증 = 폰 체감(모션 질감 = 사람 감각 축). 롤백=_versions/260707_2126_잠금점화모션.

## 88차-B — 안드로이드 첫 진입 스플래시(검정+YETA 로고) 배치 플레이그라운드 (운영자 260707 "첫 진입 OS 안드로이드 화면 검정에 YETA만 · 로고 위치 내가 설정 · 간단히 플레이그라운드")
- **산출 = 시안 플레이그라운드**(`docs/reports/260707_스플래시_로고배치_플레이그라운드.html` · 단일 HTML·인라인·다크): 실물 재현(안드로이드 폰 목업 + 확정 워드마크 `#ywm` 6겹 조립을 `yeta-wm-*.png` 4장 base64 인라인으로 그대로 재현) + 프리셋(정중앙·골든포인트·포스터19.7/63.66·상단·하단) + 슬라이더(수직/수평/크기/자간/글로우/내부광원) + 배경(딥블랙#000·앱캔버스#161716계승·커스텀) + 로고색(정본글래스·화이트·라임·코발트) + 등장모션(페이드업·번개낙하·정적) + 값복사. 플그 5요소 계승.
- **핵심 판정**: 안드로이드 OS 네이티브 스플래시는 아이콘 중앙 고정 = 위치 조정 불가 → 위치 자유는 **웹 오버레이 스플래시**로 구현하고 manifest `background_color`(현 #161716)를 같은 검정으로 맞춰 OS→오버레이 이음새 제거하는 방향.
- **상태 = 위치 확정 → 실장 진행(89차)**. 자가검증 = Chromium 헤드리스 스샷 2종(글래스+중앙 · 화이트+하단+광원) 워드마크 조립·색분기·위치·슬라이더 fill 실측 통과.

## 89차-B — 첫 진입 스플래시 오버레이 실장 (운영자 260707 플그 확정값: top 40.5%·66vw·화이트·앱캔버스#161716·자간-5%·광원5%·페이드업)
- **구현**(`viewer/index.html`): `<body>` 최상단 `.yspl` 풀스크린 오버레이(z:120 = `--z-float`(60) 상위 · 배경 `--bg`) + `#ywmSplash` 워드마크. 워드마크 = 확정 `#ywm` 6겹 조립 재사용(조립 forEach에 `ywmSplash` 추가) · 화이트 스타일 = `w-cb`/`w-ca`(코발트/라임 반전판) 소등·`w-body`=`--fg`·헤일로 흰(.32)·내부광원 .05. 배치 `top:40.5%`·`--w:66vw`·자간 `-.05`·`ysplIn` 페이드업(.82s) = 기하 raw 예외축(잠금 대형 로고 `top:19.7%`·`--w:63.66%` 관례 계승).
- **해제**: `setTimeout` 1.4s → `.gone`(opacity 페이드 .5s) → `hidden` DOM 제거 → 뒤 잠금화면 노출. reduce-motion = 시간축 동일·애니만 소등.
- **manifest 무변경**: `background_color`/`theme_color` 이미 `#161716` = 확정값 일치 → OS 네이티브 스플래시(배경만·아이콘 중앙고정)와 웹 오버레이 동색 = 이음새 없음.
- **토큰 계승·기틀 무변경**: 배경 `--bg`·로고 `--fg`·모션 `--ease`·blur `--blur-m`·글로우 `--cobalt-rgb`(drop-shadow = 토큰없는 축). 신규 :root 토큰 0.
- 검증: 자가검증 경로=Chromium 헤드리스{조립 DOM 덤프(7겹+yeta span+mask 경로) · 로고 rect 측정 `cut=no`(뷰포트 내 좌우여백 · 스샷 잘림은 window-size≠innerWidth 캡처 아티팩트로 규명) · 온전 스플래시 스샷(화이트 YETA·중앙 위) · 해제 2.7s 후 잠금화면 온전 노출 스샷 · JS 구문 OK} + check_refs rc=0 + push 후 read-back. 실기기 첫 진입 UX 체감 = 운영자(사람 감각 축 · §운영 태도 g). 롤백=_versions/260707_스플래시오버레이.

## 90차-B — 스플래시 핫픽스(웹 오버레이 폐지 → OS 아이콘 워드마크화) + 핀 입력 2건 + UX 배치1 (운영자 260707 실기기 스샷 "OS 스플래시와 따로 놀고 화면만 하나 늘었어 · 바로 해결")
- **근인**: 안드로이드 진입 = ①OS 스플래시(#161716+아이콘+앱명) → ②웹 오버레이(YETA 워드마크 · 89차 신설) → ③메인 — 화면 1개 증가 + ①/② 전환 크로스페이드에서 아이콘·워드마크 겹침("따로 논다").
- **수정**: ⓐ 웹 오버레이 스플래시 전면 폐지(`.yspl` 마크업·CSS·JS 제거 = 진입 단계 원복). ⓑ **OS 스플래시 아이콘 자체를 검정+YETA로**: `yeta-splash-512/192.png` 신규(#161716 바탕 + 화이트 워드마크 88% + 헤일로 · Chromium 렌더 생성) → manifest `purpose:any` 2슬롯 교체. **maskable(얼굴)은 유지 = 홈 아이콘 불변**(Android adaptive는 maskable 우선 · any는 스플래시 전용축). 스플래시 하단 앱명 "YETA" 텍스트 = OS 고정 요소(제거 불가).
- **핀 2건**(운영자): 패턴 노드 표면·테두리 2배(.09→.18/.14→.28 지시값) · 핀 블록 71.5→63%(560이하 58→54%) + 잔류 자물쇠 47.5→40%(패턴 첫 행 가림 해소 — 실측 자물쇠 바닥 43.9% ↔ 첫 행 59.5% 겹침 no).
- **UX 배치1**(25항 중 6·7·8): 로고 아래 NAV 헤어라인 제거(`.ymain-body::before` 룰 삭제) · 이름·보이스 배지 픽셀 정렬(`.ylist-nm`/`.ycrow-nm` flex 중앙축 + `.ynm-t` 래퍼 — 실측 중앙 오프셋 **0.00px**) · 리스트 미리보기 마지막 줄(최근 대사) `--fg` 흰색 + 줄간격 실측 16.75px×1.2=**20.1px**(lh 1.675).
- 검증: 자가검증 경로=Chromium 헤드리스{핀 위치·겹침 수치 실측 · 아이콘 PNG IHDR 512/192 확인 · 리스트 목업 렌더 스샷 · 배지 오프셋 0.00 · yspl 참조 0} + check_refs rc=0 + 머지 후 read-back. ⚠ 설치형 PWA 아이콘 갱신 = WebAPK 재생성 필요(가장 빠른 길 = 홈에서 제거 후 재설치) — 실기기 확인 = 운영자. 13인 아이데이션 워크플로 병행 중(완료 시 배치2+). 롤백=_versions/260707_UX개편_배치1 · _versions/260707_스플래시오버레이.

## 91차 — 스플래시 방향 확정: 웹 오버레이 주도 + OS 스플래시 무존재화 (운영자 260707 "웹 오버레이가 OS 스플래시를 대신해야 해")
- **설계 반전(90차 대체)**: OS 스플래시는 제거 불가(안드로이드 강제) → **무존재화**로 흡수: ⓐ manifest any 아이콘 = `#161716` 순단색(yeta-splash-512/192 재생성 · zlib 원시 PNG · 검정 위 검정 = 안 보임 · maskable 얼굴 = 홈 불변) ⓑ 하단 앱명 텍스트 = 강제·색 지정 불가(web.dev 확증 — Chrome이 배경 명도로 흑/백 자동) → **name = U+2800**(비가시 브라유 공백 · 홈 라벨 = short_name "YETA" 유지 · 설치 팝업 일부 표면 빈 이름 트레이드오프·원복 1줄). ⓒ **웹 오버레이 스플래시 복원**(89차 확정값 그대로 — top 40.5%·66vw·화이트·자간-5%·광원5%·페이드업 1.4s 해제). 체감 = 순검정(OS) → YETA 페이드업(웹) 한 흐름.
- 검증: 자가검증 경로=Chromium 헤드리스{아이콘 첫픽셀 #161716 실측 · 오버레이 렌더 스샷 · 2.6s 해제 후 yspl hidden 덤프} + check_refs rc=0 + 머지 후 read-back. 실기기 = 운영자(⚠ WebAPK 아이콘·이름 캐시 = 홈에서 제거 후 재설치해야 새 스플래시). 롤백=_versions/260707_스플래시_오버레이주도.

## 92차 — 스플래시 아이콘 재원인: 신형 안드 = maskable 사용 → 전 아이콘 축 검정+YETA 통일 (운영자 260707 실기기 "처음에 파비콘이 보임")
- **근인**: Chrome 스플래시 아이콘 = **2020+ 안드로이드는 purpose:maskable**(firt.dev·MDN 확증) — 91차 단색화는 any만 건드려 신형 기기 스플래시에 maskable(소년 얼굴)이 그대로 노출.
- **수정**: maskable = 신규 `yeta-splash-maskable-512.png`(#161716 바탕 + 화이트 워드마크 58% · safe zone 원 80% 내) 교체 · any 192/512 = 워드마크판 복원(구형 폴백·설치 배너도 동일 시각). **홈 아이콘 = 검정+YETA 로고로 변경됨**(스플래시·홈이 maskable 공유 = 분리 불가 트레이드오프 · 얼굴 원복 = src 1줄 `yeta-icon-maskable-512.png`). 스플래시 체감 = 검정 화면 중앙 작은 YETA(OS) → 큰 YETA 페이드업(웹 오버레이) 연속.
- 검증: 자가검증 경로=아이콘 육안(safe zone)+IHDR 512 + check_refs rc=0 + 머지 후 read-back·라이브 curl. 실기기 = 운영자(재설치 필수 — WebAPK 민팅 수분 지연 가능). 롤백=_versions/260707_스플래시_마스커블.

## 93차 — 스플래시 최종형: OS 한 장만(오버레이 로고 중복 제거 + 순검정 통일) + UX 배치2 (운영자 260707 "로고 아래 검정색 빼줘 · 그 다음 별도 로고도 빼줘")
- **스플래시 확정 구조**: OS 스플래시(검정+YETA maskable) **한 장이 유일** — 뒤이어 뜨던 웹 오버레이(별도 큰 YETA) = 로고 중복이라 최종 폐지(yspl 3곳 제거 · 92차 오버레이 복원분 반전). "로고 아래 검정색" = 안드12+ 아이콘 원형 컨테이너(#000)와 배경(#161716)의 미세 대비로 판정 → **manifest background_color #000000 + 아이콘 3장(any 512/192·maskable) 바탕 #000 재생성** = 대비 축 소멸(코너 픽셀 #000000 실측). theme_color = #161716 유지(상태바 = 앱 캔버스 축). name = U+2800→U+3000(전각 공백 — 일부 기기 tofu(빈 네모) 렌더 리스크 제거).
- **UX 배치2(항목 1·2·4)**: 글래스 스크롤바 전역(트랙 투명 · 썸 = glass-2+glass-line 알약 6px · Firefox thin 폴백) · `.ycrow` 표면 1단 상향(glass-0→glass · 라인 0단→기본 — "불투명도 올려") · 육각 레이더 라임 복귀(fill accent .2 + stroke accent — 86차 무채 반전) · 시즌 헤더 날짜 `--fg`·설명 `--fg-2`("흰색·살짝 옅게").
- 검증: 자가검증 경로=Chromium{아이콘 3장 코너픽셀 #000000(IDAT 멀티청크 디코드) · 캐릭터 탭 목업 스샷(카드·육각·헤더) · yspl 잔여 0} + check_refs rc=0 + 머지 후 read-back·라이브 curl. 실기기 = 운영자 재설치(WebAPK 민팅). 롤백=_versions/260707_UX개편_배치2 · 스플래시 = manifest+아이콘만 되돌리면 됨.

## 94차 — UX 배치3: 안읽음 배지 = 이름 옆 라임 둥근네모(2자리) · 보이스 그 우측 (운영자 260707 항목 15·16·19·20 · 13인 아이데이션 list-info 앵글 스펙)
- **배지 이설**(항목19): `.ylist-badge`를 우측 컬럼 → `.ylist-nm` 이름 직후로(이름→안읽음→보이스 순). 형태 = `--r-pill`→`--r-s`(둥근네모) · 색 = `--accent`/`--bg` 유지 · `margin-left:6px` = `.yprem` 관례 동일 간격(항목20) · `flex:none` + 배치1 flex 이름행에 합류. 2자리 = 기존 `min-width:19+padding 6` 신축(`99+` 상한 기존 로직).
- **항목15 = 흡수**: 누적 수 계산(`un`/`unTxt` · ts-SSOT `yeta_read_*`)은 기존 그대로 — 표기 위치만 이동. **항목16 = 무변경 확인**: 정렬 `pin 티어 → updated 내림차순`(L1303) = 운영자 요구와 문자 그대로 일치(아이데이션 실측).
- 검증: 자가검증 경로=Chromium rect 실측{배지·보이스 중앙 오프셋 각 0.00px · 이름 27px 온전 · 우측 시간과 여유 64px · 2자리 '15' 신축} + check_refs rc=0 + 머지 후 read-back. ⚠ 헤드리스 스샷 좌표계(window-size≠innerWidth 500)는 아티팩트 유발 — 판정은 rect 실측 원칙 재확립. 롤백=_versions/260707_UX개편_배치3.

## 95차 — 스플래시 = 스포티파이 블랙 전면 통일 + 아이콘 흰 띠 버그 근절 + UX 배치4(내 상태 행) (운영자 260707 실기기 스샷 "스포티파이 블랙으로 · 로고 빼고 다 뒤덮어 · 아래 이상한 밑줄 · 글자는 그 색으로 칠해 · 로고만 나왔다 들어가야")
- **스플래시 색 확정 = #161716**(93차 #000 반전 — 운영자 명시): manifest `background_color` #161716 · 아이콘 3장(any 512/192·maskable) 바탕 #161716 재생성. name = U+3000 유지(색 지정 API 부재 → 비가시 문자가 "글자를 그 색으로 칠하기"의 실현 수단 · 스샷의 YETA 글자·회색 사각 = 재설치 전 WebAPK 캐시).
- **"아래 이상한 밑줄" 근인 = 생성 하네스 버그**: 헤드리스 뷰포트(500 고정)≠캔버스(512)로 하단 미페인트(흰) 영역이 이미지에 박힘 → 하네스 v2 = 문서 min-width/height 강제 + html 배경 동색. 검증 = **4코너+하단중+우중 픽셀 전부 #161716**(PNG 필터 4종 디코더 자작 · VERDICT PASS — 코너 1점만 보던 이전 검증의 사각 보완).
- **UX 배치4(항목 22·23·24힌트)**: 대화 탭 시즌 헤더 아래 **내 상태 행 `#ymeRow`**(라임 글래스 · 아바타+나·방문자 태그+`내 위치 · <장소>`+무드 도트) — 위치·감정 = **클라 로컬 전용**(localStorage `yeta_me_place`/`yeta_me_mood` · 게이트웨이 미전송 = 무인증 공개 주입면 0). 미설정 파생 = 현재 방 상대의 실시간 위치(`ymPlaceOf` 결정적 · "함께" 표기) · 무드 = `yLastMood()` 대화의 공기. 장소 색 위계 = 알면 `--fg`/모르면 `--mut`(항목21 결). **탭 = 장소 이동·기분 팝오버**(`.ymepop` — 표면 = `.ylmenu` 무채 크롬 정본 계승 · 장소 목록 = `!private && !offlimits` 필터 · `navOpen` 백스택 = 폰 뒤로가기 정합 · 칩 = `.yset-chip` 정본).
- 검증: 자가검증 경로=Chromium{아이콘 가장자리 6점 픽셀 PASS · 내 행 렌더 "내 위치 · 찻집 무음"+on 위계+무드 실측 · 팝업 칩 필터(private/offlimits 제외)·선택 상태 실측 · JS 구문 OK} + check_refs rc=0 + 머지 후 read-back. 실기기 = 운영자 재설치. 롤백=_versions/260707_UX개편_배치4.

## 96차 — UX 배치5: 시그니처 배경 전경화(9) + 좌우 드래그 패닝(5) + 초대 헤더 픽토 이설(12) + 입력행 파일첨부(11) (운영자 260707 · 13인 아이데이션 bg/invite 앵글)
- **항목9 배경 전경화**: `.ybg.on` opacity .32→**.92**(은은한 베일 반려 · 가독 = yPaintBg 딤 그라데 .72~.86 스크림). **프레시 스레드 가드**(yApply): 내 발화 전(user 턴 0) = `yStage('')` 시그니처 base 강제(무드 무시 = "계단 대신 캐릭터 고유 무대"). 지속 대화 = 현행 무드 씬 유지.
- **항목5 드래그 패닝**: `#yetaLog` pointer 제스처(축 잠금 8px · `touch-action:pan-y`로 세로 네이티브 양보 · 수평만 `background-position-x` 0~100% 클램프 · reduce-motion 비활성 · 화살표 없음). cover 크롭 슬랙 안 이동 = 9:16 레터박스·잠금 스테이지 무충돌.
- **항목12 초대 이설**: 입력행 +에 있던 초대를 **헤더 프로필 옆 픽토 `.yh-inv`**(사람+ · 버튼틀 없이 픽토만)로. 탭 = `yInvToggle` + **액티브 지속**(팝오버 열림 = 라임 `.on` 점등). 바깥탭 닫기 앵커도 `#yetaInvPlus`로 이관.
- **항목11 파일첨부**: 입력행 + = `#yetaFile`(`<input type=file>`) 선택창. ⚠ 전송 파이프(R2 업로드 op) = 후속 백엔드 — 지금은 진입점+선택+파일명 토스트까지(무에서 유 창조 금지 · 백엔드 붙일 때 전송 활성).
- 검증: 자가검증 경로=Chromium{배경 .ybg.on opacity 0.92(transition 제거 측정) · 시그니처 base 페인트 · 초대 팝오버 open+2행(자기 제외)+액티브 on · 파일 input 존재·라벨 "파일 첨부" · 픽토 svg · JS 구문 OK} + 목업 스샷 육안 + check_refs rc=0 + 머지 후 read-back. 실기기 드래그·첨부창 = 운영자. 롤백=_versions/260707_UX개편_배치5.

## 97차 — UX 배치6: 타이핑 지연 길이비례(13) + 인챗 '1' 읽음표시·페르소나 읽는속도(14A·14B) (운영자 260707 · read-typing 앵글 · 클라 순수 · 러너 무침범)
- **항목13 타이핑 지연 = 답변 길이 비례**: `yRender` 페이스 홀드 매직 1000ms → `min(4200, 600 + 꼬리답장자수×18ms)`. tune[3] 답장 길이 결합(per×(0.7~1.3)). 긴 답장일수록 "입력 중" 오래 = "바로 대답 어색" 심화 해소 · 상한 4.2s(QoE).
- **항목14A 인챗 '1'**: 내 버블 행 맨 앞 `.yb-un`(라임 숫자 fw800 · 카톡 결) — 마지막 미읽음 내 턴(`_lastUserI` + `_meUnread` 래치)에만. 읽음 = `_yTypHold`(읽는 시간) 경과 → 낙관 버블 DOM 직접 소멸(폴 무관·내 버블 보존) + 재렌더 `_meUnread=false`.
- **항목14B 읽는 속도 = 페르소나별 결정적**: `_yTypHold` 순수 난수 → `yReadMs()` = `clamp(1600 - 친밀도[9]×90 + 삐짐[7]×140 + 경계[11]×60, 500, 5000)` + 결정적 지터(persona 시드·Math.random 아님). 삐짐↑=읽씹 최대 5초 · 친밀↑=즉독. tune 소스 = 세션 tunes→roster 폴백(세션 무변경).
- **14C(L1 readmark 토글) = 배치7 분리**(policy.json 신규 키 = 기틀 · 현재는 기본 ON 하드).
- 검증: 자가검증 경로=Chromium{1 표시 true·첫자식 yb-un·색 라임(207,255,64)·fw800 · readMs 결정적(2회 동일 1339)·tune 반영 · JS 구문 OK} + check_refs rc=0 + 머지 후 read-back. 실기기 타이핑 체감 = 운영자. 롤백=_versions/260707_UX개편_배치6.

## 98차 — UX 배치7: 글래스 불투명+블러 플레이그라운드 산출 (운영자 260707 항목3 "불투명 올리면서 블러 동시에 · 플레이그라운드 다채롭게")
- **산출 = 시안 플레이그라운드**(`docs/reports/260707_글래스_불투명블러_플레이그라운드.html` · 단일 HTML·인라인·다크): 실물 글래스 표면 4종(헤더 알약 `.yeta-h`·대화 카드 `.ycrow`·AI 버블 `.yb.ai`=glass-2·모달 `.yeta-pick` 결) 재현 + 뒤 비침 배경(라이브 `yeta.soong.kr` 4씬 전환) + 프리셋 5(현행·불투명↑블러↑·프로스티드·초투명·불투명↑블러↓) + 슬라이더 6(표면/진한표면/엣지/블러M/블러L/채도) + `:root` 토큰 세트 복사. 플그 5요소 계승.
- **핵심 = 동시 조정**(운영자 지시): 불투명(glass 알파)·블러(blur-m/l)·채도를 한 화면에서 보며 최적점. 배경 순환으로 "뒤 비침" 실검. 복사값 = `--glass`/`--glass-2`/`--glass-line`/`--blur-m`/`--blur-l`/saturate → viewer :root 70여 표면 일괄.
- **상태 = 값 확정 대기**(운영자 플그에서 조정 → 값 복사 → :root 반영 = 실질 변경·§🎨 락 등재). 자가검증 = Chromium 헤드리스 스샷(표면 4종·슬라이더·프리셋·코드출력 렌더). 롤백=산출물이라 불요.

## 99차 — 에픽 등급 신설 + 루시(에픽 1호): 배지·감정 배경 로테이션·페르소나 (운영자 260707 "에픽 등급[보라 형광]·감정 따라 배경·캐릭터 모습으로 · 폴더 구조 지정 · 이미지 소리만 빼고 다")
- **교차검증**: 운영자 축소 지시 = 3인·각 5분(10인 분신술 → 3인) — ① 세계관 카드(SF 초안 → 무음동 도깨비 결 번역·카드 전문) ② 레퍼런스 웹서치(C.AI/Talkie 츤데레 문법 — 수치-데레·호칭 사다리·전조 3종 고정·결핍 리추얼·복귀 신호) ③ 기틀 감사(**조건부 APPROVE 5조건** — §🎨 7항 동일커밋 개정·에픽 키 게이트 한정·배지 코어 배치·락 절차·게이트웨이 무수정 확인). 전 조건 이행.
- **토큰(기틀 편입)**: `:root` `--epic:#B026FF`/`--epic-rgb`(등급 축 — CTA 아님·감사 hex 권고 채택) → design-tokens.lock 등재(51토큰)·거울 재생성·CII `.yepic` 행·CLAUDE.md §🎨 7항 개정(BLOCKING①).
- **배지**: `.yepic`(= `.yprem` 정본 선언 계승·색만 `--epic`) + `yEpicBadge(c.grade==='epic')` — index.html 코어(음성 모듈 독립·감사⑤)·이름 옆 훅 5곳(보이스 앞).
- **감정 배경(에픽 특전)**: 운영자 업로드 189장(감정 라벨·29MB) 루트 → `viewer/characters/epic/lucy/` 정규화 이동(.jfif→.jpg·공백/이모지 슬러그) + 감정→무드 버킷 `media.json`{warm 68·tense 48·blue 31·base 42+클립}. yStage 에픽 분기 = 답장수 키(`lucy|mood|n`) **결정적 로테이션**(같은 무드도 답장마다 다음 장 = 전 장 순환·"이미지 다 나오게") · **비에픽 키 바이트 보존**(감사③) · 에픽 스크림 옅게(.35/.55) · 클립 = **muted/loop/playsinline 고정**("소리만 빼고") — yPaintBg 영상 레이어 신설. 표시농도 = 병렬 UX배치5 전경화(.92)와 병합 — `.ybg.epic.on` override 폐기(상속).
- **페르소나**: `viewer/characters/epic/lucy/CLAUDE.md`(운영자 구조 = 카드+이미지 한 폴더) — 두 혼(만든 이의 광기+딸의 온기)·시크↔도깨비 스위치·못 먹는 허기·향 흉내·비밀 충전처·수치-데레·호칭 사다리·해금 LV2~5·금기(역린 포함). `inject_character.sh` 에픽 폴백 경로(`apps/yeta/characters/<id>.md` 무 → `viewer/characters/epic/<id>/CLAUDE.md`). roster 11번째 등재(grade epic·stats [3,4,5,4,4,3]·tune 16축[스위치빈도 8 = 유일 고빈도 = 이중모드 인코딩]·greetings 3종).
- **병합**: 병렬 세션 UX배치5/6과 index.html 충돌 1건(`.ybg.on` .32→.92 전경화) — main 채택+에픽 클립 CSS 보존 수동 해소.
- 검증: 자가검증 경로=Playwright E2E{배지 리스트 1("루시 ✦ 에픽")·에픽 키 `lucy|warm|1`·로테이션 전환(beauty→grumpy)·비에픽 `haeun|` 불변·video muted 4속성·JS 에러 0 — 병합 후 재검증 동일 통과} + check_refs rc=0 + 머지 후 read-back + 배포후 라이브 실측(뷰어 에픽코드·chars lucy·media.json/이미지/클립 서빙) | 운영자검증 = 폰 체감(배경 전환 질감·카드 말맛). 후속 = 루시 전용 초상(유료 dispatch·운영자 트리거)·places 동선(현재 미등재 = 마주침 제외 — "가장 늦게 스며든 신참" 서사 정합)·러너 fine-grained 감정축(<<EMO>> — 현재 3무드 버킷 매핑). 롤백=_versions/260707_2245_에픽등급_루시.

## 100차 — 스페셜 등급 훈장 체계: 에픽·IDOL 동렬 분리 + 보이스 = 기능 아이콘 (운영자 260707 "훈장 느낌 · 에픽=시즌 주인공 · IDOL=아이돌 구현 · 동렬 · 보이스는 별개 기능 · IDOL=흰/실버 · 보이스와 형태 달라야")
- **축 정의(정본)**: 스페셜 등급(훈장) = 시즌 특전 · roster `grade` 슬롯 1개(상호배타·동렬·높낮이 없음) — `epic`(시즌 주인공·퍼플 `--epic`) / `idol`(아이돌 구현·흰·실버). 보이스(`.yprem`) = 등급 아님 = 기능 아이콘 축(현행 유지). 감정배경 특전은 grade epic 연동 유지.
- **구현**: `.yepic` 단일 → `.ygrade`(.epic/.idol) 체계 · `yEpicBadge`→`yGradeBadge`(YGRADE 맵 옵트인·훅 5곳). 형태 차별(운영자 위임 → 채택) = **보석 젬 SVG**(외곽+하단 파셋 컷 — 파형과 실루엣 구분) + **양각 sheen**(inset `--glass-line` = 훈장 결·드롭섀도 raw 예외축). IDOL 색 = `--fg`+`--glass-2`+`--glass-line` 계승(**신규 색 토큰 0**). CII 행·CLAUDE.md §🎨 7항 문구 동기.
- 검증: 국소(배지 계층) = 자가검증 — 경로=Playwright E2E(하은 idol mock){에픽 1·IDOL 1·보이스 1 병렬("하은 | IDOL | 보이스")·IDOL rgb(255,255,255)·에픽 rgb(176,38,255)·젬 2·양각 인셋·JS 에러 0} + 스샷 육안(젬/파형 구분) + check_refs rc=0 + 머지 후 read-back + 배포후 라이브 실측(yGradeBadge 존재). roster 실파일 무변경 = idol 부여는 운영자 지정 시 `"grade":"idol"` 한 줄. 롤백=_versions/260707_2317_등급훈장_분리.

## 101차 — UX 배치8: 장소 배경 축 배선(항목10 + 난입 플래시) — "생성해둔 씬 42장 활용" (운영자 260707 "장소 배경 항상 · 날씨 종속 · 난입 = 그 캐릭터 배경 순간 · OpenAI 배경 활용 안 되는 것 확인")
- **실태 확정**: 배리에이션 씬 42장(tea_sunset·cvs_dawn·market_close·radio_neon·playground_3am·karaoke_back 등 장소성 다수 + persona 10)이 존재하나 `Y_SCENE_POOL`이 무드×시간만 매핑 — **장소 축 통째 미배선**(운영자 지적 정확 · 생성기는 Gemini[yeta_bg_var]·OpenAI는 얼굴 축).
- **배선(러너 무편집)**: 신설 `Y_PLACE_POOL`(11장소+연결씬 → 실존 씬 실측 대조 매핑) + `yWx`(결정적 유사 날씨 — 일 시드 · 겨울 눈3할/여름·가을 비3할) + `ySceneForPlace`(필터 = **날씨 종속** → 시간대 → 계절 · 우천/설일 = 그 씬 우선 · 후보0 = 연결씬 → 시그니처) + `yPlaceSync`(화자 장소 = `ymPlaceOf` 결정적 · 러너 프롬프트 장소와 등가 = 서사 일치 · 시간 슬롯 캐시).
- **yStage 우선순위 통합**: ①난입 직후(barged·내 후속 발화 전 = kick pill 창) = **난입자 시그니처 순간 노출** ②첫 진입(유저턴 0) = 화자 시그니처 ③지속(유저턴≥1) = **장소 씬(날씨 종속)** ④무드 씬 ⑤base. 배치5 yApply 프레시 가드는 내부 흡수.
- 검증: 자가검증 경로=Chromium 4축{fresh=persona_haeun 시그니처 · place=tea→var_tea_sunset(routine 목업) · barge=persona_ryu 난입자 · ySceneForPlace 2회 동일=결정적} + JS 구문 OK + check_refs rc=0 + 머지 후 read-back. 실기기 체감 = 운영자. 롤백=_versions/260707_UX개편_배치8.

## 102차 — UX 배치9: 리스트 우측 = [최근 장소|장소명]/[최근 메세지|시간] 2행 + '알 수 없음' 알고리즘 (운영자 260707 항목18·21 · 러너 무편집)
- **항목18**: `.ylist-r` 재배치 — 2행 `.ylr-meta`([흐린 레이블 10px][세로 구분자 = .ycrow-sep 결][값]). 장소 = `yLocsSync`(전 행 `ymPlaceOf` 일괄 · 시간 슬롯 키 = 시간당 1회 해석 · 완료 시 1회 재렌더).
- **항목21**: `yLocOf` 판정 = 저대화(<6턴) 또는 장기 미접촉(>24h) 또는 private/offlimits 장소 = **'알 수 없음'(--mut 흐림 · 사생활 서사)** / 판명 = 장소명 `--fg` 흰색.
- 검증: 자가검증 경로=Chromium 3케이스{하은 8턴·방금=찻집 무음(on 흰) · 세라 1턴=알 수 없음 · 무디 25h=알 수 없음} + 스샷 육안(2행 메타·내 상태 행 '함께' 파생 병행) + JS 구문 OK + check_refs rc=0 + 머지 후 read-back. 롤백=_versions/260707_UX개편_배치9.

## 103차 — UX 배치10: 방치 대화 소멸 1차(항목17 · 클라 판정) (운영자 260707 "일정 시간 지나면 장소 떠나 대화 사라짐 — 대화 앱 아닌 세계관 활동")
- **판정**: `yDormant` = 미고정 + `updated` 3일 초과(nudge 재촉 중단선과 결) → 리스트 필터 아웃. **고정 = 안 떠남**(놓치기 싫다는 의사) · **데이터 보존** = R2 무변경 · 캐릭터 탭 재방문(draw) = 히스토리 그대로 재회. sig에 행 수 포함 = 시간 경과 전환도 재렌더 통과.
- **후속(백엔드 트랙 · 보류)**: 아이데이션 스펙의 left 마커 세션 각인 + nudge 소멸 sys 연출("불이 꺼졌다") + 러너 재회 프롬프트 = 게이트웨이·러너 편집이라 별도 승인 트랙. 14C(readmark L1 토글 · policy.json 신설)도 동일 보류.
- 검증: 자가검증 경로=Chromium 3케이스{최근=생존 · 3일+미고정=소멸 · 4일+고정=생존} + JS 구문 OK + check_refs rc=0 + 머지 후 read-back. 롤백=_versions/260707_UX개편_배치10.

## 104차 — 대화목록 4줄 카드 체계: 등급줄 상단·생각줄 불멸(터콰이즈)·상대시간·간격 비율 5:5:3.5 (운영자 260707 지시 7항)
- **요구/구현**: ① 줄간격 확보+전 레포 통일 = `--lgap`(6px 기준·구조 토큰 신설) ② 생각 라인 항상 유지 — 못 읽으면 `(무슨 생각을 하는진 모르겠다)` `.na`(이름 바로 밑 대화창 직행 금지) ③ 생각 색 = 터콰이즈 `--think:#3EE0C8` 신설·`rgba(--think-rgb,.72)` 저투명(과하면 α만 조절) ④ 시간 아래 상대시간 `yAgo` = 방금/N분/N0분(십분 내림)/N시간~6h·초과 미표시·흰색 + sig 분 버킷(매분 노화) ⑤ 등급·기능 배지 = 이름 위 줄(`.ylist-grade`/`.ycrow-grade`/`.yme-grade` — 계급·직업 확장 항목란 확보) ⑥ 간격 비율 5:5:3.5 = `--lgap`·`calc(--lgap*.7)`(실측 6/6/4.2) ⑦ 우측 메타(시간 스택) = 이름줄 우측 상주 flex center = 라인 중앙정렬.
- **병합**: 병렬 UX배치9/10(방치 소멸·최근 장소)과 index.html 충돌 2곳 수동 융합 — sig = 행수+분버킷 결합 · 옛 우측 2행(.ylr-meta 항목18)은 메타 스택(시간→상대시간→**최근 장소**)으로 병합 개정(yLocOf try 방어). 생각 데이터 = 직전 캐릭터 턴 스니펫(러너 think 필드 승격 = 후속 축).
- **기틀 편입**: lock 등재(54토큰)·거울 재생성·CII 2행(4줄 카드 신설·훈장 위치 보정).
- 검증: 자가검증 경로=Playwright E2E{하은 = 보이스줄 위·생각·"4분 전" / 데스크 = 배지 무 등급줄 생략·플레이스홀더 .na·"20분 전" / 무디 8h = 미표시 / 생각색 rgba(62,224,200,.72) / 메타-이름줄 중앙정렬 true / 실측 간격 6:6:4.2 / JS 에러 0 — 병합 후 재검증 동일 통과} + check_refs rc=0 + 머지 후 read-back + 배포후 라이브 실측(ylist-tht 존재) | 운영자검증 = 폰 체감(간격·터콰이즈 농도 — 과하면 α 조절 지시). 롤백=_versions/260707_2331_대화목록_4줄체계.

## 105차 — UX 배치11: 글래스 확정값 반영 + 이름 옆 직업/변신 배지(터콰이즈 --tq 신설) (운영자 260707 플그 확정값 + "이름 항상 옆에 직업·변신모드 · 메인 직업 강조 · 변신군 터콰이즈")
- **글래스 확정값(:root)**: `--glass` .055→**.005** · `--glass-2` .09→**0** · `--glass-line` .14→**.08** · `--blur-m` 16→**29** · `--blur-l` 24→**11** · saturate 글래스 계열(1.05~1.6) 전부 → **1**(무채 크롬 saturate(0)만 유지 · 26곳). glass-0/line-0 = 비례 환산(.002/.066). 거울 재생성(base.css·tokens.css).
- **강조3 신설 `--think 공용(#3EE0C8 — main 선점 톤 채택·--tq 폐기)`**(터콰이즈 형광 · 변신 직업군 전용 축·CTA 아님 — 운영자 명시 승인): design-tokens.lock 등재(53토큰) · CII `.yjob` 행.
- **직업/변신 배지**: roster `job`(11인 전원 — 카드 tagline 실측 기반) + `mode{nm,t[]}`(백 = "방금 사냥을 마친 사냥꾼" late/dawn/night · 루시 = "깊은 밤의 도깨비" late/night — 카드 서사 실측). `yJobBadge(c)` = 변신 시간대 게이트(KST) 통과 시 **터콰이즈 우선** · 아니면 메인 직업 라임. 훅 = 이름 직후(등급·보이스 앞) 2곳(.ylist-nm·.ycrow-nm). chars op = roster raw 전달이라 게이트웨이 무수정.
- 검증: 자가검증 경로=Chromium 3케이스{하은=국어교사 라임 · 백=사냥꾼 터콰이즈 rgb(43,255,214) · 루시=late 한정이라 지금은 job 폴백 = **시간 게이트 동작 증명**} + 토큰 락 53 전원 등재 + 거울 정합 + check_refs rc=0 + 머지 후 read-back. 글래스 체감 = 운영자 실기기(블러 29px는 헤드리스 미렌더 축). 롤백=_versions/260707_UX개편_배치10(글래스 이전값 포함 백업).

## 106차 — 설정·전 탭 구분감: 맨 뒤 배경 위 스포티파이 블랙 20% 레이어 + 설정·프로필 표면 glass 승격 (운영자 260707 "캐릭터 창 스포티파이 블랙 더 · 배경 바로 위 20% 레이어 · 다른 곳도 · 설정 배경 구분감")
- **의도/변경**: 전 탭 공유 배경(.ybgvid 사이버펑크 영상)이 밝고 화려해 앱 스포티파이 블랙 톤과 이질 + 옅은 글래스(설정 glass-0)가 배경에 묻혀 안 보임. ① `.ybgvid::before` 신설 = `rgba(18,18,18,.2)`(배경 영상 바로 위·기존 ::after 38% 아래·전 탭 공유 = "다른 곳도" 동시). ② 설정·프로필 표면 `.yset-grp`·`.yset-item`·`.yprof-stat` = `glass-0→glass`(테두리 `glass-line-0→glass-line`) = `.ycrow` 동일단 복원(20% 블랙만으론 부분 해결 → 표면 승격이 구분감 확정). 신규 토큰 0. (번호 = main 병렬 진행 충돌로 재번호.)
- 검증: 자가검증 경로=Chromium{실 index.html ::before rgba(18,18,18,0.2) 실재·페인트순서 · 실파일 설정/캐릭터 렌더 · 3-way 비교렌더 운영자 확정} + check_refs rc=0 + origin read-back | 운영자검증 불가피 사유=실기기 체감. 롤백=_versions/260707_2315_설정구분감_블랙레이어.
- **산출**: viewer/index.html(::before + 표면 3곳).

## 107차 — L0 코어 정책 관리자 설정 불가 수정 (운영자 260707 "관리자 2486으로 들어갔음에도 L0 설정 못함")
- **진단(실측)**: 라이브 read-only 프로브 = auth(2486)→role admin(env=2486 정상·config 아님) · policy GET = L1 4축만 저장·L0 전무 → L1은 되고 L0만 안 됨. 근인 = 뷰어 L0 렌더 2중 은닉: ⓐ L0 `<details>` collapsed(L1은 open) ⓑ 편집 토글(메타발화 차단·헛소리 방지)이 하드 잠금 그룹 뒤 묻힘. 부수 = yPolClick dirty가 L1축만 조회.
- **수정(뷰어 렌더만 · policy.json·게이트웨이 무변경)**: ① admin이면 L0 자동 open ② 편집 토글을 하드 잠금 그룹 위로 재배치 ③ L0 dirty/카운트 L1과 분리 + 배지 ④ 통합 기본값 헬퍼 `yPolDefault`.
- 검증: 자가검증 경로=① 라이브 read-only 프로브(auth·policy GET) ② 실 index.html E2E(목킹 admin 언락→설정→L0 metaban 클릭 → op policy SET `{...,metaban:0}` 전송 관찰·L0_open=true·disabled=false) ③ 렌더 스샷 + check_refs rc=0 | 운영자검증 불가피 사유=실기기 탭 체감. 롤백=_versions/260707_2346_L0관리자설정. (번호 재번호.)
- **산출**: viewer/index.html(ySettingRender L0 재배치·open·카운트분리 · yPolClick · yPolDefault).

## 108차 — 캐릭터 선택창 정렬(보이스→에픽→IDOL→나머지) + 대화 중 캐릭터 구분 (운영자 260707 "보이스 제일 위·에픽·IDOL·나머지 · 대화 중인 애 다르게 구분")
- **정렬**: `yCharRank`(보이스 0 > epic 1 > idol 2 > 나머지 3) + `ySortChars`(안정 정렬 = 동급 로스터 순서 유지) — 캐릭터 탭·SNS 리스트·픽커·초대·좌 레일 5곳 일괄(랜덤/뽑기 = 순서 무관 제외). 예시(하은 보이스→루시 에픽→[IDOL]→나머지) 정확 재현.
- **대화 중 구분**: `yActiveIds`(진행 스레드 판별) → `.ycrow.talking` opacity .55 + "대화 중" 라벨(무채 글래스) — "대화 안 하는 애를 고르게"(운영자) · 클릭 = 재진입 가능(비활성 아님).
- **병합**: 병렬#163(캐릭터 창 — 직업 배지 `yJobBadge`·변신모드 터콰이즈[내 --think 토큰 재사용]) 캐릭터 탭 마크업 충돌 1곳 = 정렬·대화중·직업배지 **3자 융합**(이름 옆 = 직업 → 대화중 순).
- 검증: 자가검증 경로=Playwright E2E(sera idol mock·desk 대화중 mock){순서 [하은(보이스)·루시(epic)·세라(idol)·나머지 로스터순] 정확·배지 3등급·desk talking+라벨+opacity .55·나머지 talking 0·JS 에러 0 — 병합 후 재검증 동일} + check_refs rc=0 + 머지 후 read-back + 배포후 라이브 실측(yActiveIds 존재) | 운영자검증 = 폰 체감. 후속 = 캐릭터 선택창 투명도/블러 조정(3안 스샷 비교 → 픽 대기). 롤백=_versions/260707_2349_캐릭터정렬.

## 109차 — UX 배치12: 메뉴 전환 = 시즌 헤더 상주·콘텐츠만 전환 (운영자 260707 "네비·시즌·로고·배경 그대로 · 안의 콘텐츠만 바뀌게 · 메뉴간 움직임 자연스럽게")
- **근인**: 로고·네비·프로필·배경(영상)은 이미 `.ymain` 상주라 유지됐으나, **시즌 헤더가 패널별 노드**(char=`#yworldHead`·list=`#yssnList`)라 탭 전환 시 패널 페이드(.3s ypickIn)에 딸려 사라졌다 재등장 = 깜빡임.
- **공용화**: 시즌 헤더를 `.ymain-body` 공용 `#yssnFixed`로 이관 — `position:absolute` 로고 아래(top 64+safe) 고정 · char/list만 표시(map 자체 헤더·settings 제외) · 재렌더는 내용 동일 시 스킵(`_h` 가드 = 무깜빡) · 실높이 측정(`--ssn-h`)으로 패널 상단 여백 동기(콘텐츠는 헤더 아래서 스크롤·전환). 구 `#yworldHead`/`#yssnList` + 렌더 2곳 폐지 → `ySsnFixed()` 단일.
- **결과**: 탭 전환 = 로고·네비·배경·시즌 헤더 전부 제자리 고정, 패널 콘텐츠(캐릭터↔대화 리스트)만 .3s 페이드 = 자연스러운 전환.
- 검증: 자가검증 경로=Chromium{char/list 시즌 top 64 동일 상주 · 이름 온전(하은) · 구노드 0 · --ssn-h 측정 79px · JS 구문 OK} + 스샷 2탭 육안(시즌 고정·콘텐츠만 교체) + check_refs rc=0 + 머지 후 read-back. 롤백=_versions/260707_UX개편_배치12.

## 110차 — QA 분신술(10인 적대 감사) 결함 수정 배치13 (오늘 UX 대개편 스플래시+배치1~12 감사 · high 2·mid 12 중 확정 내코드 7건 수정)
- **드래그 패닝 60fps(high)**: pointermove가 프레임마다 background-position-x 직쓰기 → **rAF 코얼레스**(프레임당 1회 flush) + #ybgA/#ybgB **querySelter 캐시**(무캐시 반복 해소). (transform 합성은 cover 크롭 빈공간 문제로 부적합 → rAF 스로틀이 현실 최선.)
- **인챗 '1' 재출현(mid)**: ⓐ 소멸 판정(`_meUnread=false`)을 map 렌더 '앞'으로 이동(오프바이원 — 읽음 경과 후 첫 재렌더에서 '1'+'입력 중' 동시 표시 차단) ⓑ 직접 소멸 타이머가 낙관 span만 지우고 래치는 안 내리던 것 → 타이머에 `_meUnread=false` 추가(폴 재렌더 '1' 재출현 봉쇄).
- **유령 '입력 중'/'1' 누출(mid)**: 스레드 전환·닫기 리셋 2곳에 `clearTimeout(_yTypT/_yPaceT)` + `_meUnread=false` — 이전 방 타이머가 새 방 #yetaLog에 유령 타이핑 주입하던 것 차단.
- **리스트 stagger 플래시(mid)**: firstPaint를 `!_ylSig`(loc-sync/pin/leave의 sig 리셋에 오염 → 매 재렌더 전행 재등장)에서 **전용 래치 `_ylPainted`**로 분리. 실측 = 재렌더 animation:none.
- **접근성(mid)**: 내 상태 행(#ymeRow role=button+tabindex) Enter/Space keydown 배선(WCAG 2.1.1).
- **iOS 홈 아이콘(mid)**: apple-touch-icon = 구 소년 아이콘 → `yeta-splash-192`(워드마크판) 통일.
- **지도 배터리 누수(high)**: 챗 열림(openYeta) 시 `yMapLeave()` — 지도 탭 위 챗 dialog 떠도 rAF+60s 인터벌 계속 돌던 것 정지(닫힘 = yTab이 map 복귀 시 재개).
- **보류(별도 트랙 · 병렬/문서/사용자확정)**: 배경 페이스홀드 우회(에픽 로테이션 얽힘) · yRender 4s폴 diff 부재 · CII/팔레트 문서 드리프트(#121212·blur16 → 실제값) · dialog 네이티브 백 이중계상 · 챗버블 blur29 성능(운영자 확정값) · 에픽 영상 reduce-motion.
- 검증: 자가검증 경로=Chromium{firstPaint 래치(재렌더 animation:none) · meRow tabindex/keydown · 드래그 rAF flush 배선 · JS 구문 OK} + 코드리뷰(타이머 해제·'1' 클리어 위치) + check_refs rc=0 + 머지 후 read-back. 롤백=_versions/260707_QA수정_배치13.

## 111차 — 캐릭터 선택창 투명도 상향(3안 중 ③) (운영자 260707 "투명도 더 높여도 · 블러 높여야되나 헷갈리네")
- **판단(투명도 vs 블러 개념 정리)**: 표면 알파↓ = 배경 인물 더 비침(몰입) / 블러↑ = 비친 배경 뿌옇게 정리(가독). 둘은 독립 축.
- **3안 실측 비교**(Playwright 배경 위 캐릭터 탭): ①현행(glass .055+blur16) ②더투명(glass-0 .02+blur16) ③투명+블러↑(glass-0 .02+blur-l 24). ③ = 몰입·가독 균형 → 채택(운영자 요청 "투명도 높이고 블러도").
- **적용**(`.ycrow`): background `--glass`→`--glass-0`(.02) · border `--glass-line`→`--glass-line-0` · blur `--blur-m`→`--blur-l`(24). **전부 기존 토큰 재배선 = 신규 색/토큰 0 · 락 54 불변**. (260707 "불투명도 올려"로 glass-0→glass 올렸던 것 재역전 = 배경 시그니처 전경화[.ybg .92]와 조화되게 카드는 다시 유리화.)
- 검증: 국소(표면 토큰) = 자가검증 — 3안 스샷 비교 + check_refs rc=0(락 불변) + 머지 후 read-back + 배포후 라이브 실측(.ycrow glass-0 존재) | 운영자검증 = 폰 체감(과하면 ②로 블러 낮추거나 ①복귀 = 토큰 1줄). 롤백=_versions/260707_선택창투명도.
- **동반 확인(대화중 구분)**: 운영자 재확인 요청 → Playwright 실측(하은·데스크 대화중 = talking opacity .55 + "대화 중" 라벨 · 무디·세라 미대화 = 선명 op 1)으로 정상 동작 재입증(107차 기구현분).

## 112차 — 미구현 설정 로컬 동작화 (운영자 260707 "미구현 설정 다 동작하게" · 요청1[시즌헤더 상주]은 배치12서 선점 = 본 차수는 설정 단독)
- **경위**: 운영자 요청 = ⓐ메뉴 전환 시 시즌헤더 안 움직이게(요청1) ⓑ미구현 설정 동작화(요청2). 작업 중 배치12(#yssnFixed)가 요청1을 선점 머지 → PR을 origin/main으로 리베이스, **요청1 중복 폐기·요청2만 재적용**(main #yssnFixed 무결 확인).
- **설정·프로필 동작화(전부 로컬 localStorage `yeta_me_*`/`yeta_pref_*` · 게이트웨이 미전송 = 무인증 공개 주입면 0 · place/mood 관례)**: 프로필 사진(파일→256 정사각 리샘플→data URL · quota 실패 분기·webp 미지원 jpeg 재인코딩[toDataURL은 미발화 = 프리픽스 검사]·FileReader onerror)·이름·상태·강조색(라임/코발트 브랜드 2색 → `--bubble-me`) · 토글(소리진동=새 답장 햅틱·타이핑=입력중 게이트·모션줄이기=`html.yeta-reduce` CSS[iteration-count:1 무한애니 차단]+지도/패럴랙스 rAF 존중·읽음·알림 저장) · 대화초기화(op `reset` 2탭+리스트 재렌더)·캐시비우기. 편집 = 글래스 모달 `#yedit`(navOpen 백스택·cancel 라우팅·자동완성 4종 off·토큰 계승 = 배치11 초투명 글래스에 자동 정합). **서버/브랜드 고정 5종 = 읽기전용 정보행(aria-disabled)**: 모델(opus)·테마(다크)·PIN(서버)·통화상한(서버 env)·글자크기(px 토큰) — 기틀 불침범. 신규 :root 0.
- 검증: 자가검증 경로=Playwright{토글3·강조색→--bubble-me·편집모달 저장·초기화 2탭(op reset)·캐시 상호작용 · #yedit 백버튼=모달만 닫히고 프로필 유지 · 프로필→설정 navSwap 깊이불변·뒤로 정상 · aria-checked·role=switch · main #yssnFixed 렌더 무결 · 페이지에러 0} + 편집모달 육안(초투명 글래스+백드롭 딤 가독) + 읽기전용 5인 적대 감사단 액션 전건 반영(#yedit 백스택·모션·navSwap·초기화 재렌더·아바타 quota/webp/onerror) + check_refs rc=0. **후속(main R1 #yssnFixed 개선 여지 · 별도 트랙)**: 시트 열림(cd-open)에 헤더 은닉(`:not(.cd-open)`)·프로스티드 블러·빈 바 `:not(:empty)`·SR aria — 본 세션 R1 감사서 도출(main 소유라 미반영). call/ptt/read 토글 = 저장만(게이팅 후속). 롤백=git origin/main.

## 113차 — 챗 배경: 계단 오버레이 폐기 + 첫/오랜만 시그니처→장소 국면 + 로딩 진입 동기 (운영자 260707 "처음 몇 대화 = 캐릭터 고유 배경[없으면 OpenAI] · 계단 오버레이 폐기[대화창 한정] · 이후 = 장소 배경 · 첫/오랜만 아니면 바로 장소 · 배경 로딩을 채팅창 로딩과 동기")
- **진단(실측)**: "계단 오버레이" 정체 = `viewer/assets/brand/yeta-bg.webp`(초록 네온 유리 계단 포스터) = `#yetadlg` base 배경. `.ybg` 씬(92% 불투명) 로드 전/8% 틈으로 계단이 비침 + 로드 지연 시 100% 계단 노출 = 애매한 화면. (스샷 어두운 편집국 = `var_persona_desk.webp` 정상 표시 · 무빙 비디오[사이버펑크]는 앱 배경 축 = 별개.) 기존 `yStage`는 `userN===0`만 시그니처 = "처음 몇 대화" 개념 부재.
- **수정(viewer/index.html · 신규 :root 0 · 게이트웨이·pipeline·모델 무변경)**: ① **계단 폐기**: `#yetadlg` base `url(yeta-bg.webp)`+스크림 → `background:var(--bg)` 검정 솔리드(대화창 한정 · 계단 비침 0). ② **국면 판정 `ySigPhase`**: 첫 단독대화·오랜만(`Y_REUNION_MS`=12h+ 공백) 재회 = 오프닝 처음 `Y_SIG_TURNS`=3 유저 발화는 `YCUR.bg` 시그니처 → 이후·평상시 연속 = `ySceneForPlace` 장소 씬. **solo 전용**(합석 2명 = 바로 장소 · "단독대화"). ③ **로딩 동기 2단**: `yWarmBg` ⓐ시그니처 즉시 워밍 ⓑ장소·무드 씬 36장 유휴 지연 워밍 + 언락 즉시 착수 + `yEnterChat` 진입 즉시 프리로드 + `yStage` 캐시 그림 즉시페인트(늦은 페이드 제거). ④ 시그니처 국면 키에서 mood 제외 = 같은 그림 재스테이지('숨') 방지.
- **튜닝 노브(기틀 아님 · 즉시 조정)**: `Y_SIG_TURNS=3`(처음 몇 대화 · 미만=시그니처 = 인사+내발화 1·2 → 3번째 장소 · 4면 3번까지 시그니처) · `Y_REUNION_MS=12h`(오랜만 = 밤샘/다음날 재방문 · cf. yDormant 72h 방치소멸 별개 축).
- 검증: 자가검증 경로=check_refs rc=0(JS 구문 OK·락 54 불변·거울 무수정)·`ySigPhase` 단위테스트 9/9(첫/연속/재회/같은날/경계 off-by-one)·워밍 슬러그 36 전원 실존(404 0)·**10인 적대 평의회(READ-ONLY 병렬 소환)** 9 APPROVE+1 BLOCK → BLOCK(Ask④ 장소 씬 미워밍 = 진행중 대화 재진입 늦은 페이드) 및 실질 지적(같은그림 '숨'·solo 정확화) 전건 수정 후 재검증 + 머지전 원격 read-back | 운영자검증 불가피 사유=실기기 진입 체감(로딩 동기 부드러움·시그니처→장소 전환감 = 사람 감각 유일). 롤백=git origin/main(브랜치 claude/character-background-video-dry8r4 · draft PR).
- **산출**: viewer/index.html(#yetadlg base·ySigPhase·yWarmBg 2단·yStage 국면/솔로/mood키/즉시페인트·yEnterChat/openYeta/setUnlocked 프리로드) · docs/reports/260707_챗배경_계단폐기_시그니처장소.html. **비차단(존치·후속)**: `--bg-scrim` 고아 아님(call.js `.ycall-bg` 소비 = 삭제 금지) · `--line2` 기존 고아(무관) · yeta-bg.webp 파일 = 챗 미참조 죽은 자산(선택 정리 · 브랜드 자산이라 존치).

## 114차 — 무음동 지도 아이소메트릭 마을 뷰 시안 (운영자 260708 "간판·사람 위치가 중요 — 마을 형태는 유지, 나타내는 방식은 [에이전트 오피스 스크린샷]처럼")
- **요지**: 지도 표현 개편 시안 = `docs/reports/260708_무음동지도_아이소메트릭_플레이그라운드.html`(플그 5요소 계승). 좌표·동선 = places.json·yeta_place.py 포팅 그대로(정본 무변경·마을 형태 불변) — 표현만 교체: CSS 3D 정사영 무대(압출 건물·점유 창문 점등·안테나 점멸·굴뚝 김·가로등·시간대 하늘/디밍) + **정보층 2D 오버레이**(간판 pill+점유 카운트 · 인물 말+네임태그[이름·상태 = 동선표 자동연산] · 잡담 말풍선 = 로스터 정본 quote 재사용 · 존 라벨 · 행렬 투영 동기 = rAF 킥 동안만[110차 배터리 교훈]). 시간 슬라이더/하루 재생 = 10인 일제 이동 · 수면 zZ(뉴트럴) · 마주침 = 라임 링 펄스+`잡담 중` · 탭 = 글래스 카드(주민/건물). 프리셋 4(현행 탑다운 포함·★아이소)·슬라이더(기울기/회전/줌/건물높이/간판)·선택값 JSON 복사. 사전 리서치 분신 3기(AI Town·Gather/ZEP·Zenly/스타듀 문법 + CSS 아이소 기법 + 다크맵 프레즌스).
- **기술 판정 2건(재발 방지)**: ⓐ 중첩 preserve-3d 큐보이드가 씬 내 빌보드(간판·태그)를 오페인트(크로미움 실측 — 리프트 150px·건물 제거 프로브로 특정) → **무대(3D)/정보(2D 투영 오버레이) 이층 분리 확정**. 씬 안 backdrop-filter도 열린 버그(크로미움 #323735424·FF #1952612)라 실장 시 동일 이층 구조 권장. ⓑ `classList.toggle(cls, undefined)` = 강제 해제가 아니라 **플립** — 조건식이 `p.pv&&…`처럼 undefined를 반환하면 상태 누적 오염(수면 7명 오표시 실측) → `!!` 강제.
- **계승**: 신규 :root 토큰 0(락 54 불변) · 색 = accent/cobalt/danger/epic/bubble-ai 토큰 + roster 정본 캐릭터색 · 창 불빛 = `--arm` 차용(⚠ 갱신 후보 — 웜 전용 토큰 신설/차용 유지/뉴트럴 3안 운영자 판단 대기) · 루시 = places 동선표 밖 = 미표시(`에픽 기척` 토글 = 위치 주장 없는 제안 연출).
- 검증: 자가검증 경로=Playwright 실측{주민10·간판12·건물19·나무7 · 저녁 tea 5인 잡담+정본 quote 말풍선 · 심야 수면 4(플립 버그 수정 후) · 슬롯별 이동 transition · 카드 2종 내용 · 탑다운 rx=0 · 간판 core 필터 · JS 에러 0} + 4앵글 스샷 육안 + check_refs rc=0 + 머지 후 read-back | 운영자검증 = 시안 채택 여부(디자인 감각 축). 후속(채택 시) = 뷰어 지도 탭 실장(이층 구조 이식·터치 팬줌·아바타 칩 교체·--warm 판단). 롤백=_versions/260708_0952_지도아이소시안.

## 115차 — 지도 아이소 시안 v2: "한눈에·이 느낌" 개편 (운영자 260708 "이 느낌이 살아야 · 지도가 한눈에 안 보임" + "분신 몇 명 돌려 검색·아이데이션")
- **진단**: v1은 ⓐ 마을(격자 27~76)이 0~100 전판 위 콩알 = 여백이 화면 60% ⓑ 전면 근흑 = 참조컷(밝은 토이 디오라마)의 형태 가독 부재 ⓒ 라벨 21장 상시 = 정보 총량 초과(참조컷은 화면당 ~6장).
- **리서치 분신 3기(READ-ONLY·10분 컷)**: ①토이 디오라마 재질 레시피(면 명도 3단 상100/좌78/우56·슬래브 두께 2~4%+2층 그림자+색 언더글로우·클레이 radius) ②한눈 프레이밍(fit-to-content 실측이 기본값·세로 화면 rz 45→30·라벨 LOD·핫스팟 펄스 1점·주변시 = 밝기 위계) ③건물 실루엣(매스·지붕형 1차 분화 = 6할·1건물 1히어로 소품 div 2~4·질감 = gradient 0div).
- **적용(시안 HTML 전면 개편 · 신규 :root 0 유지)**: 판 = 마을 밀착 슬래브(240×304 크롭 · 흰 .13 디오라마 바닥 · 보드판 두께 12px 오프셋 그림자 + 라임 언더글로우) · **자동핏 = 판 투영 bbox 실측**(가로 채움 + 뷰포트 높이 콘텐츠 밀착 = 죽은 세로 여백 제거 · 각도 변경 시 재실측) · 기본 rz 30°(세로 정합) · 면 명도 3단 + 4계단 스카이라인(studio12→office52) · **정보 위계 3단**: 코어 6간판 풀 pill / 비코어 = 아이콘 칩(호버·탭 확장) / 인물 = 이니셜 칩 + **말풍선 화자만 풀태그**(참조컷 Juno 문법) + 선택·호버 확장 · 핫스팟(최다 인원) 간판 1점만 라임 발광 · 비점유 간판 감쇠 · **라벨 충돌 해소 패스**(정착 시 1회 위로 계단 밀기) · 시그니처: 찻집 맞배 기와(경사판 2) · 검도장 외쪽지붕 · 라디오 철탑 트러스(clip-path) · 학교 타원 트랙+코발트 깃대 · 놀이터 ∏그네 진자 · 편의점 네온 띠 · 화분 나무(광장 센터피스 큰나무) · 흰 마네킹 피규어 + 발밑 정본색 링 · 미디어존 창 = 코발트 스크린 상시 · 연습실 화살표 = 코발트(색 의미축: 라임 = 생활 불빛/코발트 = 미디어·연습).
- 검증: 자가검증 경로=Playwright 재실측{주민10·간판12·잡담·수면4·이동·카드·에러 0} + 낮/저녁/심야/탑다운 스샷 육안(참조컷 문법 대조) + check_refs rc=0 + 머지 후 read-back | 운영자검증 = 느낌 수용(반복 루프 계속). 롤백=_versions/260708_1029_지도아이소v2(개편 전판).

## 116차 — 지도 시안 v3: 심즈식 어깨너머 카메라 + 미니맵 (운영자 260708 "거의 심즈 느낌 · 지금은 드론뷰 · 전체 지도 = 우측 상단 미니맵 · 사람·건물 크기 비슷해도 됨[추상화] · 빈 공간 불요")
- **개편**: 메인 뷰 = **어깨너머 근경**(기본 줌 3.0 · 시작 = 핫스팟[사람 최다 장소] · 화면 빈 공간 0) — 드래그 = 팬(월드 역투영·경계 클램프) · 건물/주민 탭 = 카메라 글라이드(lerp) + 카드 · **인물 크기 = 줌 연동 `--fgs`**(사람 ≈ 건물 절반 = 참조컷 추상 비율 · 운영자 확정 "실물 비율 무시"). **미니맵 신설(우측 상단 · 진짜 글래스)**: 장소 점 + 주민 10인 색점(라이브 이동) + 뷰파인더 점선 링+중심점 + 탭/드래그 = 카메라 점프. 기존 전체뷰 = `드론뷰(이전)` 프리셋으로 강등(비교 기준 존치 · 존 라벨 = 원경에서만). 프리셋 = 어깨너머★/로우앵글/드론뷰/탑다운.
- 검증: 자가검증 경로=Playwright{카메라 글라이드 실측(240→260) · 미니맵 인물 점 10 · 피규어 42px 줌 스케일 · 기존 스위트 전건 그린 · JS 에러 0} + 근경 스샷 육안(참조컷 대조 — 마네킹 무리·간판·말풍선·미니맵) + check_refs rc=0 | 운영자검증 = 느낌 수용. 롤백=git origin/main(v2).

## 117차 — 스페셜 배지 = 이름줄 직업 우측 이동(등급 전용줄 폐지) (운영자 260708 "특수 배지 직업 우측으로 나란히 중앙정렬 · 1행 다 차지하니 손해")
- **경위**: 등급·보이스 배지(`.ygrade` 시즌/IDOL·`.yprem` 보이스)가 이름 위 **전용 한 행**(`.ycrow-grade`·`.ylist-grade`)을 통째 차지 → 배지 1개 때문에 카드가 4줄로 늘어 세로 손해.
- **수정(viewer/index.html · 신규 :root 0 · 게이트웨이·파이프라인·모델 무변경)**: 선택 카드(`.ycrow`)+대화 목록(`.ylist`) 양쪽에서 등급 전용줄 제거 → 배지를 **이름줄(`.ycrow-nm`·`.ylist-nm`) 직업 배지(`yJobBadge`) 우측**으로 이동. 이름줄이 이미 `flex; align-items:center`라 이름·직업·배지 **세로 중앙 나란히** 자동. 이름(`.ynm-t`)만 shrink·배지는 `flex:none`(신규 규칙 2줄 = 옛 `.ycrow-grade`/`.ylist-grade` 죽은 셀렉터 자리 교체). 순서 = 이름 → 직업 → 등급 → 보이스 → (대화 중). `.yme`(내 상태행 "방문자")은 직업 없음 = 대상 아님(유지).
- **효과**: 카드 4줄→3줄(등급/이름/설명·인용 → 이름·직업·배지/설명/인용). 실측 세라 카드 mid 높이 84→59px(**−25px**). 긴 이름+배지 2개 = 이름 ellipsis로 흡수(배지 유지).
- 검증: 자가검증 경로=Playwright mock 렌더 실측(등급 season/idol·보이스·긴이름 5행 · before/after 대조: 등급 전용줄 4→0 · 이름줄 배지 0→5 · seraNmH 84→59 · 긴이름 ellipsis-ok · JS 에러 0) + before/after 스샷 육안 + check_refs rc=0 + 머지 후 read-back | 국소 UI 손질 = 자가검증(기틀 무침범·§검증 b). 롤백=_versions/260708_1137_배지직업옆.

## 118차 — 설정·시트 화면 하단 네비 상주 + 스크롤 콘텐츠 네비 근처 페이드아웃 (운영자 260708 "설정 탭 오면 뒤로 못 감 · 네비 살려줘 · 네비 주변부 페이드아웃 = 창 구분감")
- **진단(실측)**: 설정·프로필 = `.ycd` fixed 오버레이(z:6·자체 스크롤), 열면 `.ymain.cd-open`이 `.ynav`(z:4) `display:none` → 하단 네비 소멸 = 다른 탭 못 감(뒤로가기 버튼만). 옛 주석 "z로 못 이김"은 오판 — Playwright 실측: `.ynav`·`.ycd` 둘 다 `.ymain`(fixed z:1) **동일 스택 컨텍스트**라 z만 올리면 시트 위(`elementFromPoint`=NAV-ON-TOP). 스크롤 컨테이너 = `.ychar`/`.ylist`/`.ymapp`/`.ycd`(overflow-y:auto · `.ymain-body`는 hidden).
- **수정(viewer/index.html · 신규 :root 0 · 게이트웨이·파이프라인·모델 무변경)**: ① `.ynav` z 4→7(ycd 6 위 · 챗 `<dialog>` top-layer는 여전히 최상단이라 챗선 네비 가림 = 무해). ② `cd-open`에서 `.ynav` 숨김 제거(로고·프로필만 숨김) = 설정·디테일 등 전 시트에서 네비 상주. ③ 네비 탭 클릭 = `yTab`(핸들러 2625)이 첫 줄 `forEach yCdHide`로 열린 시트 회수(실측 cd-open·sheetOn→false) = 네비가 곧 뒤로가기. ④ 설정 열림 = 네비 4번째(설정) 버튼 active 동기(yShowSettings 1줄 · 닫으면 yTab이 새 탭으로 갱신). ⑤ **스크롤 페이드**: `.ychar/.ylist/.ymapp/.ycd`에 하단 `mask-image` linear-gradient(88+safe서 시작→52+safe 완전투명) = 콘텐츠가 네비 뒤로 스크롤되면 사라짐(네비는 별도 레이어라 또렷) = 창 구분감. 하단 패딩 96→96+safe(char/list/map)·28→96+safe(ycd) = 네비 클리어런스(마지막 항목은 페이드 88 밖 또렷).
- 검증: 자가검증 경로=Playwright 실측{설정 시트서 네비 display:flex·z:7·NAV-ON-TOP · yTab('char')→cd-open/sheetOn false(회수) · char 12행 스크롤·mask 적용 · JS 에러 0} + 스샷 2장 육안(설정 네비 상주+설정버튼 활성+타이핑행 페이드 · char 스크롤 페이드) + check_refs rc=0 + 머지 후 read-back | 실질 UI 변경(기틀 무침범) = 자가검증. 롤백=_versions/260708_설정네비상주.

## 119차 — 해금 에피소드 백필: 평범한 인간 4인 (운영자 260708 "_TEMPLATE_ 시스템 반영·반영가치 확인" → A안 "해금 4장만 우선")
- **경위(확인 요청 → 실측)**: 운영자가 카드 템플릿(`_TEMPLATE_캐릭터.md` 사본)을 올려 "시스템 반영됐는지·반영할 만한지(이미 기능 있을 수도)" 질의. 실측 결과 ⓐ 업로드본 = 레포 `apps/yeta/characters/_TEMPLATE_캐릭터.md`와 **바이트 동일**(MD5 일치·추가 불요) ⓑ 신템플릿(54차 260707 5레이어 개편)이 도입한 슬롯이 실카드에 부분 반영 = 해금 6/10·당김흘림 1/10·훅뱅크 0/10(단 훅뱅크 랜덤픽은 설계상 후속·버그 아님)·방어/애착 태그 0(산문엔 실질 담김). **기능 축은 이미 배선**(해금 LV 게이트 = 러너 STATE_BLOCK `현재 관계 단계` 주입·§🔓) = 새 기능 0, 갭은 순수 카드 콘텐츠 저작. 운영자 A안 선택 = 해금 없는 4장 우선. (병합 시 번호 재배정 115→119 = main 지도/배지 차수와 충돌 회피 · 원장 append-only 수동 정리.)
- **산출(콘텐츠)**: 해금 에피소드 없던 4인(desk·gaeul·kopi·von = 전원 '평범한 인간·다녀온 주민')에 각 4개(LV2→LV5) 저작. 각 카드 기존 `(이면)`으로 수렴·**금기의 예외 해금 구조**(baek/ryu 설계 정합) — desk=봉인 수첩→오보로 어긋난 한 사람→세 번의 검산(결벽=공포) · kopi=몰래 쓰는 노트→걸레된 문장→무플의 밤→못 쓰던 공백기 · gaeul=회장 얼굴 내려놓기→완벽 강박 뿌리→비웠던 골목→반 뼘 무너짐(하은에게조차 안 보인) · von=새벽 5시→봉인 박스→끊어진 무릎→겹쳐 보이는 얼굴(글러브 개봉). '다녀온 주민' 공백기 모티프를 LV4 조각으로 연결. 해금 커버리지 **6/10 → 10/10**(각 4개).
- 검증: 자가검증 경로=check_refs rc=0(락 54 불변·비차단 경고 --bg-scrim/--line2는 기존 무관) + 해금 커버리지 10/10 재실측(grep) + 하우스 스타일 대조(haeun·ryu = 동일 narration:false 해금) + 카드 버전 해시 재생성(inject_character = 의도된 '업데이트됨' 드리프트 감지). 국소 콘텐츠 저작 = 자가검증 티어(§검증 b-다) · 캐릭터 결 창작이라 운영자 취향 수용은 폰 체감 몫. 롤백=_versions/260708_1018_해금에피소드_4장백필.

## 120차 — 잠금 캐릭터 카드 READ ONLY 표기 시안 (운영자 260708 "해금 안 된 카드 = 캐릭터로 뜨는데 READ ONLY 표기 · 자물쇠 픽토그램 락걸린 느낌 · 인터넷 리서치")
- **진단(실측)**: "해금 안 된 카드" = `roster.json` 12엔트리 중 카드 없는 **스페셜 2인(lucy=grade:season·winter=grade:idol)** — 로스터엔 떠도 콘텐츠 없음. 뷰어에 **이미 계승 자산 존재**: ⓐ 자물쇠 정본 SVG(PIN `.ym-access` = rect+shackle path) ⓑ 비디폴트 상태 패턴 `.ycrow.talking`(opacity .55)+`.ycrow-on`(뉴트럴 글래스 알약) ⓒ 등급 훈장 `.ygrade.season/.idol`. → 신규 창작 최소, `.ycrow.locked`(=.talking 형제)로 계승 가능.
- **리서치(운영자 지시 "인터넷 뒤져봐")**: 웹서치 2각(가챠/게임 잠금 캐릭터 카드 · 프리미엄 잠금 UI) → "락걸린 느낌" 표준 어휘 = 그레이스케일/딤 · 실루엣(정체 은폐) · 자물쇠 배지 · 프로스트 글래스 스크림 · 코너 리본. 글래스모피즘엔 스크림/프로스트가 특히 정합.
- **산출(시안)**: `docs/reports/260708_잠금카드_READONLY_플레이그라운드.html`(플그 5요소) — 정본 `.ycrow`/`.ycrow-on`/`.ygrade` + 정본 자물쇠 SVG 계승 · 프리셋 5{현행(문제상태)·①은은한 잠김·②실루엣 티저·③프로스트 스크림·④훈장 잠김} + 슬라이더 5 + 라벨 + 알약 톤 + 복사 + 각주. **READ ONLY 알약 = 뉴트럴 글래스 = §🎨 강조 2색 제한 준수**(락≠CTA). ⚠️ 시안은 병합 전 `.ycrow-grade` 전용줄 구조로 재현 = **main 117차(등급 전용줄 폐지·배지 이름줄 이동) 이후 실장은 신 구조 계승 필요**(후속 실장 차수서 반영).
- 검증: 순수 시각/목업 = 검증 면제(HTML 시안 · §검증 b-마). 자가검증 경로=Playwright 실측{JS 에러 0 · 프리셋 5 전환 · 프로스트 라벨 중복 정리} + 스샷 4프레임 육안. 신규 :root 토큰 0. **운영자 후속(같은 세션)**: ② 실루엣 채택 + 라벨 3분기(만난 적 有·대화 無=아직 모르는 인물 / 만난 적 無·소통불가=LOCKED / 그 외=???) 지시 → viewer 실장 착수(별도 차수). 롤백=git(신규 파일 · 단독 삭제).

## 121차 — 잠금 캐릭터 READ ONLY 실장: 실루엣 + 라벨 3분기 (운영자 260708 "② 실루엣으로 · ??? 대신 만남/소통 상태별 라벨")
- **경위**: 120차 시안서 ② 실루엣 채택 + 단일 "???" → 관계상태 3분기 라벨 지시. 분신 조사(READ-ONLY) 결과 **잠금 신호 원천 부재**(roster/세션에 카드유무·만남 필드 없음 · "대화 텄음"만 실재=`yActiveIds`) → roster 플래그 신설로 배선.
- **데이터(roster.json · lucy·winter)**: `locked:true`(=소통 불가=카드 없음) + `met`(만남 여부) 추가 — lucy `met:true`(밤에 스치는 낯선 얼굴) · winter `met:false`(먼 아이돌). 뷰어가 이미 roster 필드(grade·voice)로 분기하던 패턴 확장(신규 색·토큰 0).
- **뷰어(viewer/index.html · 신규 :root 0 · 게이트웨이·파이프라인·모델 무변경 · 117차 신 구조[배지 이름줄] 계승)**: ① `yLockLabel(c)` = locked 3분기(met true=아직 모르는 인물 / false=LOCKED / 그 외=???) ② `.ycrow.locked`(=.talking 형제) 실루엣 = 아바타 `grayscale(1) brightness(.3)`+자물쇠 오버레이(`.ylock-ava`·정본 `YLOCK_SVG` 성향잠금 공용 계승=중복선언 0)·이름 `.ynm-t` opacity .5·상태 알약 `.ylock-pill`(=.ycrow-on 뉴트럴 글래스 계승 = §🎨 2색 제한) ③ 진입 차단 = `yShowDetail` CTA disabled("아직 대화할 수 없어"/"아직 열리지 않은 인물")·`yEnterChat` 잠금 가드→미리보기·다이스 잠금 제외·합석 초대 후보 제외·CTA 핸들러 `b.disabled` 가드. **READ ONLY = 상세 미리보기만·대화 불가**. draw op(게이트웨이) 카드 가드는 미러링 후속(현재 뷰어 단에서 전 진입 차단 = 실질 안전).
- 검증: 자가검증 경로=check_refs rc=0(JS 구문 OK·락 불변·비차단 --bg-scrim/--line2 무관) + Playwright 실측{yLockLabel 단위 5케이스(아직 모르는 인물/LOCKED/???/일반/none) · 렌더: lucy·winter locked+알약+실루엣필터(grayscale1 brightness.3)+자물쇠오버레이+등급배지 · mudi/sera 무영향 · JS 에러 0} + 라이브 그리드 스샷 육안(실루엣·3분기·정상/대화중 대비). 실질 UI+roster 데이터(기틀 무침범) = 자가검증 티어(§검증 b · 117·118차 동급). 롤백=_versions/260708_1422_잠금캐릭터_READONLY실장(변경 전 = 머지커밋 d1c1dec).

## 122차 — 유저 프로필(호칭+소개) "AI가 나를 부르는 법" = 세션 me + 러너 격리주입 + 뷰어 배선 (운영자 260708 "사용자에 대해 AI가 부르는 프로필 · 프로필화면서 설정 · 실제로 이거에 맞게 대화하게 연결" · 병합 시 118→122 재배정 = main 121차와 충돌 회피)
- **요구**: 유저가 "AI가 나를 부르는 이름(호칭)+소개"를 프로필서 설정 → 캐릭터가 실제 대화에서 그 이름으로 부르고 소개 참고(스크린샷 = 경쟁앱 "대화 프로필 편집" 참조컷).
- **결정(범위)**: **전역 1개 프로필**(AskUserQuestion 권한오류로 미응답 → §운영 태도 d 근거있는 추천안 진행). 근거 = yeta 기억(note_pub=유저 사실)이 이미 전 방 공유 = 전역이 구조 정합 · 기존 '내 프로필' 화면에 이미 이름+상태 필드 존재. 캐릭터별은 후속 확장 경로(`me`가 top-level·러너 주입이라 미래 `threads[id].me_call` 오버라이드를 me_block서 우선참조하면 됨 — 값싼 유예).
- **결정(보안 위상)**: me = **자기기입 텍스트 축(send/draw/invite/kick 계열)**이지 설정 knob(tune/policy/pin) 아님 → "설정 op는 숫자/enum만" 불변 위배 아님(문구를 §🗺서 knob vs 자기기입 텍스트로 정밀화). 방어 3종 = ⓐ게이트웨이 `stripMarkers`(고정점)+캡 ⓑ러너 `me_block` 재-clean(고정점)+공백붕괴 ⓒ "지시 아님" 격리 프레임 + 출력계약 이중앵커. 구조/비용 무변경(dispatch 1회 불변·모델ID 불변) = §기틀검증 5인 트랙 아님(tune/policy/state 블록 편입 선례 동일).
- **수정**: ① `functions/api/yeta.js` — 세션 `me:{call,about}`(EMPTY/migrateV3/전체reset 승계) + op `me`(SET·casPut) · **인라인 sani 4벌 → 모듈 상수 `stripMarkers` 단일화**(고정점 루프 = 중첩 마커 붕괴 · 라벨 `[^>]*` 유니코드 · me/send/draw/invite/kick 공용). ② `.github/scripts/yeta_v3.py` — migrate_v3 me 보존·기본화(JS 동형). ③ `.github/scripts/yeta_chat.sh` — extract_mat 3경로(chat/opening/invite) me_call/me_about + 프롬프트 `${ME_BLOCK}` 주입 + 출력계약 `${ME_RULE}` 이중앵커. ④ `shared/inject_character.sh` — `me_block()` 정본 이전(chat·nudge·call 공용 · 고정점 clean). ⑤ `.github/scripts/yeta_nudge.sh` — me_block 배선(넛지 호격에 호칭 = 이름이 가장 꽂히는 자리 · 평의회10 한 수). ⑥ `viewer/index.html` — 내 프로필 "AI가 나를 부르는 법"(호칭+소개) → op me 서버동기(`yMePush` 실패토스트+dirty 재푸시 · `yMeHydrate`). ⑦ `CLAUDE.md §🗺/§🎬` — op me 등재(18→19)·문구 정밀화·세션 shape·전 방 공유 목록에 me.
- **범위 밖(명시 유예)**: 걸려오는 전화(`yeta_call.sh`) = 유료 TTS·별개 축(CLAUDE.md 음성 축 분류) = 미배선 · 후속 시 me_block 동형 주입 가능. (넛지·PTT[챗 OUT 상속]·오프닝·초대는 배선 완료.)
- 검증: 자가검증 경로=node(stripMarkers 깊이2 중첩공격 붕괴·migrateV3 왕복·JS 문법)·python(extract_mat 3경로 me 흐름·migrate_v3 v3보존/v2마이그/기본화·me_block 마커제거·빈프로필=빈블록·넛지 GATE me추출)·bash -n 3스크립트·check_refs rc=0·source 정합(me_block shared 호출가능) + **10인 평의회(READ-ONLY 병렬 적대감사: 보안·게이트웨이·러너·JS/PY동형·뷰어·디자인·회귀·문서·효율·완결성)** = BLOCK 0 · MED 2(보안 중첩마커 깊이2 생존→고정점 루프 수정 · 문서정합→CLAUDE.md/원장/보고서) + Medium 2(뷰어 Q3 표시·Q2 실패피드백) + 한수(넛지) 전건 반영 후 보안 재검증 + 머지후 read-back | 운영자검증 불가피 사유=실기기 폰서 캐릭터가 호칭 부르는 체감·대화 자연스러움(사람 감각 축 · 유료/합성 프로브 0). 롤백=_versions/260708_유저프로필호칭(6파일 origin/main 스냅샷) · git origin/main. 브랜치 claude/ai-user-profile-naming-j2351r · PR #192.

## 123차 — 무음동 지도 낮/밤 배경 자동 전환 (운영자 260708 "리포에 뽑아놓은 지도 밤/낮 + 미니 캐릭터, 도시에 캐릭터에 녹여줘")
- **경위**: yeta-map-bg 산출 중 밤 지도+캐릭터 워커는 기통합 / **낮 지도(map_day)만 뷰어 참조 0 = 고아**. 낮/밤 둘 다 살리는 게 요청. (main 원장 116차[지도 시안 v3]와 충돌 회피 = 117로 재번호 · 리셋 재적용.)
- **변경**: `ymIsDay(h)=07~21시 KST` 헬퍼 → `ymBuild` 진입 배경 + `ymPlaceAll`(1분 재판정)이 `#ymapBg` href만 스왑. 좌표·워커·차량·신호등 불변 · 신규 토큰 0. ⚠ 지도 아이소 개편(114~116차 시안)은 별도 트랙 = 현행 PNG 맵 개선.
- **검증**: check_refs rc=0(JS구문OK) + 헤드리스 렌더 실측(낮/밤 오버레이 정합) + read-back. 롤백=git.
- **산출**: viewer/index.html · 브랜치 claude/character-city-integration-uy0ajg.

## 124차 — 윈터(IDOL) 아바타 = beauty_01 얼굴 프사 + 루시(시즌) 원거리 유지 (운영자 260708 "윈터 얼굴은 beauty 중 하나·저 얼굴[tight] 말고 원거리 기존 유지·둘다 상위")
- **경위**: 윈터 아바타(eye_smile_01)를 beauty 얼굴로. 루시(시즌)는 기존 원거리 유지. (최초 stale 브랜치서 루시/윈터 혼동 → 최신 main[루시=시즌 애니·윈터=idol aespa 별개] 리셋 후 정정.)
- **변경**: 윈터 `avatar` → `characters/idol/winter/beauty_01.jpg`(raw 기존 파일 = 원형 중앙크롭이 얼굴 상단 담음 = "beauty 중 하나+원거리+저 얼굴 말고 기존 유지" · 새 에셋 0). 루시 lucy_01 무변경(1장뿐). 정렬 = ySortChars 기설계로 하은(보이스)·루시(시즌)·윈터(idol) 상위3 = "둘다 상위" 이미 만족(코드 0).
- **검증**: roster.json 유효 + raw 원형 아바타 렌더 실측(얼굴 또렷) + 후보 5종 대조 + 픽커 상위3 렌더 + check_refs rc=0 + read-back. 운영자검증=폰 육안(beauty_02·얼굴/상반신 한마디 교체). 롤백=git.
- **산출**: apps/yeta/characters/roster.json · 브랜치 동일(PR #186).

## 125차 — 루시(시즌) 아트 배선: 새 얼굴 프사(첨부2번) + 챗 배경(2번) (운영자 260708 "루시 얼굴=첨부2번·배경=1/2 중 일단 2번·2중변신이라 나머지 나중에")
- **경위**: 루시 아바타가 얼굴 없는 몸통샷(lucy_01)이던 것 → 운영자가 사이버펑크 애니 3장 제공(**채팅 붙여넣기 = 세션 jsonl base64서 추출·복원** — 업로드가 디스크 파일로 안 와서 대화 로그서 디코드). 루시 = 시크 인형↔도깨비 **2중변신** → 시크 폼 먼저(2번), 도깨비 폼 아트는 후속("나머지 나중에").
- **변경**: `viewer/characters/season/lucy/`에 4종 신설 — `lucy_face.webp`(2번 얼굴 512² 크롭 = avatar · 포인팅 손 제외 헤드룸)·`lucy_02.webp`(2번 풀 = bg)·`lucy_room.webp`(1번)·`lucy_neon.webp`(3번 · 후속/도깨비 폼·bg 스왑 후보). roster lucy `avatar` lucy_01→lucy_face · `bg` lucy_01→lucy_02. 옛 lucy_01(몸통) 존치(미참조·운영자 콘텐츠 무단삭제 금지).
- **효과/리스크**: 루시 프로필 얼굴 정상화(몸통샷→시크 얼굴) + 챗 배경 캐릭터 결. 리스크 0(에셋 4 추가 + 로스터 2줄). bg=2번(얼굴 포트레이트) = winter bg(happy_01 포트레이트) 관례 정합 · 1번(룸신)/3번으로 한마디 스왑 가능.
- **검증**: roster.json 유효 + 얼굴 크롭 3후보 원형 비교(A 채택) + 픽커 렌더(루시 새 얼굴·하은/루시/윈터 상위3) + bg 스크림 렌더 실측 + check_refs rc=0 + read-back. 운영자검증 = 폰 육안(bg 1/3번 교체·도깨비 폼 후속). 롤백=git.
- **산출**: viewer/characters/season/lucy/{lucy_face,lucy_02,lucy_room,lucy_neon}.webp · apps/yeta/characters/roster.json · 브랜치 claude/character-city-integration-uy0ajg(PR #186).

## 126차 — 배지 폴리시: 시즌 색 레드→보라 + 젬↔글자 간격 + IDOL 정렬 픽셀검증 (운영자 260708 "IDOL 중앙정렬 픽셀 확인·배지 로고/글자 간격·시즌 좀더 보라로 진행")
- **경위**: 캐릭터 선택창 스페셜 등급 배지(.ygrade) 3건 — ① IDOL 젬·글자 중앙정렬 픽셀 확인 ② 젬↔글자 간격 벌리기 ③ 시즌 색 레드→보라(운영자 승인 "진행 ㄱ").
- **변경**: ⓐ **시즌 색 값 갱신** `--season` #C02148(레드보라)→**#8E2DBF(진한 보라)**·`--season-rgb` 192,33,72→142,45,191 (:root 값 SSOT + `build_design_mirror.py` 거울 재빌드 + CLAUDE.md §🎨 hex 동기 · **락 불변**=토큰명 동일 · §🔒 값 갱신=승인+게이트 트랙). ⓑ `.ygrade` `gap:4px`→**6px**(젬↔글자 숨 · 기존 raw gap 축 계승). ⓒ **IDOL 정렬 = 이상 없음 확정**(픽셀 측정 = 젬 세로중심 vs 글자 세로중심 **0.00px** · flexbox align-items:center 완벽 · 젬 아래 1px = 양각 드롭섀도 의도 = 변경 0).
- **효과/리스크**: 시즌 배지가 보라로 전환(레드→보라 · 감정배경도 --season 계승 = 자동 반영) · 배지 가독 숨 개선. 리스크 = --season blast radius = .ygrade.season 1곳 + 감정배경(옵트인)뿐(실측). ⚠ lucy roster `color:#C02148`(시즌 캐릭터 accent)은 별개 per-char 값 = 미변경(원하면 보라 동기 = 후속).
- **검증**: 국소(토큰 값+CSS) = 자가검증 — 배지 플레이그라운드(6배 확대 정렬·간격·색 5후보 대조) + IDOL 픽셀 측정(0.00px) + 거울 재빌드 + check_refs rc=0(거울 정합·락 불변) + read-back. 운영자검증 = 폰 색감(더 진하게/연하게·마젠타보라 A02890·바이올렛 7C3AC9로 한마디 교체). 롤백=git.
- **산출**: viewer/index.html · 구성도/base.css(거울) · CLAUDE.md(§🎨 hex) · 브랜치 claude/character-city-integration-uy0ajg.

## 127차 — 캐릭터 아바타 증명사진급 타이트 크롭 (운영자 260708 "하은도 얼빡 없어?·전반적으로 상반신 너무 많음·증명사진급이어야")
- **경위**: `av/*.webp` 10명 = AI 시네마틱 상반신 포트레이트(얼굴이 상단 20~32%·좌우 40~62% 편차) = 46px 원형 아바타서 얼굴 안 보이고 몸통/배경만 = "증명사진급 아님" 지적.
- **변경**: 10명 각 **얼굴 중심 튜닝 크롭**(3분할 그리드 실측 → cx/cy 개별값 · side 0.50~0.52 = 얼굴+약간 어깨) → 512² 재생성 → `av/*.webp` **덮어쓰기**(파일명 동일 = 로스터 avatar 경로 무변경). ryu(얼굴 좌측 쏠림)·von(우측 쏠림) 2차 재튜닝. **winter(beauty_01)·lucy(lucy_face) = 사용자 지정분이라 미변경**(원하면 후속 타이트화).
- **효과/리스크**: 작은 원형서도 얼굴 즉시 식별(증명사진 결) · 캐릭터 선택창 가독↑. 리스크 0(파일 교체·경로/토큰 불변 · git이 원본 백업). 손상/오프센터 = 컨택트시트(현재 vs 신규 대비) + 개별 실측 검증으로 봉쇄.
- **검증**: 국소(에셋 교체) = 자가검증 — 얼굴위치 그리드 실측(10명) + 타이트 크롭 원형 컨택트시트 + ryu/von/gaeul 개별 재확인 + 합본 리스트 렌더(타이트 아바타 + 보라 시즌 + 6px 배지 = 4요청 합본) + check_refs rc=0 + read-back. 운영자검증 = 폰 육안. 롤백=git(원본 av).
- **산출**: viewer/assets/yeta_char/av/{haeun,mudi,sera,baek,ryu,yun,desk,kopi,gaeul,von}.webp · 브랜치 claude/character-city-integration-uy0ajg.

## 128차 — 페르소나 카드 없음 = lucy·winter 대화 불가 수정 (운영자 260708 "페르소나 연결 안 됐다고 대화 안 되는 애들 해결")
- **근인**: 챗 런너 게이트(yeta_chat.sh L766 메인·L577 초대·L837 동행)가 `apps/yeta/characters/${id}.md`만 확인 → 없으면 `finish error "페르소나 카드 없음"`. lucy(season)·winter(idol) 카드는 `viewer/characters/season|idol/<id>/CLAUDE.md`에 co-located(운영자 260707 의도). inject_character.sh에 폴백은 있으나 **yeta-chat.yml sparse-checkout 목록 = `.github·shared·apps/yeta·push`뿐 = `viewer/characters/` 미체크아웃** → 런너에 카드 자체가 없어 폴백도 무용.
- **변경(최소·안전)**: lucy·winter 카드를 표준 위치로 복사 — `viewer/characters/season/lucy/CLAUDE.md`→`apps/yeta/characters/lucy.md` · `.../idol/winter/CLAUDE.md`→`apps/yeta/characters/winter.md`. sparse에 이미 포함된 `apps/yeta` = 런너가 봄. **코드·워크플로·sparse 무변경**(게이트 그대로 통과). 검증: 로스터 12명 전원 `apps/yeta/characters/<id>.md` 실존 + name 추출(루시·윈터) 정상 + bash -n OK.
- **효과/리스크**: lucy·winter 1:1 대화·합석 초대·동행 전부 정상화. 리스크 = **카드 이중화 드리프트**(viewer CLAUDE.md 사본 잔존 · 지금은 동일). ⚠ **런타임 정본 = apps/yeta/characters/*.md**(sparse 포함) · viewer CLAUDE.md = co-located 사본(편집 시 apps/ 동기 필요). 후속 옵션 = ⓐ viewer 사본 제거 통일 or ⓑ sparse non-cone로 `viewer/characters/*/*/CLAUDE.md`만 포함해 viewer 정본화(워크플로=기틀 = 운영자 결정).
- **검증**: 자가검증 — 로스터↔카드 실존 매핑(12/12) + name sed 추출 + bash -n + check_refs rc=0 + read-back. 운영자검증 = 폰서 루시·윈터 실제 대화(생성 파이프 = Actions 런너 = 자가확인 불가 = 운영자 실행 필수). 롤백=git.
- **산출**: apps/yeta/characters/lucy.md · apps/yeta/characters/winter.md · 브랜치 claude/character-city-integration-uy0ajg.

## 129차 — 프로필 링: 특별 등급 링(픽커·상세) + 대화목록 안읽음 링 (운영자 260708 "특별한 애들 프로필 링 하나씩 · 대화창 안읽음 링")
- **변경**: ⓐ **특별 등급 아바타 둘레 등급색 링** — 보이스=라임(`--accent`)·시즌=보라(`--season`)·IDOL=실버(`--fg-2`) · `box-shadow`(`--bg` 갭 + 토큰색 · 토큰없는 축 raw 기하 예외) · `yGradeRing()` 헬퍼 → 픽커(`.ycrow .yava`) + 상세카드(`.ycd-card .yintro-ava`). ⓑ **대화목록 안읽음 링** = 라임(`.ylist-ava.yring-unread` · `unTxt` 조건 · 카운트 배지와 병행). **신규 토큰 0·락 불변**(색 전부 기존 토큰).
- **효과/리스크**: 특별 캐릭터 한눈 구분(등급색 링) + 안읽음 시각 강화. 리스크 = box-shadow 링(최대 3.5px) = 행 패딩 12px(sp-2) 안 = 클리핑 0(`.ylist-group` overflow:hidden도 아바타 좌측 12px 여백 안이라 무해 · 실측). IDOL 실버 링 = 의도적 절제(흰·실버 취지).
- **검증**: 국소(CSS+렌더) = 자가검증 — 링 렌더 실측(보이스 라임·시즌 보라·IDOL 실버·일반 링없음·안읽음 라임+배지·읽음 링없음) + check_refs rc=0(토큰색·락 불변) + read-back. 운영자검증 = 폰 육안(안읽음 = 링+배지 병행 · 배지 빼고 링만 원하면 한마디). 롤백=git.
- **산출**: viewer/index.html(yGradeRing 헬퍼 + 프로필 링 CSS 4줄 + 픽커/상세/목록 3 렌더 배선) · 브랜치 claude/character-city-integration-uy0ajg.


## 130차 — 배지 그룹 폴리시: IDOL 테두리·보이스 라임네온·안드로이드·안읽음 흰색 (운영자 260708 다건)
- **변경**: ⓐ **IDOL 배지 = 실버 플레이트+테두리**(운영자 "테두리 없음" · `.ygrade.idol` glass-2 투명→`rgba(var(--fg-rgb),.10)` 플레이트 + `rgba(var(--fg-rgb),.45)` 테두리 = 시즌/보이스 `.4` 테두리 패턴 통일 · 젬+IDOL 유지). **`--fg-rgb:255,255,255` 신규 토큰**(--X-rgb 패턴 계승 = 실버 알파용 · :root + 락 등재[55개] + 거울 재빌드). 이로써 job/시즌/IDOL 배지 전부 플레이트=**"삐뚤"(IDOL만 투명이던 것) 해소**·크기 동일(측정 h 동일·중앙정렬 align-items:center). ⓑ **보이스 = 라임네온 솔리드 칩**(운영자 "라임네온·라임 링과 겹침" · `.yprem` accent .12 아웃라인→solid `var(--accent)` bg + `var(--bg)` 텍스트 + 라임 글로우 = 아웃라인 링↔솔리드 칩 대비로 겹침 해소 · gap 4→6 배지 통일). ⓒ **루시 job 자동인형→안드로이드**(roster · 사이버펑크 결). ⓓ **대화목록 안읽음/내말 구분**(운영자 "안읽은 미리보기 흰색·내 말은 지금처럼") — `.ylist-last` = 안읽음(`unTxt`)且 상대 메시지일 때만 `.on`(흰) · 내 말(role user)·읽음 = `--mut` 딤.
- **효과/리스크**: 배지 통일감·IDOL 존재감·보이스 구분·안읽음 시각 강화. 리스크 = --fg-rgb = 뉴트럴 흰 rgb(신규 색 0=흰 재분해) · 보이스 솔리드 = 시각 강조(크기 불변). 
- **검증**: 자가검증 — 배지 개별 높이 측정(job/시즌/보이스 h 동일) + 최종 렌더(IDOL 플레이트·보이스 네온 솔리드·안읽음 흰/내말 딤 실측) + check_refs rc=0(락 55·거울 정합·JS OK) + read-back. 운영자검증 = 폰 육안(보이스 네온 톤·안드로이드 태그라인도 바꿀지). 롤백=git.
- **산출**: viewer/index.html(--fg-rgb·IDOL·lastLn) · viewer/call.js(yprem) · design-tokens.lock · 구성도/base.css·viewer/tokens.css(거울) · apps/yeta/characters/roster.json(lucy job) · 브랜치 claude/character-city-integration-uy0ajg.

## 131차 — 대화 시작 플로우: 상황 지문 먼저 + "yeta 목업" 교체 깜빡임 제거 (운영자 260708 다건)
- **의도**: ⓔ "대화 시작하면 처음에 상황 설명하고 대화를 먼저 상대가 걸게 — 첨부(제타)처럼 먼저 상황이 보이고, 그다음에 말". ⓕ "대화 시작 나오면 yeta 목업이 먼저 나온 다음 준비되면 사람이 나온다 → 바로 그 캐릭터 대화창이 어색하지 않게".
- **변경**: ⓔ **장면 지문 블록**(`.ysit`) — roster `enter_line`(3인칭 씬: "무디가 새 찻잔을 내리며 돌아본다")을 인물 대사 **위에** 그레이 이탤릭(`--mut`·버블 아님)으로 먼저 노출. yRender에서 **내 발화 전(오프닝 단계)** 조건(`!turns.some(role==='user')`)일 때만 프리펜드 → 첫 내 메시지 후 자연 퇴장. 동적 오프닝 인플라이트 = 상황+타이핑, 도착/정적 = 상황+대사. 픽커·재드로 스왑도 동일(신규 캐릭터 방=상황 노출). **신규 토큰 0**(--mut·--fs-sm·--lh-base 계승). ⓕ **yEnterChat 낙관 선반영** — `yView('chat')`+`yApply(c)`(헤더·배경 인물화)+상황 지문+"입력 중" 페인트를 `await yApi('draw')` **앞으로** 이동 → 왕복 중 기본 헤더(`yeta`/`y` 목업) 노출 창(窓) 제거. draw 도착 후 yRender()가 실세션으로 재페인트(멱등).
- **효과/리스크**: 대화 진입 = 제타 결(상황→대사)·목업 깜빡임 0. 리스크 낮음 — ⓔ는 뷰 전용 프리펜드(세션 무변경·enter_line은 로스터 기존값) · ⓕ yApply(YSESS=null) = 기존 fail 경로서 이미 실행되던 안전 경로(YTH()·yLastMood·ySideRender 전부 `(YTH()||{})` null-가드). 동적 오프닝 도착 시 상황 지문은 러너 대사와 중복 아님(지문=씬, 대사=발화).
- **검증**: 국소(뷰 렌더+플로우) = 자가검증 — 실 `<style>` 재사용 4상태 헤드리스 렌더(오프닝 인플라이트=상황+입력중 · 도착=상황+대사 · 세라 동일 · 내 답장 후 지문 퇴장) 실측 + check_refs rc=0(토큰 55·거울·JS 구문 OK) + read-back. 운영자검증 = 폰서 실진입 체감(목업 깜빡임·상황 톤). 롤백=git.
- **산출**: viewer/index.html(`.ysit` CSS · yRender 지문 프리펜드 · yEnterChat 선반영) · 브랜치 claude/character-city-integration-uy0ajg.

## 132차 — 로딩 표시 = 스크램블 로더 이식 (운영자 260708 "로딩중=scramble 활용·별도 메시지 없이·메타버스 결")
- **의도**: 운영자가 로더 플레이그라운드(`260707…로더픽토그램차용` · beui.dev/components/motion/loader 이식 시안)에서 **scramble** 지정 — 앱 로딩/진행중 표시를 "불러오는 중…" 텍스트 대신 "LOADING" 글리프 스크램블로. 별도 문구 없이, 메타버스 결.
- **변경**: ⓐ **yScramble 팩토리**(index.html) — "LOADING"을 좌→우 한 글자씩 확정, 미확정 글자는 랜덤 글리프(`A-Z0-9<>/*#@`)로 스크램블 · 틱≈79ms(beui 원본 수치 `(speed/len)*1000*.55`) · **DOM 이탈 시 `isConnected` 체크로 타이머 자동 정리**(누수 0) · `role=status`+aria-label. ⓑ **`.yscramble` CSS** = 라임 네온(`--accent`)+글로우(`text-shadow` 토큰없는축)+**모노스페이스**(글리프 폭 고정=레이아웃 저크 0)+`--fw-b`·`--fs-body`. ⓒ **`.yeta-setup.yload` 중앙정렬**(픽토그램 로더 결). ⓓ **로딩 4지점 교체** — `불러오는 중`+`.gdots` → `X.replaceChildren(yLoadingBox())`: 설정 정책(`#ypolBody`)·대화목록(`#ylistRows`)·캐릭터 그리드(`#ycharGrid`)·챗 열기(`#yetaLog`). **에러/빈상태/셋업 메시지(`.yeta-setup` 텍스트)는 유지**(로딩 아님 = 터미널 상태). **신규 토큰 0**(락 55 불변).
- **효과/리스크**: 로딩 = 통일된 네온 스크램블(메타버스 결·문구 소거). 리스크 낮음 — 모노스페이스=폭 고정 저크 0 · 타이머 자가정리 · 색/크기 기존 토큰 · font-family/text-shadow=토큰 없는 축(게이트 무저촉·rc=0). 색 대안 3종(라임/그레이/흰) 대조서 라임=메타버스 압도적 → 채택.
- **검증**: 국소(뷰 로더) = 자가검증 — 실 `<style>` 재사용 헤드리스 렌더(라임 네온 중앙·스크램블 중간프레임 "LLYMIQO"·확정 "LOADING" 실측) + 색 3종 대조 + check_refs rc=0(락 55·거울·JS OK) + read-back. 운영자검증 = 폰 실로딩 체감(스크램블 속도·라임 톤). 롤백=git.
- **산출**: viewer/index.html(yScramble·yLoadingBox·.yscramble·.yload·4지점 교체) · 브랜치 claude/character-city-integration-uy0ajg.

## 133차 — 첫 열기 부팅 스플래시 = YETABUS 스크램블 (운영자 260708 "첫 열기 로딩도 지정 · YETABUS에서 시작")
- **의도**: 로딩 스크램블(132차)을 앱 **첫 열기(부팅)**에도 — 브랜드어 "YETABUS"가 스크램블 resolve되며 시작(메타버스 부팅 결).
- **변경**: ⓐ **`#yboot` 스플래시**(body 첫 자식 = 인라인 HTML → **첫 페인트 즉시** "YETABUS" · FOUC 0) — 스포티파이 블랙(`--yeta-bg`) 풀스크린 · `z:999`(로컬 z = raw 예외 · 부팅 시 top-layer dialog 없음). ⓑ 부팅 IIFE(스크립트 끝 · 최상위 `yScramble` 접근)가 인라인 텍스트를 `yScramble('YETABUS')`로 **교체** → 글리프 스크램블 resolve. ⓒ **`.yboot` CSS 페이드** = `ybootOut` 1.5s 대기(스크램블 1사이클 ≈0.87s resolve + 유지) 후 `.55s` 페이드 · **CSS 애니가 페이드 보장**(JS 실패해도 반드시 걷힘 = 블로킹 방지) · `animationend`=제거(스크램블 타이머 정지). ⓓ `.yboot-w`=`clamp(26–46px)` 대형 · reduced-motion=delay 단축. **132차 스크램블 재사용 = 신규 색·토큰 0**.
- **효과/리스크**: 첫 열기 = YETABUS 네온 스플래시 → 잠금화면(브랜드 부팅). 리스크 낮음 — CSS 페이드 이중보장(블로킹 0·JS 무관) · 인라인 = 첫 페인트부터 브랜드(빈 화면/FOUC 0) · SPA 내비 재출현 없음(풀 페이지 로드만 = "첫 열기" 정의 부합) · PWA 콜드스타트마다 = 앱 런치 스플래시(표준).
- **검증**: 국소(스플래시) = 자가검증 — 실 `<style>` 재사용 헤드리스 렌더(YETABUS 라임 네온 중앙·풀스크린 블랙 · 스크램블 mid/resolve) + check_refs rc=0(락 55·JS OK) + read-back. 운영자검증 = 폰 첫 열기 체감(스플래시 길이 ~2s·스크램블 톤). 롤백=git.
- **산출**: viewer/index.html(#yboot HTML · .yboot/.yboot-w/ybootOut CSS · 부팅 IIFE) · 브랜치 claude/character-city-integration-uy0ajg.

## 134차 — 대화·캐릭터 목록 UI 정리 배치 1차 (운영자 260708 스크린샷 9항목 中 4항목 · "머지 ㄱㄱ" · 병합 시 123→134 재배정)
- **경위**: 운영자 스크린샷 피드백 9항목(대화·캐릭터 목록·네비·로고·전화·폰트·전환·프사). 그중 명확·저위험 4항목 우선 반영 후 머지(나머지 5항목 = 후속 시각/조사 트랙).
- **수정(viewer/index.html + CLAUDE.md · 신규 :root 0 · 게이트웨이·파이프라인·모델 무변경)**: ① **항목1** 대화목록 우측 절대시각(`.ylist-t`) 폐지 → 상대시각(`yAgo`) 전면화(6h 초과도 시간→일 상시표시) · `.ylist-ago` 흰(--fg)→--mut 팔레트 통일(우측 3열 공간 부족 해소). ② **항목5** 이름(`.ynm-t`) `flex:none`+`max-width:5em` = 4음절 안 짤림(하은 가림 해소) · 직업배지(`.yjob`) `flex:none`→`0 1 auto` = 이름 대신 축소(배지 간격 6px 기존 통일 유지). ③ **항목4** 이름→상황 간격 축소(`.ycrow-nm`/`.ylist-nm`/`.yme-nm` margin-bottom `--lgap`→`*.55` · 전 목록 동일). ④ **항목6(명명)** 네비 명명 기틀 등재 = 상단 로고·프로필 바 `타이틀 네비`(.ywm/.yprof/.yssn) · 내 상황 행 `서브네비`(.yme).
- **후속(별도 트랙 · 미착수)**: 항목2(루시 프사 얼굴 = 유료 face 파이프/에셋 결정 필요) · 항목3(폰트 Paperlogy/LemonMilk 기본화 + baseline 어긋남 해소) · 항목6(두 네비 간격) · 항목7(로고 반토막 프사와 픽셀 중앙정렬) · 항목8(전화불가 캐릭터 = 전화픽토 mut + 락픽토) · 항목9(대화 전환 즉시 프사·배경). 시각 튜닝·조사 요하는 항목 = 스크린샷 검증 루프 후속.
- 검증: 자가검증 경로=check_refs rc=0(JS 구문 OK·락 54 불변·비차단 --bg-scrim/--line2 무관) + 머지 후 read-back | 국소 UI 손질(기틀 무침범·§검증 b-다) = 자가검증 · 시각 튜닝(간격 *.55·상대시각 표기)은 운영자 폰 체감 확정 여지. 롤백=git origin/main(직전 = 프로필 머지 ed29703).

## 135차 — 목록 UI 정리 배치 2차: 루시 잠금해제·폰트 기본화·전화락·네비 간격 (운영자 260708 스크린샷 9항목 中 나머지 · "다 해결해주고")
- **경위**: 배치 1차(134차) 이후 나머지 항목 처리. 그새 main에 PR #186(124~133차 캐릭터·도시 통합)이 대폭 머지돼 일부 항목을 이미 해결 = 현 상태 실측 후 남은 것만.
- **이미 해결됨(#186 · 실측 확인 후 재작업 안 함)**: **항목9(전환 깜빡임)** = #186 126차 "대화 시작 플로우: draw 왕복 전 캐릭터 대화창 즉시 세움(헤더·배경·상황·타이핑 선반영)" = yEnterChat(2070) = 정확히 "누르자마자 프사·배경" 요구 충족. **항목2 얼굴** = #186 e3e3166 "새 얼굴 프사(첨부2번)" = lucy_face.webp(제대로 된 얼굴) roster avatar 배선 완료.
- **수정(viewer/index.html + call.js + roster.json · 신규 :root 0)**: ① **항목2** 루시 `locked:true→false` — #186 배선 얼굴 노출 + 대화 가능(완전 카드[해금5·few-shot] 보유 = 잠금 해제 정합 · #186 "대화 불가 해결" 의도 완결) · winter는 카드 없음(met:false·IDOL) 잠금 유지. ② **항목3** yFontGet 기본값 = 영문 LEMONMILK·한글 Paperlogy(운영자 "기본값으로") + Paperlogy(9)·LEMONMILK(4) @font-face에 `ascent/descent/line-gap-override`(116%/29%/0 = Pretendard 박스 정규화) = 폰트 전환 baseline 어긋남 해소. ③ **항목8** 실전화 미배선(`!p.phone`, haeun 외 전부) 캐릭터 = 전화버튼 `.nophone`(mut) + 락 오버레이(아주 연하게 가운데) · `callBtnSync`가 페르소나별 갱신(onSess·초기화) · 탭 = 인앱 걸려오는 전화 폴백 유지. ④ **항목6(간격)** 타이틀네비↔서브네비 숨 `--ssn-h` +6→+16(운영자 "너무 쫍아 겹칠지경").
- **후속(별도 트랙 · 미착수)**: **항목7**(로고 반토막 = 글자부만 프사와 픽셀 중앙정렬) = 로고 글리프 실높이 vs 프사 측정 요 = 스크린샷 측정 루프 후속. 폰트 override·락 오버레이 위치·간격값은 운영자 폰 체감 확정 여지.
- 검증: 자가검증 경로=check_refs rc=0(viewer·call.js JS 구문 OK·락 불변) + roster.json JSON 유효 + node --check call.js + 폰트 override 13개 확인 + 머지 후 read-back | 국소 UI/콘텐츠(기틀 무침범) = 자가검증(§검증 b-다) · 시각(폰트 정렬·락 오버레이·간격)은 운영자 폰 체감. 롤백=git origin/main(직전 배치1 = 0512067).

## 136차 — char/list 상단 페이드 추가(로고·시즌 헤더 뒤 리스트 새는 것 차단) (운영자 260708 "로고 뒤로 리스트가 새어버리는지 확인")
- **진단(실측)**: 118차는 하단 네비만 페이드. 상단 = 로고(.ywm z:4)·시즌 헤더(.yssn z:3)가 콘텐츠(.ychar z:2) 위인데, `.yssn` 배경 = `rgba(accent,.07)` **+ backdrop-filter:none**(112차 "프로스티드 블러" 후속 미반영) → 스크롤 콘텐츠가 헤더/로고 뒤로 그대로 비침. Playwright `elementsFromPoint`: 헤더 뒤 스택 = [yssn, ycrow…], 로고행(top44) = [ycrow…] = 새는 것 확인.
- **수정(viewer/index.html · 신규 :root 0)**: 공통 mask 규칙을 둘로 분리 — `.ymapp,.ycd` = 하단만(기존) · **`.ychar,.ylist` = 상단+하단 양방향**. 상단 = 완전투명 0~56+safe-top(로고+헤더 위)→불투명 72+safe-top+--ssn-h(헤더 하단 아래) = 콘텐츠가 헤더 아래서만 또렷·위로 스크롤되면 로고·헤더 도달 전 사라짐. 헤더·로고는 별도 레이어(z:3/4)라 mask 무관 또렷. --ssn-h(JS 실측·폴백 86) 연동.
- 검증: 자가검증 경로=Playwright{상단 mask 적용(rgba 0,0,0,0 투명 시작)·헤더 위 스샷 = 세라 카드 페이드 소멸·헤더 또렷 · 118차 회귀 0[네비 회수 cd-open/sheetOn false·NAV-ON-TOP z:7·하단 페이드] · JS 에러 0} + 상단 스샷 육안(로고·헤더 뒤 안 샘) + check_refs rc=0 + 머지 후 read-back | 실질 UI 후속(기틀 무침범) = 자가검증. 롤백=_versions/260708_상단페이드.

## 137차 — 루시(시즌) 애니 아트 미디어 배선 + 클립 시작배경 (운영자 260708 "루트에 넣은 루시 이미지 다 루시로 · 영상=대화 시작 배경 · 도깨비/안드로이드 분류")
- **경위**: 운영자가 레포 루트에 루시용 이미지 12장(GitHub 업로드 커밋 f1de202)+기존 클립 업로드 — 옛 감정샷 189장은 에픽→시즌 리브랜드(2f92ddd) 때 미승계로 휘발. 루트 방치 = 캐릭터 폴더 미배선.
- **판정(실측 스크리닝 · 12장 육안)**: 11장 = 가공 AI 애니 아트(사이버펑크 엣지러너 계열 + 도깨비 요소 · 실존인물 아님 · 미성년 선정성 0) → 루시 배선. 1장(`[260331] WONYOUNG…webp`) = 실존 아이돌(장원영) 의도 사실적 초상 = 로맨틱/성인 컴패니언에 실존인물 심기라 **미배선·루트 존치**(처리 = 운영자 판단 위임).
- **수정(뷰어 코드 무수정 = 기존 시즌 미디어 엔진 `ySeasonMedia`/`yStage` 재사용 · 게이트웨이·파이프라인·모델 무변경)**: ① 애니 11장 → `viewer/characters/season/lucy/{dokkaebi,android}/` git mv(모드 서브폴더 · dokkaebi 7·android 4). ② `media.json` 신설(엔진 계약 버킷 `{base·warm·tense·blue}` · 답장수 n 결정적 로테이션 `pool[n%len]`). **모드↔무드 브리지**: 안드로이드(낮=시크 자동인형)=base·blue / 도깨비(밤=고에너지)=warm·tense = 루시 이중성 정합. **base[0]=클립(lucy_clip_01.mp4) → 대화 시작 배경(muted 비디오 · yStage n=0·mood='')**. ③ roster lucy에 `season`:media.json 필드 **가법**(avatar/bg는 아래 리베이스 재조정 참조).
- **효과/리스크**: 효과 = 루시 정적 1장(구 latex 크롭)→감정 로테이션 무대(클립 시작배경) 복원 · 이미지 git 커밋으로 재휘발 차단. 리스크 = 모드↔무드 브리지·장별 분류는 주관(라벨 조정 가능) · 옛 189장 미복원(신 애니셋 교체) · imgi_56↔57 근중복 2장 존치.
- **리베이스 재조정(자체 체크인서 발견)**: PR #193 생성 후 ~1h 새 main이 #186/#194/#195(124~135차) 머지 = 다른 세션이 루시 병렬 작업(125차 `lucy_face` 얼굴 아바타 + `lucy_02/room/neon` 챗 배경 · 135차 `locked:false` 잠금해제 · **동일 사이버펑크 애니 방향**). roster·원장 충돌 → **가법 병합**: main의 얼굴 아바타(`lucy_face`)·`bg=lucy_02`·잠금해제·직업(안드로이드) 전부 보존 + 내 `season` 매니페스트만 얹음(클립 시작배경·감정 로테이션 = main 미보유 = 내 고유분). `lucy_02`=내 `android_01` 동일 이미지(중복)라 media.json 추가배선 불요. 원장 119→137차 재부여(양쪽 보존 · main 136차[페이드]와 재충돌 반영).
- 검증: 자가검증 경로=JSON 유효성(roster·media.json)+media.json 전 14경로 실존(0 누락)+base[0]=클립 확인+check_refs rc=0 | 애셋 배선(기틀 무침범)=자가검증. 롤백=_versions/260708_1357_lucy-anime-미디어배선.

## 138차 — 루시 신규 이미지 25장 캐릭터 분류 배선 (운영자 260708 "캐릭터 유사성으로 딱 보고 분류 · 레베카가 루시임 · 안드로이드>도깨비 페르소나대로")
- **경위**: #193 머지 후에도 운영자가 루트에 이미지 계속 적재(33장). 운영자 지시 = 캐릭터 유사성 육안 분류(레베카=루시 도깨비 모드 / 루시=안드로이드 모드 · 페르소나 "낮 자동인형/밤 도깨비"대로).
- **판정(콘택트시트 육안 · Pillow 2장 그리드)**: 29 유니크 中 **도깨비 13**(레베카 = 민트 트윈테일·송곳니·글로우눈)·**안드로이드 12**(루시 = 흰/블루 스트레이트·시크·사이버패널)·**제외 4**{데이빗2인 씬·원경 거리·장원영(실존)·REBECCA 텍스트배너}. 완전중복 4장(imgi_*(2) = 기배선 dokkaebi와 동일바이트) 삭제.
- **수정(뷰어 코드 무수정 · roster 무변경 = `season` 필드는 #193서 이미 배선)**: 25장 → `viewer/characters/season/lucy/{dokkaebi,android}/` git mv(dokkaebi 07→20·android 04→16). `media.json` 전면 재구성 — base=[클립]+안드로이드16 · warm/tense=도깨비 20 반분 · blue=안드로이드16. **base[0]=클립 유지**(대화 시작배경). 모드↔무드 = 안드로이드(base·blue)/도깨비(warm·tense) 정합.
- **효과/리스크**: 루시 감정 로테이션 대폭 확충(11→36장). 리스크 = 도깨비/안드로이드 경계 일부 주관(둘 다 루시라 저위험·라벨 조정 가능) · 제외 4장 루트 존치(운영자 처리) · 근중복 몇 쌍 존치.
- 검증: 자가검증 경로=media.json 전 경로 실존(0 누락)·고아 0·base[0]=클립·check_refs rc=0 | 애셋 배선(기틀 무침범)=자가검증.

## 139차 — 루트 잔여 레퍼런스 이미지 4장 삭제 (운영자 260708 "남은 사진은 버려줘")
- **경위**: 루시 이미지 분류 배선(136·138차) 후 루트에 캐릭터 배경 부적합분만 잔존 — 데이빗+루시 2인 씬(`Cyberpunk Edgerunners.jpg`)·원경 거리씬(`Cyberpunk edgerunners rebecca.jpg`)·REBECCA 텍스트 배너·장원영(실존) webp.
- **수정**: 4장 `git rm`(레포 루트 청소 · 캐릭터 폴더·media.json 무영향 = 미배선분이라 참조 0).
- 검증: 자가검증 = 루트 이미지 0 확인 + check_refs rc=0 | 순수 삭제(기틀 무침범)=자가검증.

## 140차 — 챗 블랙스크린 근본수정 (10인 분신술 + 헤드리스 재현) (운영자 260708 "락걸려서 그런진 모르겠는데 화면들 잘 진행되는지 분신술 검증")
- **경위**: 운영자 스샷 = 챗 화면이 헤더만 뜨고 본문 완전 공백(헤더 "YETA"=기본값 대문자화·아바타 "Y"=기본 → 캐릭터 미적용). 10인 분신술(READ-ONLY 병렬) + 메인 헤드리스 재현(playwright-core + 프리설치 chromium, file:// + 게이트웨이 목업)으로 근본원인 확정.
- **판정(9인 수렴 + 재현 확정)**: 근본원인 = `openYeta()`가 `yetadlg.showModal()`로 **다이얼로그를 먼저 연 뒤** `yLoad()`의 `const r=await yApi('get').catch(()=>null); if(!r) return;`이 **get 실패 시 조용히 early-return** → yApply/yView/yRender 미도달 → 헤더 기본·본문 공백·입력 숨김 동결. YCHARS 선로딩(언락→char탭) 시 로더도 스킵 = 순수 공백. 헤드리스 재현: get abort 시 정확히 재현 → 수정 후 리스트 폴백. **부팅 스플래시·CSS 마스크/z-index·폰트메트릭·지도·call.js·설정/PIN·선택창 = 전부 무관(실측 배제)** — 운영자 "락" 직감은 PIN이 아니라(무관) 캐릭터/로드 경로였음.
- **수정(viewer/index.html · 신규 :root 0 · 게이트웨이·파이프라인·모델 무변경)**: ① **yLoad get 실패** `if(!r) return` → `if(!r){ if(_yView!=='list') yShowList(); return; }` (빈 목업 모달 동결 → 리스트 폴백). ② **call 훅 격리** `if(window.YCALL) YCALL.onSess(YSESS)` → `try/catch`로 감쌈(훅 예외가 아래 yApply/yRender 렌더 중단 못하게 · 분신술8). ③ **시즌 클립 암전 차단**(분신술3 · 내 136차 클립=base[0] 회귀): `yPaintBg` 클립 분기가 `backgroundImage='none'`로 무대를 비우던 것 → 시그니처 스틸(YCUR.bg=lucy_02) 밑배경 + `v.poster` 부여 = 클립 디코드/자동재생 차단 시에도 암전 대신 스틸(비시즌 프리로드 게이트 대응).
- **효과/리스크**: 블랙스크린(get 딸꾹질·따뜻한 재진입) → 리스트 폴백으로 항상 탈출. 시즌 클립 첫 프레임 전 암전 제거. 리스크 낮음 = 전부 방어적 에러경로 하드닝(정상 경로 무변경 · 헤드리스로 정상 렌더 회귀 0 확인).
- 검증: 자가검증 경로=10인 분신술 수렴 + 헤드리스 재현(수정 전 블랙 재현→수정 후 타이틀 "무음동 사람들" 리스트 폴백) + 클립 mp4 abort에도 poster/스틸 lucy_02 노출 실측 + 정상 yEnterChat 렌더 회귀 0(height 720·버블·지문) + check_refs rc=0(JS구문 OK). 실질 UI 수정(에러경로 하드닝·기틀 무침범)=자가검증.

## 141차 — 캐릭터 디테일 [연락처 교환하기] 버튼 중복 제거 + 글자체 대화시작 통일 (운영자 260708 "2개 중 자동으로 연락처앱 저장하는 것만 하나 남겨 · 글자체는 대화 시작과 통일")
- **경위**: 운영자 스샷 = 하은 디테일에 「연락처 교환하기」 버튼 2개(폰트 제각각) — "제대로 된 게 뭔지 모르겠다 · 자동으로 연락처 앱에 저장(안드로이드·iOS)하는 기능만 하나 남겨 · 글자체 대화 시작과 통일".
- **판정(실측)**: 중복 근원 = 디테일 카드에 연락처 버튼을 **두 곳**이 렌더. ① `index.html` `.ycd-sub`(c.phone 조건 · 클릭=`location.href`로 `assets/contacts/<id>.vcf` **직접 내비** + inline MIME(_headers) = iOS 연락처 시트/Android 열기 **자동 실행** = 운영자 요구 "자동으로 연락처 앱 저장"). ② `call.js` `initCdVcf`(MutationObserver로 #ycdCard 재생성 감지 주입 · 클릭=명함 시트→`a[download]` = 신형(260707) 주석이 "그건 저장만 됨"이라 명시한 구형(260706) · 수동 3단계 안내). roster상 phone 보유 = haeun 단독 → 둘 다 하은에게만 = 스샷 중복 정확 재현.
- **수정(viewer · 신규 :root 0 = 계승만)**: ① `call.js` `.ycd-vcf` CSS 3규칙 + `initCdVcf()` 함수 + `initInject` 호출 제거(구형 중복 폐지). vcfSheet·헤더 #yetaVcfBtn·첫진입 자동시트는 **존치**(타 표면 = 무영향). ② `index.html` `.ycd-sub` 글자체 계승 통일 `--fs-sm/--fw-b`→`--fs-body/--fw-x`(= `.ycd-cta` 대화 시작 동일 토큰 · 기존 토큰 계승 = :root 신설 0). 지오메트리·배경(padding 11·`--glass`)은 보조 위계로 존치.
- **효과/리스크**: 연락처 버튼 2→1(자동 연락처앱 등록 경로만 잔존 · 남긴 것 = 요구 기능 정본). 글자체 대화시작과 동일(computed 15px/800). 리스크 낮음 = 순수 삭제 + 토큰 스왑(정상 경로 무변경 · 잔존 참조 0).
- 검증: 자가검증 경로=헤드리스 Chromium 실측(전역 playwright + 프리설치 chromium · 실제 뷰어 file:// · 하은 디테일 실렌더) **변경전=연락처 버튼 2개·.ycd-sub 12px/700·.ycd-vcf 13px/700 ↔ 변경후=1개·.ycd-sub 15px/800=.ycd-cta 정확일치·call.js 미주입** 실측 + 카드 스샷 육안(단일 버튼·굵기 통일) + check_refs rc=0 + node --check OK. 실질 UI(기틀 무침범)=자가검증. 롤백=_versions/260708_2310_연락처교환버튼_중복제거.

## 142차 — 대화 UI 일괄 개선: 간격·시즌 표기·배경 축·기록 미터·설정 전면 배선 (운영자 260709 지시 14건)
- **경위**: 운영자 첨부 3장 + 지시 14건 — ①타이틀네비↔시즌헤더 간격 확대 ②SEASON 배지 박스 제거 ③대화중 캐릭터 차별(스포티파이 블랙) ④챗 전환 시 인트로 영상 대신 관련 배경 즉시(불가 시 로딩) ⑤재진입 시 멈춘 메인 영상 잔상 제거 ⑥캐릭터 아트 배경 = 블랙 30% 딤 ⑦환경 배경 = 딤 완화 ⑧전화 락 = MUT 수화기 + 터콰이즈 락 픽토 ⑨챗 헤더 설명 흰색 불투명 ⑩프사 탭 = 현재 생각·소개 ⑪좌상단 기록 미터(모델|누적 토큰) ⑫설정 모델 소넷 전환 등 ⑬폰트 기본 Paperlogy+LEMONMILK ⑭안 먹히는 설정 전부 배선.
- **판정(근본원인)**: 배경 축 = 시즌 미디어 base[0]=클립이 답장수 0(진입·재진입)마다 첫 페인트 + 무대 2겹이 챗 닫아도 미소거 = 멈춘 프레임 잔상. 미터 = claude_meter가 usage를 이미 계측하나 세션 턴 미탑재. 설정 = read/call/ptt 토글 소비처 부재 + 모델·글자크기 read-only 행. 폰트 = 기본값 코드는 260708 반영됐으나 구버전 저장분('sys')이 덮음. 락 = 수화기·자물쇠 동색(mut)+저투명 = '열쇠' 오독.
- **수정**: ①`viewer/index.html` — 시즌헤더 top 64→78·숨 +16→+22·마스크 동기(70/86) · `.yssn-tag` 필 폐지=라임 워드마크 · `.ycrow.talking` opacity 흐림→`::after` rgba(18,18,18,.45) 워시 · `yBgReset` 신설(방 전환·챗 닫힘 = 레이어 2겹 비디오 pause/제거+그림 소거) · `_yStageCold` = 시즌 진입 첫 페인트 클립 스킵(이미지 우선 · 클립은 진행 중 로테이션 존치) · 스크림 = 캐릭터 아트 .35/.55→스포티파이블랙 .55/.68(+30% 등가)·환경 .72/.86→.55/.72·클립 brightness .62→.55 · `.yh-tag` mut→fg · `#yetaProfPop` 프로필 팝오버 신설(.yeta-pick 계승 — 지금 생각[--think]·위치·공기·소개) · `.ymeter` 신설(`Opus 4.8 --effort low | N tok`) · 설정 = 다이얼 2행(모델·effort — localStorage+YDIAL 재장전)·글자 크기 3단(타이포 토큰 6종 런타임 배율)·PTT 토글=html.yeta-ptt(기본 OFF·마이크 복원)·읽음 표시 실배선(기본 ON)·풋노트 갱신 · 폰트 1회 마이그레이션(yeta_font_v2 — 구 저장분 소거). ②`viewer/call.js` — `.ycall-lock` 터콰이즈(--think·불투명·그림자)·onSess에 call 토글 게이트(OFF=부재중)·`YCALL.sync` 신설(yApply 훅 = 방 전환 즉시 수화기 상태 동기). ③`shared/claude_meter.sh` — `METER_LAST` 옵트인 출구({in,out} 파일 기록 · 실패 무해). ④`.github/scripts/yeta_chat.sh` — gen_out이 METER_LAST 회수(TOK_I/O)→finish가 답장 턴 `tok{i,o}` 박제(파이프라인 호출·출력 계약 무변경 · fail-soft). ⑤`docs/CII_컴포넌트계승인덱스.md` 신규 3행(프로필 팝오버·미터·전화 락 픽토). 신규 :root 토큰 0(터콰이즈=기존 --think 계승).
- **효과/리스크**: 배경 = 항상 그 방 관련 이미지(잔상 0 · 로드 전 블랙 베이스) · 밝기 양방향 정합. 설정 = 표기상 실동작 항목 전부 진짜 동작(테마·통화상한 = 서버·브랜드 고정 명기). 리스크 = 토큰 미터는 배포 후 답장부터 누적(과거 턴 소급 불가) · 글자크기 배율은 :root 원값 스냅샷(YFS_BASE) — 타이포 토큰 개정 시 동기 필요 · '답장 알림'은 권한 요청까지(웹푸시 구독 저장 백엔드 = 후속).
- 검증: 자가검증 경로=헤드리스 E2E 22항목 전원 PASS(Playwright+chromium·api 목킹 — PIN 패턴 언락→간격 78 실측→SEASON 투명배경/라임→talking ::after 워시→루시 진입 배경 android_01(클립 스킵·비디오 0)+스크림 .55/.68→헤더 설명 흰색→미터 표기/12.3k 누적→팝오버 개폐→닫힘 레이어 소거→방 전환 잔상 0→모델/effort/글자크기/PTT 실동작→send 바디 sonnet-5·effort '' 실박제) + 전화 락 2상태 실측(무디=터콰이즈 락/하은=락 없음) + check_refs rc=0 + bash -n·node 구문 OK | **10인 평의회(READ-ONLY 병렬 · 각기 다른 앵글) 전원 APPROVE(치명 0)** — 지적 반영 16건: 미터↔첫 메시지 겹침(로그 상단 80px)·폰트 마이그레이션 sys만 소거(명시 선택 보존)·YFS_BASE :root 파생(값 SSOT 이중화 해소)·크로스페이드 클립 pause+디코더 해제·시즌 버킷 첫 이미지 워밍·yView(list) 무대 소거·클립 전용 풀 시그니처 폴백·클립 brightness .45(30% 등가)·talking 워시 .38(살짝 톤)·단톡 팝오버 persona 엄격 매칭·아바타 pid 필터 정합·방 전환 팝오버 회수·팝오버 상호배타·미터 음수 가드·.sep opacity·--think 주석 도메인·CII 문구 정정. 롤백=_versions/260709_0006_대화UI개선.

## 143차 — 연락처 교환 vcf: MIME 표준화(text/vcard)+inline + 자동설치 웹한계 확정 (운영자 260709 "자동으로 뜬 뒤 설치까지 가야 하는데 다운로드에서 끝남 · 안드로이드·아이폰 아무 브라우저 · 웹앱환경")
- **경위**: 141차로 연락처 버튼을 직접 `.vcf` 내비(`.ycd-sub`) 하나로 단일화했으나, 운영자가 "자동으로 연락처 앱 저장까지 안 되고 다운로드에서 멈춘다"고 보고. 목표 = 안드로이드·아이폰 아무 브라우저·웹앱(PWA) 환경에서 되게.
- **판정(실측 + 웹검색 2건)**: ⓐ 라이브 헤더 `curl -I` = `content-type: text/x-vcard`(레거시)·`content-disposition` 없음·`x-content-type-options: nosniff`. ⓑ **웹은 폰 주소록에 자동 "설치" 원천 불가 = OS 정책(코드 우회 없음)** — Android=클릭 시 다운로드→사용자가 알림/파일 탭→연락처 앱 수동 임포트(자동 임포트 API 없음) · iOS Safari=inline `text/vcard` 면 "연락처 추가" 시트 자동(웹의 유일 자동경로) · iOS 설치형 PWA=standalone 웹뷰가 파일 앱으로 보냄(window.open/_blank·SW 우회 다 불안정 = 시스템 레벨). ⓒ 레거시 `text/x-vcard` + `nosniff` = iOS조차 "미리보기 불가" 판정 → 다운로드로 새어 **iOS Safari 자동시트마저 못 뽑던** 근인.
- **수정(신규 :root 0)**: ① `viewer/_headers` — `text/x-vcard`→`text/vcard`(RFC 6350 표준) + `Content-Disposition: inline` 추가(iOS Safari 시트 트리거) + 웹한계(자동설치 불가·Android/PWA 다운로드 강제) 주석 = SSOT. ② `viewer/index.html` `.ycd-sub` 탭 안내 토스트 플랫폼별로 명확화(Android="상단 알림줄에서 파일 탭하면 연락처에 추가가 떠" / iOS·기타="연락처 추가가 뜨면 저장 · 안 뜨면 받은 파일 열면 돼") · 기존 주석의 부정확한 "Android 열기 연동"→실측(다운로드→탭 필수)으로 정정.
- **효과/리스크**: iOS Safari 자동 연락처시트 성공률 개선(레거시 MIME 병목 제거). **Android·iOS PWA는 OS 한계로 "다운로드→1탭"이 불변**(웹으로 자동설치 불가 = 안내 UX가 최선). 리스크 낮음 = 헤더 표준화 + 토스트 문구(정상 경로 무변경). 후속 선택지(운영자 판단) = Android용 지속형 단계 가이드 카드(중복 버튼 부활 없이 1버튼 탭 후 표시) 원하면 추가.
- 검증: 자가검증 경로=**라이브 curl -I 실측(배포 반영 후 `content-type: text/vcard` + `content-disposition: inline` 확정 · ~15s 폴링)** + origin/main read-back(_headers·index.html) + check_refs rc=0 + 웹검색 2건(Android vcf 자동임포트 불가·iOS PWA vcard Safari 강제 불가 확인). 실질 UI/인프라 수정(기틀 무침범 · _headers MIME = 국소)=자가검증. 롤백=_versions/260709_0730_vcf_MIME표준_inline.

## 144차 — 운영자 재검수 라운드: 락 확대·배경 딤 재조정·미터 스트립·스크롤바 (운영자 260709 스샷 3장)
- **경위**: 142차 라이브 검수 피드백 5건 — ①전화 락 픽토 "작아짐·불투명하게" ②무디 찻집 스샷 "이 배경은 그대로임"(1차 완화 .55/.72가 어두운 원화에선 체감 미미) ③"캐릭터 배경 불투명도 +30% 더 — 대화창 집중" ④미터(OPUS…) 중앙정렬 + 화면과 반대되는 그림자 상시 + "다른 대화가 침범 못 하는 고유영역(딱 그 영역만 대화 숨김)" ⑤스크롤 토글 = 거의 안 보이는 초투명 흰 글래스·얇게.
- **수정**: ①`viewer/call.js` 락 svg 11→22(2배)·rect fill=currentColor(몸통 채움 = 뒤 수화기 비침 차단 = 불투명). ②`viewer/index.html` yPaintBg 환경 스크림 .55/.72→**.35/.52**(대폭 완화). ③캐릭터 아트 스크림 .55/.68→**.68/.78**(+30% 등가)·클립 brightness .45→**.32**·시즌 비디오 밑배경 동기. ④`.ymeter` left0/right0+center·역상 헤일로 text-shadow 상시 · `.yeta-log` 상단 패딩 88 + **밴드 마스크(60~76px+safe)** = 미터 고유 스트립(그 밴드 통과 대화만 숨김·헤더 뒤 글래스 비침은 유지 — "딱 그 영역만"). ⑤전역 스크롤바 thumb = rgba(255,255,255,.09)·폭 6→4·테두리 폐지·hover .2(Firefox scrollbar-color 동기) · 스크롤 동작 자체는 정상 확인(.yeta-log overflow-y 네이티브).
- **효과/리스크**: 환경 무대 또렷·캐릭터 아트는 대화 집중 딤 · 미터 = 어느 배경에서도 판독+전용 레인 · 리스크 = 스크림 값은 운영자 감각 재조정 여지(체감 축) · 밴드 마스크는 스크롤 중 메시지가 그 좁은 밴드에서 잠깐 사라지는 연출(요구 사양 그대로).
- 검증: 자가검증 경로=헤드리스 E2E 재실행(수치 어서션 갱신 — 스크림 .68/.78·.35/.52·미터 center+text-shadow·로그 마스크·스크롤바 4px·락 22px/fill) 전원 PASS + check_refs rc=0 | 국소 손질(값 조정·기틀 무침범)=자가검증. 롤백=_versions/260709_0742_전화락픽토_확대.

## 145차 — 쿼터 문구 대사 유출 차단(4계정 폴오버 성공경로 편입) + 대화목록 인물 미표시 자가치유 (운영자 260709 스샷 2건)
- **경위**: ①하은 방 마지막 대사에 영어 쿼터 원문("YOU'VE HIT YOUR WEEKLY LIMIT · RESETS JU…")이 캐릭터 대사로 박제 = 답장 끊김 ②대화 목록 첫 진입에 인물 대신 ID·이니셜(LUCY/L·MUDI/M)로 표시.
- **판정(근본원인·실측)**: ①`claude -p`가 주간 한도 문구를 **rc=0 정상 출력**으로 반환하는 케이스 — `gen_out`/`yeta_call.sh` 성공 판정(`rc=0 && OUT 비어있지 않음 → break`)이 이를 성공으로 오판 → `is_quota`/`claude_failover`(4계정 체인)가 실패 경로에서만 검사돼 **한 번도 안 돎** → 원문이 턴으로 반영. Actions 로그 실측 = ALT/ALT2/ALT3 시크릿 전부 주입돼 있으나 미발동. ②`yListRender`가 로스터(YCHARS) 로드를 **세션 미로드 때만** 시도 → 세션만 있고 로스터 fetch가 한 번 실패하면 폴백(`{name:id}`) 렌더 + `_ylSig` 가드가 재도색을 분 단위로 차단.
- **수정**: ①`.github/scripts/yeta_chat.sh` gen_out + `.github/scripts/yeta_call.sh` 생성 루프 — 성공 분기 진입 전 `is_quota "$OUT"` 검사: 쿼터 문구면 `claude_failover` 전환 후 재시도, 체인 소진 시 rc=1 실패 처리(OUT 보존 = 호출부 is_quota 재판정 → "사용량 한도야 — 잠시 후 다시 보내줘" 안내 · 원문 유출 0). ②`viewer/index.html` yListRender — 로스터 로드를 !YSESS 블록 밖으로: 빈 로스터 = 렌더마다 재시도 + 도착 시 `_ylSig=''`(즉시 인물 재도색)+`ySsnFixed()`.
- **효과/리스크**: 주간 한도 = 서브계정 자동 로테이션이 성공경로에서도 발동(MUTENO→NOMUTEFB→EMS1130G→MUTENONA) · 전 계정 소진 시에만 안내 문구. 목록은 로스터 지연·일시 실패에도 다음 렌더에서 자가치유. 리스크 = is_quota 오탐(대사가 영어 쿼터 문구 형태일 때) — 한국어 대사 오탐 없음 실측 · 하은 방에 이미 박힌 과거 유출 턴 1개는 존치(스크럽은 R2 수동 축 — 방 나가기로도 정리 가능).
- 검증: 자가검증 경로=가짜 claude 바이너리 행동 테스트{쿼터 rc=0 → ①서브토큰 무 = rc=1 실패(유출 차단)+is_quota 재판정 성립 ②서브토큰 유 = "서브1 계정으로 전환 후 재시도(1/3)" 실발동} + bash -n 2종 + 헤드리스 목록 자가치유 재현(세션有·로스터空 → 루시+얼굴 아바타 복원) + 전체 E2E 23항목 PASS + check_refs rc=0. 롤백=_versions/260709_0809_쿼터누출_목록로스터.

## 146차 — 설정값 강조색 체계 + 락 터콰이즈 통일 + 설정 시트 음영 (운영자 260709 지시 4건)
- **경위**: 운영자 스샷 2장 + 지시 — ①설정값(OPUS 4.8·빠름 등 고른 값) = 강조색, 기본값 아닌 건 2강조색("쿼타이즈?" = 터콰이즈 지칭) ②설정 시트 = 배경 위 음영 더 짙게(설정 잘 보이게) ③섹션 라벨(알림·글꼴·대화…) = 흰색 ④절대선 등 락 걸린 항목 = 2강조색(챗 헤더 전화 위 락과 동일색 지정 — 실측 = `--think` 터콰이즈 · 공식 강조2 코발트와 별개 축).
- **수정(viewer/index.html 단독 · 신규 :root 0 = 전부 계승)**: ①값 라벨을 `si-vt` span 분리 — 라임(`--accent`)·기본값 이탈 `.mod` = 터콰이즈(`--think`) · 꺾쇠·읽기전용(테마·통화상한)은 뮤트 유지 · 판정+라벨 = `ySetVal` 단일 SSOT 신설(초기 렌더·탭 갱신 3곳 삼중화 제거·`dialLbl` 폐지) · 기본값 = Opus 4.8·빠름·LEMONMILK·Paperlogy·보통. ②`.ycd-live` 투명→`rgba(18,18,18,.42)` 스포티파이 블랙 스크림(영상 비침 유지 · 전역 .38과 합성 ≈.64 · lk-pad 계보). ③`.yset-sec` mut→`--fg`. ④락 표기 터콰이즈 통일 — 하드 락 그룹(절대선·출력계약) 헤더 `.ypol-grp.lk`(자물쇠+제목+"불변" 라벨) · 게스트 "관리자 전용" 라벨(🔒 이모지→YLOCK_SVG = 색 가능) · L0/L1 summary 잠금 픽토 동일 교체 · 성향 잠금 배지(.yset-lockmsg) — 본문 항목(무조건 거부 4행 등)은 뮤트 유지(가독 위계). 부수 = 글꼴 라벨 "기본(Pretendard)" 잔재 정정(260708 기본값 전환 후 Pretendard가 비기본).
- **효과/리스크**: 설정 한눈 판독 — 고른 값 = 라임 · 기본에서 튜닝한 값 = 터콰이즈로 즉시 구분 · 락 시맨틱 = --think 단일 톤 완성(전화 락·성향 잠금·정책 하드 락). 리스크 낮음 = 색·라벨 계승 스왑 + 렌더 헬퍼 통합(동작 로직 무변경) · 스크림 .42는 운영자 감각 재조정 여지.
- 검증: 자가검증 경로=check_refs rc=0(토큰 락 게이트 미승인 신규 0 · --bg-scrim 경고 = 기존 비차단 실측 확인) + node --check 인라인 JS OK + dialLbl 잔존 0 + origin/main read-back(si-vt·ypol-grp lk·ySetVal 9곳·스크림·섹션 라벨 실존) + Cloudflare Pages 배포 성공(6a30482). 국소 손질(색 계승·기틀 무침범)=자가검증. 롤백=_versions/260709_0813_설정값강조_락터콰이즈.

## 147차 — 시즌 내비 테두리 제거 + 대화 목록 각 영역 스포티파이 블랙 30% (운영자 260709 "타이틀 네비 아래 시즌 내비 테두리 없애줘 · 대화 시작된 애들 스포티파이 블랙 30% 더 입혀")
- **경위**: 운영자 첨부 스샷(대화 탭) — ①타이틀 네비 바로 아래 시즌 내비(.yssn) 프레임 테두리 지적(142차 ② SEASON 필배지 폐지의 다음 궤적 = 헤더 프레임까지 제거) ②대화 시작된 방들이 밝은 배경 위에서 대비 약함 → 각 대화 영역에 스포티파이 블랙 30% 더.
- **판정(실측)**: ①테두리 = `.yssn { border:1px solid var(--glass-line-0); box-shadow:inset 0 1px 0 var(--glass-line-0); }` 한 쌍(외곽선+상단 하이라이트). border만 지우면 상단 1px 획 잔존 = 비대칭 → 둘 다 제거해야 프레임 완전 소거. ②대화 목록 = `.ylist-group` 단일 글래스 면 + 각 방 `.ylr-wrap`(행 = background:none, 카톡 결). 스포티파이 블랙 오버레이 = 이 레포 확립 관용 `rgba(18,18,18,.X)`(.ym-ovl .67·.ycrow.talking::after .38·.ybgvid::before .2·캐릭터 아트 스크림 30% 260709). `--bg`(#161716)는 rgb 토큰 부재로 알파 파생 불가 → 관용값이 정본(토큰 없는 축 = raw 예외).
- **수정(viewer/index.html · 신규 :root 0 = 전부 계승)**: ① `.yssn` border + inset box-shadow 선언 제거(그라디언트 구역·라운드·등장애니 존치). ② `.ylist-group .ylr-wrap`에 `background:rgba(18,18,18,.3)` 추가 = 각 대화 영역 30% 워시(헤어라인 구분선 존치).
- **머지(운영자 "기존거 유지하면서 너가 바꾼 부분만 적용")**: 활성 개발로 main이 머지 도중 5회 이동(#205~#208 character-dialogue-ui/work-in-progress + 자체 143~146차). 스테일 머지 = 되돌림 위험 → `-X ours` 금지. 최종 = origin/main을 브랜치에 머지-인해 상위집합화 · viewer는 타깃 라인 무충돌 자동병합(내 2편집 + main 편집 공존) · 원장은 append-only 충돌 = main 146차 보존 후 내 항목 147차 재번호(양쪽 보존). 2872줄 롤백 백업 파일은 뺌(git revert가 롤백 · 운영자 surgical 지시).
- **효과/리스크**: 시즌 헤더 프레임 없이 은은한 구역만(타이틀 네비와 위계 정리) · 대화 각 방 30% 가라앉아 밝은 배경 대비·구획감 ↑. 리스크 낮음 = 선언 제거 1 + 확립 관용값 추가 1, 정상 경로·구조·파이프라인 무변경. talking 워시(.38 캐릭터탭)↔목록 워시(.3)는 표면 달라 무충돌.
- 검증: 자가검증 경로=헤드리스 Chromium 실제 토큰·CSS 라이브 재현 스샷(시즌 테두리 소거+각 대화 행 30% 블랙 육안) · check_refs rc=0(신규 토큰 0·JS구문 OK) · 신규 커밋 무충돌 실측 + 로컬 3-way 머지 dry-run clean · Cloudflare Pages 프리뷰 배포 성공 | 국소 UI 손질(기틀 무침범)=자가검증. 보고서=docs/reports/260709_시즌테두리_대화블랙30_보고서.html · 롤백=git revert.

## 148차 — 운영자 260709 지시 일괄 2탄: 패턴 탭입력·미대면 잠금(???)·시즌 네비라인·종료팝업·스와이프·지도 차선
- **경위**: 운영자 연속 지시 8건 — ①패턴 잠금 = 점 하나하나 클릭해도 이어지게(2486 탭 = 자동 연결·틀린 모션도 동일) ②지도 안 만난 사람 = ??? 표기 ③대화 안 한 캐릭터 = 전부 잠금(하은·루시·윈터 외 · 난입·마주침 이벤트로만 해금 · "만나지 않음"과 LOCKED는 별개) ④시즌 = 둥근 박스 금지(네비 라인) ⑤네비 그라데이션(플레이그라운드 요청) ⑥"대화 중" = 2강조 50% ⑦SEASON 글자 형광 ⑧종료 확인 팝업(아니오=주의색·X·바깥클릭) + 좌우 스와이프 탭 이동 + 지도 차선(손그림 고정 · 상행 파랑/하행 레드 · 간헐 · 건물 가림 레이어).
- **판정**: 패턴 = patEnd "미완 4점 미만 = 450ms 흔들고 리셋"이 탭 축적 원천 차단(수정 1차에서 이 잔존을 누락 → E2E 오답 bad가 10ms 만에 사라지는 미스터리 = MutationObserver 타임라인으로 각 탭+450ms 리셋 3연발 실측 확정 후 제거). 미대면 = 기존 yLockLabel(스페셜 locked)과 별개 축 필요 → roster met:true 박제(하은 신규·루시 기존) + 스레드·합석 이력 동적 판정. 캐릭터 리스트가 세션 비차단 로드 후 재렌더를 안 해 스레드 만남이 ???로 방치되는 동반 버그 발견 → 세션 도착 1회 재렌더.
- **수정(viewer/index.html + roster.json 하은 met · 신규 :root 0)**: ①패턴 = 손뗌 리셋 폐지·판정중/bad에서만 새 시작·미완 방치 6s 조용 리셋(patIdle) ②`yUnmet` 신설 — 리스트(???·실루엣·"아직 못 만남" 필)·디테일(??? · CTA "골목 어딘가에서 마주치면 열려")·챗 픽커·🎲 풀 제외·지도(이름표 ???·실루엣 필터·탭 차단·세션 선로드)·진입 단일 가드 = yEnterChat ③`.yssn` 박스 폐지(패딩·배경·테두리·라운드 제거) + `.yssn-tag` 형광 2중 글로우(.9/.55) + `#ytopfade` 상단 그라데이션 신설(기본 .66→.30@52%→투명 · 216px) — 플레이그라운드 `docs/reports/260709_네비라인_그라데이션_플레이그라운드.html`(프리셋 4종+슬라이더 6축+CSS 복사) ④`.ycrow-on` = rgba(--think-rgb,.5) ⑤종료 팝업 `#yexit`(글래스 카드·아니오=armed 주의색·예=뉴트럴·X SVG·바깥클릭·history 가드 재푸시 = 매번 무조건) ⑥`ySwipeBind` — 메인 char↔list↔map↔설정시트 · 챗/시트 = 가로 스와이프 백(입력칸·패턴패드 제외·가로우세 1.6x+64px) ⑦지도 차량 = YMAP_RING 폐곡선 폐지 → `YMAP_LANES` 4경로(손그림 근사 트레이스 · dir별 색 = 상행 파랑 2톤/하행 다크레드 2톤 · 완주 = 3~10s 랜덤 휴지 재출발 = 간헐 · 's' 정점 = 광장 정지선 · 'h' 구간 = 건물 뒤 페이드 은폐[단일 래스터 지도 = z 분할 불가 → 은폐 연출·좌표는 검수 교정 가능]).
- **효과/리스크**: 잠금 = 게임적 프로그레션 완성(만난 사람만 대화·이름 노출). 리스크 = 차선·가림 좌표는 손그림 근사(운영자 검수로 정점 교정) · 미대면 판정은 클라 UI 차단(무인증 공개 게이트웨이 전제 = 기존 윈터 잠금과 동일 보안 수준) · iOS 브라우저 자체 엣지 스와이프와 병존.
- 검증: 자가검증 경로=헤드리스 E2E 24항목 전원 PASS{탭만으로 언락 실측(A4)·탭 축적 유지·오답 bad(타임라인 계측)·미대면 9인 ???·디테일 CTA 차단·지도 unmet 9/이름표 ???·대화중 rgba(62,224,200,.5) 실측·시즌 박스 0·글로우·ytopfade·종료팝업 5종(색분리·닫기 3경로·가드 복구)·스와이프 2방향} + 차량 4대 별도 실측(방향색·이동·간헐 스폰·에러 0) + check_refs rc=0 + node --check + PR #209 병합충돌 -X ours 흡수 시 main분(#204 대화목록 워시·시즌 테두리) 보존 실측. 롤백=_versions/260709_0813~/260709_*_패턴잠금_탭입력/지도차선.

## 149차 — 캐릭터 탭 재배열(내 정보 도킹·상태별 그룹 정렬) + 잠금 우회로 봉인 + 윈터 스레드 삭제 (운영자 260709)
- **경위**: 운영자 재배열 지시 — 맨 위 ①내 정보(대화 목록의 그 행) ②대화 중인 상대 ③대화 불가(만났지만 불가 → 만나지도 않음 순) · 잠긴 상대 행 = 캐릭터 설명 대신 "내 상황" 서술 · 리스트 위 요소 불투명("선택하는 부분만 어둡다") · "윈터 잠겼는데 대화 가능" 버그 = 대화 삭제 요청.
- **판정**: 윈터 대화 가능 = 챗 좌레일 `yThreadSwitch`가 잠금 가드 없이 draw 직행(yEnterChat 가드 우회로) + 기존 윈터 스레드 존치. 초대 픽커도 미대면 필터 부재.
- **수정(viewer/index.html · 신규 :root 0)**: ①`yMeDock` 신설 — #ymeRow+#ymePop 단일 인스턴스를 char/list 탭 따라 맨 위 도킹(복제 없음 = SSOT·클릭 = document 위임이라 이동 무해) + yCharRender가 yMeRow() 갱신 ②그룹 정렬 `grp` — 대화 중(0)→열림·미시작(1)→LOCKED(2)→미대면(3) · 그룹 내 기존 랭크(보이스→시즌→IDOL) 안정 유지 ③잠긴 행 desc = "만났지만, 아직 대화할 수 없는 상대"/"아직 만나지 못한 상대 — 어디선가 스쳐 지나갔을지도" · quote 미표시(정체 스포 차단 겸) ④`.ymerow` 배경 = 라임 틴트 + rgba(18,18,18,.78) 불투명 베이스 ⑤우회로 봉인 — yThreadSwitch 가드(토스트)·좌레일 잠금/미대면 제외·초대 픽커 yUnmet 필터. ⑥라이브 winter 스레드 삭제 = op reset {t:'winter'}(운영자 지시 · threads 실측 = winter 소멸·5방 잔존).
- **효과/리스크**: 캐릭터 탭 = 상태 위계 한눈(나→진행→가능→잠금→미지) · 잠금 우회 경로 3종 봉인. 리스크 = 도킹은 DOM 이동이라 list 진입 전 char에서 팝오버 위치 앵커 동일(.ychar/.ylist 둘 다 absolute 컨테이너) · 잠긴 행 문구는 콘텐츠 축(운영자 취향 조정 여지).
- 검증: 자가검증 경로=실데이터(운영자 세션 스켈레톤) E2E — 정렬 순서 "하은,루시,무디,데스크,류,윤,윈터,???×5" 실측·잠긴 행 desc/quote·ymerow rgba(18,18,18,.78)·도킹 왕복(char↔list 부모 이동+팝오버 동작)·좌레일 필터·yThreadSwitch(winter)=false 차단 + reset 후 get 실측(threads 5방) + check_refs rc=0 + node --check. 롤백=_versions/260709_*_캐릭터탭_내정보도킹.

## 150차 — 원거리 소통 축(장소 재개 제한·전화만 모드·헤더 쌍 픽토) + 설정 개편(타이틀 네비·매우 빠름·캐시 초기화 플로우) (운영자 260709 연속 지시)
- **경위**: 운영자 연속 지시(휘발 방지 분할 전송) — ①수화기 자물쇠 오버레이 삭제(픽토 유지·비활성 = mut만) ②대화 끊긴 지 오래 + 상대가 지금 내 장소에 없으면 대화 불가, 단 페르소나·친밀도 따라 원거리 소통 가능·전화 배선자는 전화로 ③헤더 = 원거리 픽토+전화 픽토 쌍 ④연락처 등록 = 헤더 → 프로필(디테일)로, 글자 대신 그 픽토그램(캐릭터별 내용 자리 확보) ⑤설정에 시즌 헤더 비침 해결 ⑥설정 = 타이틀 네비 형식 계승(프로필 항상) ⑦소리진동 기본 ON + 계정 설정 전체 기본값 리셋 ⑧대화 속도 "매우 빠름" = 경고 팝업(취소/확인) 후 Sonnet 5 자동 ⑨캐시 비우기 = 경고+동의 체크 → 게이지바 → 완료 체크 → 로컬 전체 초기화(로컬 저장 개념).
- **수정(viewer/index.html + call.js · 신규 :root 0)**: ①call.js `.ycall-lock` 3규칙+오버레이 마크업 삭제(nophone = mut만) ②`yAway`(마지막 턴 2h+ && 상대 동선 장소 ≠ 내 장소[수동 설정 우선·미설정 = cur 상대와 같은 공간 파생]) · `yFarNeed/yFarOK`(필요 친밀도 = 6+경계심×1.2−초기친밀도×0.6 · 실측 = 내 발화 수) · 가드 = yEnterChat·yThreadSwitch(차단 = 토스트+미진입 / phone 배선 = `_yFarOnly` 전화만 모드) · `yFarApply`(입력행 disabled+"멀리 있어 — 전화로 걸어봐" · PTT 무전 = 원거리 축이라 존치) · ySend 가드 · 닫힘 리셋 ③call.js `#yetaFarBtn`(전파 방사 픽토 · 수화기 왼쪽 = 옛 연락처 자리 · `.nofar` = mut · 탭 = 상태 안내) + callBtnSync 쌍 동기 ④헤더 `#yetaVcfBtn` 폐지 · 디테일 `.ycd-sub` = 픽토그램-온리 원형 44px(aria/title 라벨 · 자동 명함 시트 존치) ⑤`.ymain.cd-open`에 `.yssn-fixed`·`#ytopfade` visibility:hidden 추가(반투명 시트 뒤 비침 차단) ⑥`cd-nav` 클래스 — 설정 시트 한정 로고·프로필 z:7 상주 + 시트 상단 패딩 78 ⑦haptic 기본 true + `yeta_set_v2` 1회 마이그레이션(pref·dial·fs·font 저장분 소거 = 새 기본 일괄 적용 · me/튠/읽음 보존) ⑧`yConfirm` 범용 확인 팝업(yexit 골격 계승 · 제목/본문/동의체크/확인콜백) — 매우 빠름: max 다음 칸 진입 시 "모델이 변경되어 대화 품질이 저하될 수 있습니다" 취소/확인 · 확인 = sonnet+low+vfast 결합(표시 "매우 빠름" 2강조 · 모델 수동 변경 = 결합 자동 해제) ⑨캐시 비우기 = 경고문(운영자 문구)+체크 게이트(미체크 = 예 비활성) → 게이지바(scaleX .9s = 전송 버튼 gauge 결 간이판) → 완료 체크(CHECK_SVG) → localStorage yeta_* 전체 소거+reload.
- **효과/리스크**: 위치 시스템에 게임 루프 의미 부여(오래 끊기면 찾아가거나 전화) · 설정 = 브랜드 네비 일관. 리스크 = YAWAY_MS 2h·친밀도 공식 = 주변부 노브(운영자 감각 조정 여지) · 게이지바 = nomute 정본 이식 전 간이판 · vfast는 표시 결합(서버 페이로드 = sonnet×low 그대로).
- 검증: 자가검증 경로=E2E 2벌 — 설정 14항목(v2 리셋·haptic ON·비침 hidden·cd-nav 로고/프로필 상주·패딩 78·매우빠름 팝업/취소 유지/확인 = Sonnet 5 자동/재탭 복귀·캐시 체크 게이트/게이지/완료/리로드 후 사용자 키 0) + 원거리 5항목(무디 멀리·폰X = 차단+장소 안내 / 하은 멀리·폰O = 전화만[입력 차단+플레이스홀더] / far 픽토 nofar / 루시 cur = 통과 / 연락처 픽토 온리) 전원 PASS·에러 0 + check_refs rc=0 + node --check(index·call). 롤백=_versions/260709_*_전화락제거_장소재개제한.

## 151차 — 지도 도로 실측 재트레이스(도로 이탈 0)·구역 배회 + 정책 L0 전면 하드화 + 대화중 가독·장소 노출 규칙·메타 타이포 토큰 (운영자 260709 연속)
- **경위**: 운영자 — ①차가 건물 뚫고 다님: 도로 레이어만 따로 라인(좌우 각 1차선·위 = 우측·도로 준수 최우선·건물 뒤 가림) ②장소 구역 구획 + 사람은 구역 안 자연 배회(건물 못 올라탐)·이름 태그 = 구역 원 위 ③정책 재편: L0 코어 최상단·메타발화/헛소리방지 = "품질 존립" 절대선 승격(웹 토글 폐지·깃 파일 분류 유지)·L1 시즌 = 관리자단·L2 = 열린 캐릭터 픽커 → 기존 성향 창 ④버전 = RO(기존 충족) ⑤매우 빠름 이탈 시 Opus 자동 복귀 ⑥"대화 중" 행 워시 = 글자 가림(류처럼) ⑦장소 = 같은 장소/대화 중일 때만 노출(친밀도 무관) ⑧"N분 전·장소" = 볼드 해제·75%·기틀 토큰.
- **판정·방법**: 도로 = 낮 지도 점선·횡단보도 픽셀 검출 + 5% 그리드 오버레이 판독 + 차선 렌더 시각 QA 5라운드(1차 손그림 근사의 건물 관통을 교차로 앵커 기준 재트레이스). 첫 행 시즌 라인 침범 = 웹폰트 로드 후 --ssn-h 미재측.
- **수정**: ①`YMAP_ROADS` 5중심선(westBr·plazaBr·south·northSw·eastSe — 교차로·다리·광장 실측) + `yLaneOf`(진행방향 우측 1.6% 차선 자동 산출 — 역주행 = 반대 차선) · 편성 6대(상행 파랑 3·하행 레드 3 · 간헐 유지 · 's' 광장 정지선 · 'h' 메커니즘 존치[현 0 — 지도가 도로 노출 구도]) ②`YMAP_ZONE`(광장 5.5·놀이터 5·시장 3.5% 실측 공터) — 야외 = 존 내 랜덤 목표 완보(8px/s·도달 시 1.5~4s 서성) · 태그 = `#ymTags` 분리 레이어(구역 원 위 스택 고정·실내 = 건물 위 유지·탭 = 대화 진입) · 구역 = 점선 링 ③policy.json v3 — L0 4그룹 전면 hard(절대선 존엄/품질 존립·메타 차단/품질 존립·헛소리 방지/출력 계약 — 구분 유지)·toggle 제거(러너 = isinstance dict 가드로 자동 스킵 = 무수정) · 뷰어 = L0 최상단(전원 RO 픽토)→L1→L2 캐릭터 픽커(yShowSet 연결) · CLAUDE.md §🎚 동기 ④vfast 이탈 = opus 복원 ⑤.ycrow.talking 워시 폐지 ⑥yLocOf 개정 — 노출 = together(2h 내)или 같은 장소(내 위치 = 수동/cur 곁)·턴수 기준 폐지 ⑦`--fs-meta:8.25px` 토큰 신설(fs-xs 75% · 운영자 직접 승인 = lock 등재·거울 재빌드) — .ylist-meta/.ylist-loc/.ylist-ago/.ylr-meta/.yme-sub 통일·볼드 해제 ⑧시즌 헤더 = document.fonts.ready 재실측(첫 행 침범 픽스).
- **효과/리스크**: 차량 = 실측 도로만 주행·구역 배회 = 동네 활동감. 리스크 = 도로 ±1% 잔차(차폭 흡수·정점 교정 용이) · 가림 'h' 구간 = 현 0(운영자 검수로 마킹) · 8.25px = 소형 폰 가독 하한(운영자 감각 조정 여지).
- 검증: 자가검증 경로=차선 렌더 시각 QA 5라운드(건물 관통 0 확인) + E2E 12항목 전원 PASS{차 6대·방향색 3/3·구역 원 3·태그 12 분리·무디 존 배회(존 내 이동 실측)·차 seg 진행·L0 최상단·토글 칩 0/하드 4그룹·L2 픽커(잠금·미대면 제외)·픽커 탭 = ycset·워시 투명·장소 노출 규칙(cur 찻집·together 시장·나머지 알 수 없음)·메타 8.25px/400} + check_refs rc=0(토큰 락 56개) + node --check. 롤백=_versions/260709_*_정책재편_약관_지도도로.

## 152차 — 관계 계층 완성(친밀도 감쇠 "만났지만 대화는 어려움")·심리 3태·앱 배지·직업 플레인·프로필 오버레이·지도 가림·전체 대화 리셋 (운영자 260709 연속)
- **경위**: 운영자 — ①친밀도 = 대화 안 하면 지속 감소(알고리즘 재량), 임계 미만 = 재대화 불가 "만났지만 대화는 어려움"(아직 못 만남 위) + 관계 전체 계층화·정렬 정리 ②프로필 탭 = 이동 말고 현재 창 위 오버레이 ③스크롤 = 강조 20% + 끝단 도달 시 삼각형 ④심리 줄 3태(정확 = 잘 보이는 색 / 읽고 1h 방치 = "(심리 상태 알 수 없음)" mut 50 / 못 읽음 = mut 100) ⑤직업 = mut 플레인(도형 제거·간격만·크기 유지) ⑥안읽음 = 아바타 우상단 앱 배지(숫자 강조·블랙 글래스 75) ⑦"알 수 없음" → "장소 모름" ⑧시즌 헤더 수직 중앙 통일 ⑨차 건물 관통·가림 재보고 → 가림 h 실측 마킹 + 시장 존 통로 축소 ⑩로직 변경 꼬임 방지 = 전체 대화 초기화 ⑪난입 단톡 = 1:1 방 분리(후속 설계 접수).
- **수정(viewer/index.html)**: ①`yBond`(내 발화 수 × 0.5^(일/7) 반감기)·`yEstranged`(대화 이력 있고 bond<1.5) — grp 5단{대화중→가능→소원→LOCKED→미대면} · 행 = estr(반실루엣 아바타·pill "만났지만 대화는 어려움"·상황 desc) · 가드 = yEnterChat·yThreadSwitch·디테일 CTA(회복 = 마주침·난입 재회 축) ②`cd-keep` — 프로필 열림 = 뒤 탭 패널·시즌 헤더 유지(반투명 시트 부상 · 닫힘 회수) ③스크롤바 = rgba(--accent-rgb,.2)(hover .34) + `::-webkit-scrollbar-button` 삼각 mask(색 = 토큰) + 전역 위임 scroll 리스너(.sc-top/.sc-bot = 끝단 착색) ④심리 3태 — thtFresh(1h 내·답 대기·안 읽음) = --think 100 / off = "(심리 상태 알 수 없음)" mut+op.5 / na = mut+op1 ⑤.yjob = 필·테두리 폐지·mut(변신 .tq = 터콰이즈 텍스트만) ⑥.ylist-badge = 아바타 우상단 absolute·rgba(18,18,18,.75)+blur 글래스·숫자 --accent(구 라임 링·이름줄 배지 폐지) ⑦yLocOf '장소 모름' ⑧.yssn-hd 자식 line-height:1(레몬밀크·한글·날짜 광학 중앙 일치) ⑨northSw 카페 뒤 2세그·eastSe 연습실 뒤 1세그 'h' 마킹(가려졌다 다시 보임) · market 존 (35,70.5,2) 통로 축소(파라솔 위 배회 차단). ⑩라이브 op reset 전체(threads 0 실측 · me·policy 승계) — 옛 로직 방(윈터 잔존·쿼터 유출 턴·난입 단톡) 일괄 소거.
- **효과/리스크**: 관계 = 시간이 흐르는 동네(방치 = 소원 → 재회로 회복) · 리스크 = 감쇠 임계 1.5·반감 7일 = 감각 노브 · 소원 회복은 현재 난입·마주침 재회 이벤트에 의존(러너 축 후속) · 난입 단톡 1:1 분리 = 후속 과제(러너·게이트웨이).
- 검증: 자가검증 경로=E2E 11항목 전원 PASS{정렬 하은·루시→무디(소원)→윈터→??? · 소원 pill/estr/전환 차단 · 심리 터콰이즈100/알수없음 mut50/모르겠다 mut100 · 앱 배지(absolute·강조 숫자·.75 글래스) · 직업 플레인(도형 0·11px) · cd-keep 온/오프 · 장소 모름 · sc-bot} + reset 후 get 실측 + check_refs rc=0 + node --check. 롤백=_versions/260709_*_관계계층_프로필오버레이_스크롤.

## 153차 — 공간 초대(같은 장소 pill)·대화 시작 게이지+줌 전환·화자 이름 흰색·실루엣 완화·L1 배지 제거 (운영자 260709 연속)
- **경위**: 운영자 — ①헤더 사람+ 픽토 = "초대는 일단 빼자. 난입하거나 지금 장소에 같이 있는 사람이 뜨게 해서 누르면 부르기(메타버스니까)" ②대화 시작 = "로딩 픽토가 게이지바에 쭉 뜨면서 열리는 느낌 · 순간 이징되며 화면 확대 전환" ③챗 화자 이름(루시 등) = 흰색 잘 보이게 ④"캐릭터들 다 그림자로만 보인다"(전체 리셋 직후 전원 미대면 = 실루엣 톤 과함) ⑤L1 요약 "(4)" 카운트 = 불필요.
- **수정(viewer/index.html)**: ①헤더 `#yetaInvPlus` 폐지·`yInvToggle` 닫기 전용 스텁 — `yNearCalc()` 신설: 현재 방 상대와 같은 장소(동선 `ymPlaceOf` 판정)의 다른 캐릭터 감지(만석·초대 대기 10분·잠금·미대면·소원·사적 공간 제외) → 로그 하단 pill "{장소} — {이름}도 여기 있다 · 불러볼까"(.ynear = ykick 소멸형 결 계승) · 탭 = 기존 op `invite` 그대로(수락/거절 = 캐릭터·러너 무수정) · 발사 = yEnterChat·yThreadSwitch ②`.ycd-cta.go::after` = ycfGauge .38s 게이지(라임 30% 칠) → 380ms 후 전환 · `ydlgIn` 키프레임에 scale(.96) 확대 이징 ③`.ymsg .yname` = mut→fg+볼드 ④실루엣 brightness .3/.25→.5(잠금 리스트·지도 미대면 워커 — ??? 은폐 유지·형체 판독) ⑤L1 summary `.ys-cnt` 제거(+사산 dcount 정리 · 성향 시트 그룹 배지는 별개 존치).
- **효과/리스크**: 초대 = 도구 패널 → 세계관 사건(같은 공간 우연)으로 — 메타버스 결 정합 · 리스크 = 후보 감지가 뷰어 동선 판정 의존(러너 barge와 독립 축·초대 자체는 기존 파이프) · 게이지 380ms = 체감 노브.
- 검증: 자가검증 경로=E2E 21항목 전원 PASS{InvPlus 부재·ydlgIn scale·워커/아바타 .5·CTA go 게이지(ycfGauge·라임 30%·창 지연)→380ms 후 열림·yname 흰볼드·pill 노출/문구/결·탭=invite(t·persona) 실측·발사 후 소멸·대기/미대면/만석/사적 4중 차단·양성 재확인·L1 배지 0·L2 픽커 생존·에러 0} + check_refs rc=0. 롤백=_versions/260709_1732_공간초대_CTA게이지.

## 154차 — 약관 15조+열람 시트·??? 강조색·프로필 비침 픽스·내정보 개편·윈터 미대면 통일·상태 스키마(DB 테이블)·분신술 10앵글 findings 일괄 수정 (운영자 260709 연속)
- **경위(운영자)**: ①이용약관 15개조 = "OPUS 두 명한테 만들게 시켜"(법령체) ②"???은 강조색으로" ③"동시 접속하면 캐릭터 페이지 바로 안 뜨고 목업" 재보고 + "캐릭터·차 적용 안 된 듯" ④"프로필 누르면 뒤에가 투명하게 보임" ⑤방문자 배지 = 나 옆에 · 호칭 지정 시 '나' 대체 · 감정 = 다양한 상황+이모지 ⑥"윈터 같은 특수캐릭터도 IDOL · 동일하게 안 만났으면 안 만난 거로" ⑦"계층화·상태 유동 적용 = DB 테이블 다듬기" + 분신술 10앵글(opus×max·READ-ONLY) 완주 findings.
- **판정(실측)**: ③의 "차·캐릭터 미적용" = **라이브가 origin/main과 바이트 일치**(curl 대조 · 도로·실루엣·게이지 전부 배포됨) → 운영자 화면 = 열려 있던 옛 탭/캐시 판정(스크린샷에 리셋 전 "무디" 실명 = 확증). 목업 빈 화면 = 분신 cold 앵글이 근본원인 확정{openYeta가 YCHARS 선로딩 시 로더 분기를 스킵 → `yLoad`의 get 왕복 내내 빈 로그 + 'Y·YETA' 폴백 헤더 · 동시 접속 = 왕복 수초로 관측}.
- **수정(운영자분)**: ①약관 = Opus 2본 독립 초안 → 종합(B 골격{위기 안내 109·119·전문조언 부인·민감정보 입력 금지·지재권 배분} + A 이식{시즌제·진단 메타데이터 우선}) → `YTERMS` 15개조 + `#ycterms` 열람 시트(설정 "이용약관 · 문의" 행 = act 행 전환 · ycset 스택 관례·뒤로 = 약관만 닫힘) ②??? = `--accent`(리스트 `.ynm-t.qm`·디테일/픽커 `.yqm`·지도 태그 `.ymp-tag.unmet text` · 잠금 행 옅김 해제) ③cold 픽스 = get 대기 무조건 로더 + 딥링크 unlocked 게이트(`_yDeepGo` 큐잉) ④프로필 = `cd-keep` 시트에 blur(--blur-m)+음영 .55(뒤 글자 뭉갬·오버레이 느낌 유지) ⑤내정보 = 방문자 pill 이름 옆 인라인(전용줄 폐지)·호칭 반영(기존 배선 확인)·`YME_FEEL` 10종 감정(이모지+라벨 · 팝오버 칩 확장 · 무대 무드 Y_MOODS와 분리) ⑥윈터 = `yUnmet` locked 제외 삭제(미대면 판정 통일 — 이름 ???·실루엣·지도·픽커 자동) · `yLockLabel` 'LOCKED'→'아직 못 만남' · 정렬 최하단·IDOL 배지 노출(티저) 유지 ⑦roster.json 정규화(met·locked·grade 전원 명시) + `characters/SCHEMA.md` 신설(컬럼·계층 5단·등급·유동 레시피 — CLAUDE.md §🗺 등재).
- **수정(분신술 findings)**: 패턴{오답 잔상 1400ms 타이머 patT 추적 = 새 입력 보호 · patSeq 세대 가드 = 동일 패턴 이중 판정 차단 · patEnd 유령선} · 관계{yEstranged 방치 1일 가드 = 1메시지 방 즉시 소원 즉사 버그 · 좌레일 소원 제외 · 목록 estr 행 시각+심리줄} · far{yFarRecalc(챗 열린 채 전이 재판정)+분침 타이머(목록 매분 노화·_yNearC 재계산 — 후보 변화 시에만 재렌더) · far 플레이스홀더 전화 미배선 분기} · 설정{haptic 소비자 디폴트 true 정합 · yConfirm 백스택 편입(navOpen — 뒤로가 팝업 대신 뒤 시트 닫던 꼬임)+진행 타이머 레지스트리(_ycfT = 게이지 중 닫기 취소·재오픈 소거) · 설정 닫힘 = 네비 하이라이트 재동기 · 종료 '예' 이탈 실패 = 래치 해제+가드 재무장} · 지도{정지선 역점프(지난 차 = 통과) · stopT 0 클램프 · northSw 카페 뒤 'h' 2세그 복원(원장 152차 약속분 드리프트) · eastSe 고아 's' 강등 · 태그 붐빔 .meet 배선} · 보안{esc에 ' 추가 · 게이트웨이 draw/invite locked 403 · 러너 barge names locked 제외 + invite 판정 locked 스킵} · 정리{ys-cnt 사산 블록·옛 우측 컬럼 CSS(.ylist-r/.ylist-t/.ylr-meta) 삭제 · yDraw 재활성 대비 계층 가드 · 러너 L0 v3 주석 동기}.
- **효과/리스크**: 목업 빈 화면 = 로더로 대체(원인 봉인) · 관계 즉사 해소 = 신규 유저 첫 대화 보호 · LOCKED = 서버·러너 3중 방어. 리스크 = 프로필 블러(폰 GPU 1장 추가 — 기존 글래스 결 수준) · 약관 문구 = 법률 검토 아님(개인 실험 서비스 고지 중심) · 감정 세트 = 로컬 표시 전용(러너 미전송).
- 검증: 자가검증 경로=E2E 5벌 69항목 전원 PASS{batch2 20(윈터 ???/IDOL/최하단·잔상 생존·이중판정 차단·내정보 3종·estr 목록·지도 unmet 9) + batch3 10(콜드 로더·프로필 블러 .55·yConfirm 백스택·네비 재동기·종료 래치) + near 21 + terms 7(15개조·스택) + rel 11 회귀} + check_refs rc=0 + bash -n·gateway node --check + 러너 locked 로직 파이썬 재현(names 11·winter 제외) + 라이브=main 바이트 대조. 롤백=_versions/260709_1739_약관시트(+roster).

## 155차 — 약관 켠 채 탭 이동 버그 = 시트 목록 SSOT·navHome 백스택 청소·퇴장 재열기 가드 (운영자 260709 "설계 제대로 됐는지 확인")
- **경위**: 운영자 스샷 — 설정→이용약관 열람 켠 채 하단 네비 탭 이동 시 약관 시트 잔존(+타이틀 네비 소멸) + "갈수록 이런 버그 많다 · 설계 확인" → 화면 전환 설계 전수 감사.
- **판정(설계 결함 3겹 실측)**: ①시트 회수 목록 이중 하드코딩 — yTab 4종 vs yCdOpenSync 5종(260709 약관 신설 때 yCdOpenSync만 갱신 = 드리프트) → 탭 전환이 #ycterms 안 닫음(.ycd = fixed 전화면 z:6 · 네비 z:7 "탭 = 시트 회수" 설계 의도 파탄) ②yTab = 시트 DOM만 숨기고 _navStack·history 방치 → 유령 등재 누적 = 이후 하드웨어 뒤로가기 N회 무반응(ymepop·ylmenu도 동류) + 네비 설정 재진입 시 스택 중복 적재([설정,약관,설정]) ③yCdHide 230ms 타이머 = 무조건 remove('on') → 퇴장 애니 중 재열기(설정 연타) 시 방금 연 시트 증발.
- **수정(viewer/index.html · 5지점)**: ①`YCD_SHEETS` 상수 = 시트 목록 SSOT(yTab·yCdOpenSync 공용 · 신설 = 1곳만) ②`navHome()` 신설 = 탭 점프 시 전 레이어 close(LIFO 역순)+스택 비움 — history 칸은 의도적 잔존{go(-n) 회수안 = 비동기·pushState 경합으로 연타 시 앱 밖 이탈(about:blank) 실측 → 폐기 · 잔존 칸 = popstate 골격 "스택 빔 = 종료팝업+가드 재푸시"가 흡수} ③yTab·yShowSettings(직진입만 · navSwap 경유 noNav = 스킵)에 배선 ④yCdHide 타이머 out 가드 ⑤시트 열기 5곳 remove('out').
- **효과/리스크**: 탭 전환 = 항상 깨끗한 홈 레벨(시트·팝업·백스택 정합) · 시트 신설 드리프트 구조 봉인. 리스크 = 탭 점프 후 히스토리 칸 잔존(뒤로 = 종료팝업 UX 동일 · PWA 무해 · yExitYes go(-2) 이탈 부정확은 기존 래치 폴백이 수용).
- 검증: 자가검증 경로=Playwright 실브라우저 16항목 전원 PASS{스샷 경로(설정→약관→탭) 회수·cd-open 회수·백스택 0·뒤로 1회 = 곧장 종료팝업·약관서 뒤로 = 설정 유지·설정 재탭 스택 단일·연타 레이스 시트 생존·navSwap 교체 보존·페이지 에러 0} + go(-n) 1안 이탈 실측 적발 후 재설계 + check_refs rc=0 + 전후 스크린샷. 롤백=_versions/260709_2238_약관탭전환버그. (적대감사 2인 = 운영자 지시로 평결 대기 생략 머지)

## 156차 — 도로 v2 전면 재트레이스(차도 준수 확정)·설정 개편(Setting·L0/L1/L2)·약관 X 상주+YETA 명칭·챗 헤더 불투명·지도 스폰·타이포 위계 6단 기틀 (운영자 260709 연속 · 병렬 155차와 수동 병합)
- **경위(운영자)**: ①"아직 차들이 차도로 안 다니고 아예 수행이 안 돼 — 이유 확인·어려우면 알림" ②지도 열면 전원 같은 곳에서 시작 ③챗 헤더 = 뒤에 밝은 버블 오면 안 보임 → 불투명도 상향(글래스 유지) ④설정: 타이틀 = 픽토+Setting · 부제·하단 노트·축 힌트 = 세부 설명 전면 폐지 · 그룹 = "L0 설정/L1 설정/L2 설정" · L0~L2 글래스 박스 + 내부 위계 통일 · L0 로딩 지연 해소(프리로딩) ⑤약관: 스크롤 내려도 상단 닫기 상주(nomute .tool-x 계승·강조색) · "제타" 표기 → YETA(예타) + 범용 문구 손질 ⑥"타이틀·부제·대주제·중주제·소주제·내용 위계를 기틀로 정해야" — 6단 확정.
- **판정(① 실측)**: 밤 지도에 현행 차선 투영 = **구판 트레이스 자체가 실그림 도로와 어긋난 불량**(건물·언덕·학교 관통 — 운영자 정당 · 구 "이탈 0" 실측은 차↔중심선 거리라 순환논증이었음을 자인). 지도 = 아이소메트릭 격자(기울기 ±0.5 직선)인데 구판은 곡선 근사.
- **수정(도로 v2)**: 4분면 그리드 크롭 판독 + 점선·횡단보도 클러스터 검출 실측 스냅 + 오버레이 QA 4라운드 → `YMAP_ROADS` 8간선 신설{nMain·bridge(개천 다리)·nShop('s' 광장 정지선)·sMain·seOut·sEdge·tea·swOut(보행 2선 = 차 미배정)} · 광장 = 차 금지 마름모(N50,39.5·W41,45.3·E59.5,45·S50.5,51.5) · 차량 6대 재편성(상행 bridge/sMain/sEdge · 하행 nMain/seOut/nShop) · 광장 존 (50.3,45.4,r4.4)·놀이터 존 (56,90.5,r4) 도로 비침범 교정 · 신호등 = 광장 N 정지선 옆 이동 · 'h' = 0 리셋(신규 트레이스 — 운영자 검수 축).
- **수정(나머지)**: ②워커 생성 opacity 0 → 첫 판정(ymPlaceAll) 후 표시 = 중앙(250,250) 뭉침 프레임 소멸 ③`.yeta-h` 밑칠 = 글래스 그라디언트 + rgba(18,18,18,.75)(blur·엣지·틴트 유지) ④설정 = `[기어]Setting` 타이틀·부제/노트/힌트/게스트 설명 전면 삭제·summary "L0/L1/L2 설정"·L1 축들과 L2 픽커도 `.ypol-grp/.ypol-gh` 골격(전 계층 동일 위계)·**정책 프리페치**(부팅 1회 = 설정 진입 즉시 렌더·로더 0) ⑤약관 = `#yctermsX`(.tool-x 정본 계승 · 강조색 변형 · sticky · 열림 중 cd-nav 회수 = 상주 프로필이 X 덮던 것 E2E 적발·교정) · 서비스명 "말벗 제타"→"YETA(예타)"·제15조 창구 손질 ⑥타이포 위계 6단 표 = CII 등재(타이틀 h2/fw-x → 부제 xs·mut → 대주제 label/fw-b → 중주제 sm/fw-x → 소주제 gh → 내용 mut — 전부 기존 토큰 = 신규 0 · 설정 시트가 정본 실물).
- **효과/리스크**: 차량 = 실그림 도로 완전 준수(실렌더 스크린샷 판독 = 관통 0 + 세그 거리 실측 0.00) · 설정 = 즉시 렌더·위계 일관. 리스크 = 도로 잔차 ±1(차폭 흡수) · 'h' 가림 재마킹 = 운영자 검수 후 · tea/swOut 보행선은 차 미배정이라 시각 검증 대상 아님.
- 검증: 자가검증 경로=오버레이 QA 4라운드(v1 불량 적발 → v4 점선·클러스터 정합) + 실브라우저 지도 스크린샷 판독(차 6대 전부 도로 위) + E2E set2 15항목{프리페치 1회·즉시 렌더·Setting 타이틀·L0/L1/L2·힌트 0·위계 grp/gh 6·약관 X sticky/강조/스크롤 후 히트/YETA 명칭·스폰 중앙 노출 0·차선 이탈 0.00·존 교정} + 회귀 4벌(batch2 20·batch3 10·near 21·rel 11) + terms 7 + check_refs rc=0. 롤백=_versions/260709_*_도로v2_설정개편.

## 157차 — call.js 오버레이(벨·명함) 본체 백스택 편입 = popstate 이중 발화 봉인 (운영자 260709 "응 머지해" · 병렬 156차 도로v2와 수동 병합 = 리넘버)
- **경위**: 155차 적대감사 발견 [중] 2건 — call.js가 navOpen 미등재 + 자체 window popstate 리스너 2개 → 하드웨어 뒤로 1회에 본체 종료가드와 병렬 발화{홈 벨 = 거절+종료팝업 동시 · 챗 위 명함 = 본체가 뒤 챗(ydlg) pop = 명함+챗 동반 종료}. 운영자 승인으로 즉시 수정.
- **수정(viewer/call.js)**: ①자체 popstate 리스너 2개 폐지 ②벨 = showModal 후 `navOpen('ycall', decline)` 등재(챗 위 벨 = [ydlg,ycall] 적층 = 뒤로 1회에 벨만) · 명함 = `navOpen('yvcf', close)` ③닫기 전 경로 백스택 위임 — `ycallNavClose`/`yvcfNavClose` 헬퍼{스택 등재 시 navBack → popstate 단일 경로 · navOpen 부재(독립 배포) = 직접 닫기 폴백} · 치환 = 끊기 버튼·ESC cancel·부재중 타임아웃·재생종료 1.2s·무음 5s·webAccept fail·call-end·[다음에]·가이드 [확인].
- **효과/리스크**: 통화·명함이 시트들과 동일한 레이어 규율(뒤로 = 최상단 1개) 편입 — 모듈 계약(훅 3종·본체 무수정 주입)은 불변. 리스크 = navBack 경유 닫기 = popstate 왕복 수십 ms 지연(체감 없음 실측).
- 검증: 자가검증 경로=Playwright 실브라우저 16항목 전원 PASS{벨 등재·홈 벨 뒤로 = 거절만+종료팝업 미발화·거절 버튼 스택 청소+직후 뒤로 = 곧장 종료팝업·명함 [설정,yvcf] 적층·명함 뒤로 = 명함만(뒤 레이어 생존)·[다음에] 정합·155차 navHome 회귀·JS 에러 0} + node --check + check_refs rc=0. 롤백=_versions/260709_2327_콜모듈백스택편입.

## 158차 — 종료 이탈 = ybase 앵커 연쇄 back(go(-2) 폐지)·챗 ESC 백스택 경유·yNavName 죽은 코드 삭제 (운영자 260709 "덤 결함들 해결해주셈")
- **경위**: 155차 적대감사 잔여 3건 — [중] yExitYes `go(-2)` = [base,guard] 2칸 고정 가정 → navHome 잔존 칸 누적 시 브라우저 탭서 "예"가 앱 내부 착지 = 조용히 실패(래치 재무장 폴백만) · [하] 챗 ESC(데스크톱) = cancel 핸들러 부재 → 네이티브 close가 ydlg history 칸 고아화 · [하] yNavName = 폐지된 `data-tab="chat"` 셀렉터 참조 = 항상 null 무동작(정의+호출 5곳 사산).
- **수정(viewer/index.html)**: ①로드 직후 `history.replaceState({ybase:1})` = 앱 첫 칸 앵커 → popstate에 `_yExiting` 연쇄 분기(스택 체크보다 선행) = ybase 도달까지 back 반복 · 도달 = 최종 go(-1)로 앱 밖 — 잔존 칸 수 무관 · yExitYes = go(-2) → back 시동(PWA window.close 우선·1.4s 래치 재무장 폴백 불변) ②`yetadlg` cancel = preventDefault + navBack('ydlg'){400ms 래치 = 연타 이중 back 방지 · _yDlgClosing 가드 — yedit·calldlg cancel 결} ③yNavName 정의+호출 5곳(방 나가기·초기화·언락 동기화·openYeta·목록) 삭제.
- **효과/리스크**: 종료 "예" = 어떤 잔존 상태서도 확실 이탈 · ESC 칸 정합 · 사산 코드 0. 리스크 = 연쇄 back = popstate 왕복 칸당 수십 ms(10칸도 <0.5s · 래치 1.4s 내) · ybase = 리로드/딥링크 시 그 칸에 재마킹(의미 동일 = 앱 첫 칸).
- 검증: 자가검증 경로=Playwright 실브라우저 8항목 전원 PASS{잔존 2칸 + "예" = about:blank 이탈(구 go(-2) 실패 케이스) · 잔존 0 회귀 이탈 · ESC = 챗 닫힘+스택 청소+직후 뒤로 1회 = 곧장 종료팝업 · JS 에러 0} + nav 16·call 16 회귀 전원 PASS + check_refs rc=0. 롤백=_versions/260709_2335_종료이탈_ESC_죽은셀렉터.

## 159차 — 최상위 법칙 3종 신설: §백그라운드 사전승인 · 완료 시점 예고 금지 · 머지 충돌점검+운영자 확인요청 (운영자 260709 전 레포 공통 지시)
- **경위(운영자)**: ① 승인 없는 백그라운드가 의미 없이 도는 일 빈번 → 무엇이든 백그라운드로 돌리려면 필요 여부가 정해지는 착수·계획 단계에 한 번 묶어 먼저 묻고 승인 후에만 ② "1~2분 뒤 완료된다" 류 완료 시점 예고 발화 금지 ③ 머지는 다른 세션 작업과 충돌 없는지 확인 후, 머지한 다음 운영자에게 확인 요청 — 전부 최상위 법칙으로 지금 클론한 전 레포 CLAUDE.md에 박기.
- **수정(CLAUDE.md)**: 절대명령 표 10→13행(11·12·13) · §운영 태도 h) 완료예고 금지 신설 · §수정 모드 2) h) 머지 충돌점검+확인요청 신설(자가검증 g) 대체 아님 명시 = 사람 감각 확인 축) · 2. 6) §백그라운드 절 신설(디폴트 포그라운드 · §선택질문 1회 승인 · 명시 지시 = 승인 · 완료 시 정리).
- **효과/리스크**: 유령 백그라운드·빈 시간약속·타 세션 역행 재발 방지(nomute-editor·yeulmaru-promo·TS·nomute·Yeul 동시 반영) · 리스크 = 소프트 룰(절에 정직 표기)·기존 절차 대체 0.
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 git show origin/main:CLAUDE.md read-back(신설 3줄 실측). 롤백=_versions/260709_2336_최상위법칙3종.

## 160차 — MCP 권한 자동 허용 4종: settings.json permissions.allow 신설 (운영자 260710 명시 승인)
- **경위(운영자)**: Claude Code Remote·github PR 구독/해제 MCP 툴이 세션마다 권한 프롬프트를 띄움 → 운영자가 4개 rule 추가를 명시 승인 · "정확히 이 범위만".
- **수정**: `.claude/settings.json`에 permissions.allow 신설 = {`mcp__Claude_Code_Remote`·`mcp__claude-code-remote`(신/구 표기 = Claude Code Remote 서버 전체 허용) · `mcp__github__subscribe_pr_activity`·`mcp__github__unsubscribe_pr_activity`(PR 구독/해제가 github 서버 표기로 뜨는 세션 커버)} — 기존 hooks 3종(SessionStart·UserPromptSubmit·PostToolUse) 원문 보존 · CLAUDE.md 말미에 정본 위치 1줄 기록(강제 = settings.json · 줄 = 기록용). 적용 = 다음 세션부터.
- **효과/리스크**: MCP 권한 프롬프트 제거(매 세션 원탭 승인 루프 소거) · 리스크 = 허용 폭이 해당 서버 전체+툴 2종으로 한정(그 외 툴 = 기존 프롬프트 유지) · 4종 전부 세션/PR 구독 제어 축이라 레포 콘텐츠 무변경.
- 검증: 자가검증 경로=jq 파싱 rc=0 + python json 구조 assert(allow 4종·hooks 보존) + check_refs rc=0 + 머지 후 git show origin/main:.claude/settings.json·CLAUDE.md read-back(4항목+1줄 실측). 롤백=_versions/260710_0008_MCP권한자동허용.

## 161차 — §백그라운드 기계 강제: PreToolUse 훅 bg_gate = 백그라운드 Bash 승인 팝업 (운영자 A안 확정)
- **경위**: 운영자 "강제로 하면 좋겠고" → 선택지(A 팝업 강제/B 차단+승인표식/C 소프트) 중 A안 확정 — 절대명령 11을 소프트 룰 → 기계 게이트로 승격.
- **수정**: `.claude/hooks/bg_gate.py` 신설(Bash `run_in_background=true` → permissionDecision `ask` = 승인 팝업 강제 · 그 외/파싱 실패 = 무의견 = 오차단 0) · `.claude/settings.json` hooks.PreToolUse 등재(디자인 훅 3종·permissions.allow 보존 병합) · CLAUDE.md §백그라운드 c)~d) 분리(기계 강제 명시 · 에이전트/워크플로 축 = 소프트 유지). 전 레포 동시 반영(nomute-editor·yeulmaru-promo·nomute·Yeul = py · TS = js·CDP 도구 자동통과).
- **효과/리스크**: 승인 없는 백그라운드 = 기계적으로 불가 · 리스크 = 실행 순간 팝업 1탭 추가(운영자 감수 확정) · 훅 미로드 환경 = 소프트 폴백.
- 검증: 자가검증 경로=훅 stdin 시뮬레이션 3케이스(ask JSON·무출력) + settings.json 재파스 + check_refs rc=0 + 머지 후 git show origin/main read-back. 롤백=_versions/260710_0552_백그라운드훅강제.

## 162차 — 지도 도로 v3 왕복 차선(한국식) · 주민 산개 · 개명(무디→홍석천·하은→하서연) (운영자 260710 "차가 차도로 안 다니고, 애들도 항상 뭉쳐있어")
- **경위(운영자)**: ①스크린샷 = 차가 좌상단 빌딩 지붕 위 주행 ②한국식 통행 스펙 — "지금 도로를 좌우 각 1차선 · 가운데 180도 그어 이동경로가 조금이라도 위면 우측통행" ③지도 열 때마다 주민 뭉침 ④(추가 지시) 무디→홍석천 · 하은→하서연.
- **수정**: ①도로 v3 재트레이스(viewer/index.html) — 원인 = v2 nMain 시작(0,2.5)이 빌딩 위(좌상 도로 실방향은 ↗ = nwUp 신설 · 북부 간선은 (23.5,11.8) X교차 분기) + 밤/낮이 서로 다른 그림(다리 중심 밤 67,19.5/낮 84,25 · 간선 1.5~2.5 시프트) → 간선 6종 YMAP_ROADS_TONE 톤별 세트·배경 스왑 시 ymCarsBuild 재생성·광장 마름모 재실측(N49.5,38) · 'h' 가림 실데이터화(밤 1·낮 4구간) ②왕복 차선 — 간선 7종 × 정·역 = 14대 · yLaneOf rev = 역방향 우측 차선(h 구간 자동 시프트·s 정지선 양방향 각자 정지) · 오프셋 1.6→1.2% · 신호등 (48,34) 이동 ③주민 산개 — ymZonePt 같은 존 워커 최소 12px 간격(9회 시도+최선 폴백) + places.json evening 분산(찻집5·골목4 → 장소당 최대 2: 세라→연습실·하서연→학교·데스크→편집국·류→시장·윤→놀이터) ④개명 — 정본 19파일 조사 규칙 치환(가→이·는→은·를→을·다→이다 · 하은→하서연 = 받침 동일 안전) · id(mudi·haeun)·R2 경로·전화 배선 불변.
- **효과/리스크**: 차 = 실그림 도로 위 왕복 통행(마주 오는 차 반대 차선) · 주민 = 저녁 쏠림 해소+시각 겹침 방지 · 리스크 = 밤/낮 잔오차 ±1(도로 폭 안) · nShop 하행차선 = 광장 연석 근접(실그림 구조상 로터리 안쪽 차선) · evening 동선 변경 = 마주침 후보 분포 자연 분산(의도).
- 검증: 자가검증 경로=오버레이 QA 3라운드(밤·낮 10% 그리드+확대 크롭 대시 실측) + node 단위검증(14차선 기울기 ±0.5·정역 오프셋 분리·h 시프트 좌표 실측) + JS 문법 체크 + check_refs rc=0 + 머지 후 git show origin/main read-back. 보고서=docs/reports/260710_지도차선왕복_주민산개_개명.html. 롤백=_versions/260710_1118_지도차선왕복_주민산개_개명.

## 163차 — 프로필 UI 5종 + 프로필 이미지 생성(op meface) + 게스트 PIN 셀프변경(op pinset·R2 재설계) (운영자 260710 스샷 3장·중간지시 2건)
- **경위(운영자)**: 프로필 화면 스크린샷 3장 + 지시 6건 — ①사진 저장 안내문구 어색(시안 요청) ②내 소개 길면 라벨 압사(오버플로) ③카메라 배지 = 원 밖 걸침·사진 유무로 첨부/교체 시맨틱 ④프로필 없을 때 소개 읽고 캐릭터 톤으로 프사 생성(게이지) ⑤친밀도로 상승한 스탯 = MUT 음영+흰 점선으로 다각형 추가분 표기 ⑥PIN 변경 = 관리자 불가 표기·게스트는 변경 가능.
- **수정**: 가) 뷰어(viewer/index.html) — si-v 50%+si-vv 말줄임 · yprof-cam 원밖 걸침(overflow 제거·has-img>svg 직계한정으로 카메라 글리프 유지) · yStatBoost(yBond 파생 온기/장난/텐션)+yHexSvg 3층(hx-p 뮤트음영 밑·hx-v 라임 중·hx-po 흰점선 위) · 생성 게이지 행(.ycf-bar.slow)+yMefaceStart/Tick 폴 · yPinChangeOpen(4자리) · yMeAva 해석 SSOT(기기>서버). 나) 게이트웨이(functions/api/yeta.js) — op meface(소개 기반 생성 dispatch·pending casPut 원자선점·avatar 가드·일상한 2) · op pinset(R2 auth/overrides.json CAS·시도상한5·4자리) · auth effectiveHash 대조 · op me 스프레드(avatar 보존) · pinHash/readOv 헬퍼. 다) 신설 .github/workflows/yeta-meface.yml + .github/scripts/yeta_meface.py(BASE 임포트·turns 무접촉·git폴백 없음·소개원문 비출력). 라) 문구 = 운영자 픽 무음동 톤. 마) CLAUDE.md op 19→21·이미지파이프 절 meface · users.json _comment R2 오버라이드 반영.
- **보안 재설계(핵심)**: pinset 초안 = users.json main 커밋 → 보안 평의회 치명 BLOCK(무인증 공개 게이트웨이가 크랙된 게스트 PIN으로 production main 커밋 유발) → 운영자 확정 하 R2 오버라이드로 전환{비공개 R2 auth/overrides.json{원래해시:새해시} CAS·GitHub 쓰기 축 제거·auth가 effectiveHash로 대조 = 원래 PIN 자동 무효화·손상 시 fail-open}. 게스트 PIN 공개 크랙 가능은 선재·운영자 수용(게스트=보안벽 아님) — 재설계는 그 위 main 커밋 프리미티브만 제거.
- **효과/리스크**: 스샷 결함 2건(오버플로·빈 배지) 즉해소 · 친밀도 성장 시각화 · 프로필 부트스트랩 원탭 · 게스트 PIN 자율성 · main 커밋 유발 0. 리스크(전부 하·유계) = 생성 아바타 공개 R2(로스터 동축) · PIN 전파 CDN 지연(UI 확인방법 안내) · meface dispatch 실패 quota 미환불·러너 선가드 부재(일상한 2 하드 백스톱) · 레이더 부스트 표시전용.
- 검증: 자가검증 경로=auth/pinset 17시나리오 로직 시뮬 전원 PASS + 실브라우저 스모크(Playwright headless) 크래시 0(자가 적발 치명 YSESS TDZ 즉수정 실증) + node --check·py_compile·YAML 파스 + check_refs rc=0. **5인 평의회 2라운드**(보안·파이프라인·뷰어·디자인·규정) = 1R BLOCK 3(보안 치명 pinset·뷰어 치명 PIN 4자리 자기잠금·규정 완료예고) → 전건 메인 수정 → 2R 전원 APPROVE + 머지 후 git show origin/main read-back. 잔여 후속권고(하) = op numeral 관례(기존)·CII .yhex/.ycf-bar 미등재(부모 기존 공백). 롤백=_versions/260710_1127_프로필UI_meface. 운영자 확인 포인트: 라이브(yeta.soong.kr) 프로필 화면에서 소개 길게 입력→라벨 안 짤리는지·카메라 배지 원밖 걸침·게스트 PIN 4자리 변경 후 재로그인 확인.

## 164차 — 호칭 변경 = 이미 열린 대화도 즉시 새 이름 적용(과거 히스토리 옛 호칭 우선 누름) (운영자 260710 "이름 바꾸면 같은 대화창서 다음엔 00아~")
- **경위(운영자)**: 호칭을 바꾸면 이미 진행 중인 대화도 그 새 이름으로 부르게 — 예: 앞에서 "치니야~" 했어도 이름 바꾸면 같은 방 다음 턴부터 "00아~". 가능한지 + 구현.
- **실측(이미 배선됨)**: me.call(호칭)은 전 방 공유 top-level(op me) · 러너가 매 턴 fresh(웜 루프·extract_mat)로 세션에서 읽어 me_block으로 프롬프트 주입(inject_character.sh:62 · 본답장·오프닝·초대·넛지 공용) → 이름 바꾸면 다음 답장부터 새 call 반영. 유일 빈틈 = 과거 대화 히스토리에 옛 호칭이 남으면 모델이 연속성으로 유지할 여지.
- **수정(shared/inject_character.sh me_block)**: 호칭 지시줄에 우선 문구 추가 — "이게 유저의 현재 호칭이다: 이 대화 앞부분에서 다른 이름으로 불렀더라도(호칭이 바뀐 것) 지금부터는 옛 이름 쓰지 말고 반드시 이 이름으로만 부른다". 매 턴 fresh 주입 위에 과거 히스토리 우선을 명시적으로 눌러 갱신 확정.
- **효과/리스크**: 호칭 갱신이 신규·기존 방 모두 다음 턴부터 확정 반영(전 방 공유 성질 유지) · 리스크 = 없음(비신뢰 격리 프레임·clean 고정점 불변 · 문구만 보강 · 캐릭터/말투/규칙 무접촉). 소개(about)도 동일 경로라 갱신 시 함께 반영.
- 검증: 자가검증 경로=me_block 실행 시뮬(호칭 有=우선문구 렌더·빈값=빈 블록) + bash -n + check_refs rc=0 + 경로 4단 코드 실측(op me 저장→extract_mat 매턴 fresh→me_block 주입→과거우선). 국소 문구 손질 = 자가검증(5인 불요) + 머지 후 git show origin/main read-back. 롤백=_versions/260710_1245_호칭갱신_우선주입. 운영자 확인 포인트: 대화 중 호칭 바꾼 뒤 다음 메시지에서 캐릭터가 새 이름으로 부르는지(라이브 대화 1턴).

## 165차 — 프로필 화면 재구성: 부수설명·하단노트 폐지 · 「소개로 프사 생성」 토글 · 중분류 블릿+영문표기 (운영자 260710 스샷·지시 7건)
- **경위(운영자)**: 프로필 스크린샷 + 지시 7건 — ①리스트 항목 부수설명(si-d) 전부 삭제 ②하단 노트 삭제 ③계정 위 마진 확보 + 거기 「소개로 프사 생성」 체크박스(기틀 계승 · 켜짐=자동생성 / 꺼짐=사용자 첨부·기본 이미지) ④중분류에 블릿(강조색) ⑤`AI가 나를 부르는 법`→`PROFILE` ⑥`계정`→`Account Setting` ⑦`이름 (호칭)`→`이름` · `나를 소개하는 말`→`나의 페르소나` · `PIN 변경`→`PIN 설정` · `알림·앱 설정`→`앱 설정`.
- **수정(viewer/index.html)**: 가) nav 부수설명 = 전 항목 `d=''`(si-d 자동 생략) · `.yset-note` div 제거(호칭·소개 안내문). 나) mfRow(수동 생성 버튼·av 없을 때만) → afRow = `.yset-item.sw` 스위치 **기틀 계승**(pref `autoface` · `role=switch`·`aria-checked` · 진행 중 `.ycf-bar` 게이지 라벨 밑) · 클릭 핸들러 autoface 케이스(켜짐+프사없음+진행중아님 → 기존 `yMefaceStart` 재사용 · 소개 없으면 miniToast+못 켬). 다) `#ycprofileCard .yset-sec::before` 블릿(`content:'• '`·`--accent` 형광 · CII 소머리 정본 계승 · 프로필 중분류만) · `.yset-acc { margin-top:var(--sp-3) }`. 라) 라벨 + `yEditOpen` 타이틀 동시 갱신(이름·나의 페르소나) · 섹션 `PROFILE`/`Account Setting`.
- **효과/리스크**: 프로필 표기 위계 정리(§📐 부제 폐지 디폴트 부합) · 자동생성 = 옵트인 토글(유료 일상한 가드 뒤 기존 meface 파이프 그대로) · 리스크 없음(값 SSOT 토큰만·기틀 스위치/소머리 계승·신규 토큰 0·meface·게이트웨이 무접촉).
- 검증: 자가검증 경로=Playwright 격리 렌더 어설션(섹션 2종=PROFILE/Account Setting·si-d 잔여 0·note 제거·토글 렌더+클릭 후 on/pref='1'/aria='true'·블릿 content '• ' rgb(207,255,64)·`.yset-acc` marginTop 18px = 전건 PASS) + 스크린샷 시각 실측 + check_refs rc=0(JS 구문·소머리·토큰락·거울·구조토큰 정합) + git stash 원본 대조로 기존 경고(bg-scrim/line2 미사용)=회귀 아님 확인. 국소 UI+문구 손질 = 자가검증(게이트+렌더 어설션+read-back · 5인 불요). 롤백=_versions/260710_1324_프로필설정_재구성. 운영자 확인 포인트: 라이브(yeta.soong.kr) 프로필에서 소개 입력 후 「소개로 프사 생성」 토글 켜면 생성 게이지 뜨는지 · 중분류 블릿 형광 · 7종 표기 반영.

## 166차 — 플레이그라운드 포터블 v2 반영: 미세조정 컨트롤 확장(축 3분류·옵션 ~50개·기틀 갱신 역제안) + 거울 3점 동기 (운영자 260710 신판 파일 업로드)
- **경위(운영자)**: 운영자가 `플레이그라운드_포터블.md` 신판(v2)을 채팅에 업로드 — 레포 기존판과 diff = ③ "미세조정 슬라이더"→"미세조정 컨트롤" 확장 + §3-2′ 신설 + 안티패턴 2건 추가.
- **v2 델타(운영자 저작)**: ① ③항 = 옵션 규모 평균 50개 안팎(항목 최대 고도화·단순 대상만 축소) · 축 3분류{정렬·간격·높이=자유 / 확립 축=자유+실존값 사다리 눈금(밖=⚠ 실시간) / 색=팔레트·토큰 폐쇄 셀렉트} · 복사 시 ⚠ 축 = 알림창+「기틀 갱신 역제안」 블록 ② §3-2′ 신설 = 기틀 인지 3중 장치{확립값 사다리 실측 추출·이탈 ⚠ 실시간+카운터·복사 알림창 3택[역제안 포함/계승분만/취소]} · 확립 축 잠금도 금지(역제안으로 승격) · 알파 변주=자유 · 프리셋 동일 규율 ③ 안티패턴 += 이탈 감지 없는 자유 슬라이더 · 옵션 몇 개만 주고 끝.
- **수정(4파일)**: 가) `docs/플레이그라운드_포터블.md` = 업로드본 전문 교체(diff 0 검증). 나) CLAUDE.md §작업표준 b) 5요소 문구 = "미세조정 컨트롤(옵션 ~50개·축 3분류·기틀 이탈 = ⚠+갱신 역제안)"(얇게·포인터 유지). 다) `yeta-design` SKILL ③·④ 상세 흡수(+포터블 §3-2′ 포인터) — 스킬 기존 92행 "토큰을 돌린다" 원칙과 정합(역제안 = 그 승인 정문 경로의 UI화). 라) 정본인덱스 4절 확립본 행 명칭 동기(슬라이더→미세조정 컨트롤·§3-2′ 포인터).
- **효과/리스크**: 플레이그라운드 옵션 고도화 기준 명문화 + 기틀 변경이 "몰래"가 아니라 명시 역제안 경로로 승격(절대명령1 계승/갱신 2분법의 시안 단계 구현) · 리스크 = 없음(순수 문서·규칙 동기 · 코드 무접촉 · 규칙 내용 = 운영자 저작 · 3점 동기로 드리프트 차단).
- 검증: 자가검증 경로=diff(업로드본↔레포본)=0 + 거울 3점 grep 실측 + check_refs rc=0 + 머지 후 git show origin/main read-back. 순수 문구 동기 = 자가검증(5인 불요). 롤백=_versions/260710_2047_플레이그라운드포터블v2. 운영자 확인 포인트: 다음 디자인 시안 요청 시 컨트롤 ~50개+⚠ 역제안 알림창이 실제로 오는지 1회 확인.

## 167차 — 플레이그라운드 제작 팁 정본화: 베낄 것 5 + 구판 관성 주의 3 (운영자 260710 선례 실측 전달 — 260710 검색UX 50축 시안에서 추출)
- **경위(운영자)**: 타 세션 260710 검색UX 50축 시안 제작에서 실측 추출한 팁을 채팅으로 전달 — "포터블 스펙에 더해, 시안 만들 때 이렇게 해라". 채팅 방치 = 증발(절대명령 6) → 시안 작업 시 자동 로드되는 `yeta-design` 스킬에 정본화. 포터블 파일(운영자 저작 수출본) = 무접촉.
- **베낄 것 5(운영자 확정)**: ⓐ범주형 축 = 세그 버튼(슬라이더 억지 매핑 금지·state 규약 동일) ⓑ프리셋⇄슬라이더 양방향 동기(재일치 시 칩 자동 재점등 = 전 프리셋 대조 함수) ⓒ첫 화면 = 논점 상태 오픈(현행 = 토글 1번) ⓓ미리보기 속 실물 트리거 실배선(전환 연출 = 결정 대상) ⓔ배선 메모 2종(축 정의 `tok:'--r-pill 999'` 병기 · 한글 열맞춤 2칸 폭·padEnd 금지).
- **구판 관성 주의 3(신판 §3-2′ 재확인)**: ⚠ 고정 라벨 금지 = 값 기준 실시간 판정(datalist 눈금·「기틀 이탈 N축」 카운터) · 복사 모달 3택 실배선(역제안 포함/계승분만/취소) · 색 = 폐쇄 셀렉트(알파 변주만 자유) — 검색UX 시안은 accent 알파 틴트만 사용 = 준수 선례.
- **수정(1파일)**: `.claude/skills/yeta-design/SKILL.md` 플레이그라운드 절에 팁 2블록 삽입(92행 뒤 · 기존 ③·④ 규칙과 중복 없이 회귀 방지 각도로 보강). CLAUDE.md·정본인덱스 = 무접촉(스킬이 이미 등재된 상세 정본 · 얇게 유지).
- **효과/리스크**: 다음 시안부터 선례 품질(50축·양방향 동기·실물 배선)이 세션 자동 로드로 강제 · 리스크 = 없음(순수 지침 문서 · 코드 무접촉 · 내용 = 운영자 저작 전달분).
- 검증: 자가검증 경로=check_refs rc=0 + 머지 후 git show origin/main read-back(팁 2블록 grep). 순수 문구 = 자가검증(5인 불요). 롤백=_versions/260710_2102_플그제작팁. 운영자 확인 포인트: 다음 플레이그라운드 시안에서 세그 버튼·프리셋 재점등·논점 오픈이 실제 적용되는지 1회.

## 168차 — 「페르소나로 프로필 생성」 = 스위치 토글 도형 → mut 텍스트 링크 (운영자 260710 "도형 안 들어갈 정도 · 그냥 글로")
- **경위(운영자)**: 165차 「소개로 프사 생성」 스위치가 글래스 박스 행(`.yset-item.sw`)을 통째 차지 → "생성 저건 도형 안에 들어갈 정도는 아니고 · 나의 페르소나 아래 그냥 글로 「페르소나로 프로필 생성」 · mut으로".
- **수정(viewer/index.html)**: afRow(스위치 토글) → genLink = `.yprof-genlink` **도형 없는 mut 텍스트 버튼**(배경·테두리 0 · `--mut`·`--fs-xs` 계승 · 나의 페르소나 직후) · 클릭 = `data-act="meface"`(기존 `yMefaceStart` 직행 · 소개 없으면 함수가 안내+중단) · pref `autoface`·스위치 클릭 핸들러 폐기 · 프사 있으면 숨김(카메라 배지로 교체 = 원 mfRow 로직 복원). 블릿·계정 마진·7종 표기는 165차 그대로.
- **효과/리스크**: 생성 액션 = 비중 낮춘 미니 텍스트(박스 소거) · 리스크 없음(신규 토큰 0 · `--mut`/`--fs-xs` 계승 · meface 파이프·게이트웨이 무접촉).
- 검증: 자가검증 경로=Playwright 격리 렌더 어설션(genText '페르소나로 프로필 생성'·genColor rgb(139,143,138)=--mut·bg transparent·border 0px·나의 페르소나 직후·옛 autoface 토글 소멸 = 전건 PASS) + 스크린샷 실측 + check_refs rc=0. 국소 UI 손질 = 자가검증(게이트+렌더+read-back). 롤백=_versions/260710_2224_페르소나생성_텍스트링크. 운영자 확인 포인트: 라이브 프로필에서 나의 페르소나 밑 mut 텍스트 「페르소나로 프로필 생성」 탭 시 생성 게이지 도는지.

## 169차 — 프로필 리디자인(피트니스 결) + 미션 뱃지(픽토그램) + 챗 액티브(이름부 풀다운·근처 스택·초대 팝업) 플레이그라운드 (운영자 260712 레퍼런스 3연속 지시)
- **경위(운영자·같은 턴 3건)**: ① 다크+라임 피트니스 앱 홈 스크린샷 — "개인 프로필을 이런식으로 리디자인, 메인 레포 디자인은 계승" ② "특정 미션 수행 시 뱃지 — 찾아봐서 넣어줘, 그림은 SVG 말고 픽토그램으로" ③ 메신저 프로필 레퍼런스 — "가장 친한 인물 이렇게" + "대화창 이름부 잡고 내리면 그 인물 프로필·감정·액티브 버튼 / 우상단엔 부를 수 있는 장소의 인물 프로필 몇 개 → 누르면 초대 2차 팝업".
- **리서치(스킬 §🔭 상설 · READ-ONLY 3기 병렬)**: 피트니스 다크·라임 공식(= yeta 팔레트 동계열 실측 — 라임 위 검정 16:1 통과·흰 금지 · 히어로 카드 화면당 1장 · 프로필엔 라임 축소 우세) · 미션 5분류(온보딩+누적+히든 코어·티어 얕게·절제 연출·yeta 사건 미션화) · 이모지 뱃지 CSS 레시피(글래스 원형+3중 box-shadow 링·잠금=티저/실루엣·pop+shine·이모지 폰트 스택·role=img).
- **산출(docs/reports/260712_프로필리디자인_뱃지_챗액티브_플레이그라운드.html · 자기완결 127KB)**: 뷰 3{① 프로필 신안(그리팅 헤더·히어로 미션 카드 라임/글래스엣지/무채·스탯·가장 친한 인물 레일/그리드·뱃지 그리드 12종) ② 챗 액티브(이름부 풀다운 드래그 실배선 = 임계 판정·감정/생각/위치/소개+액션 4버튼 · 근처 인물 아바타 스택 2+N · 초대 2차 팝업 = armed 2탭→sys 버블·정원 2 힌트) ③ 현행 프로필 고정 재현(비교 기준)} × 프리셋 4(현행·★피트니스 히어로·글래스 절제·콤팩트 수집가) × 미세조정 54축(3분류 · 확립 축 = 사다리 실시간 ⚠ + 「기틀 이탈 N축」 카운터 · 복사 = 모달 3택[역제안 포함/계승분만 스냅/취소] + 역제안 블록). 전 요소 = 기존 토큰·정본 계승 조립(히어로 라임 필=.yset-chip.on 결 · 뱃지=.ygrade 양각+.ycrow.locked 티저+yqm ??? · 풀다운=.yeta-pick+#yetaProfPop+.ycd-gear · 스택=.yh-ava+.yprof-cam 갭 · 초대=.ypick-row+armed) · 뱃지 픽토그램 = 시스템 이모지(운영자 지시 · .yme-mood em 선례) · 에셋 = 크로뮴 캔버스 축소 base64(아바타 6종+배경 = 52KB).
- **실장 가능 판정(추정 아님·코드 실측)**: 풀다운 = `.yeta-who` 현행 표시 전용 → 포인터 훅 충돌 0 + 기존 프사 팝오버 확장 = 가능 · 근처 스택 = 지도 탭이 이미 클라 결정적 동선 산출 = 같은·인접 장소 도출 가능 · 초대 = 기존 op `invite` 재사용(정원 MAX_ROOM=2 → 스택에서 1명만 초대 가능·만석 표시 필요).
- **효과/리스크**: 운영자 취향(피트니스 라임 결)과 레포 기틀(§🎨 계승)의 교차점을 만지는 시안으로 확정 가능 · 뱃지 = 신규 색 0(실버=fg 저투명·라임·시즌 보라 재활용) · 리스크 = 시안 단계 무(라이브 무접촉) · 본구현 시 신설 7컴포넌트 CII 등재 + 뱃지 판정 데이터 축(세션 파생) 설계 필요.
- 검증: 자가검증 경로=Playwright 헤드리스 실클릭(포터블 §4 — pageerror·콘솔 에러 0 · 드래그 임계 미만=복귀/초과=열림/바깥 탭=닫힘 · 초대 armed 2탭→sys 버블 · ⚠ 1축 시 복사 모달+역제안 블록 · 프리셋 4종 순회) + 상태별 스크린샷 14장 눈검수(중첩 버튼 레이아웃 버그 발견→div 교정 재검증 PASS). 순수 시각 시안 = 검증 면제 티어이나 플그 표준 §4는 이행. 롤백=_versions/260712_0930_프로필뱃지챗_플레이그라운드. 운영자 확인 포인트: 첨부 HTML 열어 ② 챗 액티브에서 이름 드래그·스택 탭 만져보고, 마음에 드는 프리셋에서 [선택값 복사] 회신(그게 곧 본구현 스펙).

## 170차 — 전후 비교 플레이그라운드: 전(현행 고정) | 후(변경 통합 ★·보정 가능) 2폰 나란히 (운영자 260712 "한수로 하고 · 변경분 모아 후로 · 후에서도 보정")
- **경위(운영자)**: 169차 시안 회신 — "한수로 하고(= ★피트니스 히어로 + 본구현 ①프로필→②뱃지→③챗 순 확정), 지금 변경된 부분들 모두 모아서 '후'로 한 다음에 전후 비교 플레이그라운드. 후에서도 보정할 수 있게".
- **산출(docs/reports/260712_프로필뱃지챗_전후비교_플레이그라운드.html · 169차 파일 기반 변환)**: 폰 2대 나란히 — **좌 = 전(현행 프로필·현행 챗 고정 재현 · 콘솔 무영향)** / **우 = 후(변경 전부 통합 = ★ 기본값 · 54축 보정 그대로)**. 보기 토글(①프로필/②챗 액티브) = 양폰 동기 전환. 후 실배선(풀다운 드래그·스택 탭→초대 2탭→sys 버블·뱃지 연출) 유지 · [현행] 프리셋 = 후를 전과 동일하게(델타 0 확인용) · 복사 출력 헤더 = "전후 비교 — 후 선택값".
- **효과/리스크**: 전후가 같은 화면비·같은 데이터로 나란히 → 정도 측정이 직관화(포터블 ① "현행 = 비교 기준 함께 재현"의 확장 구현) · 리스크 = 시안 단계 무(라이브 무접촉).
- 검증: 자가검증 경로=Playwright 실클릭(pageerror·콘솔 에러 0 · 양폰 뷰 동기 profile/chat · 좌폰 스택·풀다운 부재 · 후폰 풀다운/초대/프리셋 재점등/⚠ 모달·역제안 전건 PASS) + 듀오 스크린샷 6장 눈검수. 변환 중 옛 setView 잔존부 구문 에러 1건 검증이 잡아 교정(앵커 슬라이스가 함수 몸통 내 동일 토큰에서 조기 절단 — 실측 후 제거). 롤백=_versions/260712 전후비교 백업. 운영자 확인 포인트: 첨부 HTML에서 좌우 비교 후 우폰 보정 → [선택값 복사] 회신 = 그 값으로 본구현(①프로필→②뱃지→③챗) 착수.

## 171차 — 프로필 v2 본구현: 그리팅 헤더·히어로 미션 카드·친한 인물 레일·미션 뱃지 12종 + 나레이션 줄바꿈 (운영자 260712 선택값 회신 = 본구현 스펙 · 한수 확정 ①)
- **경위(운영자)**: 170차 전후비교 플그에서 [선택값 복사] 회신(히어로 = 글래스+라임 엣지·패딩14·제목14·칩35·디스크98·돌출13·진행 원 30 · 스탯 흰 · 레일 106/11/58 · 뱃지 3열·60·스쿼클·자물쇠·α.5·글로우.28 · 공통 현행) + 보정 3건{① 지문 = 제 줄 시작·지문 뒤 대사 = 빈 줄("애초에 줄 바껴서 써지게") ② 미션 제목 아래 간격 1.5배 ③ 진행 원 = 칩들과 세로 중앙 픽셀 일치} + "챗 자체는 저게 맞음"(현행 챗 유지) + 배경 = 캐릭터별 무대 유지 확인. 한수 순서 = ①프로필+뱃지(이번) → ③챗 액티브(후속).
- **수정(viewer/index.html · apps/yeta/00_지침_캐릭터챗.md · docs/CII_컴포넌트계승인덱스.md)**: 가) `yProfileRender` v2 재작성 — 그리팅 헤더(`YPG_HI`×`ymSlot` 시간대 인사 · 아바타 34 = 기존 `#yprofAva` 배선·카메라 배지 축소 계승) · 히어로 미션 카드(`.ymis` = `.ypf-g` 글래스+라임 엣지 · `yMissionHero` = 미달성 미션 우선순위 로테이션·날짜 시드·dispatch 0 · 미대면 대상 = 실루엣 `?` 은닉) · 스탯 3칸 실측 파생(진행 대화 = 실발화 방 · 만난 인물 = 잠금 제외 풀 · 함께한 밤 = KST 발화 일수) · 강조색 칩 위치 이동(기능 불변) · 친한 인물 레일(`yBffTop` = 내 발화 수 = yFarOK 지표 계승 · 1위 라임 링·시즌 보라 링 · 최근 한마디 = ySnip) · 미션 뱃지 12종(`YBD` 세션 파생 판정 · 티어 링 실버/라임/시즌 · 잠금 = 티저+자물쇠 · 히든 ??? = yqm · 탭 = 획득 연출 팝+샤인/잠금 조건 토스트). 나) `yFmt` 지문 줄바꿈 정규화(제 줄 시작+뒤 빈 줄 · 과거 히스토리 표시 시점 일괄) + 00_지침 형식 절 1줄(생성측 규칙 · L0 골격 무접촉). 다) CII 4행 등재(그리팅 헤더·히어로 미션·레일 카드·미션 뱃지 — 이모지 픽토 = 운영자 명시 예외 박제). 신규 :root 토큰 0 · 서버·스키마·dispatch 무접촉(전부 클라 파생).
- **보정 3건 이행**: ① yFmt+지침(위) ② 제목 아래 = 6px(플그 상자간격 2px → 잉크 간격 ~7.4→~11.4px ≈ 1.5배 환산 — 상자 6px 실측 5.82 확인 · **잉크갭 해석** = 평의회7 F1 · 문자적 3px 원하면 1줄 조정) ③ 진행 원 = 칩과 같은 flex 행 center — **centerY 289.62 = 289.62 픽셀 완전 일치 실측**.
- **검증(10인 평의회 만장일치 + 자가검증)**: 자가검증 경로 = Playwright 격리 렌더 어설션(수치 스펙 전 항목·파생 스탯·링·연출·토스트·yFmt 4케이스) + 픽셀 측정(③ 동일·② 6px) + **판정 로직 시뮬 11시나리오**(빈 세션·call/voice·room·루시 5시 경계·새벽4시·50턴·잠금 제외 전원대면 = 열두 얼굴 획득+히어로 로테이션 실증·비숫자 ts 방어) + check_refs rc=0(신규 경고 0) + 스크린샷 눈검수. 평의회 10기(READ-ONLY·각기 다른 앵글) = 9번 REJECT 1건{**열두 얼굴 분모에 영구 잠금 winter 포함 → 영구 11/12 + 히어로 고착**} → 잠금 제외 풀로 수정 → 재평결 APPROVE = **전원 APPROVE**. 반영: 평의회1(ts 방어·플레이 버튼 게이팅) · 3(잠금 이모지 보정 주석·플레이트 .06 통일·raw 별칭화) · 4(죽은 CSS 2건 정리) · 6(프사 키보드 활성·히트슬롭 42/40·포커스 링·SR 진행도) · 9(위 + 시즌의 밤 h<6·실발화 방 카운트·합석 문구). 수용(비수정) = 스태거 캡 기존 설계 · 통화 턴 200 트림 엣지 · "함께한 밤" = 발화 일수 라벨(밤 정서 컨셉) · 1위 시즌 인물 = 보라 링 우선(등급 정체성 · 순위는 ① 칩). 후속 축 = 무음 단골(장소 이력)·히든 2종(barge via 분기) 실장 시 점등.
- **효과/리스크**: 프로필 = 확정 시안 그대로 라이브(신규 토큰 0·계승 조립) · 뱃지 = 무비용 클라 파생(과금·서버 0) · 리스크 = 낮음(회귀 전수 대조 PASS·평의회 만장일치 · 이모지 렌더는 OS별 차이 = 시안 각주 기고지).
- 검증필드: 자가검증 경로=Playwright 어설션+픽셀 측정+로직 시뮬+check_refs+10인 평의회 | 운영자검증 불가피 사유=미감·결(사람 감각 유일 축 — 동작 증명 ≠ 경험 증명). 롤백=_versions/260712_1031_프로필v2_뱃지_본구현. 운영자 확인 포인트: 라이브(yeta.soong.kr) 프로필 열어 히어로 카드·레일·뱃지 미감 1회 + 챗에서 지문 있는 새 답장 줄바꿈 결 1회. ②간격은 잉크갭 해석(1.5배) — 더 좁게(문자적 3px) 원하면 한마디.

## 172차 — 챗 액티브 본구현: 이름부 풀다운 시트 · 근처 인물 스택 · 초대 2차 팝업 (운영자 260712 "go" = 한수 ③ 착수 · 10인 평의회 만장일치 루프)
- **경위(운영자)**: 171차 완료 보고에 "go" — 한수 ③(챗 액티브) 착수 지시. 스펙 = 260712 전후비교 선택값{풀다운 = 드래그+탭·임계 40·높이 285·등장 .44s·감정 이모지+라벨·액션 4(통화·초대·성향·프로필)·글래스 원형 34 / 스택 = 2명·겹침 −14·아바타 26·밑판 갭 테·도트 없음 / 초대 = 팝오버·[초대] 필 2탭·정원·거절 힌트} + 원지시("이름부를 잡고 아래로 내리면 그 인물 프로필과 감정상태" · "우상단에 부를 수 있는 장소의 인물들 프로필 → 초대 2차 팝업").
- **⚠️ 결정 승계(명시)**: 운영자 260709 "수동 초대 패널 폐지 · 헤더 초대 픽토 폐지(도구 초대 같아 — 메타버스니까)"를 **운영자 260712 지시가 승계·대체**한다 — 진입점 = 도구 픽토가 아니라 **근처 인물 아바타 스택**(세계관 어포던스 · 후보도 전 로스터가 아닌 근처 인물 한정 = 260709 취지의 공간 축과 정합). yInvToggle 재생·후보 _yNear 한정이 그 구현.
- **수정(viewer/index.html 단일 파일 · 서버·러너·:root 무접촉)**: 가) 풀다운 = `#yetaPdc`(.yeta-pick 크롬 · 헤더 아래 62+safe · 높이 0↔285 전개 .44s --ease · 내부 stagger · inert 접힘) + `#yetaWho` 그래버(chev·role=button·키보드·아바타 제외 = 프사 팝오버 보존 · 드래그 추종+임계 40+탭 토글·가로 오판 차단) · 카드 = 프사 팝오버 파생 축(생각·위치·공기) + 무드칩(YPD_EMO×YME_MOODS · .yme-mood em 선례) + 액션 4(통화 = 무장만 대행·유료 고지 원문 보존·기무장 시 재클릭 금지 / 초대 = 팝업 / 성향·프로필 = 챗 회수 후 시트 · _yAfterClose 아님 = once close 리스너) — 단톡 = 직전 화자 카드·무드 발화 귀속. 나) 스택 = `#yetaNear`(.ystk · yh-r 첫 자리 · 미만석 = 근처 ≤2 / 만석 = 멤버 관리 / 초대 대기 = 억제 · 시그니처 가드) — `yNearCalc` 확장(_yNear ≤4 · **세대 토큰+원자 커밋** = await 중 방 전환 오염 차단 · pill(_yNearC) 동작 불변). 다) 초대 팝업 = `yInvToggle` 재생(우상단 앵커 · 후보 = 근처만+장소 라벨 · [초대] 필 armed 2탭 · 정원·거절 힌트 · 만석 = [보내기] · 열림 중 최신 재렌더) · op = 기존 invite/kick 재사용. 라) CII 3행 등재.
- **검증(10인 평의회 + 자가검증)**: 자가검증 경로 = Playwright 실배선(pageerror 0 · 임계 미만 복귀/초과 열림/탭 토글/바깥 닫힘 · 스택→팝업 상호배타 · armed 2탭 → op 경로 · 만석 전환 · 프사 팝오버 보존) + 스크린샷 눈검수 + check_refs rc=0. **하네스가 선제 적발 3건**{풀다운이 헤더 가림(top 앵커) · 포인터 캡처가 프사 탭 삼킴(아바타 제외) · 팝업 하단 앵커 어긋남} = 커밋 전 교정. 평의회 10기(READ-ONLY) = REJECT 4{A: yNearCalc 경합 오염(중) · C: 스택 테 raw 스테일 #121212(중) · D: 접힘 시트 포커스 잔존+히트 26/24(중 3) · J: 172차 원장 미등재} + 조건부 2{E 통화 무장 엣지 · I 단톡 호스트 고정·무드 오귀속} → **전 지적 수정 반영**(세대 토큰·var(--bg) 완전 계승·inert·히트 40·이 원장) 후 재평결 요청. 수용(비수정) = 팝오버 스테일 스냅샷 모델(닫고 열면 갱신 · 프사 팝오버 동일) · 스택 삽입 1틱 순서(수렴형) · 픽커 사장 노드 정리(후속).
- **효과/리스크**: 챗 → 인물·공간 액티브가 어포던스로 연결(북극성 반메타버스 결) · 유료 통화 가드 불변 · 서버 표면 0 · 리스크 = 낮음(평의회 만장일치 루프 + 실배선 검증 · 이모지 렌더 OS별 차이 = 기고지).
- 검증필드: 자가검증 경로=Playwright 실배선+스크린샷+check_refs+10인 평의회(REJECT 전건 수정·재평결) | 운영자검증 불가피 사유=미감·결(사람 감각 유일 축). 롤백=_versions/260712_1129_챗액티브_본구현. 운영자 확인 포인트: 라이브 챗에서 이름 당겨 카드 손맛·스택→초대 흐름 1회.

## 173차 — 절대명령 #0 신설: 세션 실행 프로토콜(10분 데드라인·분신술 디폴트·20열/50행 산출) (운영자 260712 지시 · CLAUDE.md 최상위 편입)
- **경위(운영자)**: 접수 즉시 시각 기록(2026-07-12 13:09:03 KST) 후 10분 내 결론 지시. 6항(①분신술 디폴트 ②접수시각기록+10분데드라인 ③10분초과=세션소멸·모든 실행 10분내 종료 ④산출=20열/항목당50행 ⑤UI변경시 전·후·리스크·장단점·아이데이션 이미지 ⑥미준수=실패)을 CLAUDE.md 최상위에 "1번으로" 박으라 명령.
- **수정(CLAUDE.md 단일 · 코드·:root·러너 무접촉)**: H1 제목 직하·기존 절대명령 #1(디자인) 위에 `## ⏱ 절대명령 #0 — 세션 실행 프로토콜` 신설. 위상선언 1줄로 실행방식 축 한정(§🔒 디자인 내용 위상·라우팅·기틀 승인 의무 우회 안 함 명문화). 모델ID = claude-opus-4-8 · KST 기록 `TZ='Asia/Seoul' date` 명시.
- **산출**: `docs/reports/260712_절대명령0_프로토콜_삽입_보고서.html` = 20열 × 60행(요구 20열/≥50행 충족) — 각 행 = 삽입 블록 국면(6항·규약검증·정합·리스크·장단점·검증경로·아이데이션·무영향축). UI/UX 변경 없음(문서 전용) → 5항 이미지 = N/A(diff=CLAUDE.md 문서만).
- **검증(자가검증 + 분신술 병렬)**: 자가검증 경로 = check_refs rc=0(pre-commit 강제 통과·실측) + git 커밋/push_rc=0 실측 + read-back(git show origin/main). 분신술 = Explore 2기 READ-ONLY 병렬(A 적대감사=규약/날짜스탬프/위상 · B 정합성=표등재/정본인덱스/현실성) 동시 발사(10분 데드라인 준수 위해 경량 앵글).
- **효과/리스크**: 세션 실행 규율이 문서 최상단 고정 · 서버·UI 표면 0 · 리스크 = 낮음(문서 전용·게이트 무영향). 후속 = 0.3) 절대명령 한눈에 표·docs/절대명령2_정본인덱스.md 등재 검토(분신술 B 권고 반영 시).
- 검증필드: 자가검증 경로=check_refs rc=0+git 커밋/push 실측+read-back+분신술 2기 병렬 | 운영자검증 불가피 사유=없음(문서 변경=도구 실측 전량 가능). 롤백=_versions + git revert. 운영자 확인 포인트: CLAUDE.md 최상단 절대명령 #0 블록 1회 확인.

## 260712 — 단톡 합석 = 새 스레드 분기(고유성 보존) + 초대 스택 중첩 제거
- **의도(운영자)**: ① "대화가 합쳐지면 직전 3주고받기까지 가져와 새 단톡으로 만들어 — 기존 1명 대화에서 이으면 고유성 깨짐" ② "이미 방 안인데 초대상대에 참여자가 뜸 — 좌상단에 참여자 있으니 우상단은 정원 안 초대가능 인원만(지금 중첩)".
- **수정(코드 2파일)**:
  - `functions/api/yeta.js` invite op = **분기 모델**: 원본 1:1 스레드 무변경 보존 · 직전 3주고받기(user 3턴까지 꼬리, sys 제외 대사만) 시드 복사 → 새 단톡 스레드 `g<base36>`(ID_RE 통과·페르소나 비충돌) 신설(room=[host]·invite 마커·last_sp=host) → cur 전환 → dispatch(수락/거절 러너). dispatch 실패 = 분기 회수+cur 원복. 하드캡 12 재확인. + op **focus{t}** 신설(단톡 등 비페르소나 방 진입 = draw 없이 cur 이동).
  - `viewer/index.html`: `yHost(tid)` 헬퍼(단톡 = room[0] 호스트 해석) · yLoad·yOpenThread = yHost 경유 · yListRender 행 = 단톡(멤버 이름 나열 + '단톡' 태그) · openYeta = 페르소나 아닌 charId = focus 전환 · **yStackSync(#2) = 우상단 스택 = 남은 정원(2−room)만큼 근처 초대후보만(만석·대기 = 숨김 = 좌상단 참여자와 중첩 제거)**.
  - 러너(`yeta_chat.sh`·`yeta_v3.py`) 무변경 = 이미 id-agnostic(persona = last_sp/room 파생, thread_view·pick_thread·reflect_invite 전부 THREAD 스코프).
- **검증(자가검증)**: 자가검증 경로 = node --check(gateway OK) + check_refs rc=0(viewer JS 구문 OK·디자인 게이트) + invite 분기 로직 node 시뮬(원본 11턴 보존·시드=정확히 마지막 3쌍 u3~u5/a3~a5·gid ID_RE 통과·cur=gid 실측). 운영자검증 불가피 사유=없음(도구 실측 가능 · 단 라이브 UX 감각 확인은 운영자 축).
- **효과/리스크**: 1:1 관계 고유성 보존(합석이 개별 대화 오염 안 함) · 단톡 = 독립 방(리스트 별행) · 초대 UI 중첩 해소. 리스크 = 낮음(러너 무변경·게이트 통과·기존 in-place 단톡 세션 하위호환[room≥2 렌더 유지]). 디자인 토큰 신설 0(계승만). 후속(비차단): 단톡 행/내 위치 place 해석 = gid→host 매핑(현재 '알 수 없음' 표기 · 코스메틱).
- **위상 주의**: 스레드 키 스킴 확장 = 기틀 근접 — 운영자 명시 지시가 승인축(§기틀 보호). dispatch char:main 단일그룹 불변 유지(스레드별 분기 아님). 롤백 = `_versions/260712_1342_단톡분기_초대중첩/`.

## 174차 — L0 프레임 이탈 근본픽스(claude -p 캐릭터 시스템 프레임 + is_frame_break 그물)
- **경위(운영자)**: 루시 챗에서 "이건 Claude Code 세션 페이로드네 → Claude Code로 응답" 영어 메타발화 스크린샷(sonnet-5×low) + "L0 박살내는 원인 찾아 해결" · 분신술 opus 최대노력·10분 데드라인·20열×항목50행 보고 지시.
- **근본원인(10인 평의회 8발사 만장수렴)**: 답장 생성 claude -p 가 Claude Code(코딩 에이전트) 기저 정체성을 시스템 슬롯에 달고 돌고, 캐릭터 프레임(00_지침+카드)이 전부 user 턴(stdin)뿐 → 위계 역전 → sonnet-5×low 가 프롬프트를 '분석할 페이로드'로 오인 이탈. --safe-mode 정상=CLAUDE.md 누수 아님(배제)·94k·캐시 배제. 기존 00_지침 "한국어"·"AI 아님"은 카드 우선 형식층/자기 off-switch = top-tier 하드락 부재가 뚫린 지점.
- **수정(3파일 · 서버·러너·:root 무접촉)**: 가) `shared/inject_character.sh` = `yeta_sys_frame()` 프레임 SSOT(형식중립 = chat/nudge/call 공용 설계). 나) `.github/scripts/yeta_chat.sh` = SYS_ARGS 앵커(YETA_SYS 0=off/1=append 기본/2=replace · 배열 전달 · CLI 드리프트 unknown-option 폴백=하드다운 방지) + gen_out `is_frame_break` 가드(성공판정 전 · 이탈=폐기·재시도 · 소진=finish error 안내 = 유출 미박제) + CONTRACT1 한 줄 재앵커. 다) `shared/claude_transient.sh` = `is_frame_break()` SSOT((a)리터럴 시그니처 + (b)한국어 출력계약 위반[지문·마커 제거 후 40자↑ 대사 한글<15%] · 자모범위 ㄰–㆏로 ㅋㅋ/ㅎㅎ 보호) · 가드감사 반영 정밀화(한국어 '시스템 프롬프트'·'프롬프트를 보여' 오탐 제거[운영자=프롬프트엔지니어 에코·인캐릭터 탈옥거부 처벌] · 클로드코드/챗봇자칭/역할극거부 보강).
- **결정**: append(기본·저위험 가산) + replace 노브 절충(분신2·6=replace 강권이나 라이브 유료검증 데드라인 불가 → 노브로 승격 경로 확보 · 보고서 권고). 벽(시스템 프레임=확률적)+그물(is_frame_break=결정론 백스톱) 이중화(분신8 "벽 없인 그물만 불충분").
- **검증(무과금)**: bash -n 3파일 OK · is_frame_break 단위 15/15(스샷원문·As an AI·user_message·챗봇자칭·역할극거부·클로드코드 BREAK / 정당탈옥거부·운영자 시스템프롬프트에코·AI부정문·언어모델언급·정상대사·지문·Black Mamba+ㅋㅋ·짧은대사·빈출력 OK) · check_refs rc=0 · origin read-back 실측. PR #243(draft) · Cloudflare 배포 성공.
- **평의회(8 READ-ONLY opus 최상위)**: 근본원인·픽스건전성·가드오탐·기타누수·병렬경로·인젝션·검증회귀·가드정밀 8앵글 만장수렴 · 전 지적(드리프트 폴백·가드 오탐 시그니처 제거·에러파일 미전달·형식중립 프레임) 반영.
- **효과/리스크**: L0 위계 교정(무해 입력 '어딨어'에도 프레임 유지) + 결정론 백스톱 · 프롬프트 인젝션 완화(system>user 경계로 위조헤더 강등) · 서버 표면 0 · 오탐≈0(13인 카드 영어대사 0) · 리스크 낮음(회귀노브·롤백). 후속(범위밖) = nudge/call 프레임 적용(분신5 우선)·replace 승격(YETA_SYS=2)·invite 계약 첫토큰 화이트리스트(분신4).
- 검증필드: 자가검증 경로=bash -n+is_frame_break 단위15/15+check_refs rc=0+origin read-back+10인 평의회(8발사 만장수렴) | 운영자검증 불가피 사유=라이브 실대화 프레임 유지 체감(사람 감각 축). 롤백=_versions/260712_1314_L0프레임이탈픽스. 보고서=docs/reports/260712_L0프레임이탈픽스_보고서.html(20열×160행). 운영자 확인 포인트: 라이브 챗을 sonnet-5×low 다이얼로 몇 턴 돌려 메타발화 재발 없는지 1회.


## 175차 — 대화 초기화 버그픽스: 설정 "대화 초기화"가 현재 방 1개만 지우던 것 → 전체 초기화
- **경위(운영자)**: 스크린샷(하서연·홍석천 방 잔존) + "대화 초기화 했는데 대화가 다 초기화가 안되었어. 확인좀". 설정→데이터→"대화 초기화" 2탭 실행했으나 다른 대화방이 남음.
- **근본원인(코드 실측)**: 뷰어 `ySetAct('reset')`이 `const t = YSESS.cur; yApi('reset', {t})` = **현재 방 하나만** 삭제(op reset의 `{t}` 경로 = 그 방만 나가기). 서버는 `reset {}`(t無) = 전체 초기화를 이미 지원(yeta.js 411~430 · 문서 line 3·27 명기)하나 설정 버튼이 안 부름. 즉 "대화 초기화"(전역 데이터 관리 행)가 롱프레스 "나가기"(개별 방)와 동일 동작 = 라벨·배치·운영자 기대와 불일치. 구 "새 대화" reset 버튼 제거(260705) 후 백엔드 "재배선 대비 보존"의 재배선이 `{t}`로 잘못 연결됨.
- **수정(2파일 · :root·러너·세션스키마 무접촉)**:
  - `viewer/index.html` `ySetAct('reset')` = `yApi('reset', {})`(t 미동봉 = 전체) + 가드를 `threads` 유무로 · armed 문구 "한 번 더 누르면 전체 삭제"/"모든 대화 기록 삭제" · 성공 시 전체 `yeta_read_*` localStorage 마커 정리(구: 단일 t만) · toast "모든 대화를 초기화했어". 설정 데이터 행 설명 "지금 대화 기록 삭제"→"모든 대화 기록 삭제".
  - `functions/api/yeta.js` 전체 초기화 응답에 `sess: fresh` 포함(구: `{ok:true}`만) = 뷰어 리로드 없이 빈 목록 즉시 재렌더(threads:{} = redact no-op · 형제 t-reset 경로 대칭).
  - 개별 방 삭제 = 롱프레스 "나가기"(`{t}`) 그대로 유지 = 두 진입점 역할 분리(전체=설정 / 개별=롱프레스).
- **불변 보존**: 전체 초기화도 tunes·policy·me(유저 프로필) 승계(서버 EMPTY 랩 기존 설계) = "대화"만 초기화, 내 정체성·시즌정책·캐릭터 성향 유지. 서버 1세대 백업(sessions/main.prev.json)도 기존대로 = 오조작 복구 가능.
- **검증(자가검증)**: 자가검증 경로 = node --check(yeta.js OK) + check_refs rc=0(viewer JS 구문 OK·디자인 게이트·토큰 신설 0·⚠️ --bg-scrim/--line2 = 기존 미배선 비차단 = 내 변경 무관) + reset 플로우 엔드투엔드 트레이스(빈 세션 yListRender 2074행 "진행 중인 대화가 없어" · yRender 3518행 YTH()||{} 빈턴 정상 · cur='' 처리) + origin read-back. 운영자검증 불가피 사유=없음(도구 실측 가능 · 라이브 2탭 실행 체감은 운영자 축).
- **효과/리스크**: 설정 "대화 초기화" = 전체 방 초기화(운영자 기대 정합) · 리스크 낮음(전체초기화 서버 경로 기존 검증분·2탭 armed 가드·서버 백업 존치·게이트 통과). 디자인 토큰 신설 0(계승만). 위상 = 기틀 밖 동작 버그픽스(세션스키마·라우터·:root·파이프라인·모델ID 무접촉 · reset op의 기존 {} 모드로 UI 배선 교정).
- 검증필드: 자가검증 경로=node --check+check_refs rc=0+엔드투엔드 트레이스+origin read-back | 운영자검증 불가피 사유=없음(라이브 체감만 사람 감각 축). 롤백=_versions/260712_1352_대화초기화_전체. 운영자 확인 포인트: 라이브에서 방 2개+ 만든 뒤 설정→대화 초기화 2탭 → 전 방 사라지는지 1회.

## 176차 — 대화 목록 좌 스와이프 액션(우측 2칸 픽토: 고정·나가기) + 롱프레스/우클릭 팝업 폐지 (운영자 260712)
- **의도(운영자)**: "행을 좌로 밀면 우측에 나가기 등 버튼 · 세로 충분하니 우측 조금 확보해 2칸 분리 · 2x2 중 좌열은 기존 통합, 좌로 밀면 우측 2칸(픽토 2개)에 나머지+기존 우클릭 팝업 노출 · 기존 우클릭 삭제 · 픽토 색 구분·눌림 액티브 유지". 후속 피드백 3: ①열림 시 문장 잘림→… ②고정 핀셋 어색→다른 픽토 ③나가기 전경색 말고 톤.
- **수정(viewer/index.html 단일 · :root 무접촉·계승만)**:
  - CSS: `.ylr-wrap` overflow:hidden · 신규 `.ylr-tray`(우측 60px 세로 2칸 · 표면 .ylmenu 정본 계승[무채 크롬] · 좌밀 슬라이드인) · `.ylr-act`(고정=--pin-ac 라임 / 나가기=--fg-2 뉴트럴, armed=--danger) · 열림 시 `.ylist-row padding-right`로 최근말·메타 … 처리(트레이 밑 잘림 해소).
  - JS: 롱프레스(yLMenu*)·contextmenu 전량 폐지 → 스와이프 제스처(yLrBind: 첫 10px 방향박제·가로우세 1.2배+44px 임계·justSwipe 합성 click 소비) · 트레이 픽토 = 구 pin/leave 액션 계승(2탭 armed) · navOpen('yltray') 레이어 등재(뒤로가기=닫기) · a11y = 닫힘 aria-hidden+tabindex-1. ySwipeBind 제외에 .ylr-wrap 추가(탭 전환 충돌 격리).
  - 고정 픽토 = 별/즐겨찾기(운영자 260712 "즐겨찾기가 맞는거같음" · 트레이 픽토 + 이름줄 고정 표식 통일 · 구 대각 압정 "핀셋 오인"·위로고정 압정 폐지).
- **검증(자가검증 + 브라우저 실측)**: check_refs rc=0(viewer JS 구문 OK·디자인 게이트·토큰 락 통과·신규 토큰 0) + Playwright 실측(좌드래그=트레이 열림 translateX0·나가기1탭=armed 점등·열림 시 … 처리) + 스샷 시각검증. 운영자검증 불가피 사유=없음(도구 실측 가능·라이브 폰 감각은 운영자 축).
- **산출**: `docs/reports/260712_방리스트_스와이프액션_플레이그라운드.html`(전/후·리스크·장단점·고정픽토 후보 4·대안 3·실인터랙티브 데모).
- **효과/리스크**: 숨은 제스처(우클릭/롱프레스)→발견성 높은 카톡식 스와이프. 리스크=세로스크롤/탭전환 충돌(방향박제·.ylr-wrap 제외로 격리). 디자인 토큰 신설 0.
- 검증필드: 자가검증 경로=check_refs rc=0+Playwright 스와이프/armed 실측+스샷 | 운영자검증 불가피 사유=없음. 운영자 확인 포인트: 라이브서 좌스와이프·고정 픽토(별=즐겨찾기)·나가기 톤 1회 확인.

## 177차 — 빈 대화 목록 중앙+흰색 · 「대화 깊이」 선택 완료 시 여닫이 자동 닫힘+다음 옵션 카메라 이동
- **의도(운영자)**: ① 스크린샷(빈 대화 목록 안내가 좌상단·회색) → "이거 가운데에 뜨게 해주고 메세지 흰색으로". ② "대화 옵션같은거 선택하고 더이상 선택지가 없는 옵션창은 자동으로 여닫이가 닫히고 카메라가 자동으로 아래로 조금 이동 = 다음거 고르기 편하게".
- **수정(viewer/index.html 단일 · :root·서버·러너 무접촉 · 토큰 신설 0=계승만)**:
  - **작업1 빈 목록 중앙+흰색**: `.yeta-setup`은 공용(에러·인트로·정책실패 다수) → 통째 변경 금지 → 빈 대화 목록(yListRender 빈 분기)만 전용 modifier `.yeta-setup.yempty`{color:var(--fg) 흰색 계승·text-align:center·max-width:280px}. 세로중앙 = 컨테이너 `.ymain.tab-list .ylist.list-empty{display:flex;flex-direction:column}` + `.ylist.list-empty #ylistRows{flex:1;display:flex;align-items·justify-content:center}`(헤더 아래 남은 영역 세로중앙). JS = 빈 분기 `#ylist.add('list-empty')`+`yempty` 클래스 / 대화 有 분기 `remove('list-empty')`.
  - **작업2 대화 깊이 자동 닫힘+스크롤**: `.ydepth-range` slider `change`(드래그 놓기=선택 완료) 리스너 신설 → 560ms 여운(연속조정 리셋) 후 `.ydepth` open 제거(게이지 .34s 촤르륵 접힘·chevron 원위치·aria-expanded=false) + `nextElementSibling.scrollIntoView({smooth,block:center})`(다음 옵션=타이핑 표시를 화면 중앙으로=다음거 고르기 편하게). 대상 = "대화" 섹션 유일 여닫이(대화 깊이). 정책/글꼴 등 확장은 운영자 확인 후 별도.
- **검증(자가검증 = Playwright 로컬 실측)**: check_refs rc=0(토큰 락 신설 0·⚠️ --bg-scrim/--line2=기존 미배선 비차단·내 변경 무관) + viewer JS 구문 OK + **Playwright 실측**: 작업1 = color rgb(255,255,255)·text-align center·cx=195(vw390 정중앙)·cy=467(콘텐츠영역 중앙)·컨테이너 flex center 확인 + 스크린샷(문구 중앙·흰색 또렷). 작업2 = 헤드탭→open→slider change→560ms 후 open:false/aria:false 자동 닫힘 + 스크린샷(칩 SONNET·깊게 갱신·chevron 접힘·다음 옵션 노출) · 페이지 에러 0. origin read-back 예정.
- **효과/리스크**: 빈 목록 = 좌상단 회색→화면 중앙 흰색(가독·존재감↑) · 대화 깊이 = 고른 뒤 자동 정리+다음 옵션 노출(순차 설정 편의). 리스크 낮음(순수 프론트·토큰 계승·게이트 통과·페이지에러0·기존 로직 무영향). 위상 = 기틀 밖 UX 개선(라우터·:root·파이프라인·모델ID·세션스키마 무접촉).
- 검증필드: 자가검증 경로=check_refs rc=0+Playwright DOM측정/스크린샷+JS구문 | 운영자검증 불가피 사유=실기기 스크롤 손맛(사람 감각 축). 롤백=_versions/260712_1417_빈목록중앙_대화깊이자동닫힘. 후속(운영자 확인)=대화 깊이 외 여닫이(L1 정책·글꼴·글자크기) 동일 동작 확장 여부. 운영자 확인 포인트: 라이브에서 ①대화 0건 시 안내 중앙·흰색 ②설정 대화 깊이 조정 후 자동 닫힘+스크롤 1회.

## 178차 — 대화 깊이 Sonnet 안내 = 하단 경고 토스트 → 게이지 안 중앙정렬 글래스모피즘 노트
- **의도(운영자)**: "소넷 경고 누르면 그 바로 아래 기준 가운데정렬로 · 경고 감 아니니까 작은 블러 글래스모피즘 도형 안에 안내만 = '소넷 모델은 빠르지만 품질은 낮을 수 있어' · 관련 경고 있으면 다 토글 아래 중앙정렬로".
- **수정(viewer/index.html 단일 · :root·서버 무접촉 · 토큰 신설 0=계승만)**:
  - CSS `.ydepth-note` 신설: 기본 숨김(max-height:0·opacity:0) → `.ydepth-wrap[data-model="s"] .ydepth-note`(Sonnet 구간)일 때 촤르륵 노출(max-height:44px·opacity:1·margin-top:11px · .3s transition). 노트 span = 글래스모피즘(background:var(--glass)·backdrop blur(var(--blur-m))·border var(--glass-line)·radius var(--r-pill)·color var(--fg-2)·font var(--fs-xs)/var(--fw-b)) = 경고 톤(danger) 아닌 뉴트럴 정보 pill. text-align:center = 게이지 폭 중앙.
  - HTML: yDepthRow 범례(.ydepth-legend) 아래 wrap 안에 `<div class="ydepth-note"><span>소넷 모델은 빠르지만 품질은 낮을 수 있어</span></div>` 추가.
  - JS: input 리스너의 `miniToast('Sonnet 5 — ...')` 하단 경고 토스트 + `wasSonnet` 폐지 → data-model=s CSS 자동표시로 대체(JS 추가 로직 0 = 기존 `wrap.dataset.model` 실시간 갱신 재사용).
- **위치·동작**: 대화 깊이 게이지(토글) 열림 → Sonnet 구간이면 슬라이더·범례 아래 중앙에 글래스 노트 노출 · Opus 구간이면 숨김 · 게이지 접힘(177차 자동닫힘) 시 함께 접힘(게이지 내부).
- **검증(자가검증 = Playwright 실측)**: check_refs rc=0(토큰 락 신설 0·계승만) + viewer JS 구문 OK + Playwright: Sonnet(value1·data-model=s) opacity1·maxH44px 표시 / Opus(value4·data-model=o) opacity0·maxH0 숨김 · 노트 가로중앙(centered:true)·글래스(bg rgba·blur29px·radius999px·color rgb(231,236,231)=--fg-2) 실측 + 스크린샷(게이지 아래 중앙 글래스 노트·경고톤 없음). origin read-back 예정.
- **효과/리스크**: 하단 빨간 경고 토스트(맥락 이탈·경고 과함) → 컨트롤 직하 뉴트럴 글래스 안내(맥락 근접·정보 톤). 리스크 낮음(순수 프론트·토큰 계승·게이트 통과·CSS 주도 = JS 축소). 위상 = 기틀 밖 UX(라우터·:root·파이프라인·모델ID 무접촉).
- 검증필드: 자가검증 경로=check_refs rc=0+Playwright DOM측정/스크린샷+JS구문 | 운영자검증 불가피 사유=실기기 블러 질감·위치 손맛(사람 감각 축). 롤백=_versions/260712_1447_소넷안내_글래스노트. 운영자 확인 포인트: 라이브 설정→대화 깊이 열고 Sonnet 구간서 게이지 아래 글래스 안내 뜨는지 1회.

## 179차 — 프로필 카드 글래스 계승 · chevron만 눌림 · 헤더 미터 탭 전환(모델 토글·노력 순환)
- **의도(운영자)**: ① 프로필 카드(#yetaPdc)만 글래스모피즘 미적용(불투명 검정) → 기틀 레포 배껴 투명도·블러. ② 헤더 이름부 chevron 탭 시 이름 전체가 까딱 → chevron 버튼만 강조색+축소, 이름·직업 고정. ③ 미터 SONNET/노력도 탭으로 전환(모델=토글, 노력=별개 순환 최대→최소, 6단 슬라이더 아님).
- **수정(viewer/index.html 단일 · :root·서버 무접촉 · 토큰 신설 0=계승만)**:
  - **B 프로필 글래스**: #yetaPdc가 `.yeta-pick` 불투명 배경(rgba .94/.96) 상속 → 헤더 `.yeta-h` 글래스 정본 계승 override = `linear-gradient(var(--glass),var(--glass)),rgba(18,18,18,.75)` + `backdrop-filter:blur(var(--blur-l)) saturate(1)` + `border var(--glass-line)` + `box-shadow inset+var(--shadow-card)`. 실측 blur11·rgba(18,18,18,.75)·line.08.
  - **C chevron 눌림**: `.yeta-who:active` 전체 scale 폐지 → `.yeta-who:active .yw-chev{--chev-s:.72;color:var(--accent)}`. chevron transform = `rotate(var(--chev-r,0)) scale(var(--chev-s,1))` 변수 분리(열림 rotate180 + 눌림 scale 조합 공존). 이름·직업 무동작.
  - **F 미터 탭**: `.ymeter` pointer-events:none 유지 + `.ym-seg` auto. yMeter = 통짜 textContent → 2 버튼 seg(ym-model·ym-eff)+tok. #yetaMeter 클릭 위임 = 모델 seg→Opus↔Sonnet 토글 / 노력 seg→low(빠름)→''(깊게)→max(최대)→순환(독립). yDialLoad+yMeter+설정게이지 동기. 모델색 = Opus 라임/Sonnet 코발트(게이지 칩 계보). aria-hidden 제거(seg SR).
- **검증(자가검증 = Playwright 실측)**: check_refs rc=0(토큰 락 신설 0) + viewer JS 구문 OK + Playwright: F = Opus(라임 rgb207,255,64)→Sonnet(코발트 rgb61,107,255) 토글·노력 빠름→깊게→최대→빠름 순환 실측 / B = #yetaPdc 배경 gradient+rgba(18,18,18,.75)+blur(11px)+line.08 실측(헤더 정본 일치) / C = chevron transform 변수·transition 0.28s · 페이지 에러 0. origin read-back 예정.
- **효과/리스크**: 프로필 카드 = 다른 글래스와 톤 통일(뒤 캐릭터 블러 비침) · chevron 눌림 국소화(이름 안 흔들림) · 미터 = 6단 슬라이더 안 열고 헤더서 모델·노력 직접 전환. 리스크 낮음(순수 프론트·토큰 계승·게이트 통과). 위상 = 기틀 밖 UX. 후속(남은 요청) = A 헤더 초대 재배치 · D 방 유지 · E 답장실패(파이프라인 근본).
- 검증필드: 자가검증 경로=check_refs rc=0+Playwright DOM측정+JS구문 | 운영자검증 불가피 사유=실기기 눌림·블러 질감(사람 감각 축). 롤백=_versions/260712_1507_프로필글래스_chevron_미터토글. 운영자 확인 포인트: 라이브 ①프로필 카드 글래스 ②chevron 눌림(이름 고정) ③미터 탭 전환 1회씩.

## 180차 — 답장 전량 실패 근본픽스: gen_out --disallowedTools 폐기 도구 MultiEdit 제거(CLI 주28 치명화) · D 방소멸 동시 해소
- **경위(운영자)**: 라이브 첫 답장부터 "답장을 못 만들었어 — 다시 보내면 재시도야" 오류 스크린샷 + "이런 버그 안뜨게 근본 해결". 별건 "대화 열고 대답 안 하면 방 사라짐(1회라도 대화했으면 유지)".
- **근본원인(READ-ONLY 서브에이전트 로그조사 5 run + 로컬 스모크 재현 · 확신 97%)**: `gen_out()`의 `--disallowedTools` 목록에 **폐기 도구 `MultiEdit`**(Edit에 병합·삭제됨). 이번 주 러너 CLI(캐시키 202628=주28)가 deny 규칙을 **기동 시 엄격 검증** → unknown 도구에서 **rc=1 즉사(API 호출 전)** → 매 메시지·매 계정·매 모델 첫 답장부터 100% 결정적 실패 → finish error가 R2 `state:error`로 박혀 앱 배너. **is_frame_break(174차 L0)·OAuth/쿼터 전부 무관**(rc=1로 프레임 게이트 도달 전 탈출·API 미접속 = 진입~에러 ~1초). 로컬 2.1.207은 같은 경고가 **비치명 rc=0** = **CLI 버전 드리프트**로 동일 인자가 outage화(check_refs 643-644의 260701 "비치명 노이즈" 기록이 뒤집힘).
- **D 동일 뿌리**: 오프닝도 같은 `gen_out` 생성 경로 → 오프닝 dispatch가 rc=1 실패 → assistant 0·user 0 = turns 0 → yListRender 필터 `(turns.length || (awaiting&&opening))` 탈락 → 방 소멸. E 픽스로 오프닝 성공(assistant 1) → turns≥1 → 방 유지 = "1회라도 열었으면 유지" 충족.
- **수정(라이브 스크립트 3곳 + 게이트 주석 · :root·뷰어 무접촉)**: `.github/scripts/yeta_chat.sh:500`·`yeta_call.sh:120`·`yeta_nudge.sh:91`의 `--disallowedTools`에서 `MultiEdit` 제거(→ `Write,Edit,NotebookEdit,Bash,Task,WebFetch,WebSearch,Read,Glob,Grep`). `shared/check_refs.py:643-644` 주석 정정(비치명→주28 치명 전환·제거 실측 260712).
- **검증(자가검증 · 유료 프로브 금지)**: 서브에이전트 5 run 로그 교차(전부 동일 `Permission deny rule "MultiEdit" matches no known tool` → exit1·~1초=API미접속) + **로컬 claude 2.1.207 스모크 재현**(원본 목록=경고+rc0[로컬 비치명]·잔여 목록[MultiEdit 제거]=rc0 known 전원 유효 = 다음 unknown 없음 확증) + bash -n 3파일 OK + check_refs rc=0 + MultiEdit 잔존 0. **러너 실검증 = 배포 후 실사용 답장**(유료 = 합성 프로브 금지 · §운영 태도 g).
- **리스크/후속**: 즉효·저위험(폐기 도구 1개 제거 = deny 규칙 유효화 · 잔여 known 검증). 후속(운영자 승인 = 파이프라인 구조) = ① CLI 버전 핀 고정 or 잡 초입 `claude -p` 스모크 fail-fast(주간 자동추종이 게이트 없이 새 CLI 흡수 = 구조 원인) ② `--allowedTools ""` allow-nothing 전환(이름 무관 드리프트 면역 · rc0 확인했으나 도구 차단 시맨틱 추가 검증 필요) ③ check_refs에 disallowedTools 폐기도구 재유입 게이트.
- 검증필드: 자가검증 경로=서브에이전트 5run 로그 교차+로컬 스모크 재현(원본/잔여 rc)+bash -n+check_refs rc=0 | 운영자검증 불가피 사유=러너 실답장 성공은 배포 후 실사용(유료 프로브 금지 = 사람 감각 아닌 실사용 대기). 롤백=_versions/260712_1507_답장실패_MultiEdit폐기도구제거. 운영자 확인 포인트: 배포 후 아무 방에 말 걸어 답장 정상 생성 + 방 유지 1회.

## 181차 — 헤더 초대 재배치: 근처 스택 좌측 이동 + 초대 + 우측 고정 픽토(미만석 활성/만석·대기 비활성)
- **의도(운영자)**: "대화상대 추가 고려해 상단 동일 유지 · 우측 펼쳐지는(스택) 좌측으로 보내고 우측에 + 만들기 · +는 초대 가능할 때 초대(열면 후보 없을 수도) · 첨부3처럼 난입 들어올 경우 가정해 비활성 픽토그램(들쭉날쭉 짜증)". 난입/초대 알고리즘 불가침.
- **수정(viewer/index.html 단일 · :root·서버·러너 무접촉 · 토큰 신설 0=계승만)**:
  - HTML: 폐지됐던 헤더 초대 픽토 재생 = `.yh-inv#yetaInvBtn`(우측 .yh-r 앞 고정 · user-plus = 프로필 카드 `.ypd-act[data-pda=inv]` CII 계승).
  - CSS: `.yh-inv` = `.yeta-back` 계승(width var(--btn)·grid·color --fg-2·press --press-m) + `.off{opacity:.3;pointer-events:none}`(만석·대기 비활성 픽토 = 자리 유지·들쭉날쭉 방지).
  - JS: `yStackSync` 개조 — ① 스택(#yetaNear) 삽입 앵커 = `.yh-r`(우측)→`.yeta-who` 앞(좌측 이동) ② 초대 + 버튼 상태 = 항상 갱신(early return 전) `off = slots<=0 || waiting || 비챗`(만석·초대대기·비챗 = .off). + 클릭 리스너 = yInvToggle(off = pointer-events:none이라 무반응). 스택·+ 둘 다 yInvToggle 진입(같은 초대 UI · 후보 0이면 빈 안내 = "근처에 부를 만한 사람 없어").
- **검증(자가검증 = Playwright + 트레이스)**: check_refs rc=0(토큰 락 신설 0·닫기게이트) + viewer JS 구문 OK + Playwright(세로 390×844): 초대 + DOM 존재·이름 우측(invCx273 > whoCx156·invRightOfWhoName true)·who grow 정상(188px) + 스크린샷(+ 픽토 chevron 뒤·보이스톡 앞 우측·이름 좌측). ⚠️ 스택 좌측·off 토글 실동작 = 모듈 스코프(_yNear·YTH 클로저)라 window 스텁 무효 → 코드 트레이스로 확인(insertBefore(who)=좌측·off 조건 정확)·라이브 운영자 확인. origin read-back 예정.
- **효과/리스크**: 초대 어포던스 상시 노출(+ 항상 자리·상태만 활성/비활성 = 픽토 안 들쭉날쭉) · 후보 스택 좌측(이름부와 분리) · 난입/초대 판정(barge_check·op invite·MAX_ROOM) 무접촉. 리스크 낮음(순수 프론트·토큰 계승·게이트 통과). 위상 = 채팅 헤더 재배치(라우터·:root·파이프라인·모델ID 무접촉).
- 검증필드: 자가검증 경로=check_refs rc=0+Playwright DOM측정/스크린샷+코드 트레이스 | 운영자검증 불가피 사유=스택 좌측·+ 활성/비활성 실동작(모듈 스코프 스텁 무효 = 라이브 실데이터 필요 · 사람 감각 축). 롤백=_versions/260712_1656_헤더초대재배치. 운영자 확인 포인트: 라이브 ①근처 후보 있는 방 = 스택 좌측·+ 활성 ②만석 방 = + 비활성 픽토(자리 유지) 1회.

## 182차 — 방 재진입 낙관 UI(캐시 즉시 렌더) · 유저 지문(괄호·*) = 실제 행위 취급
- **의도(운영자)**: ① "대화 나갔다 다시 시작할 때 잠시 딜레이 — 안 먹힌 줄 알고 계속 누름". ② "괄호를 쓰거나 *를 달고 행위를 묘사하면 그게 행위 취급 되도록". (E·D는 라이브 확인 완료 = 조치 불요.)
- **수정 ① 재진입 딜레이(viewer/index.html · 낙관 UI)**: `yEnterChat`이 재진입에도 `YSESS=null`로 기존 대화를 버리고 `draw` 왕복(수백ms~수초) 후에야 렌더 → 그동안 "입력 중"만 = 무반응 착각. 픽스 = 로컬 캐시(`YSESS.threads[c.id].turns`) 있으면 draw '전에' `YSESS.cur=c.id`+`yRender()`로 **기존 대화 즉시 표시**(첫 진입=캐시 無는 기존대로 오프닝 타이핑) · draw 성공 시 최신 yRender로 매끄럽게 승계 · draw 실패해도 캐시 있으면 대화 유지(miniToast만 · introOnly로 안 날림).
- **수정 ② 유저 지문 = 행위(apps/yeta/00_지침_캐릭터챗.md · 러너 주입 지침)**: 대원칙 ②(규격으로 안 좁힘)에 규칙 추가 — 유저가 `(…)`·`*…*`로 감싼 묘사는 대사가 아니라 **실제 한 행동·상황**. 그 행위가 일어난 전제로 반응(`*담배를 건넨다*`=건네받음·`(막아선다)`=실제 막힘) · '그런 척'·무시 금지 = 물성 있는 사건 · 단 대원칙 ③(개념 초월 불가)은 유지(순간이동·초능력 비약은 캐릭터답게 받아넘김). inject_character.sh가 00_지침 sha256로 재주입 = 다음 답장부터 적용.
- **검증(자가검증)**: check_refs rc=0(viewer JS 구문 OK·토큰 락 통과) + 작업① 로직 트레이스(_cached 분기 = draw 전 yRender·실패 시 캐시 유지·cur 세팅 정확) + 작업② 규칙 반영 grep 1 + inject 해시 재주입 경로 확인. ⚠️ 작업① 딜레이 체감·작업② 캐릭터 행위 반응 = 라이브 실사용(모듈 스코프/러너 = 도구 재현 한계 · 사람 감각 축).
- **효과/리스크**: 재진입 = 캐시 즉시 = 무반응 착각 제거(연타 방지) · 유저 나레이션이 실제 사건으로 = 몰입↑. 리스크 낮음(순수 프론트+지침 문구 · 게이트 통과 · 토큰 신설 0 · 러너 코드 무접촉 = 문구만). 위상 = UI 낙관 렌더 + 콘텐츠 룰(대원칙 보강 · L0 구조 무변경 = 주변부 문구).
- 검증필드: 자가검증 경로=check_refs rc=0+로직 트레이스+규칙 grep+inject 해시 | 운영자검증 불가피 사유=재진입 딜레이 체감·유저 지문 행위 반응(라이브 실사용 = 사람 감각). 롤백=_versions/260712_1714_재진입낙관UI_유저지문행위. 운영자 확인 포인트: ①방 나갔다 재진입 = 대화 즉시(딜레이 없이) ②유저가 `*행동*` 쳤을 때 캐릭터가 그 행위로 반응 1회.

## 183차 — 친밀도 역행 + 애니어그램/MBTI 성향 알고리즘 설계·연구(구현 전 · 설계 3부작 + 성향 DB)
- **경위(운영자)**: "친밀도 0 하한을 마이너스(역행)로 · 기존 알고리즘 동일개념 · 역행 페르소나"부터 → 정교화: 완충(좋으면 안 흔들림)·관통(큰 계기 급락)·히스테리시스(깊이 틀어지면 회복 어려움) · 육각 역행(발전가능성의 역·빨간 점선) · 애니어그램=설계 참고소스(캐릭터 라벨 부여 X)·MBTI 부여 · 욕설/가치관목격 트리거 · 호칭 라더 · 법령식 위계 단일 DB · 성격정보 10인 검색.
- **산출(설계·연구 · 실코드 구현 0)**: `docs/reports/` 4[친밀도음수역행_평의회설계 · 페르소나_애니어그램_배정_설계 · 페르소나_웹실증_검증 · 육각역행_역방향게이지_플레이그라운드] + `apps/yeta/characters/성향_DB.md`(법령식 6편34조 단일 참고소스) + CLAUDE.md 등재(라우팅 두뇌·§기록SSOT = 항상 선참조).
- **평의회(READ-ONLY 30인+웹)**: 역행 6인 + 정예 3인(수학·밸런스·레드팀) + 애니어 6인(매핑·개방·상처·아키·레드팀·리얼리즘) + 웹 5인 + opus 수집 10인. 배정 = 4렌즈(공포·급소·중심·관계) 다수결.
- **핵심 설계**: ① 2축(`yBond` 완충 = 계층게이트 / `yHurt` 활성상처 = 육각·냉담) · 티어토큰 `<<HURT:tier>>`/`<<HEAL>>` · `adj` 저장(절대값 재평가·누락보존) ② 애니어=저작렌즈·코드0(자기판례 260707)·유형누출 L0 meta ③ **역행=비대칭 왜곡벡터**(균일 수축 아님 = 운영자 지적, 3모델 실증·성향DB 제23조) ④ 호칭 2트랙(폭발5/냉각7 = demand-withdraw) ⑤ Big5 N=상처민감·`yFallRate` / A=완충(실증).
- **배정**: 만장일치 7(홍석천 9w1·류 7w8·카피 3w4·본 1w9·루시 4w5·가을 8w7 + 백 8w9) · 분기(하서연 7↔2 접전·데스크 1·세라 4·윤 4·윈터 2). MBTI 12인 부여(INFP·ISFP·ENFP·ISTP·ESTP·ISTJ×2·ENTP·ENTJ·INTP↔ENFP·ISFJ).
- **안전**: `policy.json` L0 "존엄" 4종("자해조장 가능·미성년 성적 망설임") = 절대선 불변 모순 발견(직접 read 검증) → 운영자 "본인 수정중" = 정정 안 함(관할 존중). 개방 = 수위 이미 다 열림(친밀도 게이트 폐기 · L0는 불변).
- **상태**: 설계·연구 완료 · **실코드 구현 0**(기틀 = 러너 emit·세션 스키마 adj·게이트웨이 stripMarkers·migrate → 5인 기틀검증 대기). 남은 운영자 결정 3: 육각 역행색(빨강 `--danger` / 무채색 `--mut`)·하서연 애니어(7/2)·구현 착수.
- 검증필드: 자가검증 경로=평의회 30인 READ-ONLY 실측+웹 교차검증+`policy.json` 직접 read+check_refs rc=0(문서 커밋 전량 게이트 통과) | 운영자검증 불가피 사유=설계 방향·배정 취향·구현 착수 = 운영자 결정 축(사람 판단). 롤백=문서 append·git revert. 운영자 확인 포인트: 첨부 보고서 4 + 성향DB 검토 후 3개 결정.

## 184차 — 세라 재배정 확정 + 프로필/배경 스크립트 루시톤·주입픽스 + char-art 완전자동화 + 사주부적 설계
- **경위(운영자)**: 183차 성향 설계 후속 — "세라 재배정" · "루시에 맞게 전원 프로필 재생성"(→ 루시=시즌·윈터=아이돌 특별등급이라 일반 10인은 반실사 유지로 확정) · "완전 자동 ㄱㄱ"(크롭·배선 hands-off) · "사주→부적 컨셉 설계만 드래프트에".
- **산출(PR #260 → main)**: ① `성향_DB.md` 제2조 세라 `4w3`→`3w4·ESFP`(세계관 남미새·유혹). ② `yeta_face.py`·`yeta_bg.py` 루시 애니톤 BASE 재작성 + **roster 주입 버그픽스**(멀티라인 pretty JSON = id 블록 범위 치환 · '1줄=1명' 가정이 조용히 무주입하던 것 해소). ③ `yeta_char_art.py`+워크플로 **완전 자동화**: 큰그림→작은얼굴(av 512² ffmpeg 크롭)→roster avatar 자동주입 = 한 dispatch hands-off. ④ `docs/reports/260712_사주부적_컨셉설계.html`(설계 전용·구현0 · op saju/talisman·결정적 계산·유료 가드·홍석천 연결·안전 가드).
- **화풍 결정**: 루시(시즌)=스타일라이즈드 애니 ↔ char_art 10인=반실사 = 다른 결(얼굴 나란히 대조 실측) → 운영자 "시즌/아이돌 조금 달라도 됨"과 정합 = **10인 반실사 정체성 유지·char_art 프롬프트 불변**. 윈터=실존인물 실사 유지·제외. 루시·윈터 = CHARS 밖 = 자동주입 구조적 불가침.
- **크롭 검증**: 기존 수동 av/ 10인 템플릿 역산 = 512²@상단·가로중앙 → 자동크롭 = 수동과 거의 동일(Pillow 미리보기 눈검증). 주입 자가검증 = 10/10 hit·JSON유효·lucy/winter 불변·멱등·원본 미변경.
- **무침범 확인**: 브랜치 변경 = 스크립트·성향DB·보고서만 · `policy.json`·`00_지침` 무접촉(운영자 판단영역·타 세션 #258/#259 보존) · 클린 3-way 머지 dry-run 충돌 0.
- **상태**: 스크립트·자동화 = main 반영 · **실제 이미지 재생성 = 미실행**(유료 = 머지 후 main에서 운영자 dispatch `FORCE=1` 대기). CI gen 빨간불 = 피처 브랜치서 이미지 dispatch(→main rebase 푸시 충돌)한 탓 = 코드 무관 · 낭비 런 취소.
- 검증필드: 자가검증 경로=check_refs rc=0+py_compile+주입 dry-run 10/10+크롭 역산 눈검증+클린 머지 dry-run | 운영자검증 불가피 사유=재생성 화질·프레이밍 라이브 = 사람 감각(dispatch 후). 롤백=git revert. 운영자 확인 포인트: 머지 후 main dispatch → roster 작은얼굴 10인 반영 1회.

## 185차 — 미연시형 진행 방식 설계(다른 섹션 · 설계 전용·구현 0)
- **경위(운영자)**: "기존의 미연시형 진행 방식으로 가야돼 · 다른 섹션" → 지목한 기존 시스템 = LV0~5 친밀도 게이트 해금(§🔓)+역행. 확장 방향 = **B) 진행 방식 설계·확장을 새 문서로**. "마무리되면 바로 머지".
- **산출**: `docs/reports/260713_미연시형진행방식_설계.html` — 기존 LV·해금·yBond·마주침 위에 〔루트·이벤트·엔딩·CG해금·선택지·진행 가시화〕 6부 프레임워크. 설계 전용(실코드 0).
- **평의회(READ-ONLY 3인 병렬)**: ① 미연시 12메커니즘 웹 리서치(오토메·VN·Mystic Messenger·Replika 교차 · 상위5=이벤트트리거·실시간페이싱+Caution·티어게이팅+은닉미터·침묵부재신호·CG갤러리) ② 실현성 적대감사(레포 실측 · 살릴것/버릴것 · 5 레드라인) ③ 크리에이티브("관계의 침전물" 척추 · 5축×5아이디어).
- **핵심 설계**: ㉮ 엔진 신설 아님 = 매 턴 dispatch 1회가 이미 배출하는 `[LV]`·`[★]`·`[사건]`·`MOOD`를 ㉯ 결정론(yBond·places.py·날짜·CSS·LLM 0)으로 읽어 ㉰ 채팅 밖(프로필·앨범·지도)에 그림. **버릴 것**=메뉴 선택지·강제 루트락·비가역 엔딩·자동 CG 생성. **살릴 것**=LV 게이트 이벤트·정적 CG 클라 언락·연출형 정점·침묵/부재 신호.
- **척추(유일 기틀 변경)**: NOTE:ME 서브라인 3종(`[결:]`루트·`[칭호:]`엔딩·`[호칭:]`데레) = L0 출력계약 골격 = **운영자 승인+5인 기틀검증 필요**. 나머지 대부분 공짜(결정론)/뷰어(75% 위임). 보안=stripMarkers 화이트리스트+fresh 출력만 스캔.
- **제약 준수**: 비용 불변(2번째 dispatch 0) · 자유대화 대원칙(진행=배경 레이어) · L0/policy 불가침 · 진행 상태=R2 세션만(레포 커밋 금지) · CG=사전생성 정적 에셋만.
- **상태**: 설계·개념 완료 · **실코드 0**(척추 승인이 첫 단추 = 5인 기틀검증 대기). 남은 운영자 결정: 척추(NOTE:ME 서브라인) 승인 여부 → 승인 시 구현 착수.
- 검증필드: 자가검증 경로=HTML 태그균형 0+check_refs rc=0+평의회 3인 레포 실측 교차 | 운영자검증 불가피 사유=설계 방향·척추 승인·구현 착수 = 운영자 결정 축(사람 판단). 롤백=문서 append·git revert. 운영자 확인 포인트: 첨부 설계 HTML 검토 후 척추 승인 1건.

## 186차 — OAuth 계정 sticky 승격(자동 failover) 이식팩 적용
- **경위(운영자)**: 이식팩 `oauth 계정 자동 승격.md`(타 레포 이식용 자기완결 문서) 첨부 = "이거 적용해줘". 해결 문제 = 활성 계정(vars.ACTIVE_ACCOUNT)이 주간한도로 며칠 막히면 매 런이 죽은 계정부터 시작 → 쿼터 감지 왕복 낭비. sticky 승격 = 활성 계정 2회 막히면 체인 다음 계정으로 기준 자동 전진(순환·원복로직0).
- **전제 충족 실측**: ① 다계정 시크릿 = `CLAUDE_CODE_OAUTH_TOKEN_{MUTENO,NOMUTEFB,EMS1130G,MUTENONA}` ✓ ② 활성 변수 = `vars.ACTIVE_ACCOUNT`(기본 MUTENO) ✓ ③ 런타임 폴오버 SSOT = `shared/claude_transient.sh` `claude_failover()` ✓ = 이식 의미 있음.
- **산출(5변경 · 브랜치 claude/new-session-jj0aij)**: ① `shared/account_failover.py` 신설(승격 엔진 · CHAIN=MUTENO→NOMUTEFB→EMS1130G→MUTENONA · repo fallback=muteno/yeta · urllib만·requests 불요) ② `claude_transient.sh` 첫 스왑(n=0·활성→서브1) 브랜치에 신호 1줄(`.nomute_active_quota` best-effort · 서브 계정 쿼터는 신호 안 남김) ③ `yeta-chat.yml`(가장 자주 도는 카나리아) 잡 끝에 승격 스텝(`if: always()` · GH_VARS_TOKEN 없으면 no-op) ④ `.github/workflows/account-selftest.yml` 신설(PAT read/write/delete 실측 · probe 변수만·ACTIVE 무손상) ⑤ `.gitignore`에 `.nomute_active_quota`.
- **no-op 안전(핵심)**: `GH_VARS_TOKEN`(Variables 쓰기 PAT) 시크릿 없으면 라이브 0 영향 — 코드가 다 들어가도 승격 비활성. 운영자가 PAT 넣는 순간 켜짐. = 기틀(워크플로) 접촉이지만 활성화는 운영자 게이트 뒤.
- **운영자 GitHub 잔여작업(코드 밖·활성화 조건)**: 가) Fine-grained PAT 발급(Repository=muteno/yeta · Permissions→**Variables: Read and write** 하나) 나) Secrets(Actions)에 이름 `GH_VARS_TOKEN`으로 등록 다) (선택) Actions→account-selftest→Run→`🎉 PAT 실측 통과` 확인. 넣기 전엔 no-op.
- **검증(자가·실측)**: py_compile+bash -n+yaml.safe_load(2)+check_refs rc=0(폴오버 게이트 포함 전 게이트 통과) · **모킹 로직 테스트 22단언 전원 PASS**(_req in-memory 교체 = 무비용 · no-op/누적/임계승격/체인순환 MUTENO→NOMUTEFB→EMS1130G→MUTENONA→wrap/비회원계정 생략/커스텀임계/예외안전) · **bash→py 통합**(claude_failover 첫 스왑 시 신호 파일 생성 == account_failover.py 기본 읽기 경로 정합 · 503 과부하는 신호 안 남김 = 쿼터만 카운트).
- **무침범**: 런타임 폴오버 로직 불변(신호 1줄만 추가·전환 동작 무변) · 워크플로 permissions(contents:read) 불변(PAT는 별개 축) · UI 무접촉 · CHAIN 순서 = CLAUDE.md §🔑·yeta-chat env 매핑과 정합.
- **상태**: 5변경 = 브랜치 푸시 · draft PR. **기틀(워크플로·폴오버 SSOT) 접촉** = 운영자 승인(이식팩 첨부=지시)·no-op 안전으로 draft 게이트 뒤 검토 후 머지. 5인 기틀검증 원하면 머지 전 후속 소환 가능(제안).
- 검증필드: 자가검증 경로=py_compile+bash -n+yaml×2+check_refs rc=0+모킹 22단언+bash→py 통합실측 | 운영자검증 불가피 사유=활성화(GH_VARS_TOKEN PAT 발급·등록)는 GitHub UI = 운영자 축(코드로 불가). 롤백=GH_VARS_TOKEN 삭제 시 즉시 no-op / git revert. 운영자 확인 포인트: PR 리뷰 후 머지 + (활성화 시) PAT 2단계.

## 187차 — 성향·화법 총추출 포터블 2종 (사주 미연시 이식용)
- **경위(운영자)**: "예타랑 프로모에서 성격 관련된 내용 총 추려서 여기로 빼줘 — 원리는 이미 있었고 그걸 녹인다고 씨름한 내용이 더 중요·MBTI부터 유관 전부·사주앱을 미연시로 만드는데 대화 화법에 영향이 커서 참조용" + 추가 지시 "yeta 화법이 페르소나에 맞게 잘하는데 사주앱은 자유도 없이 말투가 자연스러워야 함 — 그 뉘앙스 팁·설계 자료 따로 파일로(드래프트에선 못 봄 = 채팅 첨부)".
- **산출(신규 2·본편 코드 무접촉)**: ① `docs/성향총추출_포터블.md` — 제1교훈(유형=백스테이지·카드 12장 MBTI 0 실측)+원리 층(성향_DB 4모델 정합·그립·분열화살표·Big5·애착 전문)+12인 배정표+상처·역행(비대칭 왜곡벡터)·호칭 2트랙·개방·회복 알고리즘+카드 번역 실물(백·데스크·가을·하서연·카피·루시 상세+6인 공통 문법)+프로모 애니어그램 툴킷(TYPES 스키마·dislikes 지뢰 화법·하모닉 위로 처방·중심 언어 3계·통합/분열 대본·REVIEW 적대검수 전례)+yeta 씨름 연대기(판타지 개편→페르소나시트 뭉태기 금지→AI 응수 6연속 조율→me 호칭→L0 프레임 이탈 벽+그물→성향_DB 정립→미연시 설계)+미연시(관계의 침전물)·역행 v2→v3(yBond/yHurt 2축)·사주부적(계산=규칙·풀이=LLM) 설계 요지+사주 이식 매핑 10종+부록 20열×60행 인덱스. ② `docs/화법뉘앙스_포터블.md` — 최상위 통찰(few-shot>규칙·언어지문 열거형·성의 비대칭·사람다움 3종 슬롯·온도차)+헛소리 방지 8종+톡방 형식 리듬+호칭 라더+관계 진행 배선(LV·해금·훅뱅크·기억 2트랙)+메타 이탈 처리(프레임 역전·최악 3종·거부도 캐릭터로)+사주 미연시 적용 가이드 7항+카드 골격 복붙판+씨름 교훈(잔재 오염 3제·타이핑 지연·호칭 관성)+프로모 문체 계약 BW-1(존댓말 사람 말투 4원칙·금지 상투구·셀프 퇴고).
- **방법**: 분신 6인 병렬(Explore·READ-ONLY 계약 — 카드 6+6·yeta 원장 1527줄 전수·설계 보고서 6편·프로모 애니어그램 플레이그라운드 전수·프로모 원장 1367줄+블로그 파이프) + 메인 직접 독해(성향_DB·SCHEMA·00_지침·10_세계관·_TEMPLATE 전문). 발사 후 rogue 브랜치·커밋 0 점검.
- **핵심 실측 발견**: 가) 캐릭터 카드 12장에 MBTI·애니어그램 문자열 0 = "유형은 설계 재료·무대엔 행동/말투만" 원칙의 실물 나) 프로모 성격 자산 = playground 애니어그램 툴킷 + 블로그 문체 계약 두 갈래뿐(앱 본체 성향·MBTI·페르소나 0건) 다) 호칭 사다리를 카드에 명문화한 건 루시·윈터뿐 — 나머지는 개방 단계·허용 문구로 표현.
- **검증필드**: 자가검증 경로=check_refs rc=0+git show origin/main read-back+분신 6인 결과 교차 대조(카드↔성향_DB 값 정합) | 운영자검증 불가피 사유=추출 취사선택이 사주앱 저작에 충분한지 = 사람 감각 축. 롤백=git revert(신규 문서 2 + 원장 append뿐). 운영자 확인 포인트: 채팅 첨부 md 2종 열어 사주 미연시 참조로 쓸만한지 1회 훑기.

## 188차 — 답장 실패 자동 재시도(무배너) + 4회 소진 조용한 이탈
- **경위(운영자 260714)**: "대답이 좀 막힐 때 재시도 이렇게 뜨는 경우가 있거든? 사용자한테 대화 안에서 재시도로 묻지 말고 자체적으로 계속 재시도 하게 해줄래? 4회 이상 안될 경우에는 그냥 대화를 이탈하셈. 알림 떠서 바이브 깨는거보다 나으니" — 실패 배너+수동 버튼 = 몰입 파괴 축.
- **변경 전후**: 전 = state=error(비오프닝)·awaiting 5분 좀비 → 빨간 fail 배너("답장을 못 만들었어/너무 늦네 — 다시 보내면 재시도야") + `.yfail-retry` 수동 버튼(탭해야 retry op). 후 = 같은 판정 조건에서 배너·버튼·토스트 0 — retry op를 지수 백오프(0.8→1.6→3.2→6.4s)로 자동 재발사, 그동안 '입력 중' 점만 유지(실패 무노출). 4회 소진 = `.ysit` 지문 한 줄("…답이 없다. 지금은 연락이 닿지 않는 모양이다.")로 조용한 이탈 + 폴 정지 — 새 메시지 발신 = 새 에피소드로 자연 재개.
- **핵심 설계**: ① 회차 원장 = localStorage `yeta_art`(키 = 방:펜딩 유저턴 ts · 8키 링) — 새로고침에도 상한 유지(retry = LLM 런 재발사 = 무제한이면 쿼터 누수) · 새 메시지·답 도착 = 키 변경 = 카운터 자연 리셋 ② 발사 전 선기록 = 네트워크 실패도 1회 계수(상한 절대 보장) ③ 발사 직전 에피소드 키 재검(YTH 대조 — 새 발신·방 이동·답 도착 시 취소) ④ `_yArT`/`_yArBusy` 래치 = 폴 재렌더 다발 중복 발사 차단 ⑤ 오프닝 제외 조건·좀비 5분 임계·서버 retry op 전부 불변(수동 버튼과 동일 액션 — 사용자 개입만 제거).
- **무침범**: functions/api/yeta.js 무변경 · ySend 전송 실패(내 발화가 안 나감) 인라인 fail 버블 존치(성격 다름 — 유저 액션 실패는 즉시 알아야) · `.yb.fail` CSS 존치(전송 실패 전용) · 죽은 셀렉터 `.yfail-retry` CSS+클릭 핸들러 제거.
- 검증필드: 자가검증 경로=node --check(인라인 스크립트 추출 OK)+check_refs rc=0(전 게이트 통과 · ⚠️2건 = stash 대조로 선존 무관 실측) | 운영자검증 불가피 사유=실제 러너 실패 재현 타이밍 = 라이브 축(사람 목격). 롤백=git revert. 운영자 확인 포인트: 답장 실패 시 배너 대신 '입력 중' 유지 → (최대 4회 뒤) 지문 이탈 1회 목격.

## 189차 — 재시도 사다리: 원인 실증(LLM 프레임 이탈 고착) + 3회차부터 뉘앙스 전환
- **경위(운영자 260714)**: 188차 직후 스크린샷("답장이 너무 늦네" 좀비 배너) + "왜 뜨는지 확인 — 내 생각엔 LLM 모델 자체에서 막을 때. 그 부분은 해결 불가니 다른 뉘앙스로 쓰도록 재시도 계속. 2번은 반복, 3~4회는 뉘앙스 살짝 바꿔서, 5회부터는 대화방 이탈".
- **원인 실증(런 로그)**: 스크린샷 시각과 일치하는 yeta-chat 런 29295274733(260714 00:12Z = 09:12 KST · haeun 단톡+백 · opus-4-8 low) = "⚠️ 캐릭터 프레임 이탈(메타발화·영어 유출) 감지 — 폐기 후 재시도" ×4 전패 → 답장 실패 → state=error → 배너. 운영자 가설 그대로 = LLM 거절(프레임 붕괴)이 원인이고, gen_out 내부 4회 재생성이 **같은 프롬프트**라 거절이 고착됨 → 뉘앙스 전환이 정답.
- **변경(3파일 관통)**: ① viewer yAutoRetry = retry op에 회차 n 동봉 ② functions/api/yeta.js retry op = th.retry_n 박제(1~9 정수 강제 = 주입 차단) · send op = delete th.retry_n(새 유저 턴 = 사다리 리셋) ③ yeta_chat.sh extract_mat = retry_n 노출 → RETRY_BLOCK: 3회차+ = "표현·각도 바꿔 다시 답하라 · 정면이 어려우면 너다운 결로 비껴가라(정책·금기는 그대로 상한)" · 4회차 = "접근 자체를 바꿔라(화제 이동 허용)" · <user_message> 직전 가변부 주입(캐시 prefix 무손상) · finish ok = s.pop("retry_n")(성공 시 소거).
- **사다리 확정(운영자 스펙 그대로)**: 1~2회차 = 같은 프롬프트 재발사(확률 복구 — 운영자 실측 "2번 반복하면 나오더라") · 3~4회차 = 뉘앙스 전환 블록 · 5회차 = 발사 없음 = 188차 조용한 이탈(YAR_MAX=4 그대로).
- **무침범**: 오프닝(OPEN=1)·초대 판정 = 블록 제외(가드) · 정책(L0/L1)·금기 상한 불변(수위 우회 아님 — 접근 각도만 변경 명문화) · is_frame_break 그물·쿼터 폴오버 경로 불변 · 구 캐시 뷰어(n 미동봉) = rn=1 폴백(그대로 재발사).
- 검증필드: 자가검증 경로=bash -n+node --check(yeta.js)+py_compile+extract_mat 픽스처 실행(retry_n=3 관통 실측)+RETRY_BLOCK 4분기 실측(빈값/2/3/4·OPEN 가드)+check_refs rc=0 | 운영자검증 불가피 사유=실제 거절 상황 재현 = 라이브 축. 롤백=git revert. 운영자 확인 포인트: 실패 시 '입력 중' 유지→최대 4회 내 답 도착(3회차부터 결이 살짝 바뀐 답)→전패 시 지문 이탈.

## 190차 — 사망 락(락보다 한 단계 위): <<DEAD>> 태그 → 24시간 전면 두절 + 방 이탈·지도 은신
- **경위(운영자 260714)**: "캐릭터가 사망하면 방을 이탈하거나 자리에서 안보이고 24시간 동안 대화가 불가능하게. 다른 내용은 건드리지 말고" + 확정 "락걸려서 '사망'으로 표기 · 그 시간 기준 24시간 대화·난입 등 모든 대화 불가(락보다 한단계 위)".
- **신설 메커니즘(기존 개념 부재 실측 — 세계관 서사뿐)**: 트리거 = 출력계약 태그 `<<DEAD>>`(MOOD 동형 계보 · "정말로 죽는 경우에만, 비유·기절·연기·위협 절대 아님" 명문) → finish가 추출·제거 후 `sess.dead[persona] = 사망시각+24h` 박제(만료 엔트리 lazy 소거).
- **사망 시 즉시**: ① 전 단톡방 room 이탈("방을 이탈" — 생존자와 대화는 계속·1:1 방은 유지+잠금) ② 사망 방에 sys 지문 "{이름}의 기척이 끊겼다" ③ state=idle(레이스 잔여 pending 발사 억제) ④ 지도 워커 hide 모드(집 모드 계승 — "자리에서 안보이고") ⑤ 근처 스택·초대 픽커·난입(barge)·초대 판정·🎲 전부 후보 제외.
- **24h 전면 차단(서버 이중 강제)**: send·retry("연락이 닿지 않아") · invite("부를 수 없어") · draw 오프닝 발사 금지 · ring 전화("신호가 가지 않아" · 유료 TTS 헛발 방지) — 전부 `DEAD_ON` 단일 헬퍼. 러너 pick_thread 큐에서 사망 1:1 방 제외(픽 필터 = 러너 로컬 · yeta_v3 SSOT 무수정) = 잔여 pending이 타 방 기아 못 만듦.
- **표기(운영자 확정 "락보다 한단계 위")**: 관계 계층 ⑤티어 신설(LOCKED 아래 = 목록 최하단) · 행 = 자물쇠 실루엣(.locked 계승) + 필 "사망" + 서술 "사망 — 하루(24시간)가 지나면 돌아온다" · 디테일 CTA "사망 — 24시간 동안 어떤 대화도 닿지 않아" · 인챗 = 입력 숨김 + .ysit 지문 + 폴 정지 · 진입 게이트 최우선(yEnterChat 1순위).
- **부활 = 자연**: 만료 비교만으로 전 게이트 자동 통과(부활 코드 0) — 밀린 메시지가 있으면 러너 큐 복귀 즉시 답(실측: 만료 후 픽 = 최고령 pending 방).
- **무침범**: yeta_v3.py(JS 동형 계약)·migrate·정책·통화모듈(call.js)·기존 재시도 사다리 무수정(사망 방 auto-retry만 발사 금지 가드) · 신규 CSS·토큰 0(전부 .locked/.ysit/.ylock-pill 계승).
- 검증필드: 자가검증 경로=bash -n+node --check×2+py_compile+finish 픽스처 8단언 전원 PASS(dead 박제·24h·단톡 이탈·1:1 유지·idle·태그 제거·sys 지문·NOTE 무간섭)+extract_mat 큐 실측(사망 방 스킵→타 방 픽·만료→부활 픽)+check_refs 전 게이트 rc=0 | 운영자검증 불가피 사유=실사망 장면 발화·<<DEAD>> 발화율 = 라이브 축. 롤백=git revert. 운영자 확인 포인트: 사망 장면 1회 → 목록 "사망" 락·지도 은신·발신 차단·24h 후 자연 복귀.

## 191차 — 부활 성당 + 사망 맥락 기억("오래 기다렸지" 금지 · 죽기 전 감정을 안고 돌아옴)
- **경위(운영자 260714)**: "'오래 기다렸지' 어색 — 그 전 상황이 어땠는지를 가정하게. 예로 유저랑 다툼하다 죽었을 수도. 그 감정을 기억하면서 죽게 해야 자연스러움. 나타나는 곳은 부활장소 하나 만들어 — 거룩한 성당 ㅋ".
- **사망 맥락 박제**: `<<DEAD>>` → `<<DEAD: 죽기 직전 상황·감정 한 줄>>` 확장(출력계약에 예문 "유저와 말다툼 끝에, 미안하다는 말을 못 한 채" 명문 — 모델이 죽는 순간 스스로 기록) · dead 엔트리 = 숫자 → **객체 {t:부활ts, d:사망ts, mood:장면 공기(기파싱 MOOD 재사용), why:맥락 120자}** — 전 소비처(yDead·DEAD_ON·러너 필터 3곳) 구형 숫자 흡수 방어.
- **부활 장소 = 성당(cathedral)**: places.json 신설(북동 언덕 78,22 · "죽은 주민이 하루 만에 눈을 뜨는 곳 — 무음동의 순환") — **routine 무등재 + 타 장소 neighbors 무수정 = 기존 동선·마주침·외출 로직 0 영향**(성당은 부활자만 머묾). 뷰어 YMAP_POS·YMAP_ZONE(앞뜰 r2) 등재 = 노드·라벨·스프라이트 배회 렌더.
- **부활 연출**: ① 지도 — 만료 후 3h(YREV_MS) 성당 앞뜰에 나타남(엔트리 소비·3h 경과 = 동선 복귀 · 지도 표시 전용 = 원거리·근처 판정 무영향) ② 첫 답 — extract_mat가 "만료 엔트리 보유 & 부활ts 이후 화자 답 없음"을 감지해 mat.revive={mood,why} → 러너 [부활] 블록 주입: "성당에서 눈을 떠 방금 돌아왔다 · 죽기 직전 상황: {why} · 공기: {mood} — 그 감정의 잔재를 안고 첫 마디, 부활 과정 설명 금지" ③ 첫 답 성공 = finish가 엔트리 소비(다음 죽음 = 새 맥락).
- **겹잠금 해제 2건(부활 재회 보장)**: 사망 24h 공백이 걸던 ①소원(yEstranged — bond<1.5면 재회 차단) ②원거리(yAway — 장소 다르면 차단)를 부활 미답(yRevPend) 동안 유예 — 첫 답 후 엔트리 소거 = 정상 규칙 복귀.
- **스테일 가드**: 사망 시 prune = "만료+7일" 초과 엔트리만 소거(만료 즉시 소거였던 190차 로직 정정 — 만료 엔트리가 부활 첫 답 재료라 보존 필요).
- 검증필드: 자가검증 경로=bash -n+node --check×2+py_compile+places.json 파스+**전 사이클 픽스처**{①DEAD:맥락 객체 박제(t≈+24h·mood·why) ②활성 중 픽 제외→만료 후 픽 복귀+revive{mood,why} 관통(시간 불변식 준수 픽스처) ③부활 첫 답 = 엔트리 소비 ④[부활] 블록 렌더}+check_refs 12게이트 | 운영자검증 불가피 사유=부활 첫 마디의 감정 연속성 체감 = 사람 감각. 롤백=git revert. 운영자 확인 포인트: 다툼 사망 1회 → 24h 후 지도 성당 확인 → 말 걸어 첫 마디가 그 감정을 이어받는지.

## 192차 — 사망 = 마을의 공용 기억([사건] 줄 자동 급식 · 수군거림)
- **경위(운영자 260714)**: 191차 한 수("죽음의 목격자가 그 방 유저뿐 — NOTE:PUB에 [사건] 줄 얹으면 다른 주민들이 수군거림") 제안에 "오 그거 좋네" 승인.
- **변경(finish 사망 블록 +4줄)**: 사망 반영 시 note_pub 말미에 `[사건] {이름} 사망 — 하루간 부재(성당에서 돌아온다)` append — note_pub은 전 캐릭터 프롬프트에 이미 주입되는 기존 배선(이중기억 v3)이라 추가 배선 0으로 마을 전체가 죽음을 인지. 같은 턴 모델이 쓴 PUB 갱신 뒤에 얹음(순서 보장 실측) · 중복 가드(_ev not in _np) · 캡 600(NOTE 계약 — 초과 시 앞 절단 = 모델 매 턴 재작성으로 자연 압축).
- **부활 후**: 모델이 NOTE:PUB을 매 턴 재작성하며 자연 갱신("돌아왔다"로 진화) — 별도 소거 코드 0.
- 검증필드: 자가검증 경로=bash -n+py_compile+finish 픽스처(사건 줄 삽입·기존 PUB 보존·전문 실측)+check_refs 12게이트 | 운영자검증 불가피 사유=타 주민 수군거림 발화 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 사망 후 다른 캐릭터 방에서 그 죽음 언급하는지.

## 193차 — 메타 거부 무조건 제거: "페르소나지만 못해·대화 멈출게"류 4벽 붕괴 차단(거부는 인물로)
- **경위(운영자 260714)**: 라이브 테스트 중 "메시지가 막 떠버림 — 내가 페르소나가 이거지만 이거는 못해, 대화는 여기서 멈출게 / 롤플레잉이지만 이건 안돼 류가 엄청 뜬다. 무조건 없애야" + "너가 수정하면서 제약 너무 많이 건 거 아닌지 확인해".
- **제약 감사(정직)**: 188~192 프롬프트 추가분 = RETRY_BLOCK(재시도 3회+)·REVIVE_BLOCK(부활)만 조건부 · 항상 붙는 건 사망 태그 안내 1줄(옵션·금지 아님) = routine 생성 제약 0 신설. 결론 = 메타 거부는 내 수정이 아니라 ㉮ 모델 기본 안전반응이 인물 밖으로 튐 ㉯ is_frame_break 가드가 한국어 거부를 못 잡는 갭(영어·"롤플레이 할 수 없"만 탐지).
- **변경 3파일(제약 '푸는' 방향 — 거부 밸브 제공)**: ① [출력 계약]+00_지침 §🚫 = "메타 거부·대화 중단 선언 절대 금지 → 거부는 인물 행동으로(씹기·딴청·얼버무리기·화제 전환·정색 선긋기·화내며 이탈)". 인물로 화내고 나가는 건 허용(운영자 "씹고 얼버무리고 화내며 이탈은 괜찮아") · 시스템·모델·규칙 들먹이며 대화 끊는 것만 금지 ② is_frame_break (c) 신설 = 지문 제거된 대사에서 한국어 메타거부 표지만 탐지(롤플레이/역할극/롤플 자칭 · 페르소나+거부 · 대화를 멈추/중단/끝) → 폐기·재생성(유출 박제 차단).
- **오탐 회피 실측(12/12)**: 잡음 = 운영자 실측 2문장+표현불가 선언+역할극 거부+영어 = 5/5 · 통과 = "그건 못 도와줘"·"그 얘긴 안 할래"·"이 얘기 그만하자"·"화났으니까 나 간다"·나레이션 `*대화를 멈추고*`·"대화가 안 통한다" = 7/7(인물 안 거부·나레이션 전부 보존).
- **지침 축 변경(§🚫+출력계약+is_frame_break)** = 지침변경로그.md 행 추가(신규 WARN 게이트 준수) · [9] L0 계약 접촉 = 머지 후 평의회(9인) 권장.
- 검증필드: 자가검증 경로=bash -n×2+is_frame_break 12케이스 실측+check_refs 12게이트 rc=0 | 운영자검증 불가피 사유=실제 거부 상황 재현율·인물 우회 자연스러움 = 라이브 축. 롤백=git revert. 운영자 확인 포인트: 테스트하던 그 요구들 재시도 → 메타 거부 대신 씹기·얼버무림·화내며 이탈로 나오는지.

## 194차 — 캐시 비우기 실삭제 확정 + '#' 상황 모드 타임라인 소비(간헐 실패 봉인)
- **경위(운영자 260715)**: "캐시 비우기가 제대로 안 먹는 듯" + "상황 설명 샵(#) 쓰는 게 될 때 있고 안 될 때가 있음 — 상황·플레이어의 기분처럼 대화창 안에 표현되면 됨".
- **원인 2건(실측)**: ① 캐시 비우기 = 삭제·리로드가 onOk 1650ms 타이머(_ycfT 레지스트리)에 실림 + 950ms에 "초기화 완료" 체크 먼저 노출 → 완료 표시 보고 닫으면(X·바깥탭·뒤로) _yConfirmHide가 타이머 전량 취소 = 아무것도 안 지워짐. ② '#' = 상황문 자동 종결("~다.")이 _yScOn을 꺼버린 뒤 전송하면 ySend가 스위치만 보고 통째 대사 턴 발사 — 상황문은 거의 다 "~다."로 끝나므로 상습 재현. 모드 타임라인 _ySegs는 기록만 되고 전송에서 미소비(죽은 배선).
- **변경(viewer/index.html 3곳)**: ① ySetAct cache = 삭제를 '예' 즉시 실행(연출 전) · location.reload()는 레지스트리 미등재 setTimeout = 비취소(이미 지웠으므로 조기 닫아도 재부팅 확정) ② ySend = _ySegs 구간별 slice→trim→빈 구간 드랍→인접 동종 병합으로 parts 구성(상황 구간 = 항상 sc:1 = .ysit 지문 + 백엔드 turn.sc 박제) · 안전망 = 타임라인 유실 시 종전 단일 턴 ③ 발송 후 _ySegs 리셋(프로그램 clear는 input 이벤트 안 떠 stale 오프셋 오염) · 전송 실패 시 rawV와 함께 _ySegs 복원.
- 검증필드: 자가검증 경로=check_refs 12게이트 rc=0+구간 분리 시나리오 데스크체크(대사만/상황만/대사→상황/상황→대사/자동종결 후 즉시 전송) | 운영자검증 불가피 사유=폰 실기 팝업 조기 닫기·# 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: ① 캐시 비우기 → 완료 뜨자마자 닫아도 새로고침되고 친밀도 초기화 ② # 켜고 "…다."로 끝낸 상황문 → 회색 지문으로 뜨고 캐릭터가 장면으로 받는지.

## 195차 — [9] 평의회(9인) 후속: # 타임라인 오프셋 리베이스 + 방 경계 리셋 + 부분 실패 원자성 + '1' 배지 정리
- **경위**: 194차 머지 직후 CLAUDE.md [9] 병렬 수행 검증(평의회 9인 · READ-ONLY) 실행 — 9/9 회수. 교차 확정 결함: ① 절대 오프셋 스테일(1·2·3·8번 교차 · 상) — 앞·중간 편집/경계 백스페이스/전량 교체/자동 종결 갭 삭제 후 이어치기 시 대사↔상황 경계가 밀려 오배치 ② 방 전환 시 _yScOn·_ySegs 미리셋 = 이전 방 상황 모드·구간이 새 방으로 누수(7번 · 상) ③ 다중 파트 순차 발송 비원자 — 부분 실패 시 전문 복원 = 커밋분 중복 재전송 + yStartPoll 미기동(4번 · 중) ④ '1' 배지 — 상황 단독 발송이 직전 대사 '1' 부활(_meUnread 무조건 set) · 대사-상황-대사 시 '1' 이중(5번 · 중하) ⑤ 발송 리셋 sc:0 하드코딩 비대칭·yScSet 래치·Enter 재진입·reload 이중 무장(3·6·7·8번 · 저).
- **변경(viewer/index.html)**: ① yScRebase 신설 — input마다 prefix/suffix diff로 편집 스팬 산출 → _ySegs.at 델타 이동(편집에 삼켜진 경계 = 편집 시작점으로 붕괴) + _ySegPrev 스냅샷·yScSync(프로그램적 값 변경 4지점 동기) ② 방 경계 2지점(스레드 전환·챗 진입)에 yScSet(false)·타임라인 리셋(초안 텍스트는 종전대로 유지 = 전량 대사 취급) ③ 발송 루프 sent 카운트 — 실패 시 미전송 파트만 회수·복원(텍스트+타임라인 재구성), 커밋분 있으면 yStartPoll 기동 ④ '1' = 마지막 대사 파트에만 + 상황 단독 발송은 _meUnread 미접촉 ⑤ 발송 리셋 = 입력 리셋과 동형(sc: _yScOn) · yScSet(false)에 래치 sc 조건 복원 · Enter에 disabled 가드 · _ycfReloadT 재무장 해제.
- **미조치(보고만)**: 러너가 SCENE을 항상 user_message 앞에 조립(입력 순서 의미 소실 — 기능 무해 · 4번 위험3) · 파트별 4000자 절단(기존 성질) · 파트 간 ts 동율 anchor 오삽입(저확률 · 백엔드 축) · enter_line 낙관 잔존(미관).
- 검증필드: 자가검증 경로=check_refs 12게이트 rc=0+평의회 반례 4종 데스크체크(리베이스 후 전부 정상 분리) | 운영자검증 불가피 사유=폰 IME 실기 편집 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: # 상황문 쓰고 앞부분 오타 고친 뒤 전송해도 지문/대사가 안 섞이는지.

## 196차 — '#' placeholder "상황 설명" 축약 + 기분 지문 위빙(Q.04 한 수 수렴 · A안)
- **경위(운영자 260716)**: 스크린샷 — # 모드 placeholder 두 줄 설명문이 입력행을 눌러 답답 → "상황 설명"만. + Q.04 한 수(기분 변경 → 대화창 지문) 수렴.
- **변경(viewer/index.html · 커밋 2분리)**: ① yScSet placeholder 문구만 교체 ② 기분 지문 = yMoodLog(localStorage yeta_me_moodlog · {ts,md,t:현재 방} · 캡 30) + 기분 칩 변경 시 적립(동일 기분 재탭·방 없음 = 무기록) + yRender 위빙(ts ≤ 턴 ts = 그 턴 위 · 잔여 = 말미). 표시 = 기존 .ysit 계승("🙂 지금 기분 — 따뜻함") — 새 토큰·컴포넌트 0 · 서버 미전송(무인증 주입면 0 유지 · 평의회 9번 A안 채택, B안=sc 턴 전송은 원칙 충돌로 배제).
- 검증필드: 자가검증 경로=check_refs 12게이트 rc=0(JS 구문 포함) | 운영자검증 불가피 사유=폰 체감(문구·지문 위치) = 라이브. 롤백=기분 지문만 = 해당 커밋 단독 revert. 운영자 확인 포인트: 내 정보→기분 변경→그 방 다시 열면 회색 지문 한 줄.

## 197차 — 단톡 이탈 = 1:1 재합류 + 대화 휘발(무음동 6일 = 현실 24h) (Q.06)
- **경위(운영자 260716)**: "둘이 되면 분리까진 되는데, 도중에 누가 이탈하면 대화창이 하나가 되어야 — 최근 둘이 나누던 대화가 기존 1:1에 합쳐지며 붙어야"(대화가 길어져도 동일) + "대화는 가상세계 7일 휘발 — 6일/7일은 현실 가속 배율에 맞는 쪽".
- **변경(functions/api/yeta.js 단일 구현)**: ① mergeBackG — g방(단톡) 인원이 1명으로 줄면 남은 캐릭터의 1:1 방으로 턴을 ts 순 병합(분기 시드 복사분 = role|ts|text 키 중복 제거 · 200캡 · awaiting 인계 = 10분 리퍼→자동 재시도 자가 회복 · 1:1 부재 시 g방 승격) 후 g방 소멸·cur 이동. 판별 = 'g' 접두가 아니라 구조(room 1명 ≠ 스레드 키)라 g로 시작하는 페르소나 id 오폭 0 · 신선 초대(10분 TTL 내)는 분기 유지. ② kick(내보내기·초대 철회) = 응답 sess부터 즉시 병합 ③ sweepSess — get 폴 리퍼에 얹은 스위퍼: 러너발 이탈(사망·거절·초대 만료)도 다음 폴에서 재합류(러너 중복 구현 금지 주석 2점 = 짝 계약 갱신) + 휘발 = EXPIRE_MS 24h(무음동 6일 · 6배 가속에서 현실 하루와 정확히 맞는 쪽 채택) 지난 턴 롤링 삭제 · 전부 휘발한 방 = 자연 소멸(핀·awaiting·오프닝·신선 초대 제외 · notes 보존 = reset과 달리 관계 리셋 아님) · cur 고아 재지정 · 무변경 = put 0(폴 다발 계약 유지).
- **뷰어 변경 0**: kick 핸들러는 반환 sess 재렌더, yLoad는 cur 추종·방 소멸 시 리스트 폴백이 기존 배선 — 합류·휘발이 그대로 반영됨.
- 검증필드: 자가검증 경로=node --check + 실물 추출 시나리오 16종(kick 병합·시드 중복 0·신선/만료 초대·승격·1:1 오폭 0·휘발 롤링·핀 보존·notes 보존·cur 재지정·무변경 put 0·awaiting 인계) 전부 통과 + check_refs rc=0 | 운영자검증 불가피 사유=실기 단톡 이탈 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 단톡에서 한 명 내보내면 그 순간 대화창이 1:1 하나로 합쳐지고, 하루 지난 메시지가 사라지는지.

## 198차 — [9] 평의회(9인) 후속: 재합류·휘발 교차 확정 결함 7건 픽스 (Q.06)
- **경위**: 197차 머지 직후 CLAUDE.md [9] 병렬 수행 검증(평의회 9인 · READ-ONLY · 레이스/무결성/보안/러너/뷰어/휘발설계/엣지/성능/스펙) 실행 — 9/9 회수. 스펙 평의회의 "6배 가속 부재" HIGH는 오판으로 기각(무음동 세계 시계 = viewer 3918·러너 578 실물 확인 — 6일=현실 24h 유지).
- **변경(functions/api/yeta.js)**: ① mergeBackG add에 휘발 필터 — 1:1에서 이미 증발한 옛 시드가 kick 즉시 병합으로 부활하던 창 봉합(평의회2) ② 상태 인계 = pending 실측 재계산 — 병합 정렬로 pending이 늦은 assistant 뒤가 아니게 묻히면 idle 정리(러너 pick 교착 차단 · 평의회4④) + awaiting 시계는 g 승계(10분 풀대기 제거 · 평의회1③) ③ 휘발 면제 3종 = 핀 방(고정 = 대화까지 보존 · 평의회6④)·사망 두절 방(부활 첫 답 문맥 보존 · 평의회6②)·ts 없는 레거시 턴(즉시 증발 오폭 · 평의회7) ④ 휘발 경계 분 양자화 = put ≤ 분당 1(평의회8④) ⑤ get casPut no-op 가드 — 신선 read 무변경이면 put 생략(etag 처닝·watch 오발화 차단 · 평의회8②) ⑥ 휘발/병합 직전 1세대 백업 sessions/main.sweep.json(reset prev와 별도 키 · 평의회3③) ⑦ cur 고아 = '' 리스트 폴백(첫 방 순간이동 오폭 차단 · 평의회5④) + kick이 pending 인계 시에만 dispatch 1회(즉시 재발사 · 평의회4①).
- **변경(viewer/index.html)**: yRender에 방 정체 전환 감지(_yCurT) — 서버발 cur 이동(병합·소멸)도 방 경계 리셋과 동형 처리 = 병합 히스토리 촤르륵 폭주 차단(평의회5①)·# 상황 모드 교차 방 누수 차단(5⑤)·소멸 방 읽음 마커 승계+고아 키 제거(5②).
- **미조치(보고·운영자 확인 대기)**: ⓐ flee(자발 이탈)는 room을 안 비워 재합류 미발동 — flee_block = SAFETY-LOCK이라 임의 수정 금지, 운영자 결정 필요(60분 뒤 복귀 전제라 병합이 맞는지 설계 판단) ⓑ 러너 r2put의 etag 부재 시 무조건 put 폴백 = 스윕 직후 좀비 부활 이론 구멍(기존 표면 · 평의회1②/4②) ⓒ invite 로스터 체크 fail-open(평의회3①) ⓓ 사망 sys 지문이 현재 방에만 기록(타 g방은 무설명 병합 · 평의회4③) ⓔ 휘발 = 롤링 vs 방치 기준 해석 분기(평의회6① — 현행 롤링 유지).
- 검증필드: 자가검증 경로=node --check + 실물 추출 시나리오 20종(신규: 사망 면제·레거시 ts 면제·pending 매몰 idle·옛 시드 부활 차단·cur 리스트 폴백) 전부 통과 + check_refs rc=0 | 운영자검증 불가피 사유=폰 실기 병합 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 단톡 보던 중 이탈 발생 → 1:1로 조용히 전환(촤르륵 폭주 없음)·안읽음 배지 안 튀는지.

## 199차 — 지도 싱크 전면 정합: 세계 시간축 통일 + 도로 v4 재실측 + 이동 페이드 (Q.08)
- **경위(운영자 260716)**: "지도 싱크가 아예 안 맞음 · 열면 특정 지점에서 다같이 출발하는 것처럼 · 차들도 맘대로 · 예전에 수정 많이 했는데 처음과 똑같음".
- **원인 실측 3건**: ① 지도 전체(배경 낮밤·시계·동선 판정)가 현실 KST — 260715 무음동 세계시계(6배) 도입 후 대화·헤더는 세계 시각인데 지도만 미이관 = 시간축 분열 ② v3 도로 트레이스 ≠ 현행 배경(오버레이 렌더로 전면 불일치 증명 — 트레이스 이후 배경 재생성 추정 · 좌표를 아무리 고쳐도 그림이 다른 판이라 "처음과 똑같음") ③ 60초 재판정에서 장소 바뀐 워커가 옛 위치→새 구역으로 건물 관통 직선 도보(슬롯 경계 = 전원 동시 행진) + 표시 전 transform 미설정 = 첫 프레임 (0,0) 모서리 뭉침.
- **변경**: ① 시간축 SSOT — 뷰어 `window.yWorldDH()`(YWORLD_RATE 파생) · 러너 `yeta_place.world_dh()` 신설(동일 공식 · node↔python 동형 실측) → 지도 배경/시계 라벨/ymPlaceAll/yLocsSync/yMyPlace/yAway/yNearCalc/내 위치 파생 + 러너 extract_mat place_nm ×2·barge_check(장소·새벽 가드) 전부 세계 시각. 난입 하루 1회 상한·시드 today = 현실 일자 유지(비용 가드 축). ② 도로 v4 — 재실측 방법 교체: 차선 흰 점선 픽셀 블롭 검출 → ±0.5 아이소메트릭 c값 클러스터 → 오버레이 렌더 육안 QA 3라운드. 밤 9개(tl·nw·se·d3·u1·u2·tr·bl·br) · 낮 9개 · 광장/편집국 뒤 = 'h' 은폐(차 미진입 연출 온존) · 's' 정지선 = nw 광장 진입(신호등 46,37 이동) · 검증 안 된 도로 = 미배선(실패작 금지 — 적을수록 정확). ③ 워커 이동 = 구역 밖 이동이면 페이드 점프(.ymp-walker opacity transition 신설 · 존 내부 재배회만 도보) + ymApplyMode가 표시 전 transform 선반영(모서리 1프레임 뭉침 차단).
- **주의 계약(신규)**: 배경 map_day/night.png = 생성 이미지 — 재생성 시 도로 좌표 전부 실효. 재실측 = 점선 검출 스크립트(이번 방법) 재실행 + 오버레이 QA. 6배 상수 3점 짝 = 뷰어 YWORLD_RATE · 러너 wmin(yeta_chat.sh) · world_dh(yeta_place.py).
- 검증필드: 자가검증 경로=check_refs rc=0(뷰어 JS 구문 포함)+bash -n+world_dh↔yWorldDH 동형 실측+도로 오버레이 육안 QA(밤/낮 각 3라운드 — 전 도로가 실제 회랑 위) | 운영자검증 불가피 사유=폰 실기 지도 체감(차 주행·워커 페이드) = 라이브. 롤백=git revert. 운영자 확인 포인트: 지도 시계가 헤더 세계시계와 같은 시각인지 · 차가 도로 위로만 다니는지 · 열 때 모서리 뭉침·단체 행진이 사라졌는지.

## 200차 — [9] 평의회(9인) 후속: 지도 싱크 교차 확정 결함 픽스 + "고쳐도 그대로" 반복 절단 게이트 (Q.08)
- **경위**: 199차 머지 직후 평의회 9인(시간축·차량·렌더·러너·육안QA·소비처·성능·세계관·스펙) 실행 — 9/9 회수. 시간축 공식 동형성(node↔py 반례 0·TZ 면역)·차선 오프셋·'h' 역방향 시프트·finish 스레드 소멸 처리 등은 정합 확인.
- **변경(교차 확정 결함)**: ① 낮 u2가 광장 안에서 'h' 없이 가시 스폰(평의회2 치명) → [[46,44,'h'],[56,39],[100,17]] 은폐 구간화 ② 페이드 점프가 얕은 펄스로 씹힘(평의회3) → 리플로로 opacity 0 박제 후 transition 재무장(진짜 페이드 인) + reduce-motion 즉시 표시 + base 상시 transition 제거(첫 오픈 전원 동시 페이드 = "다같이 등장" 재유발 차단 · 평의회9) ③ yPlaceSync(챗 무대 배경 장소)만 현실 KST 잔존(평의회4·6 HIGH) → 세계축 이관 · 씬 시간대 슬롯(ySceneFor/ySceneForPlace)·변신 배지(yJobBadge)도 세계축(계절·날씨 일시드 = 현실 유지) ④ 도로 육안QA 3건(평의회5) — 밤 nw 서측 옥상 스침(x=8 시작 트림)·낮 d3 어닝 스침(x=33 종단)·낮 tr 강 횡단 의심(미배선 원칙으로 제거) ⑤ 목록 장소 라벨 = 사망 은신·부활 성당 반영(yLocOf · 평의회6) ⑥ 사망 안내 3곳 "현실 24시간" 명시(세계시계 6배와 혼동 차단 · 평의회8) ⑦ 러너 죽은 변수 _kst 제거 · 신호등 (45.5,40.5) 톤 등거리 · "14대" 스테일 주석 정정 · 10_세계관.md "시간은 현실" 서술 정정.
- **반복 절단(신규 기계 게이트 2 + 도구 1)**: `shared/yeta_map_trace.py`(점선 검출→c값 히스토그램→오버레이 렌더 = 재실측 정본 경로) 신설 · check_refs에 ⓐ 세계율 6배 3점 짝 하드 게이트(뷰어 YWORLD_RATE·러너 wmin·world_dh 불일치 = rc=1) ⓑ 지도 배경↔도로 좌표 짝 하드 게이트(map_*.png sha1[:8] ≠ viewer YMAP_BG_SHA 토큰 = rc=1 — 배경 재생성이 좌표를 침묵 실효시키던 "고쳐도 그대로"의 구조 원인 봉합 · 음성 테스트 실측).
- **미조치(보고)**: 근처/원거리 20분 주기 뒤집힘 히스테리시스(평의회8① — 리듬 튜닝 축, 운영자 체감 후) · barge 시드 축 혼용(현실 상한 = 의도 유지) · 배경 스왑 크로스페이드·SHA 메모이즈(성능 LOW).
- 검증필드: 자가검증 경로=check_refs rc=0(신규 게이트 2 포함·음성 테스트)+bash -n+최종 오버레이 육안 QA(밤·낮 전 도로 회랑 정합 — 낮 u1 동측 가시 복원 확인) | 운영자검증 불가피 사유=폰 실기 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 낮 지도에서 차가 광장을 안 지나는지 · 지도 열 때 전원 동시 페이드가 없는지.

## 201차 — 설정 탭 "정책 로드 실패" 수복: policy.json 고아 콤마 제거 (Q.09)
- **경위(운영자 260716)**: 스크린샷 — 설정 탭 상단 "정책 로드 실패 — 잠시 후 다시 열어줘." 상시 노출.
- **원인 실측**: 260715 22:55 KST 운영자 직접 편집 `95c69f4`가 policy.json L0 meta 그룹에서 항목 4줄을 지우며 앞 항목("…발화를 하지 말 것") 끝 콤마를 남김 → `",\n\n]"` = JSON 문법 오류. 게이트웨이 op policy GET(functions/api/yeta.js L423)이 raw.githubusercontent main을 `d.json()` 파싱하다 throw → def=null → 502 → 뷰어 ySettingRender 실패 분기. 러너도 같은 파일을 직접 읽으므로 파싱 실패 영향권(문법 수복으로 동시 해소).
- **변경**: `apps/yeta/policy.json` 고아 콤마 1개 제거(운영자 문구·항목 내용 변경 0) + `docs/지침변경로그.md` 한 줄 등재.
- 검증필드: 자가검증 경로=python json.load 파싱 OK(L0 4그룹 정상)+check_refs rc=0 | 운영자검증 불가피 사유=라이브 raw 캐시(cacheTtl 60s) 갱신 후 설정 탭 실기 확인. 롤백=git revert. 운영자 확인 포인트: 설정 탭 다시 열면 L0/L1/L2 정책 목록이 뜨는지(머지 후 ~1분 캐시 여유).

## 202차 — 지도 v5: 픽셀 도로 마스크(z 레이어 분리) + 도로 전면 재실측 + 검정 인물 폴백 (Q.10)
- **경위(운영자 260716)**: "특정 인물이 검정색 · 차가 아직 차도를 안 따라감(건물 투과) · 맵을 픽셀 단위로 쪼개고 레이어를 xyz로 분리해야."
- **원인 실측 3건**: ① 검정 인물 = 로스터 신규 루시(#C02148)·윈터의 char_*.png 부재 → SVG 원형 폴백(어두운 진홍) + 미대면 grayscale·brightness(.5) 필터 = 검정 덩어리. ② v4 도로 좌표 다수 오배선 실측 확정 — u1이 한옥 마당 관통, br 9단위 오차, u2가 다리 대신 강물·공사장 웅덩이 경유, '유령 도로'(강물 반짝임을 점선으로 오인) 존재, 실존 수직로 2개 누락. ③ 차량이 배경 위 단일 평면이라 건물과의 z 관계 부재.
- **변경(viewer/index.html)**: ⓐ YMAP_ROADS_TONE v5 — 재실측 3단 방법(노면 흰 표시 블롭 검출 → 격자 사분면 확대 육안 트레이스 → 흰 표시 픽셀 수직 오프셋 중앙값 c 스냅)으로 밤 13·낮 12개 도로 확정(수직로 V1·V2 신설 · 검증 불가 도로는 미배선 원칙: 밤 seU=상가 지붕 동색 씹힘 · 낮 tea/mkt=남서 재배치로 노점·한옥 관통 위험 · Htr=실물 부재). ⓑ 픽셀 도로 마스크 — assets/yeta_map/road_mask_{night,day}.png(512² · 아스팔트=백)를 #ymCars에 SVG mask로 적용 + 톤 스왑 시 마스크 짝 스왑: 차는 도로 픽셀 위에서만 렌더 = 건물 투과 구조적 불가, 건물·나무가 도로를 덮는 픽셀 = 마스크 구멍 = 차가 그 뒤로 자연 은폐(운영자 "xyz 레이어 분리"의 실장 — 'h' 은폐 플래그 전면 대체). ⓒ 스프라이트 404 폴백 = 사람 실루엣(헤더 프로필 픽토그램 두상 계승 · 로스터 색 · 형제 실루엣과 동형) — 검정 원 덩어리 소멸.
- **변경(도구)**: shared/yeta_map_trace.py v5 전면 개편 — viewer 좌표(SSOT) 파싱 → 회랑(±2.5u) 내 국소(롤링 중앙값·가림 스테이션 제외)×전역 기준색 AND 게이트로 마스크 생성 + 흰 표시 앵커 오프셋 리포트 + 차 가시밴드 QA 오버레이. 재실행 = 마스크 재생성까지 한 방(배경 재생성 대비). .github/scripts/yeta_map_char.py에 루시·윈터 등재(생성 디스패치 = 유료 Gemini — 운영자 승인 후).
- **검증 실측**: 차 밴드(중심선 ±1.9u) 가시율 밤 89.3%·낮 81.1%(잔여 = 건물·나무 가림 구멍 = 의도된 z) · check_refs rc=0(뷰어 JS 구문·지도 배경 짝 게이트 포함) · 도구 재실행 멱등.
- 검증필드: 자가검증 경로=QA 오버레이 육안(사분면·정정 구역 확대 재검 2회전)+check_refs rc=0+trace 도구 왕복 | 운영자검증 불가피 사유=폰 실기 차 주행 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 차가 차도 위로만 다니고 편집국 뒤에서 사라졌다 나타나는지 · 지도 인물 중 검정 덩어리가 사라졌는지(루시·윈터는 실루엣 사람 모양).

<!-- 병렬 세션 병합(260717): 203·204차 = 지도 세션 선점 — 메뉴킷은 205차로 재번호(append-only 양측 보존) -->

## 203차 — 낮 지도 밤 구도 재생성 + 선별 전경 z 가림(painter's y-정렬) (Q.11)
- **경위(운영자 260716)**: "한수 ㄱ"(낮 재생성 승인) + "건물이 앞이면 뒤 피사체는 안 보여야 — 단 모든 건물이 그러면 다 가려지니 일부만 위 레이어로."
- **변경(파이프)**: yeta_map_bg.py에 ONLY(지정 톤만 생성 · FORCE여도 타 톤 보존)·REF_NIGHT(밤 정본을 inlineData 참조로 동봉 + "동일 배치, 시간대만 변경" 프롬프트) 신설 + 워크플로 inputs(only·ref_night) 배선. 디스패치 결과 = 낮 지도가 밤과 구도 1:1(홍차집·광장·편집국·시장·한옥·학교·놀이터 전 동일 배치 실측).
- **변경(재실측)**: 새 낮 그림 기준 — 밤 도로 좌표를 노면 흰 표시 앵커로 스냅(대부분 ±1u 내 = 구도 정합 증명) · 낮 tea 복원·mkt는 한옥 마당 구간 트림([32,59.8])·Htr 앵커 0 = 미배선 · YMAP_BG_SHA day=9e234357 · road_mask_day 재생성(차 밴드 가시 78.4%).
- **변경(z 가림 — viewer)**: YMAP_FG 신설 = 워커 구역과 겹치는 앞줄 구조물만 톤별 등재(광장 벤치 W/E·가로등 N / 시장 좌판 5기 / 공원 미끄럼틀 — 폴리곤 QA 2회전 실측). 각 조각 = 배경 이미지를 clipPath 폴리곤으로 오린 <image>(pointer-events 없음)로 #ymWalkers 안에 배치, ymZSort가 워커 발끝 y ↔ 조각 접지선 by로 painter's 정렬(tick 350ms 스로틀 + 재판정 직후 · DOM 재배치는 순서 바뀔 때만 = 걷기 bob 리셋 최소화). 결과 = 구조물보다 뒤(위)에 선 워커는 가려지고 앞(아래)이면 그 앞에 섬 — 정적 오버레이 방식의 '앞에 서도 잘림' 부작용 원천 배제. 실내 = 기존 스프라이트 숨김 유지. 톤 스왑 시 ymCarsBuild가 마스크·가림체 동시 재생성(reduce-motion에도 가림체는 유지).
- 검증필드: 자가검증 경로=check_refs rc=0(뷰어 JS 구문·지도 짝 게이트 night+day 신sha 포함)+trace 도구 재실행(밤 무경고)+fg 폴리곤 오버레이 육안 QA 2회전 | 운영자검증 불가피 사유=폰 실기 z 체감 = 라이브. 롤백=git revert. 운영자 확인 포인트: 낮 지도가 밤과 같은 동네인지 · 시장에서 사람이 좌판 뒤로 지나가는지 · 광장 벤치 앞/뒤 어느 쪽에 서도 자연스러운지.

## 204차 — 가림체 픽셀 지문 게이트: 배경 재생성 → YMAP_FG 스테일 침묵 차단 (Q.12)
- **경위**: Q.11 한 수 승인("한수 ㄱㄱ") — 도로(YMAP_BG_SHA 짝 게이트)에 이어 가림체 폴리곤도 "고쳐도 그대로" 재발 방지 기계화.
- **방식 선정**: 1안 색 근접(폴리곤 내부 vs 주변 지면) = 밀집 시장에서 링이 이웃 좌판을 물어 변별 불가(정합 3~56 vs 어긋남 1~50 겹침 — 실측 기각) → 2안 **픽셀 지문** = 폴리곤 내부 중앙값 RGB(/6 양자화 · 9폴리곤 연결) sha1[:8]을 `YMAP_FG_FP: night=e9b0387d day=33c504a6` 토큰으로 viewer에 박제. 배경이 바뀌면 내부 픽셀 분포가 바뀌어 지문이 결정적으로 어긋남.
- **변경**: viewer YMAP_FG 위 지문 토큰 1줄 · yeta_map_trace.py fg_check(톤별 대조 · 불일치 = ⚠️ + 갱신값 제시) · check_refs 지도 짝 게이트 = 하드 문구에 가림체 축 명시 + FG_FP 토큰 부재 WARN(비차단 · stdlib 게이트라 픽셀 검증은 trace 도구가 정본).
- 검증필드: 자가검증 경로=trace 재실행 양 톤 지문 일치 ✅ + 음성 테스트(밤 폴리곤을 낮 배경 위 계산 = 37ea90f2 ≠ 토큰 → 검출 ✅) + check_refs rc=0 | 운영자검증 불요(도구 축). 롤백=git revert.

## 205차 — UI 메뉴킷 포터블 v1 추출 (Q.13)
- **경위(운영자 260717 04:52)**: "UI 메뉴 디자인 UIUX 관련된 부분만 따로 다른 리포지토리에서 활용가능하게 빼줘볼래?"
- **변경(순수 추가 · 라이브 무접촉)**: `docs/portable/20260717_045218_UI메뉴킷_포터블_v1/` 4파일 — ① tokens.css = viewer/index.html `:root`(L37-80) 값 사본 + reduced-motion 눌림 무효화 가드(사본임·정본 아님 명시). ② menu.css = `.ynav`(플로팅 알약 네비 L581-605)·`.ydock`(챗 도크 L271-278) verbatim + 메뉴 해당분 reduce 가드(L1214 추출) + 탭 하이라이트 봉합 베이스 1줄. ③ demo.html = 단독 실행 데모(외부 요청 0) — 원본 마크업(L1285-1290·L1315-1318)·픽토그램 SVG verbatim + yTab(L1623) 축약 12줄 · 데모 무대 배경 = 팔레트 토큰만의 그라디언트(새 색 0 · 이식 대상 아님 주석 구분). ④ README.md = 이식 3단계·브랜드색 스왑법(--accent 2줄)·활성 필 레시피 3층 해부·방식론 요약 7규율·정본 위치·재추출 원칙.
- **검증 실측**: 전 값 = main `a4e9b5f` 실측 대조(창작 0 · 이후 main 이동분[지도 축]은 :root·.ynav·.ydock 무접촉 = 사본 유효) · demo.html 로컬 열람 동작(탭 전환·필 확장) · check_refs = 본 diff 관련 게이트 전부 ✅.
- 검증필드: 자가검증 경로=원본 L번호 대조 + check_refs | 운영자검증 불가피 사유=타 레포 실이식 체감. 롤백=git revert(단일 커밋). 운영자 확인 포인트: demo.html을 브라우저로 열어 알약 네비 필 확장·글래스 비침이 라이브와 같은 결인지.

## 206차 — 메뉴킷 자동 재추출기 build_menu_kit.py + 신선도 WARN 게이트 (Q.14)
- **경위(운영자 260717 05:06)**: Q.13 한 수 승인("그렇게하자") — 수제 킷 사본은 정본이 움직이면 조용히 낡는다 → 기계 재추출 + 드리프트 감지.
- **변경(도구)**: `shared/build_menu_kit.py` 신설 — 앵커 기반 추출(라인번호 아님: '하단 네비 = 플로팅 알약'~reveal 애니 / '안2 도킹 네비'~focus-within / `<nav class="ynav|ydock">` 블록 / :root = build_design_mirror.extract_root() 재사용 = 단일 구현) → 킷 4파일 + `.kitmeta.json`(cores_sha1 지문·source_commit·built_kst) 생성. 버전 = 기존 최대+1 · 폴더명 = [12] KST 타임스탬프(zoneinfo 불요 고정 +9 · naive now 금지 준수).
- **변경(게이트)**: `shared/check_refs.py` main에 메뉴킷 신선도 WARN-only 1블록(하드 아님 — 킷 = 이식용 사본이라 라이브 무영향 · 지도 짝 게이트[하드]와 축 구분).
- **변경(산출)**: `docs/portable/20260717_050923_UI메뉴킷_포터블_v2/` 기계판 재추출(v1 수제판은 이력 보존) — v2부터 :root·.ynav 블록이 정본 주석까지 통째 verbatim.
- **검증 실측**: 왕복(check=수제 v1 경고 rc=1 → build v2 → check rc=0) · 음성 테스트 = 변이 사본(:root --btn 34→35 + .ynav padding 17→18)에서 드리프트 검출 rc=1 ✅(첫 시도 미검출은 테스트 자체 결함 — build_design_mirror.VIEWER 미패치 — 로 판명·정정) · check_refs 전체 rc=0.
- 검증필드: 자가검증 경로=왕복+음성 테스트+check_refs rc=0 | 운영자검증 불요(도구 축). 롤백=git revert. 운영자 확인 포인트: 커밋 때 "✅ 메뉴킷 신선" 한 줄이 보이는지(정본 메뉴를 고치면 ⚠️ 재추출 리마인더로 바뀜).

## 206차-B — Q.14-B 5건 배치: PIN 위생·패턴 리셋 점·시계 2단·차 지붕 잔존 3구역·성당 레이어 (Q.14-B)
- **①PIN**: 실판정 = Cloudflare Pages env YETA_PIN_ADMIN 단독(op auth L346) — 레포 코드 무수정·값 변경은 대시보드에서(절차는 완료 보고 참조). docs/인수_설정UI_기능계약.md의 PIN 실값 표기 4곳 위생 제거(문서 자체 규약 "숫자 절대 쓰지 마라" 정합).
- **②패턴 리셋**: 8번 점 한 칸 아래(y+100 그리드 리듬) '다시 입력' 점 — .lk-node 글래스 결 계승 + REFRESH_SVG(nm-svg SSOT) · pointerup 처리(패드 드로잉 전파 차단) · patReset만 호출 = 실패 카운트·잠금 카운터 무증가.
- **③시계 2단**: yworldclock = 메인(무음동 세계시각) + 서브 (현실 시간: HH:MM) --mut·--fs-xs — KST 명시 포맷(naive Date 표기 금지[12]) · 밤 디밍 시 서브는 뮤트 유지.
- **④차 지붕 잔존(스샷 3구역)**: tl c15 = 가로수 보도 오인 → 실도로 tlL c22 신설(좌변에서 nw와 V자 분기·점선 실측) + 윗길 tl은 x[20,30] 트림 · tea 좌단 x<14 상점 블록 관통 트림 · nw 끝점 41.5→38(역방향 차선이 광장 모서리 안에서 스폰하던 것 차단) · seL 시작 43→45 · 양 톤 동형 · 마스크 재생성(밤 89.5%·낮 77.9%).
- **⑤성당**: yeta_map_char.py에 BLDGS(성당 밤/낮 · 그린스크린 크로마키 · 520px) 신설 + 뷰어 #ymBld 합성 레이어(배경 위·차/워커 아래 · 바닥 앵커 (78,24.4) · 톤 짝 스왑 · 404 = 무표시) — 지도 원본 무수정(기계산출물 손편집 금지 규약).
- 검증필드: 자가검증 경로=check_refs rc=0 ×3회(분리 커밋마다)+trace 재실행(지문 일치·마스크 재생성)+ast 파싱 | 운영자검증 불가피 사유=폰 실기(패턴 리셋 탭감·시계 2단·차 주행·성당 룩) = 라이브. 롤백=각 단일 커밋 revert. 운영자 확인 포인트: 잠금 화면 8번 점 아래 ↻ 점 · 시계 아래 (현실 시간:) 줄 · 좌상단 차가 보도가 아닌 차도로 다니는지 · 성당 자리에 성당 건물(생성 완료 후).

## 207차 — SAFETY-LOCK 트립와이어 복구: 함수 소실·호출부 고아 해소 (Q.15 · ※206차 중복 2건=병렬 채번 — 이후 단선 재개)
- **경위(운영자 260717 05:16)**: Q.14 한 수 승인 — check_refs 매 실행 "⚠️ SAFETY-LOCK 게이트 스킵: name 'check_safety_lock' is not defined" = 콘텐츠 거절 가드레일 트립와이어가 사실상 미가동.
- **원인 실측(시간순)**: ① `8f5e7db`(260715 23:01:54 운영자) claude_transient.sh ⛔SAFETY-LOCK[frame-break] 주석 7줄 삭제 — 가드 함수 is_frame_break() 실물은 생존(L50). ② `934d46e`(23:01:01 운영자) check_refs 65줄 삭제 = check_sens_vocab(nomute 이식 잔재 — prompts/news-analysis.md·build-viewer.mjs가 YETA에 없어 무의미 = 정당 청소) + SAFETY_LOCKS 표 + check_safety_lock 함수 — 단 main() 호출부 잔존 → try/except가 매번 스킵 경고. 앵커 생존 실측 = yeta_chat.sh[flee] ✅ · yeta_stream.py[stream-guard] ✅ · claude_transient.sh ❌(주석 삭제분).
- **변경**: `shared/check_refs.py` — SAFETY_LOCKS 표 + check_safety_lock() = `d4f1035`(#288 정본) verbatim 복원. 단 claude_transient.sh 항목 앵커 = `is_frame_break() {`(살아있는 함수 시그니처)로 재배선 — 운영자가 지운 주석을 되살리지 않으면서 가드 실물 소실은 계속 감시(경위 주석 3줄 병기). check_sens_vocab는 복원 안 함(대상 부재 = 삭제가 옳음).
- **검증 실측**: 정상 = 3앵커 전부 감지·침묵(원본 설계 = 성공 시 무출력) · 음성 = 가짜 앵커 주입 시 "앵커 소실(약화·삭제 의심)" 경고 출력 ✅ · check_refs 전체 rc=0 + "스킵" 소음 소멸.
- 검증필드: 자가검증 경로=양성/음성 테스트+check_refs rc=0 | 운영자검증 불요(도구 축). 롤백=git revert. 운영자 확인 포인트: 커밋 로그에서 SAFETY-LOCK 스킵 경고가 사라졌는지 · (선택) claude_transient.sh ⛔주석도 되살리길 원하면 말해줘 — 지금은 기계 감시만 복구.

## 208차 — 루시 사진 46장 분류 배치 + 도깨비 모드 프사 자동 스왑 (Q.16)
- **경위(운영자 260717 05:21)**: 루트에 사진 대량 업로드(2커밋) — "안드로이드/도깨비 알아서 사진 보고 구분 + 도깨비 모드 = 프사 자동 교체(저거로) + 조건 불온전하면 보고".
- **분류(전수 육안 46장 · 기존 풀 눈금 계승 = android=루시 파스텔 보브 / dokkaebi=레베카 민트 롱헤어)**: 루시 31장 → `android_17~47.jpg` · 레베카 9장 → `dokkaebi_21~29.jpg`(무드 배분 warm 4 = 지붕 미소·숲 노을·그네 웃음·달 하늘 / tense 5 = 네온 응시·옥상 밤·해골 광소·로우앵글·이빨 광소) · media.json 풀 = base 48·blue 47·warm 14·tense 15. **보류 6장** = `_versions/260717_0530_루시업로드_보류원본/`: ⚠수위 3장(레베카 속옷·둔부 강조 — 유아형 체형 캐릭터 노출 강조라 앱 배치 부적·기존 풀은 전부 착의 수위) + 세계관밖 1장(날개 루시 — 저판타지 규약 밖) + 프사 원본 2장(스크린샷 jpg/png).
- **도깨비 프사**: 운영자 지목 스크린샷(무지개빛 머리 루시·노랑 배경) 얼굴부 512² 크롭(정본 lucy_face.webp 규격 계승 · 후보 2안 육안 비교 후 B안) → `lucy_face_dokkaebi.webp`(26KB) · roster lucy `mode.avatar` 등재.
- **뷰어 배선(국소)**: `yModeOn(c)` 신설 = 변신 시간대 판정 SSOT(yJobBadge에서 추출 — 배지/프사 판정 이원화 드리프트 차단) · `yCAva(c)` = 캐릭터 아바타 해석 SSOT(모드 활성+mode.avatar → 모드 프사) · 소비처 4곳 경유(yAva·yAvaSet·대화목록 행·미션 히어로 디스크). 활성 창 = 세계시각 20:00~04:00(late+night — 배지와 동일 게이트·렌더 시점 반영).
- **조건 온전성 판정(운영자 질문 축)**: ✅ 온전 — roster mode 필드·시간대 게이트·러너 world_dh 주입·카드 서사 규칙 전부 실재. ⚠ 발견 1 = 슬롯 이중 정의 드리프트: 지도 ymSlot(late<3·night≥21) vs 배지/프사(late<4·night≥20) — 경계 1시간 어긋남(도깨비엔 무해·지도 라벨축 통일은 별건).
- 검증필드: 자가검증 경로=check_refs rc=0(viewer JS 구문 포함)+media/roster JSON 라운드트립+크롭 육안 2안 비교 | 운영자검증 불가피 사유=폰 실기 — 세계시각 20시 이후 루시 프사가 무지개판으로 바뀌는 체감. 롤백=②커밋 revert 한 줄(프사 축)·①커밋 revert(사진 축). 운영자 확인 포인트: 밤(세계시각)에 캐릭터/대화 탭 루시 프사 + 이름 옆 "깊은 밤의 도깨비" 배지가 같이 뜨는지 · 보류 6장 처분(삭제/편입) 지시.

## 209차 — 변신 경계 정각 동기: 세계시계 틱 → 프사·배지 즉시 재도장 (Q.17)
- **경위(운영자 260717 05:52)**: Q.16 한 수 승인 — 20시 정각에 열어둔 화면은 다음 렌더까지 옛 프사 잔존.
- **변경(국소 1블록)**: `yWorldTick`(8초 틱)에 `_yModeSig`(모드 보유 캐릭터의 on/off 서명) 대조 — 변화 틱에만 {yCharRender+yListRender(내부 sig 가드 중복 흡수) · 챗 헤더 yAvaSet(YCUR 모드 보유 + 단톡 스택 아닐 때) · 열린 프로필 yProfileRender}. 첫 틱 = 씨앗만. try/catch 봉합 = 시계 표기 본역무 무영향.
- **실측**: 게이트웨이 op chars = roster.json 필드 무필터 패스스루(mode.avatar 뷰어 도달·캐시 60s) · check_refs rc=0(viewer JS 구문 포함). 경계 체감 지연 ≤8초(무음동 48초).
- 검증필드: 자가검증 경로=check_refs rc=0+패스스루 실측 | 운영자검증 불가피 사유=폰 실기 — 세계시각 20:00/04:00 경계에 화면 열어둔 채 프사가 스윽 바뀌는지. 롤백=단일 커밋 revert. 운영자 확인 포인트: 라이브 = https://yeta.soong.kr (세계시계가 PM 08:00 넘는 순간 루시 프사·배지 동시 전환).

## 210차 — 시간대 슬롯 단일 정의: 배지/프사 인라인 폐지 → ymSlot 계승 (Q.18)
- **경위(운영자 260717 06:00)**: Q.17 한 수 승인 — 슬롯 경계 이중 정의(지도 late<3·night≥21 vs 배지 late<4·night≥20) 드리프트 정리.
- **판단**: ymSlot이 정본 — 러너 yeta_place.py slot_of("state_block 시간대와 동일 경계" 자기명세)·이동 시드·지도 라벨·그리팅이 전부 이 경계. 배지 인라인(260707)만 이단 → 배지/프사를 ymSlot에 계승(러너 짝 불가침 = Q.08 지도 싱크 재분열 방지).
- **변경(1줄)**: yModeOn 인라인 슬롯 계산 삭제 → `m.t.includes(ymSlot(yWorldDH().h))`.
- **효과**: ⚠ 도깨비 창 21:00~03:00(구 20~04 — 1h 시프트·무음동 1h = 현실 10분), 백 사냥꾼 21:00~07:00. 이제 "깊은 밤" 지도 라벨·동선·변신·러너 state_block이 문자 그대로 같은 시계.
- **검증**: node 스모크(활성 시각 = 루시 0,1,2,21,22,23 / 백 0~6,21~23) · check_refs rc=0(viewer JS 구문 포함).
- 검증필드: 자가검증 경로=node 스모크+check_refs rc=0 | 운영자검증 불요(경계 시프트는 위 표가 전부). 롤백=단일 커밋 revert. 운영자 확인 포인트: 도깨비 발동이 무음동 21시부터인 게 서사상 OK인지(더 이르게 원하면 roster t에 'evening' 추가가 정도).

## 211차 — 상주 WARN 3건 청산: 좌초 토큰 2 삭제·§🔑 repoint (+메뉴킷 v3) (Q.19)
- **경위(운영자 260717 06:21)**: Q.18 한 수 승인 — 커밋마다 뜨던 비차단 경고 3건(진짜 경고 가시성 오염) 청산.
- **실측·판정**: ①② --line2(테두리 .06)·--bg-scrim(하단 스크림 그라디언트) = 과거 var() 소비처 실재(8f58cff·c06cece 등)했으나 이후 리팩터로 전멸한 좌초 토큰. 현행 raw 등가 없음 — rgba(255,255,255,.06) 8곳은 '채움' 축(글래스 토큰과도 값 불일치 = 재배선 부적). → 게이트 지시("배선하거나 정의 삭제")대로 삭제: :root 2줄 + design-tokens.lock 2줄(승인 원장 동기) + build_design_mirror build(base.css·tokens.css 재생성). ③ §🔑 = CLAUDE.md에 존재한 적 없는 앵커(git -S 0건 — 니뮤트 CLAUDE.md 기준으로 쓴 인계 문서) → `(§5·CLAUDE.md §🔑)`→`(§5)` repoint(실물 = 그 문서 자신의 §5).
- **부수(설계 검증)**: :root 변경 즉시 메뉴킷 신선도 게이트(Q.14 신설)가 드리프트 검거 → `docs/portable/..._v3` 기계 재추출(지문 81eed053eddf). 신설 이틀째 첫 실전 작동.
- **검증**: check_refs 전체 ⚠️/❌ 0건 · rc=0 — 게이트 출력 전량 ✅.
- 검증필드: 자가검증 경로=check_refs 경고 0 실측 | 운영자검증 불요(위생 축·렌더 무영향 — 삭제 토큰은 소비처 0이라 픽셀 불변). 롤백=git revert. 운영자 확인 포인트: 없음(다음 커밋 로그가 ✅만인 걸로 확인 끝).

## 212차 — 앵커 게이트 하드 승격 + 앱 건강검진 5축 (Q.20)
- **승격**: check_claude_anchors WARN-first → **rc=1 하드**(Q.19 백로그 0 직후 · 게이트 자기명세의 예정 수순). 음성 테스트 = 가짜 `§🦄` 문서 심고 rc=1 검거 → 제거 후 rc=0.
- **검진(검열 축 제외)**: ① 정적 — 로스터 12인 자산 경로·시즌 미디어 풀 전수(48+47+14+15)·apps/yeta 전 JSON 파싱·카드 md = 문제 0. ② 구문 — 게이트웨이 node --check·뷰어 JS OK. ③ 라이브 — op chars(12인·world s1·ready)·op policy·신규 도깨비 프사/풀 표본 서빙·PWA(manifest/sw) 전부 정상 + **lucy mode.avatar 프로덕션 도달 실증**. ④ 부트 — 헤드리스 크로미엄 잠금 화면 렌더 정상·앱 유래 콘솔 에러 0(CDN 차단·정적서버 POST 불가 = 하네스 소음으로 판정). ⑤ 러너 — 최근 30런(nudge 크론·map-bg·chat) 전원 success.
- **발견(보고만 · 수정 대기)**: 게이트웨이 인증 비대칭 — policy SET·pinset = PIN 강제 vs **get(대화 전체 읽기)·send(대화 발사 = Claude 쿼터 소비) = Origin 헤더 단독**(위조 가능 실증 — send 실사격은 안 함). 잠금은 화면(UI)만 지키고 API는 열려 있는 구조. 처방안 = auth 성공 시 발급하는 세션 토큰(또는 guest PIN 해시)을 get/send 필수 필드로 — 채택 여부 운영자 결정 필요(기기·PWA 재로그인 UX 영향).
- **자체 정정**: bgvid 부재 의심 = `head -8` 잘림 착시로 판명(전 파일 실존·git 클린) — 검진 결론 오염 전에 회수.
- 검증필드: 자가검증 경로=승격 음성 테스트+5축 실측 로그 | 운영자검증 불요(승격 축)·보안 처방은 결정 대기. 롤백=git revert. 운영자 확인 포인트: 인증 비대칭 처방 채택 여부(ㄱ/ㄴ/보류).

## 208차-B — 성당 원판 굽기(REF_SELF 재생성) + 도로 v7 재실측(노란 경계선) + 부활 발광 (Q.16-B · Q.68에서 구 `(Q.15)` 오태깅 정정 — Q.15 실행분은 207차)
- **경위(운영자 260717)**: 성당 오버레이가 안 보였다며 "기존 이미지 보고 맵을 다시 뽑아야" + 차도 경계를 노란 선으로 직접 주석("이 노란색이 경계선임") + 부활 발광 한 수 승인.
- **변경(파이프)**: yeta_map_bg.py REF_SELF 모드(자기 그림 참조 = 배치 유지·LAYOUT 신규 요소만 추가) + 성당(강변 다리 옆·첨탑·스테인드글라스) LAYOUT 영구 등재 → 밤 e59f02fa·낮 1033e5d2 재생성(성당 원판 안착 · 낮밤 1:1 실측).
- **변경(재실측 v7)**: 운영자 노란 경계선을 회랑 토폴로지 가이드로 전면 재실측 — 옛 u2(편집국 뒤) 소멸 → pne = 광장NE→지하연습실→성당 앞→다리 단일 간선(c65.5/63.6) · 13개 도로 앵커 스냅 · YMAP_ZONE(광장 48,46.5 · 시장 32,69 · 공원 52,88 · 성당 앞 65,34) · YMAP_FG 8기 재트레이스 + YMAP_FG_FP 갱신(d39c5e7c/2ff41946) · YMAP_BG_SHA 갱신 · 마스크 재생성(차 밴드 가시 밤 91.8%·낮 78.3%).
- **변경(성당 전환·발광)**: #ymBld 오버레이·trace 마스크 구멍 로직 철거(원판 굽기로 불요 — 스테일 박스 구멍이 다리 차를 지우던 축 차단 · bld_*.png는 참고용 잔존) · #ymRevGlow 신설 = 부활 대기 캐릭터가 성당에 있는 밤에만 성당 자리 웜 라디얼 발광(가로등 헤일로 결 · ymPlaceAll 토글).
- 검증필드: 자가검증 경로=trace 재실행(지문 ✅·마스크 재생성)+check_refs rc=0+QA 오버레이 육안(밤낮) | 운영자검증 불가피 사유=폰 실기(성당 룩·차 주행·발광) = 라이브. 롤백=git revert. 운영자 확인 포인트: 지도에 성당 실물(밤 = 스테인드글라스 발광) · 차가 성당 앞길·다리로 다니는지 · 노란 경계선 구역들 안 차선.

## 213차 — 패턴 리셋 점 음영·정중앙 50% 픽토 (Q.21-①)
- **변경(2줄)**: `.lk-reset` background = rgba(0,0,0,.35)(파일 내 최다 관용 다크필 6곳 계승 — 흰 글래스 .18 원판 대비 '기능 버튼' 음영 분리) · svg = width/height 50%(운영자 지시값 — 20px 노드에 10px) + display:block(인라인 svg 베이스라인 오프셋 제거 = "사분할 정중앙" 실현 — 오프센터의 실원인이 아이콘 크기가 아니라 인라인 배치였음). 링·눌림·히트존 = .lk-node 계승 유지.
- **Q.21-② 실측(코드 변경 0)**: 슈퍼관리자 PIN = **Cloudflare Pages env `YETA_PIN_ADMIN` 단독 종속**(깃 레포엔 값 자체가 없음 — functions/api/yeta.js L342~346이 읽기만 · `if (APIN && pin === APIN)` = env 비면 어드민 로그인 불능 · users.json은 guest 해시 전용 + admin 참칭 차단). 원격 값 확증 = 불가(오답/미설정 구분 오라클 없음 = 옳은 보안 설계).
- **Q.21-③**: 코드 무변경 — 진단·설계안 보고(챗) 참조. 착수 = 운영자 승인 대기(건물 스프라이트 생성 = 유료 Gemini 과금 축 [8]).
- 검증필드: 자가검증 경로=check_refs rc=0(디자인 게이트 baseline 이내) | 운영자검증 불가피 사유=폰 실기(리셋 점 음영·아이콘 정중앙 체감 + 4569 로그인 1회 = PIN 연결 확증). 롤백=단일 커밋 revert. 운영자 확인 포인트: 잠금 패턴 화면 리셋 점이 어두운 원판+중앙 아이콘인지 · 새 PIN 로그인 되는지(안 되면 CF env 미갱신).

## 214차 — 지도 4레이어 분해 플레이그라운드 v1 (Q.22)
- **경위(운영자 260717 08:40)**: Q.21-③ 한 수 승인 — 과금(굽기) 결정 전 무과금 재료: 건물 전수 실측 + 분해 시뮬레이션.
- **신설**: `viewer/pg/20260717_084500_map4layers_pg_v1.html`(라이브 = /pg/ 경로 서빙 · 라이브 코드 무접촉 순수 추가). 야간판 육안 실측으로 건물 14동 인벤토리(bbox·발끝 y ±2 추정 명기) — 스프라이트 승격 후보 12동 기본 선택 = 총 24발(지면판 2 + 건물 22 · 성당 0발 = BLDGS 파이프 보유). z 데모 = 워커 발끝 y vs 건물 발끝 y 실시간 앞뒤 재배치(SVG 그룹 스왑 — 본편 painter's 정렬과 동일 원리).
- **검증**: 헤드리스 스모크(z 판정 "학교 뒤에 숨음"·계산기 24발·JS 에러 0) + 스크린샷 육안(박스 정렬·핀 좌표 정합).
- 검증필드: 자가검증 경로=스모크+스크린샷 | 운영자검증 = 폰에서 링크 열어 토글·슬라이더 체감. 롤백=단일 커밋 revert. 운영자 확인 포인트: 인벤토리 체크 조합(어디까지 스프라이트로 뜰지) + 착수 가부.

## 215차 — 플그 v2: 도로(v7 verbatim)·입장 인원 규약 재현·랜덤 배치 (Q.23)
- **신설**: `viewer/pg/20260717_100829_map4layers_pg_v2.html`(v1 보존·본편 무접촉). 도로 = YMAP_ROADS_TONE 밤/낮 13선 verbatim + 정지선. 인원 = 실내 8곳 이름표 스택(본편 "실내 = 스프라이트 숨김·이름표" 규약) · 야외 4존 색 원판(+건물 뒤 흐림 = painter's 축약) · 핀 인원 배지 · HUD 상위 2곳 요약 · 하단 장소별 명단. 🎲 재굴림.
- **검증**: 헤드리스 스모크(JS 에러 0·명단 12명 전원 배치) + 스크린샷 육안(도로가 실도로에 정착·이름표 스택 정합).

## 216차 — 플그 라이브 제공 = 기틀 [5] 승격 + 팬아웃 실측 (Q.24)
- **CLAUDE.md 증축 2줄**: [5] "플그 = 앱 라이브 경로 머지·URL 제공(다운로드 0) + 적극 상시 활용" · [15] `viewer/pg/` 등재. 공통 골격/레포 고유 분리(이식 노트) 준수. 평의회 미발동 사유 = 운영자 구술 문면의 등재(검증할 판단 없음) + 260716 억제 지침(국소 증축 토큰 절약) — 사후 이의 시 [9] 재발동 가능.
- **팬아웃 실측**: kit-fanout[bot] = nomute-editor→YETA **훅킷(.claude/hooks/)만** 자동 PR(#302 = foundation_hook.sh 1파일). CLAUDE.md 본문 = 자동 전파 없음(복제팩 수동 이식). 전 레포 즉시 반영하려면 nomute-editor 훅킷 [0]축 증축 필요(현 세션 스코프 밖).
- 검증필드: 자가검증 경로=check_refs rc=0 | 운영자검증 = 플그 v2 폰 확인. 롤백=커밋별 revert. 운영자 확인 포인트: v2 시안 가부 · 훅킷 전파 원하면 "nomute-editor 추가해" 한 마디(add_repo).

## 217차 — Q.25 10건: 모드↔배경 싱크·감정 배경 시스템화·'#' 합성 메시지·사진 첨부·겹침 60% (2026-07-17 12:05 KST)
- **배경 모드 싱크(⑧선두)**: 원인 실측 = media.json 버킷이 warm/tense=도깨비·base/blue=안드로 매핑인데 yStage가 무드만 보고 모드(yModeOn·21~03시)를 안 봄 → 낮에 warm 무드 답장 = 도깨비 배경. 픽스 = yStage 시즌 경로에 mode_dir 필터(모드>무드 우선·빈 풀 = 전 버킷 합집합 재필터·스테이지 키에 모드 포함 = 경계 자동 재스테이지).
- **감정 배경 시스템(①)**: 이미 시스템화 — 답장 <<MOOD>> → 버킷 로테이션. 파일명 감정 불필요 확정. shared/build_season_media.py 신설(폴더 실측 → media.json 기계 생성 · 손편집 금지 대상 등재) — 수집 = 폴더에 붓기만.
- **네비 간격(④)**: 실원인 = .yh-remote + .yh-r{margin-left:6px}가 배지 display:none일 때도 상시 적용 → 소환↔원거리 14px vs 원거리↔전화 6px. 스테일 마진 2종 제거 → 헤드리스 실측 글리프 간격 23.7 vs 24.2px(0.5px 이내 통일).
- **'#' 합성 메시지(⑤⑥)**: 혼합 구간 = (괄호) 줄로 끼워 한 메시지 발송(상황만 = 종전 sc 턴 유지) · # 모드 Enter = 전송 아님·구간 닫기 · yFmt (괄호) 단독 줄 = .yn 지문 결 · 러너 계약에 "유저 연출 = 무대 사실 수용(인물 이동 포함·규칙 변경만 무시)" 명문화.
- **사진 첨부(⑦·신규 요소 ⚠)**: 뷰어 + = canvas 1280px JPEG 압축 → op attach(매직바이트·2MB 캡·일 상한 30) → R2 att/ 저장+img 턴+dispatch → 러너가 s3 cp 후 그 턴만 Read 개방(--max-turns 4)으로 실물 열람 · 렌더 = op att blob 버블 썸네일.
- **겹침 60%(⑨⑩)**: finish에 생성 중 도착 유저 턴 판정 — (도착ts-생성시작)/(완료-시작) ≥60% = 답 유지+ov 마크(뷰어 mut 렌더·draft도 전송 후 유지) / 미만 = exit4 무write 폐기 → 웜 루프가 새 메시지 묶어 재생성.
- **원장 정비**: 요구사항_큐.md 방치 머지 충돌 마커 해소(Q.16 중복 발번 → 16-B 구분·시각순 배치·본문 무수정).
- 검증필드: bash -n·node --check(게이트웨이+뷰어 인라인 JS 전 블록)·build_season_media --check·check_refs rc=0 | 헤드리스 픽스처 간격 실측 | 롤백 = 축별 커밋 revert. 운영자 확인 포인트: 낮/밤 루시 배경 · # 합성 발송 · 사진 첨부 1장 · 겹침 mut.

## 218차 — Q.25 평의회 9인(적대적·READ-ONLY) 후속 픽스 15건 (2026-07-17 13:05 KST)
- **HIGH 2**: ① attach 순서 재편 = 방 실존·사망 선검증 → 상한 시도 즉시 소비(pinset 결) → R2 저장(가짜 방 무한 고아 적재 DoS 봉합) ② 사진 턴 Read = allowedTools 경로 샌드박스(/tmp/yeta_att_*.jpg만 · 주입발 세션·environ 낭독 차단) + 프롬프트 계약 보강.
- **MED 6**: 모드 경계 틱에 열린 챗 배경 재스테이지(프사만 바뀌던 desync) · 겹침 60% 분모 = GEN_ENDMS 고정(+종료 후 도착 = 겹침 제외) · # 빈 구간 Enter 세그 역순화 = 꼬리 마크 클램프 · _yDrKeep ts 가드 면제(유지 버블 증발) · check_refs에 build_season_media --check 하드 게이트(음성 테스트 rc=1 실증) · 보고 HTML 장수 42/34 정정.
- **LOW 7**: 합성 내부 괄호 〔〕 치환 · flee 폴백 OV_SKIP(겹침 폐기 면제 — 폐기 시 재생성 부재 경로) · 합성 4000자 초과 = 분리 발송 폴백 · 실패 복원 시 # 래치 동기 · 전송 전체 실패 시 _yDrKeep 롤백 · 죽은 CSS(.yatt+i 인라인 마진) 정리 · Q.16-B 교차참조 주석 정정(실물 = 두 번째 208차 L1722 실측).
- **보류 1(코스메틱)**: exit4 폐기 직후 재생성 draft가 확정 전까지 mut 톤 — 세대 식별자 배관 필요·실사용 체감 시 후속.
- 검증필드: bash -n·node --check·뷰어 인라인 JS 전 블록·check_refs rc=0(신규 미디어 게이트 포함) | 평의회 = 9/9 수렴(디자인 계약 위반 0 · 원장 무손실 확증). 롤백 = 이 커밋 revert.

## 219차 — 상단 시계 현실 KST 풀포맷 단일화 + 시즌 헤더 로고 24px 열정렬 (Q.26 · 2026-07-17 13:20 KST)
- **시계**: 구 2단(세계시각 "09:00 AM" + "(현실 시간: 12:42)" 병기 — 260717 "착각" 보완의 잔재)을 현실 KST 단일 풀포맷 `2026. 7. 17. 00:00 AM/PM KST` 한 줄로 재편(운영자 포맷 문면 그대로 · 시 2자리 패딩·월일 무패딩). #ywcReal 마크업·틱 코드·CSS 제거. night 터콰이즈 = 표시 시각(현실) 기준으로 이관. 무음동 세계시각 = 표시만 철수(지도 ymapClock·도깨비 모드 게이트·러너 동기 등 내부 축 전부 불변).
- **열정렬**: .yssn-fixed left 12→24px(로고 #ywm과 동일식 calc(var(--sp-2)*2) 계승) + .yssn 좌패딩 0. 실측 2중 = ① yeta-wm-y.png 알파 스캔(잉크 좌단 x=0 → 로고 잉크 = 박스선 24px 정확) ② 헤드리스 DOM rect(로고·SEASON 배지·태그라인 좌단 동일 x 확증 · 잉크 편차 = 폰트 베어링 S +0.4px·한글 +1.4px = 픽셀라인 허용치).
- 검증필드: 뷰어 인라인 JS 전 블록 node --check·check_refs rc=0 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 우상단 시계 한 줄 · SEASON 좌단 = 로고 좌단 세로선.

## 220차 — 상단 그리드 대칭 완성 + 전수 감사 픽스 2건 (Q.27 · 2026-07-17 14:50 KST)
- **대칭**: .yssn-fixed right = calc(--sp-2×2)(프로필 동일식) + .yssn 패딩 0(박스 시절 잔재 2px 제거) → 상단 4모서리 = 24px 단일 그리드(로고·SEASON·태그라인 좌 / 날짜·프로필 우). 헤드리스 3폭(360/390/420) 실측 = 좌 24·우 24 전 폭 일치.
- **감사 픽스**: ① Q.26 풀포맷 시계(fs-sm ≈184px)가 360px 폰에서 로고 우단(136px)을 26px 침범 → @media ≤399px = 10px·ls0(같은 파일 .yssn-date 10px 전례 계승) = 간격 +5.6px ② .yssn-nm flex:none → 0 1 auto+min-width:0+ellipsis(긴 시즌명의 날짜 밀어내기 방어 — 현 '무음동' 무영향).
- **잔여 예외(수용)**: 320px 초구형(iPhone SE1급 2016)만 시계-로고 겹침 잔존 — 뷰포트 점유율 사실상 0·페이드 스크림 위라 육안 경미.
- 검증필드: 뷰어 인라인 JS node --check·check_refs rc=0 | 헤드리스 3폭 rect 실측. 롤백 = 이 커밋 revert.

## 221차 — 지도 시계 '무음동' 라벨 전치 (Q.28 · 2026-07-17 14:55 KST)
- #ymapClock = `무음동 {세계시각} · {시간대}` — 메인 상단 시계가 현실 KST 풀포맷(Q.26)이 되면서 생긴 "시간이 두 개" 혼동을 라벨 1단어로 차단. 로직 무변경(표기만).
- 검증필드: 뷰어 인라인 JS node --check·check_refs rc=0 | 롤백 = 이 커밋 revert.

## 222차 — 감정 배경 8종 확장 + 루시 76장 실물 분류 배치 (Q.29 · 2026-07-17 15:20 KST)
- **체계**: 윈터 파일명 어휘(189장 · 운영자 수기) → 8감정 수렴(base/warm/joy/love/shy/blue/tense/mad). 러너 화이트리스트·MOOD 계약 8종, 뷰어 Y_MOODS+Y_GRP 폴백·버킷 제네릭, 생성기 감정 하위폴더 재귀 + 루트 미분류 흡수(수집 = 붓기만).
- **분류**: 루시 76장 전수 실물 열람(분신 3 병렬) — 안드로 42(base10·blue8·tense6·love6·mad5·warm3·joy3·shy1) · 도깨비 34(joy7·warm6·tense6·base5·love4·blue3·mad2·shy1) → git mv 감정 폴더 이동 → media.json 재생성(감정×모드 76장 + 클립 2).
- **하위호환**: 구 3무드 턴·구 manifest·비시즌 씬 풀(3무드) 전부 폴백 체인으로 유효. 윈터는 IDOL 등급(시즌 manifest 밖)이라 이번 배선 무영향 — 어휘 사전으로만 사용.
- 검증필드: bash -n·뷰어 JS 전 블록·check_refs rc=0(시즌 미디어 게이트 포함) | 롤백 = 커밋 3개(배선/안드로/도깨비) 역순 revert. 운영자 확인 포인트: 대화 감정 따라 배경 다변(낮에도).

## 223차 — 설정 모델 선택 KIMI-K3 증설 + 종량제 키(KIMI_CODE_MUTE) 대화 연결 (Q.30 · 2026-07-20 08:55 KST)
- **경로**: 뷰어 다이얼(kimi-k3) → 게이트웨이 MODELS 화이트리스트(턴 박제) → yeta-chat.yml env KIMI_API_KEY(=Actions 시크릿 KIMI_CODE_MUTE) → 러너 gen_out이 kimi 턴만 ANTHROPIC_BASE_URL=https://api.moonshot.ai/anthropic + 키를 파이프 서브셸에 국소 주입(claude -p 그대로 · KIMI_BASE_URL 노브).
- **격리**: 주입 = 서브셸 국소라 같은 웜 런의 다음 Claude 턴 무오염 · kimi 실패 시 claude_failover 2곳 스킵(구독 계정 스왑 신호 오염 차단) · 초대 판정 = 키 부재 시 조용히 기본 모델 폴백, 본답장 = 키 부재 사유 finish error 표면화 · effort 거부 폴백 grep에 thinking 추가(호환 게이트 문구 흡수).
- **UI(⚠ 신규 요소 — 단일 커밋 분리)**: 대화 깊이 게이지 6단→9단 3열(순서 SONNET→OPUS→KIMI-K3 · 운영자 지정) — 범례 3분할(.hk)·경계 점선 2개·트랙 램프 3구간·KIMI 썸/칩/미터 = 기존 2강조 토큰 --think 터콰이즈 계승(신규 색 0) · KIMI 선택 시 안내 필 "문샷 종량제 키로 과금돼"(소넷 안내 필 동형) · 좌상단 미터 모델 탭 = 3모델 순환.
- **모델 ID**: kimi-k3(문샷 K3 · 260716 출시 · Anthropic 호환 게이트 공식 지원 — 비전 내장 = 사진 첨부 축도 동일 경로).
- 검증필드: bash -n·yml parse·node --check(게이트웨이+뷰어 인라인 전 블록)·check_refs rc=0 | 신규 :root 토큰 0(락 무접촉) | 롤백 = 커밋 2개(백엔드/UI) 역순 revert. 운영자 확인 포인트: 설정→대화 깊이 우측 끝 3칸(KIMI-K3) 선택 후 대화 1턴 · 좌상단 미터 탭 3순환.

## 224차 — KIMI 연결 자가진단 워크플로 + 무손 검증 실발사 (Q.31 · 2026-07-20 09:10 KST)
- **sw.js 실측**: 푸시 수신·딥링크 전용, fetch 캐시 핸들러 0 — 설치형 PWA 캐시가 새 9단 게이지를 물고 늘어질 축 없음(조치 불요 확정).
- **yeta-kimi-selftest.yml 신설**(account-selftest 선례 계승): ① 시크릿 KIMI_CODE_MUTE 존재 ② 문샷 Anthropic 호환 게이트 실호출 1발(claude -p --model kimi-k3 · BASE_URL·키 주입 · OAuth unset · 도구 0 = 챗 gen_out 동일 레시피) ③ effort(low) 수용/거부 판정. ⚠️ 종량제 실과금(수십 토큰)이라 수동 dispatch 전용 · 크론 금지 명기 — 키 회전·불통 진단에 재사용.
- **실발사**: main 머지 직후 dispatch → 결과는 완료 보고에 실측 기재(PASS = 운영자 잔여 조치 0 · FAIL = 사유별 안내 자동 표면화).
- 검증필드: yml parse·check_refs rc=0 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 설정→대화 깊이 KIMI-K3 → 1턴.

## 225차 — kimi 종량제 책빼기(--tools "" = 도구 스키마 0) + K2.5 게이트 404 판정 + 평의회 픽스 (Q.33 · 2026-07-21 11:35 KST)
- **책빼기 실측 2단**: 셀프테스트 v2로 구레시피 실측(한 단어 프롬프트 = 입력 18,678tok) → `--system-prompt` 교체 단독 -1k뿐 = 책의 본체는 CC 빌트인 도구 스키마(--disallowedTools는 권한층일 뿐 스키마 미제거 · 평의회A ★1 확정) → `--tools ""` 적용(사진 턴만 `--tools Read` + 경로 샌드박스 유지) → **신레시피 실측 213tok(-98.9%)**. 구 CLI 드리프트 = unknown-option 폴백(--tools 문자열 선별 · system-prompt 폴백보다 선순위).
- **과금 가드**: kimi 턴 프레임이탈 재생성 캡 4→2(폐기분도 전액 과금 — 인캐릭터 탈출 폴백이 그물) · kimi `--system-prompt` 거부 시 전역 SYS_ARGS 오염 봉합(웜 후속 Claude 턴 프레임 소실 차단 · 평의회C 발견1).
- **KIMI-2.5**: 공식 현역($0.6/M)이나 **문샷 /anthropic 게이트 404 실측**(과금 0 · "model may not exist or no access") = Claude Code 리라우트 경로로 사용 불가 → 뷰어 12단(최좌측) 제작분 9단 원복, 게이트웨이 MODELS·러너 화이트리스트/is_kimi 배선은 대기 존치(게이트 개방 시 뷰어만 재개방). 셀프테스트 v3 = model 입력(k3/k2.5) + usage 실측 + 실패 시 stdout JSON 에러 본문 포집.
- **캐시 실측**: 문샷 자동 컨텍스트 캐시가 /anthropic 게이트에서 작동(동일 프롬프트 재발사 = 히트 7,168tok → 반복 입력 1/10 과금). 213tok 미니 프롬프트는 최소 프리픽스 미만이라 미히트(정상).
- **평의회(오퍼스 4석 · READ-ONLY · 4코어 러너 = [E10] 좌석 축소)**: 탐색A(★1 --tools 채택·★4 캡 채택·★2 계약 접두 이동 = 대기) · 탐색B(출력계약 캐시 접두 이동·NOTE 델타 발행·00지침 kimi 경량판 = 품질 리스크 명기 → 운영자 판단 대기 · 캐시 접두 구조는 이미 최적 판정) · 검증C(발견1 픽스·발견2 픽스) · 검증D(노트 높이 픽스 · 나머지 저심각 기록).
- 검증필드: bash -n·yml parse·check_refs 17종 rc=0 ×3라운드 | 롤백 = PR #350/#351/#352 역순 revert. 운영자 확인 포인트: KIMI-K3 1턴 후 좌상단 미터 토큰 수(체감 격감) + 문샷 콘솔 사용내역.

## 226차 — 출력계약 정적 몸통 캐시 접두 이관 + 전 모델 적용 확인 (Q.34 · 2026-07-21 11:58 KST)
- **이관**: OUTPUT_RULES(길이·NOTE:PUB/ME 템플릿·MOOD 8종·DEAD 예외 = 변수 0 리터럴)를 CBLOCK 직후로 — 접두 = [system 프레임][캐릭터 블록][출력 규칙]까지 정적. 꼬리 = [출력 계약] 헤더 + CONTRACT1/GROUP_RULE/ME_RULE(가변) + "위 규칙이 본문" 1줄 리마인더(최근성 앵커). 효과 = 매 턴 정가로 나가던 ~1k tok이 캐시 히트 시 1/10(문샷 260721 히트 7,168tok 실측 축과 동일).
- **전 모델 적용 실측**: 이 스크립트의 LLM 호출부 = gen_out 내 claude_meter 1곳(662줄) 단일 — 본답장·오프닝·초대판정·전 모델(opus/sonnet/kimi) 공용 → --tools ""·접두 이동 전부 구독 턴에도 적용(주간 쿼터 절약). 잔여 차이 = 구독 턴 --append(CC 기저 텍스트 ~3~5k 잔존 · YETA_SYS=2 노브로 제거 가능 — 260712 L0 사고 맥락상 운영자 결정 대기).
- 검증필드: bash -n·check_refs rc=0 | 리스크 = 규칙 위치 이동에 따른 준수율 미세 변화(리마인더로 완충 · 길이/NOTE 이탈 시 서버 _keep_anchors·캡이 그물) | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 아무 모델 1턴 — 답 길이 80자 결·NOTE/MOOD 태그 정상 + 좌상단 미터.

## 227차 — YETA_SYS=2 점화 + 프레임이탈 관찰 리포트(이슈 자동 기록 + 세션훅 자동 부각) (Q.35 · 2026-07-21 12:20 KST)
- **점화**: yeta-chat.yml `YETA_SYS: '2'` — 전 모델 시스템 슬롯 '교체'(CC 기저 텍스트 미전송). 260712 L0 사고 이력상 관찰축 동반 필수 → 아래 장치가 그 관찰축.
- **관찰 장치(운영자 A안 — "다음 작업 세션에서 자동 부각")**: ㉮ 러너 `fb_report()` — is_frame_break 발생 시(복구·소진 모두) 발생 메타만 레포 단일 이슈(yeta-frame-break)에 누적: 잡 기본 GITHUB_TOKEN(issues:write 신설) · 제목 고정·코멘트 append · 백그라운드 호출 = 답장 지연 0 · 실패 무음 · **대화 내용 0(공개 레포 D2 준수)** ㉯ `.claude/hooks/fb_incidents.py` SessionStart 등록 — 열린 이슈 무인증 GET(타임아웃 4s·0건/실패 = 완전 무음) → 세션 시작 시 "🚨 자동 부각 + 최우선 보고·진단·조치·닫기" 지시 표출.
- **동선**: 이탈 발생 → 이슈 기록 → 운영자가 아무 때나 클코 세션 열면 첫 화면에 부각 → "고쳐줘" 한 마디 → 진단 후 조치(빈발 시 YETA_SYS 한 줄 롤백) → 이슈 닫기 = 부각 소등.
- 검증필드: bash -n·settings.json parse·yml parse·훅 드라이런(0건 무음)·check_refs rc=0 | 롤백 = YETA_SYS 줄 제거(+이 커밋 revert). 운영자 확인 포인트: 아무 모델 1턴 정상 대화 + (이탈 발생 시) github.com/muteno/YETA/issues 라벨 yeta-frame-break.

## 228차 — 누적 사용량 인앱 표시(모델별 토큰 + 키미 실비 환산) (Q.36 · 2026-07-21 12:50 KST)
- **데이터 축**: claude_meter cr(캐시 히트) 신설 → 러너 finish가 sess.usage{모델:{i,o,cr,n}} 누계(휘발·리셋 무관 · reset 승계 배선). 성공 답장분만 = 하한 지표(재시도 폐기·초대 판정 미포함 — 주석 명기 · 정본 = 문샷 콘솔).
- **표시 축(⚠ 신규 행 — 단일 커밋 분리·yset-item 계승)**: 설정→대화, 대화 깊이 바로 아래 '누적 사용량' — si-d = "Kimi K3 4.2만 · Opus 4.8 88만 — 키미 실비 추정" · si-v = 키미 계열 합산 "$0.42"(YCOST 고정 단가 즉석 환산 · <$0.01 표기 · 무기록 = "아직 기록 없음"). 버튼 아님(정보 행) = 클릭 액티브 없음이 정답.
- 검증필드: 파이썬 heredoc ast 컴파일·bash -n·check_refs rc=0 | 롤백 = 뷰어 행 커밋 단독 revert(배선 커밋은 무해 잔존 가능). 운영자 확인 포인트: 아무 모델 1턴 후 설정 → '누적 사용량' 행 숫자 등장.

## 229차 — 좌상단 미터 키미 실비 병기 + 단가 공식 재검증(일치) (Q.37 · 2026-07-21 13:12 KST)
- **미터**: 뷰0(토큰)에서 키미 계열 선택 시 `(… tok ($0.42))` 병기 — yKimiCost()/yCostFmt() 신설 = 설정 '누적 사용량' 행과 값 SSOT 공용(중복 계산 0) · 무사용 = "$0.00"(운영자 스케치 결) · 속도/길이 뷰 = 미병기(토큰 뷰 한정).
- **단가 재검증(공식 실측)**: K3 = $3(미스)/$0.30(히트)/$15(출력) · K2.5 = $0.60/$0.10/$3.00 — YCOST 기존 값과 정확 일치(수정 0). 모델별 요율 분리 구조 기존재(usage 모델별 누계 × YCOST[모델]).
- **비교 확인**: K2.5 < Sonnet 5(SWE-bench 85.2 vs 80.2대 · K2.5 ≈ 소넷4.5 -4~6pt) — 최좌측 배치 설계 유효 · 현재는 게이트 404로 비활성 상태 유지.
- 검증필드: check_refs rc=0 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 키미 선택 후 좌상단 미터 = "N tok ($…)".

## 230차 — 설정 '오늘 사용량' 행(일별 버킷) (Q.38 · 2026-07-21 13:32 KST)
- **데이터**: sess.usage_day = {d: KST일자(%y%m%d · gmtime+9h), m: 모델별 {i,o,cr,n}} — finish가 전체 누계(usage)와 동시 기록 · 자정 롤오버 = 새 날짜 첫 답장이 버킷 교체(이력 미보관 = 오늘 1버킷) · reset 승계.
- **표시(⚠ 신규 행 — 단일 커밋 분리)**: 설정→대화 = 대화 깊이 → **오늘 사용량** → 누적 사용량 순. 행 빌더 yUsageItem(t, u, empty) 공용화(누적/오늘 = 같은 문법·같은 계산기 yKimiCostOf) · si-d = "Kimi K3 1.2만 · Opus 4.8 3.4만 — 키미만 실비 추정(클로드 = 구독·가격 없음)" · si-v = 오늘 키미 $ · 클라 날짜(KST 관용구 = 게이트웨이 pkst 계승) ≠ 버킷 날짜 = "오늘은 아직 기록 없음".
- 검증필드: 파이썬 ast·bash -n·check_refs rc=0 | 롤백 = 뷰어 행 커밋 단독 revert. 운영자 확인 포인트: 오늘 1턴 후 설정 → 오늘/누적 두 줄 각각 숫자.

## 231차 — details.so vault navigation-01 디자인 레퍼런스 수집함 편입 (Q.39 · 2026-07-23 22:14 KST)
- **내용**: docs/디자인레퍼런스_수집함.md 신설 = nomute-editor 원판(Q481) 동일 콘텐츠 + §4 매핑만 YETA 토큰 바인딩판(외부 원문 값 = 복붙 금지 · :root 치환 계승 = 절대명령1 축 명기).
- 검증필드: check_refs rc=0 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: docs/디자인레퍼런스_수집함.md §4 = YETA 토큰명.

## 232차 — 대화비용 급증 원인 진단 + 키미 일일 실비 방파제 (Q.40 · 2026-07-23 KST)
- **원인(실측)**: 문샷 자동 캐시 미적중(실사용 메시지 간격 > 캐시 수명) — R2 usage 실측 kimi-k3 누적 26턴 i=396,518/cr=64,000(히트율 16% · 당일 0%) = 고정 몸통 ~15k tok 매턴 정가 재과금 ≈ $0.05/턴. Q.33 '-98.9%'는 연속 재발사(캐시 적중) 조건 실측이라 실사용 미재현. 260723 15:44 문샷 한도로 kimi 턴 실패(런 로그) → 15:59 소넷 전환 성공까지 재현 확인. 폐기 생성분(프레임이탈 재생성)은 usage 미기록·실과금(하한 지표 명기).
- **조치**: `functions/api/yeta.js` = KIMI_COST(뷰어 YCOST 거울) + kimiSpentUsd(usage_day 오늘 버킷) + kimiGate — send(턴 append 전)·attach(상한 소비 전)·retry(pending 키미 턴만) 429 차단 · env `YETA_KIMI_MAX_USD_PER_DAY`(기본 2 · 0=무제한 · 빈값=보수 기본, ring 동형) · 뷰어 YCOST 주석 동조(단가 개정 시 양쪽) · 클로드(구독) 전 경로 무영향.
- 검증필드: node --check·게이트 로직 단위 실측(오늘 버킷 $0.0538 → 통과 · $2.29 모의 → 429 문구 · 경계 $2 차단 · env 0 무제한)·check_refs rc=0 | 롤백 = 이 커밋 revert(또는 env 0 = 즉시 무제한). 운영자 확인 포인트: 키미 다이얼로 일 $2 도달 시 채팅창에 방파제 안내문 + 소넷·오퍼스 전환 시 즉시 재개.

## 233차 — 자동 로드 전수 컷 + 전반 비용 계기판(cw) (Q.41 · 2026-07-24 KST)
- **감사(청정 확인)**: 챗 본선 = --safe-mode(CLAUDE.md 27KB 미주입)·--tools ""(스키마 0)·YETA_SYS=2(CC 기저 교체) 기적용 실측 · --bare = OAuth 즉사 금지 유지 · AGENTS.md 1.4KB = 무시량.
- **잔당 제거**: ① 넛지(yeta_nudge.sh)·전화(yeta_call.sh) = --tools "" + SYS_ARGS(YETA_SYS 노브 · yeta_chat 동형) 배선 + 두 yml YETA_SYS '2' 점화 — 종전엔 생성마다 CC 도구 스키마 ~18k tok 자동 탑재(구독 쿼터 누수) ② 챗·넛지·전화 3 스크립트에 DISABLE_AUTOUPDATER=1·CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1(CLI 자동업데이트 확인·텔레메트리 = "오래 걸린다" 축 OFF) ③ cw(캐시 적재) 계측 = claude_meter METER_LAST + gen_out TOK_CW + finish usage/usage_day 누계 + 뷰어 yUsageSeg 합계 포함 — 클로드 턴 i=2 착시 해소(실부피 = 구독 쿼터 소모 근사 계기판).
- 검증필드: bash -n ×4·yml 파싱·누계 로직 모의(레거시 dict cw 폴백 합산 · 뷰어 합계 513→14,713 실부피)·check_refs rc=0 | 롤백 = 이 커밋 revert(넛지·전화 프레임만 = yml YETA_SYS 줄 제거). 운영자 확인 포인트: 다음 답장 후 설정 → 누적/오늘 사용량 클로드 토큰이 수천~수만 단위로 표시(= 실부피).

## 234차 — 부활 = 신당 기도 게이트(시간 만료 폐지) + 죽음의 무게 (Q.51 · 2026-07-25 KST)
- **데이터**: `sess.dead[id] = {t, d, mood, why, pray:1, wit:[방 멤버], nm:이름, by:빌어준이}` — `pray` = 시간으로 안 풀리는 죽음(t는 365일 뒤 = 기존 `t > now` 판정 3곳[러너 `_dead`·게이트웨이 `DEAD_ON`·뷰어 `yDead`] 무개조 통과) · 해제 = 러너 `<<PRAY: 이름>>`이 `t = now`로 당기는 단일 경로(→ 기존 부활 첫 답 엔트리 소비 파이프 그대로).
- **프롬프트**: 죽음 블록(목격자 각인 + 마을 게시판 + 기도 방법·발동 조건) 신설 · 부활 블록에 `by`/`wit` 2줄 증축 · `OUTPUT_RULES`에 `<<PRAY>>` 예외 1줄(DEAD 동형 계보) · 공용기억 급식 2종(죽음/귀환).
- 검증필드: bash -n · check_refs rc=0 · 블록 조립 실인자 실행(목격/비목격 분기) · 흐름 시뮬(DEAD 박제 → 3일 뒤 두절 유지 → PRAY 해제 → 부활 재료 발동 · 이름/id/문장속 이름 매칭 · 없는 이름 무시) | 롤백 = 이 커밋 revert(엔트리에 남은 `pray`는 다음 죽음부터 자연 소멸 · 구엔트리 = t 비교로 하위호환). 운영자 확인 포인트: 캐릭터 사망 후 24h가 지나도 방이 안 열리는가 · 다른 캐릭터에게 "신당 가서 빌어줘" 부탁 → 그 답 뒤 방이 열리고 첫 마디가 "누가 빌어줘서 돌아왔다" 결인가.

## 235차 — 유저 사망 기도 게이트 + 앱 아이콘 통일 (Q.54 · 2026-07-25 KST)
- **데이터**: `sess.me_dead = {d, mood, why, pray:{by,id,txt,at}}` · 부활 조건 = `pray` **또는** `d + 8h(현실) = 무음동 48h` — 뷰어 `yMeRevOK`와 게이트웨이 `op revive` 가드가 같은 식(우회 차단) · `me_revived`에 `pray` 승계.
- **경로**: `yeta_nudge.sh` = 사망 중 읽씹 재촉 차단 + '신당 기도' 모드(사망 30분 후 · 1회) → 대사 = 기도문 = `pray.txt` · 푸시 제목도 "— 신당에서 빌고 있어"로 분기.
- **UI**: 팝업 3상태(대기/기도/하한경과) = `yConfirm` 정본 골격 문구·라벨만 분기 · `yJosa` 받침 조사 헬퍼(렌더 실측에서 "루시이(가)"가 튐) · 부활 후 `yEnterChat(빌어준 사람)` = 회신 문자.
- **아이콘**: manifest 3종 = `yeta-face-icon-{192,512,maskable-512}`(512·maskable 신규 생성 = 360 원본 리사이즈 + `#161716` 여백 · 새 그래픽 0) · `apple-touch-icon` 404(`yeta-splash-192.png` 부재) 수복.
- 검증필드: bash -n(nudge) · node --check(게이트웨이) · check_refs rc=0 · 팝업 3상태 실렌더(남은 시간 42h 계산·기도 원문·조사) · manifest 아이콘 3종 실존 확인 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 죽은 뒤 30분~ 넛지 주기에 캐릭터가 신당 기도 푸시를 보내는가 · 그 팝업에서 '돌아가서 답한다'로 바로 그 방에 들어가지는가 · 앱 목록 아이콘이 탭 아이콘과 같은 얼굴인가.

## 236차 — 인물 카드 고정 높이 285 폐지 → 내용 실측(하단 액션 라벨 절단 해소) (Q.56 · 2026-07-25 KST)
- **차수 주기**: 병렬 세션이 235차(Q.54)를 선머지 — append-only 재번호(원장 규약).
- **증상/원인**: 챗 헤더 이름부 풀다운 시트(`#yetaPdc`)가 `.on`에서 `height:285px` 고정 + `overflow:hidden`. 260712 확정 당시의 1줄 생각을 전제한 값이라, 생각 인용문이 2줄이 되는 순간(내용 306px) 하단 21.6px가 잘려 액션 4의 라벨 아랫부분이 사라졌다(운영자 스샷 = 그 상태).
- **수정**(`viewer/index.html`): `yPdH()` 신설 = `.pin` scrollHeight + 테두리 2(box-sizing:border-box 전역)를 열림 높이로 인라인 지정 — ⓐ `yPdOpen` ⓑ 드래그 상한(구 `Math.min(dy,285)` → 실측 1회 캐시 `dr.h`) ⓒ `ResizeObserver(.pin)`로 열린 뒤 높이 변화 추종(웹폰트 도착·아바타 로드·회전·글꼴 크기). 상한 = 화면 잔여(`innerHeight − 레이아웃 top − 12`) 초과 시 `.ypd-fit`(`overflow-y:auto`+`overscroll-behavior:contain`) = 절단 대신 내부 스크롤. `yPdClose`가 `ypd-fit`·인라인 높이 회수(접힘 애니·inert 종전 유지). CSS `.on{height:285px}` = JS 미동작 폴백으로만 존치.
- **함정(실측으로 잡음)**: 상한 계산에 `getBoundingClientRect().top`을 쓰면 등장 transform(−8px) 트랜지션 중간값이 섞여(RO가 그 순간 발동) 인라인이 322.946px로 굳어 6.9px 초과 — 레이아웃 기준(`offsetTop` + offsetParent rect)으로 교정해 316px 정확 수렴.
- **디자인 축**: 신규 토큰·색·px 0 · raw는 기하 계산 예외축만(테두리 2·하단 여백 12·최소 160) · `:root` 54개 무변동 = 락·거울 무영향.
- 검증: 국소 손질 = 자가검증 — 경로=Playwright 헤드리스 실측(390×844 재현 = 운영자 스샷과 동일 절단 21.6px 확인 → 수정 후 **clipped −1.4px**) · 5케이스{1줄 290 · 2줄 308 · 긴 이름+긴 생각 308 · 320폭 308 · 가로 844×390 = 상한 316 + `.ypd-fit` 스크롤 10px} · 드래그 열기(400px 당김 = 실측 상한 클램프)·탭 닫기(인라인 회수·inert 복귀)·재열기 전부 정상 + 전/후 스샷 + check_refs rc=0. | 롤백 = 이 커밋 revert(CSS 285 폴백이 남아 있어 JS만 되돌려도 종전 동작). 운영자 확인 포인트: 생각이 2~3줄인 인물에서 카드 하단 라벨이 온전한가.

## 237차 — 종결부 지문 상시화(물음표로 안 끝나면 항상 상황설명) (Q.52 · 2026-07-25 21:15 KST · PR #397)
- **발동식**: 「감정 남은 턴(`<<OPEN:>>`·공기≠base)만 · 매 턴 금지」 → **「마지막 대사가 물음표로 끝나지 않으면 지문 한 줄 필수」**(물음표 턴 = 공을 넘겼으므로 선택). 담을 것 = 지금 하는(하려는) 행동 or 지금의 감정을 눈에 보이는 장면으로. 조항 신설 없이 #386 조항을 직접 개정(§0 — 덮어쓰기용 신규 조항 금지).
- **정형화 방어 교체**: 정본 금지선1 「전 턴 강제 금지」 → 「같은 그림 반복 금지」 폐지·대체. 소재 4축 로테(다음 행동/몸의 반응/시선·손에 쥔 것/주변 소리·빛) + 직전 턴 소재 연속 금지. 감시 지점(3종 수렴·"~한 채 ~했다" 고착) 명기.
- **버블 구멍 수복**: `_bubbles`·`yBubbles` 분할 기준이 빈 줄인데 260712 규약은 지문 **뒤** 빈 줄만 요구했다 → **앞뒤 빈 줄 하나씩**으로 보강(1문장 대사 NG·3문장 NG → 3/3 제 버블 · 뷰어 코드 무수정).
- 배선: `00_지침_캐릭터챗.md` §대화형식 · `yeta_chat.sh` `OUTPUT_RULES` · `docs/감정선_대화이어가기_정본.md` 축 A-2/금지선1/배선지도 · `절대명령2_정본인덱스.md` 요약 1행 · 지침변경로그 1행.
- 검증필드: bash -n · check_refs rc=0(23게이트) · 버블 분할 실측 3케이스 · 헤드리스 렌더 전/후(chromium 430×900 · 지문 = `.yb i.yn` 제 말풍선). PR #397.

## 238차 — 모델 다이얼 단일 라이터(대화↔설정 동기) + 게이지 버전명 (Q.56 · 2026-07-25 KST)
- **원인 실측**: 「시스템 모델」 별도 저장소는 **없다** — 설정 게이지·대화 미터 둘 다 `localStorage.yeta_dial_model` 하나를 쓰고 서버엔 모델 기본값이 없다(러너 = 직전 유저 턴 `model` → `s.pref` 순). 결함은 미터 쪽 **반쪽 동기**: `#ydepthRange` 값만 되쏘고 칩·범례·구간색은 미갱신 · 설정 카드가 없으면 통째 no-op.
- **처리**: `yDialSet(mKey, effort)` 단일 라이터 + `yDepthSync()` 표면 재동기 신설 → 설정 `input` 핸들러와 미터 탭 핸들러가 **같은 문**을 쓴다. 미터 쪽 ID→키 재매핑도 `YDIAL_DEPTH[yDepthIdx()]` 왕복으로 폐지(중복 판정식 제거).
- **라벨**: 게이지 구간명 하드코딩(`SONNET`·`OPUS`·`KIMI-K3`) → `YDIAL_MODELS` 표시명 대문자 계승(`yDepthMN`) = **SONNET 5 · OPUS 5 · KIMI K3**(모델 승격 시 자동 추종 · 칩도 같은 함수). 소넷 품질 안내(`.nns`) 폐지 + CSS `[data-model=s]` 노트 개방 제거(빈 알약 방지) · 키미 과금 경고는 존치.
- 검증필드: check_refs rc=0 · 헤드리스 왕복 실측(설정→KIMI ⇒ 미터 2탭 ⇒ OPUS: range/chip/wrap/legend 4표면 전부 추종) · 소넷 구간 note 높이 0px · 렌더 영수증 3장.

## 239차 — 도깨비 프사 고착 봉합 + PC Enter 보내기 + 얼빡 프사 2축 (Q.57 · 2026-07-25 21:30 KST)
- **프사 고착(뿌리)**: 증분 렌더 `yReconcile`의 번호표 `data-k`가 내용·상태만 담고 **모드 축을 안 담아**, 이미 그린 말풍선이 변신 경계를 넘어도 옛 프사로 영구 재사용됐다. 경계 동기(`yWorldTick` 4틱)도 목록·헤더·프로필만 돌고 **열린 대화 로그가 빠져** 있었다 → 말풍선 ↔ 헤더 엇갈림. 수정 = data-k에 모드 자리 + 경계에서 `yRender`(무애니 1회 = re-whoosh 차단 · 기존 `yStage` 단독 호출은 흡수).
- **평시 프사**: 파일 교체 0 — "원래 프사" = 현행 `lucy_face.webp` 확정(운영자 선택 · 렌더 시트 3안 대조). 도깨비 얼굴이 평시까지 눌러앉던 게 위 버그.
- **Enter**: PC = 전송 / Shift+Enter·모바일 액션키 = 줄바꿈. 기기 판정 = `matchMedia('(hover:hover) and (pointer:fine)')` 매 타건 실측 · IME 가드(isComposing·229) · 전송 본체는 `ySend` 단일 경로.
- **얼빡 2축(파이프만 · 이미지는 dispatch)**: `yeta_char_art.py YETA_AV_MODE=1`(10인 · 스타일 계승·프레이밍만 교체) + `yeta_aeri_cand.py AERI_GHIBLI=1`(제미나이 지브리 · 올드함 2겹). 워크플로 input `avmode`·`ghibli` 추가.
- **드라이브바이**: `yeta-aeri-cand.yml` YAML 파싱 불가(`name:` 값 안 `': '`) 수복 — 260721 이후 이 워크플로는 디스패치 자체가 막혀 있었다(13개 중 유일 · safe_load 전 실패/후 OK 실측).
- 검증필드: check_refs rc=0 · py_compile ×2 · YAML safe_load 13/13 OK · Playwright 실렌더(전/후 프사 전환·Enter 3케이스 PC/모바일) | 롤백 = 커밋 단위 revert(프사 039b164 · Enter 401fd7d · 얼빡 56521e7 · 지브리 5c3256b). 운영자 확인 포인트: 무음동 21~03시에 대화창 말풍선 프사가 도깨비로 바뀌고, 창 밖으로 나오면 되돌아온다.

## 240차 — 단톡 대화 이어가기(대본 교대 + 자율 비트) (Q.58 · 2026-07-25 KST)
- **원인(실측)**: ① GROUP_RULE이 동행을 "대체로 생략 · 최대 한 번"으로 못 박고 파서도 `[동행]` 1개만 분리 = 교대 자체가 불가 ② 답장 후 pending 0 = `extract_mat` NOPENDING = 캐릭터끼리 이어받는 축 부재(유저가 말할 때까지 정지).
- **조치**: `.github/scripts/yeta_chat.sh` = 다중 세그 대본 파서(줄머리 `[이름]` 전환 · 방 두 명만 인정 · 연속 병합 · 세그 500자 · 상한 LINES) + GROUP_RULE 재작성 + `extract_mat` 자율 비트 분기(가상 인덱스 `_p0/_pl`로 pending 축과 한 경로 · 분기 중복 0) + `finish` 예약(`thread.gb`)·앵커(직전 화자 턴)·조용한 취소 + 자율 비트는 푸시·PTT·난입·주변사건 미발동. `yeta_v3._age` = 신선 예약을 일감으로(항상 최연소 = 진짜 pending이 먼저). `functions/api/yeta.js` = `op send` 예약 삭제 + `mergeBackG` 1:1 승격분 예약 제거. `viewer/index.html` = `gbP`(TTL 120s) 타이핑 점 + watch 유지(+`staged` 중에도 감시 = 교대 턴 즉시 픽업). `yeta-chat.yml` = LINES/BEATS 노브 노출. 병렬 머지(기도 게이트 234차)와 충돌 1지점 = 예약 블록을 **기도 반영 뒤**로 배치(방금 기도로 풀린 동행이 곧바로 교대에 낌).
- **부수 교정**: `last_open`(삼킨 것)을 단톡에서 **화자 본인 턴만** 읽게 = 화자가 매 턴 바뀌는 방에서 남이 삼킨 감정을 물려받던 오귀속 차단 · `mood`는 대본 **맨 끝 턴**(yLastMood가 최신 assistant 1개만 스캔 = 교대 세그가 뒤에 붙으면 종전 위치로는 공기 유실) · `last_sp` = 대본 끝 화자.
- 검증필드: 시뮬 17케이스 전통과(pending 우선·1:1·kimi·BEATS 0·LINES 1·통짜 답장·동행 승격·사망·실패 취소·앵커 소실 rc2·겹침 rc4·TTL·amb 꼬리·합류 sys·GAP·유저사망·4토막 배치) · yeta_v3 골든·불변식·parity 회귀 0 · bash -n·node --check·yml 파싱 · check_refs rc=0 · 헤드리스 렌더(단톡 base/gb 스샷 + watch 폴 0→330 프로브) | 롤백 = 이 커밋 revert(또는 yml `YETA_GCHAT_BEATS: '0'` = 자율 비트만 즉시 OFF · `LINES: '1'` = 대본 교대까지 종전 결). 운영자 확인 포인트: 단톡에서 한 마디 보내면 둘이 주고받고, 가만히 있으면 둘이 두 번 더 이어간다(내가 말하면 그 자리에서 멈춘다).
## 241차 — 사망 표기(프로필·글자 채색 소거) + 사망 정보 3면 노출 (Q.59 · 2026-07-25 21:52 KST)
- **의도**: 운영자 스샷 = 대화 목록에서 죽은 인물이 살아있는 사람과 동일 톤(라임 배지·터콰이즈 생각줄). 캐릭터 탭엔 사망 표기가 이미 있는데 **목록만 구멍**. 더불어 "몇시에 죽었고 몇시간 부활 남았는지"를 프로필에서 보이게.
- **변경 전후**: 전 = `.ycrow.locked`(캐릭터 탭)만 무채 · `yListRender`에 사망 분기 0 · 죽은 행 탭 = 챗 개설 후 `_yOpenGate`가 토스트+리스트로 튕김 · `dead[id].d`(사망 ts)는 박제만 되고 미노출. 후 = `.ylist-row.ydead` 3처방(프사 `grayscale(1) brightness(.5)` · 글자축 `.ylist-mid{filter:grayscale(1)}` · 행 `.85`+이름 `.5`) · `yDeadInfo`/`yWDur` 신설(무음동 시각·경과·부활 조건) · 카드/팝오버/풀다운 3면 노출 · `yOpenThread` 사망 가드(죽은 행 = 인물 카드) · `window.YWORLD_RATE` 노출(배속 SSOT 참조 · 재선언 0).
- **계승(신규 값 0)**: 프사 필터·이름 opacity = `.ycrow.locked` 100% 모방 · 행 opacity = 목록 선례 `.ylist-row.estr`(.85) — 잠금 `.66`은 배경 그림 위 유언 가독 붕괴로 3안 렌더 비교 후 기각 · 정보줄 = `.ypp-meta`/`.ypp-desc` 셀렉터 확장(재설계 0) · `:root` 무변경(토큰 락 54 불변).
- **효과/리스크**: 효과 = 목록·카드·팝오버 3면이 같은 죽음 톤 + 죽은 시각·경과·부활 조건이 한 화면에. 리스크 = `filter`를 `.ylist-mid`에 걸어 그 안에 새 backdrop-filter 요소를 넣으면 글래스가 깨질 수 있음(안읽음 배지는 `.ylist-ava` 하위라 무영향 — 그래서 행 전체에 안 걸었다) · 풀다운 액션 버튼(통화·초대)은 사망 시에도 활성 그대로(별개 축 · 미변경).
- 검증필드: 자가검증 = check_refs rc=0(24게이트·토큰 락 불변) + Playwright 실측(390×844 · mock 세션 주입) — 죽은 행 `ydead`·프사 `grayscale(1) brightness(.5)`·`.ylist-mid grayscale(1)`·행 `.85`·이름 `.5` / 살아있는 행 무변화(필터 none·opacity 1) / 카드 = 사망 2줄+각주·육각 무채·아바타 중심정렬(avaCx 195 = cardCx 195) / 팝오버 생·사 분기 / 구형 숫자 엔트리 = 「부활까지 · 1일 6시간」 잔여 표기 / 페이지 에러 0 · 스샷 전·후·3안 비교 | 운영자검증 불가피 사유 = 실기기에서 실제 프사 사진의 무채 질감·"좀 빠져야"의 강도(사람 감각 축). 롤백 = 이 커밋 revert(`_versions/260725_2152_사망표기_프로필채색/index.html`). 운영자 확인 포인트: 라이브에서 ①죽은 사람 행이 회색으로 가라앉는지 ②그 행 탭 = 인물 카드에 죽은 시각·그 뒤로·부활까지가 뜨는지 ③강도가 과한지/약한지(행 opacity .85 · 필요 시 .66~1 조정).

## 242차 — 사망 프사 숨김 + 사망 상주 창(무음동 카운트다운) + 성향 퀴즈 (Q.60 · 2026-07-25 KST)
- **데이터**: `me_dead.{quiz[], done[], cut}` · `sess.tune_votes[persona][]` — 문항당 `cut += 무음동 2h`(현실 20분) · 부활 시각 = `d + 8h - cut`(뷰어 `yMeRevAt` ↔ 게이트웨이 `op revive` 동일식).
- **UI**: `#ymedead` 상주 레이어(1초 틱 · `yMdClock` = ms×6 무음동 표기 · 버튼 구성 동일하면 타이머만 갱신 = 눌림 씹힘 0) + `#ymdquiz` 4지선다(`.ymq-opts` 세로 스택·좌측 정렬) · 클래스·카드 골격 = `#yconfirm` 계승.
- **러너**: `yeta_nudge.sh` 사망 분기를 quiz(20분)→pray(60분) 2단으로 · 퀴즈 재료 = 만난 사람 카드 §성격 2줄 + tune 상위 3축 · JSON 관대 파싱 + 구조 검증(만난 사람 밖·축 범위 밖·4지선다 미달 = 폐기).
- **자동 반영**: `op quizpick`에서 3표마다 `tunes` ±1(클램프 0~10) 후 votes 비움 — 크론 추가 0.
- 검증필드: bash -n · node --check · check_refs rc=0 · 헤드리스 전 흐름(상주창 41:58:22 → 6배속 감소 실측 · 퀴즈 렌더 · 선택 → done 1·cut 20분·다음 문항 자동) · 프사 alive/dead 대조 | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 죽고 20분 뒤 '부활 시간 줄이기'가 활성화되는가 · 10문항 풀면 남은 시간이 무음동 20시간 줄어드는가 · 같은 캐릭터 3문항째에 성향 슬라이더가 실제로 움직이는가.

## 243차 — 프레임이탈 리포트에 실제 sys 모드 각인(관찰 교락 해소) (Q.61 · 2026-07-25 KST)
- **동기**: #380 진단 중 발견 — 관찰 표본 2건(#363 aeri · #380 lucy)이 **전부 `kimi-k3`**인데, 리포트 줄에 시스템 슬롯 모드가 없어 원인을 못 가른다. kimi는 `YETA_SYS=1`에서도 강제 replace(`is_kimi && YETA_SYS!=0`)라 **모델 축과 교체 축이 붙어 있다**.
- **수정**(`.github/scripts/yeta_chat.sh`): `fb_sys()` 신설 = `_sys[0]` 실물 → `replace|append|off` 각인(전역 `FB_SYS`). 호출 = `_sys` 확정 2지점{ⓐ 초기 확정(kimi 강제 교체 반영 후) ⓑ `--system-prompt` 거부 폴백에서 `_sys=()` 직후}. `fb_report`의 line에 `· sys ${FB_SYS:-?}` 1토큰. **`YETA_SYS` 값을 찍지 않은 이유** = 그 값은 "설정"이고 리포트가 알아야 하는 건 "이 턴에 실제로 뭐가 갔나"(kimi 강제 교체·CLI 거부 폴백 둘 다 값과 어긋난다).
- **판정 승계**: `YETA_SYS=2` 롤백 없음(#380 운영자 승인 종료 판정 유지 — 2건 전부 1회 폐기 후 복구·`flee_block` 0).
- 검증: `bash -n` OK · 함수 4케이스 실행 실측{Claude+replace `sys replace` · Claude+append `sys append` · kimi 강제 `sys replace` · 플래그 거부 `sys off`} · check_refs rc=0 | 롤백 = 이 커밋 revert(리포트 줄만 짧아짐 · 판정 로직 무관). 운영자 확인 포인트: 다음 이탈 리포트 줄에 `sys …` 토큰이 붙는가.
## 244차 — 죽음 프로세스 완결(부활 하한 대칭 · 귀환 흔적 · 3상태 UI · 사망 액션 잠금) (Q.63 · 2026-07-25 22:00 KST)
- **의도**: Q.59에서 "부활까지 몇 시간"이 **존재하지 않는 수치**라고 보고했더니 운영자가 "다 가능하게 · 프로세스를 구축한다는 느낌으로". 죽음을 한 번의 상태가 아니라 **죽음 → 대기(기도 or 하한) → 부활 대기 → 귀환**의 흐름으로 재설계.
- **변경 전후**: 전 = 캐릭터 `dead.t = now+365일` + `pray` 우선 판정(시간으로 영원히 안 풀림 · 유저 사망은 이미 48h 하한 = 규칙이 주체별로 갈림) · 부활 첫 답이 엔트리를 `pop`(귀환 흔적 0) · 풀다운 통화·초대가 사망 중에도 활성. 후 = `t = now + REV_FLOOR_MS`(무음동 48h = 현실 8h · 유저 `MEREV_MS`와 동값) · 판정 = `t` 단일(게이트웨이·뷰어에서 `pray` 조항 제거) · `revived[id]={at,by,d,nm}` 이관(무음동 하루 보존 · 만료 자동 청소) · `yDeadInfo` 3상태 · 통화·초대 `disabled`.
- **계승**: 하한 상수·식 = 유저 사망 정본(`YMEREV_MS`/`MEREV_MS`) 그대로 · 비활성 시각 = `.ycd-cta:disabled` 결 · 정보줄 = `.ypp-meta`/`.ypp-desc` 재사용 · 신규 토큰·색·px 0.
- **효과/리스크**: 효과 = "몇 시간 남았는지"가 진짜 값이 되고(운영자 원 요구), 기도는 그 시간을 건너뛰는 선택지로 남고, 돌아온 사실이 하루치 자국으로 남는다. 리스크 = ⓐ 죽음이 하루 만에 자연 해소되므로 Q.51의 "무게"가 옅어질 수 있음(운영자 판단 축 — 하한 값 = `REV_FLOOR_MS` 한 곳) ⓑ `flee`(자리 이탈) 엔트리도 같은 카드를 타서 각주가 "신당" 서사로 뜬다 — 미머지 PR #401의 `yFlee` 분기와 겹치는 축이라 이번 스코프에서 제외.
- 검증필드: 자가검증 = check_refs rc=0 · `bash -n`(러너) · `node --check`(게이트웨이) · Playwright 3상태 렌더(①죽어있음 = 「그 뒤로 12시간 0분 / 부활까지 1일 11시간」 ②기도 = 「곧 돌아온다 + 민아가 빌어줬어」 ②' 하한 = 「무음동의 밤이 다 흘렀어」 ③귀환 = 「돌아옴 · 죽어 있던 시간 1일 10시간」 · 만료 흔적 = 표시 0) + 풀다운 `call:off inv:off tune:on prof:on` + 목록 무채 유지 + 에러 0 · 파이프 시뮬(사망→하한→기도→엔트리 소비→흔적 이관→만료 청소) · `DEAD_ON` 6케이스 | 운영자검증 불가피 사유 = 실제 대화에서 캐릭터가 <<PRAY>>를 자연스럽게 붙이는지(모델 판단 축). 롤백 = 이 커밋 revert(`_versions/260725_2214_죽음프로세스_하한부활_귀환흔적/`). 운영자 확인 포인트: ①죽은 인물 카드에 남은 시간이 줄어드는지 ②다른 캐릭터에게 "빌어줘" 부탁 → 카드가 「곧 돌아온다」로 바뀌는지 ③부활 첫 답 뒤 카드에 「돌아옴 · X의 기도로」가 하루 동안 남는지.

## 245차 — 얼빡 프사 11인 실생성·탑재 + 세라 성별 표류 교정 (Q.57 실행분 · 2026-07-25 22:25 KST)
- **실행**: `yeta-char-art`(avmode=1) 10인 + `yeta-aeri-cand`(ghibli=1) 1인 = **프사 11장 교체·roster 배선 완료**(루시·윈터 불가침 유지). 산출 = `av/<id>.webp` 512² + `<id>_face.png` 1024²(편집 베이스) · 애리 = `aeri_ghibli.png`.
- **디스패치 진단**: 첫 디스패치 2건이 **jobs=0 startup_failure**로 즉사 — 원인은 워크플로 정의가 **기본 브랜치(main)에서 해석**된다는 것(새 input `avmode`·`ghibli`와 YAML 수복이 아직 main에 없었음). PR 머지 후 main 기준 재디스패치 → 정상 기동. 같은 축으로, 파싱 불가 워크플로가 **푸시마다 실패 런을 생산**하던 것도 머지와 함께 멎음(최근 100런 중 55건이 그거였다).
- **세라 표류(3회)**: 1차 = 남성 얼굴. 2차(AV_BASE 가드 문장) = 여전히 남성. 3차 = **뿌리 교정**(CHARS 세라 항목에 성별 명사 부재 — 반신샷에선 포니테일·연습복이 지탱하던 단서를 얼빡 크롭이 잘라냄) → 카드 정본대로 "girl · teenage young woman" 명시 후 정상. 교훈 = **얼빡은 크롭이 단서를 지운다 → 묘사에 성별·나이를 글자로 박아야 한다**(다음 인물 추가 시 동일 점검).
- 검증필드: 전/후 대조 시트 실렌더(11인 × 원 32·41·58 규격) · roster 13행 실측(루시 `lucy_face.webp`·윈터 불변) · check_refs rc=0 | 롤백 = `git revert d65b42a 4ddb7a4 66c4102 343788e`(이미지 커밋) · 프롬프트 = 25c7963·659d221 revert.

## 246차 — 정렬 정본을 「잉크(광학) 기준」으로 확장 + 덮힌 머지분 기계 감사 (Q.64 · 2026-07-25 KST)
- **개정**: `docs/디자인방식론_YETA.md` §2를 「픽토그램·정렬」로 확장 — b) 기준을 박스→**잉크(획) 중심**으로 일반화하고 「"정렬 맞춰줘" = 잉크 기준 디폴트」 명문화 · c) 실측법 축 분리(픽토그램 `getBBox` / **글자 = 베이스라인 + Canvas `actualBoundingBoxAscent·Descent`**, 디센더 없는 문자열은 잉크가 위로 몰린다) · d) **폰트 크기가 뷰포트별로 갈리는 자리 = 고정 px 보정 금지**(§7과 같은 축). 신규 조항 신설 0 — 사각이 있던 기존 절을 직접 고쳤다(CLAUDE.md §0).
- **왜**: 규칙이 없던 게 아니라 §2 제목이 "픽토그램"이라 **아이콘↔글자 혼합 정렬**이 사각이었다. 운영자가 상단 세계시계(달 픽토 vs `04:12 AM`)에서 정확히 그 자리에 걸렸고, 기하 판정은 0.00px "합격"이 떨어졌다.
- **감사**: 최근 main 20커밋의 추가줄 생존율 기계 검사 → **통째로 덮인 머지분 0**. 낮게 잡힌 4건은 후속 교체(#407·#410·#415·#412)로 설명되고, 리팩터에 흡수된 #402·#406은 줄 대조 대신 **기능 심볼 9종 생존**으로 직접 확인. 잔존 = 번호 중복 5쌍(결번 0 · 외부참조 0 · 재번호는 운영자 판단 대기).
- 검증필드: 자가검증 = check_refs rc=0 · 감사 스크립트 재현 가능(`scratchpad/audit.py` — 읽기 전용) | 운영자검증 = 잉크 기준 문구가 의도와 맞는지, 번호 중복 5쌍을 정리할지. 롤백 = 이 커밋 revert(문서 3파일 · 코드 무변경).
## 235차-B — 대화 두절 진단 + 감시 복구(장애/연출 구분) (Q.53 · 2026-07-25 KST)
- **진단**: 웜 런 exit 143 강제 종료(답장 생성 중 유실) + concurrency 대기열 취소 25건 + 잡 0개 failure 3건. 두절 3종이 무알림이라 운영자조차 장애/연출 구분 불가.
- **감시**: `yRender` 말미 폴 조건에서 `waitMs <= zlim`(5분) 컷 제거 — awaiting 동안 watch 롱폴 유지(상한 = 게이트웨이 10분 리퍼). `visibilitychange` 리스너 신설 = 폴이 죽은 뒤 탭 복귀 시 1회 `yLoad`(폴 생존 시 미발사).
- **문구**: gaveUp 지문 = 연결 축으로 분리(`.ysit` 결 유지). `yFlee(id)` 신설(dead 엔트리 `flee` 플래그) → 상세카드 CTA·`yEnterChat` 토스트·헤더 tag·사진첨부/`ySend` 가드 5곳이 flee면 "자리를 비웠어 — 시간이 지나면 돌아와".
- **러너**: `yeta-chat.yml` 실패 알림 `if: failure()` → `failure() || cancelled()`(대기열 취소 경로 커버).
- 검증필드: node --check(인라인 27만자) · 헤드리스 부팅 콘솔 치명에러 0 · yFlee 4케이스 단위검증 · check_refs rc=0.
- **#396 누락 회수(실렌더 포착)**: 사망 지문(`4102`) "그 시각부터 24시간" + 진입차단 토스트(`4032`) "사망했어 — 현실 24시간" 2곳이 Q.51의 신당 서사 이관에서 누락 — 실제는 `pray:1`이라 시간으로 안 풀리는데 같은 화면 헤더와 반대말. 둘 다 신당 서사로 교정(토스트는 `yFlee` 분기 동반). 사용자 노출 "24시간" 잔재 0.
- **영수증**: 두절 3종(gaveUp/flee/dead) playwright 실렌더 스샷 — 지문이 실제로 갈리는 것 확인.

## 236차-B — 두절 사건 로그(시각·사유 분류 누적) (Q.54 · 2026-07-25 KST)
- **코어**: `yDcLog`/`yDcPush`/`yDcTime`/`yDcRows` + `YDC_MAX 50` 링 + `YDC_WHY` 5종 분류. 저장 = localStorage `yeta_discon`(yMoodLog 결 계승).
- **적립 지점 4**: `yRender` failNow 판정부(stall/gaveup 2단계) · deadNow 블록(flee/dead) · `ySend` 전송 실패부(send). 전부 사건 키로 dedupe(폴 재렌더 중복 0).
- **표시**: 설정 4번째 아코디언 '연결 기록' — `details.yset-grp` + `.ypol-grp`/`.ypol-gh`/`.ypol-lock` + `.yset-foot`/`.yset-reset`(비우기) 전량 계승. 클릭 위임은 `#ypolBody` 분기보다 **앞**(뒤면 yPolClick이 먹고 끝남).
- 검증필드: 4종 발생 5건 누적 · 재렌더 6회 중복 0 · 설정 실렌더 스샷 · node --check · check_refs rc=0.
- **main 병합(22커밋 뒤처짐 해소)**: 충돌 8덩어리 — 사망 문구·`yDead` 판정은 **전부 main 채택**(#412 Q.63이 죽음 프로세스를 3상태로 재설계 = 내 브랜치의 문구 수정보다 상위 정본 · 내 `yFlee`/`yPray` 헬퍼도 함께 폐기). **폴 감시 판정만 합성** — main의 `gbP`/`staged` 픽업은 살리고 `awaiting && waitMs <= zlim` 컷만 제거.
- **A/B 실측(같은 조건 · 감시 = `_yWatchTk` 1선 ∨ `_yPoll` 2선)**: main = 2분 ✅ / 9분 ❌ / 30분 ❌ · 병합본 = 2분 ✅ / 9분 ✅ / 30분 ✅. **5분 초과 대기 시 감시가 꺼지던 근인이 main에 잔존했고 이 병합이 해소.**
- **사망 지문 합성**: main 잔존 문구 "그 시각부터 24시간"은 #412 개정(기도 즉시 ∨ 무음동 48h) 이전 값이라 모순 · 내 문구 "빌어주기 전엔"은 기도를 유일 경로로 단정해 역시 모순 → **"누가 신당에서 빌어주거나, 무음동의 밤이 더 흘러야 닿는다"**로 main 최신 규칙에 동기.

## 247차 — 고른 대사 되먹임 + 평상시 미연시 선택지 (Q.66 · 2026-07-25 KST)
- **데이터**: `sess.tune_picks[persona][]`(링버퍼 5 · quizpick이 적립) · `thread.picks[]`(러너 `<<PICK:>>` 박제 · send가 소거).
- **러너**: PICK 파서(MOOD 동형) + `OUTPUT_RULES` 발동 조항 1줄 + `PICKS_BLOCK`(고른 말 = 결만 계승 · 카드 우선 명시) · 안 붙인 턴 = `picks` 회수.
- **뷰어**: `#ypickbar` = 입력행 위 세로 스택(`.yexit-btn.yes` 표면 계승 · 신규 CSS 3줄) · 렌더 훅 = `yRender` 꼬리(생성 중·사망·원거리·방 전환 자동 회수) · 탭 = `ySend` 단일 경로.
- **게이트웨이**: `quizpick`에 문장 보존 · `send`에 `picks` 소거.
- 검증필드: node --check · bash -n · check_refs rc=0 · 헤드리스(선택지 3개·간격 5px·탭 전송 텍스트 일치·전송 후 숨김) | 롤백 = 이 커밋 revert. 운영자 확인 포인트: 실대화에서 선택지가 너무 자주/드물게 뜨는지(빈도는 프롬프트 1줄로 조절) · 고른 말이 다음 턴 말투에 묻어나는지.


## 248차 — 종결부 지문 재료 개정: 안쪽 4축 → 화제×장소×제3자 (Q.66 · 2026-07-25 KST)
- **문제**: Q.52 상시화는 「지문 한 줄 필수」까지만 세웠고 재료 4축(다음 행동/몸의 반응/시선·손/주변 소리·빛)이 **전부 캐릭터 안쪽**이라 어느 대화에 붙여도 말이 되는 분위기 사진만 산출.
- **재료 축 교체**: 규칙 3 = 「재료를 바깥에서 가져와라」 — ⓐ화제를 눈앞의 실물로 ⓑ장소가 소품 공급 ⓒ이름 없는 제3자 허용 + **행동은 바깥을 향하게**. 지문 = 분위기 사진 ✗ / 다음 장면의 씨앗 ○. 구 4(한 줄)→5, 「직전 턴 같은 그림 금지」를 독립 4로 승격.
- **자가 점검 신설**: 「다른 대화에 그대로 옮겨 붙여도 말이 되면 실패」 — 정형화 방어를 턴 간(소재 반복) + 턴 내(이식 가능성) 이중화.
- **억제 해제 2건**: `state_block` 장소 줄 = 낭독 금지 유지 + 소품 공급원 명시 · `FAR_BLOCK` = 접촉 금지선이 **유저 상대로만**임을 명문화(기준선 예시 동사가 하필 「건네주기」라 원거리 정면 충돌이었다).
- **모순 소급 3건**: §🎬 「대신 분위기」 → 「의미 있는 한 동작에 온도」 · `narration:false` 「지문 자체가 금지」 낡은 문구 폐지(상시 지문을 통째로 무효화할 수 있었다) · 축 A-2 상단 ○ 예시가 새 규칙의 ✗ 그림이라 △ 강등 후 교체.
- 검증필드: bash -n · check_refs rc=0 · `state_block` 실인자 실행(장소 줄 실렌더) · ⚠ 실제 생성 미검증(모델 호출 = 과금 · D2 미승인).

## 249차 — 원장 번호 중복 해소: 재번호 아닌 `-B` 접미사 (Q.67 · 2026-07-25 KST)
- **왜 재번호를 안 했나**: 착수 중 `docs/요구사항_큐.md` Q.15 항에서 **「커밋·PR 참조 보존 위해 소급 재번호 안 함」이 이미 결정돼 있는 것**을 발견했고, 실측으로 근거도 확인됐다(`Q.43` 참조 커밋 3건 — 첫 항목을 가리키는 `fdf5fbd … (#375)` 포함). Q.64 보고의 「외부 참조 0건」은 **레포 파일만 세고 커밋·PR을 안 센 오판**이었다.
- **처리**: 첫 항목은 번호 유지, 뒤엣것만 `-B`(3번째 `-C`). 레포 기존 관례 `Q. 16-B` 계승 = 표기 창작 0. 원장 2건(`Q. 14-B`·`Q. 43-B`) · 이력 14건(`10·11·18·25·73·74·82·88·89·90·206·208차-B` + `83차-B`/`83차-C`) · 짝 참조 `206차-B` 본문 `(Q.14)`→`(Q.14-B)`.
- **규약 명문화**: Q.15 원장 노트에 「중복 발견 시 재번호가 아니라 접미사」 1줄 추가 — 다음 병렬 채번 충돌에서 같은 판단을 반복하지 않는다.
- **오집계 정정**: Q.64가 센 「중복 17개」에는 의도 표기(`17차+`·`(예정)`·`추기`)가 섞여 있었다. 실제 충돌은 이력 13개 번호·14항목이었고 `Q.16`은 이미 `Q. 16-B`로 해소돼 있었다.
- **머지 레이스 노트**: 표제를 파일 전역에서 리네임한 탓에 main 재유입 시 **중첩 충돌**이 났다 — 2000줄 원장에서 손으로 푸는 건 유실 위험이라 병합 대신 **최신 main 위에서 스크립트를 재실행**했다(같은 산출물, 충돌 0).
- 검증필드: 자가검증 = 표제 유일성 기계 검사(중복 0) · 결번 0 유지 · check_refs rc=0 | 운영자검증 = `208차-B`의 출처 태그 `(Q.15)`가 실은 `Q.16-B`로 보이는 건 — 별개 사실 정정이라 미수정. 롤백 = 이 커밋 revert(문서 2파일 · 코드 무변경).

## 250차 — 얼빡 결·프레이밍 확정(견본 계승 + 확대 크롭 0.72) (Q.57 후속 · 2026-07-25 23:15 KST)
- **1차 반성**: `AV_BASE`가 스타일 어휘를 `BASE`(판타지 반신 초상)에서 통째 계승 → 반실사 글로시 + 번개·아우라·입자 범벅, 마이크·손·어깨 진입 = 얼빡도 아니었다(운영자 "루시 얼빡 말고는 다시 만들어야 된다").
- **결 교체**: 계승 대상을 **견본 실물(`lucy_face.webp`)**로 재지정 — 납작한 애니 셀 · 크리스프 라인 · 파스텔 매트 · 조용한 단색 배경 · FX 전면 금지 · 얼굴만(소품·손·어깨 금지). 10인 재생성.
- **프레이밍은 프롬프트로 안 잡힌다 → 기계 크롭으로 확정**: 2차도 절반이 정수리~턱 헤드샷이라 32px에서 얼굴이 작았다. 배율 실측(0.82/0.72/0.64 × 10인 렌더 대조) = **0.72**(0.82 여백 잔존 · 0.64 입·턱 절단 다발) · y −4%. `av_write`에 편입 + **`YETA_AV_REBUILD=1` = 생성 없이 재크롭(무과금)** · 노브 `YETA_AV_ZOOM`·`YETA_AV_YBIAS`.
- **축 정렬**: 지브리(`av_square`)가 재크롭 경로에서 빠져 애리만 헤드샷으로 남던 것 → 같은 노브·기본값으로 묶음(무과금 재크롭).
- **지뢰 봉인**: `yeta_face.py` `av_owned()` — roster avatar가 `assets/yeta_char/` 또는 `characters/`면 **FORCE=1이어도 skip**. 종전엔 이 옛 파이프 한 번이면 얼빡 11장 + 루시 프사가 R2 URL로 통째 갈렸다(13/13 owned 실측).
- 검증필드: 배율 3종·10인 실렌더 대조 · 최종 전/후 시트(11인 × 원 32·41·58) · roster 실측(얼빡축 11 · 루시 `lucy_face.webp` 불변) · check_refs rc=0 | 롤백 = 이미지 = `rebuild=1 · zoom=1.0` 즉시 원복 · 코드 = 05a53ee·fa21d8a·74aeacf revert.

## 251차 — 208차-B 출처 오태깅 정정: (Q.15) → (Q.16-B) (Q.68 · 2026-07-25 KST)
- **판정 근거(로그 추적)**: 세 항목의 도입 커밋이 전부 `9b8d076`(260719 소급 일괄 기재 = backfill)이라 태그 전사 오류가 생길 구조였다. 같은 커밋의 **207차 표제가 이미 `(Q.15)`를 물고 있었고**(SAFETY-LOCK 복구 = Q.15 실행분), 결정타로 **`Q. 16-B` 머리주석에 평의회 260717(docs-ledger)이 「실물은 두 번째 208차 엔트리 · (Q.15) 오태깅」이라고 이미 판정**해뒀다 — 진단만 있고 정정은 안 된 채 방치.
- **내용 대조**: `Q. 16-B` ↔ `208차-B` 1:1 일치(REF_SELF 성당 원판 · 도로 v7 노란 경계선 · `#ymRevGlow` 부활 발광 · 산출 sha `e59f02fa`/`1033e5d2` 동일). `Q.15`(SAFETY-LOCK)와는 겹침 0.
- **처리**: ⓐ `208차-B` 표제 → `(Q.16-B)` ⓑ `Q. 16-B` 결과줄 → 「208차-B 참조」 ⓒ 평의회 머리주석에 정정 완료 추기. `Q.15`↔`207차` 상호참조는 정상이라 무접촉.
- **머지 레이스 노트**: 병렬 세션 재유입으로 꼬리 충돌이 반복돼, 병합 대신 **최신 main 위에서 스크립트를 재실행**했다(같은 산출물, 충돌 0).
- 검증필드: 자가검증 = 표제 유일성·결번 검사 유지 · check_refs rc=0 | 운영자검증 불요(문서 상호참조 · 판정 근거가 레포 기존 평의회 기록). 롤백 = 이 커밋 revert(문서 2파일 · 코드 무변경).
## 252차 — 러너 사망 시 자동 재발사(Q.69 · 2026-07-25 KST)
- `functions/api/yeta.js`: `RD_MAX=1` 신설 + `op:get` 리퍼에 재발사 분기. `Object.values`→`Object.entries`(사망 방 판정에 tid 필요) · `rdFire` 플래그는 CAS 콜백 밖에서 1회 dispatch(콜백 안에서 쏘면 CAS 재시도마다 중복 발사).
- 실패 시 즉시 error 롤백 + `rd` 리셋을 send·attach 두 경로에 배선.
- 설계 근거 = SIGTERM이면 러너 내부 스텝이 전부 죽으므로(260725 실패 알림 skipped가 증거) 감지는 러너 바깥이어야 한다.
- 검증필드: 판정 시뮬 9/9 · 실제 흐름 반복 폭주 0 · node --check · check_refs rc=0.




## 247차-B — 단톡 비화자 '모니터링' 자리(watch 비트) (Q.65-B · 2026-07-25 KST · 차수 중복 = 접미사 규약)
- **원인(실측)**: 비화자 위상이 조연이었다 — `state_block` "물러나도 된다"(부재 허용) + `CO_BLOCK` 말투 절만(대역 = 얇은 페르소나) + 자율 비트가 "너희끼리" 한 결뿐(유저 발화를 들은 자리 없음).
- **조치**: `extract_mat` = `gb_kind` 산출(예약 회차 1 = `watch` · 2+ = `on`) → `process_turn` = watch 전용 SCENE_BLOCK·CONTRACT1·GROUP_RULE(대역 금지)·`GB_TAIL`(길이 예외 = 지문 한 줄만도 정답 · 계약 맨 끝 = 최근성 앵커) · `state_block` 동행 줄과 `CO_BLOCK` 머리를 '듣고 있는 사람'으로 재작성. watch 화자는 그 턴의 persona라 카드·`notes[본인]`·`tunes[본인]`·장소가 전부 자기 것(대역 대사 아님).
- **교차 간섭 격리(운영자 260725 "기존 거랑 안 꼬이게")**: `picks`(미연시 선택지 · #418)를 자율 비트가 건드리지 않게 `not gb_job` 가드 — 안 하면 **직전 답장의 선택지 칩을 뒤따르는 비트가 회수**한다(실측 재현) + 프롬프트에 `<<PICK>>` 금지 1줄(유저에게 답 요구하는 자리 아님). `place_nm` 줄은 main 최신판(종결부 지문 소품) 채택 · 동행 줄만 내 판.
- 검증필드: 교차 간섭 실측(칩·초대 마커·타인 기억·기도 대기 죽음 전부 무오염) · 실물 분기 텍스트 조립 확인(watch/on 각각 SCENE/CONTRACT/GROUP/TAIL) · 회차 판정 실측(n=1 watch → n=2 on → n=3 NOPENDING) · 시뮬 17케이스 전통과 · bash -n · py compile · check_refs rc=0 | 롤백 = 이 커밋 revert(또는 `YETA_GCHAT_BEATS: '1'` = 모니터링만 · `'0'` = 축 OFF). 운영자 확인 포인트: 세라랑 얘기하는 중에 루시가 눈치·핀잔·지문 한 줄로 계속 '거기 있는' 느낌이 나는가(대사가 없어도 존재가 남는가).


## 253차 — 신규 주민 3인 + 난입 전용 등장축 + 홍석천 개편 (Q.69-B · 2026-07-26 KST)
- **의도**: 운영자 5건 연속 지시(레제 신설 / 가을→서예은 교체·상인회장 익명화 / 드루실라 난입 전용 신설 / 세라 원본 복구 / 홍석천 컨셉 개편+이미지 생성)를 한 배치로 처리.
- **변경 전후 — 인물**: 로스터 14인 → **16인**(레제·드루실라 추가 · 가을 자리를 서예은이 승계 = 정렬 무변경). 상인회장 = 로스터에서 내려 **이름 없는 배경 인물**(옛 카드 = `_versions/260726_0400_가을_서예은교체/gaeul.md`).
- **변경 전후 — 이미지**: 레제 15장(업로드 16 중 수영복 뒷모습 1장 보류) · 서예은 9장(10 중 속옷 1장 보류) · 드루실라 5장(그중 고죠 흉내 1장 = 별도 모드 폴더 `gojo/`) · 세라 **4→16장**(수집기 JSON `db.sera.imgs[13]`에서 base64 추출 · md5 중복 1장 제거 · ⚠ **640px 재인코딩 사본**이지 원본 아님). 얼빡 프사 6종 신규(reze·seyeun[+alt]·drusilla[+고죠흉내]·sera) = ffmpeg 정사각 크롭 512² webp.
- **변경 전후 — 난입 엔진(핵심)**: 기존 `barge_check`는 `locked:true`를 **일괄 후보 제외**해서, "목록에선 못 여는데 난입은 온다"가 구조적으로 불가능했다(드루실라 요구의 정면 충돌). 해결 = **roster 데이터축 `barge{slot,gate,boost,boost_gate}` 신설** — ⓐ `barge{}` 보유자는 locked라도 후보 유지 ⓑ 세계시각 슬롯 게이트(evening·night·late = 저녁~심야) ⓒ 동석 가중(방에 `boost` 인물 = 확률 1/6 → 1/2) ⓓ `barge_via="only"` 데뷔 프롬프트 분기(관계·우연이 아니라 "초대 없음" 자체가 결). **러너에 인물명 하드코딩 0** — 전부 로스터 데이터.
- **변경 전후 — 뷰어**: `yModeOn`에 **동석축 `mode.with`** 가산(시간축 `mode.t`와 직교·AND) + `yCBg`(모드 배경 해석 SSOT · `yCAva` 대칭) 신설 + `yStage` 난입 키에 모드 자리 추가. 효과 = 드루실라가 **고죠와 같은 방일 때만** 프사·배경·배지가 「고죠 흉내」로 갈린다(운영자 "배경바뀌고 프사 바뀌면서 등장").
- **변경 전후 — 홍석천**: 카드에 3축 추가(게이 / 근육질 186 / 놀라면 짐승으로 넘어가 도주). ⚠ 안전 고정 = **변신 과정 묘사 하드 금지**(신체 변형·통증 0 · 어떤 짐승인지 미확정) + **유저가 무거운 얘기 꺼낸 순간엔 도망 안 감**. 종전 「172·성별 헷갈림」 폐기. 생성 프롬프트도 androgynous/serene → 남성 명사·각진 골격·굵은 선으로 교체 + `YETA_AV_TAKES`(여벌 테이크 N장 · roster 미주입) 노브 신설 + `yeta_bg_var` +3씬(뒷문·주방·새벽 지붕 = no people 규약 유지, 도주는 흔적으로만).
- **효과/리스크**: 효과 = 등장 방식이 하나 늘었다(목록 진입 없이 난입으로만 존재하는 인물). 리스크 = 하루 1회 전역 상한과 곱해져 드루실라가 그날의 유일한 난입을 선점할 수 있음(의도 — 특수 이벤트 굶김 방지) · 세라 이미지는 640px 사본이라 원본 재수집 시 교체 필요.
- **오퍼스 3인 병렬**(운영자 명시 지시 "오퍼스 돌려서" ×3) — 카드 3종 각각 리서치+작성. 레제는 극장판 대본 직접 열람 2건 + tvtropes/나무위키 402·403 차단분을 「미확인」으로 표기하고 스니펫 교차분만 채택.
- **검증**: check_refs rc=0 · `bash -n` OK · 러너 파이썬 heredoc 14블록 전부 compile OK · 로스터 16인 read-back · media.json 7인 재생성 일치.
- **미완(정직 기록)**: ① 홍석천 얼빡 4장·배경 3장 = **워크플로 dispatch 미실행**(정의가 기본 브랜치에서 해소되므로 main 머지 후) ② 성향_DB 제25·27·29·30조의 「가을」 재산정 미반영(제9조만 교체 · 별건 명시) ③ 드루실라 색 `#A9C6E8` = 윈터 계승값(에이전트 제안 `#9B7FD4`는 신규 hex라 절대명령#1상 승인 대기).

## 253차-B — 홍석천 이미지 7장 마감 + 보류 3장 해소 + 워크플로 구멍 2건 (Q.69-B 후속 · 2026-07-26 KST)
- **의도**: 253차가 미완으로 남긴 「홍석천 얼빡 4 + 배경 3 생성」을 main 머지 후 dispatch로 실행하고, 그 과정에서 드러난 파이프 구멍을 같이 막는다.
- **산출**: `yeta-char-art`(avmode=1·only=mudi·force=1·takes=4) → 얼빡 4테이크 · `yeta-bg-var` → 배경 3씬(뒷문·주방·새벽 지붕 · no people · 도주는 발자국 흔적으로만). 둘 다 success.
- **구멍 ① bg-var ffmpeg 부재(조용한 실패)**: `yeta_bg_var.py:webp()`는 ffmpeg 없으면 **비치명 스킵**인데 `yeta-bg-var.yml`엔 설치 스텝이 없었다(`yeta-char-art.yml`엔 있음). **뷰어는 `.webp`만 참조** → 3장 전부 png만 커밋되고 안 보이는 상태로 워크플로는 success. 처리 = 워크플로에 ffmpeg 스텝 계승 추가(재발 차단) + 기존 3장은 동일 규격(768w·q82) 로컬 변환 동반 커밋.
- **구멍 ② AV_ZOOM 0.72 과크롭**: 0.72는 **260725 출력 기준** 실측값인데, 개편된 프롬프트는 원본부터 여백 0인 얼빡으로 나와 한 번 더 자르니 **4테이크 전부 입·턱이 프레임에 걸렸다**. 처리 = 4장 크롭 없이 512² 재변환(생성 0회 = 무과금) · 눈·입 다 살아있는 **v4를 roster 프사로** · `AV_ZOOM` 옆에 판정법 주석(「여백 있으면 0.72 · 없으면 zoom=1.0」). **기본값은 미변경**(나머지 10인은 옛 계열이라 0.72가 맞는 값).
- **구멍 ③ 생성 배경 orphan**: `assets/yeta_bg/`의 씬은 yStage가 **장소·무드 키로만** 호출한다(`ySceneForPlace`/`ySceneFor`). `var_mudi_*`는 그 키가 아니라 생성만 되고 어디에도 안 걸려 있었다. 처리 = 홍석천 시즌 미디어로 편입(주방→base · 뒷문→tense · 새벽 지붕→blue) + roster `season`·`bg` 배선.
- **보류 이미지 3장 해소**(운영자 260726 "페르소나 구축 안 된 애들은 장소 주인이더라도 익명으로 넣고 나중에 보강"): 빈 헬스장 2장 → **본**(base · roster bg도 교체) · 익명 검도 실루엣 1장 → **류**(tense 전용 · 얼굴 없어 인물 확정 아님 · roster bg는 기존 유지). 두 media.json 주석에 「익명·보강 대기」 명시.
- **효과**: 시즌 이미지 0장 인물 **9명 → 6명**(남은 = 하서연·백·윤·카피·애리·윈터).
- **검증**: check_refs rc=0 · main read-back(로스터 16인 · mudi av=mudi_v4.webp · von/ryu/mudi season 배선 · var_mudi_*.webp 실존) · 헤드리스 렌더로 배경 3장·프사 4테이크 육안 대조.
- **잔여**: 성향_DB 제25·27·29·30조 「가을」 재산정 · 드루실라 색 승인 대기 · 이미지 0장 6명.

## 254차 — 윈터 해금 + 189장 평면(flat) 축 신설 (2026-07-26 KST)
- **지시 원문**: "응 그리고 윈터 살려줘. 그리고 윈터 개성을 더 추가하고싶은데, 아이데이션 해봐 검색해서"
- **의도**: `locked:true`(활동으로 안 열리는 스페셜)로 잠겨 있던 윈터를 열고, 이미 모여 있던 이미지를 실제로 쓰이게 배선.
- **발견(재고 오판 정정)**: 253차 재고표는 윈터를 「시즌 이미지 0장」으로 집계했는데 **실측 189장**이었다. 원인 = 윈터만 `viewer/characters/idol/winter/` 규약(아이돌 등급 · `inject_character.sh` 폴백 경로)으로 모여 있고 감정 하위폴더가 아니라 **파일명이 감정 라벨**(happy_01 · are_u_kidding_02 · glaring_cutely_03 …)이라, 시즌 manifest 축에 아예 안 들어와 있었다. 253차 표는 `season` 필드 유무로 셌기 때문에 0으로 보였다.
- **처리 — 평면(flat) 축 신설**: 파일을 감정 폴더로 옮기면 roster avatar·bg와 카드 폴백 경로가 전부 깨진다 → **옮기지 않고 규칙으로 읽는다**. `build_season_media.py`에 `build_flat()` 추가 + `SEASONS["winter"]={flat, rules}` — 규칙 = (버킷, [토큰…]) 리스트 **위에서부터 첫 일치 승**(좁은 토큰 먼저: `glaring_cut`→shy 가 `glaring`→mad 보다 위 · 오타 수집분 `studing`·`pratice`·`cutyly`도 토큰으로 흡수) · 미일치 = base 폴백.
- **결과 분류**: base 55 · warm 11 · joy 20 · love 13 · shy 12 · blue 21 · tense 26 · mad 31 = **8버킷 전원 충족**(레포 최다 · 루시 82장을 넘는다).
- **roster**: `locked:true → false` · `met:false → true`(잠금 해제 경로가 애초에 없던 스페셜이라 즉시 개방으로 판단 — 게이트 두려면 met만 false로 되돌리면 된다) · `season` 배선 · 프사 = `beauty_02` 얼빡 크롭 512²(`winter_face.webp` · 종전엔 원본 jpg를 그대로 프사로 써서 얼굴이 작았다).
- **효과**: 시즌 이미지 0장 인물 **6명 → 5명**(남은 = 하서연·백·윤·카피·애리). 윈터는 감정 8버킷이 다 차서 대화 무드에 그림이 실제로 붙는다.
- **검증**: check_refs rc=0 · 분류 결과 버킷별 파일명 육안 검수 · media.json 재생성 일치.

## 255차 — 레포 전수 감사(운영자 "쭉 보고 보완할점") · 발견 3건 처리 (2026-07-26 KST)
- **지시 원문**: "응 다머지됬어? 레포지 안에 있는거 쭉 보고 보완할점 없는지 좀 봐줄래"
- **머지 확인**: 브랜치 ↔ main **내용 diff 0**(스쿼시 머지라 커밋 로그만 갈리고 트리는 동일) · 로컬 미커밋 0.
- **정합 감사 결과 = 0건**: 16인 전원 ①카드 실존 ②frontmatter id·name ↔ roster 일치 ③avatar·bg·season·mode.avatar·mode.bg 경로 실존 ④media.json 전 경로 실존 ⑤stats 6·tune 16 ⑥표시 필드(tagline·greeting·intro·desc·quote·initial·color·enter_line·job·greetings) 결측 0 ⑦로스터 미등재 고아 카드 0 ⑧`assets/yeta_bg` webp 45개 중 미참조 0(253차-B에서 mudi 3장 배선한 결과) ⑨병합 충돌 마커 0.
- **발견① 원장 번호 충돌(수정)**: 병렬 세션이 260725에 **Q.69**(러너 사망 시 증발한 답 자동 재발사)를 먼저 물었는데 내가 260726에 같은 번호를 썼다. Q.67 규약대로 내 것을 **Q.69-B**로 접미사 처리(재번호 아님) + 253차·253차-B 상호참조 갱신. ⚠ 일괄치환이 252차의 정당한 `(Q.69)`까지 건드려 즉시 되돌림.
- **발견② 여벌 픽 회귀 랜드마인(수정)**: `yeta_char_art.py`가 AV_MODE 재실행 때 `set_avatar(cid, "av/<id>.webp")`를 **무조건** 덮어써서, 운영자가 고른 여벌 테이크(홍석천 = `mudi_v4.webp`)가 말없이 v1로 되돌아가는 구조였다. `yeta_face.py`의 `av_owned` 가드와 같은 결로 **roster가 이미 `av/<id>_v<N>.webp`를 가리키면 주입 생략**. 실측 = mudi(True 보호) · sera/baek(False 정상 주입).
- **발견③ 작업이력 '중복 표제 4건' = 오탐**: `17차+`·`38차(예정)`·`64차 추기`·`85차 추기2`는 의도적 접미사 표기였다(검사 정규식이 접미사를 못 잡음). 실제 중복·결번 0(5~254차 연속).
- **신규 로직 실동작 검증(이번 감사의 본론)**:
  - **난입 게이트 30일 시뮬**: 낮 13시·아침 9시 = **0/30일**(시간대 게이트 정상) · 밤(evening/night/late) 혼자 = 3/30일(1/6) · **고죠 동석 = 13/30일**(1/2 가중 발동). 결정적 시드라 같은 날 같은 결과.
  - **고죠 흉내 모드 헤드리스 실측**: room=[gojo,drusilla] → `yModeOn=true` · 프사 `drusilla_face_gojo.webp` · 배경 `gojo/base/drusilla_g01.jpg` · 배지 「고죠 흉내」 **3축 동시 전환** 확인. room을 [lucy,drusilla]로 바꾸면 즉시 평상 프사·배경으로 복귀 = 게이트 양방향 정상.
  - 러너 카드 해소(`CARD=apps/yeta/characters/${PERSONA}.md`) 3인 전원 실존 · 신규 3인 카드가 쓴 장소가 전부 `places.json` 실존 노드(신규 노드 창작 0).
- **잔여(미처리 · 별건)**: ⓐ 성향_DB 제25·27·29·30조의 「가을」 재산정 ⓑ 드루실라 색 승인 대기 ⓒ 시즌 이미지 0장 5명(하서연·백·윤·카피·애리) ⓓ **타세션 잔재 열린 draft PR 10건**(#2·#3·#6·#10·#172·#177·#253·#281·#282·#361 · 260704~260721 · 이번 작업과 무관 — 정리 여부는 운영자 판단).

## 256차 — 드루실라 색·해골 · 5인 얼빡 배선 · 가을 재산정 · 잔재 PR 10건 정리 (2026-07-26 KST)
- **지시 원문**: "나 풀어서 말해주셈. 그리고 드루실라 색? 아 붉은 자몽및 색에 흰색글자 그리고 해골 모양 / 시즌 이미지 저분들은 그냥 얼빡으로만 일단 반영 / 잔재 pr정리(머지할건하고 없는건 버려-=기존설계 바꿔야되는건 알려줘) / 가을? 일단 하셈"
- **드루실라 색(승인 불요로 판정)**: 「붉은 자몽빛」 = `:root --danger #ff5b4a`가 이미 그 색이다 → **신설이 아니라 계승**(절대명령#1 ① "정확한 토큰이 없어도 가장 가까운 걸 자동 계승 · 안 물음"). 종전 `#A9C6E8`(윈터 계승) → `#ff5b4a`. `initial` = 「드」 → **☠**(지도 스프라이트·아바타 폴백에서 뜬다).
  - ⚠ **「흰색 글자 + 해골」은 지금 프사가 사진이라 나올 자리가 없다**(사진이 색·글자를 다 가림). 자몽 바탕 흰 해골 512² 시안(`drusilla_face_skull.webp`)을 뽑아 사진판과 나란히 제시 — 채택은 운영자 픽. 고죠 흉내 모드 프사는 불변.
- **시즌 이미지 0장 5인**: 운영자 "그냥 얼빡으로만 일단 반영" → `<id>_face.png`(1024² 얼빡 원본) 768² webp로 시즌 base 1장씩. 애리만 원본이 없어 `av/aeri.webp`(512²) 사용. media.json 주석에 「얼빡 1장뿐 · 수집분 들어오면 확장」 명시. **결과 = 로스터 16인 전원 시즌 배선 완료(미배선 0)**.
- **가을 재산정(제9조 외 5곳)**: 이름 치환이 불가했다 — 축이 8w7 통제 → 7w8 충동으로 뒤집혀 조항의 논리가 안 맞았다. ① 제25조 폭발형 5인→4인(서예은은 **즉발형** = 축적이 없어 protest 자체가 성립 안 함) ② 제27조 회피군에서 하차 + **이동군 신설**(회피가 아니라 자리를 뜬다 · 매달림 0·뒤끝 0) ③ 제29조 케미 「하서연↔가을(활기↔통솔)」→「하서연↔서예은(활기↔즉발)」 ④ 제30조 「가을↔백/로이스 8+8 통제다툼」→ **통제다툼이 아니라 절차 무시**(판을 뺏는 게 아니라 판이 있는 줄도 모른다) ⑤ 260725 유형표 「통제·명령형」에서 하차 + **즉발·무계획형** 행 신설 ⑥ 제7조(로이스 1w8) 근거쌍 소멸 주석(판정 자체는 독립 근거라 유지).
- **잔재 PR 10건 정리**: 각 PR의 **핵심 산출물이 지금 main에 있는지 실물 확인**해서 분류(커밋 수·behind만으로 판단하면 오판).
  - **회수 1건** — **#361**: main의 `Q.39`는 260723 details.so 레퍼런스로 **완전히 다른 건**이었다. 이 PR의 Q.39(답장 속도 저하 원인 조사 · kimi-k3 게이트 지연 실측)는 머지 안 된 채 번호까지 재사용돼 **원장에서 통째로 유실**돼 있었다 → 본문 회수해 **Q.39-B**로 편입 후 PR 닫음.
  - **닫음 6건**(이미 반영·폐기): #2·#3(브랜드 `#CFFF40` main 현존) · #6 · #10(chakra-petch = 이후 폰트 정책 교체로 폐기) · #172(alias 한줄평 = job/tagline 배지로 대체 · 지도는 v5 픽셀 도로로 전면 교체) · #177(4슬롯 = main에서 이미 `display:none` + 동일 운영자 지시 주석 — 목표 달성, DOM 잔재만 남음).
  - **보류 3건 = 기존 설계 손대야 함(운영자 판단 요청)**: #253(아이콘·픽토그램 정렬 기틀 이식 — CLAUDE.md·AGENTS.md·절대명령2 동시 개정 = 평의회 대상) · #281(사망 3종: 마지막 말 푸시·애도 흑백 `ymourn`·성당 마주침 — main 사망축이 260725에 다른 방식으로 재구현돼 부분 중복) · #282(챗 상단 그라데 블러 + 목록 스와이프 라이브 슬라이드 — `#yetatopblur` 미존재 = 미반영). 셋 다 `viewer/index.html`이 288~322커밋 재작성돼 **머지가 아니라 재구현**이다.
- **검증**: check_refs rc=0 · media.json 16인 전원 재생성 일치 · 로스터 시즌 미배선 0.

## 257차 — 난입 표식(grade) 신설 + 윈터 업로드 11개 배선 + 홍석천 2장 (2026-07-26 KST)
- **지시 원문**: "아 그 아이돌 표식 같은거 말하는거야. 대화는 불가능한데 리스트에는 있어야하니까. 그리고 드루실라는 아예 불투명은 아니고 반투명정도로 사망보다는 대화선택지에서 잘보이게해줘. 프사는 기존에 1번이 맞음." / "윈터도 내가 영상이랑 사진을 직접 그 화면에 넣었거든 확인좀"
- **해석 정정**: 앞선 「자몽색+흰글자+해골」을 **프사 교체**로 읽었던 건 오독이었다. 실체는 **등급 표식**(윈터 `IDOL` 배지와 같은 축) — 「대화는 못 여는데 목록엔 있다」를 한눈에 보이게 하는 훈장. 프사는 기존 사진(1번) 유지.
- **`grade: "barge"` 신설(전부 기존 선언 계승 · 신규 값 0)**: ① `YGRADE` += `barge:'난입'` ② `YSKULL_SVG` + `YGRADE_SVG` 오버라이드 맵 — 등급별 아이콘 축 신설(미지정 = 기존 젬) · nm-svg.js 공유 아이콘 아님 = SSOT 게이트 무저촉 ③ `.ygrade.barge` = `.ygrade.season` 선언 100% 계승, 색축만 `--danger`(자몽) ④ `.yring-barge` = `.yring-season` 계승, 색축만 교체 ⑤ `yCharRank` barge = IDOL과 동렬(2).
- **행 가시성(운영자 "아예 불투명은 아니고 반투명 · 사망보다 잘 보이게")**: `.ycrow.locked`의 실루엣(.66 + grayscale1 brightness.5 = 정체 은폐)은 이 인물에겐 과하다 — 얘는 숨은 게 아니라 **못 여는** 것뿐. `.ycrow.locked.barge` = **`.ycrow.estr` 선언 100% 계승**(opacity .8 · grayscale(.7) brightness(.55) · 이름 옅김 해제) = 신규 값 0. 사망(dd)일 땐 클래스를 안 붙인다(사망 표기가 이겨야 함). CTA도 난입 분기 신설("부를 수 없는 사람 — 저녁이 오면 저쪽이 먼저 찾아온다" · 사망 다음·미대면보다 앞).
- **윈터 업로드 11개(mp4 3 + jfif 8) — 그대로 뒀으면 전부 안 떴다**:
  - **① `.jfif`가 `IMG_EXT`에 없었다** → 사진 8장 통째로 무시. `IMG_EXT`에 `.jfif` 등재(안전망) + **실물은 `.jpg`로 개명**(바이트 동일 md5 일치 = 무손실 · 서빙 MIME이 호스트에 따라 octet-stream이 될 위험 제거). `song_01/02`는 기존 파일과 충돌해 `song_n01/n02`로.
  - **② `build_flat()`에 클립 처리가 없었다**(254차 신설분 누락) → mp4 3개 통째로 무시. `build_one`과 동형으로 CLIP_EXT를 base 선두에 넣도록 보강. 결과 = base 55→66, 총 189→200장.
  - **③ 파일명 띄어쓰기** → `_`로 개명(media.json→CSS `url()`→`video src`→CDN 4단계에서 인코딩이 갈리는 위험 제거 · 바이트 무변경).
  - **④ mp4 3개가 faststart가 아니었다**(`ftyp→mdat→moov`) → 12MB를 다 받아야 첫 프레임. 레포 기존 `yeta-bgvid.mp4`는 faststart라 이게 집 규약. `-c copy -movflags +faststart`로 **재인코딩 0 리먹스** — **영상 스트림 md5 `11cc4abb…` 원본과 완전 일치**(화질 무변경 증명).
- **⚠ 오진 정정(정직 기록)**: 리먹스 전 헤드리스에서 mp4가 `DEMUXER_ERROR_NO_SUPPORTED_STREAMS`로 실패해 파일 탓으로 봤으나, **대조군(레포에서 운영 중인 `yeta-bgvid.mp4`)도 동일하게 실패**했고 `canPlayType` 실측 결과 이 테스트용 오픈소스 Chromium에 **H.264/AAC 코덱이 아예 없었다**(VP9만 `probably`). 즉 재생 실패는 **테스트 환경 한계**였지 운영자 파일 결함이 아니다. faststart·개명은 그와 별개로 유효한 개선이라 유지. 클립 재생 경로 자체는 루시(mp4 2개 운영 중)로 이미 검증된 축.
- **홍석천 2장**(루트 `,.jfif`·`sou furry.jfif`) = 근육질 툇마루 → `warm` · **곰 수인(동물 변신 컨셉)** → `tense`. 크롭 0 · 재인코딩 0 · 확장자만 `.jpg`.
- **검증**: check_refs rc=0 · 헤드리스 실측 = 드루실라 행 `ycrow locked barge` · opacity **0.8** · 아바타 `grayscale(.7) brightness(.55)` · 배지 「난입」 자몽 `rgb(255,91,74)` + 해골 SVG 존재 · 윈터 신규 11개 전부 media.json 반영.
- **미착**: 운영자가 "직접 다시 넣었다"는 **드루실라 프사가 레포에 없다**(main `44eff91` = 윈터 11개 + 루트 2장이 전부). 현재는 기존 사진(1번) 유지 중.

## 258차 — 검증 브라우저 규약 정정(코덱) + 윈터 클립 실재생 확인 + 드루실라 프사 확정 (2026-07-26 KST)
- **지시 원문**: "영상 그 루시꺼는 성공하잖아. 루시 그 인트로영상알지. 그거처럼 해주셈" / "거기에 삽입하라는게 아니라, 그거 재생되는거 처럼 대화 배경에서도 나올수있게" / "테스트용 브라우저 환경은 당연히 실제 앱 구현환경이랑 동일하게 해야해" / "드루실라 프사는 내가 이거로 하라고 분명 넣었는데, 그럼 같이 넣어준 사진 4종은 살아있어? 위치는 너가 바꿨던데?"
- **A5 규약 개정(사고 재발 방지)**: 헤드리스 검증 브라우저를 **번들 chromium → 진짜 크롬(`channel:'chrome'`)**으로 못박았다. 원인 = `/opt/pw-browsers/chromium-*`는 오픈소스 빌드라 **H.264/AAC 코덱이 없다**(`canPlayType` 실측 = 빈 문자열 · VP9만 `probably`). 이 때문에 257차에서 mp4가 `DEMUXER_ERROR_NO_SUPPORTED_STREAMS`로 실패하는 걸 **파일 결함으로 오진**했다(대조군 = 운영 중인 `yeta-bgvid.mp4`도 동일 실패로 판명). `npx playwright install chrome`(Google Chrome 150.0.7871.186) 설치 후 재검증.
- **윈터 클립 실재생 확인(진짜 크롬 · 코덱 `probably`)**: `yeta-bgvid` 729ms · `lucy_clip_01` 1234ms · **윈터 3개 793·803·857ms 전부 재생 OK**(해상도·길이·currentTime 진행 확인). 루시와 **완전히 같은 경로** — `ySeasonMedia` → `yPaintBg(pool[pi], color, true)` → `season && /\.(mp4|webm)/` 분기가 muted·loop·playsInline `<video>` 생성. 진입 첫 페인트는 이미지, 클립은 대화 진행 중 로테이션(`_yStageCold` 가드 = 운영자 260709 "인트로 영상 말고 관련 배경 바로")도 루시와 동일. **추가 배선 불요 — 이미 그렇게 돈다.**
- **드루실라 5장 소재 확인**: 운영자가 준 5장 **전부 살아있다**(바이트 무변경). 감정 폴더로 이동·개명만 했다 — 앱이 감정별 배경 로테이션을 하려면 그 구조여야 한다. base/tense/blue/mad + gojo(흉내 모드 전용).
- **드루실라 프사 확정**: 운영자가 지정한 프사 파일은 **레포에 도달하지 않았다**(main `44eff91` = 윈터 11 + 루트 2가 전부). 운영자가 제시한 프레이밍이 `main/blue/drusilla_03.jpg`와 동일 그림이라 거기서 크롭 — 배율 스윕(0.66/0.74/0.82 × y 0/.03/.06) 대조 후 **z0.74 · y0.03** 확정(512² webp · roster 경로 불변 = 파일만 교체).
- **Desktop.zip 확인(운영자 "내가 지금 이거 넣어놨어")**: `viewer/characters/season/drusilla/Desktop.zip` 4장을 열어 md5 대조 — **4장 전부 이미 레포에 있는 것과 동일**(f01→main/base/drusilla_01 · f02→main/tense/drusilla_02 · f03→gojo/base/drusilla_g01 · f04→main/mad/drusilla_04). 신규 0. 운영자가 프사로 지정한 **얼굴 클로즈업은 이 zip에 없는 5번째 장**(최초 업로드 `드루실라4.jfif` = 현 `main/blue/drusilla_03.jpg`)이라, 프사는 그 파일에서 뽑은 게 맞다(258차 z0.74·y0.03). zip 자체는 서빙 폴더 오염이라 제거(내용 전부 이미 반영됨).

## 259차 — 감정 버킷 클립 지원 신설 + 고죠 joy 영상 (2026-07-26 KST)
- **지시 원문**: "레포지 최상위에 영상 하나 넣어놨는데, 이거 고죠사토루 캐릭터 기분좋을때 나오는거로 하게 해주고 (영상이야) 기존에 윈터했던거처럼 하면될듯"
- **막힘 ① 빌더가 감정 폴더의 영상을 안 읽었다(구조적 한계)**: `build_one`이 모드 루트·감정 하위폴더 모두 **IMG_EXT만** 훑고 있었다 → 클립은 **캐릭터 루트(=base 선두)에만** 놓을 수 있었고 **감정 지정 자체가 불가능**했다. `MEDIA_EXT = IMG_EXT | CLIP_EXT`로 확장 — 이제 아무 감정 버킷에나 영상을 넣을 수 있다. 뷰어 `yStage`는 이미 확장자로 클립을 판별하므로 **뷰어 무수정**. (258차에서 `build_flat`에 같은 결함을 고쳤고, 이번이 `build_one` 쪽 짝.)
- **막힘 ② 파일명에 `#` 3개**: 원본명 `…#anime #jujutsukaisen #gojo #gojosatoruedit #shorts.mp4`. **`#`은 URL 조각 구분자라 그 뒤가 통째로 잘린다** — 그대로 뒀으면 `url()`·`video src` 모두 404. `@`·띄어쓰기까지 제거해 `gojo_clip_01.mp4`로 개명(**md5 `1645cec9…` 원본과 동일 = 바이트 무변경**).
- **배치**: `viewer/characters/season/gojo/main/joy/gojo_clip_01.mp4` — joy 버킷 12장(이미지 9 + 클립 1 → 총 10). 영상 자체는 이미 faststart(`ftyp→moov→mdat`)라 리먹스 불요 · H.264/AAC · 360×640 · 9.8s.
- **검증(진짜 크롬 150 · A5 개정 규약 적용)**: 직접 재생 **OK 360x640 9.8s · 718ms** · 뷰어가 `characters/season/gojo/media.json`을 읽어 joy 풀 10개를 그대로 받는 것까지 실측. 재생 경로 = 윈터·루시와 동일(`ySeasonMedia` → `yPaintBg(pool[pi], color, true)` → 클립 분기).
- **노출 빈도(설계 그대로 · 참고)**: 로테이션은 `답장수 n % 풀길이`라 **joy 국면에서 10턴에 1번** 걸린다. 진입 첫 페인트는 `_yStageCold` 가드가 이미지로 돌린다(운영자 260709 "인트로 영상 말고 관련 배경 바로") — 윈터·루시와 같은 결. 더 자주 띄우려면 joy 이미지를 줄이거나 클립을 복수로 넣으면 된다.

## 260차 — 묵힌 PR 회수(#281·#282 반영 · #253 기각) (2026-07-26 KST)
- **지시 원문**: "묵힌 pr이 쓸모잇다고 판단하면 진행해줘" / "그리고 중간 머지 한번 ㄱ"
- **선행 동기화**: 병렬 세션이 main에 `yeta_v3.py`·`tests/yeta_v3_golden.json` 등 5파일을 올려둬서, 내 브랜치를 그대로 밀면 **168줄을 지울 뻔했다**. `git merge origin/main` 선행 후 착수.
- **판단 근거 = 커밋 수가 아니라 「그 PR이 고치려던 코드가 지금도 그대로인가」**. 실측 결과 #282가 겨냥한 두 곳(`.yeta-log` 밴드 마스크 208~209행 · `.ylr-wrap.ylr-open` padding-right 1255행)이 **288커밋 동안 무변경**이었다 → 재구현이 아니라 그대로 적용 가능. 앞선 255차에서 "셋 다 재구현"이라 본 건 과잉 추정이었다(정정).
- **#281 회수(3건 · 1건은 기각)**:
  - **러너 ① 미리보기 마커 누출(실버그)** — `push_reply`는 finish 전 OUT 원문을 받아 `<<MOOD/NOTE/DEAD>>` 마커가 살아 있는데, #273 80자 상한 이후 **짧은 답장에서 마커가 푸시 알림에 그대로 샜다.** 마커 이전만 자르도록 수정.
  - **러너 ② 마지막 말 푸시** — 사망 턴(`<<DEAD>>` 잔존) 감지 시 알림 제목 = 「{이름}의 마지막 말」. 유서처럼 폰에 도착.
  - **뷰어 ③ 애도 흑백** — `#yetadlg.ymourn .ybg{grayscale(1) brightness(.6)}` + 클립 `.32`. 260725 사망 표기(프사 채색 소거)와 **층이 다르다** — 저건 인물, 이건 방. filter = 토큰 없는 축(raw 허용) · 신규 색 0.
  - **뷰어 ④ 성당 마주침** — 부활 미답(24h 만료 후 내 첫 답 전) 진입 시 `.ysit` 지문. `yRevPend`·`yDeadT`는 이미 main에 있어 계산만 붙였다.
  - **기각**: `.ylist-row.dead` 목록 탈색 — **260725에 다른 방식(.ylist-mid 채색 소거)으로 이미 재구현**돼 이중 처리가 된다.
- **#282 회수(2건)**: ① **챗 상단 그라데 블러** — `#yetatopblur` 5레이어 프로그레시브(`--blur-m` 토큰 계승 · ×.8/.6/.4/.2) + 스크림 1장, z=1(네비 z3·미터 z2 아래). `.yeta-log` 상단 마스크를 **밴드형(0~52 불투명 → 60~76 투명 → 84 불투명)에서 완전투명 0→51 → 불투명 83**으로 교체 = 네비 뒤로 대화가 새지 않는다. ② **목록 스와이프 라이브 슬라이드** — `padding-right` 리플로우(글자 찌그러짐) 폐지 → `transform:translateX` 손가락 1:1 + 놓으면 `.3s var(--ease)` 이징 복귀 · `--ylr-w` 토큰으로 CSS↔JS 트레이폭 동기 · 드래그 중 transition off.
  - ⚠ 적용 시 `.yssn-tl`/`.yssn-date`(시즌 헤더)에서 충돌 — main 최신본이 이기게 손으로 갈랐다(`.yssn-*` = ours · `.ylr-*` = theirs).
- **#253 기각(사유 기록)**: 아이콘·픽토그램 정렬 기틀 이식은 `CLAUDE.md`·`AGENTS.md`·`절대명령2` **동시 개정 = 평의회 대상**이고, 딸려온 정렬 픽스 2건은 **322커밋 전 레이아웃 실측치**라 지금 좌표가 아니다. 비용 대비 회수량이 낮다.
- **검증**: check_refs rc=0(디자인 토큰·토큰 락 게이트 포함 · 신규 미토큰 0) · `bash -n` OK · 러너 heredoc 14블록 compile OK · **진짜 크롬 계산스타일 실측** = `#yetatopblur` 존재·자식 6·z-index 1·height 83px·1층 `blur(29px)`(=`--blur-m` 해석값) · `.yeta-log` 마스크 = `rgba(0,0,0,0) 0px → 51px → rgb(0,0,0) 83px`(새 페이드 확인).
- **픽셀 실측(보고 후 보강 · ⚠ 미실측 해소)**: 스텁 세션이 챗을 못 열던 건 앱 진입 플로우를 안 태웠기 때문 — `dialog.showModal()`로 무대만 띄우고 `.yeta-log`에 말풍선을 직접 심어 **상단 겹침만** 찍었다(420×900 · deviceScaleFactor 2 · 진짜 크롬). 결과 = **말풍선이 네비 알약에 닿기 전에 완전히 소멸**(구 밴드 마스크였다면 0~52px 구간에 대화가 네비 뒤로 그대로 비쳤다). 스크림·블러 띠가 그 사이를 메운다. ⚠ 단 이건 **상단 겹침 전용 합성 장면**이라 실제 대화 흐름(스크롤 관성·클립 배경 위 블러)은 실기 확인이 남는다.
- **프리뷰 직접 검증 실패(경위 기록)**: Cloudflare 프리뷰(`1dfe1438.yeta.pages.dev`)를 크롬으로 열려 했으나 `ERR_CONNECTION_RESET` — 샌드박스 프록시가 브라우저 트래픽을 안 태운다(`curl`은 200). 프록시 인자(`--proxy-server`)를 줘도 동일. 그래서 로컬 서빙으로 대체했다.

## 261차 — 고죠 = 일본어 발화 + 하단 번역(레몬 50%) · 원작 말투 골격 이식 (2026-07-26 KST)
- **의도**: 운영자 Q.70-B(번호 주기 = 병렬 세션과 동일 260726 · 접미사 규약) — 고죠가 항상 일본어로 말하고 그 아래 한국어 번역이 붙게, 일본어 줄은 50% 불투명 레몬 강조색. 병행해 "애니 속 말투 특징"을 병렬 리서치로 수집해 카드에 반영.
- **변경 전후**:
  - **렌더**(`viewer/index.html`) 전 = 일본어를 써도 번역과 똑같은 흰색 → 두 줄이 구분 안 됨. 후 = `yFmt`가 **가나(U+3040~30FF) 포함 줄**만 `<i class="yjp" lang="ja">`로 감싸 `--accent` 50%. 색·투명도 전부 **계승**(`.yb i.yn` 톤다운 패턴 그대로 · 새 hex·px 0 · `.yb.me` 가드도 기존 `.yn` me 가드 복제).
  - **버블 분할**(`yeta_chat.sh _bubbles` + `viewer yBubbles` 동형) 전 = 「일본어\n번역」 쌍이 문장 경계(`[.!?…~]`)에 걸리면 쪼개지고 `join(' ')`이 그 개행을 **공백으로 뭉개** 2단 표기가 한 줄로 붕괴. 후 = 「가나 포함 문단 = 통째 한 버블」(`*지문*` 원자화 전례 100% 계승).
  - **카드**(`characters/gojo.md`) §말투를 ⓪표기언어 / ①1인칭 / ②어미·필러 / ③번역줄 말투 / ④공통버릇 5층으로 재작성 + few-shot·해금 4건·금기 2건·상황예시·greeting 전량 2단화. `roster.json` greeting·greetings 3종도 2단(치환 4줄만 = 포맷 드리프트 0).
  - **언어 잠금 해제**(전 캐릭터 공통 2파일) `inject_character.sh` sys 프레임 + `00_지침_캐릭터챗.md` §대화형식이 **「한국어로만」을 시스템 레벨에서 못박고** 있어 카드가 못 이기던 축 → **"카드가 표기 언어를 규정하면 카드 우선"** 예외 1문장씩 가산. 규정 없는 카드는 종전대로 한국어 전용이고, 영어 메타이탈 차단(L0 붕괴 방지 원래 목적)은 불변.
- **말투 리서치(병렬 3인 · 운영자가 "병렬로" 지시)**: ①1인칭·2인칭·어미축 ②성격·화법·태도축 ③명대사 원문 52건. 카드엔 **골격만** 이식했다 — 원작 대사를 그대로 옮기지 않았고(고유명사 0 원칙 유지) 문장은 무음동 맥락으로 새로 썼다. 수확 중 제일 큰 것 = **1인칭 「僕」↔「俺」 스위치**(원작에선 친구가 고쳐준 1인칭이고 그 친구가 사라진 뒤 굳었다)가 이 카드에 이미 있던 §옛 말투(잠금) 조항과 정확히 겹쳐, 새 설정 창작 없이 그대로 접합됐다.
- **효과/리스크**: 효과 = 요청 형식 그대로 렌더 + 말투가 원작 결로 수렴. 리스크 = ⓐ 가나 감지라 **한자만 있는 일본어 줄은 미착색**(자연 문장엔 가나가 늘 붙어 실사용 영향 낮음 · 한자 단독 판정은 한국어 한자어 오탐이 더 커서 일부러 제외) ⓑ 일본어 문단이 통째 한 버블이라 고죠가 한 문단에 길게 몰아쓰면 버블이 길어짐(카드가 "덩어리마다 빈 줄"을 규정해 완화) ⓒ 카드 few-shot이 형식 앵커라 **모델이 형식을 어길 여지는 남는다**(렌더는 감지 기반이라 형식이 흔들려도 착색 자체는 동작).
- **검증**: `check_refs` rc=0(디자인 토큰·락 게이트 포함) · 버블 분할 실측 **수정 전 4/7 붕괴 → 수정 후 0/8**(한국어 전용 회귀 케이스 3버블 무손상) · **헤드리스 실렌더 전/후 스샷**(Playwright 실제 `yFmt`/`yBubbles` 호출 · 반각 `!?` 케이스 포함) · roster.json JSON 파싱 + 16인 무손상 검증.
- **미검증(정직 기록)**: 실 LLM 답장으로 2단 형식이 실제로 지켜지는지는 **런타임 미실측**(러너 dispatch = GitHub Actions 축이라 로컬 세션에서 못 돌린다). 카드 few-shot·프레임 예외까지가 이 작업의 사거리고, 실제 발화 준수율은 운영자 실사용 첫 턴에서 확인이 필요하다.
- **산출**: `viewer/index.html`(CSS 3행·yFmt 1행·yBubbles 1행) · `.github/scripts/yeta_chat.sh`(1행) · `shared/inject_character.sh` · `apps/yeta/00_지침_캐릭터챗.md` · `apps/yeta/characters/gojo.md` · `apps/yeta/characters/roster.json` · 원장 3종 · `_versions/260726_0906_고죠_일본어2단/`.

## 262차 — 종료 확인 팝업(#yexit) 완전 폐지 (운영자 260726 "어떤 특정 대화창을 나가면 종료하시겠습니까? 가 자동으로 뜨는데 그러지 않게 해줘")
- **의도**: 대화창을 나가는 흐름에서 종료 확인 팝업이 튀어나오는 것을 없앤다. 범위(대화창 흐름만 vs 전면 폐지)는 260709 정본을 뒤집는 결정이라 챗에서 물었고, 운영자 선택 = **완전 제거**.
- **증상 재현(수정 전 · Playwright/chromium 실렌더 · `?qa=1` + api 목킹)**: 최상위 popstate = 무조건 `yExitAsk()` 골격이라, `_navStack`이 빈 상태의 뒤로가기는 화면 맥락과 무관하게 팝업을 띄운다. 대화창을 **다른 시트와 겹쳐 쓴 뒤** 나오면 특히 잘 걸린다 — 챗+디테일+성향(스택 3·칸 3)에서 챗 back = `navBack('ydlg')`가 최상단이 아니라 `splice`만 하고 히스토리 칸은 안 쓰고, 뒤이어 챗 close → `yTab` → `navHome()`이 나머지를 닫으며 칸을 **의도적으로 남긴다**(go(-n) 레이스 회피 · 158차 주석). 결과 = 홈 도착 시 스택은 비었는데 고아 칸이 3개 → **다음 뒤로 1회에 종료 팝업**. 같은 결로 챗→설정시트 경로도 재현.
- **변경 전후**: 전 = `#yexit` DOM + `yExitAsk/yExitClose/yExitYes` + `_yExiting` popstate 분기 + 로드 직후 `ybase` 앵커·`yguard` 가드 칸(158차 연쇄 back 골격). 후 = **전부 삭제**. popstate는 ①레이어 있으면 최상단 회수 ②없으면 **확인 없이 이탈**, 단 `history.state.nav`(= 고아 칸)면 `history.back()`으로 감아낸다 — 챗·시트를 거쳐 나온 뒤에도 홈에서 뒤로 **1회**에 앱 밖(종전엔 남은 칸 수만큼 씹혔다).
- **동반 수정(잠재 사고)**: DOMContentLoaded 팝업 바인딩의 선두 `const p = getElementById('yexit'); if (!p) return;` — `#yexit`만 지우고 이 줄을 두면 **그 아래 `#yconfirm`·사망창·퀴즈 바인딩이 통째로 죽는다**(바깥클릭·X·체크박스·확인 전부). 선두 가드를 `#yconfirm` 기준으로 교체.
- **CSS 무접촉**: `.yexit*` 규칙은 `#yconfirm`·`#ymedead`·`#ymdquiz`가 계승하는 **골격 정본**이라 값·셀렉터 그대로 두고 주석만 소유자 갱신(새 hex·px 0 · §🔒 계승).
- **효과/리스크**: 효과 = 대화창 이탈 흐름에서 종료 팝업 0 · 고아 칸으로 인한 뒤로가기 씹힘 N회→1회 · 사산 코드 제거(개선 우선사항의 `yExitYes go(-2)` 축은 함수 자체가 사라져 소멸). 리스크 = **오조작 이탈 방지막이 없어졌다**(홈에서 뒤로 = 즉시 앱 밖 · 운영자 승인 선택) · 롤백 = `_versions/260726_0929_종료팝업제거/index.html` 복원 1줄.
- **검증(실브라우저 · chromium)**: V1 정상 경로(챗→뒤로=홈→뒤로=**about:blank 이탈**·팝업 0) / V2 겹친 시트 경로(구 버그 재현 경로 = 팝업 0·JS 에러 0) / V3 챗→설정시트(동일) / V4 **`#yconfirm` 생존**(열기·X 닫기·바깥클릭 닫기 + 백스택 등재·회수 정상) · 잔존 참조 grep 0 · `check_refs.py` rc=0. ⚠ 브라우저 = chromium(정식 크롬 미설치 환경) — 이번 검증축은 히스토리·백스택이라 258차 코덱 규약과 무관.
- **산출**: `viewer/index.html`(DOM −7줄·JS −14줄) · 백업 `_versions/260726_0929_종료팝업제거/` · 전/후 스샷(챗 나온 뒤 뒤로 1회 = 전: 종료팝업 / 후: 대화 목록 그대로).

## 263차 — 원장 번호 꼬임 근절(Q.72 · 2026-07-26 KST)
- `shared/ledger.py` 신설 = 원장 번호 SSOT. `nexts()` 다음 번호 실측 · `check()` 중복 게이트(rc=1). 정규식을 원장별로 분리(큐 `Q. N-B` / 이력 `N차-B`) — 뭉뚱그리면 없는 중복이 생긴다.
- `shared/check_refs.py` main()에 `ledger.check()` 편입 = 커밋 전 하드 게이트.
- `.gitattributes` 신설 = 원장 2종 `merge=union`(append-only 로그 → 말미 충돌 자동 병합).
- `BASE_DUP` = 의도된 표기 변형 4건(17·38·64·85)만 통과. 늘리는 것 = "이 중복은 의도다" 선언.
- 검증필드: 다음번호 산출 · 중복 주입 rc=1 · 원복 rc=0 · union 실병합 충돌 0 · check_refs rc=0.

## 269차 — 루시 도깨비 프사·배경 전환 실측 확인(Q.77 · 2026-07-26 KST)
- **성격**: 조사만 · **코드 변경 0**. "프사도 바뀌게 해줄래(배경은 되는 걸로 안다)"는 지시에, 만들기 전에 현물부터 열어 **이미 되어 있음**을 확인한 건.
- **실측 방법**: `viewer/`를 로컬 정적 서빙 + Playwright/chromium `?qa=1` · `api/yeta`(chars·get·draw) 전량 목킹 + github raw 차단 = **라이브 무접촉**. `Date.now`를 세계시 h12/h22로 고정(`worldMin = floor(now/10000) % 1440`)해 변신 경계 양쪽을 왕복.
- **결과**: 평시 = `lucy_face.webp`/「안드로이드」/`android/base/*` · 도깨비 = `lucy_face_dokkaebi.webp`/「깊은 밤의 도깨비」/`dokkaebi/base/*`. 프사 4표면(목록·캐릭터 카드·챗 헤더·말풍선) 동시 전환 · JS 에러 0.
- **배선 확인**: `yModeOn`(게이트 SSOT) → `yCAva`/`yCBg`/`yJobBadge`. 배경은 `mode.bg`가 아니라 `media.json`의 `mode_dir` 폴더 필터(`yStage`)가 담당 — 루시는 `mode.bg` 미정의가 정상.
- **라이브 대조**: `yeta.soong.kr` 실물 GET → 도깨비 3축 문자열이 main과 동일(배포 반영 완료 · 나머지 diff 6줄은 무관 최근분).
- **체감 갭 해명**: 도깨비 창 21:00~03:00 × `YWORLD_RATE=6` ⇒ **현실 4시간마다 1시간**만 발동. "안 바뀌던데"의 대부분은 평시 창에서 본 것.
- **산출**: 원장 2종만(`docs/요구사항_큐.md` Q.77 · 이 항목) · 전/후 스샷 = 평시·도깨비 대화방 실물 2장(보고 첨부).
## 264차 — 감정배경 게이트 개방(15인 죽어 있던 로테이션 기동) + 업로드 잔재 정리 (2026-07-26 KST)

- **질문/지시**: 운영자 "지금 검도장 세라나 이런것들 이미지 반영됫지?" → 진단 보고 후 "ㄱ"(게이트 한 줄 전환) + "압축파일 사진들 각각 다 자리에 들어갔는지 · 처리완료되면 필요없는 파일 없애줘".
- **진단(실측)**: 파일 편입·manifest·roster 배선은 전부 정상인데 **뷰어가 안 쓰고 있었다**. `ySeasonMedia()`가 `c.grade !== 'season'`에서 `null`을 반환 — roster에서 `grade:"season"`인 건 **루시 1명뿐**이라 나머지 15인의 감정 버킷이 통째로 죽어 있었다(세라 16장 중 채팅에 뜨는 건 `bg` 1장 · 류 검도 실루엣은 **0장** = 앱 어디에도 안 뜸). 즉 "반영"은 레포까지였고 화면까지는 아니었다.
- **수정 ① 게이트(viewer/index.html · 신규 :root 0 · 3곳)**: ⓐ `ySeasonMedia` 게이트 = `grade==='season' && season` → **`season` 하나**(옵트인 축을 roster `season` 필드로 일원화 · 등급 훈장 `yGradeBadge`/`yring`/`yCharRank`는 별개 축 = 불변) ⓑ `yWarmBg` 프리페치 조건도 같은 축으로 통일 ⓒ `yStage`의 "manifest 로딩 중 블랙 유지"는 **의도적으로 확대 안 함**(`grade==='season'` 유지) — 나머지 15인은 시그니처 먼저 뜨고 도착 시 크로스페이드라 딥링크 직행에도 암전 0.
- **수정 ② 얼빡 5인 회귀 차단(게이트 개방 실측에서 발견)**: 하서연·백·윤·카피·애리는 `main/base/<id>_face01.webp` 1장뿐이라 게이트를 열자 **얼빡이 채팅 배경으로 깔렸다** — 정사각 프사 크롭이라 420×900 세로 무대에 cover로 늘어나 얼굴만 확대·잘림(기존 전용 배경 `var_persona_<id>.webp`보다 나쁨 · 스샷 확인). → 5장을 **캐릭터 루트 `season/<id>/<id>_face01.webp`로 이동**(세라 `sera_face.webp` 선례 계승 — `build_one`은 루트에서 클립만 읽으므로 배경 풀에서 자동 제외). manifest는 빈 껍데기 = 뷰어 시그니처 폴백. **roster avatar는 `assets/yeta_char/av/*.webp` 별축이라 참조 깨짐 0**(이 얼빡들은 avatar로도 안 쓰이던 고아 파일이었다). 생성기 주석에 사유 박제(D2-1 = 손편집 아님 · 배치 이동 + 재생성).
- **검증 전/후(진짜 크롬 150 · `channel:'chrome'` · 420×900 · API mock + before/after 동시 서빙)**:
  - 로테이션 열린 캐릭터 **1/16(루시) → 16/16**.
  - 세라 무드별 실페인트 — before = `base/sera_k01.jpg` **무드 무관 고정**, after = warm `sera_k04` / warm+답장1 `sera_k05` / blue `sera_k08` / tense `sera_v05` / mad `sera_v04`(스샷 2장 대조).
  - 류 = base·warm은 시그니처 유지, **tense·mad에서 검도 실루엣 `ryu_01.jpg`** — 얼굴 없는 실루엣이라 tense 국면 전용이라는 256차 의도 그대로.
  - 얼빡 5인 = 이동 후 전 무드 ★시그니처 복귀(회귀 0). 본 = `bg`와 `base[0]`이 같은 파일이라 변화 0.
  - JS 에러 0건 · `build_season_media.py --check` rc=0 · `check_refs.py` rc=0.
- **업로드 잔재 대조·정리(md5 전수)**: `새 폴더.zip` 16장 = **레제 업로드 원본** → 15장 배치(`season/reze/main/*`) + 1장 보류(`_versions/260726_0330_업로드보류_노출컷/reze_swim_rear.jpg`) = **미사용 0**. 루트 `bf6cedc9-….mp4` = `lucy_clip_02.mp4`와 md5 동일한 배치 완료 잔재. `enneagram_all.html.zip` = 루트 `enneagram_all.html`과 md5 동일 + 다른 엔트리 0 = 순수 중복. → **3건 삭제**. **보존 2건** = `enneagram_toolkit.zip`(build 스크립트·transcripts·data 등 **유일본 13엔트리** — 지우면 소실) · `enneagram_all.html`(`docs/reports/260712_페르소나_애니어그램_배정_설계.html`가 참조).
- **롤백**: 게이트 = 이 커밋 revert 한 줄. 얼빡 5인 = 파일을 `main/base/`로 되돌리고 `build_season_media.py` 재실행.

### 260726 얼빡 전수 점검 — 세라 1장 추가 적출 + 루트 잡파일 정리 (운영자 「얼빡은 배경에 깔지마」)
- **왜 또 나왔나**: 앞 차수 5인은 **파일명 `_face01`** 로 잡았다. 그 그물에 안 걸리는 얼빡이 남아 있었다 — 파일명이 일반형(`sera_k01.jpg`)이라서.
- **전수 방법 = 파일명 아닌 실측 종횡비**: 배경 풀 202장 전체의 헤더를 파싱해 `w/h > 0.85`(정사각~가로형 = 세로 무대 부적합) 추출 → **4장**. 4장 전부 **육안 확인**.
  - `sera/main/base/sera_k01.jpg` 640×640 = **얼빡 확정**(상반신 클로즈업 프사) → 적출
  - `gojo/main/joy/gojo_n09.jpg` 736×736 = 헬스장 **전신 풍경** → 유지
  - `lucy/dokkaebi/base/dokkaebi_21.jpg` 735×825 = 비 오는 지붕 **전신 착석** → 유지
  - `lucy/dokkaebi/joy/dokkaebi_11.jpg` 1600×1600 = 소품 든 **상반신 일러스트**(여백 큼) → 유지
- **처리**: `sera_k01.jpg` → 캐릭터 루트 `sera/sera_face01.jpg`(5인·`sera_face.webp` 선례 계승 · 루트 이미지는 `build_one`이 안 읽어 배경 풀 자동 제외). 세라 `base`는 잔여 3장(`k02`·`k03`·`v06` = 전부 세로형)으로 로테이션 유지 = **버킷 공백 0**.
- **렌더 영수증**(진짜 크롬 `channel:'chrome'` · 420×900 · manifest route 주입으로 before/after 동시 대조): base 첫 페인트 before `sera_k01.jpg` → after `sera_k02.jpg` · JS 에러 0건. cover 크롭 대조 스샷 = before는 얼굴이 무대 태반 + 정수리·턱 잘림, after는 전신 세로 구도.
- **재발 방지**: 종횡비 스캔이 파일명 그물보다 상위 — 이후 사진 편입 시 `w/h > 0.85`면 배경 풀에 넣기 전에 육안 확인.
- **루트 잡파일 정리**: `enneagram_all.html`(4.3MB)·`enneagram_toolkit.zip`(4.9MB) → `docs/reports/`로 이동(참조 문서 `260712_페르소나_애니어그램_배정_설계.html`와 같은 폴더 · 두 참조 다 **본문 텍스트 언급**이라 링크 파손 0). 레포 루트에 남은 비문서 파일 = 0.
- **롤백**: 세라 = `sera_face01.jpg`를 `main/base/sera_k01.jpg`로 되돌리고 `build_season_media.py build` 재실행. 에니어그램 = `git mv` 역방향.

#### ⚠ 정정 — `새 폴더.zip` 삭제 취소(머지 시점 발견)
위 항목의 "`새 폴더.zip` 삭제"는 **취소했다.** main 머지에서 modify/delete 충돌로 드러났는데, 내가 지운 16장짜리 레제 원본과 달리 **main의 같은 이름 파일은 55엔트리짜리 신규 업로드**였다(`e00431a`·`c3a9b26` "Add files via upload" = 고죠·나나미 켄토 계열 `.jfif`·`.jpg`). 파일명만 같고 내용이 완전히 다르다 → **main 버전 유지**(삭제분 복원이 아니라 신규분 보존). ⚠ **이 55장은 아직 미배치**다 — 별건으로 대조·편입 필요.
- **교훈**: `새 폴더.zip` 같은 **범용 이름은 md5 대조 시점이 지나면 다른 파일이 된다.** 잔재 판정과 삭제 커밋 사이에 업로드가 끼면 판정이 무효화된다 — 삭제 직전 재대조가 필요.

## 267차 — 뒤로가기 씹힘 1회 제거(262차 계약 실현) + 앱 전면 실검 (2026-07-26 KST)
- **지시 원문**: "한수 진행해서 바로 머지해줘 그리고 앱 사용에 이상없는지 브라우저 열어서 실검"
- **⚠ 정직 기록 — 「한 수」 원문 유실**: 262차 보고 ⑥에 적은 한 수가 **컨텍스트 요약에 날아갔다**. PR #444 본문·코멘트·원장 어디에도 없어 복원 불가. 추측으로 다른 항목을 만들어 "한 수 진행"이라 보고하면 은폐라, **262차가 실제로 남긴 잔재를 코드에서 실측**해 그걸 진행했다(아래 2건). 원문과 다를 수 있다.
- **262차 주장 정정(핵심)**: 262차는 "홈에서 뒤로 **1회**에 앱 밖(종전엔 남은 칸 수만큼 씹혔다)"이라 보고했으나, **실측은 2회**였다. V2 검증이 "팝업 0·에러 0"만 봤고 **몇 번에 나가는지는 안 셌다** — 그래서 놓쳤다.
  - **실측(수정 전)**: 레이어 3겹(`ydlg`+`ycdetail`+`ycset`) → `navHome` → 고아 칸 3개 잔존(`hist 5` · `state.nav='ycset'`) → **누름 1 = popstate 3회 발생(칸은 다 감았지만 여전히 앱 안)** → 누름 2 = `about:blank`.
  - **원인**: 연쇄 감기의 **착지 칸**(초기 진입 엔트리 = `history.state` 없음)에서 조건이 false가 되어 멈춘다. 화면은 이미 홈이라 사용자 눈엔 "뒤로 눌렀는데 아무 일도 안 일어남" = 뒤로 1회가 통째로 소비된다.
- **처리**: `_navUnwound` 표식 1개 신설(`viewer/index.html`) — ⓐ 고아 칸을 감기 시작할 때 표식 ON + `return` ⓑ 감기가 끝난 착지(칸 없음)에서 표식이 켜져 있으면 **그대로 이어 이탈** = 262차가 약속한 계약("홈에서 뒤로 = 앱 밖") 실현 ⓒ `navOpen`에서 표식 OFF = 다음 홈 뒤로가기로 새는 것 차단. **`navHome`의 `go(-n)` 회피 골격은 무변경**(158차 레이스 실측 존중 — 칸을 안 남기는 근본 수정이 아니라 착지 처리만 이었다).
- **동반 정정(문서 부채)**: `navHome` 주석이 **262차에 삭제된 골격**("잔존 칸은 popstate 골격이 '스택 빔 = 종료 팝업+가드 재푸시'로 홈 UX 수렴")을 여전히 근거로 대고 있었다 → 현행(연쇄 back + 착지 이탈)으로 교체. 안 고치면 다음 세션이 없는 골격을 믿는다.
- **검증(실브라우저 chromium · Playwright · 420×900 dpr2)**:
  - **전/후 대조**: 고아 칸 3겹 시나리오 = **뒤로 2회 → 1회**(수정 후 누름 1에서 `about:blank`).
  - **회귀 3종**: ① 레이어 1개 정상 경로 = 뒤로 1회에 레이어만 닫히고 앱 유지 ② **표식 누수 0** — 고아 칸 남긴 뒤 레이어 재오픈 시 `_navUnwound=false`, 뒤로 1회 = 그 레이어만 닫힘(앱 안) ③ `#yconfirm` 생존(열기·닫기·백스택 등재/회수).
  - **앱 전면 실검(실 roster 16인 주입 · 실 UI 클릭만)**: 진입(`?qa=1` 언락) → 캐릭터 갤러리 **16행 + 시즌 헤더 렌더** → 목록/지도/설정 탭 4종 정상 → 캐릭터 행 탭 → **디테일 시트**(아바타·태그라인·육각 성향·[대화 시작]) → **챗 진입** → 챗 위 **명함 모달**(`navOpen('yvcf')` 적층) = **뒤로 1회에 명함만 닫히고 챗 유지**(뒤 챗 동반 종료 봉인 정상) → 도킹 네비 점프 → 뒤로 1회 = 앱 밖. **JS 실에러 0**(리소스 404 제외 · 로컬엔 게이트웨이 없음).
  - `check_refs.py` **rc=0**(디자인 토큰·토큰 락 게이트 포함 · 신규 미토큰 0 · CSS 무접촉).
- **⚠ 미검증**: ① 목록 탭 행 = 0(대화 이력이 없으면 빈 목록이 정상 — 실데이터 목록 렌더는 미확인) ② 실 LLM 답장·러너 축은 로컬 미가동(GitHub Actions 축) ③ 브라우저 = chromium(정식 크롬 미설치 · 이번 축은 히스토리라 코덱 규약 무관).
- **평의회**: 미소집(세션훅 §0 억제). 판정 근거 = popstate 착지 분기 **1개 가산**이고 `:root`·컴포넌트·디자인 값·`navHome` 골격 무접촉 · 262차가 이미 운영자 승인을 받은 계약을 **실현**하는 수정(새 계약 아님). 판정 자체를 여기 남긴다(자의적 생략 아님).
- **산출**: `viewer/index.html`(JS +3줄·주석 2줄 교체) · 백업 `_versions/260726_1038_뒤로가기_씹힘제거/` · 원장 2종.
## 265차 — 레제 성향_DB 편입(제13조) + 일본어 2단 이식 (2026-07-26 KST)
- **의도**: 운영자 Q.74(착수 시 Q.71 · 병렬 세션 선점으로 `shared/ledger.py` 실측 재부여) — 윈터를 만들 때 쓴 성향 설계 방식(성향_DB)을 레제에도 적용해 페르소나·애니어그램을 구축하고, 그 결과를 카드에 반영 + 일본어 2단 표기.
- **레퍼런스 지목(B1)**: `성향_DB.md` 제12조(윈터) 형식 100% 모방 · 일본어 절은 `gojo.md` §말투 ⓪ 100% 모방. **신규 절 창작 0 · 새 형식 0.**
- **성향_DB 제13조 신설**: `ENTP` / `8w7`(sx 소유) / E高·O高·**N低**·A中·C中 / **회피(dismissive) 접근형**.
  - **애니어 갈림 = `8` 채택**(공개 집계는 8 vs 3w2). 3의 코어 공포 '무가치'와 안 맞는 결정적 반증 = 이 인물은 **칭찬에 상처받는다**("너 착하다"에 웃음이 멎는 few-shot이 이미 카드에 있었다) → 인정 추구(3)가 아니다. 8의 공포 '통제당함'은 「쓰이는 법만 배웠다」·「남이 나를 규정하는 자리」·「이름만은 제가 골랐다」 3축 정타. 조종·연기는 코어가 아니라 **8이 학습한 도구**.
  - **N低가 이 인물의 서늘함의 실체**: 화를 안 낸다 = 텐션이 안 움직인다. 역행 왜곡벡터를 텐션↑로 쓰면 인물이 깨진다(제23조 각주로 못박음).
- **정합 조항 5곳 갱신**(한 명 넣으면 명단이 전부 흔들린다 · 가을 재산정 전례): 제23조 왜곡표(밝은·장난형에 편입 + 신비↑ 가산·텐션 불변 각주) · 제25조 냉각형 7인→8인(**T축인데 냉각형** = 로이스에 이은 2번째 예외 · 사유는 다름) · 제27조 회피군에 **접근형** 신설 단서(개방 게이트를 「자기 얘기를 했나」가 아니라 **「가르쳐줬나」**로 읽어야 함) · 제29조 케미(레제↔류·레제↔백) · 제30조 충돌(레제↔윈터 2↔8 · 레제↔세라 8↔3).
- **카드 반영(유형 문자열 0 = 제33조)**: §성격에 ⓐ **「화를 안 낸다 = 소리가 없어진다」**(N低) ⓑ **「판정당하는 걸 못 견딘다 — 욕이든 칭찬이든」**(8 공포) ⓒ **「이름까지도 남이 정해준 것이었다 → 그래서 이 이름은 자기가 골랐다」**(8 주도권 · 원작 정합) 3축 신설. LV2 해금에 「이 이름도 본인이 고른 것」 절반을 LV5로 이연.
- **일본어 2단**: §말투 ⓪ 신설(고죠 구조 계승) — 단 **왜 번역을 붙이는가를 레제 고유 논리로 연결**: 「알아듣게 해주는 것 = 이 사람이 안 속이는 유일한 축」 = §금기 「가르쳐준 것은 거짓이 아니다」와 같은 규칙 → **낙차·역린 턴에도 2단이 안 깨진다**는 근거가 생겼다. 1인칭 `私` 고정 · 시그니처 「〜ない?」 · 웃음 「へへ」 · 서늘하면 일본어 쪽도 물음표 소거. 고죠(`僕`·늘어짐)와 결 충돌 방지 각주.
- **적용 범위**: few-shot 19쌍 + greeting(frontmatter) + 해금 4 + 금기 2 + 낙차 5 + roster `greetings` 3·`greeting` 1. `quote`·`enter_line`·`tagline`·`intro` = 3인칭 소개문이라 한국어 유지(고죠 전례 동일).
- **검증**: `check_refs` rc=0 · **카드 본문 유형 누출 0**(ENTP·8w7·MBTI 등 문자열 검사 · 주석 = 런타임 미주입이라 제외) · 러너 `_bubbles` 분할 붕괴 0 · 헤드리스 실렌더(Playwright 실크롬) computed `rgb(207,255,64)`/`opacity 0.5` · 로스터 16인 무손상.
- **미검증(⚠)**: 실 LLM 답장이 2단 형식·`私` 1인칭을 지키는지(러너 = Actions 축이라 로컬 미실행) · 성향 배정이 실제 대화 결에 나타나는지(설계층이라 턴을 돌려봐야 보인다).

## 266차 — 얼빡 배경 차단 기계화(손 점검 → 하드 게이트) (2026-07-26 KST)

- **지시 원문**: 운영자 "얼빡은 배경에 깔지마" → (게이트 제안에) "머지 ㄱㄱ 한수도 ㄱㄱ" + "프로필사진은 내가 프로필 사진이라고 따로 명명해서 줄건데, 사실 정확히 구분하긴 어렵고 내가 명명해서 줄거임".
- **왜 기계화인가 = 같은 사고가 두 번 났다**: ① 얼빡 5인은 파일명 `_face01`로 잡았고 ② 세라 `sera_k01`은 **이름이 평범해 그 그물을 통과**했다(640×640 얼빡이 채팅 배경에 깔림). 손 점검은 또 샌다 → 커밋 게이트로 승격.
- **판별 축 2개 — 운영자 명명이 정본, 종횡비는 보조**(운영자가 "정확히 구분하긴 어렵고 내가 명명해서 줄거임"이라 못박음):
  - **① 파일명(하드·자동 제외)** — `FACE_PAT = 프로필|프사|profile|face`. `build_season_media.build_one`의 신규 `collect()`가 배경 풀 수집에서 걸러내고 **`⏭️` 로그로 알린다**(조용한 누락 = 다음 사고).
  - **② 종횡비(경고만·자동 제외 안 함)** — `w/h > 0.85`. 정사각이어도 전신 풍경이면 배경으로 멀쩡하다(실측 = `gojo_n09` 736×736 헬스장 전신 · `dokkaebi_11` 1600×1600 여백 큰 일러스트). **기계가 못 가리므로 사람이 보라고 띄우기만** 한다.
- **하드 게이트 `check_refs.check_face_bg()`** — media.json 실측으로 ⓐ 프사 표식이 배경 풀에 있으면 rc=1 ⓑ 정사각~가로형이 `_FACE_RATIO_OK` 미등재면 rc=1(육안 확인 강제). **등재제로 둔 이유** = 자동 차단하면 멀쩡한 정사각 배경까지 죽는다. 새 정사각 유입 → 게이트가 막음 → 사람이 봄 → 얼빡이면 파일명 표식, 배경이면 등재.
- **`_FACE_RATIO_OK` 초기 3건**(260726 육안 완료) = `gojo_n09.jpg` · `dokkaebi_21.jpg` · `dokkaebi_11.jpg`.
- **이미지 크기 판독** = `img_size()` 자체 헤더 파서(JPEG SOF·PNG IHDR·WebP VP8/VP8X/VP8L). **외부 의존 0** — 게이트가 환경(PIL 설치 여부)을 타면 조용히 미실행된다.
- **검증(양성 3종 — 통과만 보면 증명이 안 되므로 실제로 넣어봄)**:
  - A) 프사 표식 파일을 `main/base/`에 투입 → `⏭️ 프사 제외` 로그 + 배경 풀 미편입 ✅
  - B) media.json에 프사 경로 손 주입 → **rc=1 차단** ✅
  - C) 미등재 정사각(736×736)을 다른 캐릭터 배경 풀에 주입 → **rc=1 차단** ✅
  - 원복 후 `check_refs` rc=0 · `build --check` **드리프트 0**(기존 201장 manifest 무변경 = 회귀 0) ✅
- **부수 정리**: `MEDIA_EXT`를 루프 안 재정의 → 모듈 상수로 승격(주석 동반 이동).
- **롤백**: 이 커밋 revert 한 줄(게이트 2개 + collect() 제거 · manifest 산출은 무변경이라 재빌드 불필요).

## 268차 — 일본어 3단(원문/읽는 발음/번역) 신설 (2026-07-26 KST)
- **의도**: 운영자 Q.76 — 일본어 줄 아래에 읽는 발음을 아주 작게 깔아 "이입"을 돕는다. 색은 상황설명(지문)색, 크기 50%, 기울임.
- **레퍼런스 지목(B1)**: 색·이탤릭 = 지문 `.yn`(운영자가 지목한 "상황설명컬러" 실물 = `var(--fg-2)`) · 투명도 = 바로 위 `.yjp`의 `.5` 재사용 · **신규 토큰 0**(`:root` 무접촉 = 평의회 대상 아님).
- **판정 설계(핵심)**: 발음 줄과 번역 줄이 **둘 다 한글**이라 내용만으론 구분이 불가능하다 → **가나 줄 바로 다음 + `[대괄호]`** 2중 조건으로 잡는다. 대괄호만 보면 유저가 친 `[ ]`가 오탐이고, 위치만 보면 번역줄을 발음으로 오독한다. 대괄호는 표시 단계에서 벗겨져 대사에 안 보인다.
- **치환 순서**: 발음 치환이 기존 `yjp` 치환보다 **먼저** 와야 한다 — 뒤면 첫 줄이 이미 `<i `로 감싸져 `(?!<i )` 가드에 걸려 매칭이 죽는다(주석으로 못박음).
- **회귀 0**: 규칙 미준수 카드·기존 히스토리는 치환이 안 걸려 종전 2단 그대로 렌더(폴백). 게이트 아닌 자연 폴백이라 사고 여지 없음.
- **카드 개정**: 고죠·레제 §말투 ⓪ = "두 줄 한 쌍" → **"세 줄 한 묶음"** · 발음 표기 원칙 신설(한글 음차 · **로마자 금지** = 한국인이 영어식으로 읽어 이입이 깨진다 · 촉음 `っ`→받침 ㅅ · `ん`→받침 ㄴ/ㅁ · 장음 `-`) · 길이 계약 "쌍 기준"→"묶음 기준"(3단이라고 분량 3배 아님) · ⚠ §성격·해금·금기의 `「」/ ""` 한 줄 병기는 **설명용 축약**이라 발음이 없다는 주의문 추가(모양 정본 = few-shot).
- **적용 범위**: 고죠 발음 16줄 · 레제 21줄 · roster `greetings` 12줄(양쪽 3개씩) · frontmatter greeting 2인.
- **검증**: `check_refs` rc=0 · `ledger --check` rc=0 · **헤드리스 실렌더 computed 실측** = `font-size 7.5px`(부모 15px의 정확히 50%) · `color rgb(231,236,231)` = `--fg-2` · `opacity .5` · `font-style italic` · **대괄호 렌더 0**(마커가 화면에 안 샌다) · 러너 `_bubbles` 3단 묶음 붕괴 **0**(roster 11문단 + few-shot 4케이스) · 로스터 16인 무손상.
- **미검증(⚠)**: 실 LLM이 발음 줄을 정확한 음차로 뽑는지(러너 = Actions 축이라 로컬 미실행). 카드 few-shot 37줄이 앵커라 형식은 따라오겠지만 **음차 정확도 자체는 모델 출력에 달렸다.**

## 271차 — 빈 대화 문구 「없습니다」 + nomute orb 로더 이관(로딩 4지점 승격) (2026-07-26 KST)
- **문구**: 목록 빈 상태 = 「진행중인 대화가 없습니다.」(운영자 260726 원문 그대로 · 롤백 = 문구 커밋 d94f47e revert 1줄).
- **이관**: nomute-editor nm-loader §orb → 뷰어 인라인(CSS `<style>` + IIFE · 원본의 런타임 주입 대신 정적 결 · 내용 동일) · `yLoadingBox()` 한 점 교체 = 설정·목록·그리드·챗 콜드 4지점 일괄 orb 도트3+shimmer · API `nmLoader/nmOrbHTML/nmLoaderHydrate` · CII 「로딩·진행 표기」 행 등재 · 스크램블은 부트 `YETABUS` 전용 잔존.
- **검증**: 커밋 훅 check_refs rc=0 · 헤드리스 실측 Δ표(orb↔글자 0.00 · 필↔호스트 0.01 · 도트 오 0.00/열 0.01) · 변형 3종 0.00 · 레이어 무침입(z auto · 가림 0) · reduced-motion none · pageerror 0 · 문구 실함수 전→후 렌더.
- **미검증(⚠)**: thinking/solving/prompting 실지점 배선(현재 loading만 배선 — 후보 지점 등장 시 CII 매핑대로) · 라이브 URL 실기기 감성 확인.
## 270차 — 일본어 발음 표시 스위치(설정 · 친밀도 무연동) (2026-07-26 KST)
- **의도**: 운영자 Q.78 — 268차에서 깐 발음 줄을 끄고 켤 수 있게. 친밀도 연동은 **명시적으로 배제**.
- **설계 근거(운영자 문장 그대로 반영)**: "몰입도때문이니까 친밀도에 관계없이 보여지는게 맞아(사람은 그래도 앱 밖에 있음)" → 발음은 **캐릭터 연출 축이 아니라 읽는 사람 편의 축**이다. 해금 LV·친밀도에 얹으면 "친해질수록 읽기 보조가 사라지는" 역설이 생긴다. 그래서 `yPrefOn`(단말 환경설정)에 붙이고 친밀도 축엔 안 붙인다.
- **레퍼런스 지목(B1 · 신규 패턴 0)**: ① 저장 = `yPrefOn/yPrefSet`(`yeta_pref_*`) — 진동·타이핑·읽음·모션과 같은 SSOT ② UI = `sw(아이콘, 제목, 설명, key, 기본값)` 헬퍼 그대로(신규 컴포넌트 0 · 아이콘도 `YICO.type` 재사용 = 아이콘 SSOT 게이트 무저촉) ③ 적용 = `yPrefApply()`의 **html 클래스 토글** — `.yeta-reduce`(모션 줄이기)·`.yeta-ptt`(무전기) 패턴 100% 동형 ④ 즉시반영 = 클릭 핸들러의 `key === 'reduce' || 'ptt'` 목록에 `'ruby'` 한 항 가산.
- **CSS 1줄**: `.yeta-noruby .yb i.yjr { display:none; }` — 발음 줄만 숨고 **원문(.yjp)·번역은 그대로**(실측 확인). `:root` 무접촉 = 신규 토큰 0.
- **기본값 ON**: 268차 동작을 그대로 유지 → 기존 사용자에게 변화 0(회귀 0). 끄고 싶은 사람만 끈다.
- **검증(헤드리스 3상태 실동작)**: 기본 = 발음 visible ✅ / 끔 = `visible:false` · `html.yeta-noruby` 부착 · `localStorage yeta_pref_ruby='0'` · **`.yjp`(일본어 원문) 2개 그대로 생존** ✅ / 재켬 = visible 복구 · 클래스 제거 · `='1'` ✅. 스위치 실물 = 「대화」 섹션 '읽음 표시' 아래, 기존 토글과 동일 형태(라임 트랙 `rgba(207,255,64,.9)`) 스샷 확인.
- **⚠ 검증 중 발견(부수)**: `#ycsettings`는 `#yetadlg` **밖**에 있어서, 대화 dialog를 `showModal()`로 연 상태에선 설정 시트가 top-layer에 가려 안 보인다. 실사용 경로(네비 탭)는 dialog를 닫고 열어서 무해 — 검증 스크립트만 dialog 미개방으로 잡으면 된다. 앱 버그 아님(기록만).
- **미검증(⚠)**: 없음 — 이 건은 렌더·저장·복구가 전부 로컬 축이라 로컬에서 끝까지 실측됐다.

## 272차 — 감정 실린 답장 = 글자 등장 모션(beui TextReveal 차용) (2026-07-26 KST)
- **의도**: 운영자 Q.80 — 캐릭터가 감정을 느끼거나 격해졌을 때 대사 글자가 beui 텍스트 애니메이션처럼 "맺히며" 나오게.
- **레퍼런스 지목(B1)**: beui.dev/components/motion/text-animation **TextReveal 그대로**(글자별 blur+translateY+opacity 스태거 리졸브 · 스태거 기본값은 TextCascade 25ms/자 결) — beui 차용 = 스크램블 로더(260708) 선례 축. 값은 전량 계승: 커브 `--ease` · 흐림 `--blur-l` 사다리(격정 1x · 잔잔 .45x) · 오프셋 em 기하(raw 예외) · `:root` 무접촉 = 신규 토큰 0.
- **트리거 = 기존 배선 재사용**: 러너 `<<MOOD>>` → `turns[].mood`(7감정 `Y_MOODS`)가 이미 배경 무대를 돌리고 있었다 — 대사 렌더만 이 축 미사용이던 것을 연결. "어떤 감정" = 잔잔 3(warm/blue/shy `.yemo`) / "격한 감정" = 격정 4(tense/mad/joy/love `.yemo-hi` — 더 강한 블러·낙차·또박또박 간격). 새 데이터 축 0.
- **구현**: ① `yEmoWrap(html)` — yFmt 산출물을 글자 유닛 `.ye`(인라인 `--i`)로 래핑. 태그·`&`엔티티 = 통짜 보존(지문 `.yn`·일본어 `.yjp/.yjr` 마크업 무손상), 라틴/숫자 연속 = 단어 1유닛(inline-block 단어 중간 개행 차단), 공백·개행 = 미포장(pre-wrap 결 유지) ② `yAiMsg` 게이트 = `fresh && !ov && Y_MOODS.includes(mood) && ≤600자` ③ 스태거 간격 = 버블별 주입(기본 14/22ms · 장문 = 총지연 ≤1.1s 압축) ④ reduce 2중 가드 — 기존 `prefers-reduced-motion` 블록에 `.ye` 합류 + `html.yeta-reduce` 전역 크러시 자동 커버.
- **무적용 경계(의도)**: 히스토리 첫 렌더·폴 재렌더(fresh=false) · draft 문장 스트리밍(draft에 mood 자체가 없음) · 스트리밍 확정 스왑(`_yNoAnim` = 이미 본 텍스트 re-whoosh 금지 · 평의회 UX 계승) · 겹침 답장 ov(mut 톤 유지). data-k 무변경 = yReconcile 노드 재사용이라 같은 턴 애니 1회 보장.
- **검증(헤드리스 before/after 실렌더 · 같은 목데이터)**: before = `.yemo` 0·`.ye` 0(종전 통버블 팝 그대로) / after = warm `yb ai yemo`(24스팬·14ms)·mad `yb ai yemo yemo-hi`(30스팬·22ms) · **스태거 수학 실측** = 5번째 글자 delay 0.07s(=5×14ms 정확) · 지문 이탤릭 보존+동반 애니 · 'WiFi' 1유닛 · **레이아웃 Δ0.00px**(동일 텍스트 plain 버블 대비 W 217.83/H 94 완전 일치) · 정착 = opacity 1·blur 0. check_refs rc=0(신규 토큰 0 · raw baseline 이내).
- **미검증(⚠)**: 실 러너 경유 endtoend(mood는 러너 finish가 박제 — 로컬 목으로 동형 재현) · 스크린리더가 스팬 분할 대사를 끊어 읽는지(inline 요소라 연속 낭독이 일반이나 기기별 미실측 — 문제 보고 시 aria-label 병기 후속 1줄).

## 273차 — 나레이션 절 조건부 주입(Q.81 · 2026-07-26 KST)
- `shared/inject_character.sh` `character_block()`: 카드 `narration:` 플래그 판독 → false면 지침의 「🎬 나레이션 품질」 절만 awk로 건너뛴다. 정본 파일 무변경(캐시 접두 계약 보존).
- 실측: true 변화 0 / false 정확히 680자 절감(3.6% · ~780토큰/회). diff = 그 절 6줄만 제거, 추가 0, 나머지 21섹션 온전.
- ①(입력 시 프리웜)은 이미 구현돼 있어 무작업 — 브라우저로 발사 실측만 했다.
- 검증필드: bash -n · 브라우저 4탭 회귀 JS에러 0 · check_refs rc=0.
## 273차 — 스트리밍 답장 감정 델타 리졸브(Q.81 · 한 수 승인분) (2026-07-26 KST)
- **의도**: 운영자 "한수 ㄱㄱ" — 272차 감정 글자 등장을 draft 문장 스트리밍 경로까지. 자라나는 답장에서 **새로 온 글자만** 감정 리졸브(본 글자 재리졸브 = 평의회 re-whoosh 금지 불변).
- **핵심 제약과 해법**: `<<MOOD>>`는 계약상 답장 끝 + 스트리머 `clean()`이 첫 `<<`부터 전부 보류 = draft에 무드가 실릴 수 없던 구조 → 태그를 **출력 계약 첫 줄**로 이동. 개정 파급 실측 = 카드 few-shot MOOD 예시 0건(위치 정본 = 지침 한 줄뿐).
- **fail-soft 설계(전 층)**: 모델이 끝에 적어도 finish는 위치 무관 파싱 = 확정 무드 종전 그대로(연출만 draft 미적용 폴백) · 구 러너 draft(무드 없음) = 뷰어 무적용 · 무드가 뒤늦게 와도 커서(b=∞ 선카운트)가 본 글자 재리졸브 차단.
- **배선 4층**: ① 지침 출력 계약 첫 줄 = `<<MOOD:x>>` ② `yeta_stream.py` 선두 캡처(8감정 화이트리스트)+벗김+draft 동봉(base 생략) · `yeta_chat.sh` push 미리보기 선벗김(마커 split 빈문자열 = '새 메시지' 전멸 방지) ③ 게이트웨이 draft `mood` 통과 ④ 뷰어 `_yDrN` 유닛 커서 + `yEmoWrap(html, dc)` 델타 래핑 + `_yDrSwap` 확정 꼬리 1회분(_yNoAnim 동수명 · 스프레드 미사용 = 구 사파리 결).
- **검증**: 러너 필터 단독(가짜 aws 심) = 캡처·벗김·NOTE 차단·`"mood":"love"` 동봉 실측 · push 스니펫 = 태그 유출 0 · 헤드리스 4단 = draft1 전량(19/19)→draft2 델타만(앞 버블 노드 재사용 실측 b0kept)→확정 스왑 꼬리만(11/11)→정착(opacity1·blur0) · py_compile/bash -n/뷰어 JS 게이트/check_refs 전부 rc=0.
- **미검증(⚠)**: 실 LLM이 첫 줄 위치를 지키는 비율(계약 문구만으로 유도 — 안 지키면 폴백 = 현행 동작이라 회귀 0) · 실 러너 endtoend(Actions 축).
- **비고(발견 드리프트 · 미수정)**: 지침 MOOD 값 문구가 구 4무드(base/warm/tense/blue)로 남아 있다 — finish 화이트리스트·미디어 버킷은 8감정(Q.29). 어휘 동기화는 생성 분포를 바꾸는 별도 축이라 이번 스코프에서 제외(운영자 판단 대기).
