# 📑 CII — 컴포넌트 계승 인덱스 (Component Inheritance Index)

> **운영자 정본 · 260628 · 분신술 10인 도출.** UI/UX 컴포넌트를 **표로 인덱싱**해 "항상 계승"시키는 기틀. §🎨 "📦 정본 컴포넌트 인벤토리"(불릿)의 **기계가독 표 승격판**. 새 버튼·입력칸·모달·아이콘을 만들거나 이식할 때 **반드시 이 표의 정본 셀렉터를 계승**(재설계 금지). 충돌 시 §🎨·viewer `:root`가 정본.

## 왜 (재발 방지)
드리프트 2대 근원: ① thumb/ly/k/comp가 index `:root` 토큰 미상속(raw 값 박힘) ② 같은 컴포넌트 4중 복제(한 곳만 고쳐짐). 이 표 = "어떤 컴포넌트는 *무엇을* 계승해야 하는가"의 단일 출처. 코드가 표와 어긋나면 드리프트.

## 위계 (값·규칙·강제 3층 — §🎨 그대로)
1. **값 SSOT** = `viewer/index.html :root` 토큰 (색·`--btn*`·`--r*`·`--sp*`·`--blur*`·`--z*`·타이포·모션).
2. **규칙·인덱스** = 이 표 + §🎨 + `구성도/00_가이드북_버튼인터랙션.html`.
3. **강제** = `shared/check_refs.py check_design()`(raw 값 baseline·5파일 커버) + `build_design_mirror.py`(거울 정합).

## 표기 규칙
- **크기·마진·반지름·z = 토큰만**(raw px 금지). 없으면 가장 가까운 토큰, 정 없으면 `:root`에 토큰부터 추가.
- **아이콘 = SVG만**(이모지·`✕`문자 금지). 같은 의미는 같은 path.
- **닫기/최소화/버튼묶음 = 헤더 우측**(`margin-left:auto`는 *묶음 래퍼*에 — 개별 버튼에 걸면 갇혀 좌측붙음 = editdlg 버그).
- **이미지 위 오버레이 버튼 = 검정 20%**(아이콘색만 accent). 일반 다운로드 = `.dlbtn`(accent 10%). ⚠️ **카드 위 다운로드류(저장 `.save`·오버레이 PNG `.ovl` 등)는 빠짐없이 `.dlbtn`** = 클릭 액티브 통일(안 붙이면 누락 · 260629 `.ovl` 교정).
- **누름 :active = press *토큰 티어* 계승** — viewer :root `--press-pico/xs/s/m/l` 5토큰 사다리(아래 「:active 눌림 효과 토큰화(260628)」 항목 = 현행 · 티어 용도는 :root 주석 정본)를 `scale(var(--press-*,폴백))`로 참조. 게이지·보라·카드는 셀렉터 화이트리스트 제외. raw scale 창작·고정 0.96 금지(옛 리터럴 표기 폐기 · 260702).
- **소머리(구분자) `•` = 텍스트 흰색(`--fg`)·`--fw-x`(800) / 블릿 형광(`--accent`)·`--fw-b`(700) · `--fs-label`(13px)** — 크기·굵기 *토큰*(리터럴 금지). 특수(이전제작·최소화)만 블릿 보라(`--hist-accent`) · 텍스트는 *항상* 흰색. **회색(`--mut`) 소머리·블릿 없는 소머리 금지.** 블릿 메커니즘 = **블록은 `::before content:'• '`(공백·뉴스 정본 동일), flex(`.csec`)는 `content:'•'`+`gap:6px`**(둘 다 6px급 간격·layout별). 정본 = 뉴스 `index .cref-lbl`. `.gospec`(명세 readout)은 소머리 아님(예외). ⚠️ **flex `.csec`에 토글(`.ovfmt`/`.onoff`) 붙으면 토글 세로패딩(3px·탭영역)이 행을 키워 `•`가 ~3px 내려앉음 → `.csec .ovfmt, .csec .onoff { margin-block:-3px }`로 행높이 기여 상쇄**(탭영역 보존·토글 유무 무관 첫 블릿 화면선 통일·운영자 260629 저작권탭 교정). 강제 = `check_refs.check_soremeori()`.

## 인덱스 표

| 컴포넌트 | 정본 셀렉터 | 크기 | 플레이트 | :active | 아이콘 | 위치 | 마진 | a11y | 정본 |
|---|---|---|---|---|---|---|---|---|---|
| 닫기 X | `.tool-x`(별칭 `.dlg-x/.ed-x/.askx/.mx`) | `--btn-sm`(30) | glass `.06`+`--line` | 회전180 .92 | path `M6 6 18 18M18 6 6 18` sw1.8 | 헤더 우측묶음 / abs 우상단 | — | `aria-label="닫기"` | index `.tool-x` |
| 최소화 − | `#toolMin`·`.askmin`·`.ed-min`·`.rmin` | `--btn-sm` / 28(rfab) | glass(헤더) / glass border:0(rfab) | 헤더 .8 · rfab=픽토 .55 | path `M6 12h12` | 닫기 좌측(헤더) | — | `aria-label="최소화"` | index `#toolMin` |
| 복원 ▲ | `#toolRestore` + `.min-pick` | `--btn-sm` | glass `--blur-l` | scale .82 | ▲ | 우하단·`--z-min`(200) | — | focus 이동 | index |
| 최소화 라벨 | `MIN_REG` label | — | — | — | — | picker | — | — | "부모 메뉴 - 세부"(이미지-썸네일/영상-자막/뉴스 요약-신청/카드뉴스-이미지-수정) |
| 입력칸 focus | `input/textarea:focus` | — | — | — | — | — | — | `:focus-visible` 2px accent 링 | 4파일 통일 `rgba(var(--accent-rgb),.35)`+`.08` 링 |
| 클립 3버튼 | `.iobtn-edge`(별칭 `.urlclip/.scnclip/.askclip` → 통일 대상) | 26 | glass `.06`/blur13 opacity.6 | scale .85 | COPY/PASTE/CHECK/ERASE/UNDO `_SVG` | **입력칸 우측 걸침 2케이스**(우하단=base / 우상단=`.asktawrap.clip-top`) · 가로 right 14/46/78px 사다리(우측 기준) · 다른 버튼 위치 따라 상/하 선택 · **케이스 2개 고정**(과증식 금지·운영자 260629) | — | — | thumb `attachCopyPaste` · index `.asktawrap.clip-top` |
| 다운로드 | `.dlbtn`(+컨텍스트) | `--btn` | accent 10% / 이미지위 검정20% · **받음=`.dl-done`→흰색(`--fg`)** | ↓바운스 + ✓팝·링(클립) · 클릭 후 흰색 영속(URL키 `DLED` 복원·260701) | `DOWNLOAD_SVG` | abs 우상단행 | — | `aria-label` | index/thumb `.dlbtn` |
| 소머리(구분자) `•` | `.cref-lbl`(정본)·`p.lbl`·thumb `.csec`·k/ly/comp/thumb포스트 `label.fl`·`.hist-bul`(특수) | `--fs-label`(13)·텍스트`--fw-x`/블릿`--fw-b` | 텍스트 흰색`--fg` · 블릿 형광`--accent`(특수=보라`--hist-accent`) | — | 블록=`::before content:'• '` / flex(.csec)=`content:'•'`+gap6 | 섹션·폼 머리 | 14~16 0 | 블릿 ::before/`aria-hidden`=SR 무시 | index `.cref-lbl` |
| 최외곽 박스(뷰) | 레거시 `.scrap-cols`·뉴스요약 `.card`·SNS `.soc-item` | radius `--r-l`(16) | — | — | — | 각 뷰 최바깥 | — | — | 전 뷰 16px 통일(레거시 기준·운영자 260701) · 모달은 예외 `--r-modal`(22) · 내부 중첩(sc-item13·failtray14)은 각자 유지 |
| 모달 헤더 | `.tool-h`(+`.tool-hbtns`) | — | — | — | — | 제목 좌 + 버튼묶음 우(`margin-left:auto` 래퍼) | `11px 16px` | `aria-labelledby` | index `.tool-h` |
| 모달/팝업 | `<dialog>` + `history.pushState` | — | glass blur | `@starting-style`(등장) | — | top-layer | radius 22px · **헤더↔입력 간격 = 15px 단일표준**(`.askhead margin-bottom` · ask-family[askdlg·revdlg·crevdlg·tredodlg(이미지 수정 요청)] 전부 동일 · 클립 하단걸침이라 상단 클리어런스 불필요 · 모달별 override 금지 · 운영자 260629) · **스크롤형 모달 둥근 모서리 음영 차단(260630)**: 모달이 스크롤 컨테이너면 `scrollbar-gutter:stable both-edges`(PC 세로 스크롤바가 우측에만 거터 생겨 우상단 둥근 모서리에 음영 누출하던 것 = 좌우 대칭 예약으로 차단) **+** sticky 헤더에 top `border-radius`=모달 radius(헤더 직각 배경이 둥근 모서리 덮음 차단) / `overflow:hidden` 가능한 모달(#tooldlg)은 그걸로 갈음(둘 다 불필요) · 모바일 `radius:0`이라 무해 | 백버튼=닫기 | index `tooldlg`·`.askhead` |
| FAB 빠른메뉴 | `#rfab`(.rc/.ro/.rmin) | 54/35/28 | glass `rgba(0,0,0,.34)`+blur14 | rotate135(코어) | + / 도구 픽토 | 우하단·z70 | — | PC=숨김 | thumb `#rfab`(ly/k 동기) |
| 토스트 | `.nm-toast`(긴급 빨강)/`.nm-toast.fail`(실패 앰버) | 버튼 28(`.ft-act`)·이동 min54 | 글래스 `.86`+blur18(색만 구분) | 버튼 `.sbtn` press-s | `.ft-act`=.sbtn·svg14 · 이동↗=`.go-ready`(초록 요약완료)/`.go-wait`(회색 준비중·실패) · 클릭 시맨틱모션(`data-motion`) | fixed 하단·bottom 86·`--z-float` | — | ⚠ `role=alert` 필요 | index `showToast`(긴급 [✓체크][이동])/`showFailToast`(실패 [✓확인][↻재시도][↗원문]) |
| 아이콘 누름 모션 | 위임 click(캡처·1핸들러) | — | — | — | `ic-spin`(회전·기본)·`ic-bounce`(↓다운로드/저장)·`ic-rise`(↗이동 7시→2시 상승)·`ic-check`(✓팝)·`ic-share`(별자리 훑기 — 공유 3점 위→아래 순차 강조색 플레어+선 드로우 · SHARE_SVG 전용[원3+선2 path분리·pathLength=1 훅 필요] · 운영자 260703) · 버튼 `data-motion="rise/check/share"` 우선(없으면 다운로드=bounce/그외=spin) | — | — | reduced-motion 무효 | index 위임핸들러(§🎨) |
| 픽토그램-온리 액션(발행본) | `.pubpop .pub-ico`(`.pub-share`/`.pub-rm`) | 34(히트 `::before -4px`=42) | **무도형**(배경·테두리 0) · **상시 의미색**(공유 `--accent`/삭제 `--danger` — rest 무채는 운영자 260703 폐지) | 공유=`data-motion="share"` 별자리 훑기 · 삭제=`:active` scale `--press-xs` | SHARE_SVG·`_TRASHSVG` | 행 우측(qrow gap 유지) | svg 18px | disabled=`--mut` 40%·행높이 51px(`.pubpop .qlist` basis 정합) · 별자리 훑기 도입처 = 발행본 `.pub-share` + 요약 헤더 `.dlg-pdf`(okflash 초록 플래시와 병행·260703) | index pubRowHtml(#1377 복원·260703) |
| 채팅 버블(yeta) | `.yb.me`/`.yb.ai`(+`.sys`/`.fail`) · 캐릭터 = `.ymsg`(아바타+이름+버블) | me max-w 78%·msg 86% | me=`--bubble-me`(브랜드 라임=`var(--accent)`)·ai=**글래스**(`--glass-2`+`--glass-line`+`--blur-m`·`@supports not backdrop-filter`=불투명 `--bubble-ai` 폴백) · sys=글래스 알약(`--glass`+`--r-pill`) — 글래스모피즘(260705) | — | *나레이션*=`i.yn` 이탤릭 | 꼬리쪽 모서리만 `--r-s` | radius `--r-l`·pad 10/13 | 시각 `.ytime`=KST 오전/오후 h:mm·버블 밖 꼬리쪽 하단(캐릭터 `.ybrow` 우하단·내 것 `.ymrow.me` 좌하단·카톡 결·tabular · 옛 `.yb-cap` 소요초 폐지 260705) | index `#yetadlg`(yRender·yAiMsg) |
| 다이얼 세그먼트(yeta) | `.ydial`(radiogroup)+`button.on` | h~30·flex:1 | 무채 `.04`+선택 `.07`·선택색=`--yeta-ac`(페르소나색) | `--press-m`(.9) | 텍스트만 | `.yeta-dials`(입력행 위) | gap 4/6·동심원 r=`--r-m`-3 | `role=radio`·`aria-checked` | index `yPaintDials` |
| 대화목록 4줄 카드(yeta) | `.ylist-mid` 슬롯 = `.ylist-grade`(배지줄·이름 위) → `.ylist-nm`+`.ylist-meta`(시간+`.ylist-ago` 상대시간 — **이름줄 우측 상주 = 라인 중앙정렬**) → `.ylist-tht`(생각·**불멸** — 못 읽으면 `.na` "(무슨 생각을 하는진 모르겠다)") → `.ylist-last`(최근말 흰) | 간격 = `--lgap` 비율 **5:5:3.5**(등급↔이름·이름↔생각 = `--lgap` · 생각↔말 = `calc(--lgap*.7)` — 전 레포 공통·.ycrow/.yme 동형) | 생각 = `rgba(var(--think-rgb),.72)` 터콰이즈 저투명(과하면 α만) · 상대시간 = `--fg` 흰(N분·N0분·N시간~6h·초과 미표시 · `yAgo`) | — | — | 대화 탭 카드 | — | — | index `yListRender`·`yAgo`(운영자 260707 4줄 체계) |
| 스페셜 등급 훈장(yeta) | `.ygrade`+`.season`/`.idol`(생성 = `yGradeBadge(c)` · `c.grade` 옵트인 — 시즌 특전 · 동렬·상호배타 · **보이스 `.yprem`은 별개 "기능 아이콘" 축**) | 인라인 pill | 베이스 = `.yprem` 선언 계승 + **양각 sheen**(`inset 0 1px 0 --glass-line`) · season = `--season` 12% 플레이트+테 `.4` · idol = 흰·실버(`--glass-2`+`--glass-line`+`--fg` — 신규 색 0) | — | **보석 젬 SVG**(외곽+하단 파셋 컷·drop-shadow 양각 — 파형과 실루엣 차별) + 등급명(시즌/IDOL) | **등급줄(이름 위)** — `.ylist-grade`/`.ycrow-grade`(보이스 `.yprem` 앞 · 훅 5곳 병렬 · 260707 4줄 체계로 이설) | pad 1/8·`--r-pill` | title="스페셜 등급 — {등급}" | index `yGradeBadge`(코어 — call.js 아님·음성 모듈과 독립 · 운영자 260707 훈장/기능 분리) |
| 캐릭터 챗 화면 | `#yetadlg`(dialog+pushState) | **풀스크린** 100vw×100dvh(데스크톱 = `.ymain` 정합 9:16 프레임) | **글래스모피즘**(운영자 260705) — 배경 `assets/brand/yeta-bg.webp` full-bleed + `--bg-scrim` + 균일 딤(rgba(0,0,0,.3)) · 표면(헤더 `.yeta-h`/버블 `.yb.ai`/입력 `.yeta-in`/소개 `.yintro`)=`--glass`·`--glass-2`+`--glass-line`+`--blur-l`(수신 콜 `.ycall-bg`/`.ycall-line` 확립 패턴 계승) · 헤더 safe-area 상단 | — | 닫기=`.tool-x`·전송=`.yeta-send`(모션 위임 등재) | **풀스크린**(팝업 카드 폐지 · 닫기=메인 복귀) | radius `0`(풀블리드) | 뒤로가기=popstate | index `openYeta`·greeting SSOT=roster.json(카드 frontmatter와 동기 유지) |
| 캐러셀 | scroll-snap 트랙 | — | — | — | — | — | — | — | `feed-ui`/`cardRefCarousel` |
| 수정 진행바 | `.reshoot-badge`+`.reshoot-bar`(썸네일) · 카드=`.reshoot-badge`(텍스트만·바 없음) | 바 118px·h4 | 배지 glass `rgba(8,15,11,.62)`blur10 · 바 track `rgba(255,255,255,.16)`·채움 `--accent` | — | 없음(텍스트 "수정 중…" + 인디터미넌트 스윕) | 이미지/슬롯 정중앙 abs·z3 · dim `brightness(.5)` | radius `--r-pill` | reduced-motion=감속(2.4s) | 썸네일 index `markSlotReshooting` · 카드 `markReshooting` |
| 수신 콜 화면(yeta) | `#calldlg`(dialog)+`.ycall-btn`(.take/.drop) | 버튼 `--btn`+`--sp-3`×2 원형 | 페르소나 bg+`--bg-scrim` · 자막 `--glass`+`--blur-m` · dialog=`#yetadlg` 결(글래스 엣지·`::backdrop`·모바일 margin:auto) | `--press-m` | 수화기 SVG 단일 path(거절=회전135°) · 파동 링=키프레임(안무 예외) | top-layer(dialog) | pad `--sp-3` | `aria-label` 받기/거절 · Esc=거절 · 뒤로가기=거절 | viewer/call.js(플러그인 · 앱 구조 CLAUDE.md [15] ☎️) |
| YETA 워드마크 로고 | `.ywm`(+`.wl` 겹층 6: halo·판B·body·판A·scan·flash · JS 조립 `#ywm`) | `--w:min(360px,76%)`·AR 3.6356 | 레터링 = 포스터(main_v2) 원본 추출 마스크 4장 `assets/brand/yeta-wm-*.png` · 몸체 = 글래스 80% 그라디언트(흰+`--accent-rgb`) · 판 = 코발트+라임 반전(`--cobalt-rgb`/`--accent-rgb` .85 screen) · 그림자 = 코발트 글로우(drop-shadow 무토큰 축) | — (비인터랙티브 `role=img`) | 모션 6종 키프레임(`ywmGl*`·Glint·Vhs·Scan·Pulse·Flick — 안무 예외축) · 자간 -22% 체감·엇갈림 = `--w` 비례 기하 | 캐릭터 탭 상단(타이틀) | margin `4px auto 18px` | `aria-label="YETA"` · reduced-motion = 전 애니 정지 | index `.ywm`(운영자 260705 확정 조합 박제) |
| 입력행 픽토 3종(yeta) | 마이크 `#yetaMic`(`.ycall-mic`) · 전송 `.yeta-send` · +더보기 `.yeta-plus` | 전부 `--btn` · svg 22(동일 크기) | **픽토그램-온리**(도형 제거 · 운영자 260705) — 전송=강조색 `--accent`(CTA) · 마이크=무채 `--fg-2`(#9 강조색 뺌) · +=무채 `--fg-2` · 마이크 녹음=`--danger` 점멸 | `--press-m` | 마이크/종이비행기/+ SVG 단일 path | `.yeta-in`(중앙정렬 `align-items:center` · 바 레이어 없음) — 마이크 좌측(call.js 주입) · +·전송 우측 | — | `aria-label` · 녹음중 placeholder 교체 | call.js `initPtt`+`ensureCss`(스타일 로드 시 주입 — 옛 버그: `ensureDom`[콜 때만]에만 있어 마이크가 기본 버튼으로 뜨던 것 수정) · 전송/+ = index.html |
| 프리미엄 보이스 배지(yeta) | `.yprem` | `--fs-xs`·`--fw-b` | accent 12% 플레이트+40% 테두리(`.dlbtn` 결) | — | 파형 SVG 단일 path | 캐릭터 이름 우측(`yPremBadge(c)` 훅) | ml 6 | `title="전용 음색(프리미엄)"` · roster `voice` 존재 시만 | viewer/call.js `yPremBadge` |
| 통화 헤더 버튼(yeta) | `#yetaCallBtn`(픽토-온리) | `--btn`·svg 19 | **픽토그램-온리 무채 `--fg-2`**(도형 제거 · 운영자 260705) · **2탭 재확인 armed=`--arm`**(유료 발동 가드) · focus-visible=라임 링(.tool-x 계승) | `--press-m` | 수화기 SVG 단일 path(call.js 공용) | `.yh-r` 첫 자리(모듈 주입·본체 무수정) | — | `aria-label="통화"` · phone 배선=보이스톡 / 미배선=op ring 폴백 | viewer/call.js `initCallBtn` |
| 설정 탭 시즌 정책 패널(yeta) | `.ysetting`(패널 골격=`.ychar`/`.ylist` 계승) + 축 행=`.yset-row.ypol-row` · 3단 세그먼트=`.yset-chip`(.on) · 섹션=`details.yset-grp` · 저장 배지=`.yset-saved` (전부 기존 정본 재사용 · 신규=골격·`.ypol-hint`·`.ypol-lock`뿐) | 칩 `--fs-xs`·pad 7/16 | `--glass`+`--glass-line`+`--blur-m`(yset-grp) · 활성 칩=`--accent` 채움 | `--press-m`(칩) | 자물쇠=`.ym-access` SVG path 재사용 | 메뉴4 탭(`.ymain.tab-settings`) · 딤=tab-char 동값 | pad=패널 계승 | L1만 편집(enum) · L0=read-only 잠금 목록 · 축·라벨·문구 정본=`apps/yeta/policy.json`(op policy GET) | viewer `ySettingRender` · CLAUDE.md 🎚 3계층 |
| 챗 프로필 팝오버(yeta) | `#yetaProfPop`(표면=`.yeta-pick` 정본 계승) + 내부=`.yintro-ava`·`.ycd-nm`·`.ycd-tag`·`.ycd-quote` 재사용 · 신규=`.ypp-think`(생각)·`.ypp-meta`(위치·공기)·`.ypp-desc` | `--fs-sm`/`--fs-xs` | `.yeta-pick` 무채 글래스 크롬 그대로 · 생각=`--think` 이탤릭 · 메타 구분자=`.ylr-meta .sep` 결 | — | — | `.yeta-stage` 상단(픽커 자리) · 열기=헤더 프사(`#yetaAva`/`#yetaAva2`) 탭 · 바깥 탭=닫힘 | pad `--sp-3`/`--sp-2` | 생각=마지막 답장 결 파생(러너 think 필드 승격 전) · 위치=`yPlaceSync` 캐시(미해석=알 수 없음) | index `yProfPop`(운영자 260709 "프사 탭=현재 생각·소개") |
| 챗 상단 미터(yeta) | `.ymeter`(`#yetaMeter`) | 10px(서브캡션 선례 계승 — `.yssn-date`·`.ylr-meta .lb` 동급)·tabular | 무도형 · `--mut` · 역상 헤일로 text-shadow 상시(밝은 무대 판독 — 토큰 없는 축) | — | — | `.yeta-stage` 상단 중앙(left0/right0·center — 운영자 260709 재검수) · `pointer-events:none` · **고유 스트립** = `.yeta-log` 밴드 마스크(60~76px+safe)가 통과 대화를 그 밴드에서만 숨김 | top 62+safe | `aria-hidden` · 형식=`모델 --effort eff ┃ 누적 tok`(다이얼=YDIAL · 토큰=답장 턴 `tok{i,o}` 합 = 러너 claude_meter 실측) | index `yMeter`(운영자 260709 첨부3) |
| 프로필 그리팅 헤더(yeta) | `.ypg-head`(+`.ypg-hi`/`.ypg-nm` · 아바타 = `.yprof-ava` 34 재사용) | 아바타 `--btn`(34) | 무도형(텍스트 2줄) | 아바타 = `.yprof:active` 경로 | 카메라 배지 `.yprof-cam` 16 축소 | 프로필 시트 최상단 | pad 2/4/12 | `#yprofAva` role/tabindex/aria-label 유지 | index `yProfileRender` v2(운영자 260712 전후비교 확정) · 인사 = `YPG_HI`×`ymSlot` |
| 히어로 미션 카드(yeta) | `.ymis`(+`.ymis-badge/-ttl/-sub/-row/-chip/-play/-disk`) | 라운드 `--r-modal`·pad 14·칩 h35·디스크 98(돌출 13)·진행 원 30 | `.ypf-g` 글래스 + 라임 엣지(`rgba(--accent-rgb,.38)` 테) · 배지/진행 원 = `.yme-tag`/`.yset-chip.on` 라임 결 | 진행 원 `--press-m` | 시계·번개 인라인 svg 13 · 재생 삼각(광학 1px) | 그리팅 아래 | 2/0/4 | 진행 = `aria-label="미션 진행"` · 미대면 디스크 = `.um` 실루엣(정체 은닉) | index `yMissionHero`(파생 = `yProfDerive` · dispatch 0) |
| 친한 인물 레일 카드(yeta) | `.ypb-card`(+`.ypb-avw/-ava/-no/-nm/-q` · 레일 = `.ypb-rail`) | 폭 106·아바타 58·순위칩 17 | 글래스 카드(`.ypf-g` 동값) · 1위 = 라임 링 `rk1` · 시즌 = 보라 링 `rks`(yring 결) | `--press-l` | — (아바타 = `yAva` 재사용) | 스탯 아래 가로 스크롤 | gap 11 | button+이름·인용 텍스트 | index `yBffTop`(내 발화 수 = yFarOK 지표 계승) |
| 미션 뱃지(yeta) | `.ybd`(+`.ybd-core/-em/-nm/-pg/-lock` · 그리드 = `.ybd-grid` 3열) | 지름 60·픽토 28·자물쇠 34% | 글래스+양각 인셋(`.ygrade` sheen 결) · 스쿼클(`corner-shape` · 폴백 32%) · 티어 링 = 실버(`--fg` .3)/라임/시즌 보라(배지 전용축) | `--press-m`(core) | **픽토그램 = 시스템 이모지**(운영자 260712 지시 = SVG-온리 규칙의 명시 예외 · `.yme-mood em` 선례) · 잠금 = 티저 필터+자물쇠 오버레이(`.ycrow.locked` 결) | 레일 아래 | gap 12/8 | button `aria-label`=이름+조건+잠김 · 이모지 `aria-hidden` · 상태 = `.ybd-pg` 텍스트 병기 | index `YBD` 12종(판정 = `yProfDerive` 세션 파생) · 연출 = 팝+샤인 1회(reduced-motion 가드) |
| 이름부 풀다운 시트(yeta) | `#yetaPdc`(+`.pin` · 그래버 = `#yetaWho`+`.yw-chev`) | **높이 = 내용 실측**(`yPdH()` = `.pin` scrollHeight+테두리 2 · 상한 = 화면 잔여, 초과 시 `.ypd-fit` 내부 스크롤 · CSS 285 = JS 미동작 폴백 · 구 고정 285는 생각 2줄에서 액션 라벨 절단) · 임계 40 · 등장 .44s `--ease` | `.yeta-pick` 크롬 계승(top = 헤더 아래 62+safe — 열림 중 헤더 조작 가능) · 내용 = 프사 팝오버 파생 축(생각·위치·공기) + 무드칩(`.ypd-mood em` = `.yme-mood em` 선례) + 액션 4(`.ypd-act i` = `.ycd-gear` 결 34 글래스 원형) | `--press-m`(액션) | 수화기·유저플러스(신규 동계열)·기어·유저 = YICO SVG | 챗 헤더 알약 바로 아래 | 내부 stagger .05s 계단 | `#yetaWho` role=button+키보드 · 드래그+탭 트리거 · 바깥 탭 = 닫힘 · 팝오버 4종 상호배타 | index `yPdRender/yPdOpen/yPdClose`(운영자 260712 — 260705 "표시 전용"을 인물 카드 진입으로 승격) |
| 사망 표기(yeta) | `.ylist-row.ydead`(대화 목록) · `#ycdCard.ydead`·`#yetaProfPop.ydead`(인물 카드·프사 팝오버) · 정보줄 = `.ypp-meta.ydead-l`(2줄 분리 · sep 매달림 회피) + `.ypp-desc`(각주) 재사용 · 사망 중 액션 잠금 = `.ypd-act:disabled`(`.ycd-cta:disabled` 결 계승 · 통화·초대만) | — (신규 치수 0) | **채색 소거 3처방**: 프사 `filter:grayscale(1) brightness(.5)`(`.ycrow.locked .yava` 100% 계승) · 글자축 `filter:grayscale(1)`(`.ylist-mid`·`.ycd-tag`·`.ycd-quote`·`.ypp-think`·`.yhex`) · 행 `opacity:.85`(목록 선례 `.ylist-row.estr` 값 — 캐릭터 탭 `.66`은 배경 그림 위라 유언 줄이 묻힘) + 이름 `opacity:.5`(`.ycrow.locked .ynm-t`) | 목록 행 = `--press-l` 유지(탭 = 인물 카드) | 없음(별도 픽토 0 — 표기는 색·정보만) | 목록 행 전체 · 카드 = 육각 위 · 팝오버 = 생각 아래 | — | 색 의존 0(정보는 텍스트로 병기) · 죽은 행 탭 = 챗 대신 인물 카드(`yOpenThread` 가드) | index `yDeadInfo`(3상태: 죽음→부활 대기→귀환 · `yRevived`)·`yWDur`(무음동 환산 = `window.YWORLD_RATE` SSOT 참조)·`yListRender`·`yShowDetail`·`yProfPop`(운영자 260725 "죽으면 프로필 채색이 빠지면서 · 몇시에 죽었고 몇시간 부활 남았는지") |
| 근처 인물 스택(yeta) | `#yetaNear`(`.ystk`+`.sa`) | 아바타 26 · 겹침 −14 | 밑판 갭 테 2px(`.yprof-cam` 갭 결 = `--bg` 계보) · 도트 없음(선택값) · 미만석 = 근처 ≤2 / 만석 = 방 멤버 | `--press-m` | — (`yAva` 재사용) | `.yh-r` 첫 자리(수화기 앞 고정) | — | aria-label = 근처 N명/멤버 관리 · 탭 = 초대 팝업 | index `yStackSync`(`_yNear` = `yNearCalc` 확장 · 시그니처 가드) |
| 초대 2차 팝업(yeta·개정) | `#yetaInv`(`.yeta-pick.yinv` — 260709 폐지 → 260712 스택 진입점으로 재생) | 필 pad 4/12 | 우상단 앵커(top 62+safe · 스택 정합) · 후보 = **근처 인물만**(`_yNear` · 장소 라벨) · [초대] 필 = `.yme-tag` 결 · armed 2탭 = 수화기 결 · 만석 = 멤버 [보내기](기존) · 힌트 = `.yinv-note`(`--fs-meta`) | `--press-l`(행) | — | 챗 헤더 아래 우측 | — | 정원·거절 힌트 상시 · 바깥 탭 = 닫힘 | index `yInvToggle`(재작성) · op invite/kick 재사용(서버 무접촉) |
| 전화 락 픽토(yeta) | `#yetaCallBtn .ycall-lock` | svg 22(2배 · 운영자 260709 재검수 "작아짐") | 수화기=`--mut`(미배선 `.nophone`) · **락=`--think` 터콰이즈 · 몸통 채움(fill) = 불투명**(운영자 260709 — 옛 동색 저투명·선만 있던 몸통에 수화기 비침 = '열쇠 꽂힌' 오독) · 그림자=drop-shadow(토큰 없는 축) | — | 자물쇠 SVG(`#ymAccess`(PIN 자물쇠) path 정확 계승 — `YLOCK_SVG`(성향잠금 변형 · rect 4,11,16×9)와 별개 | 수화기 정중앙 오버레이(inset:0 grid) | — | 미배선 캐릭터만 표시 · 방 전환 동기=`YCALL.sync`(yApply 훅) | viewer/call.js `initCallBtn`·`callBtnSync` |
| 로딩·진행 표기(전 지점 공통) | `.nm-load`+`.nm-orb[data-orb]` = `nmLoader(type,label[,opts])`(viewer 인라인 · nomute-editor `viewer/nm-loader.js` §orb 이관) | orb 22·gap 9·fs 13.5 = 원본 확정 기하(raw 예외 · 좁은 버튼 = size 18·fs 12.5) | — | — | orb SVG 3형 — **loading**(데이터 불러오는 중)=통통 도트3 / **thinking**(요약·분석 판단)·**prompting**(설계)=소용돌이 링 / **solving**(변환·렌더·산출)=흩뿌린 입자 · `.nm-shim` 빛 스윕 라벨 | `yLoadingBox()` 경유 = 설정·목록·그리드·챗 콜드 4지점(부트 `YETABUS` 스크램블 = 별개 정체성 축 잔존 · 타이핑 버블 점3 = 별개 정본) | — | 호스트 `role="status"` + 가시 라벨 · reduced-motion 가드 내장 | 운영자 260723 원본 승인(nomute Q459~461) → 260726 YETA 이관 지시 — 입자 `--fg`·shimmer `--mut`·커브 `--ease` 계승(신규 토큰·hex 0) · 4분할 중앙선 정렬(orb 중심=글자 중심) 실측 도입 · raw 점 로더 신설 금지(원본 하드락 정신 계승) |
| 감정 대사 글자 등장(yeta) | `.yb.ai.yemo`(잔잔 warm/blue/shy)+`.yemo-hi`(격정 tense/mad/joy/love) · 글자 유닛 = `.ye`(인라인 `--i` 스태거 번호 · 라틴/숫자 연속 = 단어 1유닛) · 생성 = `yEmoWrap`(게이트 = `yAiMsg` fresh+`t.mood`+600자 캡) | 키프레임 `yEmoIn`(from opacity0+blur+translateY) | 커브 = `--ease` · 흐림 = `--blur-l` 사다리(hi=1x · 잔잔=.45x) · 오프셋 = em 기하(raw 예외) · 스태거 = 14/22ms(장문 총지연 ≤1.1s 자동 압축) | — (비인터랙티브) | — (텍스트 자체) | 챗 신규 답장 버블 안(히스토리·재렌더·draft 스트리밍·겹침 ov = 무적용) | — | reduce 2중 가드(`prefers-reduced-motion` `animation:none` + `html.yeta-reduce` 전역 크러시) · 레이아웃 Δ0 실측(같은 텍스트 plain 대비 W/H 0px) | index `yAiMsg`·`yEmoWrap`(Q.80 · 레퍼런스 = beui.dev text-animation TextReveal — 스크램블 로더 차용 선례 축) |

⚠ = 현 드리프트(후속 교정). 별칭 셀렉터(`.ed-x` 등)는 `.tool-x` 스펙을 *복제*가 아니라 *계승*해야 함 — 단일화 후속.

## 적용 절차 (새 컴포넌트·이식)
1. 이 표에서 해당 행 찾기 → **정본 셀렉터를 그대로 계승**(클래스 추가만, 재설계 금지).
2. 표에 없는 새 컴포넌트면 → 위 표기 규칙대로 만들고 **이 표에 행 추가**(인덱스 갱신 = 등재).
3. 4뷰어(index/thumb/ly/k) 공통이면 같은 패턴으로 미러(SVG 상수·토큰).
4. `python3 shared/check_refs.py` 통과 확인(raw 값 baseline·거울 정합).

## 강제·후속 로드맵
- ✅ `check_design` 5파일 커버(ly/k/comp 편입·260628) · ✅ ly/k 토큰화 · ✅ 도구앱 focus-visible.
- ✅ **이모지→SVG 전면**(4뷰어 UI 이모지 픽토그램화 · PR #1119·1120·1121·1125 · 핀·블릿·💡 외 전부 · 운영자 "전부 무조건").
- ✅ **P1 = `nm-svg.js` 아이콘 SSOT**(260628) — 공유 아이콘 10종(CHECK·COPY·PASTE·ERASE·UNDO·WAIT·ERR·OK·DOWNLOAD·WARN) 단일정본·4뷰어 `<script src="nm-svg.js">` 로드(`cscroll.js` 패턴)·인라인 복제 제거. 발산본 통일=정본(DOWNLOAD=14px[thumb CSS 12px 재지정 무관]·WARN=index/ly/k 다수본). **하드 게이트** = `check_refs.check_icon_ssot()`(인라인 재선언=섀도잉·미로드=ReferenceError 차단 → "하나 바꾸면 다 바뀜" 보장).
- ✅ **:active 눌림 효과 토큰화**(260628 · PR #1133·1135 · 분신술 10인) — 14개 임의 scale → 4뷰어 `:root` 5토큰 사다리(`--press-pico/xs/s/m/l` = `.55/.82/.85/.9/.95`, 기존 우세값 스냅·델타 ≤.03) + `scale(var(--press-*,fallback))` + reduced-motion 무효화 블록(`--press-*:1`). **픽토온리 = `:active svg{scale}`로 강제**(배경 plate 안 따라움직임 — `.vh-fbtn`·`.jvar-dl` 버그픽스). **제외=셀렉터 화이트리스트**(값 기반 금지 — `.mergebox(.95)`·`.rev-fab(.9)` 충돌): 게이지(`.go/.mkbtn/.ed-go/.edattach/.sc-pick/.unmerge-go/.hist-clr`)·보라(`.mergebox/.mb-x/#histRemote/.rev-fab`)·카드(`.card/.abadd .99`). 색플래시(복사 형광) 보존. ⚠️ 4뷰어 `:root` 독립(상속 0) → 토큰 4곳 각각·index만 거울 대상. ⏳ 잔여 = 이모지 SVG도 nm-svg 편입 검토 · `check_press_tokens` 게이트(raw scale 재등장 차단).
- ⏳ P2 = `tokens.css` 공유 `<link>` + `build_design_mirror` 확장(thumb/ly/k에 `--r/sp/blur/btn/z` 주입).
- ⏳ P3 = `구성도/00_컴포넌트_인덱스.html` 시각본 + (선택) `build_components_index.py`로 표↔코드 diff 하드게이트(`build_library` 패턴).
- ⏳ 잔여 = 닫기 X 13클래스 단일화 · 토스트 토큰화+`role=alert` · z충돌(`.totop`/`.nm-top`) · radius/gap 토큰화.

> 상세 감사·전후·우선순위 = `docs/reports/260628_UIUX_기틀_분신술10인.html`.

## 🎨 브랜드 팔레트 v2 (260704 · 운영자 그린 전환)
> yeta 색 정체성 = **다크·그린·글래스**로 통일(옛 네온보라·라이트 코발트 폐지). 값 SSOT = viewer :root(색은 base.css 거울·구조 blur는 tokens.css 거울).
- **강조 2색만**: `--accent:#CFFF40`(브랜드 라임 · 1차 · 버튼·링·PIN·챗버블·픽토 액티브) · `--cobalt:#3D6BFF`(2차 · 링크/정보/대비 · `::selection`). 그 외 = 흰(`--fg`)·스포티파이 블랙(`--bg:#121212`).
- **글래스모피즘**: `--glass`/`--glass-2`(프로스티드 표면) · `--glass-line`(엣지 하이라이트) · `--blur-m:16`/`--blur-l:24`(구조토큰). 계승 적용처 = PIN 패드(`.lk-pad`)·하단 네비(`.ynav`)·히어로 카드(`.ym-access-card`).
- **그림자·스크림**(절대1: raw 금지→토큰): `--shadow-card`(글래스 카드·히어로 포스터 그림자) · `--bg-scrim`(step1 포스터 전면 하단 딤·가독).
- **하단 네비 = 플로팅 알약**(운영자 260705 레퍼런스 전환): 중앙 알약 `--r-pill`+`--glass`(고투명)+`--blur-l`+글래스 엣지 · **전부 무채** — 활성 `--fg`(흰)·비활성 `--mut`(옛 라임 액티브 폐지) · 챗 입력행·헤더 픽토(전송 제외)도 무채 통일. 강조 라임 = CTA(전송·PIN·내 버블)에만.
- 폐지: `--bubble-me` 보라→`var(--accent)` · `--accent-1/2`(오렌지/코발트)·`--m-*`(라이트)·`--sky`·`--cream` 제거 · 메인 화면 라이트→다크.
- 강제: `check_refs.check_design()` accent 패턴 = `rgba(207,255,64)`/`#cfff40`·baseline 0(새 브랜드 raw = var() 강제). 미러 = base.css/tokens.css(`build_design_mirror.py build`).
- **절대명령#1 기계강제(260704 · 15인 교차검증 v2)**: `design-tokens.lock`(승인 원장) — `:root` 새 토큰이 락 미등재면 `check_refs.check_token_lock()` rc=1(커밋 차단·승인=락 등재). 계승 3티어(정확→근접 자동→갱신) · 갱신 2티어(값=승인+check / 구조 토큰 신설=기틀=교차검증) · raw 예외축(기하·안무·광학·OS·로컬z·box-shadow). 포터블 이식판 = `docs/절대명령1_포터블.md`.
- **팔레트 확립본(시각 정본·260704)** = `docs/브랜드_팔레트_확립본.html` — 강조 2(라임 `--accent`+코발트 `--cobalt`)·의미 `--danger`/`--arm`·중립 흰/스포티파이블랙·글래스. **이 팔레트 밖 색 금지.** 챗창 `--yeta-bg`=`--bg`(스포티파이 블랙) 통일 + 글래스 엣지. nomute 전용(편향·수집함·서브코어 accent-3~5·이력 보라) 제거.
- **직업·변신 배지 `.yjob`/`.yjob.tq`**(운영자 260707 "이름 옆 직업/변신모드"): 플레이트 = `.yprem` 정본 계승(패딩 1px 8px·`--r-pill`·12% bg·40% border) · 메인 직업 = `--accent` 라임 강조 · 변신 모드 = **`--think` 터콰이즈 공용**(#3EE0C8 — 생각줄 토큰과 단일 톤 · 변신 직업군 배지) · 데이터 = roster `job`(전원)/`mode{nm,t[]}`(시간대 게이트 · KST ymSlot) · 훅 = `yJobBadge(c)` 이름 직후(등급·보이스 앞).

## 📐 타이포 위계 6단 (운영자 260709 "타이틀·부제·대주제·중주제·소주제·내용 — 위계가 없어 흔들린다" · 기틀 확정)
> 화면·시트의 텍스트 위계 = 아래 6단이 정본. **새 화면·그룹·행은 이 표의 단에 스냅**(임의 크기·색 조합 창작 금지 — 전부 기존 `:root` 토큰 = 신규 토큰 0). 정본 실물 = 설정 시트(Setting).

| 단 | 용도 | 타이포 | 색 | 정본 셀렉터(계승원) |
|---|---|---|---|---|
| 1 타이틀 | 화면·시트 제목(픽토 동반 가능) | `--fs-h2` + `--fw-x` | `--fg` | `.ycd-nm`(`.yset-ttl` = 픽토+텍스트 변형) |
| 2 부제 | 타이틀 바로 밑 캡션 — **꼭 필요할 때만**(기본 = 생략) | `--fs-xs` | `--mut` | `.ycd-tag` |
| 3 대주제 | 섹션 라벨(글꼴·알림·대화·정보…) | `--fs-label` + `--fw-b` | `--fg` | `.yset-sec` |
| 4 중주제 | 그룹 박스(글래스) 헤더 — L0/L1/L2 설정 | `--fs-sm` + `--fw-x` | `--fg` | `details.yset-grp summary` |
| 5 소주제 | 그룹 안 서브헤더·행 라벨 | `--fs-xs`(gh)·`--fs-sm`(행) + `--fw-b` | `--fg-2`(gh)·`--fg`(행) | `.ypol-gh b` · `.yset-nm`·`.si-t` |
| 6 내용 | 항목 본문·설명·값 | `--fs-xs`~`--fs-sm` | `--mut`(판명·활성 = `--fg`) | `.ypol-lock`·`.si-d`·`.yterm-b` |

- 그룹 구분 = **글래스 박스**(`details.yset-grp` = `--glass`+`--blur-m`+`--r-m`) · 그룹 내부 서브구획 = `.ypol-grp`(상단 `--glass-line` 라인) + `.ypol-gh`(5단) — **L0·L1·L2 전 계층 동일 골격**(운영자 260709 "박스로 구분 · 안의 위계는 동일").
- 세부 설명(옛 시트 부제·하단 노트·축 힌트) = **폐지가 디폴트**(운영자 260709) — 정말 필요한 안내만 6단(내용)으로.
