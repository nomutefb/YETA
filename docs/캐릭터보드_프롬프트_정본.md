# 캐릭터 보드 프롬프트 정본 — 「감정 + 배경」 한 장 시트

> **뭐냐**: 캐릭터 한 명을 **한 장의 시트 이미지**로 뽑는 GPT(gpt-image) 프롬프트 레시피. 칸마다 *감정 하나 + 배경 하나*.
> **왜 정본이냐**: 감정 축·배경 축을 임의로 짓지 않고 레포 실측 SSOT를 계승한다 —
> · 감정 9종 = `shared/build_season_media.py` `EMOS`(뷰어 `Y_MOODS` · 러너 `<<MOOD>>` 화이트리스트와 짝인 **불변 계약**)
> · 배경 = `apps/yeta/places.json` `places` 실존 노드(신규 노드 0)
> · 그림 결 = `.github/scripts/yeta_char_art.py` `AV_BASE`(납작한 애니 셀 · FX 0) 어휘 계승
> **산출을 어디 쓰나**: 칸을 잘라 `viewer/characters/season/<id>/main/<감정>/`에 붓고 `python3 shared/build_season_media.py` → `media.json` 재생성(손편집 금지).

---

## 0. 쓰는 순서 (운영자 3스텝)

1. **ChatGPT에 레퍼런스 이미지를 첨부**한다(캐릭터 원본 1장). ← 이게 「똑같이 나오게」의 8할이다. 글만으로는 얼굴이 표류한다.
2. 아래 §2 **시트 프롬프트**를 통째로 붙여넣는다(§1 캐릭터 락이 안에 이미 들어 있다).
3. 마음에 안 드는 칸만 §4 **리롤 지시문**으로 다시 뽑는다. 시트 전체를 다시 굴리면 다른 얼굴이 나온다 — **칸 단위로만 고친다.**

⚠ 실무 갓차 4개
- **라벨은 영문 대문자로만** 넣는다. 한글 라벨은 gpt-image가 자모를 깨뜨린다(그래서 라벨 = `BASE`/`JOY`… = 감정 폴더명과 동일 = 잘라 넣을 때 그대로 분류된다).
- **칸이 많을수록 얼굴 디테일이 떨어진다.** 20칸은 "보드용", 앱에 실제로 꽂을 그림은 9칸 시트나 §3 개별 프롬프트로 뽑는 게 낫다.
- **의상·머리는 절대 안 바뀐다**고 매 프롬프트에 못 박는다. 감정만 바꾸라고 안 하면 모델이 계절옷·헤어스타일을 창작한다.
- 시트를 받으면 **1번 칸을 기준으로 나머지를 눈으로 대조**한다. 어긋난 칸 = 리롤 대상.

---

## 1. 캐릭터 락 (identity anchor) — 흑발 교복 여고생

레퍼런스 실측 기술(260801). **모든 칸에서 이 항목은 한 글자도 안 바뀐다.**

<!-- BOARD:LOCK — 아래 코드펜스 = 기계 파싱 대상(.github/scripts/yeta_char_board.py). 앵커 삭제 금지 -->
```
CHARACTER LOCK — every trait below stays IDENTICAL in every panel:
- One single original fictional character: a young woman, late teens, a Korean high-school student.
- Hair: jet black, dead straight, glossy, extremely long — falling well past the waist; a full BLUNT straight fringe
  cut level just above the eyes, completely hiding the eyebrows; long straight side locks framing both cheeks,
  reaching below the chest; a few loose strands catching the light.
- Face: pale porcelain skin, softly rounded jaw, small nose, soft pink airbrushed blush across the cheeks and the
  bridge of the nose; large almond eyes with pure black irises and a thin crisp black lash line; NO visible eyebrows
  (the fringe covers them); no beauty marks, no glasses, no earrings, no hair clips.
- Build: slim waist, full chest, teenage proportions. Fully clothed and tasteful in every panel.
- ATTITUDE — her default demeanour, and it is locked exactly like the hair and the uniform are:
  cold, haughty, unimpressed. Chin slightly raised, eyelids lowered so she regards the viewer DOWN along her nose,
  a flat unbothered stare. The mouth stays small and level — she does not smile with her eyes. Posture composed,
  still, shoulders squared; she never leans toward the viewer and never makes herself small.
  She is a proud, sharp-tongued girl who finds the viewer slightly beneath her attention. Every warm feeling is
  grudging — something she is CAUGHT showing, never something she offers. Even at her happiest she looks like she
  is winning an argument, not like she is being nice.
  NOT sweet, NOT gentle, NOT a girl-next-door; NO wide innocent doe eyes, NO puffed cheeks, NO shy clasped hands,
  NO cutesy head tilt, NO moe mannerisms, NO eager open-mouthed cheer.
- Outfit (NEVER changes): a crisp white long-sleeve school dress shirt buttoned to the very top with a pointed white
  collar; a wide CRIMSON / dark-red necktie hanging long down the centre of the chest; a high-waisted BLACK pleated
  skirt. No blazer, no cardigan, no jacket, no bag, no jewellery.
- Art style: clean flat modern anime key-visual — crisp dark line art, soft cel shading with gentle gradients,
  matte muted palette, warm cream ambient light, light bloom. NOT semi-realistic, NOT photoreal, NOT glossy 3D,
  NO heavy painterly rendering, NO sketch lines, NO chromatic aberration.
- Colour anchor: jet black + white + crimson red on a warm beige / cream environment.
```

> 「느낌」의 정체 = **검정·흰색·crimson 3색 + 따뜻한 크림빛 환경광 + 납작한 셀 음영**. 이 세 개가 유지되면 배경이 바뀌어도 같은 애로 읽힌다.
>
> ⚠ **태도(ATTITUDE)도 외모와 같은 급의 락이다**(260801 run#1 반성). 첫 판은 얼굴·의상·화풍만 락하고 태도를 안 박았더니
> 9칸 전부 「상냥한 애니 여고생」으로 나왔다 — 모델은 태도를 비워두면 디폴트로 **순하게** 채운다. 원본의 도도한 결이
> 「같은 애」의 8할이라, 턱 각도·눈꺼풀 높이·입꼬리를 락에 명시하고 각 감정을 그 태도의 **변주**로 적는다
> (기쁨 = 활짝 웃음 ❌ / 이겼다는 의기양양 ⭕ · 삐짐 = 볼 부풀리기 ❌ / 팔짱 끼고 무시 ⭕).

---

## 2. 시트 프롬프트

### 2-A. 시트 A — 감정 9칸(3×3) · **앱 정합판** ★권장
칸 = `EMOS` 9종 그대로. 잘라서 `<감정>/` 폴더에 1:1로 꽂힌다.

<!-- BOARD:SHEET_A — 기계 파싱 대상. `{LOCK}` 자리에 위 BOARD:LOCK 블록이 그대로 치환된다 -->
```
Using the attached image as the ABSOLUTE character reference, produce ONE single image:
a character emotion sheet (model sheet) page.

{LOCK}

SHEET LAYOUT
- One page, portrait 3:4, on a warm off-white paper background.
- A clean 3 columns x 3 rows grid of 9 equal portrait panels (each panel 3:4), thin hairline dividers,
  a small even gutter between panels, a generous outer margin.
- Every panel is a WAIST-UP shot of the SAME character, eye level, facing the viewer unless noted.
- In the top-left corner of each panel, one small plain uppercase Latin label, nothing else.
- NO other text anywhere. No watermark, no logo, no signature, no speech bubbles, no arrows, no page numbers.

PANELS (left to right, top to bottom)
Read every expression through the ATTITUDE lock above — these are the expressions of a cold, haughty girl,
not of a sweet one. The emotion shows in a small movement of the eyes and the mouth corners; the chin stays up.

1. "BASE"  — flat and unimpressed, chin raised, eyelids lowered, looking down her nose at the viewer, mouth a level line. BG: a school corridor by tall windows, midday light.
2. "WARM"  — the faintest grudging curl at ONE corner of the mouth, eyes still cool and half-lidded; she is not going to admit she is pleased. BG: a traditional teahouse window seat, lantern glow, steam rising from a cup.
3. "JOY"   — a sharp triumphant smirk with a flash of teeth, one brow-line raised, chin high, thoroughly pleased with herself — the look of having won. NOT a warm friendly grin, NOT leaning in. BG: outside a 24h convenience store at night, lime neon sign.
4. "LOVE"  — caught off guard: a deep blush she is clearly angry about, eyes darting away from the viewer, mouth tight, chin still up as if daring anyone to comment. BG: a back-alley crossroads at night, streetlamp and a bench.
5. "SHY"   — flustered and irritated at being flustered: a hard glare away to the side, ears and cheeks red, one hand yanking the necktie straight. NO fidgeting, NO timid hunching. BG: a neighbourhood playground at dusk, swings behind her.
6. "BLUE"  — hollow and guarded, eyes lowered to nothing, face carefully blank — sad but refusing to show it, mouth pressed flat rather than turned down. BG: an empty classroom by a rain-streaked window, grey afternoon.
7. "TENSE" — narrowed wary eyes fixed on something off-frame, jaw tight, shoulders squared and still; alert and dangerous, NOT frightened, NOT wide-eyed. BG: a fenced-off abandoned redevelopment lot at night, barricade tape.
8. "MAD"   — cold contempt rather than a tantrum: a withering half-lidded stare straight down the nose, chin lifted, one corner of the mouth pulled back in disdain. BG: the courtyard of a wooden kendo dojo at dusk.
9. "SULKY" — arms crossed, body squared to the viewer but face turned away in dismissal, eyes cut back sideways in a cold sidelong glare. NO puffed cheeks, NO pout — she is ignoring you, not sulking cutely. BG: a traditional market alley in daylight, coloured awnings.

CONSISTENCY IS THE #1 REQUIREMENT
Panel 1 is the master. In all 9 panels keep the SAME face, the same hair length and fringe shape, the same uniform,
the same necktie colour, the same line weight, the same shading and the same palette. Only the EXPRESSION and the
BACKGROUND change. The ATTITUDE never changes either — if a panel reads as a sweet, soft or cutesy girl, it is
wrong; redraw it cold and haughty. Backgrounds stay soft and out of focus so the character reads first. If a panel drifts from
panel 1, redraw it to match.
```

### 2-B. 시트 B — 감정+배경 20칸(5×4) · **최대 밀도판**
1~9번은 시트 A와 동일(= `EMOS` 커버), 10~20번은 연기 폭 확장.

<!-- BOARD:SHEET_B — 기계 파싱 대상. `{LOCK}` 자리에 위 BOARD:LOCK 블록이 그대로 치환된다 -->
```
Using the attached image as the ABSOLUTE character reference, produce ONE single image:
a character emotion sheet (model sheet) page.

{LOCK}

SHEET LAYOUT
- One page, square 1:1, on a warm off-white paper background.
- A clean 5 columns x 4 rows grid of 20 equal portrait panels (each panel 3:4), thin hairline dividers,
  a small even gutter, a generous outer margin.
- Every panel is a CHEST-UP shot of the SAME character (tighter than a waist-up so the face still reads at this size).
- One small plain uppercase Latin label in the top-left corner of each panel. NO other text anywhere.

PANELS (left to right, top to bottom)
Read every expression through the ATTITUDE lock above — a cold, haughty girl, never a sweet one.

1.  "BASE"       — flat and unimpressed, chin up, looking down her nose.     BG: school corridor by tall windows, midday.
2.  "WARM"       — one grudging corner of the mouth, eyes still cool.        BG: teahouse window seat, lantern glow, steam.
3.  "JOY"        — sharp triumphant smirk, chin high, pleased with herself.  BG: outside a 24h convenience store at night, lime neon.
4.  "LOVE"       — caught out: angry blush, eyes darting away, mouth tight.  BG: alley crossroads at night, streetlamp and bench.
5.  "SHY"        — flustered and annoyed by it, hard glare to the side.      BG: playground swings at dusk.
6.  "BLUE"       — hollow guarded blank, eyes lowered, refusing to show it.  BG: empty classroom, rain-streaked window, grey afternoon.
7.  "TENSE"      — narrowed wary eyes off-frame, jaw tight, alert not scared. BG: fenced-off abandoned lot at night, barricade tape.
8.  "MAD"        — cold withering contempt down the nose, chin lifted.       BG: wooden kendo dojo courtyard at dusk.
9.  "SULKY"      — arms crossed, face dismissively away, cold sidelong cut.  BG: market alley in daylight, coloured awnings.
10. "SMUG"       — a slow one-sided smirk, eyes half-lidded, openly taunting. BG: neon-lit basement stairwell down to a practice room.
11. "LAUGH"      — a scornful laugh, head turned, one hand covering the mouth, eyes mocking. BG: the alley square at night, bench and streetlamp.
12. "SURPRISE"   — a brief slip: eyes widened for one instant, mouth caught open, already recovering her composure. BG: the corridor of a 3rd-floor newsroom at night.
13. "SLEEPY"     — heavy half-lidded eyes, visibly bored of being awake, one hand loose. NO cute yawn. BG: a small radio station stairwell before dawn.
14. "SERIOUS"    — level unblinking stare straight at the viewer, mouth a hard line. BG: a school rooftop under an overcast sky.
15. "CRY"        — furious tears she refuses to release: eyes glassy, jaw clenched, glaring through them. BG: stone chapel steps at night, candlelight.
16. "RELIEF"     — shoulders dropping a fraction, a short exhale through the nose, expression still guarded. BG: a boxing gym by a high window, blue 5am light.
17. "BORED"      — utterly flat half-lidded look, chin propped on one hand, eyes elsewhere. BG: the window counter of a convenience store, daylight.
18. "WORRY"      — a tight troubled frown, eyes cut sideways, arms folded — concern she would deny. BG: a market alley at sunset.
19. "DETERMINED" — a cold resolved stare, chin level, eyes hard and fixed forward. BG: the wooden floor of a kendo dojo in moonlight.
20. "PROFILE"    — indifferent side profile, long hair lifting in the wind, not acknowledging the viewer. BG: the playground at night under a streetlamp.

CONSISTENCY IS THE #1 REQUIREMENT
Panel 1 is the master. In all 20 panels keep the SAME face, the same hair length and fringe shape, the same uniform,
the same necktie colour, the same line weight, the same shading and the same palette. Only the EXPRESSION and the
BACKGROUND change. The ATTITUDE never changes either — a panel that reads sweet, soft or cutesy is wrong. Backgrounds stay soft and out of focus so the character reads first. If a panel drifts from
panel 1, redraw it to match.
```

> 배경 = `places.json` 노드 매핑: 1·6·14 `school` · 2 `tea` · 3·17 `store` · 4·11 `alley` · 5·20 `park` · 7 `offlimits` · 8·19 `dojo` · 9·18 `market` · 10 `studio` · 12 `office` · 13 `radio` · 15 `cathedral` · 16 `gym`.

---

## 3. 개별 칸 프롬프트 (앱에 실제로 꽂을 그림용)

시트에서 자른 그림은 해상도가 낮다. 앱 스테이지(`yStage`)에 쓸 장은 **한 칸씩** 뽑는다.

<!-- BOARD:CELL — 기계 파싱 대상. `{LOCK}`·`{EXPRESSION}`·`{BACKGROUND}` 3자리가 치환된다 -->
```
Using the attached image as the ABSOLUTE character reference, produce ONE single vertical 9:16 illustration.

{LOCK}

SHOT
- Waist-up, eye level, the character placed slightly off-centre so the background reads.
- EXPRESSION: {EXPRESSION}.
- BACKGROUND: {BACKGROUND} — soft, out of focus, low contrast, so the character reads first.
- Full-bleed to the frame edges. No letterbox, no border, no frame.
- No text, no watermark, no logo, no signature, no UI, no other characters.
```

- 얼빡 프사가 필요하면 `SHOT` 절을 `.github/scripts/yeta_char_art.py` `AV_BASE`(1:1 극단 클로즈업 정본)로 갈아 끼운다 — 여기서 새로 짓지 않는다.
- 배경만 필요하면 인물 없이 `.github/scripts/yeta_bg.py` `BASE`(9:16 무인 배경 정본)를 쓴다.

---

## 4. 리롤 지시문 (칸 단위 교정)

같은 대화창에서 이어 붙인다(레퍼런스와 앞선 시트가 문맥에 남아 있어야 얼굴이 안 튄다).

| 증상 | 붙일 문구 |
|---|---|
| 한 칸만 얼굴이 다름 | `Redraw ONLY panel <행>-<열> ("<LABEL>"). Match panel 1 exactly — same face, same fringe, same hair length, same uniform, same line weight. Keep every other panel untouched.` |
| 옷이 바뀜 | `The uniform changed in panels <n>. Restore it: white shirt buttoned to the top, wide crimson necktie, high-waisted black pleated skirt. Nothing else.` |
| 앞머리가 갈라짐 / 눈썹이 보임 | `The fringe must stay a full BLUNT straight fringe that completely hides the eyebrows in every panel. No parted bangs, no visible eyebrows.` |
| 그림체가 사실적으로 감 | `Flatten the rendering back to clean flat anime cel shading with crisp dark line art. No semi-realistic skin, no glossy highlights, no photoreal texture.` |
| 라벨 글자가 깨짐 | `Rewrite every panel label in clean plain uppercase Latin letters only, exactly as listed. If a label cannot be rendered cleanly, leave that panel unlabelled rather than garbled.` |
| 배경이 인물을 잡아먹음 | `Push every background further out of focus and lower its contrast. The character must be the brightest, sharpest thing in each panel.` |
| **너무 착하게 나옴**(제일 잦음) | `She is reading far too sweet and gentle. Restore the locked ATTITUDE in every panel: chin raised, eyelids lowered, looking down her nose, mouth small and level, composed and unbothered. No soft warm smiles, no wide innocent eyes, no puffed cheeks, no head tilt. Warmth is grudging and reluctant; joy is a smug triumphant smirk, not a friendly grin.` |

---

## 5. 뽑고 나서 — 앱 배선

1. 칸을 잘라 감정별로 나눈다(파일명 자유 · 라벨이 곧 폴더명).
2. `viewer/characters/season/<캐릭터id>/main/<감정>/`에 붓는다 — 감정 = `base warm joy love shy blue tense mad sulky` 9종만.
3. `python3 shared/build_season_media.py` → `media.json` 재생성. **`media.json`은 손으로 안 고친다**(기계 산출물 · `check_refs` 게이트).
4. 얼빡(프사)은 파일명에 `face`를 넣어 배경 풀에서 뺀다(`check_refs` 얼빡 배경 게이트).
5. 로스터 등재가 필요하면 `apps/yeta/characters/roster.json` — 그건 캐릭터 편입 작업이라 별건이다.

---

## 5-2. 손으로 안 붙이고 기계로 뽑기 (dispatch)

ChatGPT 창에 붙여넣는 대신 레포 파이프로 뽑을 수 있다 — 키는 이미 배선돼 있다(`secrets.OPENAI_API_KEY` / `OPENAI_API_KEY_nomute` · `yeta-char-art` dispatch 성공 이력 7건).

| 조각 | 경로 | 역할 |
|---|---|---|
| 워크플로 | `.github/workflows/yeta-char-board.yml` | **수동 dispatch 전용**(⚠ 유료 종량제 · 자동 트리거 금지) — 입력 `sheet`(A/B) · `ref`(레퍼런스 경로) · `slug` · `takes`(≤4) · `force` |
| 스크립트 | `.github/scripts/yeta_char_board.py` | 이 문서의 `<!-- BOARD:* -->` 앵커 코드펜스를 **읽어 조립**만 한다(문구 2벌 금지 = 드리프트) |
| 산출 | `viewer/assets/yeta_char/board/<slug>_sheet<A\|B>[_v<n>].png`(+webp) | main에 직접 커밋 = 칸 자르기·편집 베이스 |

- **`ref`를 넣으면 `/v1/images/edits`**(첨부 이미지 = 동일성 앵커) · 비우면 `/v1/images/generations`(글 락만 = 「비슷」이지 「똑같이」가 아니다). 레퍼런스는 `_소재/`에 올려두고 그 경로를 넣는다.
- `{LOCK}`·`{EXPRESSION}`·`{BACKGROUND}` 자리표시자와 `<!-- BOARD:LOCK -->`·`SHEET_A`·`SHEET_B`·`CELL` 앵커는 **기계 파싱 계약**이다 — 지우면 dispatch가 죽는다.
- 워크플로는 **main에 있어야** Actions 화면에 dispatch 버튼이 뜬다(브랜치에만 있으면 안 보인다).
- 프롬프트를 고쳐서 시험할 땐 **그 브랜치를 ref로 dispatch**하면 된다 — 산출물도 그 브랜치로 커밋돼 PR 하나에 「고친 문구 + 그 결과」가 같이 담긴다.
  (260801 사고: 커밋 스텝이 `origin main`을 하드코딩하고 있어 브랜치 런이 rebase 충돌 → push 5회 전패 →
  **이미 생성·과금된 시트가 통째로 소실**. 지금은 `GITHUB_REF_NAME` 기준으로 고쳤고, 그래도 실패하면
  `board-sheets` 아티팩트로 남는다 = 과금분은 반드시 건진다.)

## 6. 이 문서로 다른 캐릭터 보드를 뽑을 때

§1만 그 캐릭터 실측으로 갈아 끼우면 §2~§5는 그대로 돈다. 캐릭터 락을 쓸 때 반드시 채울 9칸:
**머리(색·길이·앞머리 형태) · 눈(모양·색) · 피부·홍조 · 체형 · 의상 전벌(불변 선언 포함) · 그림체 · 색 앵커 3색 · 금지 목록(액세서리·안경 등) · 태도(턱 각도·눈꺼풀·입꼬리 + 금지 결)**.
빠뜨린 칸은 모델이 매 칸 새로 창작한다 = 「같은 애」가 깨지는 지점이 정확히 거기다. 특히 **태도를 비우면 무조건 순한 애로 나온다**(260801 실측).
그리고 태도를 바꿨으면 §2 패널 표정 줄도 같이 고쳐야 한다 — 락은 도도한데 칸 지시문이 `soft smile`이면 지시문이 이긴다.
