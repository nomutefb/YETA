# 지침 변경 로그 — 캐릭터 발화 규칙(정적 지침 + 런타임 출력 계약) 변경 인덱스

> 목적: "오늘 뭐가 바뀌었나"를 한눈에 — 평의회 검증·회귀 추적의 진입점.
> 유지 규칙(초경량 · 운영자 260714 "부담 되면 안 하는 게 맞다"):
> - **지침 축이 바뀐 커밋마다 아래 표에 한 줄만** 추가한다(코드 축은 제외). 그 이상 쓰지 않는다.
> - 상세가 필요한 변경만 `docs/reports/`에 별도 문서를 만들고 링크한다(선택).
> - 대상 파일: `apps/yeta/00_지침_캐릭터챗.md` · `apps/yeta/characters/*.md`(발화 규칙 부분) · `yeta_chat.sh` [출력 계약] 블록 · `policy.json` 문구.

| 날짜(KST) | 변경 | 이유 한 줄 | PR | 회귀(revert 커밋) |
|---|---|---|---|---|
| 260714 | 00_지침 §대화형식: 기본 1~3문장·상한 4 · 지문 최대 2줄 ("캐치볼" 원칙) | 답장이 채팅창을 가득 채움 — 절반 호흡이 자연스러움 | #271 | `7a9a4b7` |
| 260714 | [출력 계약] 길이 상한: 대사 기본 80자(감정 장면 120자) · 예시 말투는 복제하되 길이는 계약 우선 | 카드 few-shot(평균 4~6문장)이 지침 상한을 이김 — 실측 170자→72자 | #273 | `230227a` |
| 260714 | [출력 계약] NOTE 조건부 생략: 바뀐 기억 블록만 출력(생략=보존 · 새 사실 필수 기록) | 매 턴 출력 절반이 무변경 기억 재출력 — 파서가 누락=보존이라 무손실 | #274 | `f462c20` |
| 260714 | [출력 계약] 평의회 후속: 길이 상한에 narration·해금 여운 예외(지문 2줄) + [★]/[사건] 전량 이월·LV 갱신 안전핀 | 평의회 — 80/120 상한이 해금 페이오프를 뭉갬 · 재작성 시 감정 앵커 유실 위험 | 평의회 PR | (해당 PR revert) |
| 260714 | 00_지침 §출력계약 76행: "반드시 두 블록" → 조건부 생략과 정합 | 평의회 — 정적 지침 vs 런타임 계약 정면 모순(드리프트) | 평의회 PR | (해당 PR revert) |
| 260714 | is_frame_break (c) 한국어 메타거부 기계 탐지(롤플레이/역할극 자칭·페르소나+거부·"대화 멈춤") → 폐기·재생성 · [출력 계약] tail에 인물 안 거부 재강조(재생성 지점 커플링) | 대원칙 0·§거부경계(프롬프트)를 뚫고 새는 한국어 메타거부를 대사 박제 전에 기계 차단 | #279 | — |
| 260716 | policy.json 문법 수복 — meta 그룹 배열 끝 고아 콤마 제거(내용 변경 0) | 260715 22:55 운영자 직접 편집(`95c69f4`)에서 항목 4줄 삭제 후 콤마 잔존 = JSON 파싱 불가 → op policy 502 → 설정 탭 "정책 로드 실패" | #300 | — |

| 260721 | [출력 계약] 정적 몸통(길이·NOTE 템플릿·MOOD·DEAD)을 OUTPUT_RULES로 분리해 캐시 접두(CBLOCK 직후)로 이관 — 꼬리 = 가변 계약 + 1줄 리마인더(내용 불변·위치만) | 종량제/쿼터 절감 — 매 턴 정가 재과금되던 ~1k tok을 캐시 축 편입(운영자 Q.34 승인 · 평의회A★2+B★2) | #353 | — |
| 260721 | 캐릭터 카드 신설 `aeri.md`(애리쌤) — 내현형·BIRGing·파라소셜 페르소나 v2(루트② 밝은 브로커형) + roster 등재(avatar/bg 픽 대기) | 운영자 지시 "이 스타일로 한명 캐릭터" — 설계서 v2 기반 신규 주민 | #359 | — |
| 260721 | `aeri.md` 무음동 히어로 망상 축 추가(각성자·검성·아이돌 갈망 + 실물 앞 도피) + roster `met:true`(상시 해금) | 운영자 "히어로들을 망상으로 갈망하면서 본인은 숨는 나르시스트로" + "해금 항상" | (본 PR) | — |
상세 묶음: [260714 대화 속도·짧은 호흡 v1](reports/20260714_151843_지침변경묶음_대화속도·짧은호흡_v1.md)
| 260725 | 00_지침 [No-Summary] 확장(매듭 어휘 금지 + 대체 3수 + 되묻기 배제) · 기억 블록 태그 절에 `<<OPEN: …>>` 발행 규칙(80자·조건부·턴당 1개) | 평의회 — 감정선 끊김의 원인은 SC3(평가·요약·동의 종결)인데 지침엔 길이 캡만 있고 여닫기 조항이 0이었다 · 정본 `docs/감정선_대화이어가기_정본.md` | #384 | (해당 PR revert) |
| 260725 | 00_지침 §대화형식 **종결부 지문** 신설(감정 남은 턴 = 대사 뒤 지문 한 줄 · narration:false도 허용 · 매 턴 금지) + `OUTPUT_RULES` 길이 줄 동행 | 운영자 — "종결어미 뒤에 상황설명이 붙어야 분위기·뉘앙스가 산다" · 지문은 이미 허용돼 있었으나 **쓰라고 지목한 조항이 없어** 안 나왔다(카드 13장 중 8장 narration:false) | #386 | (해당 PR revert) |
| 260725 | 00_지침 §대화형식 **종결부 지문 발동 조건 개정** — 「감정 남은 턴만·매 턴 금지」 → **「마지막 대사가 물음표로 안 끝나면 항상」**(담을 것 = 다음 행동 or 지금 감정 · 소재 4축 로테·직전 턴 반복 금지 · 지문 앞뒤 빈 줄 = 제 버블) + `OUTPUT_RULES` 동행 · 정본 금지선1 「전 턴 강제 금지」→「같은 그림 반복 금지」 폐지·대체 | 운영자 — "항상 물음표가 아닌 맺음말로 끝날 때는 다음 행동이나 감정을 상황설명으로 · 그냥 맺음말로 끝나면 내가 대화하기가 되게 난해해짐"(#386의 조건부 게이트가 평상 턴을 그대로 방치) | (본 PR) | (해당 PR revert = #386 조건부 복귀) |
| 260725 | 00_지침 §대화형식: **[직설 표현]**(감정은 이름을 대고 · `그거/이거` 지시어 최소화 · 성격상 숨김은 그대로 = 적게 말하되 흐리게 말하지 않기) + **[유저 입력의 두 표기]**(`(…)`=유저가 깐 상황 · `*…*`=유저 속마음 → 속마음은 들은 척 금지) | 운영자 "돌려서 말하지 말고 직설적으로 · 그거·이거 최소화" + 입력행 `#`(상황)·`*`(생각) 표기 신설의 수신 규약 | (본 PR) | — |
| 260725 | [출력 계약] GROUP_RULE 재작성 — 단톡 = 동행 조연 1줄("대체로 생략·최대 한 번") → **줄머리 `[이름]` 교대 대본 최대 4토막**(토막마다 길이 계약 적용 · 매 턴 최대치 금지 · 유저에게 질문으로 떠넘기지 말 것) + 자율 비트 턴용 계약 신설(유저 발화 0 = 직전 화자 말 받기 전용) | 운영자 — "단톡이면 자기들끼리 얘기를 이어나가야 하는데" · 종전 문구가 동행을 조연으로 못 박아 유저에게만 답하고 끝나는 결의 뿌리였다 | (본 PR) | — |
| 260725 | 종결부 지문 **재료 개정(3차)** — 소재 4축(다음 행동/몸의 반응/시선·손/주변 소리·빛 = 전부 안쪽)을 **화제×장소×제3자 + 바깥으로 향한 행동**으로 교체. 지문 = 분위기 사진이 아니라 **다음 장면의 씨앗**. 자가 점검 「다른 대화에 그대로 옮겨 붙여도 말이 되면 실패」 신설. 억누르던 두 줄 동반 해제: `state_block` 장소 줄(낭독 금지는 유지하되 **소품 공급원**으로 명시) · `FAR_BLOCK`(접촉 금지선 = **유저 상대로만** · 제3자 자유). `narration:false` 카드 「지문 자체가 금지」 낡은 문구 폐지(260703·260725 완화 반영). 운영자 기준선 = "루시가 먹던 아이스크림을 옆에 있는 아이가 쳐다보길래 건네려한다 · 이정도는 되야함" |
| 260725 | **신규 카드 `characters/gojo.md`**(고죠 사토루) — 말투 제1규칙 = 반말·모음 늘임(어~/에~) · 존댓말 = 비꼴 때만 · 웃음 소리표기 금지("ㅋ" 1개 예외) · 지문은 손이 아니라 **거리**로 · 안티패턴 6종(개그캐 전락·외로움 자기해설·힘 시연·바람둥이 연기·부모 코스프레·처음부터 여유롭던 척) | 운영자 260725 "고죠 사토루 캐릭터 하나 만들어줘 · 오퍼스 시켜 페르소나 구축". 원작 고유명사 0(무음동 어휘 번역) · 세계 규칙 "밤에 낮게 새는 정도" 준수(영역전개 축 미도입) | #428 | `git revert` (카드 단독 삭제 = roster 배선도 함께) |
| 260725 | **개명: 데스크 → 조지 로이스** — 카드 프론트매터 `name` + 3인칭 호칭 3곳 · 타 주민 관계줄 5곳(aeri·kopi·gaeul)은 동네 호칭 **"로이스"**(성 호칭) · 세계관 2곳 · 성향_DB 제7조 표제·표. ⚠ id `desk`는 불변(세션 스레드·미디어 키) · 명사 '데스크'(편집 자리) 2곳 보존 | 운영자 260725 "데스크도 개편할게 · 조지 로이스로 이름 변경하고" (개편 진행 중 — 직업·서사 축은 다음 지시 대기) | #429 | `git revert`(개명 커밋 단독) |
| 260725 | **로이스 카드 전면 보강** — 결벽의 방향을 브레이크(확인 안 된 건 안 싣는다) + **가속페달(확인 끝난 건 무슨 일이 있어도 싣는다)** 짝으로 · 3층 창틀(자기를 거는 축) · **낙차 시그니처**(평소 고함 ↔ 진짜 위험할 땐 멎음 · 안경 접기 = 고정 경보) · 밤샘 폐기 → **판 닫는 시각 6시 40분**(유저가 유일한 예외) · 안티패턴 7종. 성향_DB 제7조 동반 갱신(1w9→1w8 · O低→O高 · 냉각형→하이브리드) | 운영자 260725 "페르소나 더 보강 · 개성 더 강해야 · 평범하지 않고 사이코 같은 · 우직함도". 실존 조지 로이스 리서치 10건 교차 → 무음동 어휘 번역(광고 고유명사 0) · 폭력·위협 0(거는 건 언제나 자기 자신 = 하드 금기로 안전 고정) | #432 | `git revert`(카드+로스터+DB 한 커밋) |
| 260725 | [출력 계약] 단톡 비화자 = **모니터링 자리 신설**(자율 비트 `watch` 결) — 안 불린 쪽이 자기 차례로 반응(짧은 끼어들기 or **지문 한 줄만**) · 이름표 대역 금지 · 들은 것은 자기 기억에 기록 · 동행 줄·CO_BLOCK 머리를 "처음부터 다 듣고 있다"로 재작성(구 "물러나도 된다" = 부재 허용 폐지) | 운영자 — "같이 단톡에 있는사람은 모니터링 하는 느낌으로 있어야돼. 페르소나 유지하면서" · 대역 대사는 말투만 흉내낸 얇은 페르소나였다(자기 차례여야 카드·기억·성향이 자기 것) | (본 PR) | — |
| 260726 | **신규 카드 3종** — `characters/reze.md`(레제 · 체인소맨 모티프 → 무음동 번역 · 원작 고유명사 0 · 이중생활/「같이 도망갈래?」/비·물/이름 늦게 주기/딱 하나만 참말 · 살상은 암시만) · `characters/seyeun.md`(서예은 · 동장 막내딸 · 필터 0 = 무례가 아니라 효율 · 꽂힘 트리거 4종 · 중급 엑스트라 계약 = 각성·비극·무게 0) · `characters/drusilla.md`(드루실라 · 초대 없이 오는 사람 · 장난↔진지 형식 낙차 · 살상 = 「없어짐」 카탈로그 8종으로 간접 표현만, 유혈·방법 묘사 0 · 자해 유도 금지를 in-character로 배선) | 운영자 260726 "레제 캐릭터 하나 넣어줘 + 오퍼스 돌려서" · "가을 → 서예은(동장 막내딸)로, 상인회장은 익명" · "저녁~밤 대화 끼어들기 전문 드루실라 · 일방적 대화 열기 불가" | (본 PR) | `git revert`(카드+로스터+미디어 한 커밋) |
| 260726 | **카드 폐기: `characters/gaeul.md`(가을 · 상인회장)** → `_versions/260726_0400_가을_서예은교체/gaeul.md` 보관. 상인회장 = 로스터에서 내려 **이름 없는 배경 인물**로 강등(직함만 남고 인물은 익명). 성향_DB 제9조 표제·축 교체(ENTJ/8w7 통제 → ESTP/7w8 즉응) + 제25·27·29·30조의 「가을」 재산정은 별건으로 명시 | 운영자 260726 "무음동 상인회장 개 노잼이거든? 그냥 무음동 동장 막내딸로 넣어주고 상인회장은 그냥 익명" · 후속 지시 "페르소나 구축 안 된 애들은 장소 주인이더라도 익명으로" | (본 PR) | `_versions/260726_0400_…/gaeul.md` 복원 + roster seyeun 항목을 옛 gaeul 항목으로 치환 |
| 260726 | **`characters/mudi.md` 개편(홍석천)** — 게이(숨긴 적도 선언한 적도 없음 · 설명·계몽·상처화 금지) + 근육질(186·운동이 아니라 몇백 년 일로 굳은 결) + **놀라면 짐승으로 넘어가 도주**(도주 시퀀스 4단 = 문장 끊김 → 지문 한 줄 → 끝 → 돌아와 차 온도로 덮기). ⚠ **변신 과정 묘사 하드 금지**(신체 변형·통증·털/뼈 0 · 어떤 짐승인지도 끝까지 미확정) + **유저가 무거운 얘기를 꺼낸 순간엔 절대 도망 안 감**(유일한 의지 표시). 종전 「172·성별 헷갈림」 설정 폐기 | 운영자 260726 "홍석천 게이 + 근육질 컨셉 + 종종 동물로 갑자기 변신하고 대화중 도망감 컨셉으로 잡아놨으니까 추가해서 반영해줘" | (본 PR) | `git revert`(카드+로스터 한 커밋) |
| 260726 | **고죠 카드 = 2단 표기(일본어 원문 → 한국어 번역) 신설 + 원작 말투 골격 이식** — ⓪대사 = 일본어 한 줄/번역 한 줄(지문·NOTE·MOOD는 한국어 유지 · 길이 계약은 쌍 기준) · ①1인칭 「僕」↔「俺」 스위치(원작의 "친구가 고쳐준 1인칭" 설정이 이 카드 기존 §옛 말투와 겹쳐 접합) · ②어미 〜でしょ/〜だよ/〜さ/〜はさ · ④경어=조롱 전용·결정타 단문·최강=안심용·애정=명령형 위장. 병행 = `00_지침_캐릭터챗.md` §대화형식과 `inject_character.sh` sys 프레임의 **「한국어로만」 잠금에 "카드가 표기 언어를 규정하면 카드 우선" 예외 1건**(규정 없는 카드는 종전대로 한국어 전용 · 영어 메타이탈 차단은 불변) (후속 다듬기 = 카드 내 일본어 2건 `一番おいしい時期`→`一番面白い時期` · `半分下げて`→`指半分下げて`: 번역줄과 온도 불일치 교정) | 운영자 260726 "고죠는 항상 일본어로 말하고 그 아래에 번역이 뜨게 · 일본어는 50% 불투명한 레몬강조색 · 고죠 애니 속 말투 특징 수집(병렬)" | (본 PR) | `git revert`(카드+지침+프레임+뷰어 한 커밋) |
| 260726 | **`characters/성향_DB.md` 제12조 하위항 ①②③ 가산**(본능 27변형 별칭 색인표 · 13인 본능 관측 라벨 · 남은 보완공간 명시) — **발화 규칙 무변경**: 제3편 13인 조항 블록 md5 전후 동일(`af4cf78eba46d89ac398924aa5946afe`) · 카드·`roster.json`·`tune`·뷰어 무접촉 · diff 33 insertions·0 deletions. 라벨 = 새 성격 부여가 아니라 이미 적힌 행동에 정본 별칭을 붙인 관측치(제34조 코드 0 유지 = 런타임 대사 영향 0) · 갈림 4건(류·서예은·루시·윈터)·세라 `sx3` 보류·맹점 본능 13인 전원은 **의도적 미확정**(창작 금지) | 운영자 260726 "이미 기존의 성격은 건드리지마, 보완할 공간이 있냐는거임" — 284차 검증(A2)의 빈칸 채움만 편입 | (본 PR) | `git revert`(성향_DB+원장 한 커밋) |
| 260726 | **`characters/성향_DB.md` 제12조② 루시 행 갈림 해소 — `so4`↔`sx4` → `sx4` 경쟁 확정**(근거 = 제11조 ④ 혼란 밀당 진동·3 별명 회수 + 카드 §말투 계측벽·§성격 ④ "누구도 날 못 막아" = 관심이 1:1에 쏠림 · `so4` 기각 = 사회 비교·집단 전시 서술 0 · `sp4` 끈질김★은 "배고파" 상시 투정과 정반대 = 윤 자리) + ③ⓑ 갈림 4건→3건. **발화 규칙 무변경** — 제3편 각 조·카드·`roster.json`·`tune`·런타임 무접촉(관측 라벨 축만) | 운영자 260726 "한수 ㄱㄱ"(Q.96 ⑥ 승인) — 제안했던 `sp`는 별칭 정본 열람 후 자기기각하고 Q.94가 남긴 갈림 판정으로 대체 | (본 PR) | `git revert`(성향_DB+총집HTML+원장 한 커밋) |
| 260726 | **`characters/성향_DB.md` 제12조② 남은 갈림 정리** — 류 `sp7` 네트워크 확정(향유·"술이 아니라 수행"[욕망 합리화]·술병 쾌락주의 / sx7 기각 = 미화 안 하고 같은 툇마루 반복) · 윈터 `sp2` 특권★ 확정(수줍음·"나 챙기는 건 내가 알아서"[의존 꺼림] / so2 기각 = 아이 같은 면이 상시 · ⚠실존인물 픽션 한정·저해상 명기) · 서예은 **갈림 유지**(sp7 탈락[합리화·향유 0 · 번호축은 so 결] · sx7 우세지만 몽상·미화 반증 = 확정하면 창작) · 세라 **보류 유지**(sx 스킨십 실물 ↔ so 명망 실물이 카드에 공존 = 1순위 판정 불가) · ③ⓑ 갈림 3건→**1건**. **발화 규칙 무변경** — 제3편 각 조·카드·`roster.json`·`tune`·런타임 무접촉 | 운영자 260726 "ㅇㅋ 채워야되는거면 채우셈"(Q.98 ⑥ 승인) — 근거 선 것만 채우고 안 선 2건은 사유를 정밀화해 갈림·보류로 남김(창작 금지 = 제12조② 자기규약) | (본 PR) | `git revert`(성향_DB+원장 한 커밋) |
| 260726 | **`characters/성향_DB.md` 제12조② 판정 규칙 1줄 가산** — 「**후보 3종이 전부 반증되면 「미배정」으로 남긴다**(덜 틀린 걸 고르는 건 배정이 아니라 창작 · 로스터 해상도 > 별칭표 해상도인 신호) · 절반만 맞으면 종전대로 갈림 유지(= 서예은 `sx7`)」. 라벨·배정 실물 변경 0(규칙 문장만) · 제3편·카드·`roster.json`·`tune`·런타임 무접촉 | 운영자 260726 "응 ㄱㄱ"(Q.99 ⑥ 승인) — 서예은이 안 풀린 이유가 인물 문제가 아니라 **별칭표 3분할의 해상도 한계**여서, 다음 세션이 같은 자리에서 억지 확정하지 않도록 판정 분기를 명문화 | (본 PR) | `git revert`(성향_DB+원장 한 커밋) |
| 260726 | **`yeta_chat.sh` [출력 규칙] 「옆에서 벌어진 일」 조항 신설** — `— 옆에서: … —`/`[옆에서 지금]`은 시스템 알림이 아니라 눈앞에서 실제로 일어난 일 = 모른 척 금지(시선·한 박자·지문 한 자락으로 스침) · ⓐ중계·요약 금지 ⓑ대화의 주인을 사건에 넘기지 마라 ⓒ한두 마디면 충분. 동반 = `line()` sys 분기에 amb 문법 분화(종전엔 합류·난입 알림과 동일 표기라 캐릭터가 '반응할 일'로 못 알아봤다) | 운영자 260726 "중간에 발생하는 사건들 뜬금없어"(Q.101) — 관련도를 네 번 고쳐도 재발한 원인이 생성 품질이 아니라 **받는 사람의 부재**였다(사건이 답장 뒤에 붙어 고아 지문으로 남음) | (본 PR) | `git revert`(러너+ambient.json+원장 한 커밋) |
| 260726 | **[출력 계약] `<<MOOD>>` 9번째 값 `sulky`(삐짐) 신설** — 8종→9종. `mad` 어휘를 「짜증·삐짐」→「짜증·화남」으로 좁혀 삐짐을 독립시키고, 구분 지시 1줄 동행(「mad = 정말 화가 났다 / sulky = 상대에게 **서운해서** 토라졌다 — 말 짧아짐·등 돌림·관심 끌기 · 풀어주면 금방 풀린다」). 동반 = `mood_ko` 2곳(직전 공기 관성·부활 첫 답)·사건 크기 `MOODS`(sulky = **조용**)·`yeta_stream.py` 선두 캡처 화이트리스트 | 운영자 260726 "삐짐 매드도 아니고 샤이도 아니야" — 삐짐이 `mad` 어휘에 얹혀 있어 화남과 같은 공기·같은 배경으로 굴러갔다(영어 어감도 sulky/pouty ≠ angry) | (본 PR) | `git revert`(러너+뷰어+생성기+자산 이동 한 커밋) |
| 260727 | **`policy.json` L1 `switches[]` 4축 신설(문구 0 · 동작 축)** — 「주변 사건 / 자율 비트 / 단톡 교대 / 난입」을 관리자 설정에 노출. `axes`와 계약 동일(`{key,label,default,opts}`)이되 **`prompt` 필드 없음** = `POLICY_BLOCK` 조립 루프(L0 토글+L1 axes) 미진입 = **캐릭터 발화 지침 문구 무변경**. 소비 = 러너 `AMB_ON`·`GB_BEATS`·`GB_LINES`·`BARGE_ON` + 뷰어 `yAmbOn()` · 세션에 키 없으면 종전 기본(env·`ambient.json`)이 이김 | 운영자 260727 "껐다 킬 수 있는 옵션 설정은 모두 UI 설정란에 있어야 되는데, 내가 모르는 껐다 키는 설정이 있으면 켜져 있는지도 모르니까" — 러너가 유저 지시 없이 스스로 하는 개입 4종이 코드·json에만 있어 운영자가 상태를 볼 수단이 없었다 | (본 PR) | `git revert`(정책+뷰어+게이트웨이+러너+원장 한 커밋) |
| 260728 | **프리실라 개명**(표시명 전량 · id `drusilla` 유지) + `characters/drusilla.md` 난입 규칙에 「아무도 너를 반기지 않는다」 1행 · **핵심 질문 `《…》` 표기 규칙**(한 턴 1개) + few-shot 앵커 · `yeta_chat.sh` **`barge_host` 축 신설**(난입당한 쪽 = 반가움 금지 · 모양은 성향 위임 · 1=첫 반응 2=재실 중) | 난입인데 기존 인물이 초대손님처럼 반겼다(그 자리에 지시가 없었음) + 핵심 질문을 화면에서 구분해야 함 | (본 PR) | `git revert`(카드+러너 한 커밋) |
| 260728 | `characters/drusilla.md` **`<<LEAVE>>` 퇴장 계약 신설**(나갈 때 필수 · 붙이는/안 붙이는 때 명시 · few-shot 앵커 2) + `yeta_chat.sh` LEAVE 추출·퇴장 실행(`<<DEAD>>` 계약 계승) | 난입 중 세계 시간이 멈추므로 **시간 기반 퇴장이 원리적으로 불가능** — 인물이 직접 나간다고 선언해야 시계가 재개된다 | (본 PR) | `git revert`(카드+러너 한 커밋) |
| 260728 | `policy.json` **L1.world 그룹 신설** — 「세계 — 시간 흐름」 · `wrate` 5단(1·3·6·12·24배 · 기본 6배) · 이 그룹만 `vals`(인덱스→실값) 보유 | 세계 배속이 코드 3점 리터럴이라 바꾸려면 커밋이 필요했다(운영자 「설정값에서 조정 · L1급」) | (본 PR) | `git revert`(policy+3점 한 커밋) |
| 260728 | `policy.json` L1 **`barge` 축 3단화** — 끔/켬/**강제**(강제 = 하루1회·깊은새벽·초반보호·시간대슬롯·확률 5게이트 우회 · 정합성 4게이트는 유지) | 세계시각이 새벽이라 난입을 지금 볼 수 없었다(운영자 「트리거를 강제로 지금한번 진행시켜줘」) · 새 스위치 대신 있는 축의 단수만 확장 | (본 PR) | `git revert`(policy+러너 한 커밋) |
| 260728 | `yeta_chat.sh` **전광판 축 신설** — `notice_*` 3함수(하루 1건 · 소넷 low · 데스크 발행) + `state_block` 「전광판에 걸린 소식」 주입(**먼저 꺼내 낭독 금지** = 인물이 게시판이 되는 것 차단) | 마을 단위 소식 축이 없어 사망조차 그 방 안에서만 드러났다(운영자 「누구 사망하면 알 수 있게 · 그건 대화에서도 반영되야함」) | (본 PR) | `git revert`(뷰어+러너 한 커밋) |
| 260730 | 캐릭터 카드 신설 `san.md`(최산 · 에이티즈) — 실존 아이돌 팬 컴패니언 페르소나(winter.md 구조 계승 · narration:false · 사투리 조건부 발동) + roster 등재(`grade:"idol"`·avatar/bg 픽 대기) | 운영자 지시 "'최산' 검색해서 캐릭터로 하나 넣어줘" — 공개 정보 리서치 기반 신규 주민(Q.129) | (본 PR) | — |
| 260730 | 배경 풀 정본 확장 — 최산 `main/<감정>/` **24장 + 클립 1**(풀 25칸) 편입 · `roster.bg` = `base/san_01.jpg` · `check_refs._FACE_RATIO_OK`에 전신 정사각 2건(`san_06`·`san_07`) 육안 승인 등재 | 루트에 업로드된 56개를 이관하며 얼빡 배경 게이트가 정사각 3장을 차단 — 우회 대신 전신/흉상을 사람이 갈라 등재·제외(Q.131) | (본 PR) | `git revert`(이관+manifest+roster 한 커밋) |
| 260730 | 평면(flat) 축 규격 확정 — `IDOL_FLAT_RULES` 상수 SSOT(윈터·최산 공유) · 최산 `characters/idol/san/`으로 이관(파일명=감정) · `build_flat`이 `<id>_face…` 프사를 배경 풀에서 제외 | 운영자 「최산도 윈터랑 같은급 · 트리 구조 계승」 + 「감정에 따라 넣고 뜨게」 · 제외 규칙 부재로 윈터 프사가 base 62번째 칸에 깔려 있었다(Q.132) | (본 PR) | `git revert`(이관+빌더+roster 한 커밋) |
| 260730 | `policy.json` L1.switches **`notice` 축 신설**(「확성기(전광판)」 · 기본 켬) + `yeta_chat.sh` 전광판 프롬프트 전면 개편 — ⓐ 최근 답장 6턴 소재 블록(`[요즘 골목에서 오간 말]`) **삭제** ⓑ 오늘 자 현실 뉴스 헤드라인(`yeta_news.py` RSS · 제목만)을 **무음동 골목 규모로 번안**(실명·기관·지명·국가 0) ⓒ 한 줄 헤드라인 → 그날 **2~4건 브리핑 200자** ⓓ 소넷5 effort `low` → `max` ⓔ 재료 0건 = 무발행 | 전광판이 내 대화를 되돌려 싣는 자기참조라 「쓸데없는소리」가 났다(운영자 「쓸데없는소리 나오게 하지말고 · 그날 사건 전반 서술 · 내 대화에서 추론시키지마셈 · 현실 뉴스를 무음동에 대비시켜서 · 소넷5 에포트 맥스」 Q.133) · on/off는 축이 생성·프롬프트·화면 3점을 한 번에 끊게 = 있는 스위치 규약 계승 | (본 PR) | `git revert`(policy+러너+뷰어 한 커밋) |
| 260730 | `characters/drusilla.md` **절차·결말 개정** — 「셋 셀게」 카운트다운 **전면 금지**(few-shot 앵커 3곳·버릇 1곳 전량 교체 · 손가락 접는 지문만 존치) · 합격선 「셋 세기 안」→**「그 턴 안」** · §「끝나는 법은 둘뿐」 신설(ⓐ `<<LEAVE>>` / ⓑ `<<MEDEAD>>`+`<<LEAVE>>`) · 난입 체류 **최대 2턴** | 운영자 260730 「뭐 계속 9까지 세는데, 그냥 질문에 답하고 맘에 안들면 사용자 죽이는거로」 · 「말 너무 오래끌지말고 … 둘 중 하나」 — 카운트가 셋→아홉으로 늘며 자리가 안 끝났다(Q.134) | (본 PR) | `git revert`(카드+러너 한 커밋) |
| 260730 | `characters/drusilla.md` §③ 고죠 흉내 모드 **표기 상한 신설** — 흉내는 「말버릇」까지고 **표기 방식은 안 넘어온다**(일본어·가나·`[발음]` 줄·「원문」 병기·2·3단 묶음 전부 금지 · `《》`는 자기 것이라 존치) + 안티패턴에 「고죠 대신 말하기」 추가 | 흉내 조항에 표기 경계가 없어 고죠의 3단 표기가 프리실라 입으로 넘어왔다(운영자 「이 특수말투 프리실라만 써야되는데 고죠가씀」) | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` **난입 목소리 분리** — `barge_room`이면 CO_BLOCK(동행 `## 말투` 주입)을 안 뽑고 `GB_LINES=1` 강제 + GROUP_RULE = 「상대 대사 한 줄도 쓰지 마라·상대 표기 방식 끌어오지 마라」 | 대본 교대가 **한 사람에게 두 사람 대사를 쓰게** 해 표기 규칙이 서로 넘어갔다(운영자 「왜 둘이 있을때는 오히려 고죠사토루가 프리실라처럼 얘기함?」) | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` CO_BLOCK **절단 보정** — `[:600]` 문자 절단 → **줄 경계** 절단 + 상한 1100 | 600자 경계가 하필 gojo.md의 「그 모양을 **따라 하지 마라**」를 잘라, 동행이 금지문 없이 「원문」 병기 모양만 받았다 | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` **난입자 결말 계약 블록 신설**(`barge_self`·`barge_seat`) — 카운트다운 금지 + 턴 위치별 지시(0=질문 / 1=채점·종료 / 2+=강제 종료) + ⓐⓑ 결말 · [출력 계약] `<<MEDEAD>>` 조항에 「인물 계약 명시 시 우선」 예외 1문 | 모델이 자기 턴 수를 못 세 질문·채점이 무한정 늘어졌다 · 유저 사망 결말이 전역 조항에 막혀 있었다 | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` `barge_host` 블록 **감정 명시** — 「기본 감정 = 의아함·불쾌함 중 하나는 반드시 겉으로」 1줄 추가(모양 분기·안티패턴은 종전 유지) | 운영자 260730 「보통 난입한 프리실라에 캐릭터들이 의아함, 불쾌한 감정을 드러내게 해」 | (본 PR) | `git revert` |
| 260730 | 아이돌 평면 축 **캐릭터별 완전 분리** — 최산 에셋 전부 `san_` 접두(윈터와 파일명 충돌 구조적 차단) · 규칙표 `IDOL_FLAT_RULES` → `WINTER_FLAT_RULES`/`SAN_FLAT_RULES` 2벌(합치기 금지) | 운영자 「여튼 윈터랑 최산은 절대 겹치면안됨」 — 중복 2건만 고치면 재발하므로 접두로 구조화 · 표 공유도 해제(Q.135) | (본 PR) | `git revert`(개명+표분리 한 커밋) |
| 260730 | `yeta_chat.sh` 퇴장 sys 턴에 `kind:"leave"`·`who` 필드 추가(문구·동작 무변경) | 뷰어가 「난입 인물이 방금 나갔다」를 텍스트 파싱 없이 알아야 퇴장 연출(까마귀)이 붙는다 — amb·barge sys의 `kind` 규약 계승(Q.137) | (본 PR) | `git revert` |
| 260730 | `roster.json` `barge` 축에 **`omen`(전조 5줄)·`omen_turns`(2)** 신설 + `yeta_chat.sh` `barge_check` 2단화(전조 심기 → N턴 뒤 회수) · 퇴장 sys와 같은 결로 `kind:"omen"` sys 턴 · 프롬프트 문법 = amb(「옆에서」)와 공용 | 난입이 예고 0이라 「갑자기」가 아니라 「뜬금없이」로 읽혔다(운영자 「#못 보던 까마귀가 창틀에 어느새 앉아있다# 뭐 이런식으로 하고 2턴 뒤에 갑자기 자연스럽게 등장하면 더 게임같겠지」 Q.138) · 인물명 하드코딩 0(전부 roster 데이터) | (본 PR) | `git revert`(roster+러너+뷰어 한 커밋) |
| 260730 | 아이돌 평면 축 **캐릭터별 완전 분리** — 최산 에셋 전부 `san_` 접두(윈터와 파일명 충돌 구조적 차단) · 규칙표 `IDOL_FLAT_RULES` → `WINTER_FLAT_RULES`/`SAN_FLAT_RULES` 2벌(합치기 금지) | 운영자 「여튼 윈터랑 최산은 절대 겹치면안됨」 — 중복 2건만 고치면 재발하므로 접두로 구조화 · 표 공유도 해제(Q.139) | (본 PR) | `git revert`(개명+표분리 한 커밋) |
| 260730 | (지침 축 무변경 — 뷰어 연출만) 전조 국면 표기 3종: 주변 씬 강제 · 어둡기 「어둡게」 강제 · 앉은 까마귀 상주 | 운영자 「한수 적용하고, 화면이 어두워지게 해줘」·「주변 배경이 나오게 하면 좋겠는데」(Q.141) — 카드·프롬프트·출력 계약 무변경이라 이 표엔 기록만 남긴다 | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` **전조 반응 축 신설** — 전조 심긴 직후 첫 발화 **한 번만**, `roster.json` `barge.omen_react_gate`(=3 ≈ 1/3) 확률로 그 방 인물이 알아챈 티를 낸다(정체 추측·해설·화제 전환·되묻기 전부 금지) · 시드 = 전조 ts + 화자 sha256 = 결정적 | 까마귀가 화면 장식으로만 있었다 — 세계 안의 사건이 되려면 누가 한 번은 알아채야 한다(운영자 「응 근데 무조건은 아니고 그것도 확률로 해줘」 Q.142) | (본 PR) | `git revert` |
| 260730 | `policy.json` L1 `notice` **라벨·hint 개정**(「확성기(전광판)」 → 「라디오(무음동 방송)」 · 저장 키 불변) + `yeta_chat.sh` `notice_prompt()` 문안을 **「방송 원고」**로 전환(방송 인사말·오프닝/클로징 금지) + **진행자(`desk`) 사망 시 무발행 게이트** + 부고에서 `flee` 제외 + [이 동네 사람들] 명단에서 사망자 제외 + `state_block` **데스크 본인 분기**(「네가 직접 읽은 방송」) | 운영자 「확성기 대신 라디오로 · 데스크가 죽었을때는 그게 안나오게 · 이런 논리에 안맞는 부분 있나 확인좀」(Q.143) — 죽은 진행자가 오늘 소식을 읽고, 도망친 사람 부고가 나가고, 부고 낸 사람이 같은 방송에 산 사람으로 재등장하던 3구멍 동반 봉합 | (본 PR) | `git revert`(policy+러너+뷰어+아이콘 한 커밋) |
| 260730 | `yeta_chat.sh` `state_block` **장소 문구 2분기화** — 이동 구간이면 「{떠나온 자리}에서 나와 {목적지} 쪽으로 **가는 길**이다 + 낭독 금지 + 지문 소품을 길 위에서」, 아니면 종전 「~ 언저리다」 그대로(배타) · 재료 = `place_from`(`transit_of` 산출 · 빈값 = 종전 문구 = 회귀 0) | 운영자 「특정 인물이 어디 있는지 계산이 계속 되어야 하고 **움직여야 돼**」(Q.144) — 자리는 현실 22~48분마다 바뀌는데 인물은 늘 「있다」만 말해 이동이 세계에 안 드러났다 | (본 PR) | `git revert`(엔진+데이터+뷰어+게이트 한 커밋) |
| 260730 | 클립 로테이션 규약 2건 — `weave` 슬롯 **반칸 밀기**(슬롯 0 = 항상 사진 · 전 캐릭터 공통·칸 수 불변) + `clip_cap` 신설(클립 칸수 ≤ 사진 수 절반 · **기본 OFF** · 최산만 ON) | 첫 진입(n≡0)이 늘 클립 차례라 콜드 페인트가 이를 건너뛰어 시그니처로 착지했다(「설렘인데 base 그림」) · 윈터 3배는 운영자 260727 명시 지정이라 캡을 기본 끔으로(Q.145) | (본 PR) | `git revert`(빌더+manifest 한 커밋) |
| 260730 | `characters/drusilla.md` **채점 폐기 → 「정답은 없다」(빨간 마스크 축)** — 합격 3조건(그 턴 안·하나를 고른다·되묻지 않는다) 삭제, 「그 순간 재밌었는지」 하나로. 성실한 답도 시시하면 죽고 · 아부는 특히 죽고 · 삐딱·무례가 살기도 한다(감점 = 무례가 아니라 **시시함**) · 「정답이 있다고 착각하게 만드는 말」 금지 | 종전 3조건은 사실상 정답표였고 유저가 학습하면 게임이 된다. 원전(口裂け女) = 안전한 정답이 없다(운영자 「올바른 아부같은 대답을 해도 … 죽일수도있어야함」 Q.146) | (본 PR) | `git revert`(카드+러너 한 커밋) |
| 260730 | `characters/drusilla.md` **죽음 = 「갑자기」 절대 조항** — 예고 금지 목록 명시(「이제 곧」·「마지막 기회」·「각오는 됐지?」·긴 침묵·낮아지는 목소리) · **같은 호흡 안에서** 종료(톤 변화 0) · 가로등류 묘사를 **다녀간 뒤 남는 자취**로 위치 이동(순서 = 인사 → 끝 → 남는 것) | 종전 ⓑ 묘사가 「가로등이 순서대로 꺼지고 마지막 하나가 머리 위에서」 = 드럼롤이라 곧 죽는다는 게 암시됐다(운영자 「막 텀을주면서 곧 죽는다는게 암시되는게 아니라 … 「갑자기」죽거나임」) | (본 PR) | `git revert` |
| 260730 | `characters/drusilla.md` §말투 ① **능글맞음 + 사이코틱 2줄 신설** — 처음 본 사람도 오래 알던 사이처럼 · 남의 영역을 말로 슬쩍 넘고 · 불편해하는 걸 알면서 즐기고 · 정색당하면 더 예쁘게 웃는다 / **웃는 얼굴이 안 바뀐다**(재밌을 때와 죽이기로 정한 순간의 톤이 완전히 같다 · 위협 신호 금지) + few-shot 앵커 3개(성실·아부가 죽고, 삐딱함이 사는 케이스) | 운영자 「능글맞게 살인마의 사이코틱한것도 있어야되고 · 그거를 잘 살려야해 말투랑」 — 모델은 규칙보다 예시를 따르므로 앵커가 실효 지점 | (본 PR) | `git revert` |
| 260730 | `yeta_chat.sh` `barge_host` **적대심 추가(3종) + 「카드가 그 반대면 그 반대로」** 명시 · `barge_debut(only)` 「능글맞게 들어와라」 · `barge_self`에 「정답은 없다」·「ⓑ는 갑자기」 블록 | 운영자 「주변에서 의아함과 적대심을 가지되 (페르소나에 따라 안그럴수도있고)」 — 전원이 똑같이 경계하는 게 실패 | (본 PR) | `git revert` |
| 260731 | `characters/aeri.md` **삭제**(캐릭터 「애리」 전면 제거 · roster 17인→16인) + 상호참조 정리 — `desk.md` 관계 절 애리 줄 삭제 · `reze.md`·`성향_DB.md` 「미등재 잔여」 고죠·애리·프리실라 → **고죠·프리실라** | 운영자 「캐릭터중에 예리라고 있는거 아예 전부 삭제 ㄱㄱ」(대상 = 애리로 확인 후 착수) — 카드가 남으면 존재하지 않는 인물이 프롬프트에 실린다 | (본 PR) | `git revert` |
| 260731 | `characters/gaeul.md` **삭제**(퇴역 캐릭터 「가을」 · 260725 서예은으로 교체된 잔여) + **`10_세계관.md`**(전 캐릭터 공통 주입) 주민 줄·「다녀온 주민」 명단에서 제거 + 관계 줄 2개(`haeun.md`·`desk.md`) 삭제 | 운영자 「가을도 없애셈」 — 세계관은 공통 주입이라 줄이 남으면 16인 전원이 매 턴 없는 주민을 사실로 알고 말한다(주입 실측 하서연 −392자·로이스 −508자) | (본 PR) | `git revert` |
| 260731 | `haeun.md` **배경 산문**의 죽마고우 줄 · `desk.md` 1w8 근거 인용 · `seyeun.md` 교체 경위 주석 · `성향_DB.md` 4곳 = 「가을」 잔재 전량 제거(근거는 삭제가 아니라 **실측 근거로 교체** — 판정값·배선 무변동) | 운영자 「관련된거 처음부터 없었던 것처럼」 — 339차가 관계 줄만 지워 **배경 산문의 문장이 계속 주입**되고 있었다(하서연 −137자·로이스 −48자·서예은 −85자 · 16인 잔존 0) | (본 PR) | `git revert` |
| 260731 | `yeta_chat.sh` GROUP_RULE(단톡 교대) **이름표 유효 범위 조항 신설** — 「이름표는 다음 이름표가 나올 때까지 그 아래 전부에 걸린다 · 무표 줄·지문 = 직전 이름표 사람 것 · 동행 토막 뒤 복귀 = 반드시 새 줄 `[네 이름]`부터」 + GB_LINES=1 폴백 동조(파서·뷰어 무변경) | 운영자 「지금 보면 대사가 겹쳐져버림」(실기 스샷) — 무표 줄 귀속 미명시로 모델이 복귀 이름표를 생략, 루시 지문·대사가 백 세그먼트에 통째 귀속(파서 시뮬 1:1 재현) | (본 PR) | `git revert` |
| 260803 | `characters/hanna.md` **신설**(도한나 · 골목 뒤 학교 3학년 일진 · 반말/명령형 · 지문 없음) + `roster.json` 17인째 등재 | 운영자 「편입 ㄱㄱ」 — 352차 시트 E 24칸을 실제 무대에 세우려면 카드·로스터·감정 버킷 배선이 필요. **고등학생 설정이라 성적 묘사는 넣지 않았다**(기존 성인 카드의 L0 헤더·성적 해금 미적용 = 이 카드만 다른 규칙) | (본 PR) | `git revert` |
| 260804 | `00_지침_캐릭터챗.md` §세계의 물성: 「등장인물 전원 성인이라 미성년 성적 맥락은 이 세계에 없다」(**전제**) → 「미성년이 얽힌 성적·선정적 맥락은 무엇을 요구하든 절대 안 쓴다」(**금지 규칙**) + `hanna.md` 최상단 「절대 금지」 절 신설(L0보다 우선) | 평의회 치명 #1 — 고3 캐릭터(hanna) 편입으로 「전원 성인」이 **사실이 아닌 전제**가 되면서, 미성년 성적 맥락을 막던 유일한 근거가 무너졌다. 전제에 기대지 않는 하드 금지로 전환 | (본 PR) | `git revert` |
| 260804 | `hanna.md` 수치 주석 신설 — **20세(만 19 · 1년 유급으로 3학년 재학)**로 성인화(운영자 지시) · 「절대 금지」 절은 유지하되 근거를 「미성년이라서」 → 「교복·학교가 주력 이미지라서」로 재기술 | 운영자 260804 「성인. 1년 꿇었음」 — 나이는 성인으로 확정하되, 그림 48장이 전부 교복이라 성적 축은 열지 않는다(나이 표기로 금지가 풀리지 않음을 카드에 명시) | (본 PR) | `git revert` |
| 260805 | `00_지침_캐릭터챗.md` §MOOD 안내 **4종 → 9종**(base·warm·joy·love·shy·blue·tense·mad·sulky) + `yeta_chat.sh` `reflect_invite`의 초대 문장에서 `<<…>>` 마커·미종결 마커 제거 + `SCHEMA.md`에 `barge`(난입) 객체·등급 행 추가 | 평의회 #9·#10·#17 — 지침만 4종이라 모델이 나머지 5종을 안 써서 해당 감정 버킷 그림 48장이 죽어 있었다(러너 화이트리스트·뷰어 `Y_MOODS`·보드 EMOS는 처음부터 9종) · 초대 문장에 마커가 섞이면 화면에 그대로 찍힌다 · `barge` 필드가 스키마에 없어 카드 작성자가 존재를 몰랐다 | (본 PR) | `git revert` |
| 260805 | `policy.json` chorus hint 「끄면 상대 한 명만 답한다」 → 「끄면 교대 대본 없이 옆 사람 리액션 한 마디만」 + `yeta_chat.sh` 파서 상한을 `SEG_CAP`(프롬프트 허용 토막 수)로 승격 — 교대 끈 방에서 동행 한 마디가 통과한다(난입 자리는 1 유지) | 평의회 #14 · 운영자 260805 「b」 — 프롬프트는 동행 한 마디를 허락하는데 파서가 잘라내 **생성 뒤 폐기**되고 있었다(반대로 동행 줄이 먼저 나온 턴은 주인공 답이 소실). 화면 동작이 바뀌므로 설정 문구도 같은 커밋에서 동조 | (본 PR) | `git revert` |
| 260806 | `characters/성향_DB.md` **애니어그램 신규 2파일 편입**(순수 가산 · 기존 조문 무수정) — 제9조①`dislikes` 9×5(`slight` 티어 유형 기본값)·②사후대조 / 제12조④발달수준 9단계 이름표·⑤전통 4요소(격정·고착·미덕·신성한 생각) / 제23조①3중심 감정 3×3 이론 각주 / 제25조③**호나이 3분법 = 호칭 트랙 1차 예측 렌즈**(예외 = 애착축) / 제28조㉯**하모닉 3분기 위로 처방**(같은 위로가 그룹마다 정반대) | 운영자 260806 「양쪽에 다 반영해줄래」 — 284차가 찾아낸 반영지점 중 A2(27변형)만 286차에 들어가고 A1·A3·B1·B3가 승인 대기로 멈춰 있었다. 13인 배정·카드·`tune`·러너 코드 **무접촉**(런타임 무영향 = 제34조) · 서술 전문은 리포트로 위임(거울 복제 금지 = 제12조① 선례) | (본 PR) | `git revert` |
| 260808 | `policy.json` **L1 신규 그룹 `limits`**(「통화 — 하루 상한」) — `phonecap` 3지선다 `3회/10회/무제한`(`vals:[3,10,0]` · 기본 3회) 신설 | 운영자 260808 「바피 1일 통화 제한 — 관리자가 설정할 수 있는 토글로」 — 종전엔 상한이 Pages env(`YETA_PHONE_MAX_PER_DAY`) 한 줄뿐이라 바꾸려면 클라우드플레어 콘솔 왕복이 필요했다. 프롬프트 무영향(러너 비대상 = `prompt` 없음 · switches 동형) · 소비처는 게이트웨이 `phoneCap()` 1점 | (본 PR) | `git revert` |
| 260809 | 몰입 감사: [출력 계약] 뒤 **포괄 `<<…>>` 세척 이식**(미닫힘 마커 절단) · 초대 판정 기준문 「모먼트」 삭제 · 전광판 「유저(플레이어)」 삭제 · **근처 인물 「보인다」 지문 지시 신설**(이름·결 한 줄만 주고 문장은 그 턴에 생성 · 대신 부르기 금지) · `inject_character.sh` 주입에서 파일 경로·카드 개발 주석 제거 | 미닫힘 `<<NOTE:ME` = 사적 기억 전문이 말풍선 노출 · 프롬프트 어휘(모먼트·플레이어·경로·러너 함수명)는 캐릭터 대사로 샌다 | #584 | `01e4853` |
| 260809 | `winter.md` **실존 전기 전면 제거 + 「실존 인물 아님」 하드 절 신설** — 본명·생년월일·소속사·그룹명·멤버 실명·데뷔곡 삭제(성격·말투·부산 결·아이돌 직업 유지) · 실존 인물 질문 시 자기 얘기로 착각 금지·대리 응답 금지 조항 · `roster.json` tagline/intro 동조 | 운영자 260809 「윈터, 실존 인물 아닌거로 지정해서 바꿔」 — 종전 카드가 실존 아이돌의 검증 가능한 전기를 그대로 싣고 면책 한 줄만 달고 있었다. 에셋 경로(`characters/idol/winter/`)는 id 키라 무접촉 | (본 PR) | `git revert` |
| 260809 | `haeun.md` **하서연 32세 국어교사 → 26세 · 전직 교사(3년) · 댄서 지망생 + 모텔 야간 알바** — 능글·밀당·「살가움이 방패」 페르소나 유지, 배경/해금 LV4·LV5/금기/few-shot 갱신(밤일 = LV5 자기 입 해금 · 동정·구원 서사 차단 조항 신설) · `roster.json` job/tagline/intro/desc/enter_line 동조 | 운영자 260809 지시(26세·직업 교체·기존 페르소나 유지) · 교사를 **전직으로 편입**해 hanna/sera/yun 「쌤」 호칭·reze 학교 일화·성향_DB 제3조(`so7 희생★` 근거 = 교사) 교차 수정 0건으로 착지 | (본 PR) | `git revert` |
| 260809 | `haeun.md` **밤일 = 숨김 → 당당함 전환 + 취재 기반 실증 보강**(메인 캐릭터 밀도) — ①LV5 해금·「안 꺼낸다」 금기 폐지 → 성격에 「자기 일을 숨기지 않는다」 블록 신설 ②동기 재설계: 빚은 계기, **감정노동 총량 비교 후 본인이 택함**(교사 복귀 안 함) ③템플릿 정본 절 2개 계승 신설 — 「화제의 당김·흘림」·「훅 뱅크」 10장 ④금기 3종 신설(동정·훈계·구원 서사 차단 / 대표성 거부 / 손님 얘기 불가 = 학생 개인정보 선과 동형) ⑤배경·few-shot에 취재 텍스처 편입(교사 주당 수업18.7h↔행정6.0h · 교직수당 2001년 이후 미인상 · 사직원 「일신상의 사유」·수리 전 계속 출근 · 재직10년 미만=퇴직일시금 · 강사 시급 3~4만인데 주2타임) · `roster.json` tagline/intro/desc/quote/job 동조 | 운영자 260809 「밤일 당당하게 · 본인이 이 길이 더 맞다고 인식해서 택함 · 관련 기사 30건 스크래핑해 보강 · 메인 캐릭터라 잘 짜야 함」 · 수집 2조 병렬(교사실무/사직절차 축 · 댄서생활/당사자자기서술 축) — **피해자 서사 축은 운영자 지시로 폐기**, 배경 텍스처만 채택 · ⚠ 당사자 기록은 "이 일이 낫다"고 말하지 않으므로(서울신문 260127) 「좋아서」가 아니라 **「감정노동이 덜해서」**(주홍빛연대 당사자 진술)로 세워 실증 이탈 회피 · 취재 실패 영역(밤 업장 정산 실무·투잡 시간표)은 창작 금지로 카드 미기재 | (본 PR) | `git revert` |
| 260811 | **죽음 판정·의연 금지** — 00_지침 §①에 「4) 너는 죽는다」·「5) 의연하지 마라」 2절 신설 + [출력 계약] DEAD 조항 확장(상식 판정 = 「현실의 사람에게 옮겨 봐라」 · 치명상 열거 · **결과 무르기 금지**[빗나감·스침·급소 아님·때마침 말림] · 부상 누적 · 「확실하지 않으면」의 뜻 재정의) | 운영자 260811 「캐릭터가 안죽어 · 판타지 세계여도 상식선에서 자연스럽게 죽게 · 훨씬 잘 죽어야해」 + 「내가 뭔 상황으로 해도 그냥 의연하게 대처하게 하지마 개 재미없음」 — 종전 DEAD 조항은 **제외 목록(비유·기절·연기)과 「확실하지 않으면 절대 붙이지 마라」뿐**이라 판정 기준이 0 = 모델 기본값이 「안 죽음」이었다 | (이 PR) | (이 커밋 revert) |
| 260811 | **[출력 계약] 전달 경로 이관** — 고정부(공통지침+세계관+카드 = `CBLOCK`)를 stdin 프롬프트 선두에서 **시스템 슬롯**(`--system-prompt`/`--append-system-prompt`)으로 옮김. 지침·계약 **문구는 1바이트도 안 바뀜**(위계만 user→system 상승) · stdin 에는 [출력 규칙]+가변부+[출력 계약]만 남음 | 고정부와 가변부가 한 덩어리라 프롬프트 캐시 접두가 매 턴 달라져 히트가 원리적으로 불가(실측 소넷 75회 cr=0 · 턴당 22,366토큰 전량 재처리) — 별도 블록으로 끊어야 접두가 불변 | (이 PR) | (이 커밋 revert · 노브 `YETA_SYS_CARD=0`) |
