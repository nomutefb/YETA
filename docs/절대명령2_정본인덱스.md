# 🔒 절대명령 #2 (규칙 2) — 디자인 정본 통합 인덱스

> **위상**: 규칙 1(CLAUDE.md §🔒 절대명령 #1)은 이 파일을 **절대적으로 따른다**(운영자 260704).
> 이 파일 = "값·규칙·컴포넌트·확립본·게이트가 **어디에** 있는가"의 단일 출처(위치 SSOT).
> **내용 위상은 불변**: 값 충돌 = `viewer/index.html :root` 정본 · 규칙 충돌 = CLAUDE.md §🎨 정본 — 이 인덱스는 위치만 정본이라 내용 위상과 충돌 자체가 없다.
> **등재 의무**: 새 정본(확립본·블루프린트·인덱스·모듈 계약)이 생기면 **이 표에 행 추가까지가 완성** — 미등재 = 정본 아님 = 드리프트 씨앗.

## 1) 값 (Values)
| 정본 | 경로 | 역할 |
|---|---|---|
| **값 SSOT** | `viewer/index.html` `:root` | 모든 디자인 값(색+구조 토큰 47개) 단일 출처 |
| 승인 원장 | `design-tokens.lock` | `:root` 새 토큰 게이트 — 미등재 = `check_refs` rc=1 커밋 차단 |
| 거울(구조) | `viewer/tokens.css` | build 산출 — **직접 수정 금지** |
| 거울(색+구조) | `구성도/base.css` | build 산출(AUTO-MIRROR) — **직접 수정 금지** |
| 거울 빌더 | `shared/build_design_mirror.py` | `build` = 거울 재생성(승인 편입 절차의 기계부) |

## 2) 규칙 (Rules)
| 정본 | 경로 | 역할 |
|---|---|---|
| **규칙 1(제1 핵심명령)** | `CLAUDE.md` §🔒(위상 선언) · 완전판 `docs/절대명령1_포터블.md` · 주입 `.claude/hooks/design_digest.py` · 타모델 `AGENTS.md` §🎨 | 계승/갱신 2분법 · raw 예외축 · **규칙 2(이 파일) 절대 준수 선언** |
| 규칙 전문 | **최종 권위 `CLAUDE.md` §🎨** · 운영 거울(파생) `AGENTS.md` §🎨 · 주입 `.claude/hooks/design_digest.py` | 디자인 계약(3층 방어) — 내용 충돌 시 `CLAUDE.md` §🎨 최종 정본(거울은 파생·역전 금지) |
| 포터블 이식판 | `docs/절대명령1_포터블.md` | 타 레포 이식용(훅·락·문구 세트) |
| 플레이그라운드 포터블 | `docs/플레이그라운드_포터블.md` | 타 레포 이식용(제안 표준 5요소·골격 스펙·전달 판정) |
| 웹UI 공통규약(이식 원본) | `구성도/노뮤트_웹UI_공통규약.md` | 텍스트 위계·컴포넌트 인벤토리·공통 문법 |
| 디자인 방식론(YETA) | `docs/디자인방식론_YETA.md` | 계승·픽토그램·색규율·통일·타이포·공통 UI 로직 원칙(값 아닌 방식론 · 실물은 `:root`/CII 링크) |
| 감정선·대화 이어가기 | `docs/감정선_대화이어가기_정본.md` | 대화가 애매하게 끊기는 원인(SC3)과 처방 정본 — 종결 수 **대체** 4형 + `<<OPEN:>>` 캐리어 + 금지선(같은 그림 반복 금지·길이 계약 불가침). 강제 = `apps/yeta/00_지침_캐릭터챗.md`([No-Summary]·태그 절) + `.github/scripts/yeta_chat.sh`(파싱·되먹임) |

## 3) 컴포넌트 (Components)
| 정본 | 경로 | 역할 |
|---|---|---|
| **CII 인덱스** | `docs/CII_컴포넌트계승인덱스.md` | 정본 셀렉터 표 — 신규 컴포넌트 = 행 등재 의무 |
| 버튼·인터랙션 가이드북 | `구성도/00_가이드북_버튼인터랙션.html` (+`.md`·`구성도/버튼인터랙션_기틀북.css`) | 버튼·눌림(`--press-*`)·모션 패턴 정본 |
| 아이콘 SSOT | `viewer/nm-svg.js` | 공유 아이콘 단일 정본(인라인 재선언 = 게이트 차단) |
| 음성 모듈 계약 | `viewer/call.js`(모듈 정본 ☎️) · 앱 구조 = `CLAUDE.md` [15] | 전화 수신·무전 마이크·프리미엄 배지(CII 3행 등재분·훅 3줄 플러그인) |

## 4) 확립본 (사인오프 스냅샷)
| 정본 | 경로 | 역할 |
|---|---|---|
| **브랜드 팔레트 확립본**(시각 정본·260704) | `docs/브랜드_팔레트_확립본.html` | 팔레트 v2 = 라임 `--accent`+코발트 `--cobalt`·다크·글래스 — **이 밖 색 금지** |
| 구성도 블루프린트 허브 | `구성도/index.html` | 아래 시각 정본 묶음의 입구 |
| 시각 정본 묶음 | `구성도/텍스트 위계.html` · `표면 테두리 강조.html` · `픽토그램.html` · `전송 버튼 상태머신.html` · `진행 결과 상태.html` · `버튼 배치.html` · `내어쓰기 정렬.html` · `부속 설명 규약.html` · `메뉴별 대표색.html` · `브랜드 에셋.html` | UIUX 확립 스냅샷(재설계 금지·계승 원본) |
| 음성 축 사인오프(260704) | `docs/reports/260704_전화무전기보이스_보고서.html` | 전화·무전기·보이스 요구 16행 검증 정본 |
| **플레이그라운드 제안 표준 확립본**(260706) | `docs/reports/260706_언락밝기_플레이그라운드.html` | 디자인 제안 = 플레이그라운드식 5요소{실물 미리보기·프리셋·미세조정 컨트롤(축 3분류·⚠ 역제안)·선택값 복사·한계 각주} — 이후 시안은 이 구성 계승(규칙 상세 = `CLAUDE.md` [5]·`yeta-design` 스킬·`docs/플레이그라운드_포터블.md` §3-2′) |

> ※ nomute 레포 확립본(`260704_모달셸팔레트_확립본.html`·`브랜드컬러_확립본.html`·`팔레트개편_결과.html`)은 **yeta에 존재하지 않음**(260704 실존 대조). 이식하면 이 표에 등재하고 나서야 정본.

## 5) 강제 게이트 (Enforcement — 문구가 아니라 이게 지킨다)
| 게이트 | 경로 | 발동 |
|---|---|---|
| 전체 게이트 | `shared/check_refs.py` | 커밋 전 rc=0 필수(경로실존·거울·토큰락·아이콘 SSOT·소머리·폴오버…) |
| 세션 주입 | `.claude/hooks/design_digest.py` | SessionStart/UI턴 — 계약+이 인덱스 자동 주입 |
| 저장 게이트 | `.claude/hooks/design_gate.py` | UI 파일 저장 즉시 check_refs(위반 exit 2) |
| 커밋 훅 | `.githooks/pre-commit` | check_refs 강제(`core.hooksPath` 자동 설정) |
| 타 모델용 | `AGENTS.md` | Claude 외 도구에도 동일 규칙 |

*(운영자 260704 규칙2 신설 지시 · 규칙1↔규칙2↔훅 3점 동기 — 한쪽만 고치면 드리프트)*
