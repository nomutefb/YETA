# 원장 = append-only 로그. 두 세션이 각자 파일 말미에 항목을 붙이는 구조라, 병렬로 돌면
# **매 병합마다** 같은 자리에서 충돌한다(260726 실측 = 연속 3회 전부 충돌). 그때마다 사람이
# 손으로 푸는데, 그 수동 해결이 번호 오기·항목 유실을 부르는 실제 사고 경로였다.
#
# union = 양쪽 줄을 모두 보존해 자동 병합. append-only 로그에 정확히 맞는 전략이다.
#   ⚠️ 대신 기존 줄을 '수정'한 커밋끼리 만나면 양쪽이 다 남는다(= 눈에 보이는 중복).
#      원장은 정정이 드물고, 번호 겹침은 shared/ledger.py 게이트가 커밋 전에 잡는다.
docs/요구사항_큐.md merge=union
docs/작업이력.md   merge=union
