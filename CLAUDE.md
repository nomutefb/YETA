<!-- SYNC-COMMON-START -->
> 🔁 여기~마커 끝 = 전 레포 공통 골격(자동 전파). **정본 편집 = muteno/nomute-editor에서만** — 타 레포에서 고치면 다음 전파 PR이 덮어써 증발한다. 공통 규칙 변경 지시를 받으면 「nomute-editor에서 고쳐야 전 레포 반영」을 고지하고 멈춰라. 레포 고유 값(경로·게이트·예외)은 마커 밖 **【레포 바인딩】**이 정본. 전파 = nomute-editor의 `.github/workflows/claude-sync.yml`(main 머지 → 전 레포 자동 PR · 마커 25%↑ 축소 = 자동머지 차단·리뷰 강등).

# 노뮤트 공통 골격

친근한 반말. 결론부터. 어려운 말 대신 쉬운 말.

## §0 읽는 법

매 작업 루프 = **0 확인 → A 착수 → B 수행 → C 완료**. 이 문서는 읽을거리가 아니라 체크리스트다.

값이 필요하면 **그 정본 파일을 실제로 열어라.** 열지 않고 쓴 값 = 오류다. 「미확인」 딱지 없이 확인한 척 = 은폐다.

충돌하면 **§0 > A > B > C > D** 순으로 이긴다. 같은 층은 나중 개정이 이긴다. 예외는 지는 조항을 직접 고쳐서만 만든다 — 덮어쓰기용 신규 조항 금지(그 사슬이 비대화의 뿌리다).

본문에는 **현행 값만** 쓴다. 연혁·판례·날짜는 커밋 메시지와 원장에 남긴다(이력은 `git log`가 공짜로 준다).

### §0-1 절대 법칙

1. 이 문서는 이 repository에서 절대적으로 지켜져야 할 핵심 법칙이다.
2. **판정은 C1-⑤ 체크리스트에서 기계적으로 도출한다.** 이번 지시에 **발동된** 항목이 전부 ✅일 때만 '성공', 그 외는 '실패'. **미발동 항목은 N/A**(발동 조건 인용 1줄) — N/A는 실패 사유가 아니다. 실패는 발견 즉시 은폐 없이 알린다. **실패 선언은 작업 포기가 아니라 상태 보고다**(선언 후 계속 수행해도 된다).
3. **은폐 = 보고 시점에 알고 있던 ⬜·⚠·「미확인」을 빠뜨리거나 ✅로 바꿔 적는 것.** 작업 중 발견해 고치고 재검증까지 마친 결함은 실패가 아니다.

## §A 착수 전 — 미통과 시 실행 금지

- **A1 의도** — ① 목적·배경 ② 문제 정의 ③ 초안(가장 짧은 시간에·목적을 완전히 해소할 품질로). **"이렇게 이해했고 이렇게 간다" 1줄 + 완료판정 1줄** 쓰고 착수.
- **A2 표(10칸)** — ①순서 ②내용 ③현황 ④목표 ⑤달성방안 ⑥**예상 소요(상대값만 · 절대 종료시각 예고 금지)** ⑦예상결과 ⑧리스크 ⑨완료여부 ⑩아이데이션. 착수 시 ①~⑧, 보고 C1-④에서 **같은 표에 ⑨** 채워 재게시(표는 하나다 · 사소한 건 비례 축약).
- **A3 대조** — 확정안이 A1을 만족하나. 의문이 있으면 즉시 A1부터 다시.
- **A4 정본 열람** — §R에 걸린 작업은 지목 파일을 **실제로 연다.** 보고에 「정본 영수증 = 연 파일 §섹션 + 근거 1줄」.
- **A5 디자인 착수** — UI를 만지면 그 화면을 **헤드리스 브라우저로 실제 렌더**해 픽셀부터 보고 편집한다(Playwright · `/opt/pw-browsers/chromium` · **curl 금지 = 렌더러가 아니다**). before 스샷 1장을 파일로 남긴다.
- **A6 마감** — 단발 지시 = 받은 지 10분 안에 결론. 평의회·배치가 붙으면 D3 봉투가 이 캡을 대체한다.

> A1~A6이 곧 착수 게이트다(별도 검문 조항 없음 — 규칙 + 규칙의 검문 = 이중층 = 비대화). 각 항을 "했는가?"로 읽어라.

## §B 수행 중

- **B1 창작 금지** — 디자인 코드·색·값은 임의 제작하지 않는다. 착수 1줄에 **「레퍼런스 = X 그대로」를 지목해 100% 모방**(99% = 0%). 값은 【바인딩】의 토큰 SSOT **계승만**(새 hex·px 창작 = 실패). 지목할 레퍼런스가 정 없으면 만들지 말고 B3 시안 선행.
- **B2 정렬** — 디자인에서 가장 중요한 것은 정렬이다. **정렬 = 4분할한 중심점이 같은 가로선·세로선에 있는 것.** 실렌더 `getBoundingClientRect`로 픽셀 실측해 증명한다(오차 ≤1px). 증빙 = 중심좌표 표(요소·centerX/Y·오차px).
- **B3 플레이그라운드** — 발동 = **레퍼런스를 지목 못 하거나 배치 후보가 2안 이상 갈릴 때만.** 미리보기를 고정한 채 요소를 만지는 자기완결 HTML(PC 좌/우 · 모바일 상/하). 선택지는 **대분류 5축 × 축당 10안**으로 조직(총량 쿼터 아님 — 한 요소를 잘게 쪼개 나열 금지). 정립된 값은 '고정' 배지.
- **B4 새 요소** — 화면에 새로 생기는 컨트롤·칩·배지 = ⓐ 그것만 단일 커밋 분리 ⓑ 보고에 ⚠배치(어디·어떤 모양 1줄) ⓒ "이 위치·모양으로 해석했다 — 의도 맞나?"를 먼저.
- **B5 다중 대상** — '전 파일/전 레포에 X 적용' = 맹목 일괄 금지. **대표 1개 먼저 실측**, 갭 있는 데만 적용, 없으면 보고.
- **B6 정본 신설 금지** — 참조할 정본은 【바인딩】에서 찾는다. 없으면 그 레포엔 '없음'이며, 새로 모으거나 만들지 말고 먼저 제안한다(있는 정본을 다시 모으면 라이브·게이트가 끊긴다).

## §C 완료 시

**C1 보고 = 6단 골격 그대로, 항상**(순서 고정 · 사족·장식 금지)

① **질문**(지시 원문 인용) ② **의도파악** ③ **결론: 성공/실패**(볼드 · 완벽 수행이 아니면 '실패') **+ 3줄 초등학생 요약**(문장마다 줄바꿈 · 1줄=성공/실패+무엇, 2줄=뭐가 달라졌나, 3줄=다음 액션 or '없음') ④ **처리 결과**(A2 표에 ⑨ 채워 재게시 + 전→후 비교[디자인 = before/after 스샷] + 머지 여부 + 평의회 결과) ⑤ **수행 체크리스트**(✅증거1줄 / ⚠미검증+사유 / ⬜미수행 / N/A+발동조건 — 행 누락·증거 없는 ✅ = 그 보고 자체가 미완) ⑥ **💡 한 수 1개**(work to do·리스크 포함 · 억지 금지 · 없으면 '없음'+사유 반 줄 · 품질을 절대 떨어뜨리지 않을 것)

> **판정원은 하나다** — C1-⑤가 유일한 성공/실패 근거다(C2~C4는 그 체크리스트의 행일 뿐). 체크리스트는 ④ 끝에 **3칸 표**(항목 | 한 줄 요약 | ✅/⚠/⬜/N-A)로 고정 — O/X 열만 훑으면 결론이 보인다.

- **C2 렌더 영수증(이원 판정)** — 디자인을 바꿨으면 **전·후 각 1장 렌더해 첨부**하거나, 불가 시 **「⚠미실측(코드추정)」 + 못 본 항목 명시.** 둘 중 하나가 없으면 미완이다(추정을 완성으로 포장 금지 — 검증은 가능할 때만, 정직은 항상).
- **C3 머지** — 보고와 함께 Draft PR을 바로 머지한다. 예외 = ⓐ D3 평의회 대상이면 통과 후 ⓑ D2 비가역·과금은 승인 후 ⓒ 자동 전파(sync) PR은 세션이 머지하지 않는다. **'완료' = main 머지 + `git show origin/main:<파일>` read-back 후에만** 선언(완료 시점 예고 금지 — 실측 후 보고만).
- **C4 게이트** — 커밋 전 【바인딩】의 상비 게이트 rc=0 필수. clone·기기마다 1회 훅 활성화 확인(미설정이면 게이트가 **조용히 미실행**되고 "통과했다"는 착각으로 깨진 커밋이 들어간다). 미확인이면 그 작업은 '실패'로 보고한다.

## §D 금지·승인

- **D1 스케줄링 금지** — 지시 없는 예약·크론·백그라운드·자가 체크인·임의 병렬 금지(기계 차단 = 【바인딩】 훅 · 이 문장은 요약이고 훅이 먼저 막는다). **예외 = D3 평의회**(규칙이 시키는 병렬 소환과 그 완료 대기는 위반이 아니다).
- **D2 승인** — 비가역·파괴(데이터·백업 삭제, 히스토리 재작성, 미머지 커밋을 날리는 `git checkout -B`)와 신규 과금 경로는 **실행 전 무조건 묻는다.** 승인은 **채팅 문답만** 인정(팝업 수락 ≠ 승인 · 이미 승인된 일의 분기 선택은 버튼 허용). 리스크 칸에 **롤백 1줄**(revert 대상)을 적는다 — 롤백 경로 없는 변경은 착수하지 않는다.
- **D2-1 기계산출물 손편집 금지** — 스크래퍼·크론·빌더가 생성하는 데이터·거울 파일은 손으로 고치지 않는다(자동커밋과 병합 레이스). 값 변경 = **생성하는 코드**를 고친다. 대상 = 【바인딩】.
- **D3 평의회** — ① 발동 = 사용자가 요청하거나 **【바인딩】의 평의회 대상 경로**를 건드린 경우(자기판단 아님 — 목록에 있으면 무조건, 없으면 소집 안 함). ② 방법 = 서로 다른 관점 **OPUS 5 최고노력 8인 병렬 적대 검증 · READ-ONLY**(커밋·브랜치·PR은 메인 세션만). 소집 전 **`nproc`로 동시 슬롯(코어−2) 실측** — 좌석이 넘치면 {좌석 축소 · 봉투 사전 고지 · **툴 사용 최소화**} 중 택1. ③ 봉투 = **T0 기준 8분 하드스톱 하나**(이중 마감 없음) · 초과분은 폐기가 아니라 **그때까지 회수분으로 수렴**.
- **D3-1 확인·처리 상태**(ack·seen·뮤트·dismiss) = **계정 종속이 기본값** — 기기 localStorage 단독 저장 금지(로컬 = 캐시). 알림·확인 UI는 **설계 시점에** 배선(사후 이식 금지). 구현 경로 = 【바인딩】.
- **D4 시각** — 전부 KST(러너는 UTC → `TZ='Asia/Seoul'` 강제 · 코드에 심을 때도 naive `new Date()`·`utcnow()` 금지).

## §R 라우팅 — 본문엔 지목만, 세부는 열어서 쓴다

| 작업 | 열 파일 |
|---|---|
| 디자인·UI | 【바인딩】 디자인 진입점 → 토큰 SSOT + 컴포넌트 정본 |
| 시안·분기 결정 | 【바인딩】 플레이그라운드 |
| 보고·원장 기재 | 【바인딩】 지시 원장 + 작업 이력(append-only) |
| 커밋 전 검증 | 【바인딩】 상비 게이트 |

라우팅 행은 실경로가 아니라 **바인딩 키**를 지목한다 — 전파돼도 각 레포가 자기 바인딩으로 해석해 경로가 깨지지 않는다.
<!-- SYNC-COMMON-END -->

## 【레포 바인딩】 YETA (레포 고유 · 전파 제외)

### 구조
- `viewer/` = 웹앱 본체(PWA). `index.html` = **값 SSOT(`:root` 디자인 토큰) + 메인 UI** · `call.js` = 음성·전화 · `nm-svg.js` = 아이콘 SSOT · `tokens.css` = 구조토큰 거울(빌드 산출·직접 수정 금지) · `sw.js`·`manifest.json` = PWA · `pg/` = 플레이그라운드 서빙 · `characters/`·`assets/`·`fonts/`
- `functions/api/yeta.js` = 백엔드(Cloudflare Functions) · `apps/yeta/` = 캐릭터 카드·`worlds.json`·`places.json`·`users.json`·`policy.json`·세계관 문서
- `shared/` = `check_refs.py`(전체 게이트) · `models.json`(모델 ID SSOT) + `apply_models.py`(승격 일괄 치환 · 게이트 짝 = `check_model_ids`) · `build_design_mirror.py` · `account_failover.py`
- `docs/` = `절대명령1_포터블.md` · `절대명령2_정본인덱스.md`(위치 SSOT) · `CII_컴포넌트계승인덱스.md` · `디자인방식론_YETA.md` · `브랜드_팔레트_확립본.html` · `reports/`
- `구성도/` = 시각 정본. `00_가이드북_버튼인터랙션.html` · `base.css`(거울·직접 수정 금지) · 확립본들 + `노뮤트_웹UI_공통규약.md`
- `.claude/`(훅·yeta-design 스킬) · `.githooks/`(pre-commit) · `design-tokens.lock`(`:root` 토큰 승인 원장) · `AGENTS.md`(Claude 외 도구 공통 거울)

## 🔒 절대명령 #1 — 디자인 = 루트 토큰 「계승」 또는 「갱신」, 둘 뿐

## 🎨 디자인 값 규칙 (이 레포 강화 조항 — 공통 B1의 구체화 · 디자인 규칙 위상 권위)
디자인 **값**(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나뿐이다.
- **① 계승 = 디폴트, 안 물음** — `var(--token)`. 정확한 토큰이 없어도 **가장 가까운 토큰을 자동 계승**(이 판단도 안 물음).
- **② 갱신 = 근접 토큰조차 없을 때만** — 「새로 신설할지 / 어떤 근접 토큰을 계승할지」 두 선택지로 묻고 승인 후. 토큰 신설 = `:root` 구조 변경 = 기틀 → 평의회(D3) 대상.
- 승인분은 **즉시 편입**: `:root` 추가 → `build_design_mirror.py build` → CII 행 추가 → **`design-tokens.lock` 갱신**. 락 없이 코드에만 박으면 `check_refs.py` rc=1(고아 변수 차단).
- **raw 허용 예외**(물음 없음) = 기하 계산 · 키프레임 안무 % · 광학 1px 보정 · OS 강제(safe-area) · 로컬 z-index · 토큰 없는 축(box-shadow 등). ⚠ 초록 게이트 ≠ 전면 준수.
- 컴포넌트는 값이 아니다 → `docs/CII_컴포넌트계승인덱스.md` 정본 셀렉터 계승(별개 축).

### 바인딩 키
- **서빙 엔트리**(A5) = `viewer/index.html` · 루트 로컬 서빙(헤드리스) · ⚠ 진입 시 잠금화면(unlock 경로 확인 필요)
- **QA경로**(C2) = `viewer/index.html?qa=1#char|list|map|settings` · 대표데이터 = `api/yeta`(로컬 렌더 = 빈/시그니처 폴백)
- **디자인 진입점**(B1) = `viewer/index.html` `:root`(값 SSOT) → `docs/절대명령2_정본인덱스.md`(위치 인덱스 · **충돌 시 그쪽이 이김**) + `docs/CII_컴포넌트계승인덱스.md`(컴포넌트) + `docs/디자인방식론_YETA.md`(방식론) · 완전판 = `docs/절대명령1_포터블.md` §1 · 시각 정본 = `구성도/`
- **거울 금지** = `AGENTS.md` §🎨와 `.claude/hooks/design_digest.py` 주입문은 **파생 거울** — 본문을 복제·역전하지 않는다(드리프트)
- **팔레트 예외** = 없음(생기면 여기 등재)
- **계정 축**(D3-1) = 미배선 — 알림·확인 UI 신설 시 설계 시점에 배선(구현 경로는 그때 여기 등재)
- **플레이그라운드**(B3) = `viewer/pg/`(라이브 URL 제공 정본)
- **지시 원장**(C1) = `docs/요구사항_큐.md` · 작업 이력 = `docs/작업이력.md` · 백업 = `_versions/{yymmdd_HHmm}_{라벨}/`
- **상비 게이트**(C4) = `python3 shared/check_refs.py` + `.githooks` pre-commit
- **평의회 대상**(D3) = `CLAUDE.md` · `viewer/index.html`(`:root` 값 SSOT) · `docs/절대명령2_정본인덱스.md` · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드
- **딥링크**(C3) = 앱 = `/?yeta=1&t=<캐릭터id>`(언락 후 그 방 직행 · 미언락이면 언락 완료에 큐잉) · 시안·문서 = `/pg/<파일>.html`(잠금 없는 정적 경로). **앱 루트(`/`)만 덜렁 주기 금지**

※ 이식 노트: 마커 구간 = 전 레포 공통 골격 — 정본 편집 = muteno/nomute-editor에서만(여기서 고치면 다음 전파 PR이 덮는다). 이 절은 레포 고유(전파 제외).
