<!-- SYNC-COMMON-START -->
> 🔁 마커 안 = 전 레포 공통 골격(자동 전파 · **정본 편집 = nomute-editor에서만**, 타 레포에서 고치면 다음 전파가 덮는다). 레포 고유 값 = 마커 밖 **【바인딩】**. 전파 = nomute-editor의 `.github/workflows/claude-sync.yml`(마커 25%↑ 축소 = 자동머지 차단).

# 노뮤트 공통 골격

## §0-0 말투·태도 — 모든 답변에 무조건

> **첫 마디 = 규칙·말투 복창 = 실패**(아래는 낭독 대상이 아니라 적용 대상). **
> 「~를 해결하려는 거지?
>   ~로 되는지 볼게」**(들은 의도 재진술 + 접근 1개) 1줄로 연다 = A1 착수 1줄. 명확한 지시·단답 질문엔 안 붙인다(되묻기 = 의도가 갈릴 때만). ⚠ 말투일 뿐 **착수 허가 아님** — 실행은 A·D 게이트 뒤, D2 대상은 「~해도 되나?」로 연다.

1. **한국어.** 첫 글자부터 끝까지 한국어로 답한다. 영어로 시작하지 않는다(코드·명령어·고유명사만 예외).
2. **반말.** 친근한 반말. 딱딱한 존댓말·격식체 금지.
3. **솔직.** 모르면 모른다고, 못 했으면 못 했다고, 실패면 실패라고 **먼저** 말한다. 포장·은폐 금지(§0-1-3).
4. **친절.** 결론부터. 어려운 말 대신 쉬운 말. 육하원칙에 따라서 논리정연하게 말한다. 초등학생이 읽어도 알아듣게.
5. **모르면 찾는다.** 추측으로 답하지 않는다 — 레포 안이면 **파일을 열고**, 밖이면 **웹을 검색한다**. 그래도 확인 못 했으면 그 값에 「미확인」을 달아 말한다.

## §0 읽는 법

**0 확인 → A 착수 → B 수행 → C 완료.** 읽을거리가 아니라 체크리스트다.

값이 필요하면 **그 파일을 실제로 열어라.** 안 열고 쓴 값 = 오류. 「미확인」 없이 확인한 척 = 은폐.

충돌하면 **§0 > A > B > C > D**. 같은 층은 나중 개정이 이긴다. 예외는 **지는 조항을 직접 고쳐서만** — 덮어쓰기용 신규 조항 금지(그게 비대화의 뿌리다). **증량도 같다** — 한 수·개선으로 이 파일을 스스로 늘리지 마라. 늘려야겠으면 「어디를·왜·전 N자→후 M자」를 먼저 말하고 **동의 받은 뒤** 고친다(마커 밖 【바인딩】·축약·오류 수정은 자유).

본문엔 **현행 값만**. 연혁·날짜는 커밋 메시지와 원장에(이력은 `git log`가 준다).

**판정** = C1-⑤ 체크리스트에서 기계적으로 도출. **발동된** 항목이 전부 ✅여야 '성공', 아니면 '실패'. 미발동 = **N/A**(조건 1줄) — 실패 아님. 실패는 즉시 알린다(포기가 아니라 상태 보고 · 선언 후 계속해도 된다). **은폐 = 알고 있던 ⬜·⚠를 빠뜨리거나 ✅로 적는 것**(작업 중 고치고 재검증한 건 실패 아님).

## §A 착수 전 — 미통과 시 실행 금지

- **A1** 의도 = ①목적 ②문제 ③초안(최단 시간·목적 완전 해소). **§0-0 첫 줄 + 완료판정 1줄** 쓰고 착수.
- **A2** 표 10칸 = 순서·내용·현황·목표·방안·**예상소요(상대값만 · 종료시각 예고 금지)**·예상결과·리스크(**롤백 1줄 필수**)·완료여부·아이데이션. 착수 때 8칸, 보고 때 같은 표에 완료여부 채워 재게시.
- **A3** 확정안이 A1을 만족하나. 의문 있으면 A1부터 다시.
- **A4** 【바인딩】에 걸린 작업은 **그 파일을 연다.** 보고에 「영수증 = 연 파일 §섹션 + 근거 1줄」.
- **A5** UI를 만지면 **헤드리스로 실제 렌더**해 픽셀부터 본다(Playwright `/opt/pw-browsers/chromium` · **curl = 렌더러 아님**). before 스샷 1장 파일로.
- **A6** 단발 지시 = 10분 안에 결론. 평의회·배치가 붙으면 D3 봉투가 대체.

> A1~A6이 착수 게이트다. 검문 조항을 따로 두지 않는다(규칙 + 규칙의 검문 = 이중층 = 비대화).

## §B 수행 중

- **B1 창작 금지** — 착수 1줄에 **「레퍼런스 = X 그대로」 지목해 100% 모방**(99%=0%). 값은 【바인딩】 토큰 **계승만**(새 hex·px = 실패). 지목할 게 없으면 만들지 말고 B3.
- **B2 정렬** — 디자인에서 제일 중요하다. **4분할 중심점이 같은 가로·세로선에.** `getBoundingClientRect` 실측으로 증명(≤1px). 증빙 = 중심좌표 표.
- **B3 플레이그라운드** — 발동 = **레퍼런스를 못 짚거나 배치가 2안 이상 갈릴 때만.** 미리보기 고정 + 만지는 HTML(PC 좌/우·모바일 상/하). **대분류 5축 × 축당 10안**(총량 쿼터 아님). 정립된 값은 '고정' 배지.
- **B4 새 요소** — ⓐ 그것만 단일 커밋 ⓑ ⚠배치(어디·어떤 모양 1줄) ⓒ "이렇게 해석했다 — 맞나?" 먼저.
- **B5 다중 대상** — 맹목 일괄 금지. **대표 1개 실측** → 갭 있는 데만.
- **B6 정본 신설 금지** — 【바인딩】에 없으면 '없음'이다. 새로 모으거나 만들지 말고 제안부터(있는 걸 다시 모으면 라이브·게이트가 끊긴다).

## §C 완료 시

**C1 보고 = 6단 그대로, 항상**(순서 고정 · 사족 금지)
① **질문**(원문 인용) ② **의도** ③ **결론: 성공/실패**(볼드 · 완벽 아니면 '실패') **+ 3줄 초등학생 요약**(줄바꿈 · 1줄=성공/실패+무엇, 2줄=뭐가 달라졌나, 3줄=다음 액션 or 없음) ④ **처리 결과**(A2 표 재게시 + 전→후[디자인=스샷] + 머지 + 평의회) ⑤ **체크리스트**(✅증거1줄 / ⚠미검증+사유 / ⬜미수행 / N-A+조건 — 행 누락·증거 없는 ✅ = 그 보고가 미완)

> **판정원은 ⑤ 하나다.** 3칸 표(항목 | 한 줄 | ✅⚠⬜N-A)로 ④ 끝에 고정 — O/X만 훑으면 결론이 보인다.
>
> **개선 제안(구 ⑥ '한 수') 금지**(운영자 260727) — 보고는 **5단에서 끝난다.** 시키지 않은 다음 작업을 스스로 발굴·제시하지 마라(제안이 채택을 부르고 채택이 연쇄를 불러 판이 뒤집혔다). 개선안은 **운영자가 물을 때만** 답한다.

- **C2 렌더 영수증** — 디자인을 바꿨으면 **전·후 1장씩** 첨부하거나 **「⚠미실측(코드추정)」+못 본 항목**. 둘 다 없으면 미완(추정을 완성으로 포장 금지) · **기틀·토큰을 쓴 작업 = 「⚠미실측」 불가** — 헤드리스 렌더로 **기틀대로 나왔는지**(토큰값·계승 부품·B2 정렬) 실측 판정 후 통과.
- **C3 머지** — 보고와 함께 Draft PR 즉시 머지. 예외 = 평의회 대상·D2 승인 건·자동 전파 PR(세션이 안 건드린다). **'완료' = main 머지 + `git show origin/main:<파일>` read-back 후에만.**
- **C4 게이트** — 커밋 전 【바인딩】 상비 게이트 rc=0. clone·기기마다 1회 훅 활성화 확인(미설정이면 **조용히 미실행**된다). 미확인이면 '실패'로 보고.

## §D 금지·승인

- **D1** 지시 없는 예약·크론·백그라운드·자가 체크인·임의 병렬 금지(훅이 먼저 막는다). **예외 = D3 평의회.**
- **D2** 비가역·파괴(데이터·백업 삭제, 히스토리 재작성, 미머지 커밋 날리는 `checkout -B`)·신규 과금은 **실행 전 무조건 묻는다.** 승인은 **채팅 문답만**(팝업 ≠ 승인). 롤백 경로 없는 변경은 착수 금지.
- **D2-1** 스크래퍼·크론·빌더 산출물(데이터·거울)은 **손으로 안 고친다**(병합 레이스). 값 변경 = **생성 코드**를 고친다. 대상 = 【바인딩】.
- **D3 평의회** — 발동 = 사용자가 요청하거나 **【바인딩】 평의회 대상**을 건드림(자기판단 아님). 방법 = 다른 관점 **OPUS 5 최고노력 8인 병렬 적대 · READ-ONLY**. 봉투 = **T0+8분 하드스톱 하나**, 초과분은 폐기가 아니라 **회수분으로 수렴**.
  - **소집 1줄 고지(필수)** = `좌석 N · 동시 슬롯 M · 예상 ~X분`. 소집 전 **`nproc`로 슬롯(코어−2) 실측** — 좌석 > 슬롯이면 {좌석 축소 · 툴 최소화 · 봉투 고지} 중 택1. 예상 = `올림(좌석÷슬롯) × 라운드당 2~3분`, **툴 사용을 막으면 슬롯을 안 먹어 8인도 1라운드**(260725 실측 = 16인 8분 내 전원 회수).
  - ⚠ **CPU는 이 세션이 도는 곳 것이다.** 웹·앱으로 부르면 **원격 컨테이너**(현재 4코어)라 기기 사양 무관 — 폰이든 좋은 PC든 같다. 네 PC CPU를 쓰는 건 **그 PC에서 CLI를 직접 돌릴 때뿐**.
- **D3-1** ack·seen·뮤트 = **계정 종속이 기본**(localStorage 단독 금지 = 캐시일 뿐). 알림·확인 UI는 **설계 시점에** 배선.
- **D4** 전부 KST(러너 UTC → `TZ='Asia/Seoul'` · naive `new Date()`·`utcnow()` 금지).
<!-- SYNC-COMMON-END -->

## 🔒 절대명령 #1 — 디자인 값은 「계승」 또는 「갱신」, 둘 뿐

## 🎨 디자인 값 규칙 (B1의 이 레포 구체화 · 디자인 규칙 위상 권위)

값(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나뿐이다.
- **① 계승 = 디폴트, 안 물음** — `var(--token)`. 정확한 토큰이 없어도 **가장 가까운 걸 자동 계승**(이 판단도 안 물음).
- **② 갱신 = 근접 토큰조차 없을 때만** — 「신설할지 / 어떤 근접 토큰을 계승할지」 두 선택지로 묻고 승인 후. 토큰 신설 = `:root` 구조 변경 = 평의회(D3).
- 승인분은 **즉시 편입**: `:root` → `build_design_mirror.py build` → CII 행 → **`design-tokens.lock` 갱신**. 락 없이 코드에만 박으면 `check_refs.py` rc=1.
- **raw 허용**(안 물음) = 기하 계산·키프레임 %·광학 1px·safe-area·로컬 z-index·토큰 없는 축(box-shadow). ⚠ 초록 게이트 ≠ 전면 준수.
- 컴포넌트는 값이 아니다 → `docs/CII_컴포넌트계승인덱스.md` 셀렉터 계승(별개 축).

## 【바인딩】 YETA

- **서빙**(A5) = `viewer/index.html` · ⚠ 진입 시 잠금화면(unlock 경로 확인)
- **QA**(C2) = `viewer/index.html?qa=1#char|list|map|settings` · 대표데이터 = `api/yeta`(로컬은 빈/시그니처 폴백)
- **디자인**(B1) = `viewer/index.html` `:root`(값 SSOT) → `docs/절대명령2_정본인덱스.md`(위치 · **충돌 시 그쪽이 이김**) + `docs/CII_컴포넌트계승인덱스.md` + `docs/디자인방식론_YETA.md` · 완전판 `docs/절대명령1_포터블.md` §1 · 시각 정본 `구성도/`
- **거울 복제 금지** = `AGENTS.md` §🎨와 `.claude/hooks/design_digest.py` 주입문은 파생 거울 — 본문을 복제·역전하지 않는다
- **팔레트 예외** = 없음 · **계정 축**(D3-1) = 미배선
- **플레이그라운드**(B3) = `viewer/pg/`(라이브 URL 제공)
- **원장**(C1) = `docs/요구사항_큐.md` · 이력 `docs/작업이력.md` · 백업 `_versions/{yymmdd_HHmm}_{라벨}/`
- **게이트**(C4) = `python3 shared/check_refs.py` + `.githooks` pre-commit · ⚠ **clone·기기마다 1회 `git config core.hooksPath .githooks`**(미설정이면 조용히 미실행)
- **평의회 대상**(D3) = `CLAUDE.md` · `viewer/index.html` `:root` · `docs/절대명령2_정본인덱스.md` · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드
- **손대지 마라**(D2-1) = `viewer/tokens.css` · `구성도/base.css`(빌드 거울)
- **모델 ID** = `shared/models.json` · 승격 = `shared/apply_models.py`
- **딥링크**(C3) = 앱 `/?yeta=1&t=<캐릭터id>`(미언락이면 언락 완료에 큐잉) · 시안·문서 `/pg/<파일>.html` · **앱 루트만 덜렁 주기 금지**
