<!-- SYNC-COMMON-START -->
0. [위계] 마커 안 = 전 레포 공통(정본 = nomute-editor · 타 레포 수정은 전파가 덮음). 마커 밖 레포 고유 절이 우선하고, 고유 절의 '절대 금지'는 승인받아도 하지 않는다. 이 블록을 스스로 늘리지 않는다.
1. [말투] 한국어 반말(대화만 — 산출물·커밋·PR은 레포 규격). 결론부터 · 실패는 실패라고 먼저 · 막히면 대안을 준다.
2. [의도] 사용자의 말에서 의도를 파악해 그걸 해결한다. 최단시간·저리스크·고완성 — 별거 아닌 일에 30분~1시간 뱅글뱅글 돌지 않는다. 오래 걸릴 일은 먼저 양해를 구한다.
3. [검색] 불확실하면 파일을 열거나 웹을 찾는다(확인은 읽기 전용). 추측 금지 · 끝내 못 확인한 값 = 「미확인」.
4. [디자인기틀] UI 작업 전 디자인 정본(바인딩 지정 파일)을 먼저 연다. 지정 없으면 물어본다. 입맛대로 만들어 꽂는 임의 디자인 = 최악.
5. [전후] UI 변경은 전후(before→after)를 보여준다 — 시각 변경은 html로.
6. [픽셀실측] 정렬 디자인은 실렌더에서 잰다 — 기하(픽토·글자 중심 = 컨테이너 4분할 중앙점 + 형제 같은 선)와 광학(잉크 중심) 두 축. 대상 = 손댄 요소가 아니라 그 정렬 줄 전체 · 동급은 동급끼리(헤더는 헤더끼리) 같은 선·규격 · 같은 급 도형끼리 글자 크기 동일 · 딴 표면에 있는 같은 컴포넌트의 상태 스타일(active 등)은 계승. 충돌 시 중앙점이 이기고 「기존 부채」는 면제 사유가 아니다. 잰 축은 미달 포함 전부 한 표로 보고. 판정 Δ≤0.5px · 십자선 캡처를 [전후]에 첨부.
7. [플레이그라운드] 중규모 이상 UI 시안 = 50개 이상 선택자로 만지는 플레이그라운드 HTML로 고르게 한다(기틀 이탈 금지).
8. [평의회] 기틀 문서·구조·전반급 변경만 opus effort-max 8석 적대 검증(READ-ONLY · 소집 전 1줄 고지). 국소 변경 발동 금지.
9. [머지] 작업 끝 = 머지까지: origin/main 대조 → push → PR(ready) → 체크 초록 → merge → read-back. 멈추고 묻기 = 충돌·게이트 실패/미확인·비가역·평의회 대상·레포 금지 파일. claude-sync·남의 PR 무접촉 · main 직접 push·force 금지.
10. [보고] 딱 3줄: ①결과(성공 = read-back 후만) ②전후 핵심 ③머지 여부 + 다음 액션(없으면 '없음'). 부록 = 증빙·링크만.
※ 인용은 번호 대신 [키].
<!-- SYNC-COMMON-END -->

## 🔒 절대명령 #1 — 디자인 값은 「계승」 또는 「갱신」, 둘 뿐

## 🎨 디자인 값 규칙 (B1의 이 레포 구체화 · 디자인 규칙 위상 권위)

값(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나뿐이다.
- **① 계승 = 디폴트, 안 물음** — `var(--token)`. 정확한 토큰이 없어도 **가장 가까운 걸 자동 계승**(이 판단도 안 물음).
- **② 갱신 = 근접 토큰조차 없을 때만** — 「신설할지 / 어떤 근접 토큰을 계승할지」 두 선택지로 묻고 승인 후. 토큰 신설 = `:root` 구조 변경 = 평의회(D3).
- 승인분은 **즉시 편입**: `:root` → `build_design_mirror.py build` → CII 행 → **`design-tokens.lock` 갱신**. 락 없이 코드에만 박으면 `check_refs.py` rc=1.
- **raw 허용**(안 물음) = 기하 계산·키프레임 %·광학 1px·safe-area·로컬 z-index·토큰 없는 축(box-shadow). ⚠ 초록 게이트 ≠ 전면 준수.
- 컴포넌트는 값이 아니다 → `docs/CII_컴포넌트계승인덱스.md` 셀렉터 계승(별개 축).

## 【바인딩】 YETA

- **서빙**(A5) = `viewer/index.html` · ⚠ 진입 시 잠금화면(unlock 경로 확인)
- **QA**(C2) = `viewer/index.html?qa=1#char|list|map|settings` · 대표데이터 = `api/yeta`(로컬은 빈/시그니처 폴백)
- **디자인**(B1) = `viewer/index.html` `:root`(값 SSOT) → `docs/절대명령2_정본인덱스.md`(위치 · **충돌 시 그쪽이 이김**) + `docs/CII_컴포넌트계승인덱스.md` + `docs/디자인방식론_YETA.md` · 완전판 `docs/절대명령1_포터블.md` §1 · 시각 정본 `구성도/`
- **거울 복제 금지** = `AGENTS.md` §🎨와 `.claude/hooks/design_digest.py` 주입문은 파생 거울 — 본문을 복제·역전하지 않는다
- **팔레트 예외** = 없음 · **계정 축**(D3-1) = 미배선
- **플레이그라운드**(B3) = `viewer/pg/`(라이브 URL 제공)
- **원장**(C1) = `docs/요구사항_큐.md` · 이력 `docs/작업이력.md` · 백업 `_versions/{yymmdd_HHmm}_{라벨}/`
- **게이트**(C4) = `python3 shared/check_refs.py` + `.githooks` pre-commit · ⚠ **clone·기기마다 1회 `git config core.hooksPath .githooks`**(미설정이면 조용히 미실행)
- **평의회 대상**(D3) = `CLAUDE.md` · `viewer/index.html` `:root` · `docs/절대명령2_정본인덱스.md` · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드
- **손대지 마라**(D2-1) = `viewer/tokens.css` · `구성도/base.css`(빌드 거울)
- **모델 ID** = `shared/models.json` · 승격 = `shared/apply_models.py`
- **딥링크**(C3) = 앱 `/?yeta=1&t=<캐릭터id>`(미언락이면 언락 완료에 큐잉) · 시안·문서 `/pg/<파일>.html` · **앱 루트만 덜렁 주기 금지**

- **[픽셀실측] 상비 측정기** = `shared/measure_align.js`(nomute-editor 원본 그대로 이식 · 260731 — 개조 금지 · 갱신 = 원본에서 재복사) — `node shared/measure_align.js <페이지.html> <셀렉터…> [--prep js]` → 기하(픽토Δ·정렬선Δ) 하드 판정(≤0.67) + 광학 잉크Δ 참고 + 십자선 캡처([전후] 증빙). 서빙 루트 = viewer/ 자동. 수동 실행 전용 · 훅 편입 금지.
