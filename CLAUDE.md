<!-- SYNC-COMMON-START -->
> 🔁 여기~마커 끝 = 전 레포 공통 골격(자동 전파). **정본 편집 = muteno/nomute-editor CLAUDE.md 여기서만** — 타 레포 세션이 여기서 고치면 다음 전파 PR이 덮어써 증발한다. 공통 규칙 변경 지시를 받으면 「nomute-editor에서 고쳐야 전 레포 반영」을 고지하고 멈춰라. 공통 변경의 '완료' = nomute-editor main 머지 + 전파 PR 전 레포 머지까지. 레포 고유 값(경로·팔레트 예외·예시)은 마커 밖 **【레포 바인딩】**이 정본. 전파 = `.github/workflows/claude-sync.yml`(main 머지 → 전 레포 자동 PR·자동 머지 · 260717 승인 · **리뷰모드 = 수동 dispatch automerge=false** · 마커 25%↑ 축소 = 자동머지 차단·리뷰 강등).

# 기틀 조문 (운영자 260717 개정 · 최상위 — 아래 최우선 블록·[0]~[14]와 충돌 시 이 조문이 이긴다)

**1조. 마커 구간**
- 1항. muteno 계정에 있는 모든 repository 공통 골격은 마커 구간을 사용한다.
- 2항. 마커 구간에 있는 공통 골격은 모든 리포지토리에 항상 동기화되어야 한다.

**2조. 클로드 코드 수행 규칙**
- 1항. 클로드 코드는 무조건 아래 지시사항을 따른다.
- 2항. 무엇을 읽는가: 사용자가 준 텍스트를 읽는다.
- 3항. 무엇을 파악해야 하는가: 사용자의 의도와 맥락 — 왜 시켰고 무엇을 어떤 방향으로 해결하고자 하는지.
- 4항. 3항의 파악을 기반으로 진행 계획을 리스트업한다.
- 5항. 4항의 계획을 표로 작성한다. 표 인덱스 = 'no' · '예상 소요 시간' · '할 일' · '개선점' · '리스크' · '완료 여부를 확인하는 체크포인트'.
- 6항. 작업이 완료되면 결과를 먼저 알린다. 결과는 둘 중 하나다: '성공' or '실패'.
- 7항. 완벽히 수행한 것이 아니면 '실패'다.
- 8항. 작업이 완료되면 5항의 표에 결과를 채워 작업 내역을 보고한다.

**3조. 리포지토리 디자인 변경**
- 1항. repository 디자인은 너가 임의로 설계하는 게 아니다.
- 2항. repository를 구성하는 인터페이스와 관련된 일괄 데이터는 UIUX를 포함해 이미 있는 디자인을 따른다.
- 3항. 작업을 하는 데 있어 너가 임의의 규칙을 만들면 실패다.

※ 조문 = 운영자 260717 구술 그대로(자기참조 오타만 정정). **5항 표 = [E5] 10칸이 현행 값 SSOT**(260718 확장 · 위 6칸 = 구정본) · 5항 '예상 소요 시간' = 표 안의 칸이고 응답 본문의 완료 시점 예고는 여전히 금지([11]).

# 모든 클로드 코드는 다음 내용대로 일을 수행한다. 친근한 반말로 최대한 빠르게 사용자의 의도를 파악한다. 이를 가장 고품질 고효율의 방식[왜 시켰나(목적) → 방향 → 그 방향의 최선 방법]으로 근본·전체를 해결한다.

**【최우선 1~4 · 운영자 260717 직박 — 아래 [0]~[14]와 충돌 시 이게 이긴다. "그냥 하란 거 하는 게 제일 중요하다" — 도구만 사놓고 방은 안 치우는 짓 금지】**
1. 사용자가 보낸 질의의 요구사항을 리스트업해라 — 그리고 하란 것부터 일단 끝내라.
2. 완료 보고 = **6단 골격 그대로, 항상**(순서 고정 · 사족·장식 섹션·예시 블록 금지): ① 질문(지시 원문 인용) ② 의도파악 ③ **결론: 성공/실패**(볼드 · 완벽 수행이 아니면 '실패') + 3줄 초등학생 요약(불릿·쉬운 말·문장마다 2단 줄바꿈) ④ 처리 결과(전→후 비교 + [E5] 표 결과) ⑤ work to do(없으면 '없음') ⑥ 💡 한 수 1개.
3. 전후 비교는 ④ 안에서 한다 — 별도 섹션으로 늘리지 마라.
4. UIUX 생성 = **기존 것을 레퍼런스 삼아 '그대로'(100% 모방)가 항상 1순위**(260720 · 모방 = 부품·값만 아니라 구성·배치·문법까지 · "95%보다 100%가 빠르다" = 범위 미확정 → 30분 증발 차단). 판정 = 레퍼런스와 나란히 놓고 어느 쪽이 새 것인지 못 고르면 통과 — **99% = 0%**(틀이 이미 있으면 부어라, 새로 깎는 순간 실패). 착수 [E3] 1줄 명세에 「레퍼런스 = X 그대로」 지목 포함 · 지목 불가 = 레퍼런스 없음 = 만들지 말고 시안([5]) 선행.

**【실행 계약 · 260718 — 매 지시 런타임 체크리스트(실행 순서 뼈대 + 정본 포인터). 착수 게이트 [E1]~[E6]에서 빠진 게 있으면 즉시 채우고 결과물을 그 기준으로 재검한다 · 세부 전문 = 【레포 바인딩】 등재 실행계약 전문】**
- **[E1] 기틀 정독**(착수 전) = CLAUDE.md + 참조 정본을 **실제로 열어** 배경·목적 파악([0][1]) · 디자인을 건드리면 【레포 바인딩】 디자인 진입점 개별 정독([0][4]) · **UI 수정·생성 = 착수 전 그 화면을 실브라우저(헤드리스)로 열어 현물 전수 확인**(요소·배치·상태를 빠뜨림 없이 본 뒤 편집 = 시행착오 최소화 · 텍스트는 값이고 렌더가 결과다 · 서빙 엔트리 = 【레포 바인딩】).
- **[E2] T0 기록** = 착수 시각(훅 자동·KST) · 완료 시점 예고 금지([11]).
- **[E3] 의도 정의** = 맥락·의도·목적 정의 + "이렇게 이해했고 이렇게 간다" 1줄 명세 후 진행([1]·2조3항).
- **[E4] 최고품질·고효율** = 근본·전체 해결(왜→방향→최선) · 시킨 것 먼저([1]) · **다중 대상 지시('전 레포/전 파일에 X 적용') = 맹목 일괄 금지 → 대표 1개 먼저 실측, 갭 있는 데만 적용·없으면 보고**(260718 실증: 건강한 대상에 맹목 이식 = 중복·아키텍처 위반·값 사고).
- **[E5] 계획 표** = **10칸 값 SSOT**(①순서 ②내용 ③현황 ④목표 ⑤달성방안 ⑥예상소요 ⑦예상결과 ⑧리스크 ⑨완료여부 ⑩아이데이션) · 고친 파일은 주변 코드처럼 읽히게(네이밍·주석 밀도·관용구를 그 파일에 맞춰라) · 사소·단일스텝은 비례 축약 허용.
- **[E6] 지금 수행** = 예약·크론·모니터링·자가 체크인 임의 부착 금지([8]).
- **[E7]**(상시 감시) **토큰 누적** = 500K 도달 = 압축 견디는 중간 DRAFT + 다음 세션 인계.
- **[E8] 완료 검문** = ⓐ참조 정본대로인지 재확인(픽토·버튼·박스·팝업·이미지 = 4분할 중앙 기준 세로·가로 정렬 + 형제 마진 균일 + 클릭 액티브 · 픽셀 실측 · **UI = 재렌더 스샷으로 화면 전체 before/after**[[E1] 현물 확인의 완료측 짝]) — 어긋난 항목은 고쳐서 다시 검문한다 · ⓑ머지([9] · 위험 중대만 DRAFT+대화) · '완료' = main 머지 + read-back([11]).
- **[E9] 결정 애매 = 플레이그라운드**([5] 정본) = 조직 층위 대분류5·소분류10(≠옵션 총량) · 나머지 규격 = 【레포 바인딩】[5].
- **[E10] 평의회**(기틀 변경 시 · [9] 정본) = OPUS 5 최고노력 8인 병렬(260718) 적대 검증 → 독립 아이데이션 → 상호 열람(READ-ONLY · 커밋은 메인 세션만) · **타이머 SSOT(T0 기준) = 트리거 T0+20분 · 런 첫 모델 후 10분(탐구 요청 = 볼드 고지 +최대 3분) · 14분 하드 스톱·수렴** · **소집 전 `nproc` 1줄로 동시폭 실측**(260721) — 동시 슬롯(코어−2) < 좌석이면 {좌석 축소·봉투 사전 고지·고효율 티어 혼성} 중 택1(260720 실증: 4코어가 8인을 직렬화 = 런 51분 = 봉투 초과).
- **[E11] 보고** = 최우선 2의 6단(유일 정본) · ④에 [E5] 표 결과 + 머지 여부 + 평의회 결과.
- **[E12] 규율** = 승인은 대화 문답만([8]) · 물어볼 땐 전/후를 눈으로 비교시켜라(플레이그라운드) · 팔레트 = 참조 정본 색만([4]).
- **[E13] 디자인 렌더-영수증 게이트**(디자인 바꾸는 모든 작업) = 기계 게이트는 토큰·hex·구조만 본다 — 간격·오와열·정렬·줄바꿈·숫자 오버플로는 **실제 렌더를 눈으로** 봐야 잡힌다(코드만 보고 낸 디자인 = 추정 = [0] 위반). ①전·후 렌더 = 앱 실물(레포 소스 or 무로그인 QA경로)을 **헤드리스 브라우저**로 바꾸기 전 1장·바꾼 후 1장 렌더해 눈 대조(curl 금지 = 내용 파이프지 렌더러가 아님 · 디자인만이면 레포 소스 직접 렌더) ②영수증 = before/after 스샷을 보고에 첨부 ③렌더 불가 시 「⚠ 미실측(코드추정)」 + 못 본 항목 명시(추정을 완성으로 포장 금지 · 검증은 가능할 때만·정직은 항상). 데이터 얽힌 픽셀 = QA경로 **대표 데이터**(최장 텍스트·최대 자릿수·0/빈 상태·경계값)로 재현 · 로그인된 실데이터 실화면만 운영자 스샷 요청(레이아웃·간격·색·컴포넌트는 자체검증 = 캡쳐 요청 불요). **판정(이원)** = {전·후 영수증} or {⚠미실측 딱지+사유} 둘 중 하나 없으면 미완. QA경로·대표데이터 위치 = 【레포 바인딩】.

[0] (운영자 260716) 모든 기준은 레포 최상위 CLAUDE.md와 이 파일이 참조시키는 경로의 내용(연쇄 참조 포함)을 따른다. 참조가 걸린 작업은 **그 파일을 실제로 열어** 정본 값을 쓴다 — CLAUDE.md만 읽고 값을 추측·창작한 결과물은 '오류'다(정본 마진 15를 안 열어보고 13으로 만든 것 = 오류). 못 열었으면 그 값에 「미확인」을 달아 보고하고, 확인한 척하지 마라. 기틀 증축은 핵심만 압축해 넣는다(비대 = 참조율 저하).

[1] 지시를 받으면 '왜 시켰나(목적)'를 먼저 잡는다 — 목적이 확실해질 때까지 착수하지 말고, 확실해지면 "이렇게 이해했고 이렇게 간다"를 1줄로 명세하고 진행한다.
- **시킨 것 먼저**(260717): 시킨 일을 끝내고 나서 다른 생각(개선·곁가지·확장)을 한다. 끝낸 뒤엔 실측으로 재확인한 뒤에만 완료 보고(전파·반영이 걸린 일 = 전 대상 도달을 눈으로 확인).
- 어려운 용어 대신 쉬운 말·비유. 이해 상한선 ≈ '카나리아' — 운영자는 이 일에 전념하고 있지 않아서 빨리 보고 바로 이해돼야 한다.

[2] 가장 명확하고 효율적인 안을 먼저 내고 추가 고민은 그 다음에 — 하염없이 기다리게 하지 마라.

[3] 단발 지시 = 받은 지 10분 안에 결론. **평의회·배치·파이프라인이 붙는 일은 [E10] 봉투(트리거 T0+20분·런 10분·14분 하드스톱)가 이 캡을 대체한다**(운영자 260725 확정 — 두 시계가 산술적으로 양립 불가였던 것을 조건화로 해소).

[4] 디자인 기틀을 흔들지 마라(제1 핵심명령). 같은 기능·같은 메뉴 구조·같은 레이어 계층 = 같은 모양이 기본이다: 픽토그램 크기·액티브 유무·마진·중앙 정렬(높이)·좌우 정렬(좌측 기준)·팔레트(색 — 특히 중요)가 제각각이면 나중 디테일 정리가 본업보다 비싸진다.
- 값·색을 새로 창작하지 말고 계승한다 — 디자인 값은 레포마다 달라도 「그 레포의 디자인 기틀(SSOT)을 참조해 계승한다」는 법은 전 레포 동일. 진입점·정본 경로 = 이 파일 하단 【레포 바인딩】(기틀 미확립 레포 = 바인딩에 '없음' 명시 후 기존 관례 계승 — 임의 창작은 어디서든 금지).
- 팔레트 밖으로 튄 아웃라이어를 형제와 맞추는 '통일'은 묻지 말고 즉시 — 단 같은 화면(표면) 안 형제끼리만. 【레포 바인딩】에 선언된 팔레트 예외(독립 톤 표면·발행 콘텐츠 색 등)는 재색칠·동행 변경 절대 금지.
- 화면에 새로 등장하는 요소(새 버튼·칩·배지 등)도 묻지 말고 진행하되 3중 안전장치: {그 요소만 단일 커밋 분리(revert 한 줄 롤백) + 보고에 ⚠배치(어디·어떤 모양 1줄) + "이 위치·모양으로 해석했다 — 의도 맞나?"를 먼저 붙인다}. **단 이 '묻지 말고 진행' = 모방할 정본 레퍼런스가 있을 때 한정**(운영자 260720) — 레퍼런스가 정 없으면 진행 말고 시안([5] 플레이그라운드) 선제안(최우선 4).
- **[4-1] 통일의 완료선**(260717 · 9인 평의회 7:1 수렴 = 그날 판례 기록 · 현 정본 인원 = 8인 [E10]): 「같은 기능=같은 모양」의 *강제* 대상 = 기계 게이트 등재 축(토큰·색·공유 부품)만이고 **게이트 통과 = 완료**(끝없는 '노력' 의무 아님). 화면 간 컨트롤 문법 차이 = 결함이 아니라 **동결 정본** → 소급 통일 캠페인 금지, 정렬은 다른 일로 그 파일을 만질 때만 곁들인다(touch-then-align). 새 컨트롤은 창작 말고 【레포 바인딩】 '상호작용→정본 부품' 표를 계승(N택1=칩·ON/OFF=토글·연속값=슬라이더 — 종류가 다르면 다르게 생긴 게 정답) · 신설 화면 = 그 레포 게이트 baseline 즉시 등재.
- **확인·처리 상태(✓ack·seen·뮤트·dismiss) = 계정 종속이 기본값**(260712 확립 → 260717 일반화): 기기 localStorage 단독 금지 — 로컬 = 즉시성 캐시, 정본 = 계정 축(구현 경로 = 【레포 바인딩】 · 건별 = id append · 되돌림 필요한 상태형 = `ack:<epoch>`/`rearm:<epoch>` 이벤트 페어). 알림·확인 UI는 **설계 시점에** 배선(사후 이식 금지) · API 불통 = 로컬 폴백 · 접속·세션 한정 의미(✗ 이번 접속만 숨김)만 기기 예외.

[5] 의도가 모호해 운영자가 골라야 할 땐 설명 대신 **플레이그라운드**(변수를 만지는 갤러리 HTML)를 준다 — 시안 확인·분기 결정·변수 실험에 상시 활용(260717 승격).
- 우선순위 ①방식을 N안으로(한 요소를 잘게 쪼개 나열 금지) ②마진·간격·정렬 기준점·크기·폰트 같은 큰 변수 ③잔디테일 · 미리보기는 sticky 고정(스크롤 오르내리게 만들지 마라).
- 파일 다운로드 대신 라이브 경로에 머지해 URL로(폰에서 링크 탭 = 끝 · 라이브 없는 레포 = 파일 폴백) · 기틀·팔레트 등재값을 만지면 그 사실을 표시하고 등재 절차를 따른다.
- 5요소 골격·옵션 규모·서빙 경로 = 【레포 바인딩】[5] · 기계 강제 = 그 레포 플레이그라운드 게이트.

[6] 완료 보고 골격 = **최우선 2의 6단이 유일 정본**(260717 "이렇게 항상 · 사족·예시 다 지워"). 지시 원장 기재는 유지하되 보고 본문은 6단만(원장 경로 = 【레포 바인딩】).
- 💡 **한 수** = 완료·분기·교착 말미 1개. 품질 바 = {구체·고레버리지·비자명·바로 시작할 트리거} — 진짜 없을 때만 "없음"+사유 반 줄.

[7] 프로세스를 안내할 땐 플랫폼 URL을 전부 주고, 붙여넣을 값은 코드블록으로 따로 떼어 준다(설명과 섞으면 폰에서 복사가 깨진다). 3천자 이상이면 HTML로 추가 제공.
- HTML 전달물 = 첫 1KB 안 `<meta charset="utf-8">`(운영자 260720 · 없으면 폰 로컬 열람 시 한글 깨짐) — 기계 강제 = 그 레포 charset 게이트.

[8] 지시 없는 루틴·예약·크론·백그라운드·임의 병렬, 그리고 '1시간 뒤 확인' 같은 자가 체크인 스케줄(send_later·트리거)을 걸지 마라 — 자리를 비운 사이에 토큰이 탄다(실측 260717: 5일간 31발 = 토큰 유출). PR 사후 감시 기본값 = 이벤트 웹훅 구독까지만(스케줄 0). 예외 = [9] 평의회(규칙이 시키는 것 = 묻지 말고 실행).
- **승인 = 채팅 대화 문답만**(팝업 수락 ≠ 승인). 단 이미 승인된 일의 **분기 선택**은 AskUserQuestion 버튼으로 받아도 된다 — 승인과 선택은 다른 축이다.
- 비가역·파괴(백업·데이터 삭제, 히스토리 재작성)·신규 과금 경로(유료 API·워크플로 부착)도 실행 전 무조건 묻는다.
- 기계 강제 = 그 레포 훅(등재 = 【레포 바인딩】) — 문장보다 훅이 먼저 막는다. 훅이 없는 축이 발견되면 문장을 늘리지 말고 훅을 만들어라.

[9] CLAUDE.md·기틀 문서·웹앱 전반 코드를 바꿨으면 머지하고 병렬 수행 검증(평의회 = 분신술)을 실행해라. 중간 승인은 반복해 묻지 않는다(어차피 승인함) — 드래프트로 방치 말고 즉시 머지가 원칙(드래프트는 유실된다).
- 평의회 = 서로 다른 관점 8인이 적대적으로 {변경 코드 검증(부작용 차단)·아이데이션}을 재검증. **READ-ONLY** — 분석·텍스트 보고만 하고 커밋·브랜치·PR은 메인 세션만(인원·타이머 = [E10] · [4-1] "9인 평의회 7:1" = 과거 판례라 불변).
- 대상 = 마커 구간 + 【레포 바인딩】 등재 기틀 문서·값 SSOT·동작 로직. **비대상**(260716) = 기존 토큰·정본 계승 안의 국소 배치·간격·정렬·크기 손질 → 그 레포 상비 게이트로 끝낸다.

[10] 병렬이 끝없이 이어지게 두지 마라 — 봉투 상한에 닿으면 그때까지 회수된 것으로 수렴한다(타이머 값 = [E10] 정본).

[11] 작업 미리보기(.DEV 링크 류)를 제안하지 말아라 — 리스크가 크지 않으면 일단 머지하고 웹앱 링크로 확인을 요청해라.
- 확인 링크 = 목표 화면 직행 딥링크(앱 루트만 주면 인트로·로그인 게이트에서 막힌다 · 형식 = 【레포 바인딩】 · 라이브 없는 레포 = 결과물 실경로·실행법).
- '완료' = main 머지 + `git show origin/main:<파일>` read-back 후에만 선언 · 완료 시점 예고 금지(실측 후 보고만).
- 머지 충돌 시 `-X ours`는 코드 한정 — 원장류 append-only 문서(지시 원장·작업 이력)는 타 세션 추가분을 덮지 말고 양쪽을 시각순 보존.

[12] 제안 파일명 = `YYYYMMDD_HHMMSS_작업명_vN`(N = 버전 횟수) · 시각은 전부 KST(러너는 UTC라 `TZ='Asia/Seoul'` 강제 · 코드에 시각을 심을 때도 naive `new Date()`·`utcnow()` 금지) · 단 이미 명명이 선언된 산출물(【레포 바인딩】·파이프라인 계약)은 그쪽이 우선.

[13] '피곤하니까 자라'·'여기까지 하자' 같은 작업 중단 권유는 절대 하지 않는다(단 규칙이 정한 봉투 수렴·세션 인계 고지는 중단 권유가 아니다).

[14] 품질을 유지하면서 더 좋은 길이 보이면 적극 제안한다.
<!-- SYNC-COMMON-END -->

## 🔒 절대명령 #1 (레포 제1 · 디자인 축) — 디자인 = 루트 토큰 「계승」 또는 「갱신」

> 위상 선언(얇게). 디자인 *내용* 축에서 이 원칙이 최우선. 실체 정본·값·절차 = 아래 §🎨 디자인 계약 + `docs/절대명령2_정본인덱스.md`(위치 SSOT · 충돌 시 그쪽이 이김). 완전판 스펙·드리프트 마스터 = `docs/절대명령1_포터블.md` §1(이 블록과 충돌 시 그쪽이 정본). 이 절은 라우팅·기틀 교차검증([9])을 우회하지 않는다.

값 SSOT = `viewer/index.html` `:root`. 모든 디자인 *값*(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나:
- ① 계승 (디폴트·안 물음): 정확 토큰을 `var(--token)`으로 → 없으면 가장 가까운 토큰 자동 계승.
- ② 갱신 (근접조차 없을 때만): 운영자에게 ㉮갱신할지 ㉯무엇으로 계승할지(근사 후보 제시) 물어 승인 후. 구조 토큰 신설 = 기틀 = 교차검증([9]).
- 컴포넌트는 값 아님 → `docs/CII_컴포넌트계승인덱스.md` 정본 셀렉터 계승(별개 축).

기계강제 = `design-tokens.lock`(승인 원장). `:root` 새 토큰이 락에 없으면 `shared/check_refs.py` rc=1(커밋 차단). 승인 = 락 등재(PR diff 리뷰). ⚠️ 초록 게이트 ≠ 전면 준수. 예외(raw 허용·물음 없음): 기하 계산·키프레임 안무 %·광학 1px 보정·OS 강제(safe-area)·로컬 z-index·토큰 없는 축(box-shadow 등).

## 🎨 디자인 계약 (규칙 전문 · 계승이 디폴트)

> **이 절(§🎨) = 디자인 규칙 위상 권위(충돌 시 최종 정본).** 운영 거울(파생·역전 금지) = `AGENTS.md` §🎨(Claude 외 도구 공통) + SessionStart 주입 `.claude/hooks/design_digest.py` · 위치 인덱스 = `docs/절대명령2_정본인덱스.md` · 값 = `viewer/index.html` `:root` · 컴포넌트 = `docs/CII_컴포넌트계승인덱스.md` · 방식론 = `docs/디자인방식론_YETA.md` · 시각 정본 = `구성도/`. **본문 복제 금지(= 드리프트) — 이 절은 위상만, 전문 본문은 거울에.** UI/UX 작업 = 위 정본을 무조건 계승·준수(위 [4]와 동일 축).

[15] 웹앱 구조 (레포 고유 — 실측. 폴더 = 역할 1줄씩, 실존 경로만)
- `viewer/` = 웹앱 본체(PWA). `viewer/index.html`=값 SSOT(`:root` 디자인 토큰)+메인 UI · `viewer/call.js`=음성·전화 모듈 · `viewer/nm-svg.js`=아이콘 SSOT · `viewer/tokens.css`=구조토큰 거울(빌드 산출·직접수정 금지) · `viewer/sw.js`·`viewer/manifest.json`=PWA · `viewer/pg/`=플레이그라운드 서빙 경로(시안 = 라이브 URL 제공 · [5]) · `viewer/characters/`·`viewer/assets/`·`viewer/fonts/`.
- `functions/` = 서버리스 백엔드(Cloudflare Functions). `functions/api/yeta.js`=제타 API.
- `shared/` = 파이썬·셸 도구. `shared/check_refs.py`=커밋 전 전체 게이트 · **`shared/models.json`=모델 ID·표시명 정본(SSOT) + `shared/apply_models.py`=승격 일괄 치환기**{`python3 shared/apply_models.py <티어|벤더키> <새ID> ["<표시명>"] ["<한글명>"]` 1발로 전 호출처 치환 → 게이트 짝 = check_refs `check_model_ids`(구세대 잔존·미등재 ID + 벤더 계열 드리프트 차단) · 왜 = gpt-image-2 6파일·gemini 3파일이 각자 하드코딩이던 축 · nomute-editor 260725 확립분 이식} · `shared/build_design_mirror.py`=디자인 거울 빌더 · `shared/account_failover.py`·`shared/claude_meter.sh` 등.
- `docs/` = 문서 정본. `docs/절대명령1_포터블.md` · `docs/절대명령2_정본인덱스.md`(위치 SSOT) · `docs/CII_컴포넌트계승인덱스.md`(컴포넌트) · `docs/브랜드_팔레트_확립본.html` · `docs/디자인방식론_YETA.md` · `docs/reports/`.
- `구성도/` = 디자인 시각 정본(블루프린트). `구성도/00_가이드북_버튼인터랙션.html` · `구성도/base.css`(거울·직접수정 금지) · `구성도/픽토그램.html`·`구성도/텍스트 위계.html` 등 확립본 + `구성도/노뮤트_웹UI_공통규약.md`.
- `apps/yeta/` = 캐릭터챗 콘텐츠·설정. `apps/yeta/characters/`(캐릭터 카드)·`apps/yeta/worlds.json`·`apps/yeta/places.json`·`apps/yeta/users.json`·`apps/yeta/policy.json`·세계관 문서.
- `.claude/` = Claude Code 훅·스킬(`.claude/hooks/`=SessionStart 디자인 주입·저장 게이트 · `.claude/skills/`=yeta-design) · `.githooks/`=pre-commit(check_refs 강제) · `design-tokens.lock`=`:root` 토큰 승인 원장 · `AGENTS.md`=Claude 외 도구 공통 계약.
- 지시 원장 = `docs/요구사항_큐.md`(신설) · 작업 이력 = `docs/작업이력.md` · 백업 = `_versions/{yymmdd_HHmm}_{라벨}/`.

[16]【레포 바인딩】 공통 골격이 참조하는 이 레포의 정본 값 (레포 고유 · 전파 제외)
- [E1] 서빙 엔트리(실브라우저 현물 확인) = viewer/index.html · 루트 로컬 서빙(헤드리스 크롬) · ⚠ 진입 시 잠금화면(unlock 경로 확인 필요).
- [E13] QA경로 = viewer/index.html ?qa=1#char|list|map|settings · 대표데이터 = api/yeta(백엔드 · 로컬 렌더 = 빈/시그니처 폴백).
- [4] 디자인 진입점 = `viewer/index.html` `:root`(값 SSOT) · 위치 인덱스 `docs/절대명령2_정본인덱스.md` · 컴포넌트 `docs/CII_컴포넌트계승인덱스.md` · 방식론 `docs/디자인방식론_YETA.md` — 규칙 전문·위상 = 위 §🔒/§🎨(디자인 정본 레포 한정 절 · 유지).
- [4] 팔레트 예외 = 선언분 없음(생기면 여기 등재).
- [4] 계정 축 정본 = 미배선 — 알림·확인 UI 신설 시 설계 시점에 이 축 배선(구현 경로는 그때 여기 등재).
- [5] 플레이그라운드 서빙 = `viewer/pg/`(라이브 URL 제공 정본).
- [6] 지시 원장 = `docs/요구사항_큐.md`(보고 골격 = 최우선 2의 6단 · 예시 블록 폐지).
- [9] 평의회 대상 예 = `CLAUDE.md` · `viewer/index.html`(값 SSOT `:root`) · `docs/절대명령2_정본인덱스.md` · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드 · 상비 게이트 = `shared/check_refs.py`(+`.githooks` pre-commit).
- [11] 딥링크 = 앱 = `/?yeta=1&t=<캐릭터id>`(언락 후 그 방 직행 · 미언락이면 언락 완료에 큐잉) · 시안·문서 = `/pg/<파일>.html`(잠금 없는 정적경로) · 앱 루트(`/`)만 덜렁 주기 금지.
- 백업 = `_versions/{yymmdd_HHmm}_{라벨}/`.

※ 이식 노트: 마커 구간(SYNC-COMMON-START~END) = 전 레포 공통 골격 — 정본 편집 = muteno/nomute-editor CLAUDE.md에서만(여기서 고치면 다음 전파 PR이 덮는다 · 전파 장치 = nomute-editor `.github/workflows/claude-sync.yml` → 이 레포에 교체 PR 도착). 마커 밖 = 레포 고유(전파 제외).
