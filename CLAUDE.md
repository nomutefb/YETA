<!-- SYNC-COMMON-START -->
0. [위계] 마커 안 = 전 레포 공통, 정본 = nomute-editor — 타 레포에서 고치면 다음 전파가 덮는다. 마커 밖 레포 고유 절이 공통보다 우선하고, 고유 절의 '절대 금지'는 승인받아도 하지 않는다. 이 블록을 한 수·개선으로 스스로 늘리지 않는다.
1. [말투] 한국어, 친근한 반말로 답한다(사용자 대화에만 — 산출물·커밋·PR은 그 레포 규격). 솔직 — 실패면 실패라고 먼저 말하고 포장·은폐 금지. 친절 — 결론부터 쉬운 말로, 막히면 대안을 준다.
2. [의도] 사용자의 텍스트를 보고 의도와 맥락을 파악해라. 육하원칙에 따라 실행해야 할 내용을 정의해라. 가장 짧은 시간 내에 리스크 없이 높은 완성도의 결과물을 도출한다.
3. [검색] 100% 확실한게 아니면 검색한다 — 레포 안이면 파일을 열고, 밖이면 웹을 찾는다. 추측(할루시네이션)으로 답하지 않는다. 끝내 확인 못 한 값엔 「미확인」을 붙인다. 확인은 읽기 전용으로만 한다.
4. [속도] 응답은 가능한 빠르게 대답한다. 시간이 필요할 때는 사용자에게 양해를 구한다.
5. [디자인기틀] ui나 ux를 수정할 때는 기존 디자인 기틀(정본)을 먼저 열어 참조하고 그 틀에 따라 제작한다. 정본 위치 = 마커 밖 레포 고유 절이 지정한 파일 — 지정이 없으면 추측하지 말고 물어본다. 임의로 만드는 디자인은 실패한 디자인이다.
6. [전후] ui나 ux를 수정한 후에는 전후(before→after)를 비교해 달라진 점을 확실히 알려준다. 이미지 또는 움직임이 있을 땐 html로 전후를 알 수 있게 보여줘야 한다.(의무)
7. [픽셀실측] 픽토그램·버튼·아이콘·텍스트 등 정렬이 있는 디자인을 신설·이동·정렬할 때마다 **언제나** 실렌더(헤드리스 브라우저)에서 픽셀 단위로 검증한다 — 눈대중·CSS 산식 추정으로 "정렬됨" 선언 금지(의무 · 운영자 260716/260731). 두 축을 **둘 다** 본다(둘 다 봐야 나온다): ① 기하 = 픽토 중심이 컨테이너 **4분할 중앙점**과 일치 + 정렬 축 형제들과 같은 선(getBoundingClientRect 중심 비교) ② 광학(잉크) = 실제 그려진 픽셀(잉크)의 중심 — 비대칭 도형·텍스트는 기하가 맞아도 편심돼 보인다(잉크 프로브 = 렌더 캡처 픽셀 스캔). 대상은 픽토그램만이 아니다: ③ 도형 안 글자(라벨·값)도 같은 4분할 세로 중앙 검증 + 같은 급 도형끼리 글자 크기 동일 ④ 동급 요소는 동급끼리 정렬(같은 레이어 레벨 — 섹션 헤더는 헤더끼리 · 카드 라벨은 라벨끼리 같은 수직·수평선과 규격 유지) ⑤ 상태 일관성 = 같은 컴포넌트가 다른 표면에서 active·hover·done 등 상태 스타일을 이미 갖고 있으면 동일 컴포넌트엔 동일 상태를 계승 적용한다(표면마다 제각각 창작 금지). 판정 = Δ≤0.5px(목표 0.00 · 홀짝 패리티로 정수 스냅) · 중심 십자선 캡처를 [전후] 증빙에 첨부한다.
8. [플레이그라운드] ui나 ux를 중규모 이상으로 수정해야 된다고 판단될 때는, 약 50개 이상의 인덱싱(선택자)로 구성된 플레이그라운드로 선택할 수 있게 제안한다. 단 기존에 정해진 uiux를 플레이그라운드에 표기해서 디자인 기틀을 벗어나지 않게한다.
9. [평의회] 작업량이 대규모라고 판단 되거나, 기존 리포지토리 기틀을 변경해야 하는 경우, opus 5.0 -effort max 8명을 소집, 각기다른 관점으로 적대적 검증을 완료해라. 좌석은 READ-ONLY(수정·머지 금지) · 소집 전 1줄 고지 · 단순·국소 변경엔 발동 금지.
10. [머지] 파일을 바꾼 작업이 끝나면 보고 전에 우선 머지부터 끝낸다 — origin/main 대조(남의 변경 안 되돌리기) → push → PR(Draft면 ready 전환) → 게이트·체크 초록 확인 → merge → origin/main read-back(실반영 확인). 머지를 멈추고 묻는 경우 = 충돌 · 게이트 실패/미확인 · 비가역(삭제·과금·라이브 배포) · 평의회 대상 · 레포가 금지한 파일. 자동 전파 PR(claude-sync)과 남의 PR은 손대지 않는다. 거부돼도 우회 금지(main 직접 push·force).
11. [보고] 결과 보고 = 딱 3줄, 초등학생도 이해하게, 누락 없이: ① 요청한 게 어떻게 됐나(성공/실패/보류 — '성공'은 read-back 후에만) ② 그래서 뭐가 달라졌나(전후 핵심) ③ 머지 됐나 + 다음 액션(없으면 '없음'). 3줄 밖엔 전후 자료(html·스샷)·링크·한 수 1줄만 붙인다.
12. [한수] 더 좋게 만들 추가 아이디어가 있으면 딱 1개만 제시한다 — 제시만, 실행은 사용자가 별도로 시킬 때만. [머지]의 '다음 작업'에 스스로 넣지 않는다(자가 채택·연쇄 금지). 관련 문제점도 살펴보고, 있으면 가장 큰 것 1개를 같이 알린다.
※ 조항 인용은 번호 대신 [키]로 한다 — 번호는 개정 때 바뀐다.
<!-- SYNC-COMMON-END -->

## 🔒 절대명령 #1 — 디자인 값은 「계승」 또는 「갱신」, 둘 뿐

## 🎨 디자인 값 규칙 (B1의 이 레포 구체화 · 디자인 규칙 위상 권위)

값(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나뿐이다.
- **① 계승 = 디폴트, 안 물음** — `var(--token)`. 정확한 토큰이 없어도 **가장 가까운 걸 자동 계승**(이 판단도 안 물음).
- **② 갱신 = 근접 토큰조차 없을 때만** — 「신설할지 / 어떤 근접 토큰을 계승할지」 두 선택지로 묻고 승인 후. 토큰 신설 = `:root` 구조 변경 = 평의회(D3).
- 승인분은 **즉시 편입**: `:root` → `build_design_mirror.py build` → CII 행 → **`design-tokens.lock` 갱신**. 락 없이 코드에만 박으면 `check_refs.py` rc=1.
- **raw 허용**(안 물음) = 기하 계산·키프레임 %·광학 1px·safe-area·로컬 z-index·토큰 없는 축(box-shadow). ⚠ 초록 게이트 ≠ 전면 준수.
- 컴포넌트는 값이 아니다 → `docs/CII_컴포넌트계승인덱스.md` 셀렉터 계승(별개 축).

## 【바인딩】 YETA

- **서빙**(A5) = `viewer/index.html` · ⚠ 진입 시 잠금화면(unlock 경로 확인)
- **QA**(C2) = `viewer/index.html?qa=1#char|list|map|settings` · 대표데이터 = `api/yeta`(로컬은 빈/시그니처 폴백)
- **디자인**(B1) = `viewer/index.html` `:root`(값 SSOT) → `docs/절대명령2_정본인덱스.md`(위치 · **충돌 시 그쪽이 이김**) + `docs/CII_컴포넌트계승인덱스.md` + `docs/디자인방식론_YETA.md` · 완전판 `docs/절대명령1_포터블.md` §1 · 시각 정본 `구성도/`
- **거울 복제 금지** = `AGENTS.md` §🎨와 `.claude/hooks/design_digest.py` 주입문은 파생 거울 — 본문을 복제·역전하지 않는다
- **팔레트 예외** = 없음 · **계정 축**(D3-1) = 미배선
- **플레이그라운드**(B3) = `viewer/pg/`(라이브 URL 제공)
- **원장**(C1) = `docs/요구사항_큐.md` · 이력 `docs/작업이력.md` · 백업 `_versions/{yymmdd_HHmm}_{라벨}/`
- **게이트**(C4) = `python3 shared/check_refs.py` + `.githooks` pre-commit · ⚠ **clone·기기마다 1회 `git config core.hooksPath .githooks`**(미설정이면 조용히 미실행)
- **평의회 대상**(D3) = `CLAUDE.md` · `viewer/index.html` `:root` · `docs/절대명령2_정본인덱스.md` · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드
- **손대지 마라**(D2-1) = `viewer/tokens.css` · `구성도/base.css`(빌드 거울)
- **모델 ID** = `shared/models.json` · 승격 = `shared/apply_models.py`
- **딥링크**(C3) = 앱 `/?yeta=1&t=<캐릭터id>`(미언락이면 언락 완료에 큐잉) · 시안·문서 `/pg/<파일>.html` · **앱 루트만 덜렁 주기 금지**
